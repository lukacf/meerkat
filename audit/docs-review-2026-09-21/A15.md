# A15: Changelog, historical root ledgers, and prior documentation audits

This ledger preserves the baseline evidence and independent disposition. See [the audit index](README.md) for scope, commits, and publication boundaries.

## A15-001: The historical remediation checklist's nine source-report links use an absolute private worktree path

**Candidate severity:** low. **Independent verdict:** confirm.

Readers following Source reports cannot navigate to the evidence from a normal repository rendering. The report's age does not justify broken navigation to files that remain checked in.

### Baseline documentation evidence

- [audit/docs-review-2026-04-21/unified-remediation-checklist.md:11-19](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/audit/docs-review-2026-04-21/unified-remediation-checklist.md?plain=1#L11-L19)

```text
[README.md](/Users/luka/.codex/worktrees/fc6a/meerkat/audit/docs-review-2026-04-21/README.md)
```

### Implementation proof

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import re
p=Path('audit/docs-review-2026-04-21/unified-remediation-checklist.md')
links=re.findall(r'\[([^\]]+)\]\((/Users/[^)]+)\)',p.read_text())
print('nonportable_source_links',len(links),'matching_repo_siblings',sum((p.parent/label).is_file() for label,_ in links))
PY
```

  nonportable_source_links 9 matching_repo_siblings 9

  These are actual Markdown destinations, not quoted historical evidence paths. A leading /Users/... resolves outside the repository-relative document tree on GitHub or another checkout. All nine intended reports are present beside the checklist, so portable targets already exist.

### Independent challenge

These nine destinations are active Markdown links under Source reports, not stale paths quoted as evidence of an earlier defect. Their leading /Users/... is an author-machine path, not repository-relative navigation. Each label names an existing tracked sibling report. Historical framing does not make those clickable destinations portable; readers cannot follow the checklist to its retained evidence on GitHub or in another checkout.

- [audit/docs-review-2026-04-21/unified-remediation-checklist.md:9-19](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/audit/docs-review-2026-04-21/unified-remediation-checklist.md?plain=1#L9-L19)

```text
[README.md](/Users/luka/.codex/worktrees/fc6a/meerkat/audit/docs-review-2026-04-21/README.md)
```

  All nine lines in this Source reports section use the same absolute worktree prefix in actual link destinations.

- Read-only reproduction:

```text
python3 -B - <<'PY'
import pathlib, re, subprocess
p = pathlib.Path('audit/docs-review-2026-04-21/unified-remediation-checklist.md')
for n, line in enumerate(p.read_text().splitlines(), 1):
    m = re.fullmatch(r'- \[([^\]]+)\]\((/Users/[^)]+)\)', line)
    if m:
        sibling = p.parent / m.group(1)
        tracked = subprocess.run(['git', 'ls-files', '--error-unmatch', '--', str(sibling)], capture_output=True).returncode == 0
        print(n, m.group(1), sibling.is_file(), tracked)
PY
```

  The nine links on lines 11 through 19 each have an existing, tracked sibling matching the link label: README.md and agent-01 through agent-08 reports. All nine existence/tracking checks are true.

  The files needed for a relative-link repair already exist in the repository. This check did not inspect or follow the old /Users/... destinations.

**Required correction:** In the nine Source reports links only, replace the prefix /Users/luka/.codex/worktrees/fc6a/meerkat/audit/docs-review-2026-04-21/ with an empty prefix so each destination is its sibling filename. Preserve labels, historical report text, quoted evidence paths, and checkbox states.

### Correction

Replaced only the nine Source reports destinations with their existing tracked sibling filenames. Labels, historical report text, recorded paths elsewhere, and checkbox states are unchanged.

- [audit/docs-review-2026-04-21/unified-remediation-checklist.md:11-19](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/audit/docs-review-2026-04-21/unified-remediation-checklist.md?plain=1#L11-L19)

```text
- [README.md](README.md)
- [agent-01-getting-started-and-nav.md](agent-01-getting-started-and-nav.md)
- [agent-02-concepts-and-configuration.md](agent-02-concepts-and-configuration.md)
- [agent-03-guides-auth-providers-models.md](agent-03-guides-auth-providers-models.md)
- [agent-04-guides-tools-hooks-skills-memory.md](agent-04-guides-tools-hooks-skills-memory.md)
- [agent-05-guides-mobs-comms-realtime-scheduling.md](agent-05-guides-mobs-comms-realtime-scheduling.md)
- [agent-06-surfaces-cli-api-sdk-rust.md](agent-06-surfaces-cli-api-sdk-rust.md)
- [agent-07-reference-architecture-and-contracts.md](agent-07-reference-architecture-and-contracts.md)
- [agent-08-meerkat-skill-files.md](agent-08-meerkat-skill-files.md)
```

### Final independent review

**PASS:** All nine Source reports links are real parsed Markdown links to existing tracked siblings. I compared the complete file with baseline 56208b9e6cee078f52c43af8f6b36660bc93eeb4: removing only the prescribed private-worktree prefix from those nine destinations reproduces the current file exactly. Labels, historical findings, evidence paths elsewhere, and checkbox states are preserved.

- [audit/docs-review-2026-04-21/unified-remediation-checklist.md:11-19](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/audit/docs-review-2026-04-21/unified-remediation-checklist.md?plain=1#L11-L19)

```text
- [README.md](README.md)
- [agent-01-getting-started-and-nav.md](agent-01-getting-started-and-nav.md)
- [agent-02-concepts-and-configuration.md](agent-02-concepts-and-configuration.md)
- [agent-03-guides-auth-providers-models.md](agent-03-guides-auth-providers-models.md)
- [agent-04-guides-tools-hooks-skills-memory.md](agent-04-guides-tools-hooks-skills-memory.md)
- [agent-05-guides-mobs-comms-realtime-scheduling.md](agent-05-guides-mobs-comms-realtime-scheduling.md)
- [agent-06-surfaces-cli-api-sdk-rust.md](agent-06-surfaces-cli-api-sdk-rust.md)
- [agent-07-reference-architecture-and-contracts.md](agent-07-reference-architecture-and-contracts.md)
- [agent-08-meerkat-skill-files.md](agent-08-meerkat-skill-files.md)
```

  Every listed occurrence is corrected, not merely the first example.

- Read-only reproduction:

```text
git --no-pager diff 56208b9e6cee078f52c43af8f6b36660bc93eeb4 -- audit/docs-review-2026-04-21/unified-remediation-checklist.md
```

  The only differences are the nine href replacements at lines 11-19. Independent full-document equality assertions passed after precisely those replacements.

  Historical evidence and decisions are not rewritten by the navigation repair.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
from markdown_it import MarkdownIt
import re, subprocess
p = Path('audit/docs-review-2026-04-21/unified-remediation-checklist.md')
tracked = set(subprocess.check_output(['git', 'ls-files', '-z'], text=True).split('\0'))
tokens = MarkdownIt('gfm-like', {'linkify': False}).parse(p.read_text())
hrefs = [c.attrGet('href') for t in tokens for c in t.children or [] if c.type == 'link_open']
for line in p.read_text().splitlines()[10:19]:
    label, target = re.fullmatch(r'- \[([^\]]+)\]\(([^)]+)\)', line).groups()
    assert target == label and hrefs.count(target) == 1
    assert (p.parent / target).is_file() and (p.parent / target).as_posix() in tracked
print('9 parsed links; 9 existing tracked siblings')
PY
```

  9 parsed links; 9 existing tracked siblings

  Checks actual Markdown parsing and repository targets without accessing the old private worktree.

## A15-002: Three early changelog release links point to nonexistent tags or releases

**Candidate severity:** low. **Independent verdict:** confirm.

The oldest version headings remain clickable but lead to unavailable GitHub targets. This concerns current navigation through historical notes, not whether the historical releases or their listed changes happened.

### Baseline documentation evidence

- [CHANGELOG.md:11086-11088](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/CHANGELOG.md?plain=1#L11086-L11088)

```text
[0.3.0]: https://github.com/lukacf/meerkat/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/lukacf/meerkat/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/lukacf/meerkat/releases/tag/v0.1.0
```

### Implementation proof

- Read-only reproduction:

```text
gh api repos/lukacf/meerkat/compare/v0.2.0...v0.3.0; gh api repos/lukacf/meerkat/compare/v0.1.0...v0.2.0; gh api repos/lukacf/meerkat/releases/tags/v0.1.0
```

  Each requested comparison/release returned GitHub HTTP 404 Not Found.

  Direct remote checks establish that the published changelog destinations cannot be resolved, not merely that the local checkout omitted old tags.

- Read-only reproduction:

```text
gh api repos/lukacf/meerkat/git/refs/tags/v0.1.0; gh api repos/lukacf/meerkat/git/refs/tags/v0.2.0; gh api repos/lukacf/meerkat/git/refs/tags/v0.3.0 --jq '{ref: .ref, sha: .object.sha}'
```

  v0.1.0 and v0.2.0 returned HTTP 404. The positive control returned {"ref":"refs/tags/v0.3.0","sha":"3347d984953dceabad5e3b4b5ca87ab73e337641"}.

  Repository access and the later release ref work. The absent early refs specifically break both comparison URLs, including the otherwise existing 0.3.0 release heading.

### Independent challenge

Independently repeated the remote checks rather than relying on missing local tags. Both comparisons and the v0.1.0 release endpoint return 404; exact v0.1.0 and v0.2.0 tag-ref lookups also return 404. A v0.3.0 tag/commit positive control succeeds and its tagged-tree URL returns HTTP 200. These are broken navigation destinations today, not evidence that the historical releases or their contents never existed. The v0.3.0 release endpoint also returns 404, so that is not an acceptable replacement.

- [CHANGELOG.md:11086-11088](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/CHANGELOG.md?plain=1#L11086-L11088)

```text
[0.3.0]: https://github.com/lukacf/meerkat/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/lukacf/meerkat/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/lukacf/meerkat/releases/tag/v0.1.0
```

  These reference definitions supply the clickable version headings at lines 10722, 10789, and 10953.

- Read-only reproduction:

```text
gh api repos/lukacf/meerkat/compare/v0.2.0...v0.3.0; gh api repos/lukacf/meerkat/compare/v0.1.0...v0.2.0; gh api repos/lukacf/meerkat/releases/tags/v0.1.0; gh api repos/lukacf/meerkat/git/refs/tags/v0.1.0; gh api repos/lukacf/meerkat/git/refs/tags/v0.2.0; gh api repos/lukacf/meerkat/releases/tags/v0.3.0
```

  Each endpoint returned exit code 1, JSON status 404 / Not Found, and gh: Not Found (HTTP 404).

  Checks are specific to the unavailable destinations; they do not establish the absence of every possible archive.

- Read-only reproduction:

```text
gh api repos/lukacf/meerkat/commits/v0.3.0 --jq '{sha: .sha, html_url: .html_url}' && curl --location --head --max-time 30 --silent --show-error --output /dev/null --write-out 'tagged_tree_http_status=%{http_code}\n' https://github.com/lukacf/meerkat/tree/v0.3.0
```

  {"html_url":"https://github.com/lukacf/meerkat/commit/e238ebc0dd7f3f24c10cf767ce6fa6a96fc0a0b8","sha":"e238ebc0dd7f3f24c10cf767ce6fa6a96fc0a0b8"}
  tagged_tree_http_status=200

  Repository access works, and the existing tagged tree is a verified replacement for the broken 0.3.0 comparison.

**Required correction:** Change the [0.3.0] reference destination to https://github.com/lukacf/meerkat/tree/v0.3.0. Remove the dead [0.2.0] and [0.1.0] reference definitions, leaving those headings unlinked while retaining their literal ## [VERSION] - DATE spelling (the changelog parser expects bracketed version headings). Add a brief archival navigation note that their original tag/release destinations are unavailable, without claiming the releases never happened or that no archive exists anywhere. Preserve release dates and all historical release prose; create no tags.

### Correction

Changed the 0.3.0 reference to its verified tagged tree, removed the dead 0.2.0 and 0.1.0 reference definitions, and added a brief archival-navigation note. Retained all literal bracketed version headings, dates, and historical release prose.

- [CHANGELOG.md:11097-11097](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L11097)

```text
[0.3.0]: https://github.com/lukacf/meerkat/tree/v0.3.0
```

- [CHANGELOG.md:10797-10800](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L10797-L10800)

```text
## [0.2.0] - 2026-02-12

> Archival navigation: the original tag/release destinations for 0.1.0 and 0.2.0
> are unavailable; their historical notes are retained with unlinked headings.
```

- [CHANGELOG.md:10964-10964](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L10964)

```text
## [0.1.0] - 2026-01-15
```

### Final independent review

**PASS:** The surviving 0.3.0 reference now targets its independently verified tagged tree. The dead 0.2.0 and 0.1.0 reference definitions are removed, while their original bracketed headings and dates remain unchanged and render without links. The archival note says only that the original destinations are unavailable; it does not deny the releases or invent archival history. All 132 headings/version labels/date suffixes remain identical under the repository's actual changelog parser. The CHANGELOG diff contains only this correction and the separately approved A15-003/A15-004 changes.

- [CHANGELOG.md:11097-11097](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L11097)

```text
[0.3.0]: https://github.com/lukacf/meerkat/tree/v0.3.0
```

- [CHANGELOG.md:10797-10800](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L10797-L10800)

```text
## [0.2.0] - 2026-02-12

> Archival navigation: the original tag/release destinations for 0.1.0 and 0.2.0
> are unavailable; their historical notes are retained with unlinked headings.
```

- [CHANGELOG.md:10964-10964](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L10964)

```text
## [0.1.0] - 2026-01-15
```

- [scripts/check_semver_breaks.py:65-65](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/check_semver_breaks.py#L65)

```text
CHANGELOG_SECTION_RE = re.compile(r"^## \[([^\]]+)\](.*)$")
```

  The preserved bracketed source spelling is the executable parser contract, independent of whether Markdown renders a hyperlink.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
from markdown_it import MarkdownIt
import runpy, subprocess
base = subprocess.check_output(['git', 'show', '56208b9e6cee078f52c43af8f6b36660bc93eeb4:CHANGELOG.md'], text=True)
current = Path('CHANGELOG.md').read_text()
parse = runpy.run_path('scripts/check_semver_breaks.py')['parse_changelog']
assert [(s.heading, s.label, s.suffix) for s in parse(base)] == [(s.heading, s.label, s.suffix) for s in parse(current)]
print('unchanged parser headings:', len(parse(current)))
tokens = MarkdownIt('gfm-like', {'linkify': False}).parse(current)
for version, expected in [('0.3.0', ['https://github.com/lukacf/meerkat/tree/v0.3.0']), ('0.2.0', []), ('0.1.0', [])]:
    headings = [tokens[i + 1] for i, t in enumerate(tokens) if t.type == 'heading_open' and t.tag == 'h2' and tokens[i + 1].content.startswith('[' + version + '] - ')]
    assert len(headings) == 1
    hrefs = [c.attrGet('href') for c in headings[0].children or [] if c.type == 'link_open']
    assert hrefs == expected
    print(version, hrefs)
PY
```

  unchanged parser headings: 132
  0.3.0 ['https://github.com/lukacf/meerkat/tree/v0.3.0']
  0.2.0 []
  0.1.0 []

  Full-document GFM parsing verifies the intended linked/unlinked behavior rather than assuming reference-definition deletion is sufficient.

- Read-only reproduction:

```text
gh api repos/lukacf/meerkat/compare/v0.2.0...v0.3.0; gh api repos/lukacf/meerkat/compare/v0.1.0...v0.2.0; gh api repos/lukacf/meerkat/releases/tags/v0.1.0; gh api repos/lukacf/meerkat/git/refs/tags/v0.1.0; gh api repos/lukacf/meerkat/git/refs/tags/v0.2.0; gh api repos/lukacf/meerkat/commits/v0.3.0 --jq '{sha: .sha, html_url: .html_url}'; curl --location --head --max-time 30 --silent --show-error --output /dev/null --write-out 'v0.3.0_tree_http_status=%{http_code}\n' https://github.com/lukacf/meerkat/tree/v0.3.0
```

  Both old comparisons, the v0.1.0 release, and the v0.1.0/v0.2.0 tag refs independently returned HTTP 404. The v0.3.0 commit lookup succeeded with e238ebc0dd7f3f24c10cf767ce6fa6a96fc0a0b8; the new tagged-tree destination returned HTTP 200.

  Fresh remote read-only checks distinguish absent old destinations from a repository access failure. No tags were created or changed.

## A15-003: The current changelog preamble overstates the semver gate as covering every published crate

**Candidate severity:** medium. **Independent verdict:** confirm.

This paragraph is explicitly current and claims to state only what the gate measures, but gives downstreams and release operators a stronger coverage guarantee than the implementation provides. Naming behavior-only limitations alone does not disclose entirely unmeasured published crate classes.

### Baseline documentation evidence

- [CHANGELOG.md:13-24](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/CHANGELOG.md?plain=1#L13-L24)

```text
the release unless (1) every crate the release publishes was reached, (2) every break
```

### Implementation proof

- [scripts/check_semver_breaks.py:538-556](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/check_semver_breaks.py#L538-L556)

```text
if lib.get("proc-macro") is True:
```

  classify_crates puts proc-macro crates and crates without a library target into exempt lists and continues without adding them to scope.checkable.

- [scripts/check_semver_breaks.py:614-619](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/check_semver_breaks.py#L614-L619)

```text
missing = [crate for crate in scope.checkable if crate not in covered]
```

  Missing-report enforcement applies only to checkable crates, not every package in the release list.

- [scripts/check_semver_breaks.py:749-756](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/check_semver_breaks.py#L749-L756)

```text
crate for crate in scope.checkable if crate not in set(args.first_publish_crate)
```

  First-publication crates can additionally be excluded because there is no baseline.

- [scripts/check_semver_breaks.py:782-792](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/check_semver_breaks.py#L782-L792)

```text
"crates, and neither does this gate: " + ", ".join(unchecked),
```

  The implementation explicitly tells operators that neither the underlying tool nor this gate checks the exempt published crates.

- Read-only reproduction:

```text
python3 -B - <<'PY'
import runpy,pathlib,subprocess
m=runpy.run_path('scripts/check_semver_breaks.py')
names=subprocess.check_output(['bash','scripts/release-rust-crates.sh'],text=True).splitlines()
scope=m['classify_crates'](pathlib.Path.cwd(),names)
print('published_crates',len(names))
print('required_report_coverage',len(scope.checkable))
print('exempt_proc_macros',scope.proc_macro)
print('exempt_no_lib_target',scope.no_lib_target)
PY
```

  published_crates 43
  required_report_coverage 40
  exempt_proc_macros ['meerkat-machine-derive', 'meerkat-machine-dsl']
  exempt_no_lib_target ['rkat']

  A read-only call of the gate's own classification logic demonstrates the coverage distinction on this exact repository without running Cargo, building, or installing anything.

### Independent challenge

This is an explicitly current description of what the gate measures. The gate intentionally classifies published packages without measuring proc-macro and no-library packages; classification is not the reached-by-report-or-equivalence condition described in the preamble. Its actual coverage check only requires scope.checkable, further excluding first-publication packages supplied by the driver. A pure, in-memory invocation confirms that coverage succeeds with only the 40 checkable libraries reached even though the release list contains 43 packages. The defect is the documentation guarantee, not those deliberate implementation exemptions.

- [CHANGELOG.md:13-24](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/CHANGELOG.md?plain=1#L13-L24)

```text
the release unless (1) every crate the release publishes was reached, (2) every break
```

  The current policy promises exhaustive reached coverage without the source's published-package exemptions.

- [scripts/check_semver_breaks.py:538-556](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/check_semver_breaks.py#L538-L556)

```text
if lib.get("proc-macro") is True:
            scope.proc_macro.append(name)
            continue
```

  Proc-macro packages bypass scope.checkable; the following branch likewise excludes packages with no library target.

- [scripts/check_semver_breaks.py:614-620](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/check_semver_breaks.py#L614-L620)

```text
missing = [crate for crate in scope.checkable if crate not in covered]
```

  Required coverage is restricted to checkable crates, rather than every published package.

- [scripts/check_semver_breaks.py:751-755](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/check_semver_breaks.py#L751-L755)

```text
crate for crate in scope.checkable if crate not in set(args.first_publish_crate)
```

  Explicit first-publication exemptions also reduce the measured scope.

- [scripts/check-semver-breaks.sh:237-239](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/check-semver-breaks.sh#L237-L239)

```text
release_args+=(--first-publish-crate "$crate")
```

  The production driver actually passes the first-publication exemptions; this is not merely an unused parser option.

- Read-only reproduction:

```text
python3 -B - <<'PY'
import pathlib, runpy, subprocess
m = runpy.run_path('scripts/check_semver_breaks.py')
names = subprocess.check_output(['bash', 'scripts/release-rust-crates.sh'], text=True).splitlines()
scope = m['classify_crates'](pathlib.Path.cwd(), names)
print('release_crates:', len(names))
print('checkable_crates:', len(scope.checkable))
print('proc_macro_exempt:', scope.proc_macro)
print('no_lib_target_exempt:', scope.no_lib_target)
print('missing_manifests:', scope.missing_manifest)
errors = m['check_measured'](m['ReportParse'](), 0, scope, unchanged=scope.checkable, tool_skipped=True)
print('coverage_errors_when_only_checkable_are_reached:', errors)
PY
```

  release_crates: 43
  checkable_crates: 40
  proc_macro_exempt: ['meerkat-machine-derive', 'meerkat-machine-dsl']
  no_lib_target_exempt: ['rkat']
  missing_manifests: []
  coverage_errors_when_only_checkable_are_reached: []

  This exercises only the pure coverage classifier/checker, not Cargo or the full release gate. It disproves the unqualified all-published-packages coverage assertion.

**Required correction:** In the current semver-gate preamble, qualify both the tool's input scope and obligation (1) as eligible published non-proc-macro library crates with a comparison baseline. Explicitly say proc-macro packages, packages without a library target, and first-publication packages without a baseline are not measured. Retain reached-by-baseline-equivalence, per-finding naming, release stamping, and behavior-only limitations. Do not change the gate or hand-maintain crate counts in the prose.

### Correction

Qualified both the tool's input scope and coverage obligation as eligible published non-proc-macro library crates with a comparison baseline. Explicitly disclosed proc-macro, no-library, and first-publication-without-baseline exemptions while retaining equivalence coverage, per-finding naming, release stamping, and behavior-only limitations.

- [CHANGELOG.md:14-20](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L14-L20)

```text
claim more than it measures: it runs cargo-semver-checks over eligible published
non-proc-macro library crates with a comparison baseline, checking those whose
source or declared dependency specs differ from the published baseline tag
(crates proven identical to the baseline are recorded as reached by equivalence,
since the tool reports on a crate's own items), and it fails the release unless
(1) every eligible published non-proc-macro library crate with a comparison
baseline was reached, (2) every break
```

- [CHANGELOG.md:28-29](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L28-L29)

```text
Proc-macro packages, packages without a library target, and first-publication
packages without a comparison baseline are not measured by the tool or this gate.
```

- [CHANGELOG.md:31-34](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L31-L34)

```text
Behaviour-only breaks - a public signature that keeps its shape and changes
what it does - are invisible to cargo-semver-checks and therefore invisible to
the gate. They are declared by hand and tagged inline, and nothing enforces
them.
```

### Final independent review

**PASS:** Both the tool-input description and coverage obligation now apply to eligible published non-proc-macro libraries with a comparison baseline. The preamble explicitly discloses proc-macro, no-library, and first-publication baseline exemptions, matching executable classification and production driver arguments. Baseline-equivalence coverage, individual-finding naming, release stamping, and behavior-only limitations are retained. No hand-maintained crate counts were added, and the gate implementation and canonical crate list remain unchanged.

- [CHANGELOG.md:14-20](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L14-L20)

```text
claim more than it measures: it runs cargo-semver-checks over eligible published
non-proc-macro library crates with a comparison baseline, checking those whose
source or declared dependency specs differ from the published baseline tag
(crates proven identical to the baseline are recorded as reached by equivalence,
since the tool reports on a crate's own items), and it fails the release unless
(1) every eligible published non-proc-macro library crate with a comparison
baseline was reached, (2) every break
```

- [CHANGELOG.md:28-34](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L28-L34)

```text
Proc-macro packages, packages without a library target, and first-publication
packages without a comparison baseline are not measured by the tool or this gate.

Behaviour-only breaks - a public signature that keeps its shape and changes
what it does - are invisible to cargo-semver-checks and therefore invisible to
the gate. They are declared by hand and tagged inline, and nothing enforces
them.
```

- [scripts/check_semver_breaks.py:538-556](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/check_semver_breaks.py#L538-L556)

```text
if lib.get("proc-macro") is True:
            scope.proc_macro.append(name)
            continue
```

  Proc-macro packages bypass checkable coverage; the following library-path branch independently excludes no-library packages.

- [scripts/check_semver_breaks.py:614-620](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/check_semver_breaks.py#L614-L620)

```text
missing = [crate for crate in scope.checkable if crate not in covered]
```

  The gate requires coverage of checkable packages, not the full publication list.

- [scripts/check_semver_breaks.py:749-755](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/check_semver_breaks.py#L749-L755)

```text
crate for crate in scope.checkable if crate not in set(args.first_publish_crate)
```

  First-publication exclusions further narrow comparison coverage.

- [scripts/check-semver-breaks.sh:237-239](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/check-semver-breaks.sh#L237-L239)

```text
release_args+=(--first-publish-crate "$crate")
```

  The production driver actually supplies these exemptions.

- Read-only reproduction:

```text
python3 -B - <<'PY'
import pathlib, runpy, subprocess
m = runpy.run_path('scripts/check_semver_breaks.py')
names = subprocess.check_output(['bash', 'scripts/release-rust-crates.sh'], text=True).splitlines()
scope = m['classify_crates'](pathlib.Path.cwd(), names)
print('published:', len(names), 'checkable:', len(scope.checkable))
print('proc_macro:', scope.proc_macro, 'no_lib_target:', scope.no_lib_target, 'missing_manifest:', scope.missing_manifest)
print('coverage_errors:', m['check_measured'](m['ReportParse'](), 0, scope, unchanged=scope.checkable, tool_skipped=True))
PY
```

  published: 43 checkable: 40
  proc_macro: ['meerkat-machine-derive', 'meerkat-machine-dsl'] no_lib_target: ['rkat'] missing_manifest: []
  coverage_errors: []

  Pure classification and coverage checks independently reproduce the documented distinction without Cargo, builds, installs, or release execution.

## A15-004: The 0.8.39 notes give conflicting shipped RPC stack defaults; the tagged source uses 8 MiB

**Candidate severity:** medium. **Independent verdict:** confirm.

Both statements are inside the same 0.8.39 release section. Readers investigating that release's resource usage or stack-overflow behavior receive incompatible defaults. The stale sentence describes an intermediate refactor state, not the released configuration.

### Baseline documentation evidence

- [CHANGELOG.md:461-473](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/CHANGELOG.md?plain=1#L461-L473)

```text
`rkat-rpc` worker stack is still 32 MiB pending a documented budget.
```

- [CHANGELOG.md:394-405](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/CHANGELOG.md?plain=1#L394-L405)

```text
from 32 MiB to 8 MiB; the other three move from Tokio's 2 MiB default to
```

### Implementation proof

- Read-only reproduction:

```text
git --no-pager show v0.8.39:meerkat-runtime/src/host_stack.rs | nl -ba | sed -n '31,35p;78,85p;117,119p;133,140p'
```

  At the v0.8.39 tag, line 32 defines HOST_WORKER_STACK_BUDGET as 8 * 1024 * 1024; default_budget uses that constant, from_lookup falls back to default_budget when no override is supplied, and build_runtime passes self.bytes to thread_stack_size.

  This is evidence from the release named in the notes, not an inference from a later change to HEAD.

- Read-only reproduction:

```text
git --no-pager show v0.8.39:meerkat-rpc/src/main.rs | nl -ba | sed -n '145,175p'
```

  Lines 152-155 resolve HostStackBudget from RKAT_WORKER_STACK_BYTES and the legacy RKAT_RPC_WORKER_STACK_BYTES; line 164 executes the RPC host via budget.run(BINARY_NAME, ...). There is no separate 32 MiB default on this startup path.

  The release-tag RPC executable actually uses the shared 8 MiB no-override budget. The earlier Changed bullet is consistent with source; the later 'still 32 MiB' statement is not.

### Independent challenge

Confirmed only as a version-scoped release-note ambiguity, not because current code differs from an old release. Both contradictory statements occur inside 0.8.39, and that exact tag's RPC main resolves the shared 8 MiB default. Blame also shows the 32 MiB sentence came from the earlier stack-refactor step and the 8 MiB Changed entry from a later pre-release commit. A reader determining 0.8.39's shipped memory budget gets incompatible answers unless the intermediate statement is scoped. Preserve the historical refactor measurements and original claim with an explicit release clarification.

- [CHANGELOG.md:394-405](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/CHANGELOG.md?plain=1#L394-L405)

```text
from 32 MiB to 8 MiB; the other three move from Tokio's 2 MiB default to
```

  The Changed subsection describes the final shared worker-stack budget for 0.8.39.

- [CHANGELOG.md:461-473](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/CHANGELOG.md?plain=1#L461-L473)

```text
`rkat-rpc` worker stack is still 32 MiB pending a documented budget.
```

  The Fixed subsection of the same version still presents the earlier intermediate default as shipped.

- Read-only reproduction:

```text
git --no-pager show v0.8.39:meerkat-runtime/src/host_stack.rs | nl -ba | sed -n '31,35p;78,85p;117,119p;133,140p'; git --no-pager show v0.8.39:meerkat-rpc/src/main.rs | nl -ba | sed -n '145,175p'
```

  At v0.8.39, host_stack.rs:32 is pub const HOST_WORKER_STACK_BUDGET: usize = 8 * 1024 * 1024;. Lines 81-85 put that constant into default_budget; line 118 uses it when no override is set; line 138 passes self.bytes to thread_stack_size. RPC main.rs:152-155 resolves that HostStackBudget using the shared and legacy environment variables, and line 164 calls budget.run.

  The release named by the changelog, not HEAD, shipped the 8 MiB no-override startup path.

- Read-only reproduction:

```text
git --no-pager blame -L 394,405 -- CHANGELOG.md; git --no-pager blame -L 469,473 -- CHANGELOG.md
```

  The 32 MiB sentence is from 09d6ab37b3 at 2026-09-15 18:44:56 +0200; the shared 8 MiB Changed entry is from 58f6d2ac54 at 2026-09-15 23:58:02 +0200.

  This supports distinguishing the earlier implementation step from the final release rather than erasing its historical account.

**Required correction:** Add a narrowly scoped clarification immediately after the stale final sentence of the 0.8.39 debug-stack Fixed bullet: the 32 MiB statement describes the intermediate refactor before the shared-budget change; 0.8.39 as released defaults to 8 MiB, with the overrides described in its Changed subsection. Preserve the earlier measurements, test budgets, release heading/date, and original intermediate claim; do not modernize unrelated old releases.

### Correction

Appended a release-scoped clarification immediately after the original 32 MiB sentence. It identifies that sentence as the intermediate refactor state and states the released 0.8.39 default of 8 MiB, subject to the existing Changed-subsection overrides.

- [CHANGELOG.md:478-481](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L478-L481)

```text
  `rkat-rpc` worker stack is still 32 MiB pending a documented budget.
  Release clarification: the 32 MiB statement above describes the intermediate
  refactor before the shared-budget change. As released, 0.8.39 defaults to
  8 MiB, subject to the overrides described in its Changed subsection.
```

### Final independent review

**PASS:** The clarification is immediately adjacent to the original 32 MiB sentence and explicitly confines it to the intermediate refactor. It accurately identifies the released 0.8.39 default as 8 MiB and retains the override explanation in that same release's Changed subsection. I inspected the v0.8.39 source, not current code. The historical sentence, measurements, 4 MiB debug and 1 MiB release canaries, release date, and unrelated release prose remain intact.

- [CHANGELOG.md:478-481](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L478-L481)

```text
  `rkat-rpc` worker stack is still 32 MiB pending a documented budget.
  Release clarification: the 32 MiB statement above describes the intermediate
  refactor before the shared-budget change. As released, 0.8.39 defaults to
  8 MiB, subject to the overrides described in its Changed subsection.
```

- [CHANGELOG.md:399-410](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/CHANGELOG.md?plain=1#L399-L410)

```text
`RKAT_WORKER_STACK_BYTES` overrides the budget for
  diagnosis on every host; `rkat-rpc` still honours `RKAT_RPC_WORKER_STACK_BYTES`
  when the shared variable is unset.
```

  The existing release-scoped override semantics are preserved rather than replaced with today's behavior.

- Read-only reproduction:

```text
git --no-pager show v0.8.39:meerkat-runtime/src/host_stack.rs | nl -ba | sed -n '25,40p;72,123p;129,145p'; git --no-pager show v0.8.39:meerkat-rpc/src/main.rs | nl -ba | sed -n '143,177p'
```

  The release tag defines HOST_WORKER_STACK_BUDGET = 8 * 1024 * 1024 at host_stack.rs:32. default_budget uses that constant at :81-85; from_lookup falls back to it at :118; build_runtime applies self.bytes at :138. RPC main.rs:152-155 resolves the shared variable before the legacy one and :164 runs through the resulting budget.

  Version-scoped source authority proves the final default and override order independently of the fixed changelog.

- Read-only reproduction:

```text
git --no-pager diff 56208b9e6cee078f52c43af8f6b36660bc93eeb4 -- CHANGELOG.md
```

  Within the 0.8.39 section, exactly three clarification lines are added after the original stale sentence. All other CHANGELOG changes are the separately approved preamble and early-release navigation corrections.

  No historical measurement, intermediate claim, or unrelated release is modernized.

## A15-005: Review-2's final count still says four findings are open although R9-R12 are marked fixed and verified

**Candidate severity:** low. **Independent verdict:** confirm.

The historical warning correctly disclaims a current work queue, but it does not reconcile two incompatible accounts of the same review cycle's completion. A reader cannot tell whether the footer or final checked states represent the recorded outcome.

### Baseline documentation evidence

- [LIVE_ADAPTER_REVIEW2_TODO.md:668-674](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L668-L674)

```text
- **Total actionable:** 12 (8 closed + 4 open)
```

- [LIVE_ADAPTER_REVIEW2_TODO.md:387-387](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L387)

```text
- [x] fix · [x] verify · **R9.** `Refresh` replays full canonical
```

- [LIVE_ADAPTER_REVIEW2_TODO.md:437-437](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L437)

```text
- [x] fix · [x] verify · **R10.** Refresh `session.update`
```

- [LIVE_ADAPTER_REVIEW2_TODO.md:463-463](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L463)

```text
- [x] fix · [x] verify · **R11.** `config/patch` model switch has no
```

- [LIVE_ADAPTER_REVIEW2_TODO.md:602-602](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L602)

```text
- [x] fix · [x] verify · **R12.** Local config rejections from the
```

### Implementation proof

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import re
p=Path('LIVE_ADAPTER_REVIEW2_TODO.md')
closed=re.findall(r'^- \[x\] fix · \[x\] verify · \*\*(R\d+)\.',p.read_text(),re.M)
print('review2_recorded_closed',closed)
print('review2_counts_line',p.read_text().splitlines()[-1])
PY
```

  review2_recorded_closed ['R1', 'R2', 'R3', 'R4', 'R5', 'R6', 'R7', 'R8', 'R9', 'R10', 'R11', 'R12']
  review2_counts_line - **Total actionable:** 12 (8 closed + 4 open)

  The contradiction is wholly within the stored historical ledger: its documented checkbox convention and all twelve item states disagree with its final summary. No assertion about current runtime correctness is needed.

### Independent challenge

Confirmed narrowly as an internally inconsistent aggregate, not as a current live-adapter work queue or runtime defect. The unqualified terminal Counts section says four items are open, while the explicit checkbox convention and every R1-R12 entry record fixed and independently verified. I checked the originating 593560d191 snapshot as well: it already contains all twelve checked entries and the 8-closed/4-open total, so this is not caused by comparing different modern and historical APIs. The concrete consequence is that the record's own summary miscounts its recorded item states. Preserve the original number as a labelled historical summary rather than silently rewriting it.

- [LIVE_ADAPTER_REVIEW2_TODO.md:21-26](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L21-L26)

```text
The first is the implementer; the second is an independent agent
confirming the fix actually addresses the finding
```

  The record itself defines what its two checked states mean.

- [LIVE_ADAPTER_REVIEW2_TODO.md:668-674](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L668-L674)

```text
- **Total actionable:** 12 (8 closed + 4 open)
```

  This is an unqualified aggregate at the end of Counts, not a quoted external review or a separately identified intake snapshot.

- Read-only reproduction:

```text
git --no-pager show 593560d191:LIVE_ADAPTER_REVIEW2_TODO.md | python3 -B -c 'import re,sys; t=sys.stdin.read(); print("At review-2 commit 593560d191, checked IDs:", re.findall(r"^- \[x\] fix · \[x\] verify · \*\*(R\d+)\.", t, re.M)); print("Counts:", [line for line in t.splitlines() if line.startswith("- **Total actionable:**")])'
```

  At review-2 commit 593560d191, checked IDs: ['R1', 'R2', 'R3', 'R4', 'R5', 'R6', 'R7', 'R8', 'R9', 'R10', 'R11', 'R12']
  Counts: ['- **Total actionable:** 12 (8 closed + 4 open)']

  The numerical discrepancy exists within the original stored record. The same twelve checked entries remain at current lines 36, 99, 137, 172, 203, 241, 281, 319, 387, 437, 463, and 602.

**Required correction:** In Counts only, label the retained '12 (8 closed + 4 open)' text as the original recorded summary and add a reconciliation note that the R1-R12 checkboxes in this document record 12 fixed and verified items, not four unchecked items. Explicitly keep this as a historical recorded-state count, not a new test certification. Preserve finding descriptions, fix/verification notes, historical checkbox states, and the archival banner.

### Correction

Labelled the retained 12 (8 closed + 4 open) count as the original recorded summary and added a Counts-only reconciliation of the twelve recorded fixed-and-verified checkbox states. Explicitly disclaimed a new test certification.

- [LIVE_ADAPTER_REVIEW2_TODO.md:674-678](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L674-L678)

```text
- **Total actionable:** 12 (8 closed + 4 open) — original recorded summary

Recorded-state reconciliation: the R1-R12 checkboxes in this document record
12 fixed and verified items, not four unchecked items. This is a count of the
historical recorded states, not a new test certification.
```

### Final independent review

**PASS:** The old 12 (8 closed + 4 open) number is preserved and explicitly labeled the original recorded summary. The new Counts-only paragraph reconciles it with the document's twelve fixed-and-verified checkboxes and expressly disclaims a new test certification. Independently counted R1-R12 in baseline and current text; both sets and their order are identical. The entire pre-Counts body, including the archival banner and all fix/verification notes, is byte-identical.

- [LIVE_ADAPTER_REVIEW2_TODO.md:674-678](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L674-L678)

```text
- **Total actionable:** 12 (8 closed + 4 open) — original recorded summary

Recorded-state reconciliation: the R1-R12 checkboxes in this document record
12 fixed and verified items, not four unchecked items. This is a count of the
historical recorded states, not a new test certification.
```

- [LIVE_ADAPTER_REVIEW2_TODO.md:3-6](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/LIVE_ADAPTER_REVIEW2_TODO.md?plain=1#L3-L6)

```text
> Historical PR #650 review ledger. Checkbox state below describes that
> implementation cycle, not current Meerkat. Use `docs/guides/realtime.mdx`
> and `docs/reference/runtime-architecture.mdx` for current live-channel
> contracts.
```

  The recorded-state correction does not promote this archived ledger to current runtime truth.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
import re, subprocess
p = 'LIVE_ADAPTER_REVIEW2_TODO.md'
old = subprocess.check_output(['git', 'show', '56208b9e6cee078f52c43af8f6b36660bc93eeb4:' + p], text=True)
new = Path(p).read_text()
pattern = r'^- \[x\] fix · \[x\] verify · \*\*(R\d+)\.'
assert re.findall(pattern, old, re.M) == re.findall(pattern, new, re.M) == ['R' + str(i) for i in range(1, 13)]
assert old.split('## Counts')[0] == new.split('## Counts')[0]
print('R1-R12 fixed/verified states identical; entire pre-Counts record unchanged')
PY
```

  R1-R12 fixed/verified states identical; entire pre-Counts record unchanged

  This checks historical recorded states, not present-day implementation correctness.

## A15-006: The SessionRuntime Phase G checklist remains entirely unchecked despite twelve recorded PASS verdicts

**Candidate severity:** low. **Independent verdict:** reject.

The document defines separate fix and verify checkboxes, but its Phase G intake list and final verifier summary give opposite completion signals. The file-wide historical banner does not identify the checklist as an intentionally frozen pre-fix snapshot.

### Baseline documentation evidence

- [SESSION_RUNTIME_R2_R5_TODO.md:601-614](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/SESSION_RUNTIME_R2_R5_TODO.md?plain=1#L601-L614)

```text
- [ ] fix · [ ] verify · **G1 (P1).** [`meerkat-live/src/host.rs:1568`]
```

- [SESSION_RUNTIME_R2_R5_TODO.md:713-728](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/SESSION_RUNTIME_R2_R5_TODO.md?plain=1#L713-L728)

```text
- [ ] fix · [ ] verify · **G12 (P3).** [`README.md:227`] and
```

- [SESSION_RUNTIME_R2_R5_TODO.md:1050-1053](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/SESSION_RUNTIME_R2_R5_TODO.md?plain=1#L1050-L1053)

```text
**Phase G summary.** G1 PASS / G2 PASS / G3 PASS / G4 PASS / G5 PASS /
G6 PASS / G7 PASS / G8 PASS / G9 PASS / G10 PASS / G11 PASS / G12 PASS
```

### Implementation proof

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import re
t=Path('SESSION_RUNTIME_R2_R5_TODO.md').read_text()
open_rows=re.findall(r'^- \[ \] fix · \[ \] verify · \*\*(G\d+) \(P\d\)',t,re.M)
s=t.split('### Phase G verifier deliverable',1)[1].split('### Phase R3 verifier deliverable',1)[0]
passed=re.findall(r'^#### (G\d+)\b.*?^- \*\*Verdict\.\*\* PASS\.',s,re.M|re.S)
print('phase_g_unchecked',open_rows)
print('phase_g_recorded_pass',passed)
print('same_phase_g_ids',open_rows==passed)
PY
```

  phase_g_unchecked ['G1', 'G2', 'G3', 'G4', 'G5', 'G6', 'G7', 'G8', 'G9', 'G10', 'G11', 'G12']
  phase_g_recorded_pass ['G1', 'G2', 'G3', 'G4', 'G5', 'G6', 'G7', 'G8', 'G9', 'G10', 'G11', 'G12']
  same_phase_g_ids True

  An identity-based comparison matches each unchecked intake item to its own later PASS verdict in this same file. This establishes inconsistent recorded status without treating historical source line numbers or old tests as current evidence.

### Independent challenge

The candidate treats an original static-review intake and a separately headed later verifier report as simultaneous final-status assertions. The document expressly says its checkbox state belongs to a historical implementation cycle and is not a current work queue. Git history shows the G checklist predates the verifier deliverable, which was appended as a distinct 294-line report; its matching G1-G12 PASS summary already makes that later review's outcome explicit. Unlike A15-005's unqualified aggregate that miscounts its own checked inventory, this is a retained earlier stage followed by the later evidence. Unsynchronized archival checkboxes may be untidy, but no broken navigation, falsely current instruction, or material structural consequence has been established. Re-ticking the intake or certifying those old PASS records as a newly authoritative final status is not warranted by this audit.

- [SESSION_RUNTIME_R2_R5_TODO.md:3-7](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/SESSION_RUNTIME_R2_R5_TODO.md?plain=1#L3-L7)

```text
> Historical session-runtime extraction ledger. Checkbox state below belongs
> to that implementation cycle and is not a current work queue.
```

  Existing framing already prevents interpreting the unchecked historical list as present-day required work.

- [SESSION_RUNTIME_R2_R5_TODO.md:591-601](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/SESSION_RUNTIME_R2_R5_TODO.md?plain=1#L591-L601)

```text
### Phase G — PR #659 static review findings (sequential, must land)
```

  The unchecked list is explicitly the static-review findings stage, not the subsequently appended verifier report.

- [SESSION_RUNTIME_R2_R5_TODO.md:761-770](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/SESSION_RUNTIME_R2_R5_TODO.md?plain=1#L761-L770)

```text
### Phase G verifier deliverable
```

  A distinct section describes the later commit-by-commit review and its method; the stage boundary is not hidden.

- [SESSION_RUNTIME_R2_R5_TODO.md:1050-1053](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/SESSION_RUNTIME_R2_R5_TODO.md?plain=1#L1050-L1053)

```text
**Phase G summary.** G1 PASS / G2 PASS / G3 PASS / G4 PASS / G5 PASS /
G6 PASS / G7 PASS / G8 PASS / G9 PASS / G10 PASS / G11 PASS / G12 PASS
```

  The historical verifier outcome is explicit and identity-matched; preserving the earlier intake does not hide it.

- Read-only reproduction:

```text
git --no-pager blame -L 591,601 -- SESSION_RUNTIME_R2_R5_TODO.md; git --no-pager show --numstat --format='%h %ad %s' --date=iso-strict 67b74515b5 -- SESSION_RUNTIME_R2_R5_TODO.md
```

  The Phase G header/intake is attributed to cf3887c895 (2026-05-10 09:50:57 +0200). The later commit is '67b74515b 2026-05-10T10:55:08+02:00 Phase G verifier — adversarial pass over G1–G12 review fixes', with numstat '294 0 SESSION_RUNTIME_R2_R5_TODO.md'. Its diff appends the separately headed verifier deliverable without rewriting the intake.

  Primary repository history establishes two successive records, rather than a current statement contradicted by modern source.

**Disposition:** Rejected; no correction authorized for this claim.

## Lane limitations

- **Audit:** No builds, Cargo tests, installs, provider calls, commits, branch changes, nested agents, or documentation edits were performed. The only intended write is this audit artifact. Pure Python inspection and small read-only Git/GitHub commands supplied evidence.
- **Audit:** Every assigned file was inspected, but the 734,816-byte changelog was not semantically revalidated line by line against every historical release. Its entire reference/heading structure was scanned; close semantic reading covered the current preamble, Unreleased, and releases 0.8.36-0.8.40. Older API/signature inventories remain a semantic coverage limitation, not a clean bill of health.
- **Audit:** The 25 April audit reports and six root historical ledgers were read as historical records. Their underlying old bugs, historical test-pass claims, and all cited commit/source line numbers were not independently re-executed or re-adjudicated. Findings A15-005 and A15-006 concern only contradictory recorded status.
- **Audit:** All 32 assigned file hashes matched the manifest when checked. No symlinks were assigned or followed. The explicit current-reference destinations in all six historical root banners exist.
- **Audit:** The navigation scan found 142 inline/reference/HTML destinations across the corpus and nine nonportable local links. Remote availability was checked selectively for missing initial release refs and the three affected changelog destinations, not every external URL.
- **Audit:** No rendered documentation-site or browser validation was run. The source-report link finding follows standard repository-relative Markdown resolution and verified sibling files.
- **Audit:** Pending changes since v0.8.40 are release/CI tooling and documentation; the Unreleased Windows cross-build description matches the inspected workflow/action. No additional concrete pending public-API omission was established. This audit did not run cargo-semver-checks or certify completeness of all recent Breaking sections.
- **Challenge:** Independently adjudicated all six candidate IDs only; this is not a fresh semantic audit of every statement in the 32-file manifest. All 32 assigned file hashes matched the manifest, and HEAD was 56208b9e6cee078f52c43af8f6b36660bc93eeb4 during inspection.
- **Challenge:** No builds, dependency installs, provider calls, nested agents, commits, branch changes, or repository documentation/source edits were performed. The semver exercise invoked pure Python classification/coverage functions and does not certify an actual release-gate run.
- **Challenge:** GitHub HTTP results are point-in-time navigation checks. Missing v0.1.0/v0.2.0 refs do not disprove the historical releases or exhaust possible archival destinations.
- **Challenge:** Historical regression tests and verifier claims were not rerun. A15-004 and A15-005 authorize only narrow historical clarification/reconciliation, preserving the underlying record; A15-006 remains rejected.
