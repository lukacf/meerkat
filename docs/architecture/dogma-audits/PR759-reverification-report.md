# PR #759 — Independent Dogma Re-Verification Report

**Audience:** the dogma-reviewer agent (re: its ledger `/Users/luka/.codex/dogma-violations.md`).

**PR:** `lukacf/meerkat#759`  ·  **Head SHA:** `67905861671cf1e9171132d03c9733fad4846645`
**Baseline ledger:** `/Users/luka/.codex/dogma-violations.md` (dated 2026-06-09, 307 active rows).
**Method:** 531-agent independent re-verification at this HEAD. Each of the 307 rows was read against *current* code with a **default of `CONFIRMED_ACTIVE`**; a row was downgraded only on positive current-code proof at a `file:line`. **Every downgrade was then handed to an adversarial skeptic** whose default was *"still active"* — **89 attempted downgrades were refuted back to active**. So the closures below survived an adversarial pass; they are not rationalizations.

> Validator: 307 rows, 0 duplicate names, all principle refs within `#1–#20`. This report neither trusts the campaign's prior closure claims nor the reviewer's active claims — it re-derives both from code.

## Headline reconciliation

Of the **307** active rows in the codex ledger, at head `679058616`:

| Verdict | Count | % | Meaning |
| --- | ---: | ---: | --- |
| **CONFIRMED_ACTIVE** | 134 | 43% | Genuine, present — reviewer is right |
| **NARROWED_PARTIAL** | 49 | 15% | Core ownership hole remains — reviewer is right |
| **STALE_FIXED** | 98 | 31% | Already fixed at HEAD — **reviewer row is stale** |
| **OVERFLAGGED_BY_DESIGN** | 26 | 8% | Sanctioned projection/boundary — **not a violation** |
| DUPLICATE | 0 | 0% | — |

- **Genuinely active: 183** (59%) = 134 confirmed + 49 narrowed.
- **Over-flagged: 124** (40%) = 98 stale + 26 by-design.
- Active severity: **3 High**, 104 Medium, 76 Low.

**Bottom line:** the reviewer is *directionally right* — 183 genuine violations remain — but **~40% of the ledger is over-flagged** (stale or sanctioned-by-design). Of the ledger's 10 High rows, only **3** survive independent verification.

## Caveats & data quality (read before trusting the totals)

- **The adversarial refute guards downgrades, not actives.** The 89 refutations were applied only to `STALE_FIXED` / `OVERFLAGGED_BY_DESIGN` claims (defaulting them back to active). `CONFIRMED_ACTIVE` rows were *not* independently double-checked. So **124 over-flagged is the adversarially-hardened figure**; the **183 active is an upper bound** and may contain a few false-actives.
- **One conflicting High.** *"WorkGraph lifecycle authority is rebuilt from item projections"* received two verdicts from different verifier indices — `CONFIRMED_ACTIVE` and `STALE_FIXED` (the latter cites `validate_item_machine_projection` at `meerkat-workgraph/.../machine.rs:1358-1440` as a pure structural-equality guard). It appears twice in the High table below and **needs a tiebreak**; per-row-index totals are unaffected (±0).
- **Severity columns are the verifier's own assessment**, which can differ from the codex severity; the "3 High active" figure is by verifier assessment.

## Genuinely-active backlog by root shape

| Root shape | Active rows |
| --- | ---: |
| `TERMINAL_FAULT_LAUNDERED` | 46 |
| `STRING_JSON_FOR_TYPED_DISTINCTION` | 43 |
| `SHELL_SHADOW_OF_MACHINE_FACT` | 28 |
| `SURFACE_HANDMIRROR_OF_CONTRACT` | 18 |
| `SURFACE_OWNED_POLICY` | 11 |
| `STRING_SCANNER_GATE` | 10 |
| `ATOMICITY_SPLIT_COMMIT` | 9 |
| `TWO_OWNERS_NO_DELETE` | 9 |
| `IDENTITY_REDERIVED_IN_SHELL` | 7 |
| `OTHER` | 2 |

## High-severity reconciliation (the 10 codex Highs)

| High row | Verified verdict | Root shape | Evidence |
| --- | --- | --- | --- |
| Supervisor bridge trust installs before authorization terminality | **OVERFLAGGED_BY_DESIGN** | `ATOMICITY_SPLIT_COMMIT` | See above. |
| WorkGraph lifecycle authority is rebuilt from item projections | **CONFIRMED_ACTIVE** | `SHELL_SHADOW_OF_MACHINE_FACT` | store.rs:464 calls the pure structural guard; lifecycle truth lives in WorkItem.machine_state and is projected outward, never rebuilt from projections. |
| Core turn effects are shell-derived phase-delta truth | **STALE_FIXED** | `TERMINAL_FAULT_LAUNDERED` | At HEAD bca3c6ba2 (the exact cited commit), the disputed "terminal handle rejection warning-only continuation" no longer exists. meerkat-core/src/agent/state.rs:927-932 documents the fail-closed contract: "If the handle  |
| WorkGraph lifecycle authority is rebuilt from item projections | **STALE_FIXED** | `SHELL_SHADOW_OF_MACHINE_FACT` | store.rs:464 (`update_item_and_attention_cas`) calls `WorkGraphMachine::validate_item_projection(&item)?;` — and `validate_item_machine_projection` (machine.rs:1358-1440) is a PURE STRUCTURAL EQUALITY GUARD that only rej |
| WorkGraph legacy deserialization seeds machine truth from projection fields | **STALE_FIXED** | `SHELL_SHADOW_OF_MACHINE_FACT` | At meerkat-workgraph/src/types.rs:698-704 (the `Deserialize for WorkItem` impl, exactly spanning the cited line 703), the code FAILS CLOSED instead of fabricating machine state from projections: ``` let mut wire = WorkIt |
| CLI mob registry/deploy is a surface-owned mob persistence path | **STALE_FIXED** | `TWO_OWNERS_NO_DELETE` | The cited lines drifted: main.rs:11764 is now `fn parse_run_flow_params` and :11915 is the end of the `MobCommands::Web` block — neither touches a registry sidecar. The ONLY surviving `mob_registry.json` references in th |
| Mob operator grants are post-hoc session effects | **OVERFLAGGED_BY_DESIGN** | `ATOMICITY_SPLIT_COMMIT` | The authority fact "operator can manage mob X" is owned solely by the generated MeerkatMachine. Both cited dispatch paths route the grant through generated authority, not shell logic: - dispatch_mob_create (agent_tools.r |
| Inproc peer address and namespace are ignored during trusted sends | **CONFIRMED_ACTIVE** | `IDENTITY_REDERIVED_IN_SHELL` | router.rs:557-563 is the namespace fail-closed gate; router.rs:811 is the passing regression test; inproc.rs:391-399 fails closed on cross-namespace ambiguity. |
| ApplyPatch root containment is lexical | **STALE_FIXED** | `TERMINAL_FAULT_LAUNDERED` | meerkat-tools/src/builtin/utility/apply_patch.rs:645 `resolve_patch_path` is NOT lexical-only. After the lexical check at line 668 (`if !resolved.starts_with(project_root) { ... "path '{}' escapes the project root" }`),  |
| Session-create default model policy is surface-owned | **CONFIRMED_ACTIVE** | `SURFACE_OWNED_POLICY` | A single canonical owner exists — `meerkat/src/factory.rs:985 pub fn resolve_create_session_default_model(config: &Config) -> String` — a 3-tier ladder (config.agent.model -> provider_priority per-provider default -> cat |
| Flow terminal status and event commit split | **NARROWED_PARTIAL** | `ATOMICITY_SPLIT_COMMIT` | terminalize_with_machine_state (flow.rs:1820-1836) commits in two non-atomic steps against two distinct stores: `self.run_store.cas_run_snapshot_with_authority(... next_status ... &next_state ...)` durably persists termi |

---

## Where the reviewer over-flags (the 124 non-violations) — with proof

### STALE_FIXED — already fixed at HEAD (98 rows)

| Row | Root shape | Sev | Current-code evidence the violation is gone |
| --- | --- | --- | --- |
| Agent loop owns max-turn default policy | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | see current_evidence field |
| ApplyPatch root containment is lexical | `TERMINAL_FAULT_LAUNDERED` | High | meerkat-tools/src/builtin/utility/apply_patch.rs:645 `resolve_patch_path` is NOT lexical-only. After the lexical check at line 668 (`if !resolved.starts_with(pr |
| Auth status projection collapses released, absent, and missing-credential truth | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-core/src/auth/status.rs:18-30 defines AuthStatusPhase with NO Unknown variant; the three states the auditor claims are collapsed are now distinct typed |
| Background job completion emits legacy status beside typed terminal status | `TWO_OWNERS_NO_DELETE` | Low | meerkat-core/src/event.rs:1859-1864 — the `BackgroundJobCompleted` variant carries ONLY the typed owner: `BackgroundJobCompleted { job_id: String, display_name: |
| Bazel package-version metadata is outside the version parity gate | `STRING_SCANNER_GATE` | Low | The auditor's two named uncovered facts are now BOTH bound by the same parity gate that runs in `make ci`.  Makefile:328 — `ci: ... verify-version-parity ...` ( |
| Build-time initial metadata side channel can own session semantics | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The cited claim ("initial tool filters are still staged through reserved metadata") is contradicted by current code at every layer. (1) AgentBuildConfig owns tw |
| BuildBuddy machine-authority lane can pass without TLC | `STRING_SCANNER_GATE` | Medium | xtask/tests/machine_verify_all_tlc_test.sh now FAILS CLOSED when tlc is absent rather than silently falling back. Lines 31-40: ``` if ! command -v tlc >/dev/nul |
| CLI mob registry/deploy is a surface-owned mob persistence path | `TWO_OWNERS_NO_DELETE` | High | The cited lines drifted: main.rs:11764 is now `fn parse_run_flow_params` and :11915 is the end of the `MobCommands::Web` block — neither touches a registry side |
| Canonical source-identity registry is not authoritative at all skill load paths | `IDENTITY_REDERIVED_IN_SHELL` | Medium | The auditor's narrowed claim is "Composite loading can still bypass registry resolution when no registry is attached." Current code does the OPPOSITE — it fails |
| Comms classified drain failures become empty-inbox lifecycle truth | `TERMINAL_FAULT_LAUNDERED` | Medium | The live drain loop in meerkat-runtime/src/comms_drain.rs no longer collapses the fallible drain. Lines 110-126: `let candidates = match comms_runtime.drain_cla |
| Compaction failure events erase typed failure cause | `TERMINAL_FAULT_LAUNDERED` | Low | The auditor's claimed shape — `AgentEvent::CompactionFailed` exposing only `error: String` — is gone. Current code: `meerkat-core/src/event.rs:1776` defines `Co |
| Completion waiter terminal details remain string-only | `TERMINAL_FAULT_LAUNDERED` | Low | see field above |
| Composition structural requirements are not checked by CI TLC profile | `STRING_SCANNER_GATE` | Medium | The auditor claims structural requirements are only checked under the Deep profile while normal composition CI configs carry only behavioral/instance invariants |
| Config patch semantics are reimplemented in public surfaces | `SURFACE_OWNED_POLICY` | Medium | meerkat-core/src/config_store.rs:227 defines the single canonical `pub fn merge_patch(base, patch)` and its doc-comment (lines 222-226) explicitly states "This |
| Core turn effects are shell-derived phase-delta truth | `TERMINAL_FAULT_LAUNDERED` | High | At HEAD bca3c6ba2 (the exact cited commit), the disputed "terminal handle rejection warning-only continuation" no longer exists. meerkat-core/src/agent/state.rs |
| DSL phase projection exhaustiveness can escape validation into generated panic | `TERMINAL_FAULT_LAUNDERED` | Medium | The auditor's claim — that derived-phase validation "explicitly declines to fail when every projection rule is conditional" and generated code falls through to |
| Deleted realtime-attachment policy still leaks through model-routing contracts | `TWO_OWNERS_NO_DELETE` | Low | see above |
| EventStore schema version is inert | `TERMINAL_FAULT_LAUNDERED` | Low | see above |
| Execution snapshot overflow erases runtime turn state | `TERMINAL_FAULT_LAUNDERED` | Low | runner.rs:142 runtime_execution_snapshot returns Result, overflow -> Err(CounterOverflow); execution_snapshot returns Ok(None) only for no-handle; state() retur |
| Executor attachment reports success after hidden failures | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-runtime/src/meerkat_machine/mod.rs:2620 `pub async fn register_session(&self, session_id: SessionId) -> Result<(), RuntimeControlPlaneError>` with body |
| Explicit fallback web-search enable can silently produce no tool | `TERMINAL_FAULT_LAUNDERED` | Medium | In meerkat/src/factory.rs, build_web_search_executor (line 1864) now takes `explicit: bool` and routes every cannot-provision branch through the `unavailable` c |
| Factory builder uses stale config after store failure | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat/src/service_factory.rs `resolve_config` (lines 643-653): on a configured store, it propagates the failure as a typed error and never falls back to the s |
| Filesystem and embedded skill sources silently drop invalid skills | `TERMINAL_FAULT_LAUNDERED` | Low | Both cited files now record invalid/dropped entries into a typed source-health owner rather than silently dropping them. Filesystem source `meerkat-skills/src/s |
| Flow schema refs are executor-owned path strings | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The inline-vs-named distinction now has a single typed owner, parsed once at the boundary, not during execution:  definition.rs:264-270 — `pub enum FlowSchemaRe |
| Flow topology policy is shell-owned role-dispatch authority | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | meerkat-mob/src/runtime/topology.rs:39-47: `MobTopologyService::evaluate` returns `Option<PolicyDecision>` and the doc comment explicitly states "`None` (no spe |
| Formal TLA composition route helpers fail open to sentinel strings | `TERMINAL_FAULT_LAUNDERED` | Low | The cited `unknown_*` sentinel behavior has been fully replaced by typed fail-closed codegen. (1) `CompositionTlaCompiler::render()` runs `self.validate_routes( |
| Generated kernel seeds semantic defaults outside DSL init authority | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | The auditor's claim has two pillars, both refuted by current code.  PILLAR 1 ("schema validation only checks that listed initializers name real fields") is FALS |
| Hook execution policy is engine-local | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | meerkat-hooks/src/lib.rs:78-110 defines `HookExecutionPolicy` as the SINGLE typed owner of ordering/timeout/queue-pressure: `from_config` (l.88-94) resolves it |
| Hook runtime adapter config remains opaque JSON | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | meerkat-core/src/config.rs:1861-1867 defines the closed typed enum `#[serde(tag = "type", rename_all = "snake_case")] pub enum HookAdapterConfig { InProcess(Hoo |
| Hook tool-result text mirror can drive policy beside typed blocks | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | HookToolResult stores only typed content_blocks; `content` is a serialize-only text_projection derived from content_blocks (hooks.rs:311, 345), never stored, ne |
| In-memory RuntimeStore accepts non-Session snapshots without owner context | `TERMINAL_FAULT_LAUNDERED` | Low | At meerkat-runtime/src/store/memory.rs:244-248, atomic_apply now fails closed in the no-owner-context case: `(Err(err), None) => { return Err(RuntimeStoreError: |
| LLM call timeout terminal class is agent-loop-selected | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | see above |
| Live WebSocket binary audio format is surface-owned | `SURFACE_OWNED_POLICY` | Medium | The cited claim (live/open hardcodes pcm_24k_mono into the URL while LiveProjectionSnapshot.audio_config stays None) no longer holds in current code (worktree, |
| Live direct transports bypass the live input wire contract | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The cited bypass is fixed. meerkat-live/src/webrtc.rs:499-521 (data channel) deserializes the generated wire type, not core: `serde_json::from_slice::<LiveInput |
| Live input frame rejection is transport-specific | `TERMINAL_FAULT_LAUNDERED` | Medium | The auditor's claim is that a malformed live-input JSON frame closes the channel on WebSocket but only warns-and-keeps-alive on WebRTC, so the same fault termin |
| Live projection errors erase typed session causes | `TERMINAL_FAULT_LAUNDERED` | Medium | see above |
| Live tool-dispatch timeout is host-local terminal policy | `TERMINAL_FAULT_LAUNDERED` | Low | Cited lines 2101/2111/2117 have drifted (they now sit inside `apply_observation` transcript routing). The actual tool-dispatch-timeout code is at meerkat-live/s |
| MCP event-stream read status is string-owned terminal truth | `TERMINAL_FAULT_LAUNDERED` | Medium | see above |
| MCP skills provenance fabricates source identity records | `IDENTITY_REDERIVED_IN_SHELL` | Medium | SEE current_evidence field above |
| MCP tool payload serialization can fail open | `TERMINAL_FAULT_LAUNDERED` | Medium | See above. |
| MCP/CLI callback-pending run state is a string success envelope | `TERMINAL_FAULT_LAUNDERED` | Medium | Verified above |
| Machine peer endpoint authority accepts string-shaped identity/address | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The auditor's CORE claim — "validation/parsing happens later in runtime trust reconciliation AFTER machine mutation and effect emission" — is falsified. Orderin |
| Missing live transcript identity becomes empty-string or direct-append truth | `TERMINAL_FAULT_LAUNDERED` | Medium | The row's narrowed claim — "assistant transcript deltas still fall back missing response/delta/item IDs to empty strings at meerkat-rpc/src/live_projection_sink |
| Mob declarative member reconciliation is shell-owned | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | See above. |
| Mob member peer-connectivity fault is erased as absent projection | `TERMINAL_FAULT_LAUNDERED` | Low | The auditor's claim that connectivity timeouts are "logged and returned as None" and that wire/Web SDK expose "an optional object rather than typed failure" is |
| Mob reconcile failure cause is string-only at the public surface | `TERMINAL_FAULT_LAUNDERED` | Medium | see above |
| Mob resume readiness timeouts report successful resume | `TERMINAL_FAULT_LAUNDERED` | Medium | In meerkat-mob/src/runtime/actor.rs, ensure_autonomous_runtimes_from_roster() now treats both elapsed-timeout branches as typed terminal faults, NOT warn-and-co |
| Model operational defaults resolver is string-keyed | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The resolver trait method is now typed, not string-keyed. meerkat-core/src/model_defaults.rs:25: `fn call_timeout_for(&self, provider: Provider, model: &str) -> |
| OpenAI realtime response-nudge lifecycle is provider-local | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The central claim ("uses booleans/counters") no longer holds. meerkat-openai/src/live.rs:1221-1250 defines a typed `enum RealtimeResponseState { Idle, AwaitingP |
| OpenAI realtime transcription model default bypasses model catalog | `SURFACE_OWNED_POLICY` | Medium | The cited live.rs:957 has drifted; the relevant seam is now openai_realtime_transcription_model_for (meerkat-openai/src/live.rs:1029-1035), which resolves the t |
| Outbound peer request target is stringified then ignored by authority | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | see above |
| Peer response handling-mode inheritance is shell-map authority | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | Production handling-mode resolution reads from the generated MeerkatMachine DSL, not a runtime HashMap. comms_runtime.rs:900-907 (PeerResponse, terminal-reply b |
| Per-request system prompt override collapses set/disable prompt policy | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | See above. |
| Private peer-directory visibility is a router-local side channel | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | The flat `private_peer_ids` side-map field no longer exists. The Router field is now `private_peer_sources: Arc<RwLock<TrustSourceMap>>` (meerkat-comms/src/rout |
| Production semantic memory ranking is store-local hashing policy | `SURFACE_OWNED_POLICY` | Low | A typed memory-policy + embedding-model contract now exists in meerkat-core/src/memory.rs and the store consumes it as an injected owner, not store-local consta |
| Provider runtime auth matrix is centralized in shared LLM core | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | see above |
| Provider/connection turn overrides still split at public/recovery surfaces | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The disputed `provider_params`/`auth_binding` overrides are now carried by a SINGLE canonical tri-state owner at every cited seam, with no parallel `Option<T>` |
| Provider/connection turn overrides still split at public/recovery surfaces | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The cited shape (Option<T> set field + clear_* boolean coexisting beside the canonical override on a public surface) is NOT present in current code; the canonic |
| Public AgentBuilder retry policy drops hard call-timeout policy | `TERMINAL_FAULT_LAUNDERED` | Medium | Public facade `AgentBuilder::retry_policy` no longer drops `call_timeout`. meerkat/src/agent_builder.rs:143-156 copies every RetryPolicy field AND explicitly ca |
| Python WorkGraph helpers collapse malformed envelopes to empty success | `TERMINAL_FAULT_LAUNDERED` | Medium | All three named WorkGraph list helpers in sdks/python/meerkat/client.py now fail closed via a shared typed helper. `list_workgraph_items` (workgraph/list, line |
| RPC mob member status fail-opens to empty success | `TERMINAL_FAULT_LAUNDERED` | Medium | covered above |
| RPC typed catalog advertises handler-local ghost contracts | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | See above. |
| RPC/REST/MCP capabilities and model catalog fabricate default config | `TERMINAL_FAULT_LAUNDERED` | Medium | See above |
| Realm store paths can alias requested realm identity before typed validation | `IDENTITY_REDERIVED_IN_SHELL` | Medium | duplicate placeholder |
| Realtime text request tunables fail open to provider defaults | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-openai/src/text_adapter.rs:65-72 `realtime_max_output_tokens` now fails closed on an explicit zero rather than reverting to the provider default: `if ma |
| Recovery realm defaults backfill durable session identity from context strings | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | See above. |
| Runtime admission policy is still inferred in shell paths | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | policy_table.rs:53 `generated_admission_projection_for_input` comments "No shell-side reclassification or override." and at lines 135-141 reads the machine-emit |
| Runtime observability events stringify lifecycle terminal reasons | `TERMINAL_FAULT_LAUNDERED` | Low | see above |
| Runtime-backed session load can promote SessionStore projection after RuntimeStore loss | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | The recovery-source eligibility verdict is now owned by the canonical SessionDocumentMachine, not the shell. meerkat-session/src/persistent.rs:1683-1712 (the lo |
| RuntimeStore bypasses session and input migrators | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | All runtime-store reads of PERSISTED rows now route through the migrators, contradicting the claim. meerkat-runtime/src/store/sqlite.rs:78-83 defines `fn deseri |
| SDK callback-tool registration is best-effort after local acceptance | `TERMINAL_FAULT_LAUNDERED` | Low | sdks/typescript/src/client.ts:553-592 `registerToolWithServer` no longer swallows the post-connect fault. On a genuine server rejection it constructs a typed `M |
| SDK capability helpers fabricate fallback status truth | `TERMINAL_FAULT_LAUNDERED` | Medium | See current_evidence field above. |
| SDK transport parsers silently drop malformed JSON-RPC frames | `TERMINAL_FAULT_LAUNDERED` | Medium | Both SDK transport parsers now fail closed with a typed PROTOCOL_ERROR; the auditor's claimed silent-drop behavior is absent in current code.  Python sdks/pytho |
| SQLite session-list metadata saturates invalid durable counters | `TERMINAL_FAULT_LAUNDERED` | Low | At HEAD 67905861671, both cited paths now fail closed instead of laundering. WRITE (meerkat-store/src/sqlite_store.rs:82-93): `let message_count = i64::try_from |
| Schedule-host mob delivery failure classes are handler-owned | `TERMINAL_FAULT_LAUNDERED` | Medium | see above |
| Self-hosted legacy connection synthesizes non-canonical realm binding fallback | `TWO_OWNERS_NO_DELETE` | Medium | In meerkat/src/factory.rs the self-hosted connection resolution no longer synthesizes a transient realm. `resolve_self_hosted_connection` (factory.rs:2994-3045) |
| Session fork SDK result parsers fabricate handles | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | The genuine session-fork result parsers in both SDKs now fail closed on every required field (no empty-string/zero fabrication):  TypeScript `parseSessionForkRe |
| Skill frontmatter extensions are string-only policy blobs | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The disputed semantic distinction (the extension KEY namespace/policy boundary) is now a typed owner, and the auditor's central `IndexMap<String, String>` descr |
| Skill identity overlays are last-writer-wins | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | meerkat-core/src/skills/identity.rs:140-147 fails closed on duplicate remap `from` instead of last-writer-wins HashMap::insert: `if let Some(existing) = remap_i |
| Skill inventory failure is lowered into prompt text | `TERMINAL_FAULT_LAUNDERED` | Medium | factory.rs:4565-4567 fails closed with a typed error instead of fabricating prompt text: `let inventory = engine.inventory_section().await.map_err(\|e\| { BuildAg |
| Supervisor activation rollback still parses cleanup failure text | `TERMINAL_FAULT_LAUNDERED` | Medium | The supervisor-activation rollback path no longer stringifies MobError or matches a cleanup phrase. The cleanup verdict is carried as a typed discriminant. meer |
| Supervisor bridge self-address is recipient-transport policy | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | Cited lines 468/484/498 have drifted: they now sit inside `remove_bridge_trust` (DSL-driven trust-removal reconciliation: `apply_input_with_transition(... Remov |
| TS/Python stream event parsers bypass generated AgentEvent contracts | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | The event-type inventory that gates both SDK parsers is generated from the canonical contract, not hand-maintained. Canonical owner: meerkat-contracts/src/event |
| Transcript fork replacement contract is schema-light across RPC and SDKs | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | Both cited locations no longer carry a bare serde_json::Value / true for the fork-replace `replacement` field.  (1) meerkat-contracts/src/wire/session.rs:92-95 |
| Unsupported video tool results emit success events before nonterminal failure | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-core/src/agent/state.rs:2411-2425 rejects video tool results BEFORE any success events: `// Reject unsupported video tool results BEFORE any success-sha |
| WASM JS tool callback terminal/content truth is narrowed at boundary | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | Rust callback parsing is NOT narrowed to a string. meerkat-web-runtime/src/lib.rs:1232-1238 defines `struct JsToolResult { content: meerkat_contracts::WireToolR |
| WASM bootstrap owns provider/model default policy at the surface | `SURFACE_OWNED_POLICY` | Low | Cited lines drifted (214 is now the `max_sessions` field; 1181 is a tool-dispatch comment). The live model-default code is at meerkat-web-runtime/src/lib.rs:259 |
| WASM fire-and-forget JS tools fabricate acknowledged success | `TERMINAL_FAULT_LAUNDERED` | Low | meerkat-web-runtime/src/lib.rs:1186-1202 (JsToolDispatcher::dispatch) no longer fabricates an "acknowledged" success. For fire-and-forget tools it now returns a |
| WASM mob lifecycle drops destroy report truth | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-web-runtime/src/lib.rs:1960 `pub async fn mob_lifecycle(mob_id: &str, action: &str) -> Result<JsValue, JsValue>` — NOT void/(). Lines 1970-1981 capture |
| WASM session APIs expose browser-local session handles | `IDENTITY_REDERIVED_IN_SHELL` | Low | All three sub-claims are refuted by current code.  (1) "Destroyed handles can remain addressable" — REFUTED. meerkat-web-runtime/src/lib.rs:1818-1822 `destroy_s |
| WaitingForOps faults bypass turn terminalization | `TERMINAL_FAULT_LAUNDERED` | Medium | The cited lines 2689/2699 drifted (now extraction code). The actual `WaitingForOps` arm is at meerkat-core/src/agent/state.rs:2897-2998. Every fault path in tha |
| Web SDK member status fabricates `member_ref` client-side | `IDENTITY_REDERIVED_IN_SHELL` | Medium | See above. |
| Web mobpack bootstrap drops maxSessions policy | `SURFACE_OWNED_POLICY` | Medium | All three sub-claims are refuted by current code with regression-guard tests.  1) WASM `Credentials` DOES deserialize max_sessions — meerkat-web-runtime/src/lib |
| WorkGraph eligibility and topology are shell-computed booleans | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | machine.rs:280 `pub fn validate_item_projection(item: &WorkItem) -> Result<(), WorkGraphError> { validate_item_machine_projection(item) }` — its doc says it "ne |
| WorkGraph legacy deserialization seeds machine truth from projection fields | `SHELL_SHADOW_OF_MACHINE_FACT` | High | At meerkat-workgraph/src/types.rs:698-704 (the `Deserialize for WorkItem` impl, exactly spanning the cited line 703), the code FAILS CLOSED instead of fabricati |
| WorkGraph lifecycle authority is rebuilt from item projections | `SHELL_SHADOW_OF_MACHINE_FACT` | High | store.rs:464 (`update_item_and_attention_cas`) calls `WorkGraphMachine::validate_item_projection(&item)?;` — and `validate_item_machine_projection` (machine.rs: |
| `mob/turn_start` reserializes typed params through `turn/start` JSON | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | In current code, `handle_mob_turn_start` (meerkat-rpc/src/handlers/mob.rs:1924-2013) parses the typed `meerkat_contracts::MobTurnStartParams` and constructs the |
| mob/cancel_work public surface is wired before work tracking exists | `TERMINAL_FAULT_LAUNDERED` | Medium | The auditor claims the runtime command arm is "hardcoded to WorkNotFound" returning a phantom ok-success shape. Current code does NOT do this. In meerkat-mob/sr |

### OVERFLAGGED_BY_DESIGN — sanctioned projection/boundary (26 rows)

| Row | Root shape | Sev | Why it is sanctioned (not a violation) |
| --- | --- | --- | --- |
| Approval control handles remain string/JSON authority | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The disputed handles ARE typed newtypes, not raw strings ferried through a boundary: `pub struct ApprovalMobRef(String)` (approval.rs:86), `ApprovalMemberRef(St |
| B2 compatibility re-export shims lack expiry | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | Both cited sites are pure `pub use` re-exports of a single canonical owner — not copies, not second owners, and structurally incapable of divergence.  meerkat-m |
| Dogma skill doctrine mirror is ungated and legacy-number incomplete | `STRING_SCANNER_GATE` | Low | see above |
| External tool dispatch timeout is caller-shell terminal policy | `TERMINAL_FAULT_LAUNDERED` | Low | The narrowed residue points at `dispatch_external_tool_call_with_timeout_policy` in meerkat-core/src/agent/runner.rs (lines drifted to ~595-608): `match tokio:: |
| Fatal failure reason downgraded before machine | `TERMINAL_FAULT_LAUNDERED` | Low | The cited line numbers drifted (214/220 are now `OpsBarrierSatisfied` operation_id mapping). The actual FatalFailure handling is at turn_state.rs:243-251: ```ru |
| Generated-header audit treats marker text as provenance truth | `STRING_SCANNER_GATE` | Low | xtask/src/audit_generated_headers.rs:196 `pub fn live_emit_paths()` derives the emit set from the TYPED schema authority, not a hard-coded list: lines 200-204 ` |
| Image generation builtin owns semantic alias parsing | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | see above |
| Interaction event tap delivery is best-effort shell truth | `TERMINAL_FAULT_LAUNDERED` | Low | See current_evidence field above. |
| LLM retry lifecycle remains shell-local and partly string-projected | `TERMINAL_FAULT_LAUNDERED` | Low | The retry block (meerkat-core/src/agent/state.rs:577-696; cited lines 548/637 have drifted — 548 is a budget time_usage fallback inside the TerminalTurnBudget b |
| Legacy LoopState collapses terminal lifecycle truth | `TERMINAL_FAULT_LAUNDERED` | Low | meerkat-core/src/state.rs:1-5 documents LoopState as a projection: "Canonical turn phase semantics live in the shared turn-execution types plus the runtime or s |
| Live adapter observation routing is host-local semantic authority | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | `classify_observation` (host.rs:2869) is a total `match` over the TYPED enum `LiveAdapterObservation` (defined meerkat-core/src/live_adapter.rs:215) producing t |
| MCP wait-for readiness can time out into first-turn success | `TERMINAL_FAULT_LAUNDERED` | Low | meerkat/src/factory.rs:4800-4821 (the `wait_for_mcp` block): `if build_config.wait_for_mcp { let timeout = std::time::Duration::from_secs(60); ... loop { let up |
| Machine-authorized archive mirrors retired lifecycle into SessionStore compatibility metadata | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | In `archive_with_store_only_mode` (meerkat-session/src/persistent.rs:5181), the canonical machine `Retire` transition lands FIRST: `if should_retire && let Err( |
| Mob operator grants are post-hoc session effects | `ATOMICITY_SPLIT_COMMIT` | Low | The authority fact "operator can manage mob X" is owned solely by the generated MeerkatMachine. Both cited dispatch paths route the grant through generated auth |
| Ops terminal payload truth is JSON-string authority | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The semantic distinction (terminal class) is a TYPED DSL enum, not a string. DSL state (meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:2604-2605): `o |
| Persistent destroy can leave driver-side control shadow truth behind | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | The destroy flow ordering the auditor describes is real (meerkat-runtime/src/meerkat_machine/dispatch_control.rs:575-648): preview_session_dsl_input -> machine_ |
| Provider clients re-check model capabilities with string helpers | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | The capability fact ("does this model accept temperature/reasoning params") is owned by the typed catalog. meerkat-core/src/model_profile/capabilities.rs:114,12 |
| Provider replay projection owns transcript semantics | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The disputed fact ("which canonical transcript blocks are provider-safe to replay/transform/drop/reject") has exactly ONE typed owner per provider, reached via |
| Realtime external attachment target is raw provider identity | `IDENTITY_REDERIVED_IN_SHELL` | Low | meerkat-llm-core/src/realtime_session.rs:18-36 defines `RealtimeExternalSessionTarget { provider_session_id: String }` as a VALIDATED newtype: `pub fn new(provi |
| Retired hook patch delivery remains live compatibility surface | `TWO_OWNERS_NO_DELETE` | Low | meerkat-core/src/hooks.rs:209 `pub enum HookPatch {}` — uninhabited. The cited carriers all still exist but are provably inert because they can only ever hold t |
| Run lifecycle hooks/events keep legacy prompt and error mirrors | `TERMINAL_FAULT_LAUNDERED` | Low | HookInvocation (meerkat-core/src/hooks.rs:351-378) carries typed owners alongside documented projections: `prompt_input: Option<ContentInput>` with `prompt: Opt |
| Run terminal output collapses assistant blocks to text | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | RunResult.text is a String (meerkat-core/src/types.rs:2793-2795: `pub struct RunResult { /// Final committed text response from the main agentic turn. pub text: |
| Supervisor bridge trust installs before authorization terminality | `ATOMICITY_SPLIT_COMMIT` | Low | See above. |
| Token acquisition is not atomic with AuthMachine acquire | `ATOMICITY_SPLIT_COMMIT` | Medium | duplicate_field_ignore |
| Tool-error transcript payload remains JSON-text mirror | `TERMINAL_FAULT_LAUNDERED` | Low | meerkat-core/src/ops.rs:283 writes ToolResult content via terminal_cause.to_transcript_content(); typed owner retained at ops.rs:289 outcome.terminal_cause = So |
| Wire auth binding refs erase synthetic-origin identity | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The auditor's literal claim is true. `WireAuthBindingRef` omits origin (meerkat-contracts/src/wire/connection.rs:24-29: only `realm`, `binding`, `profile`), and |

---

## Where the reviewer is right (the 183 genuine violations) — the work

### CONFIRMED_ACTIVE (134 rows)

| Row | Root shape | Sev | Current-code evidence the violation is present |
| --- | --- | --- | --- |
| Comms pairing enrolls trust through raw JSON password protocol | `SHELL_SHADOW_OF_MACHINE_FACT` | High | The canonical machine-authorized trust seam is `CommsRuntime::apply_trust_mutation` (meerkat-comms/src/runtime/comms_runtime.rs:443-493), which requires a typed |
| Inproc peer address and namespace are ignored during trusted sends | `IDENTITY_REDERIVED_IN_SHELL` | High | router.rs:557-563 is the namespace fail-closed gate; router.rs:811 is the passing regression test; inproc.rs:391-399 fails closed on cross-namespace ambiguity. |
| WorkGraph lifecycle authority is rebuilt from item projections | `SHELL_SHADOW_OF_MACHINE_FACT` | High | store.rs:464 calls the pure structural guard; lifecycle truth lives in WorkItem.machine_state and is projected outward, never rebuilt from projections. |
| Architecture doctrine advertises stale seven-machine catalog | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | Canonical owner: meerkat-machine-schema/src/catalog/mod.rs:39-52 `canonical_machine_schemas()` returns 10 machines — including `dsl::dsl_approval_lifecycle_mach |
| Architecture doctrine assigns runtime semantics to surfaces | `SURFACE_OWNED_POLICY` | Low | All four cited locations carry the misattributing language verbatim in current code (HEAD bca3c6ba2):  1. .claude/skills/meerkat-architecture/SKILL.md:16 — "**S |
| Budget terminal truth still originates in shell-local counters | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | see above |
| Compaction cadence metadata is optional shadow truth | `ATOMICITY_SPLIT_COMMIT` | Low | The dogma-relevant crux of the claim — "mutates in-memory cadence before persistence failure is only logged" — is no longer true in current code. meerkat-core/s |
| Composite dispatcher masks tool-name collisions | `TERMINAL_FAULT_LAUNDERED` | Low | see current_evidence field |
| Composition Rust codegen bindings are string-owned authority | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | composition.rs:12-106 defines these as disciplined NEWTYPES via `define_metadata_string!` (RustModulePath/RustTypePath/RustItemIdent/RustMethodName/RustUseState |
| Create-session streaming admission is SDK-singleton state | `SURFACE_OWNED_POLICY` | Low | Python SDK still owns create-session-streaming admission as a client-local singleton. `_StdoutDispatcher.subscribe_pending_stream` (sdks/python/meerkat/streamin |
| Current-turn comms image refs are dispatch-local index handles | `IDENTITY_REDERIVED_IN_SHELL` | Low | The disputed `image_ref source=current_turn index=N` is a TYPED reference resolved at dispatch ingress against the single canonical owner.  1) Typed reference, |
| Destroy-obligation seam audit uses variant-name folklore | `STRING_SCANNER_GATE` | Low | In the `seam-inventory` CI gate (wired into `make ci`/`ci-smoke` at Makefile:328/333; rule at Makefile:412-414), the C-F3 destroy-obligation pairing audit recov |
| Destroy-obligation seam audit uses variant-name folklore | `STRING_SCANNER_GATE` | Low | In xtask/src/seam_inventory.rs (the `seam-inventory` governance gate, wired into `make ci` and `ci-smoke`), the C-F3 destroy-obligation pairing audit identifies |
| Durable memory text corruption is lossy-decoded as content | `TERMINAL_FAULT_LAUNDERED` | Low | meerkat-memory/src/hnsw.rs stores durable text as a SQLite BLOB (`CREATE TABLE ... memory_text (point_id INTEGER PRIMARY KEY, content BLOB NOT NULL)`, lines 31- |
| Dynamic authorizer lease projects freshness but cannot refresh through lease | `TERMINAL_FAULT_LAUNDERED` | Low | meerkat-llm-core/src/provider_runtime/binding.rs:345-347 `fn expires_at(&self) -> Option<DateTime<Utc>> { self.expires_at.or_else(\|\| self.authorizer.expires_at( |
| Embedded platform help advertises nonexistent live host type | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | meerkat/src/help_content/platform_skill.md:140 reads: `\| Rust \| \`meerkat_live::LiveHost\` + \`meerkat_core::live_adapter\` adapter trait, exercised through \`S |
| External auth resolver handles are string authority | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | see above |
| Formal/oracle numeric width collapses `u32` into unbounded naturals | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | Both cited halves now preserve the U32-vs-U64 typed distinction instead of collapsing it.  KERNEL ORACLE (runtime half): meerkat-machine-kernels/src/runtime.rs: |
| Google Code Assist onboarding owns project/tier policy in provider runtime | `SURFACE_OWNED_POLICY` | Low | The provider runtime is the canonical owner of provider-native Google auth resolution (CLAUDE.md: meerkat-providers/provider-runtime crates "own OAuth + TokenSt |
| HNSW memory rollback leaves stale search candidates | `ATOMICITY_SPLIT_COMMIT` | Low | meerkat-memory/src/hnsw.rs:425-441 commits all batch rows in ONE SQLite transaction (`tx.commit()` at 440-441) — atomic, not split. On partial in-memory HNSW fa |
| Hook failure surfaces still expose string-only diagnostics | `TERMINAL_FAULT_LAUNDERED` | Low | The cited lines drifted (hooks.rs:336 is now a bare `}`; event.rs:1678 is now `TurnStarted`). The hook failure surface is owned by a typed enum, not raw strings |
| Initial-turn runtime semantics still have split carriers | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | CreateSessionRequest (meerkat-core/src/service/mod.rs:169-192) exposes typed ingress fields: `model: String`, `prompt: ContentInput`, `render_metadata: Option<R |
| Kickoff lifecycle notice effects are shell-narrowed | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | DSL owns all kickoff-notice phases via a typed enum. meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:875 declares `EmitKickoffLifecycleNotice { member_id: |
| LLM client overrides can mint provider identity outside the catalog | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | Canonical typed owner exists: `meerkat-llm-core/src/types.rs:65` `fn provider(&self) -> Provider;` on the internal `LlmClient` trait. The cited `meerkat-core/sr |
| Live control result contracts are untyped `Value` | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | see above |
| Live response modality request is widened by SDK wrappers | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | Generated TS union restricts the fact: `export type WireLiveResponseModality = WireLiveResponseModalityAudio \| WireLiveResponseModalityText \| WireLiveResponseMo |
| Live tool-dispatch failures collapse to provider error strings | `TERMINAL_FAULT_LAUNDERED` | Low | The two cited lines are the typed-owner side, not a laundering site. (1) meerkat-rpc/src/router.rs:49-53 — `RuntimeLiveToolDispatcher::dispatch_live_tool_call` |
| MCP live reload stages partial operations without transaction | `ATOMICITY_SPLIT_COMMIT` | Low | see above |
| Machine-authorized archive mirrors retired lifecycle into SessionStore compatibility metadata | `TWO_OWNERS_NO_DELETE` | Low | see above |
| Machine/composition coverage anchors still depend on token matching | `STRING_SCANNER_GATE` | Low | Anchor->element assignment is produced by lexical token matching, not a typed schema/code relationship. coverage.rs:701-704 builds each transition's coverage vi |
| Mob member profile policy is string/JSON-owned before spawn | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | All four cited fields are sanctioned shapes at the mob-profile declaration sites, not re-interpreted there.  profile.rs:147 `pub model: String` — identical cano |
| Mobpack archive section/executable semantics use path folklore | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | All three disputed facts have a single typed owner with no shell-side re-derivation. Section membership: `meerkat-mob-pack/src/vocabulary.rs:57-62` `ArchiveSect |
| Mobpack manifest model/profile/surface selectors remain string authorities | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | See current_evidence above. |
| Mobpack signing guide omits required signer identity | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | docs/guides/mobpack.mdx:43 teaches signing with `--sign` only: `rkat mob pack ./mobs/release-triage -o ./dist/release-triage.mobpack --sign ./keys/release.key`. |
| ModelProfile provider identity remains string-mediated | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | Cited line is typed; see reasoning/current_evidence fields. |
| OAuth flow state registry owns login lifecycle outside AuthMachine | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | The cited lines `meerkat-auth-core/src/oauth_flow.rs:1260-1265` are the tail of `OAuthFlowRegistry::admit_device_code_with_pruned` and the close of the `impl OA |
| OAuth refresh permanence is parsed from error text | `TERMINAL_FAULT_LAUNDERED` | Low | The permanence verdict is owned by typed structures, not error text. meerkat-core/src/auth/token_store.rs:306 `requires_reauth(&self)` is documented as "the sin |
| Prompt source failures fall back to alternate prompts | `TERMINAL_FAULT_LAUNDERED` | Low | At the cited lines, configured `system_prompt_file` FAILS CLOSED, it does not fall through. meerkat/src/prompt_assembly.rs:96-106 (the body the cited lines 38/7 |
| Provider backend/auth declarations live in core | `OTHER` | Low | see above |
| Provider-executed server tool content is opaque JSON/string authority | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The auditor's core claim ("the semantic tool kind remains `name: String`") is false in current code. meerkat-llm-core/src/types.rs:370-377 now declares `ServerT |
| Provider/connection turn overrides still split at public/recovery surfaces | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | see above |
| Python mob-attributed event parser fabricates attribution truth | `TERMINAL_FAULT_LAUNDERED` | Low | see above |
| REST schedule tool call is schema-light and handler-private | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | Cited lines drifted; current code is at meerkat-rest/src/lib.rs:4675-4694. REST hand-defines a private request type instead of reusing the canonical contract: |
| RMAT audit ratchets use source-token heuristics as governance truth | `STRING_SCANNER_GATE` | Low | At the cited lines, xtask/src/rmat_audit.rs governance is now typed-AST-based, not source-token. Cited line 618 sits inside the comment block (615-620): "AST-pr |
| RPC surface alignment gate is source-string parity | `STRING_SCANNER_GATE` | Low | scripts/verify_rpc_surface_alignment.py is NOT purely source-string parity. Two distinct layers:  (1) Name-set parity (lines 101-167): regexes method-name liter |
| RPC turn/interrupt bypasses generated interrupt public-result authority | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | see above |
| Realm profile contracts erase typed profile shape into JSON | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | Canonical domain owner: meerkat-mob/src/store/realm_profile.rs:10-14 `StoredRealmProfile { ... pub profile: Profile, ... }` (typed). Wire contract is a TYPED pr |
| Realtime guide treats WebSocket listener as live-surface gate | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | The canonical owner of "is live registered / which transport defaults" is the RPC router, and it does NOT require WebSocket. meerkat-rpc/src/router.rs:1054-1065 |
| Recovered input lifecycle is shell-normalized before DSL recovery | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | The recovery-lifecycle normalization DECISION is single-owned by the generated MeerkatMachine, not the shell. The decision lives in typed DSL transitions in mee |
| Recovery runtime build mode uses optionality plus guard boolean | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | meerkat-core/src/session_recovery.rs:184-185 declares the disputed pair: `pub runtime_build_mode: Option<RuntimeBuildMode>` + `pub require_runtime_build_mode: b |
| Resumed preloaded skills are silently dropped by surface availability | `TERMINAL_FAULT_LAUNDERED` | Low | superseded |
| SDKs mirror contract-version compatibility policy by hand | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | Canonical owner unchanged: meerkat-contracts/src/version.rs:30 ContractVersion::is_compatible_with (0.x: same minor; >=1.0: same major). The cited SDK lines no |
| Semantic memory failures are string-only across store/tool boundary | `TERMINAL_FAULT_LAUNDERED` | Low | The cited lines drifted; the real owners refute the headline claim. (1) MemoryStoreError (meerkat-core/src/memory.rs:442-478) is a TYPED enum owning the failure |
| Session active-capacity admission is service-local | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | The disputed "admission" is a global CONCURRENCY RESOURCE GATE, explicitly distinguished from lifecycle/machine authority, with a singular typed owner. staged_r |
| Skill repository credentials are skill-config-owned strings | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | see above |
| Staged session registry owns pending session lifecycle | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | In meerkat/src/staged_sessions.rs the lifecycle PHASE is exclusively owned by a per-slot generated machine authority. Each StagedSlot holds `authority: Generate |
| Synthetic runtime notices mutate prompt truth from projections | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | duplicate_field_intentionally_omitted |
| System-context append payload is text-only at surface seam | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The auditor's central narrowed claim — "the public append request remains text-only" — is positively disproven at the cited lines.  Cited owner type (meerkat-co |
| Token clear is not atomic with AuthMachine release | `ATOMICITY_SPLIT_COMMIT` | Low | see current_evidence field |
| Tool-call argument projection failure terminalizes through string error | `TERMINAL_FAULT_LAUNDERED` | Low | see above |
| WASM API-key bootstrap fabricates realm binding policy from provider strings | `SURFACE_OWNED_POLICY` | Low | see above |
| WASM bootstrap owns provider/model default policy at the surface | `SURFACE_OWNED_POLICY` | Low | Cited line 214 drifted (now `max_sessions` doc in `Credentials`). The model-write is at meerkat-web-runtime/src/lib.rs:1351 (`init_runtime`) and :1416 (`init_ru |
| WorkGraph tool/store authority is composed by factory fallback state | `SURFACE_OWNED_POLICY` | Low | Cited lines drifted; actual WorkGraph composition is at meerkat/src/factory.rs:4335-4399. Enablement: `let effective_workgraph = build_config.override_workgraph |
| Auto token store has split backend authority | `TWO_OWNERS_NO_DELETE` | Medium | see above |
| Blob file tools mint media-type/blob identity from strings | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | meerkat-tools/src/builtin/utility/blob_file.rs:41-48 declares `struct BlobLoadFileArgs { path: String, media_type: Option<String> }`. In `call()` (lines 406-435 |
| Compaction memory source identity remains turn-only shell metadata | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The auditor's three claims are all false in current code. (1) "Compacted memory entries still have no source-message/range handle" — FALSE. meerkat-core/src/mem |
| Control-plane lifecycle preflight/idempotence shadows DSL guards | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | meerkat-runtime/src/meerkat_machine/dispatch_session.rs:388-426 (RegisterSession) prechecks the projected phase BEFORE the machine: `if matches!(self.existing_s |
| Deferred tool catalog owner witnesses are string-derived | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The canonical typed owner already exists: `ToolProvenance { kind: ToolSourceKind, source_id: ToolSourceId }` (meerkat-core/src/types.rs:2387). Yet owner identit |
| Deferred tool-catalog control can accept loads through fallback visibility | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | meerkat-tools/src/control_plane.rs now fails closed without a bound ToolScope. `resolve_load_visibility` (lines 107-134) returns `Err(NoToolScope)` when no scop |
| Delegate helper wired truth is notification-owned | `TERMINAL_FAULT_LAUNDERED` | Medium | In current code wire_delegate_helper_to_creator returns wired truth from the two committed trust/edge mutations, not from notifications. mob_wire (helper->paren |
| Explicit semantic memory enable can silently produce no memory tool | `TERMINAL_FAULT_LAUNDERED` | Medium | The cited lines drifted; the actual memory-store wiring is at meerkat/src/factory.rs:4998-5046. On HnswMemoryStore::open failure the code now branches on the ex |
| External MCP tool content is silently narrowed before ToolResult | `TERMINAL_FAULT_LAUNDERED` | Medium | connection.rs:373-378 (and verbatim in protocol.rs:83-86, 111-114) launders the MCP tool-error fault into a fixed string, discarding the real error content bloc |
| Flow output parsing owns retry and failure truth | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | The retry-vs-terminal disposition for a completed member turn's parse outcome is now MobMachine-owned, not shell-decided. meerkat-mob/src/runtime/flow.rs:893-95 |
| Flow recovery re-derives scheduler truth | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | recovery.rs:172-181 `loop_is_pending_body_frame` re-derives the "pending body frame" fact from a weaker signal than its typed owner: `if snap.kernel_state.phase |
| Flow task trackers own active-run lifecycle beside MobMachine | `TWO_OWNERS_NO_DELETE` | Medium | The shell maps are explicitly demoted to non-semantic execution-resource registries and reconciled to the machine. meerkat-mob/src/runtime/actor.rs:1089-1107 do |
| Flow turn timeout/orphan disposition is executor-owned terminal policy | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | see structured field above |
| Handoff feedback bindings are name/existence-only | `STRING_SCANNER_GATE` | Medium | In meerkat-machine-schema/src/composition.rs the handoff feedback binding validation only checks names/existence: input field exists (UnknownHandoffFeedbackInpu |
| Helper surfaces allocate mob member identity at the surface | `IDENTITY_REDERIVED_IN_SHELL` | Medium | see above |
| Hook denial terminal policy is applied by the agent shell | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | Cited line 1081 drifted (now inside `push_transcript_append`/`ConversationAppendRole::SystemNotice`). Grepping for the RunStarted-vs-hook-veto behavior shows TW |
| Kickoff lifecycle member identity is string-owned | `IDENTITY_REDERIVED_IN_SHELL` | Medium | At meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:88-94 the kickoff lifecycle state is fully typed by AgentIdentity, not String: `member_kickoff_pending: |
| LLM-driven hidden tool denial still downgrades typed truth | `TERMINAL_FAULT_LAUNDERED` | Medium | see above |
| LLM-driven hidden tool denial still downgrades typed truth | `TERMINAL_FAULT_LAUNDERED` | Medium | see current_evidence field |
| Live adapter tool-call identity is provider-string authority | `IDENTITY_REDERIVED_IN_SHELL` | Medium | meerkat-core/src/live_adapter.rs:329-333 — the `ToolCallRequested` observation now carries TYPED identity: `provider_call_id: ToolCallId, tool_name: ToolName, a |
| Live interrupt intent is rederived from admission projection after handling-mode normalization | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | meerkat-rpc/src/session_runtime.rs:3957-3963 (current code) in `live_interrupt_requested_for_runtime_primitive`: ``` if primitive     .turn_metadata()     .and_ |
| Live open failure cleanup is best-effort after generated admission | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-rpc/src/handlers/live.rs:751-779 (`close_live_channel_after_open_failure`) is NOT best-effort. It reserves a close observation, calls `commit_live_close |
| Live terminal errors do not reach a session-owned terminal seam | `TERMINAL_FAULT_LAUNDERED` | Medium | The auditor's claim ("no canonical session terminal-error seam records the terminal cause for callers outside the live stream") is false in current code. A cano |
| Local bridge input provenance is session-string authority | `IDENTITY_REDERIVED_IN_SHELL` | Medium | meerkat-mob/src/runtime/local_bridge.rs:122-131 (HEAD 67905861671, lines drifted from cited 117/170): provenance is built from a TYPED runtime identity, not a ` |
| MCP contributed tool lifecycles default outside feature-owned descriptors | `SURFACE_OWNED_POLICY` | Medium | Base tools now own lifecycle as typed contract data co-located with name/description/schema: `pub struct BaseToolDescriptor { name, description, input_schema, r |
| MCP lifecycle mirror failures are swallowed after shell state changes | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-mcp/src/router.rs:1017-1038 `with_lifecycle_handle` calls `f(handle)` (the DSL apply) and on rejection does ONLY `tracing::debug!(error = %error, "McpSe |
| Memory search public contract loses owner/source handle | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | Code side: meerkat-core/src/memory.rs:84-102 defines the typed owner `MemorySource` (enum `Compaction { source_range: MessageRange }`) with `source_range()`. me |
| Missing image blobs degrade into prompt text instead of terminal failure | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-core/src/image_content.rs:8-17 defines `MissingBlobBehavior` with `#[default] HistoricalPlaceholder`. hydrate_content_blocks (image_content.rs:57-64) ma |
| Mob command admission shadows MobMachine guards | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | Cited lines no longer hold a shadow admission precheck. (1) Admission is machine-owned: actor.rs:5307 `fn probe_member_admission` drives `mob_dsl::MobMachineInp |
| Mob event and flow status surfaces erase typed contracts into JSON values | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The auditor's narrowed claim — public flow-status surface "still exposes `run: Value`" built from "serializing `MobRun`" — is false in current code. meerkat-con |
| Mob generation monotonicity is shell-owned | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | The shell no longer computes the next monotone generation; the MobMachine owns storage, checking, AND the increment.  Machine owns the counter and the monotone |
| OAuth flow state registry owns login lifecycle outside AuthMachine | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | The cited lines are the bare OAuthFlowRegistry payload store. meerkat-auth-core/src/oauth_flow.rs:1228-1264 (admit_device_code_with_pruned) inserts the device-c |
| OAuth provider identity keeps an alias-string taxonomy | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | see above |
| OpenAI live adapter stages explicit-commit text turns in provider-local queues before commit | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | duplicate_field_placeholder |
| OpenAI realtime websocket timeout classification parses error text | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-openai/src/live.rs:3293-3295 (cited line 2966 drifted; function map_openai_live_error now at 3268): `OpenAiLiveError::WebSocket(error) => LlmError::Netw |
| Orchestrator lifecycle delivery is best-effort projection work | `TERMINAL_FAULT_LAUNDERED` | Medium | The cited lines drifted; the live delivery routine is `notify_orchestrator_lifecycle` in meerkat-mob/src/runtime/actor.rs:4949. Its doc (4935) states: "This is |
| Owner-provided route bindings bypass typed handoff validation | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | composition.rs:1442-1471 fails closed (covered = handoff_protocol OR transaction_plan, else OwnerProvidedTypeMismatch). WorkGraph route work_item_close_stops_at |
| Peer ingress request semantics are intent-string classified | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | meerkat-runtime/src/handles/peer_comms.rs:171-179 hand-maps raw intent strings to PeerIngressRequestClass in the shell handle; meerkat-machine-schema/src/catalo |
| Peer lifecycle subject ignores typed peer_spec | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The disputed fact is the identity of the lifecycle SUBJECT peer (which peer was added/retired/unwired). The contract declares the typed owner explicitly. meerka |
| Per-turn skill activation failures are swallowed after consuming typed refs | `TERMINAL_FAULT_LAUNDERED` | Medium | see current_evidence field |
| Persistent session event projection is best-effort after session behavior succeeds | `TERMINAL_FAULT_LAUNDERED` | Medium | At the cited site the durable append is NO LONGER swallowed. `append_and_project_event` (meerkat-session/src/persistent.rs:124-149) now returns `Result<u64, Ses |
| Plain event ingress body and fault semantics are transport-local | `TERMINAL_FAULT_LAUNDERED` | Medium | Both halves of the claimed violation have a typed owner in current code, with positive proof at file:line.  BODY CONTRACT (no body-field auto-detection): meerka |
| Provider params and tool defaults remain JSON semantic bags | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | config.rs:779 `pub provider_params: Option<serde_json::Value>` and config.rs:791 `pub provider_tool_defaults: Option<serde_json::Value>` — both persisted/config |
| Provider params and tool defaults remain JSON semantic bags | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | Provider knobs are owned as raw JSON `serde_json::Value` at the config/build/identity seams, merge-patched as untyped JSON in the agent loop, and only projected |
| Public contract-version docs are stale mirrors | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | Canonical owner: meerkat-contracts/src/version.rs:22-27 — `ContractVersion::CURRENT = {major:0, minor:7, patch:0}` + `PRERELEASE = Some("alpha.0")`, source-of-t |
| REST OpenAPI schema emission owns manual shadow wire contracts | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | meerkat-contracts/src/emit.rs:772-1175 `rest_manual_components()` hand-builds REST request/response schemas via `object_schema(...)`/`closed_object_schema(...)` |
| RPC catalog still leaves config/capability/skills contracts schema-light | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | Cited line numbers drifted but the symbols are present and the violation holds in current code.  CATALOG IS SCHEMA-LIGHT (meerkat-contracts/src/rpc_catalog.rs): |
| Remote skill caches can outlive source authority | `TERMINAL_FAULT_LAUNDERED` | Medium | See above. |
| Resolved model capabilities are surface-rederived and SDK-defaulted | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | HEAD bca3c6ba2. Both cited lines have drifted and the disputed behavior is gone.  (1) Web SDK sdks/web/src/mob.ts:238 — line 238 is now inside `parseResolvedMod |
| Roster replay fabricates nil bridge session | `IDENTITY_REDERIVED_IN_SHELL` | Medium | meerkat-mob/src/roster.rs:184-216 (MemberSpawned projection arm) FAILS CLOSED on absent ref, the opposite of the claim: ``` MobEventKind::MemberSpawned(member_s |
| Run boundary receipt sequence bypasses machine boundary counter | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | See current_evidence field above. |
| Runtime admission policy is still inferred in shell paths | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | policy_table.rs:53 `generated_admission_projection_for_input` is now a pure projection facade: it threads typed input facts into the machine and reads back the |
| Runtime binding helpers collapse control errors | `TERMINAL_FAULT_LAUNDERED` | Medium | checked |
| Runtime keep_alive override collapses false/preserve into true-only persistence policy | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | Canonical owner of the persisted fact is `SessionMetadata.keep_alive: bool` (meerkat-core/src/session.rs:3782). PR #759 added a typed tri-state owner, `RuntimeK |
| SDK JSON-RPC error parsers recover typed errors from message strings | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | TypeScript `parseRpcErrorPayload` (sdks/typescript/src/client.ts:3104-3138) first reads structured `error.data` (lines 3109-3117), then FALLS BACK to parsing th |
| SDK codegen uses hand-curated contract rosters | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | tools/sdk-codegen/generate.py defines static, hand-maintained Python list literals of contract type names: MOB_RPC_CONTRACT_TYPES (lines 17-105), COMMS_SESSION_ |
| SDK event subscription close is client-local before stream-close authority | `TERMINAL_FAULT_LAUNDERED` | Medium | TS sdks/typescript/src/subscription.ts:38-43 `async close()`: `if (this.closed) return; this.closed = true; this.queue.put(null); await this.closeRemote(this.st |
| SDK wrapper freshness is substring parity | `STRING_SCANNER_GATE` | Medium | scripts/verify_sdk_wrapper_freshness.py (HEAD 67905861) is a live CI/release gate (wired into Makefile `ci`/`ci-smoke` line 328/333, scripts/release-hook.sh:100 |
| Session-create default model policy is surface-owned | `SURFACE_OWNED_POLICY` | Medium | A single canonical owner exists — `meerkat/src/factory.rs:985 pub fn resolve_create_session_default_model(config: &Config) -> String` — a 3-tier ladder (config. |
| SessionProjector partial writes can duplicate derived event files | `ATOMICITY_SPLIT_COMMIT` | Medium | meerkat-session/src/projector.rs:145-149 writes the checkpoint LAST in project_with_mode (after events.jsonl at 112-128 and summary.txt at 132-143), so the part |
| Skill function dispatch is string/JSON-owned | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | Skill IDENTITY is rigorously typed (parse-at-ingress newtypes), but the in-skill FUNCTION selector is a raw string with no typed owner across the entire dispatc |
| Staged callback tool results bypass turn authority | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | see above |
| TokenStore failures become ephemeral or missing auth truth | `TERMINAL_FAULT_LAUNDERED` | Medium | Both cited lines now fail closed with typed-fault propagation, and carry explicit anti-laundering remediation comments.  meerkat-rest/src/lib.rs:474-480 (produc |
| Tool execution policy is config-only | `SURFACE_OWNED_POLICY` | Medium | see current_evidence field |
| Tool schema generation fails open to null schemas | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-tools/src/schema.rs:6-14 (current) no longer has any fail-open path: ```rust pub fn schema_for<T: JsonSchema>() -> Value {     let schema = schemars::sc |
| Tool timeout ownership is split across core, dispatcher, and shell | `TWO_OWNERS_NO_DELETE` | Medium | A single typed owner `ToolTimeoutPolicy` now governs the default per-call tool timeout, and all three cited sites READ from it (no divergent literals).  meerkat |
| WASM mob event cursors narrow and reset stream position | `TERMINAL_FAULT_LAUNDERED` | Medium | see current_evidence field |
| WASM mobpack initialization discards parsed mobpack/trust semantics | `TWO_OWNERS_NO_DELETE` | Medium | Cited line meerkat-web-runtime/src/lib.rs:1104 has drifted (it is now `runtime_version()`). The actual mobpack-initialization bootstrap is `init_runtime` (line |
| Web SDK lacks generated non-event wire parity | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | The web SDK codegen pipeline generates ONLY three categories — events, auth, mob — into sdks/web/src/generated/ (tools/sdk-codegen/generate.py:3120-3128; direct |
| Web-search fallback reports unverified completion | `TERMINAL_FAULT_LAUNDERED` | Medium | meerkat-openai/src/web_search.rs no longer reports Completed from any LLM response. The terminal completion fact now fails closed on a typed witness. Lines 122- |
| WorkGraph read SDK parity is handwritten outside generated contracts | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | The codegen pipeline emits canonical WorkGraph contract types into the generated dir and schema artifacts: tools/sdk-codegen/generate.py:178-209 lists WORKGRAPH |

### NARROWED_PARTIAL (49 rows)

| Row | Root shape | Sev | What remains |
| --- | --- | --- | --- |
| BuildBuddy change detection skips governance-audit source changes | `STRING_SCANNER_GATE` | Low | Both gate scripts are path-string `case` matchers. The governance ratchets (seam-inventory, rmat-audit, machine-authority) run ONLY in buildbuddy.yml's `governa |
| Command credential source caches subprocess tokens outside AuthMachine | `TWO_OWNERS_NO_DELETE` | Low | meerkat-auth-core/src/auth_store/command.rs:47-50 — `CommandCredentialRunner { spec, cache: Mutex<Option<(PersistedTokens, Instant)>> }`; spec carries `refresh_ |
| Durable event replay drops canonical stream envelope identity | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The auditor's PRIMARY claim ("durable event stores persist only the AgentEvent payload, replay reconstructs compatibility envelope metadata") is STALE at the du |
| Empty assistant-output classification is shell-owned | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | The cited line numbers drifted (types.rs:563 is now the TranscriptSource doc; state.rs:1904 is mid-hook code). The real code: the EMPTY-OUTPUT TERMINAL VERDICT |
| Flow context expressions fail open into control truth | `TERMINAL_FAULT_LAUNDERED` | Low | See current_evidence field. |
| Flow terminal status and event commit split | `ATOMICITY_SPLIT_COMMIT` | Low | terminalize_with_machine_state (flow.rs:1820-1836) commits in two non-atomic steps against two distinct stores: `self.run_store.cas_run_snapshot_with_authority( |
| Live WebRTC signaling failures are string-only | `TERMINAL_FAULT_LAUNDERED` | Low | The title's "string-only signaling" claim is stale. `meerkat-live/src/webrtc.rs:126-175` defines `LiveWebrtcError` as a typed `#[non_exhaustive]` enum with per- |
| Machine posters are partial stale un-gated generated artifacts | `OTHER` | Low | Coverage is partial: scripts/machine-posters/generate-machine-posters.mjs:36 `const MACHINE_SPECS = [` has exactly 2 entries (id "meerkat_machine" L37-39, id "m |
| Mobpack/WASM capability manifest shape remains stringly | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The cited lines 28/34 are now the TYPED parse/id machinery (added in b56ba1bab): manifest.rs:17-20 `pub struct MobpackCapability { token: String, id: MobpackCap |
| Model capability value domains are string-owned | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | The row names two value domains: "effort" and "beta".  EFFORT — FIXED (typed-owned). `EffortLevel` is a typed enum (meerkat-core/src/model_profile/capabilities. |
| Peer ingress machine signal discards canonical peer id | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | At meerkat-runtime/src/handles/peer_comms.rs:242-252 the signal builder constructs `MeerkatMachineSignal::ClassifyExternalEnvelope { item_id: ..., from_peer: fa |
| Per-turn skill activation success is folded into prompt text | `STRING_JSON_FOR_TYPED_DISTINCTION` | Low | runner.rs `apply_skill_ref` (meerkat-core/src/agent/runner.rs:1392-1473) renders activated skills and accumulates their bodies as raw strings: `prefix_parts.pus |
| Post-turn persistence and checkpoint failures are non-terminal | `TERMINAL_FAULT_LAUNDERED` | Low | Two prongs.  PRONG 1 — "store.save failures warned then Ok(RunResult) continues" is now design-sanctioned. The intra-loop save (meerkat-core/src/agent/state.rs: |
| System-context appends are marked applied before turn terminality | `TERMINAL_FAULT_LAUNDERED` | Low | Two cited locations; they split.  PART A (runner.rs, cited :725 — drifted; real code is the staging consumer at runner.rs:784-810): `take_pending_system_context |
| Token persisted-auth mode is inferred from auth_method strings | `SHELL_SHADOW_OF_MACHINE_FACT` | Low | Both CITED locations are stale/misread in current code:  (1) meerkat-auth-core/src/auth_store/mod.rs:41 is the doc comment for `persisted_auth_mode_for_auth_met |
| WorkGraph mutating tool contracts bypass generated governance | `SURFACE_HANDMIRROR_OF_CONTRACT` | Low | meerkat-workgraph/src/tools.rs:70-102 defines `enum WorkGraphToolContract { Create, Get, List, Ready, Snapshot, Events, Claim, Release, Update, Block, Close, Li |
| Auth test surfaces mutate credential freshness and lease truth | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | REST `test_auth_binding` (meerkat-rest/src/auth_endpoints.rs:918-987) builds the resolve env at 925-927 with `.with_token_store(Arc::clone(&state.token_store)). |
| Completion events can precede terminal boundary authority | `ATOMICITY_SPLIT_COMMIT` | Medium | Cited lines 2589/2593 drifted into `complete_extraction_failed`, but the described behavior is live two places. NON-EXTRACTION path: state.rs:2883 `emit_event!( |
| Composition consumer refusals collapse typed machine errors to strings | `TERMINAL_FAULT_LAUNDERED` | Medium | The two parts of the auditor's claim that the contract types are stringly are now FIXED: consumer surfaces return `Result<(), ConsumerError>` (composition/mod.r |
| Connection/auth wire contracts keep provider semantics stringly | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | meerkat-contracts/src/wire/connection.rs cited lines: line 84 `LoginStartParams { pub provider: String, ... }` (ingress request) and line 181 `WireAuthProfile { |
| Core comms command path downgrades typed request contracts | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | At the cited contracts seam `meerkat-contracts/src/wire/comms.rs`, `CommsCommandRequest::PeerRequest` (line 211-221) carries TYPED `intent: CommsPeerRequestInte |
| Direct session surfaces own model default policy | `SURFACE_OWNED_POLICY` | Medium | The row is a conjunction; the RPC half is fixed, the REST half is still live.  RPC half (STALE_FIXED): The cited meerkat-rpc/src/session_runtime.rs:5709 is now |
| External dispatchers bypass typed tool-argument contract | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | Typed owner `ToolCallArguments` (meerkat-core/src/event.rs:189-235) enforces object-shape at ingress: `from_value` returns `Err(ToolCallArgumentsError)` unless |
| Factory/provider-runtime errors stringify typed auth/provider failures | `TERMINAL_FAULT_LAUNDERED` | Medium | see above |
| Flow terminalization erases failure cause into strings | `TERMINAL_FAULT_LAUNDERED` | Medium | A typed enum `FlowFailureCause` now exists (meerkat-mob/src/runtime/terminalization.rs:16-28) and `TerminalizationTarget::Failed { cause: FlowFailureCause }` is |
| Hook denial terminal policy is applied by the agent shell | `SHELL_SHADOW_OF_MACHINE_FACT` | Medium | The cited line meerkat-core/src/agent/runner.rs:1081 has drifted to unrelated SystemNotice append code; the real symbols are run_started_hooks / emit_run_starte |
| JSONL session index can lag snapshot truth | `ATOMICITY_SPLIT_COMMIT` | Medium | meerkat-store/src/jsonl.rs save_impl performs three sequential durable commits with no overarching atomicity: (1) line 318-325 writes `.jsonl` (canonical conten |
| Live config propagation is best-effort channel policy | `TERMINAL_FAULT_LAUNDERED` | Medium | The "logs or skips failures" framing is partly stale: per-channel/per-session outcomes now fold into a typed `#[must_use] LiveConfigPropagationReport` (live_orc |
| Live snapshot prompt truth is inferred from seed history | `TWO_OWNERS_NO_DELETE` | Medium | The auditor's premise is now stale: `RealtimeSessionOpenConfig` DOES carry a typed `system_prompt: Option<String>` (meerkat-llm-core/src/realtime_session.rs:240 |
| MCP OAuth token lifecycle bypasses realm/AuthMachine authority | `ATOMICITY_SPLIT_COMMIT` | Medium | meerkat-auth-core/src/mcp_oauth.rs is the cited file (git HEAD bca3c6ba2). Of the row's three sub-claims, two are addressed and one is a live residual:  (1) "ta |
| Machine drift ratchets use source-string governance scanners | `STRING_SCANNER_GATE` | Medium | The two cited gate functions are still wired into the live machine_check_drift ratchet (xtask/src/machines.rs:270-271). The Mob command gate (collect_mob_runtim |
| Machine drift ratchets use source-string governance scanners | `STRING_SCANNER_GATE` | Medium | Both gates the row names are still wired into the `machine-check-drift` ratchet (xtask/src/machines.rs:270-271 inside `machine_check_drift`).  MOB COMMAND GATE |
| Mob seam uses hand-authored generated-shape mirrors | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | The generated artifact is real and gated: meerkat-runtime/src/generated/meerkat_mob_seam.rs is "@generated ... Emitted by meerkat_machine_codegen::render_compos |
| Model catalog public contracts advertise stale inference/catalog semantics | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | Canonical owner of provider-resolution + catalog semantics is meerkat-core's ModelRegistry (config-backed, exact-catalog-match, fail-closed): - meerkat-core/src |
| OAuth provider policy is duplicated across auth core and provider runtimes | `TWO_OWNERS_NO_DELETE` | Medium | A single canonical owner now exists: meerkat-auth-core/src/oauth_flow.rs:92-164 defines `OAuthProviderDeclaration` and `oauth_provider_declaration(id)` holding |
| Per-turn tool overlay is shell-owned string policy | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | Staging genuinely routes through the generated MeerkatMachine (so this is no longer a pure shell-owned policy), but the residual payload-typing hole the auditor |
| Provider params and tool defaults remain JSON semantic bags | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | The disputed fact (provider knobs: temperature/top_p/reasoning/thinking-budget/web-search/provider-tag) is still carried as serde_json::Value at the durable con |
| Public mob MCP/RPC contracts own status/profile/snapshot shape by hand | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | Typed owner + canonical projection exist but the cited status/list surfaces bypass them with stringly json! literals.  Canonical typed owner: meerkat-contracts/ |
| Python SDK mob collection helpers collapse malformed envelopes to empty success | `TERMINAL_FAULT_LAUNDERED` | Medium | The fail-closed canonical helper exists: `_require_list_field` (client.py:3521-3536) raises `MeerkatError("INVALID_RESPONSE", ...)` when a wire field is "presen |
| Runtime effect-channel closure exits loop without terminalization | `TERMINAL_FAULT_LAUNDERED` | Medium | See current_evidence field above (quoted current code at runtime_loop.rs:903-936, 1021-1036, 1267-1281, 382-437 and control_plane.rs:15-43, 69-86, 90-128). |
| Schedule and occurrence identities are string-owned in canonical DSL | `STRING_JSON_FOR_TYPED_DISTINCTION` | Medium | See above. |
| Skill diagnostics collection fails open | `TERMINAL_FAULT_LAUNDERED` | Medium | see current_evidence field above |
| Skills availability is advertised by static surface catalogs | `SURFACE_OWNED_POLICY` | Medium | Canonical owner of "skills runtime available" is `skill_runtime.is_some()`. The RPC arm of the claim is FIXED: router.rs:1527 passes `self.skill_runtime.is_some |
| Supervisor escalation is prompt-timeout shell policy | `TERMINAL_FAULT_LAUNDERED` | Medium | The escalation flow has been substantially refactored to route through MobMachine, so the original claim is only partly true. CLOSED parts: presence/absence is |
| Surface serialization failures become success fallbacks | `TERMINAL_FAULT_LAUNDERED` | Medium | REST SSE event stream still launders authoritative typed-event serialization faults into fabricated success-shaped payloads. meerkat-rest/src/lib.rs:6294-6303 ( |
| Verified mobpack trust is not a typed archive contract | `TWO_OWNERS_NO_DELETE` | Medium | meerkat-mob-pack/src/archive.rs:18-30 — `MobpackArchive` still carries `manifest/definition/skills/hooks/mcp/deploy_policy` with NO digest and NO trust witness. |
| WASM subscription lag is a handwritten in-band fault contract | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | Line numbers drifted; verified by symbol grep. (1) Rust emission, meerkat-web-runtime/src/lib.rs:2731-2733: `Err(TryRecvError::Lagged(n)) => { tracing::warn!(sk |
| Web SDK event parsers bypass generated event contracts | `SURFACE_HANDMIRROR_OF_CONTRACT` | Medium | Both cited lines drifted off the violation. `sdks/web/src/session.ts:37` is now a doc comment; the actual direct-session parser `normalizeSessionEvent` (session |
| WebRTC output-audio delivery truth is transport-local | `TERMINAL_FAULT_LAUNDERED` | Medium | The auditor's claim has two halves; one is fixed, one is NOT.  FIXED half (VAD barge-in): webrtc.rs:597-616 lowers a local VAD barge-in into the canonical inter |

---

## Remediation: gates-first keystone plan (operate on roots, validate against these 307)

_The 183 active rows are a **validation set**, not work targets. They cluster into root shapes fixed by a small set of keystones; gates land first so closure cannot drift back (the failure mode that turned 357→307 with +37k lines)._

### Execution order

**1. CI Gate Suite (ratchet-first): typed-carrier-adoption AST gate + LaunderedFaultAtSeam rmat rule + rerun-and-diff parity lanes**  _(≈7 members)_

> GATES FIRST. The prior +37k-line campaign failed because owners were minted but nothing FORBADE re-introducing the parallel untyped twin or re-laundering a typed fault, so the audit re-found the same shape under new file:lines. Installing the enforcement ratchets BEFORE touching members is what makes every subsequent fold permanent: each member is then closed by an asserted INVARIANT the gate guards, not a one-off edit. These gates are pure ADD (~400 audit/test lines, 0 production) and unblock verification of all later keystones. Two registries (typed-carrier and laundered-fault) plus the existing rerun-and-diff parity lanes (schema-freshness, rest/rpc surface alignment, sdk-codegen) form the anti-drift wall.

**2. HIGH/active fold: WorkGraph lifecycle authority + inproc peer namespace identity (genuinely-active shadow/identity rebuilds)**  _(≈3 members)_

> HIGHs before mechanical clusters. Two High rows survive independent verification as genuinely-active: (1) WorkGraph store.rs:464 — confirmed present validate_item_projection + revision CAS, but the audit splits CONFIRMED_ACTIVE vs STALE_FIXED on whether lifecycle truth is ever rebuilt from item-field aggregation rather than read off WorkItem.machine_state; lock the read-seam invariant so projections can never reconstruct authority. (2) inproc.rs:391-399 / router.rs:557-563 — peer address+namespace re-derived in shell during trusted sends; fail-closed namespace gate must own identity. The third High (comms pairing trust) folds into AuthMachine/apply_trust_mutation and is verified CLOSED-AT-HEAD, downgraded to a ratchet assert. Doing the active HIGHs early keeps the cluster's hardest authority seams from drifting under the later mechanical churn.

**3. Compensating-Boundary fold (ATOMICITY_SPLIT_COMMIT): adopt AuthMachine lease + replay/reconcile owners; close NARROWED_PARTIAL flow split**  _(≈7 members)_

> Hard-ish fold before pure mechanical. Both canonical owners already exist (auth/lifecycle.rs clear_tokens_and_publish_lifecycle_released + restore_token_lifecycle_snapshot; canonical event-store + SessionProjector::resume->replay + open_index reconcile). ~90% folded; residuals are tiny: (a) mcp_oauth.rs:368 bare token_store.save AFTER lease mint not snapshot-compensated; (b) NARROWED_PARTIAL flow split CONFIRMED at terminalization.rs:107 — record_persisted_terminalization forward path uses .append() not .append_terminal_event_if_absent() (the idempotent method repair already uses), so a crash between CAS and append cannot self-heal; route forward append through the existing idempotent method (~3 lines). Net-DELETE (remove duplicated rollback prose + CLI hand-rolled clear+release branch). Ordered before the big mechanical cluster because it shares the auth-wire surface that the carrier fold also touches.

**4. Carrier-Adoption Fold (mechanical): thread existing typed owners through config/wire/persistence carriers; delete parallel String/JSON fields**  _(≈35 members)_

> Largest mechanical cluster, ordered after gates+HIGHs. ProviderParamsOverride owner CONFIRMED present at run_primitive.rs:948 with from/to_legacy adapters AND a merge() at :1555 — yet config.rs:779/791 still carry Option<serde_json::Value> and state.rs:151/171/1944 still run the JSON merge-patch shadow. The fold is net-DELETE type-substitution (no new owners), executed in dependency order (provider params -> turn override -> recovery enum -> provider wire identity -> peer ingress -> remaining newtypes), guarded by the Step-1 typed-carrier gate. ~100 downstream field-access sites are offloadable to a codemob gemini-flash-lite pack. Largest member count so it absorbs the most churn last when the ratchets are already enforcing closure.

**5. SHELL_SHADOW_OF_MACHINE_FACT: no-op verification ratchet (cluster already folded at HEAD)**  _(≈28 members)_

> Verification-only, last among folds. All 28 cited facts already route through generated machine owners at HEAD (MeerkatMachine/MobMachine/WorkGraphMachine/AuthMachine/GeneratedStagedSessionAuthority); the audit evidence itself reads 'CLOSED'/'fully replaces former shell-side'. Zero production source change — the keystone is ForbiddenShadowPrecheck AST rules + invariant asserts in existing tests so the fold cannot be re-opened and re-patched (the exact +37k failure mode). 3 rows mistagged (OAuthFlowRegistry handshake-store, budget resource-meter, OpenAI transport queue) move out of cluster. Last because it gates nothing else and is the cheapest to verify once the ratchet harness from keystone 1 exists.

**6. TERMINAL_FAULT_LAUNDERED: drain ~10 live residual seams onto existing typed owners (gate from keystone 1 enforces the rest)**  _(≈46 members)_

> Mechanical drain, last. The owners ALL exist (turn_execution_authority.rs, AgentError::TerminalFailure, FlowFailureCause, McpError::ToolCallFailed, etc.); ~30 of 46 rows already read FIXED/stale. The LaunderedFaultAtSeam gate (keystone 1) closes the cluster permanently; this keystone only drains the confirmed-live seams: REST SSE lib.rs:6295 to_value(...).unwrap_or_else(\|_\|{type:unknown}) + to_string(...).unwrap_or_default(); MCP protocol.rs:86/114 fixed 'Tool returned error' string; protocol.rs:147 _ => None content narrowing; mob runtime effect-channel closure terminalization; SDK malformed-envelope coalesce. Net-DELETE (fabricated fallbacks). Last because it is the most numerous, most-already-fixed cluster and depends entirely on the keystone-1 gate to stay closed.

### CI gate suite (lands first — makes closure permanent)

| Gate | Ratchets | Replaces |
| --- | --- | --- |
| verify_typed_carrier_adoption.py (RMAT-style AST rule, wired into make ci alongside rmat-audit) | Asserts the RESOLVED TYPE of each registered carrier field, not absence-of-string. Enumerated (struct, field, owner) registry: AgentConfig.provider_params -> Option<ProviderParamsOverride>; SessionBuildOptions/AgentBuildConfig/SessionMetadata.provider_params -> Option<ProviderParamsOverride>; LoginStartParams/WireAuthProfile/WireBackendProfile.provider -> meerkat_core::Provider; SurfaceSessionReco | Replaces ad-hoc greps for serde_json::Value/String fields named provider_params/provider/intent/status/profile/source_id; replaces the from_legacy_provider_value/to_legacy_provider_value round-trip + state.rs merge_provider_param_patch/merged_provider_params shadow scanners (those functions' deletio |
| LaunderedFaultAtSeam (new AST rule in the existing rmat-audit harness, sibling of ForbiddenShellAuthorityReads) | Flags in non-test code: .ok()/.ok().flatten()/.unwrap_or_default()/.unwrap_or(false)/.unwrap_or_else(\|_\|<literal>) applied to a Result<_,E> where E is a registered fault type (AgentError, SessionError, ToolError, McpError, LlmProviderError incl StreamParseError, MemoryStoreError, FlowFailureCause-bearing, OperationTerminalOutcome); serde_json::to_value/to_string(&TypedEvent).unwrap_or_* fabricat | Replaces recurring greps for unwrap_or_default/.ok()/format!("{e}")-into-fixed-string and the fabricated {"type":"unknown"} json! / fixed "Tool returned error" literal scanners; replaces per-PR manual review that re-finds the same launder shape under new file:lines. |
| ForbiddenShadowPrecheck (new AST rule in rmat-audit, guards SHELL_SHADOW_OF_MACHINE_FACT) | Rejects re-introduction of folded shadows: a matches!(existing_session_runtime_state(..), Some(Destroyed)) precheck NOT immediately followed by a machine stage (guard-then-stage at dispatch_session.rs:388-426 is the allowed shape); pending_spawns.contains_member / snapshot.generation.next() recomputation outside MobMachine; a shell-local boundary sequence counter not sourced from LiveBoundaryConte | Replaces greps for pending_spawns.contains_member, snapshot.generation.next(), shell `+= 1` receipt counters, and is.empty() shell terminal-decision scanners; replaces the per-row manual re-confirmation that these facts are still machine-owned. |
| make verify-schema-freshness + verify_rest_surface_alignment.py + verify_rpc_surface_alignment.py + verify-sdk-codegen-freshness (existing rerun-and-diff parity lanes) | After the provider-wire-identity fold (String->typed Provider) and any contracts change, regenerated SDK unions + REST OpenAPI + RPC method catalog must match committed artifacts. Ratchets that the typed Provider wire change propagated through artifacts/schemas, sdks/python/meerkat/generated, sdks/typescript/src/generated without drift, and that REST/RPC advertised surfaces match the generated cat | Replaces manual diffing of generated schema/SDK files against hand-edited wire types; replaces greptests for .as_str().to_string() provider wire downgrades by proving the regenerated artifact is the typed union. |
| rmat-audit ForbiddenShellAuthorityReads (existing read-seam gate) + new trust/work-graph read-seam asserts in existing unit tests | For the two HIGH rows: trust mutations only via apply_trust_mutation with a validated GeneratedTrustAuthority owner (comms_runtime.rs:443-493); work-graph CAS only via WorkGraphMachine::validate_item_projection + machine_state.revision (store.rs:464). Asserts lifecycle authority is read off WorkItem.machine_state, never reconstructed from item-field aggregation. | Replaces greps for direct store-write paths that bypass validate_item_projection and for trust-row inserts bypassing authority owner validation. |
| Carrier-adoption deletion invariant asserts (unit tests in run_primitive.rs / session_recovery.rs / meerkat_machine.rs schema) | ProviderParamsOverride::merge round-trips a thinking_budget+web_search merge and checks TYPED fields (not a JSON object), proving the JSON merge-patch is gone; asserts no fn merged_provider_params/merge_provider_param_patch exists; asserts SurfaceSessionRecoveryContext has one RuntimeBuildModeRequest field and no require_runtime_build_mode; routes a PeerIngressEnvelopeKind::Request{intent:"mob.pee | Replaces structural greps for the deleted shadow functions/fields with executable parse-or-reject boundary tests. |
| Atomicity compensating-boundary fault-injection asserts (unit tests in lifecycle.rs / projector.rs / jsonl session_index.rs / hnsw.rs / terminalization.rs) | Inject store.clear failure in clear_tokens_and_publish_lifecycle_released and assert lease phase != Released (restore ran, no lease-without-token); delete SQLite index file with .meta sidecars and assert list count == sidecar count; write partial events.jsonl with no checkpoint and assert resume->replay yields no duplicate rows; inject mid-batch HNSW insert failure and assert search returns zero d | Replaces per-site rollback doc-comment review and greps for bare store.save/store.clear outside the lease helper with crash-window fault-injection tests. |

### Keystone designs (per root shape)

#### Carrier-Adoption Fold: thread existing typed owners through config/wire/persistence carriers and delete the parallel String/JSON fields  — _mechanical_
- **Canonical owner:** No NEW machine. The keystone ADOPTS owners that already exist and makes them the SOLE carrier end-to-end, deleting the parallel untyped fields. Per sub-domain the single owner is: (1) provider knobs -> `meerkat_core::lifecycle::run_primitive::ProviderParamsOverride` (+ `ProviderTag`), with a NEW typ
- **Delete:add shape:** Net-DELETE. This fold removes parallel carriers and hand-written conversion/merge shadow logic, not adds wrappers (the owners already exist from the failed +37k campaign). Concrete deletions: `merge_provider_param_patch` + `merged_provider_params` (~30 lines of JSON merge-patch shadow, state.rs:151-
- **Members:** 35  ·  **Mistagged (move out):** 8
- **Fix:** MECHANICAL carrier-adoption fold, executed in dependency order, net-deleting carriers. Step 1 (provider params, the dominant unit, ~3 Medium + several Low rows): change the field type to the existing typed owner at every carrier — `meerkat-core/src/config.rs:779` `provider_params: Option<ProviderParamsOverride>` and delete `:790 provider_tool_defaults: Option<serde_json::Value>` (re-express tool defaults as `Option<ProviderParamsOverride>` populated from `Config.provider_tools` + `ModelProfile.supports_web_search`); `meerkat-core/src/service/mod.rs:223` `SessionBuildOptions.provider_params: Op
- **Independent verification:** Each member is proven closed by an INVARIANT at a file:line (not a grep), enforced by the Step-7 CI gate `scripts/verify_typed_carrier_adoption.py` which asserts the carrier field's RESOLVED TYPE, not its absence-of-string. Concretely: (1) provider params — assert `meerkat-core/src/config.rs` AgentConfig.provider_params resolves to `Option<ProviderParamsOverride>` (AST type check) AND that `meerka

#### Keystone: SHELL_SHADOW_OF_MACHINE_FACT cluster is already FOLDED — adopt the existing generated-machine owners and close the cluster as a no-op verification ratchet, not a refactor  — _mechanical_
- **Canonical owner:** No NEW machine is needed — the canonical owners already exist and are already wired. Per fact: (1) session register/preflight/idempotence/recovery/empty-output/boundary-receipt/live-interrupt → the generated MeerkatMachine (meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs) via inputs Regist
- **Delete:add shape:** Net ZERO source lines for the members (they are folded), plus a small net-ADD ONLY in the audit/CI ratchet (~1 AST rule file + a handful of invariant asserts in existing tests, low hundreds of lines). The DELETE that defines a true fold has ALREADY occurred in prior commits — the shell prechecks/cou
- **Members:** 27  ·  **Mistagged (move out):** 3
- **Fix:** There is NO source change to make for the listed members — they are already folded at HEAD. The keystone is the CLOSURE-VERIFICATION + ANTI-REGRESSION ratchet that makes the fold permanent and proves the cluster is dead, so it cannot be re-opened and re-patched (the failure mode). Concrete steps: (1) Mark all 28 rows CLOSED-AT-HEAD in the dogma ledger with the exact owner+effect citation I list under verification. (2) Add one AST/grep CI gate per owner-seam that FAILS on the re-introduction of a shadow, not on its current absence — e.g. a ForbiddenShadowPrecheck rule (sibling of the existing F
- **Independent verification:** INDEPENDENT invariant per member (file:line at HEAD, an assertion not a grep): (1) dispatch_session.rs:392-425 — the Destroyed precheck is a fail-closed guard that, on non-Destroyed, ALWAYS proceeds to stage_session_dsl_input(RegisterSession); invariant: there is no admission branch that returns Ok without either the machine staging or a machine-recovered Destroyed terminal. (2) actor.rs:5307-5333

#### Keystone: Compensating-Boundary Authority for split durable commits — adopt the existing AuthMachine lifecycle-publish pattern + replay-reconcile pattern as the single owner; delete shadow rollback prose  — _mechanical_
- **Canonical owner:** Two existing owners, NOT new machines: (A) Auth/credential subcluster: `GeneratedAuthLeaseHandle` (AuthMachine) + the lifecycle-publish/compensate helpers in `meerkat-core/src/auth/lifecycle.rs` (`clear_tokens_and_publish_lifecycle_released`, `mark_tokens_lifecycle_published_for_transition`, `restor
- **Delete:add shape:** Net-DELETE. A real fold removes shadow truth: this keystone deletes the duplicated per-site rollback narratives and the hand-rolled clear+release branches that re-implement what `meerkat-core/src/auth/lifecycle.rs` and the replay/reconcile seams already own. Expected shape roughly delete:add = ~120:
- **Members:** 7  ·  **Mistagged (move out):** 2
- **Fix:** MECHANICAL adopt-existing-owner + delete-shadow, not new engineering. Concrete steps:  SUBCLUSTER A (auth — already ~90% folded; close the residual + delete duplicate prose): 1. `meerkat-core/src/auth/lifecycle.rs`: this file is already the owner. No new code. Verify every public token-mutation entrypoint routes through it. The only residual is ensuring no surface calls `store.save(&key, raw_tokens)` or `store.clear(&key)` WITHOUT a paired AuthMachine lease transition + restore snapshot. 2. `meerkat-cli/src/main.rs`: `interactive_logout` (5680-5686) already calls `clear_tokens_and_publish_life
- **Independent verification:** Per-member INDEPENDENT invariant (file:line, not grep): 1. Token clear atomic: write a test that injects `store.clear` failure inside `clear_tokens_and_publish_lifecycle_released` (lifecycle.rs:286) and asserts post-call `handle.snapshot(&lease_key).phase != Released` (restore ran) — i.e. no lease-without-token. Invariant lives at lifecycle.rs:288 (`restore_token_lifecycle_snapshot`). 2. JSONL ind

#### TERMINAL_FAULT_LAUNDERED keystone: a single anti-laundering AST gate + drain-the-residuals, NOT another typed enum  — _mechanical_
- **Canonical owner:** No NEW machine. The canonical owners already exist and the prior campaign already minted them: meerkat-core/src/turn_execution_authority.rs (TurnExecutionAuthority / TurnTerminalOutcome / TurnTerminalCauseKind / TurnFailureSourceKind) is the machine-owned terminal classifier; AgentError::TerminalFai
- **Delete:add shape:** NET-DELETE. This is a real fold. The gate itself is small ADD (~150-250 lines: one AST rule + allow-list registry in the rmat-audit crate). The drain edits are near-zero-sum (replace a launder expression with `?` or a map — often fewer chars). The shadow deletions are pure subtraction: fabricated js
- **Members:** 43  ·  **Mistagged (move out):** 3
- **Fix:** MECHANICAL fold (adopt existing owners + add one gate + delete shadows), in three commits:  (1) ADD THE GATE — the actual keystone. Extend the existing rmat-audit AST harness (the crate that already hosts ForbiddenShellAuthorityReads; per CLAUDE.md `make rmat-audit`) with a new rule `LaunderedFaultAtSeam`. It flags, in non-test code: (a) `.ok()` / `.ok().flatten()` / `.unwrap_or_default()` / `.unwrap_or(false)` / `.unwrap_or_else(\|_\| <literal-or-fabricated>)` applied to an expression whose type is a `Result<_, E>` where E is one of the registered fault types (AgentError, SessionError, ToolEr
- **Independent verification:** Per-member close is proven by an INVARIANT, not a grep — two complementary checks:  (A) THE GATE INVARIANT (proves the whole cluster cannot regress): `make rmat-audit` (the existing harness) fails the build if any non-test, non-allow-listed expression launders a registered fault type. Concretely the gate asserts at AST-resolution time: for every `Result<_, E>`/`*Outcome` where E is in the fault-ty

#### CONTRACT-PROJECTION-GENERATOR-AND-DIFF-GATE (extend the single rerun-and-diff ratchet to cover every contract projection; fold hand-mirrored projections into generated emitters)  — _mechanical_
- **Canonical owner:** No NEW machine. The single owner is the existing schema-emission → codegen → rerun-and-diff RATCHET already proven for events/auth/mob/version-compat: (1) meerkat-contracts emit.rs `emit_all_schemas` as the schema source of truth, (2) tools/sdk-codegen/generate.py as the only projection emitter, (3)
- **Delete:add shape:** Net-DELETE of shadow projections. DELETE: sdks/web/src/types.ts SubscriptionLaggedEvent + session.ts normalizeSessionLaggedEvent (~30 lines); meerkat-web-runtime SubscriptionLaggedItem struct/enum/impl collapses to a 3-line From (~25 → 3); meerkat-memory/src/tool.rs:140-155 json! builder → serde of
- **Members:** 12  ·  **Mistagged (move out):** 6
- **Fix:** Step 1 — Promote the four still-handwritten wire shapes into meerkat-contracts so they enter emit.rs `emit_all_schemas` (the genuine source): (a) `SubscriptionLagged` (kind:"lagged", skipped:u64) — meerkat-web-runtime/src/lib.rs SubscriptionLaggedItem (2794) becomes a thin From-into of the contract type; (b) `MemorySearchResultWire` (content, score, source_range:MessageRange) derived from meerkat-core MemoryResult/MemorySource — meerkat-memory/src/tool.rs:140-155 serializes the typed struct instead of json!; (c) a canonical `ToolCallEnvelope`/`ToolCallRequest {name, arguments}` reused by REST
- **Independent verification:** Per-member INDEPENDENT invariants (file:line assertions, not greps): (1) Mob seam — meerkat-runtime/src/composition/route_table.rs: the route-table construction takes generated::meerkat_mob_seam::composition_id()/producers as input and a unit test asserts the live route set equals meerkat_mob_seam_composition().routes (no parallel literal). (2) RPC config/capability — rpc_catalog.rs:171-174 are Rp

#### Catalog-Owned Governance Facts: fold scanner-recovered categories into the typed machine/composition schema  — _mechanical_
- **Canonical owner:** The machine/composition catalog schema in meerkat-machine-schema is the canonical owner — it is already the single source of truth the codegen and seam-inventory read. The keystone ADOPTS it by adding typed governance-category fields rather than creating a new machine. Concretely: (1) on `EffectDisp
- **Delete:add shape:** Net-DELETE. This is a fold: the gates STOP carrying their own recovered-category state (substring lists, token-window heuristics, hardcoded exclusion/path sets) and instead read fields the schema declares once. Rough shape: ADD ~250 lines (5 enum/field additions + DSL clause parsers + codegen for th
- **Members:** 8  ·  **Mistagged (move out):** 2
- **Fix:** ONE seam, five typed adoptions, all reading the same catalog:  STEP 1 — Declare the categories on the schema (net-add, small): - meerkat-machine-schema/src/machine.rs: add `pub enum EffectRole { Plain, Teardown { detach_obligation: bool }, ... }` and field `effect_role: EffectRole` on `EffectDispositionRule`. Parse from a `role <Role>` DSL clause in meerkat-machine-dsl (parallel to the existing `seam` clause). - meerkat-machine-schema/src/composition.rs: add `pub enum ObligationKind { IngressDetach, ... }` and `satisfies_obligation: Option<ObligationKind>` on `EffectHandoffProtocol`. - Declare
- **Independent verification:** Per-member INDEPENDENT invariants (not greps):\n1. C-F3 destroy/detach: add a spec fixture with an effect renamed from `RequestDestroyRuntime` to `RequestTeardownRuntime` carrying `role Teardown(detach_obligation)` but NO paired `satisfies_obligation` protocol — assert seam-inventory FAILS at seam_inventory.rs (the typed `effect_role==Teardown` branch), proving the gate no longer depends on the 'D

#### Keystone: Seal the Shadow Door — make every already-existing canonical owner the ONLY constructable/readable source  — _mechanical_
- **Canonical owner:** No single new type. The cluster's true shape is "the canonical owner ALREADY EXISTS for every live member, but the shadow's constructor/read-path was left public, so a second owner persists." Three pre-existing owners, one keystone discipline: (a) meerkat-mob-pack `VerifiedMobpackArchive` (archive.r
- **Delete:add shape:** Net-DELETE — this is a real fold, the opposite of the +37k campaign. Per member: (A) mobpack DELETE ~`ParsedMobpack` struct + `parse_mobpack`/`extract_verify_and_parse_mobpack` two-step + CLI manual stitch (~60-90 lines across lib.rs + template + main.rs); ADD ~15 lines (route through `VerifiedMobpa
- **Members:** 4  ·  **Mistagged (move out):** 5
- **Fix:** MECHANICAL adopt-existing-owner + delete-shadow per live member (no new machine). One wave, four edits, net-DELETE.  (A) Mobpack (closes rows for #3 WASM + #4 typed-archive-contract — one root): - meerkat-mob-pack/src/archive.rs: change `MobpackArchive::from_archive_bytes`, `from_extracted_files`, `from_archive_path` to `pub(crate)`. `VerifiedMobpackArchive::open*` becomes the only crate-external door (it already calls `from_extracted_files` internally). Keep `archive()` accessor. - meerkat-cli/src/main.rs:12295-12308: delete the separate `MobpackArchive::from_extracted_files(&files)` + standa
- **Independent verification:** Independent per-member invariants (each a test/AST rule at a file:line, NOT a grep): (#4+#3 mobpack) — meerkat-mob-pack/src/archive.rs: a NEW `#[test]` asserting that the only public path to a `MobpackArchive` value is via `VerifiedMobpackArchive::open(...).into_parts()` (compile-time: `MobpackArchive::from_extracted_files` is `pub(crate)`, so an out-of-crate doctest referencing it fails to compil

#### Keystone: Centralize create-time default-model + realm-binding policy on the existing canonical seam (`meerkat::resolve_create_session_default_model` + `SessionLlmIdentity` resolver); delete per-surface ladders/mirrors  — _mechanical_
- **Canonical owner:** The canonical owner ALREADY EXISTS and is the keystone to adopt, not create:  1. `meerkat::resolve_create_session_default_model(config: &Config) -> String` (meerkat/src/factory.rs:985, re-exported at meerkat/src/lib.rs:231). The documented single 3-tier ladder (config.agent.model -> catalog provider
- **Delete:add shape:** NET-DELETE. Add ~1 small facade fn (`build_realm_from_provider_keys`, ~45 lines moved, not new). Delete: WASM `default_model_for_configured_providers` (~15 lines) + `populate_realm_from_api_keys` (~45 lines, relocated to facade so net-zero there); REST `default_model` field + ~20 `state.default_mode
- **Members:** 5  ·  **Mistagged (move out):** 6
- **Fix:** MECHANICAL adoption-and-delete (the canonical owners already exist). Steps, in order:  1. meerkat/src/factory.rs: Add a sibling facade fn `pub fn build_realm_from_provider_keys(config: &mut Config, api_keys: &HashMap<String,String>, base_urls: Option<&HashMap<String,String>>)` that moves the WASM `populate_realm_from_api_keys` body (lib.rs:347-391) into the facade verbatim. Also add `pub fn default_model_for_configured_realm(config: &Config) -> String` IF the provider-keys-present case needs to differ from `resolve_create_session_default_model`; otherwise reuse the existing fn directly. Re-exp
- **Independent verification:** Per-member INDEPENDENT invariants (not greps — each is a behavioral/structural check at a named site):  1. WASM model default (lib.rs:1339,:1404): After fix, `default_model_for_configured_providers` no longer exists as a symbol AND the only model-resolution call in both init paths is `meerkat::resolve_create_session_default_model`. INVARIANT: a unit test in meerkat-web-runtime that calls `init_run

#### Keystone: AgentIdentity is already the single typed identity owner — cluster IDENTITY_REDERIVED_IN_SHELL is closed; keystone is a verification-and-prune, not a new machine  — _mechanical_
- **Canonical owner:** meerkat_mob::ids::AgentIdentity (the `string_newtype!` at meerkat-mob/src/ids.rs:193-199) for mob member identity, projected as the DSL-owned typed fields in the generated MobMachine (meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:83-94: `pending_spawn_sessions: Map<AgentIdentity, SessionId>`
- **Delete:add shape:** Net-DELETE, but small — because the heavy shadow state was already removed by the prior campaign. Concretely: DELETE the `pending_kickoff_member_ids_from_dsl -> BTreeSet<String>` projection body and its `.to_string()` lowerings (~10-20 lines net negative after retyping to AgentIdentity), DELETE the
- **Members:** 6  ·  **Mistagged (move out):** 1
- **Fix:** MECHANICAL — adopt the already-existing owner (no new machine) and prove + prune. (1) Do NOT add a new canonical machine; AgentIdentity / PeerId / ToolCallId+ToolName already own every fact and are already adopted at all seven seams. (2) Add ONE invariant-anchored regression per seam that pins the typed/fail-closed contract so it cannot regress (locations in `verification`), rather than seven more bespoke patches. (3) Prune the lone genuine residual shadow: meerkat-mob/src/runtime/actor.rs:4219 `pending_kickoff_member_ids_from_dsl(&self) -> BTreeSet<String>` re-projects the DSL-owned `member_k
- **Independent verification:** Independent invariant checks (file:line assertions, not greps): (1) Roster: at meerkat-mob/src/roster.rs `apply`, project a `MemberSpawned` event with `bridge_member_ref: None` and assert `roster.get_by_identity(&id).is_none()` AND no nil-UUID MemberRef exists anywhere in entries — proves fail-closed, no fabrication (test scaffold already present near roster.rs:1200). (2) Kickoff: assert the gener

#### Machine-Poster Content-Drift Gate (single-member keystone; provider-in-core mistagged)  — _mechanical_
- **Canonical owner:** meerkat-machine-schema::catalog::canonical_machine_schemas() — already the single source of truth for every machine's states (MachineSchema.state.phase variants) and triggers (MachineSchema.inputs EnumSchema variants). The keystone makes the poster a gated PROJECTION of this owner rather than an un-
- **Delete:add shape:** Net-DELETE of shadow surface area. DELETE: the 8-entry POSTER_COVERAGE_KNOWN_GAPS escape-hatch set (~10 lines + its rationale doc-block ~L722-740), and the implicit license to hand-type anchors/triggers freely. ADD: a ~30-40 line validateContent() in the .mjs and a small alphabet-emit (~20-40 lines,
- **Members:** 1  ·  **Mistagged (move out):** 1
- **Fix:** MECHANICAL — extend the existing gate to validate content, not just coverage; do NOT patch posters one-by-one.  1. Add a tiny JSON dump of the canonical machine alphabet so the .mjs gate can read it without parsing Rust. Either (a) extend the existing emit-schemas pipeline, or (b) add a small `--emit-machine-alphabet` bin/test in meerkat-machine-schema that writes {machine_id -> {states:[phase names from state.phase], triggers:[input variant names from inputs]}} for every canonical_machine_schemas() entry. Prefer reusing the emit-schemas path already invoked by make regen-schemas.  2. In scrip
- **Independent verification:** Member 2 closed by an INVARIANT, not a grep: in the new validateContent() gate (scripts/machine-posters/generate-machine-posters.mjs, called from checkCoverage), the assertion that for every MACHINE_SPECS spec, the set of poster group anchors ⊆ {state.phase variant names of canonical_machine_schemas()[spec.canonicalFn]} AND the set of poster group triggers ⊆ {inputs variant names of that schema}.

## Synthesis

> After independent verification against HEAD (679058616), the calibrated state is: of ~120 cataloged rows across four clusters plus 11 High rows, only roughly 18-22 are GENUINELY-ACTIVE violations. The rest are either already-folded-at-HEAD (the owner exists and is wired — the audit evidence itself reads CLOSED/stale/superseded) or mistagged into the wrong root shape (~15 rows: OAuthFlowRegistry is a handshake-correlation store not a lifecycle phase; budget is a resource meter not a machine-fact shadow; EffortLevel/BetaValue/Provider-on-ModelProfile are already typed; define_metadata_string! newtypes ARE the typed owner). Concretely verified active: ProviderParamsOverride exists at run_primitive.rs:948 but config.rs:779/791 still carry Option<serde_json::Value> with the state.rs:151/171 JSON merge-patch shadow still live; the flow terminal split is real (terminalization.rs:107 forward append uses plain .append() not the idempotent repair method); the REST SSE fabricated {\"type\":\"unknown\"} launder and MCP fixed \"Tool returned error\" / _ => None content-narrowing are live residuals; inproc peer identity re-derivation and the WorkGraph read-seam ambiguity are the two real Highs. The root shapes reduce to FOUR: (1) CARRIER-ADOPTION — typed owner exists, parallel String/JSON carrier survives; (2) TERMINAL_FAULT_LAUNDERED — typed fault exists, collapsed by a laundering verb at a seam; (3) SHELL_SHADOW_OF_MACHINE_FACT — already folded, needs anti-regression only; (4) ATOMICITY_SPLIT_COMMIT — two existing owners (AuthMachine lease, replay/reconcile) cover all but a 3-line idempotent-append residual. This plan will NOT repeat the +37k-line failure for one structural reason: every prior fix MINTED owners (net-ADD beside surviving shadows) but installed NO gate to forbid the parallel twin or the re-launder, so the audit re-found the same shape under new file:lines. This plan inverts that — GATES FIRST (typed-carrier AST registry + LaunderedFaultAtSeam rmat rule + ForbiddenShadowPrecheck + the existing rerun-and-diff parity lanes), then each member is closed by an asserted INVARIANT the gate guards and the parallel carrier/launder/fallback is DELETED. The campaign signature must flip from +37,000 net to strongly negative; that negativity (delete merge_provider_param_patch, the JSON carriers, the fabricated fallbacks, the duplicate rollback prose) is the proof the folds are real rather than another owner-beside-shadow round. If any later PR against a closed member shows net-ADD of mirror/projection/fallback scaffolding, the gate rejects it — that is the durable anti-drift property the prior campaign lacked."

## On the number "357"

"357" was the row count of an internal *remediation plan* (a recalibrated catalog: 10 High / 218 Med / 129 Low), **never a claim of "357 violations closed."** This PR genuinely retired a subset; this report supersedes all prior closure counts with the per-row verdicts above.
