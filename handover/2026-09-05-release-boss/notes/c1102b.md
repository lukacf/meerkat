Correction from the OB3 operator (agent bus, 2026-09-05 00:27Z), measured: the "transport-level send failure" that repair-blocked the review member was OB3's own client-side 30 s HTTP timeout on a slow uplink. A 5.8 MB review-session save failed at 30 s on three consecutive attempts; every write had COMMITTED server-side (rows landed at each attempt's 30 s mark) and the client gave up before the response. So the runtime's fail-stop reacted to a FALSE failed commit: durable state did not differ from the live runtime. The fail-closed reaction is correct given what it was told; for this issue's narrative, the member-local blocking work was a 30 s HTTP wait inside a boundary commit, twice, followed by the loop wedging every other member. OB3 fixed its client budget (30 s + 10 s/MB, cap 300 s). Production uploads the body sub-second, so the loop-isolation fix is what protects it from the next trigger.
