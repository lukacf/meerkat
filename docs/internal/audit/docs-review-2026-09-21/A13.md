# A13: Active architecture and feature design plans

This ledger preserves the baseline evidence and independent disposition. See [the audit index](README.md) for scope, commits, and publication boundaries.

## A13-001: Instruction-activation entry point still presents the tagged 0.8.30 integration as pending release work

**Candidate severity:** low. **Independent verdict:** reject.

This accepted architecture decision has a date but no historical-release qualification around its pending status and imperative publish gate. The repository can prove a tagged implementation and a later current dependency, but cannot establish the present status of external MobKit/OB3/HomeCore qualification. The issue is therefore stale entry-point/release-instruction framing, not a claim that the original 0.4.1 binding was wrong or that all external gates completed.

### Baseline documentation evidence

- [docs/architecture/durable-instruction-activation.md:9-11](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/durable-instruction-activation.md?plain=1#L9-L11)

```text
Status: Accepted for implementation, pending integrated review and CI
```

- [docs/architecture/durable-instruction-activation.md:429-436](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/durable-instruction-activation.md?plain=1#L429-L436)

```text
Until the candidate is merged, tagged, and published, this exact integration
commit remains development Git truth rather than a released package.
```

- [docs/architecture/durable-instruction-activation.md:443-447](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/durable-instruction-activation.md?plain=1#L443-L447)

```text
The exact Meerkat/MobKit pair must then pass isolated no-deploy OB3 and
HomeCore qualification before the Meerkat lead may publish 0.8.30.
```

### Implementation proof

- Read-only reproduction:

```text
git show v0.8.30:Cargo.toml | grep -n -E '^version =|^oai-rt-rs ='
```

  55:version = "0.8.30"
  183:oai-rt-rs = { version = "=0.4.1", features = ["experimental-gpt-live"] }

  The precise dependency pairing described by the plan exists in a local release-tag snapshot; it is historical release binding, not the current workspace dependency.

- Read-only reproduction:

```text
git show v0.8.30:meerkat/src/session_runtime/instruction_activation.rs | grep -n -E 'pub async fn (activate_instruction|read_instruction_activations)'
```

  77:    pub async fn activate_instruction(
  88:    pub async fn read_instruction_activations(
  100:    pub async fn activate_instruction_with_write_fence(

  The tagged Meerkat source already contains the activation/read/fenced-activation public implementation. This is independent implementation evidence, not a changelog assertion.

- [Cargo.toml:55-56](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Cargo.toml#L55-L56)

```text
version = "0.8.40"
```

  The audited workspace is later than the release candidate named in the operative instructions.

- [Cargo.toml:185-185](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Cargo.toml#L185)

```text
oai-rt-rs = { version = "=0.5.1", features = ["experimental-gpt-live"] }
```

  The old exact realtime pairing must not read as the current dependency prescription.

- [meerkat-contracts/src/rpc_catalog.rs:260-270](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/rpc_catalog.rs#L260-L270)

```text
"session/activate_instruction"
```

  The current canonical callable catalog exposes the implemented operation and its authoritative read sibling.

### Independent challenge

The candidate establishes that activation was implemented and tagged, but does not establish a falsely current instruction. This is a dated architecture decision: its status is paired with 2026-08-25, and the disputed release section explicitly concerns the 0.8.30 candidate and its cross-repository qualification. The document was introduced in the release-integration commit, not updated later to prescribe the current workspace's dependency version. The tagged source agrees with the historical 0.4.1 pairing. Neither a later workspace version nor public method existence proves the described external qualification gates completed. A historical-status banner could improve navigation, but its absence is not sufficient evidence of an actionable accuracy defect under this audit's historical-record rule.

- [docs/architecture/durable-instruction-activation.md:9-11](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/durable-instruction-activation.md?plain=1#L9-L11)

```text
Status: Accepted for implementation, pending integrated review and CI
Date: 2026-08-25
```

  The status is explicitly dated; the finding cannot silently interpret it as a September release-status report.

- [docs/architecture/durable-instruction-activation.md:427-436](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/durable-instruction-activation.md?plain=1#L427-L436)

```text
authority's exact qualified realtime Meerkat head for the 0.8.30 candidate.
```

  The release binding is scoped to a named historical candidate, not asserted as the latest supported dependency recipe.

- Read-only reproduction:

```text
git log --diff-filter=A --format='%H %aI %s' -- docs/architecture/durable-instruction-activation.md
```

  aefcff3b72d97181d22557f10ee5c6042974b5a3 2026-08-27T01:19:46+02:00 chore: release v0.8.30 (#1021)

  Independent chronology identifies the document as an artifact of the release integration it describes.

- Read-only reproduction:

```text
git show v0.8.30:Cargo.toml | grep -n -E '^version =|^oai-rt-rs ='
```

  55:version = "0.8.30"
  183:oai-rt-rs = { version = "=0.4.1", features = ["experimental-gpt-live"] }

  The source tag verifies, rather than disproves, the release-specific dependency statement. Local tags do not prove external qualification or registry publication.

- [meerkat/src/session_runtime/instruction_activation.rs:74-95](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/instruction_activation.rs#L74-L95)

```text
pub async fn activate_instruction(
```

  Current implementation is real; the rejection concerns the inference that historical release-gate prose must therefore be factually wrong, not whether activation exists.

**Disposition:** Rejected; no correction authorized for this claim.

## A13-002: Member-tool plan lacks an entry-point distinction between its shipped Meerkat contract and the original proposal

**Candidate severity:** low. **Independent verdict:** reject.

The baseline-pinned proposal is legitimate history and its unimplemented-looking prose is not itself a current product defect. However, unlike the neighboring historical plans, its only entry-point status remains Proposed for 0.8.26, without saying that the Meerkat-side contract exists in that release tag. A reader cannot distinguish original current-truth analysis and downstream rollout requirements from present Meerkat availability.

### Baseline documentation evidence

- [docs/plans/member-tool-policy-v1.md:3-11](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/member-tool-policy-v1.md?plain=1#L3-L11)

```text
Status: Proposed for Meerkat 0.8.26
```

- [docs/plans/member-tool-policy-v1.md:7-7](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/member-tool-policy-v1.md?plain=1#L7)

```text
# Proposal: Member Tool Policy v1
```

- [docs/plans/member-tool-policy-v1.md:54-54](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/member-tool-policy-v1.md?plain=1#L54)

```text
## 1. Current truth
```

### Implementation proof

- Read-only reproduction:

```text
git show v0.8.26:meerkat-mob/src/runtime/handle.rs | grep -n -E 'pub async fn (apply_member_tool_declaration|adopt_member_identity_declaration|resolve_identity_convergence_block)'
```

  7276:    pub async fn apply_member_tool_declaration(
  7296:    pub async fn adopt_member_identity_declaration(
  7316:    pub async fn resolve_identity_convergence_block(

  The release-tagged Meerkat code contains the central public APIs that this entry point still introduces only as a proposed release contract.

- [meerkat-mob/src/runtime/handle.rs:8241-8256](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/handle.rs#L8241-L8256)

```text
.execute_machine_command(MobMachineCommand::ApplyMemberToolDeclaration {
```

  The current API is implemented through the MobMachine command owner, not merely a type declaration or stub.

- [meerkat-contracts/src/rpc_catalog.rs:830-851](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/rpc_catalog.rs#L830-L851)

```text
"mob/apply_member_tool_declaration"
```

  The current canonical public catalog exposes the corresponding shipped surface alongside read, adoption, and convergence-block APIs.

### Independent challenge

The document unambiguously labels itself a proposal, pins an exact Meerkat baseline, and includes downstream delivery phases that this repository cannot certify as complete. 'Current truth' is the proposal's baseline analysis, not an unqualified current API reference. I checked the pinned baseline: it really did replace the member policy with ReadOnly, exactly as the motivating analysis says. The later v0.8.26 API implementation proves that the proposal led to implementation; it does not make the preserved proposal or its baseline analysis false. Requiring a banner merely because neighboring plans have one would be an editorial consistency finding, not a verified status inaccuracy.

- [docs/plans/member-tool-policy-v1.md:7-11](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/member-tool-policy-v1.md?plain=1#L7-L11)

```text
Status: Proposed for Meerkat 0.8.26
Baseline: Meerkat `b1f1a9458deb55d4398b894f9b06bdc8695ee832`
```

  The named proposal and immutable baseline supply the temporal scope missing from the candidate's interpretation of the heading.

- [docs/plans/member-tool-policy-v1.md:962-976](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/member-tool-policy-v1.md?plain=1#L962-L976)

```text
### Phase 4: HomeCore migration
```

  The proposal includes downstream rollout work; three implemented Meerkat methods cannot establish its overall delivery status.

- Read-only reproduction:

```text
git show b1f1a9458deb55d4398b894f9b06bdc8695ee832:meerkat-mob/src/build.rs | nl -ba | sed -n '361,368p'
```

  At lines 366-367: if profile.tools.read_only { config.tool_access_policy = Some(meerkat_core::ops::ToolAccessPolicy::ReadOnly); }

  Independent source verification confirms the historical baseline defect discussed by the proposal, rather than comparing that analysis to later corrected code.

- Read-only reproduction:

```text
git show v0.8.26:meerkat-mob/src/runtime/handle.rs | nl -ba | sed -n '7274,7287p'
```

  The tagged implementation exposes apply_member_tool_declaration at line 7276 and invokes MobMachineCommand::ApplyMemberToolDeclaration at lines 7282-7285.

  The implemented API is acknowledged, but it is a later result of the baseline-pinned proposal and does not require rewriting the proposal's historical status.

**Disposition:** Rejected; no correction authorized for this claim.

## A13-003: Shipped storage record promises a dangling-blob repair that storage doctor cannot perform

**Candidate severity:** medium. **Independent verdict:** confirm.

This is not a complaint that a historical proposal was never implemented: revision 6 explicitly labels the phase text the shipped implementation record. Its promised Phase 6 strip-to-placeholder repair is still written as a delivered successor capability, while the current CLI grammar and provider interface only diagnose dangling references. Operators must not be led to search for a nonexistent sanctioned repair.

### Baseline documentation evidence

- [docs/plans/storage-unification-plan.md:9-15](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L9-L15)

```text
The phase language below is
the accepted implementation record, not an unshipped roadmap.
```

- [docs/plans/storage-unification-plan.md:224-226](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L224-L226)

```text
- Repair verbs stay in Phase 6; doctor gains a sanctioned
  strip-to-placeholder repair for dangling blob refs there (today the only
  remedy is hand-editing production session JSON).
```

- [docs/plans/storage-unification-plan.md:437-437](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L437)

```text
## Phase 6 - Migration framework (shipped)
```

### Implementation proof

- [meerkat-cli/src/main.rs:2173-2186](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L2173-L2186)

```text
/// This command never mutates logical database content.
```

  The authoritative Doctor command variant has only json and roots inputs and explicitly remains read-only; it has no repair/apply/placeholder operation.

- [meerkat-cli/src/main.rs:8039-8068](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L8039-L8068)

```text
StorageCommands::Doctor { json, roots } => handle_storage_doctor(cli, *json, roots).await,
```

  The exhaustive storage dispatcher contains only Doctor, Migrate, and Prune. Doctor routes directly to read-only diagnosis rather than a hidden repair command.

- [meerkat-core/src/storage_diagnostics.rs:240-253](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/storage_diagnostics.rs#L240-L253)

```text
async fn diagnose(
        &self,
        scope: &DiagnoseScope,
    ) -> Result<StorageDiagnosis, StorageDiagnosticsError>;
```

  The complete provider maintenance trait exposes only a diagnosis operation, specified as no leases/exclusive locks/writes.

### Independent challenge

Unlike the two dated proposals above, this document expressly promotes its phase language to the shipped implementation record. The dangling-reference bullet promises a Phase 6 strip-to-placeholder repair, Phase 6 is marked shipped, and no recorded implementation exception qualifies that promise. The complete CLI dispatch and the complete maintenance trait establish that doctor only diagnoses; migrate and prune are separate verbs and expose no such doctor repair. Confirm only the delivery-status overclaim, not any demand to implement repair or to rewrite the original review decision.

- [docs/plans/storage-unification-plan.md:9-15](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L9-L15)

```text
The phase language below is
the accepted implementation record, not an unshipped roadmap.
```

  This explicit current-status declaration makes an unqualified phase deliverable a shipped-capability claim.

- [docs/plans/storage-unification-plan.md:224-226](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L224-L226)

```text
doctor gains a sanctioned
  strip-to-placeholder repair for dangling blob refs there
```

  This is the specific promise that needs an unshipped qualification; the historical acceptance of the idea can remain.

- [meerkat-cli/src/main.rs:2173-2186](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L2173-L2186)

```text
/// This command never mutates logical database content.
```

  Doctor's complete command variant only accepts json and roots; there is no repair mode.

- [meerkat-cli/src/main.rs:8039-8068](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L8039-L8068)

```text
StorageCommands::Doctor { json, roots } => handle_storage_doctor(cli, *json, roots).await,
```

  The exhaustive storage dispatcher routes Doctor to diagnosis and separately handles only Migrate and Prune, excluding an omitted repair subcommand.

- [meerkat-core/src/storage_diagnostics.rs:240-253](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/storage_diagnostics.rs#L240-L253)

```text
async fn diagnose(
        &self,
        scope: &DiagnoseScope,
    ) -> Result<StorageDiagnosis, StorageDiagnosticsError>;
```

  The complete provider hook has only read-only diagnosis, not an implicit repair entry point.

**Required correction:** Qualify the Phase 1 dangling-blob repair bullet as an accepted proposal that was not delivered by the shipped Phase 6 framework. State that current storage doctor reports dangling references without modifying session content. Preserve the historical review acceptance; do not invent a repair command or present manual production-JSON editing as supported recovery.

### Correction

Qualified the accepted Phase 1 strip-to-placeholder proposal with an explicit implementation exception: it was not delivered by the shipped Phase 6 framework. Stated that current storage doctor reports dangling references without modifying session content, removed the unsupported manual-production-JSON remedy, and directed recovery to Storage Operations. The historical review acceptance and full disposition ledger remain unchanged.

- [docs/plans/storage-unification-plan.md:227-232](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/plans/storage-unification-plan.md?plain=1#L227-L232)

```text
**Implementation exception:** that repair did not ship with the Phase 6
  framework. Current `rkat storage doctor` reports dangling references without
  modifying session content.
```

- [meerkat-cli/src/main.rs:2173-2186](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L2173-L2186)

```text
This command never mutates logical database content.
```

- [meerkat-core/src/storage_diagnostics.rs:243-253](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/storage_diagnostics.rs#L243-L253)

```text
Implementations must be safe against live storage (no leases, no
```

- [meerkat-store/src/doctor.rs:1190-1194](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-store/src/doctor.rs#L1190-L1194)

```text
Dangling session→blob reference sweep (sqlite backend): decode persisted
```

### Final independent review

**PASS:** The shipped-record overclaim is resolved explicitly: the accepted strip-to-placeholder proposal is retained, but the text now says it did not ship with Phase 6 and that doctor does not modify session content. The unsupported manual-JSON remedy is gone. The original review-disposition ledger remains byte-for-byte unchanged, preserving acceptance history rather than pretending the proposal was never accepted. The new Storage Operations route has an existing target and agrees with the command's read-only contract.

- [docs/plans/storage-unification-plan.md:227-232](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/plans/storage-unification-plan.md?plain=1#L227-L232)

```text
**Implementation exception:** that repair did not ship with the Phase 6
  framework. Current `rkat storage doctor` reports dangling references without
  modifying session content.
```

- [docs/plans/storage-unification-plan.md:550-555](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/plans/storage-unification-plan.md?plain=1#L550-L555)

```text
blob conformance + doctor dangling-ref repair;
```

  The historical review acceptance remains in the disposition ledger; it is now qualified by the adjacent phase implementation exception.

- [meerkat-cli/src/main.rs:2173-2186](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L2173-L2186)

```text
This command never mutates logical database content.
```

  The complete Doctor variant only accepts json and roots, not a repair mode.

- [meerkat-cli/src/main.rs:8039-8068](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L8039-L8068)

```text
StorageCommands::Doctor { json, roots } => handle_storage_doctor(cli, *json, roots).await,
```

  The exhaustive dispatcher contains Doctor, Migrate, and Prune; no hidden dangling-reference repair is routed.

- [meerkat-store/src/doctor.rs:1190-1194](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-store/src/doctor.rs#L1190-L1194)

```text
Dangling session→blob reference sweep (sqlite backend): decode persisted
```

  The implementation sweeps stored references and probes blob existence for reporting; it does not replace transcript content.

- [docs/guides/storage-operations.mdx:14-20](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/guides/storage-operations.mdx?plain=1#L14-L20)

```text
| `storage doctor` | Read-only | Never mutates logical database content or creates a database/directory; SQLite may contact WAL/SHM sidecars |
```

  The newly linked guide exists at the /guides/storage-operations route and does not invent a repair command.

## A13-004: StorageMigrator is documented as a shipped remote migration lifecycle but only defines diagnosis

**Candidate severity:** medium. **Independent verdict:** confirm.

Under a Phase 6 shipped heading, this paragraph turns a future extension story into a present framework guarantee. A downstream backend may implement its own migrations, but the exported StorageMigrator hook cannot drive the claimed lifecycle. This is a documentation overclaim about the extension seam, not a request to add mutation APIs.

### Baseline documentation evidence

- [docs/plans/storage-unification-plan.md:501-504](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L501-L504)

```text
**Downstream migration.** The `StorageMigrator` hook gives remote backends
the same lifecycle: a version ledger in their own medium (for BigQuery, a
`meerkat_schema` table) and ordered migrations under a provider-supplied
lock. Meerkat ships the framework and the disk implementation.
```

### Implementation proof

- [meerkat-core/src/storage_diagnostics.rs:240-253](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/storage_diagnostics.rs#L240-L253)

```text
pub trait StorageMigrator: Send + Sync {
```

  The entire trait contains only async diagnose. It supplies no migration command, ordered migration sequence, version-ledger API, or provider-lock contract.

- [meerkat/src/storage_provider.rs:80-86](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/storage_provider.rs#L80-L86)

```text
/// The provider's migration hook (diagnosis now; mutation verbs land as
```

  The actual provider seam explicitly distinguishes current diagnosis from future defaulted mutation verbs; migrator returns an optional reference to the diagnosis-only trait.

- [meerkat-cli/src/storage_migrate.rs:496-512](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/storage_migrate.rs#L496-L512)

```text
"backend '{other}' is not disk-migratable by this build; report-only"
```

  The actual mutation path handles sqlite/jsonl; other backend names receive a report-only disposition rather than a provider-supplied migration lifecycle.

### Independent challenge

The paragraph attributes an ordered, locked migration lifecycle to the exported StorageMigrator hook in a section expressly marked shipped. Reading the entire trait, not merely searching for a missing method, shows that it can only diagnose. The optional provider seam says mutation methods are future additions. The stock migration implementation also treats non-disk backends as report-only. A remote provider may independently implement a ledger and locking, but that is not a lifecycle supplied by this hook.

- [docs/plans/storage-unification-plan.md:437-437](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L437)

```text
## Phase 6 - Migration framework (shipped)
```

  The disputed downstream-migration paragraph is presented within the shipped phase, not merely a future extension appendix.

- [docs/plans/storage-unification-plan.md:501-504](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L501-L504)

```text
The `StorageMigrator` hook gives remote backends
the same lifecycle
```

  The text claims that this specific hook supplies the downstream migration framework.

- [meerkat-core/src/storage_diagnostics.rs:232-253](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/storage_diagnostics.rs#L232-L253)

```text
pub trait StorageMigrator: Send + Sync {
```

  The entire trait body consists of diagnose; its own contract reserves mutation verbs for future defaulted additions.

- [meerkat/src/storage_provider.rs:80-86](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/storage_provider.rs#L80-L86)

```text
/// The provider's migration hook (diagnosis now; mutation verbs land as
```

  The actual embedding seam explicitly limits what is available now.

- [meerkat-cli/src/storage_migrate.rs:496-512](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/storage_migrate.rs#L496-L512)

```text
"backend '{other}' is not disk-migratable by this build; report-only"
```

  The executable migration selector handles sqlite/jsonl and returns report-only for other backend names, not a hidden provider migration lifecycle.

**Required correction:** Correct the Phase 6 downstream-migration paragraph to distinguish the currently exported diagnosis-only hook from the shipped built-in disk migration path. Preserve remote ledgers, ordered migrations, and provider-supplied locks as provider-owned or future design work, not facilities currently driven by StorageMigrator.

### Correction

Replaced Phase 6's present-tense remote migration guarantee with the actual boundary: StorageMigrator exposes read-only diagnosis; mutation belongs to the shipped built-in disk path, and stock migrate treats external backends as report-only. Retained remote-medium ledgers, ordered migrations, and provider-supplied locks explicitly as the original provider-owned/future design, not functionality of the current trait.

- [docs/plans/storage-unification-plan.md:507-514](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/plans/storage-unification-plan.md?plain=1#L507-L514)

```text
exposes read-only diagnosis only; it does not drive mutation, ordered
migrations, or provider locking.
```

- [docs/plans/storage-unification-plan.md:511-514](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/plans/storage-unification-plan.md?plain=1#L511-L514)

```text
migrations under a provider-supplied lock — remains provider-owned or future
extension work, not a lifecycle supplied by the current hook.
```

- [meerkat-core/src/storage_diagnostics.rs:243-253](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/storage_diagnostics.rs#L243-L253)

```text
pub trait StorageMigrator: Send + Sync {
```

- [meerkat/src/storage_provider.rs:80-85](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/storage_provider.rs#L80-L85)

```text
The provider's migration hook (diagnosis now; mutation verbs land as
```

- [meerkat-cli/src/storage_migrate.rs:502-510](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/storage_migrate.rs#L502-L510)

```text
backend '{other}' is not disk-migratable by this build; report-only
```

### Final independent review

**PASS:** The revised Phase 6 paragraph correctly separates the diagnosis-only exported trait from the built-in disk mutation implementation. Remote-medium ledgers, ordered migrations, and provider-supplied locks are preserved as the original provider-owned or future design, not claimed as capabilities of StorageMigrator. This matches the entire trait, its optional provider composition seam, and the executable backend selector.

- [docs/plans/storage-unification-plan.md:507-514](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/plans/storage-unification-plan.md?plain=1#L507-L514)

```text
exposes read-only diagnosis only; it does not drive mutation, ordered
migrations, or provider locking.
```

- [docs/plans/storage-unification-plan.md:511-514](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/plans/storage-unification-plan.md?plain=1#L511-L514)

```text
migrations under a provider-supplied lock — remains provider-owned or future
extension work, not a lifecycle supplied by the current hook.
```

- [meerkat-core/src/storage_diagnostics.rs:243-253](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/storage_diagnostics.rs#L243-L253)

```text
async fn diagnose(
        &self,
        scope: &DiagnoseScope,
    ) -> Result<StorageDiagnosis, StorageDiagnosticsError>;
```

  This is the only method in the complete StorageMigrator trait; it provides no mutation, migration-sequence, or locking API.

- [meerkat/src/storage_provider.rs:80-86](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/storage_provider.rs#L80-L86)

```text
fn migrator(&self) -> Option<&dyn meerkat_core::StorageMigrator>
```

  The provider exposes an optional diagnosis implementation; future mutation verbs are explicitly distinguished in the source contract.

- [meerkat-cli/src/storage_migrate.rs:496-512](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/storage_migrate.rs#L496-L512)

```text
"backend '{other}' is not disk-migratable by this build; report-only"
```

  The selector handles memory separately, permits sqlite/jsonl, and reports other backends without invoking a remote migration lifecycle.

## A13-005: The storage doctor phase conflates the provider hook with external-provider selection in rkat

**Candidate severity:** medium. **Independent verdict:** confirm.

The heading is specifically about the shipped rkat command. Its remote-deployment promise is wider than the command's exhaustive provider selection. Embedders can compose an external provider's migrator, but rkat storage doctor does not automatically route to it. This distinction is separate from whether mutation verbs exist on the trait.

### Baseline documentation evidence

- [docs/plans/storage-unification-plan.md:211-211](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L211)

```text
## Phase 1 - Read-only `rkat storage doctor` (shipped)
```

- [docs/plans/storage-unification-plan.md:222-223](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L222-L223)

```text
- Calls the provider's `StorageMigrator::diagnose` hook so remote deployments
  get doctor output too, not just disk realms.
```

### Implementation proof

- [meerkat-cli/src/main.rs:8105-8123](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L8105-L8123)

```text
fn storage_diagnosis_migrator() -> &'static dyn meerkat_core::StorageMigrator {
```

  This complete selector is statically wired to DiskStorageProvider or the fallback DiskStorageMigrator. Its preceding comment explicitly says storage dispatch precedes runtime-scope resolution and cannot consult an external provider manifest.

- [meerkat-cli/src/main.rs:8126-8140](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L8126-L8140)

```text
let diagnosis = storage_diagnosis_migrator().diagnose(&scope).await?;
```

  The CLI invokes that fixed disk selector, not a caller-injected or manifest-selected external implementation.

- [meerkat/src/storage_provider.rs:71-86](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/storage_provider.rs#L71-L86)

```text
fn migrator(&self) -> Option<&dyn meerkat_core::StorageMigrator> {
        None
    }
```

  The optional hook is an embedding/provider composition seam; merely having it does not cause the stock CLI to discover a remote backend.

### Independent challenge

The extensibility hook is real, but the claim appears specifically under shipped rkat storage doctor. The complete CLI selector has only the built-in disk provider and disk migrator fallback; it cannot select an external provider from a manifest or inject one through command arguments. The accepted correction must distinguish the reusable diagnosis trait from the stock executable's composition. It must not overcorrect into saying doctor cannot inspect any filesystem metadata concerning externally owned realms.

- [docs/plans/storage-unification-plan.md:211-223](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/plans/storage-unification-plan.md?plain=1#L211-L223)

```text
Calls the provider's `StorageMigrator::diagnose` hook so remote deployments
  get doctor output too, not just disk realms.
```

  The heading scopes this promise to the shipped stock command.

- [meerkat-cli/src/main.rs:8105-8123](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L8105-L8123)

```text
static DISK_PROVIDER: meerkat::DiskStorageProvider = meerkat::DiskStorageProvider;
```

  Reading the complete selector shows only DiskStorageProvider and the DiskStorageMigrator fallback. There is no external-provider selection arm.

- [meerkat-cli/src/main.rs:8126-8140](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L8126-L8140)

```text
let diagnosis = storage_diagnosis_migrator().diagnose(&scope).await?;
```

  The command invokes that fixed selector rather than a runtime-composed external implementation.

- [meerkat/src/storage_provider.rs:80-86](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/storage_provider.rs#L80-L86)

```text
fn migrator(&self) -> Option<&dyn meerkat_core::StorageMigrator> {
        None
    }
```

  The optional host/provider composition seam does not imply automatic stock-CLI discovery.

**Required correction:** Replace the stock-command remote-diagnosis promise with the current disk/filesystem composition boundary. Explain that a host using an external backend must explicitly compose and invoke that provider's diagnosis implementation; implementing the optional hook alone does not route rkat storage doctor to it.

### Correction

Corrected the shipped doctor bullet to name the built-in disk provider/disk fallback and filesystem inspection boundary. External-backend hosts must explicitly compose and invoke their provider's diagnosis implementation; merely implementing the optional hook does not route the stock CLI to it. The wording does not prohibit filesystem metadata inspection of externally owned realms.

- [docs/plans/storage-unification-plan.md:222-226](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/plans/storage-unification-plan.md?plain=1#L222-L226)

```text
host must explicitly compose and invoke its provider's diagnosis
  implementation; implementing the optional hook alone does not route the
  stock `rkat storage doctor` command to it.
```

- [meerkat-cli/src/main.rs:8111-8123](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L8111-L8123)

```text
static DISK_PROVIDER: meerkat::DiskStorageProvider = meerkat::DiskStorageProvider;
```

- [meerkat-cli/src/main.rs:8120-8123](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L8120-L8123)

```text
static DISK_MIGRATOR: meerkat_store::DiskStorageMigrator = meerkat_store::DiskStorageMigrator;
```

- [meerkat-cli/src/main.rs:8126-8138](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L8126-L8138)

```text
let diagnosis = storage_diagnosis_migrator().diagnose(&scope).await?;
```

- [meerkat/src/storage_provider.rs:80-85](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/storage_provider.rs#L80-L85)

```text
fn migrator(&self) -> Option<&dyn meerkat_core::StorageMigrator> {
```

### Final independent review

**PASS:** The stock-command claim is now accurately bounded to the built-in disk provider or disk fallback. The text explains that an external-backend host must explicitly compose and invoke its implementation and that implementing the optional hook does not make the stock CLI select it. It does not overcorrect into forbidding filesystem inspection of externally owned realms.

- [docs/plans/storage-unification-plan.md:222-226](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/plans/storage-unification-plan.md?plain=1#L222-L226)

```text
host must explicitly compose and invoke its provider's diagnosis
  implementation; implementing the optional hook alone does not route the
  stock `rkat storage doctor` command to it.
```

- [meerkat-cli/src/main.rs:8111-8124](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L8111-L8124)

```text
static DISK_PROVIDER: meerkat::DiskStorageProvider = meerkat::DiskStorageProvider;
```

  The complete selector has the feature-gated DiskStorageProvider hook and a DiskStorageMigrator fallback, with no external-provider selection arm.

- [meerkat-cli/src/main.rs:8120-8124](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L8120-L8124)

```text
static DISK_MIGRATOR: meerkat_store::DiskStorageMigrator = meerkat_store::DiskStorageMigrator;
```

- [meerkat-cli/src/main.rs:8126-8140](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L8126-L8140)

```text
let diagnosis = storage_diagnosis_migrator().diagnose(&scope).await?;
```

  Doctor invokes that fixed composition over the root/realm scope.

- [meerkat/src/storage_provider.rs:80-86](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/storage_provider.rs#L80-L86)

```text
fn migrator(&self) -> Option<&dyn meerkat_core::StorageMigrator> {
        None
    }
```

  The optional embedding hook is real, but does not imply executable plugin discovery.

## A13-006: Multi-host resource chapter still creates a per-mob realm that its host-realm decision forbids

**Candidate severity:** medium. **Independent verdict:** confirm.

The preserved plan calls section 14 authoritative about physical state locality, yet sections 15.2 R4 and 15.7 instruct per-mob realm attachment and put memory under that realm's store path. Those are incompatible ownership/storage contracts within the same historical design, not a complaint that its old source anchors differ from HEAD. The implementation follows the daemon-realm contract.

### Baseline documentation evidence

- [docs/architecture/multi-host-mobs-plan.md:357-358](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L357-L358)

```text
**R1 — A materialized member's state lives in the member-host daemon's realm; `mob.{mob_id}` is never a store path.**
```

- [docs/architecture/multi-host-mobs-plan.md:497-497](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L497)

```text
realm `mob.{id}` attachable with a persistent backend when `durable_sessions` was declared
```

- [docs/architecture/multi-host-mobs-plan.md:605-605](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L605)

```text
Realm provisioning: the member host creates/attaches realm `mob.{id}` locally through the single owner `meerkat_core::mob_realm_id`
```

### Implementation proof

- [meerkat-cli/src/mob_host.rs:1054-1064](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/mob_host.rs#L1054-L1064)

```text
session_service: Arc::clone(&service) as Arc<dyn MobSessionService>,
```

  The member-host substrate receives the daemon's already-composed session service/runtime and backend durability fact; it does not construct another session service for each mob.

- [meerkat-mob/src/runtime/host_materialize.rs:106-119](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/host_materialize.rs#L106-L119)

```text
pub realm_backend_persistent: bool,
```

  The durability fact used for member materialization is explicitly the daemon's opened-backend durability, not a separately attached mob realm.

- [meerkat-mob/src/runtime/host_materialize.rs:2007-2025](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/host_materialize.rs#L2007-L2025)

```text
Arc::clone(&self.substrate.session_service),
```

  Actual member creation is performed through that injected host service's prepared actor transaction.

### Independent challenge

This is an actual unresolved contradiction inside the preserved design, not a comparison between a historical plan and a later storage refactor. Section 14 declares itself authoritative and expressly rejects a dedicated mob realm directory, while the resource chapter unconditionally requires one. Git blame places R1 and the preflight statement in the same v4.1 expansion, and the original baseline already created members through the supplied session service. Current member-host wiring follows that same ownership choice. Preserve the old resource wording as a superseded assumption rather than deleting the decision history.

- [docs/architecture/multi-host-mobs-plan.md:353-358](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L353-L358)

```text
**R1 — A materialized member's state lives in the member-host daemon's realm; `mob.{mob_id}` is never a store path.**
```

  The chapter explicitly owns the physical-state question and excludes per-mob physical realm creation.

- [docs/architecture/multi-host-mobs-plan.md:497-497](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L497)

```text
realm `mob.{id}` attachable with a persistent backend when `durable_sessions` was declared
```

  This unconditional preflight requirement contradicts R1's physical realm contract.

- [docs/architecture/multi-host-mobs-plan.md:605-605](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L605)

```text
Realm provisioning: the member host creates/attaches realm `mob.{id}` locally
```

  The revival paragraph repeats the opposite storage-ownership instruction without marking it superseded.

- Read-only reproduction:

```text
git show da467fca8:meerkat-mob/src/runtime/provisioner.rs | nl -ba | sed -n '1985,1989p'
```

  The original baseline calls self.session_service.create_session(req.create_session) at lines 1986-1989.

  The historical source already used the hosting service; this is not a newly introduced physical-state relocation.

- [meerkat-cli/src/mob_host.rs:1054-1064](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/mob_host.rs#L1054-L1064)

```text
session_service: Arc::clone(&service) as Arc<dyn MobSessionService>,
```

  The daemon injects its existing service and opened-backend durability into every member's substrate.

- [meerkat-mob/src/runtime/host_materialize.rs:2007-2025](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/host_materialize.rs#L2007-L2025)

```text
Arc::clone(&self.substrate.session_service),
```

  Materialization actually creates through that injected host service, not a newly attached per-mob persistence service.

**Required correction:** Add a bounded erratum or adjacent supersession annotation to section 15 R4 and section 15.7 identifying their per-mob physical-realm wording as superseded by section 14 R1. State that preflight checks the already-open daemon backend and memory uses that service's store_path; mob_realm_id supplies logical metadata/namespace/preferred-credential scope, not a new member-host directory or session service. Keep the original historical decision rationale.

### Correction

Added adjacent realm-scope errata to section 15 R4 and section 15.7, explicitly superseding only their per-mob physical-realm assumptions under section 14 R1. Preflight checks the daemon's already-open backend; revival uses its existing service/stores; memory uses that service's store_path/memory. mob_realm_id remains logical metadata, comms namespace, and preferred-credential scope rather than a new directory/service. Both original resource paragraphs and their historical rationale remain intact.

- [docs/architecture/multi-host-mobs-plan.md:499-499](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L499)

```text
Preflight checks the daemon's already-open backend and its durability fact, not a newly attached per-mob realm.
```

- [docs/architecture/multi-host-mobs-plan.md:609-609](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L609)

```text
Revival uses the daemon's existing session service and stores; memory lives at that service's `<store_path>/memory`, with unchanged session-scoped search semantics.
```

- [meerkat-cli/src/mob_host.rs:1054-1064](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/mob_host.rs#L1054-L1064)

```text
session_service: Arc::clone(&service) as Arc<dyn MobSessionService>,
```

- [meerkat-mob/src/runtime/host_materialize.rs:914-930](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L914-L930)

```text
realm_backend_persistent: substrate.realm_backend_persistent,
```

- [meerkat-mob/src/runtime/host_materialize.rs:2008-2018](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L2008-L2018)

```text
Arc::clone(&self.substrate.session_service),
```

- [meerkat/src/factory.rs:7044-7054](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L7044-L7054)

```text
let memory_dir = self.store_path.join("memory");
```

- [meerkat-mob/src/runtime/host_materialize.rs:872-886](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L872-L886)

```text
Some(&preferred_realm),
```

### Final independent review

**PASS:** Both conflicting physical-realm occurrences have explicit adjacent supersession notices under section 14 R1. The original R4 preflight paragraph and section 15.7 revival paragraph remain unchanged as history. The corrections name the already-open daemon backend, existing session service, and that service's memory path, while retaining mob_realm_id as logical scope. Current daemon composition, preflight observations, member creation, and memory setup all support this boundary.

- [docs/architecture/multi-host-mobs-plan.md:357-360](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L357-L360)

```text
**R1 — A materialized member's state lives in the member-host daemon's realm; `mob.{mob_id}` is never a store path.**
```

- [docs/architecture/multi-host-mobs-plan.md:499-499](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L499)

```text
Preflight checks the daemon's already-open backend and its durability fact, not a newly attached per-mob realm.
```

- [docs/architecture/multi-host-mobs-plan.md:609-609](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L609)

```text
Revival uses the daemon's existing session service and stores; memory lives at that service's `<store_path>/memory`, with unchanged session-scoped search semantics.
```

- [meerkat-cli/src/mob_host.rs:1054-1064](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/mob_host.rs#L1054-L1064)

```text
session_service: Arc::clone(&service) as Arc<dyn MobSessionService>,
```

  The daemon injects its existing service and realm_backend_persistent fact into HostMemberSubstrate.

- [meerkat-mob/src/runtime/host_materialize.rs:914-930](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L914-L930)

```text
realm_backend_persistent: substrate.realm_backend_persistent,
```

  Preflight consumes the opened host backend's durability fact, not a new per-mob realm attachment.

- [meerkat-mob/src/runtime/host_materialize.rs:2008-2018](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L2008-L2018)

```text
Arc::clone(&self.substrate.session_service),
```

  Materialization creates its actor through that same supplied service.

- [meerkat-mob/src/runtime/host_materialize.rs:872-886](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L872-L886)

```text
Some(&preferred_realm),
```

  mob_realm_id is used for preferred credential resolution, without creating a storage directory.

- [meerkat/src/factory.rs:7044-7055](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L7044-L7055)

```text
let memory_dir = self.store_path.join("memory");
```

  The enabled memory store is rooted at the factory's store_path and the search scope is for_session(session_id).

## A13-007: Portable-spec table incorrectly says operator context is never serialized and is minted on the member host

**Candidate severity:** medium. **Independent verdict:** confirm.

The cannot-travel table collapses the non-transferable authority seal and the intentionally transferable wire projection into one fact, then assigns minting to the wrong host. Later sections state the opposite contract. This is a residual historical ownership/representation contradiction; the finding does not claim a current authority bypass.

### Baseline documentation evidence

- [docs/architecture/multi-host-mobs-plan.md:418-418](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L418)

```text
re-minted by Host B's composition role, never serialized
```

- [docs/architecture/multi-host-mobs-plan.md:545-548](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L545-L548)

```text
mob_tool_authority_context: Option<WireMobToolAuthorityContext>
```

- [docs/architecture/multi-host-mobs-plan.md:512-512](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L512)

```text
mint stays controlling-host-only; the shipped context in the spec exists solely so the member-side build composes visibility
```

### Implementation proof

- [meerkat-contracts/src/wire/portable_spec.rs:223-226](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/portable_spec.rs#L223-L226)

```text
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
```

  PortableSpawnOverlay is an actual serialized wire type, not a process-local builder object.

- [meerkat-contracts/src/wire/portable_spec.rs:249-254](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/portable_spec.rs#L249-L254)

```text
pub mob_tool_authority_context: Option<WireMobToolAuthorityContext>,
```

  Its context projection is serialized for member-side visibility; the source explicitly assigns minting and dispatch re-resolution to the controlling host.

- [meerkat-mob/src/runtime/host_materialize.rs:721-741](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/host_materialize.rs#L721-L741)

```text
.and_then(serde_json::from_value::<meerkat_core::service::MobToolAuthorityContext>)
```

  The receiving host rehydrates the serialized projection without minting the private generation seal. This is not a second authority source.

### Independent challenge

Confirm only a representation/wording defect in the table. The domain context and its wire projection must not be conflated: the generation seal really is non-transferable, and the member host really does rehydrate an unsealed value. However, the row says the entire named context is never serialized and is re-minted by Host B without that qualification, while the same v4.1 chapter ships its facts in WireMobToolAuthorityContext and reserves authoritative minting to the controller. The domain type derives serde with only the seal skipped. A correction must retain, not reverse, the table's valid prohibition on transporting sealed authority and must not allege a runtime authority bypass.

- [docs/architecture/multi-host-mobs-plan.md:418-418](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L418)

```text
re-minted by Host B's composition role, never serialized
```

  The unqualified row fails to distinguish reconstruction of visibility facts from minting sealed dispatch authority.

- [docs/architecture/multi-host-mobs-plan.md:545-548](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L545-L548)

```text
mob_tool_authority_context: Option<WireMobToolAuthorityContext>
```

  The same design includes the serialized projection and explicitly says it is minted controlling-side and shipped for visibility.

- [meerkat-core/src/service/mod.rs:852-856](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/service/mod.rs#L852-L856)

```text
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct MobToolAuthorityContext {
    #[serde(skip)]
    generated_authority_seal: MobToolAuthorityContextSeal,
```

  Only the private seal is serialization-excluded; the distinction is precise and does not require changing architecture.

- [meerkat-mob/src/runtime/spec_compiler.rs:643-693](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/spec_compiler.rs#L643-L693)

```text
.map(project_mob_tool_authority_context)
```

  The controller obtains generated operator authority and projects its facts into the portable spec.

- [meerkat-mob/src/runtime/host_materialize.rs:721-741](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/host_materialize.rs#L721-L741)

```text
.and_then(serde_json::from_value::<meerkat_core::service::MobToolAuthorityContext>)
```

  The receiver deserializes the projection without minting a private generation seal; it does not become a second dispatch-authority owner.

**Required correction:** Qualify the section 14.2 table row: sealed authoritative context never crosses the wire; its WireMobToolAuthorityContext facts do travel for visibility and are rehydrated unsealed on the member host; authoritative minting/re-resolution remains controlling-host-owned under section 15 R6. Preserve the original row as annotated historical shorthand if desired. Do not remove the distinction between the domain authority and its projection or imply that a serialized seal is supported.

### Correction

Qualified the section 14.2 cannot-travel row to separate the non-transferable private generation seal from serialized WireMobToolAuthorityContext facts. The member host rehydrates an unsealed visibility context; authoritative minting and dispatch-time re-resolution remain controlling-host-owned under section 15 R6. Preserved the original re-minted/never-serialized phrase as explicitly superseded shorthand, without alleging an implementation bypass or permitting a serialized seal.

- [docs/architecture/multi-host-mobs-plan.md:418-418](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L418)

```text
Sealed authority never crosses the wire. Its `WireMobToolAuthorityContext` facts do travel for visibility; Host B rehydrates an unsealed context.
```

- [meerkat-core/src/service/mod.rs:852-855](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/service/mod.rs#L852-L855)

```text
#[serde(skip)]
    generated_authority_seal: MobToolAuthorityContextSeal,
```

- [meerkat-contracts/src/wire/portable_spec.rs:249-254](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/portable_spec.rs#L249-L254)

```text
pub mob_tool_authority_context: Option<WireMobToolAuthorityContext>,
```

- [meerkat-mob/src/runtime/spec_compiler.rs:643-660](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/spec_compiler.rs#L643-L660)

```text
meerkat_runtime::mob_operator_authority::grant_spawn_profiles_in_mob(
```

- [meerkat-mob/src/runtime/host_materialize.rs:722-741](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L722-L741)

```text
.and_then(serde_json::from_value::<meerkat_core::service::MobToolAuthorityContext>)
```

### Final independent review

**PASS:** The table now distinguishes non-transferable sealed authority from its serializable visibility projection. It retains the original shorthand as an explicitly superseded representation error, states that Host B rehydrates an unsealed value, and leaves authoritative minting and dispatch re-resolution on the controller. This is supported by the skipped private seal, the portable wire field, the host's serde conversion, and the controller's durable-facts-to-generated-authority execution path; no authority-bypass allegation or transportable-seal claim was introduced.

- [docs/architecture/multi-host-mobs-plan.md:418-418](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L418)

```text
Sealed authority never crosses the wire. Its `WireMobToolAuthorityContext` facts do travel for visibility; Host B rehydrates an unsealed context.
```

- [meerkat-core/src/service/mod.rs:852-856](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/service/mod.rs#L852-L856)

```text
#[serde(skip)]
    generated_authority_seal: MobToolAuthorityContextSeal,
```

- [meerkat-contracts/src/wire/portable_spec.rs:249-254](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/portable_spec.rs#L249-L254)

```text
pub mob_tool_authority_context: Option<WireMobToolAuthorityContext>,
```

- [meerkat-mob/src/runtime/spec_compiler.rs:643-693](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/spec_compiler.rs#L643-L693)

```text
.map(project_mob_tool_authority_context)
```

  The controlling-side compiler obtains generated operator authority and projects its serde-visible facts into the spec.

- [meerkat-mob/src/runtime/host_materialize.rs:722-741](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L722-L741)

```text
.and_then(serde_json::from_value::<meerkat_core::service::MobToolAuthorityContext>)
```

  Member-host reconstruction cannot mint the serde-skipped private generation seal.

- [meerkat-mob/src/runtime/upcall_responder.rs:813-829](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/upcall_responder.rs#L813-L829)

```text
.load_committed_placed_member_operator_authority(
```

  Controller execution reads generation-bound committed capability facts rather than accepting authority from the request.

- [meerkat-mob/src/runtime/upcall_responder.rs:897-907](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/upcall_responder.rs#L897-L907)

```text
let context = mint_mob_operator_authority_from_facts(&facts, provenance, request_id)
```

  Those facts are re-minted through generated authority on the controlling-side execution path.

## A13-008: Remote-fork lifecycle chapter puts kickoff history in a spec that explicitly excludes the kickoff

**Candidate severity:** medium. **Independent verdict:** confirm.

The lifecycle chapter and phase checklist describe a wire carrier that the same document deliberately excludes. Fork itself remains supported and Fork remains absent as a launch variant; only the claimed carrier is wrong. The distinction matters for digest coverage, deferred initial turns, and restart-safe kickoff delivery.

### Baseline documentation evidence

- [docs/architecture/multi-host-mobs-plan.md:560-560](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L560)

```text
Not in the spec, deliberately: the kickoff/initial message (delivered on the first real turn via `DeliverMemberInput`
```

- [docs/architecture/multi-host-mobs-plan.md:1088-1088](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1088)

```text
the rendered context travels inside the materialize `spec`'s initial prompt
```

- [docs/architecture/multi-host-mobs-plan.md:1098-1098](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1098)

```text
the rendered fork text is already inside the materialize spec's initial prompt
```

- [docs/architecture/multi-host-mobs-plan.md:1157-1157](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1157)

```text
remote-target fork (falls out of spec-carried prompt text
```

### Implementation proof

- [meerkat-contracts/src/wire/portable_spec.rs:23-49](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/portable_spec.rs#L23-L49)

```text
pub struct PortableMemberSpec {
```

  The complete closed struct has identity, profile, definition extract, overlay, and required_env_keys; there is no initial-message field. Its contract explicitly places kickoff on DeliverMemberInput.

- [meerkat-mob/src/runtime/actor.rs:29843-29872](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L29843-L29872)

```text
text: format!("{fork_text}\n\n"),
```

  The controlling actor adds rendered fork history to the separate first-turn prompt and stores that prompt in MobPlacedKickoffIntent, rather than adding it to PortableMemberSpec.

- [meerkat-mob/src/runtime/host_materialize.rs:1995-2000](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/host_materialize.rs#L1995-L2000)

```text
meerkat_core::types::ContentInput::Text(String::new()),
```

  Member materialization creates the session with an empty initial input; kickoff arrives later. This distinguishes a user/first-turn prompt from the spec's system-prompt field.

### Independent challenge

The fork-mode decision is valid history and must remain. The carrier description is nevertheless internally inconsistent: section 15.3 deliberately excludes initial input, whereas the lifecycle chapter and phase checklist put the rendered fork input inside that same materialization spec. The cited original local fork source builds fork text into ContentInput, not the system-prompt override, so 'initial prompt' cannot be rescued by treating it as the portable system prompt. Current code likewise constructs a separate kickoff before compiling the spec and creates the member with empty initial input.

- [docs/architecture/multi-host-mobs-plan.md:560-560](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L560)

```text
Not in the spec, deliberately: the kickoff/initial message (delivered on the first real turn via `DeliverMemberInput`
```

  This is an explicit design exclusion, not an inference from today's field names.

- [docs/architecture/multi-host-mobs-plan.md:1088-1098](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1088-L1098)

```text
the rendered context travels inside the materialize `spec`'s initial prompt
```

  The contrary carrier claim appears in L1 and is repeated in the remote-target row of L2.

- [docs/architecture/multi-host-mobs-plan.md:1157-1157](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1157)

```text
remote-target fork (falls out of spec-carried prompt text
```

  The phase checklist repeats the same carrier error; the separate archive-order phrase in this line is not accepted for change under A13-009.

- Read-only reproduction:

```text
git show da467fca8:meerkat-mob/src/runtime/actor.rs | nl -ba | sed -n '9237,9251p'
```

  Lines 9240-9248 prepend fork_text to base_prompt ContentInput blocks; lines 9249-9251 store initial_turn_prompt and pass prompt to to_create_session_request separately from config.

  The historical source cited by L1 already distinguished first-turn input from the build/system-prompt configuration.

- [meerkat-mob/src/runtime/actor.rs:29843-29899](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L29843-L29899)

```text
text: format!("{fork_text}\n\n"),
```

  The controller constructs a separate first-turn prompt and MobPlacedKickoffIntent; the following compile_portable_member_spec call receives build fields but not that prompt.

- [meerkat-mob/src/runtime/host_materialize.rs:1995-2000](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/host_materialize.rs#L1995-L2000)

```text
meerkat_core::types::ContentInput::Text(String::new()),
```

  Member materialization uses empty initial input because kickoff arrives separately via DeliverMemberInput.

**Required correction:** Annotate or narrowly correct the fork-carrier phrases in 19.L1, the remote-target bullet of 19.L2, and the Phase 3 checklist: controlling-side rendered fork history belongs to the separate first-turn/kickoff input, delivered after materialization through DeliverMemberInput, not PortableMemberSpec. Preserve the original no-Fork-wire-mode decision, historical launch enum, and phase sequencing. Do not change the unrelated disposal-order shorthand on the shared checklist line.

### Correction

Corrected all three fork-carrier occurrences: section 19.L1, the remote-target bullet in 19.L2, and the Phase 3 checklist now place controlling-side rendered history in separate first-turn/kickoff input delivered after materialization via DeliverMemberInput, not PortableMemberSpec. Added a carrier-only erratum preserving the superseded wording as history. Left the historical Fresh/Resume enum, no-Fork-wire-mode decision, phase sequence, and the shared checklist's entire ReleaseMember disposal-order suffix unchanged.

- [docs/architecture/multi-host-mobs-plan.md:1092-1092](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1092)

```text
the rendered context belongs to the separate first-turn/kickoff input, delivered after materialization via `DeliverMemberInput` (§15.3), not to `PortableMemberSpec`
```

- [docs/architecture/multi-host-mobs-plan.md:1094-1094](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1094)

```text
This corrects the carrier description only: the v4.1 Fresh/Resume launch decision and Phase 3 remote-target / Phase 6 remote-source sequence are unchanged.
```

- [docs/architecture/multi-host-mobs-plan.md:1104-1104](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1104)

```text
the controlling-side rendered fork text travels in the separate first-turn/kickoff input via `DeliverMemberInput` after materialization, not in `PortableMemberSpec`
```

- [docs/architecture/multi-host-mobs-plan.md:1163-1163](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1163)

```text
remote-target fork (falls out of controlling-side rendered first-turn/kickoff input, delivered after materialization by `DeliverMemberInput`, not `PortableMemberSpec`
```

- [meerkat-contracts/src/wire/portable_spec.rs:23-49](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/portable_spec.rs#L23-L49)

```text
The kickoff/initial message is absent:
```

- [meerkat-mob/src/runtime/actor.rs:29843-29872](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L29843-L29872)

```text
let prompt = if let Some(fork_text) = fork_context_text {
```

- [meerkat-mob/src/runtime/host_materialize.rs:1995-2000](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L1995-L2000)

```text
// Step 5 — create. Kickoff arrives later via DeliverMemberInput
```

### Final independent review

**PASS:** All three accepted occurrences now put controlling-side rendered fork history in separate first-turn/kickoff input, not PortableMemberSpec. The carrier-only erratum preserves the old wording as history and leaves the historical Fresh/Resume decision and Phase 3 remote-target / Phase 6 remote-source sequence intact. Actual spec structure excludes kickoff; the actor constructs kickoff separately and the member is created with empty initial input. The shared checklist's entire ReleaseMember suffix and section 19.L3 were compared with the audit baseline and are unchanged, preserving rejected A13-009.

- [docs/architecture/multi-host-mobs-plan.md:1092-1092](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1092)

```text
the rendered context belongs to the separate first-turn/kickoff input, delivered after materialization via `DeliverMemberInput` (§15.3), not to `PortableMemberSpec`
```

- [docs/architecture/multi-host-mobs-plan.md:1094-1094](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1094)

```text
This corrects the carrier description only: the v4.1 Fresh/Resume launch decision and Phase 3 remote-target / Phase 6 remote-source sequence are unchanged.
```

- [docs/architecture/multi-host-mobs-plan.md:1104-1104](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1104)

```text
the controlling-side rendered fork text travels in the separate first-turn/kickoff input via `DeliverMemberInput` after materialization, not in `PortableMemberSpec`
```

- [docs/architecture/multi-host-mobs-plan.md:1163-1163](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1163)

```text
remote-target fork (falls out of controlling-side rendered first-turn/kickoff input, delivered after materialization by `DeliverMemberInput`, not `PortableMemberSpec`
```

- [meerkat-contracts/src/wire/portable_spec.rs:23-49](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/portable_spec.rs#L23-L49)

```text
The kickoff/initial message is absent:
```

  The complete PortableMemberSpec contains resolved profile/definition/overlay material and required environment names, not first-turn input or launch mode.

- [meerkat-mob/src/runtime/actor.rs:29843-29899](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L29843-L29899)

```text
text: format!("{fork_text}\n\n"),
```

  Fork text is prepended to ContentInput and stored in MobPlacedKickoffIntent; the following compile_portable_member_spec call does not receive that input.

- [meerkat-mob/src/runtime/host_materialize.rs:1995-2000](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/host_materialize.rs#L1995-L2000)

```text
meerkat_core::types::ContentInput::Text(String::new()),
```

  Materialization creates without kickoff input; its explicit delivery boundary is later DeliverMemberInput.

- [meerkat-mob/src/runtime/placed_kickoff_reconciler.rs:571-582](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/placed_kickoff_reconciler.rs#L571-L582)

```text
prompt: intent.prompt.clone(),
```

  The reconciler reconstructs the turn from the separately persisted kickoff intent, not by reading an initial prompt from the portable build spec.

- [meerkat-contracts/src/wire/supervisor_bridge.rs:1167-1180](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/supervisor_bridge.rs#L1167-L1180)

```text
resume_from_role: Option<String>,
```

  The current enum is Fresh/Resume with the already-recorded additive resume role field; Fork remains absent. The correction correctly did not overwrite the preserved v4.1 enum.

## A13-009: Multi-host phase checklist contradicts its explicit quiesce-before-archive disposal rule

**Candidate severity:** medium. **Independent verdict:** reject.

The phase checklist reintroduces the exact shorthand that the preceding lifecycle rule calls unsafe. This is an intra-plan instruction contradiction, not evidence that current teardown is wrong. The modern disposal code further confirms that outer quiescence and the archive protocol's internal commit-before-retire sequence must not be conflated.

### Baseline documentation evidence

- [docs/architecture/multi-host-mobs-plan.md:1109-1109](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1109)

```text
an implementer following a flat 'archive-first' rule would archive a still-running session and race
```

- [docs/architecture/multi-host-mobs-plan.md:1157-1157](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1157)

```text
`ReleaseMember` full disposal realization (archive-first order, idempotency, ephemeral degradation)
```

### Implementation proof

- [meerkat-mob/src/runtime/provisioner.rs:3536-3562](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L3536-L3562)

```text
.acquire_quiescent_runtime_turn_finalization_boundary(session_id, deadline)
```

  The executable disposal path obtains the quiescent turn-finalization boundary before invoking the archive-under-boundary path.

- [meerkat-mob/src/runtime/provisioner.rs:3655-3678](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L3655-L3678)

```text
// protocol themselves: durable document terminal first, runtime
```

  The current implementation distinguishes the archive protocol's internal commit-first ordering from prior turn quiescence; it explicitly warns against pre-retiring an owned session before that protocol archives its snapshot.

- [meerkat-mob/src/runtime/host_materialize.rs:1514-1524](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/host_materialize.rs#L1514-L1524)

```text
let verdict = self.disposal.dispose(session_id).await?;
```

  Host release uses the shared disposal arc, rather than an independent archive-first implementation.

### Independent challenge

The candidate interprets a checklist's 'archive-first order' shorthand as an instruction to archive before quiescing, but the checklist does not say that. It references the full ReleaseMember realization, which the same chapter and A19 explicitly define as outer quiescence followed by a commit-first archive protocol. Both the design-era source and current source implement that distinction. The historical paragraph even warns against the candidate's flat reading. An optional terminology clarification is not proof of a contradictory disposal instruction, and updating this historical checklist to today's evolved retirement internals would risk rewriting history.

- [docs/architecture/multi-host-mobs-plan.md:102-102](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L102)

```text
`ReleaseMember` = outer quiesce (cancel → runtime retire → drain) THEN the commit-first archive protocol
```

  The adjudication ledger explicitly settles the two-layer ordering.

- [docs/architecture/multi-host-mobs-plan.md:1109-1109](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1109)

```text
THEN the archive protocol, whose commit-first ordering is protocol-INTERNAL
```

  The detailed realization disambiguates the checklist's archive-first terminology and expressly forbids archiving a running session.

- Read-only reproduction:

```text
git show da467fca8:meerkat-mob/src/runtime/provisioner.rs | nl -ba | sed -n '857,867p'
```

  archive_with_authority_then_unregister calls self.retire_runtime_before_archive(session_id).await? at line 866 before entering the archive portion.

  The original source, not merely later code, confirms that the named archive-first protocol was entered after outer retirement/quiescence.

- [meerkat-mob/src/runtime/provisioner.rs:3542-3562](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L3542-L3562)

```text
.acquire_quiescent_runtime_turn_finalization_boundary(session_id, deadline)
```

  Today's implementation still obtains quiescence before archive-under-boundary; it does not reveal a hidden contradictory ordering.

**Disposition:** Rejected; no correction authorized for this claim.

## A13-010: Two portable-spec descriptions still claim budget splitting after the plan explicitly deletes it

**Candidate severity:** low. **Independent verdict:** reject.

Section 18.3 deliberately records the no-split adjudication and even supplies replacement wording for section 7.3. That is legitimate history, not the defect. The two remaining portable-spec summaries still affirm a computed split without marking that assertion superseded, despite the ledger saying chapter text incorporates its decisions.

### Baseline documentation evidence

- [docs/architecture/multi-host-mobs-plan.md:409-409](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L409)

```text
budget seed (split computed controlling-side, §7.3)
```

- [docs/architecture/multi-host-mobs-plan.md:550-551](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L550-L551)

```text
split computed controlling-host (§7.3); seed rides
```

- [docs/architecture/multi-host-mobs-plan.md:967-969](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L967-L969)

```text
**O4 — `PortableSpawnOverlay.budget_limits` is the single budget carrier; `BudgetSplitPolicy` is deleted.**
```

### Implementation proof

- [meerkat-mob/src/runtime/spec_compiler.rs:614-629](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/spec_compiler.rs#L614-L629)

```text
budget_limits: budget_limits.cloned(),
```

  The spec compiler passes the caller's BudgetLimits through; this is not a controlling-host split-policy computation.

- [meerkat-contracts/src/wire/portable_spec.rs:255-263](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/portable_spec.rs#L255-L263)

```text
pub budget_limits: Option<meerkat_core::BudgetLimits>,
```

  The declared wire carrier is the overlay's resolved budget limits.

- [meerkat-contracts/src/wire/supervisor_bridge.rs:1029-1053](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/supervisor_bridge.rs#L1029-L1053)

```text
/// digest-covered `spec.overlay.budget_limits`
```

  The complete materialize payload and its contract pin one digest-covered budget carrier and explicitly exclude a payload-level sibling.

### Independent challenge

This is a preserved, expressly superseded design assumption, not an unresolved or current budget contract. The two old phrases point to section 7.3, which now contains the explicit v4.1.2 erratum, names the sole overlay carrier, and says budget-split vocabulary was deleted. O4 independently quotes the earlier computed-split sentence, labels it overstated, supplies its replacement, and explains why split computation was not implemented. That correction already existed in the original v4.1 expansion; later implementation narrowed the carrier further. Current pass-through code agrees with the recorded final choice. Requiring every retained sketch to be rewritten would erase or unnecessarily normalize an already-adjudicated historical alternative.

- [docs/architecture/multi-host-mobs-plan.md:240-240](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L240)

```text
Budget-split vocabulary does not exist on the wire (§18 O4 deletion).
```

  The very section referenced by the stale phrases gives the settled behavior and an explicit implementation erratum.

- [docs/architecture/multi-host-mobs-plan.md:965-969](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L965-L969)

```text
**overstates HEAD and is corrected here**
```

  The record itself identifies the computed-split claim as the superseded alternative; this is not a second active requirement.

- Read-only reproduction:

```text
git show 85a9fb6f0f:docs/architecture/multi-host-mobs-plan.md | nl -ba | sed -n '951,957p'
```

  Original v4.1 line 953 already says the computed-split sentence overstates HEAD and is corrected here; line 955 reads '**O4 — `MaterializeMember.budget_seed` is pass-through `BudgetLimits`; `BudgetSplitPolicy` is deleted, not carried.**'; line 957 supplies the replacement sentence.

  The no-split adjudication is historical evidence in its own right, not an interpretation imposed from current code.

- [meerkat-mob/src/runtime/spec_compiler.rs:614-629](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/spec_compiler.rs#L614-L629)

```text
budget_limits: budget_limits.cloned(),
```

  Current compilation implements the already-recorded pass-through choice.

- [meerkat-contracts/src/wire/supervisor_bridge.rs:1029-1053](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/supervisor_bridge.rs#L1029-L1053)

```text
/// digest-covered `spec.overlay.budget_limits`
```

  The closed materialize payload agrees with the explicit later carrier erratum; it does not expose a contradictory live budget API.

**Disposition:** Rejected; no correction authorized for this claim.

## A13-011: Multi-host containment summary overstates the no-secret rule as no token ever crossing the wire

**Candidate severity:** medium. **Independent verdict:** confirm.

This is an internal overstatement of the historical threat model: its refined adjudication and transport-exposure paragraph already acknowledge the sanctioned live token exception (and the legacy member-bind bearer). The finding does not allege a newly discovered implementation vulnerability or dispute host-local provider credential custody.

### Baseline documentation evidence

- [docs/architecture/multi-host-mobs-plan.md:1188-1188](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1188)

```text
No credential, token, lease, or provider secret ever arrives on the wire
```

- [docs/architecture/multi-host-mobs-plan.md:88-88](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L88)

```text
The sanctioned multi-host exception is single-use, 60s-TTL, channel-pinned live WS tokens
```

- [docs/architecture/multi-host-mobs-plan.md:1196-1196](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1196)

```text
On-path readers see envelope contents: peer messages, bridge payloads, event pages, and live WS tokens
```

### Implementation proof

- [meerkat-contracts/src/wire/supervisor_bridge.rs:1894-1899](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/supervisor_bridge.rs#L1894-L1899)

```text
pub struct BridgeLiveOpenedResponse {
    pub open: LiveOpenResult,
}
```

  The remote live-channel bridge response serializes the full live-open result.

- [meerkat-contracts/src/wire/live.rs:232-236](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/live.rs#L232-L236)

```text
pub transport: WireLiveTransportBootstrap,
```

  The bridge-carried result includes the typed transport bootstrap rather than a token-free identifier.

- [meerkat-contracts/src/wire/live.rs:253-257](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/live.rs#L253-L257)

```text
Websocket { url: String, token: String },
```

  The concrete serialized WebSocket bootstrap has a bearer token field. The blanket no-token statement cannot be true even while provider credentials remain host-local.

### Independent challenge

Confirm the blanket quantifier, not a new security finding. 'No credential, token ... ever' is broader than durable provider credential custody, and the same historical threat model explicitly acknowledges wire-carried live tokens. The typed bridge result and production sender independently prove that this exception is real, not a rejected design or unused field. A correction should retain both the historical confidentiality limitation and the provider-local custody decision.

- [docs/architecture/multi-host-mobs-plan.md:1188-1188](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1188)

```text
No credential, token, lease, or provider secret ever arrives on the wire
```

  The absolute no-token statement is the precise overclaim.

- [docs/architecture/multi-host-mobs-plan.md:88-88](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L88)

```text
The sanctioned multi-host exception is single-use, 60s-TTL, channel-pinned live WS tokens
```

  The adjudication already supplies the bounded exception that the containment summary omits.

- [meerkat-contracts/src/wire/live.rs:230-257](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/live.rs#L230-L257)

```text
Websocket { url: String, token: String },
```

  LiveOpenResult contains a serialized transport bootstrap whose WebSocket variant includes the token.

- [meerkat-runtime/src/comms_drain.rs:5754-5765](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/comms_drain.rs#L5754-L5765)

```text
BridgeReply::MemberLiveChannelOpened(BridgeLiveOpenedResponse { open }),
```

  The production receiver forwards the live.open result in its bridge response, proving the token-carrying shape is actually on the send path.

- [docs/architecture/multi-host-mobs-plan.md:1196-1196](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1196)

```text
The older member-binding protocol still exposes its raw `BindMember.bootstrap_token`
```

  The existing record also names the historical member-bind exception; the fix must not remove or conceal it.

**Required correction:** Narrow the 20.1 credentials summary to durable provider credentials/secrets and auth leases, then explicitly retain the live bootstrap-token exception and legacy BindMember residual by reference to A5/20.2. Do not claim transport confidentiality, alter the threat model's historical scope, or describe a new runtime vulnerability.

### Correction

Narrowed section 20.1's blanket no-token claim to durable provider credentials/secrets and auth leases on new multi-host payloads. Explicitly retained A5/20.2's single-use, 60s-TTL, channel-pinned live WS bootstrap-token exception and the pre-existing BindMember bootstrap bearer residual. Kept the signed-but-unencrypted posture and original host-local credential trust boundary; section 20.2 is byte-for-byte unchanged and no new vulnerability claim was introduced.

- [docs/architecture/multi-host-mobs-plan.md:1194-1194](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1194)

```text
This is not a blanket no-token claim: A5/§20.2 retain the single-use, 60s-TTL, channel-pinned live WS bootstrap-token exception and the pre-existing `BindMember.bootstrap_token` bearer residual on the signed-but-unencrypted link.
```

- [docs/architecture/multi-host-mobs-plan.md:1202-1202](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1202)

```text
The older member-binding protocol still exposes its raw `BindMember.bootstrap_token`
```

- [meerkat-contracts/src/wire/live.rs:253-257](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/live.rs#L253-L257)

```text
Websocket { url: String, token: String },
```

- [meerkat-contracts/src/wire/supervisor_bridge.rs:1894-1899](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/supervisor_bridge.rs#L1894-L1899)

```text
pub open: LiveOpenResult,
```

- [meerkat-runtime/src/comms_drain.rs:5754-5765](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/comms_drain.rs#L5754-L5765)

```text
BridgeReply::MemberLiveChannelOpened(BridgeLiveOpenedResponse { open }),
```

### Final independent review

**PASS:** The containment summary is narrowed to durable provider credentials/secrets and auth leases in new multi-host payloads, with explicit live WebSocket bootstrap-token and legacy BindMember bearer exceptions. A5 and section 20.2 are preserved; the latter was compared byte-for-byte with the baseline. The note retains signed-but-unencrypted transport and host-local custody rather than inventing confidentiality or alleging a new vulnerability. Typed replies and the production send path establish actual token transport; the default TTL and generated token admission support the stated bounded live-token exception.

- [docs/architecture/multi-host-mobs-plan.md:1194-1194](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1194)

```text
This is not a blanket no-token claim: A5/§20.2 retain the single-use, 60s-TTL, channel-pinned live WS bootstrap-token exception and the pre-existing `BindMember.bootstrap_token` bearer residual on the signed-but-unencrypted link.
```

- [docs/architecture/multi-host-mobs-plan.md:1202-1202](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L1202)

```text
The older member-binding protocol still exposes its raw `BindMember.bootstrap_token`;
```

  The pre-existing limitation and transport-exposure discussion remain intact.

- [meerkat-contracts/src/wire/live.rs:250-257](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/live.rs#L250-L257)

```text
Websocket { url: String, token: String },
```

- [meerkat-runtime/src/comms_drain.rs:5754-5765](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/comms_drain.rs#L5754-L5765)

```text
BridgeReply::MemberLiveChannelOpened(BridgeLiveOpenedResponse { open }),
```

  The real bridge response forwards LiveOpenResult, including its token-bearing transport bootstrap.

- [meerkat-live/src/transport.rs:68-69](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-live/src/transport.rs#L68-L69)

```text
pub const TOKEN_TTL: Duration = Duration::from_secs(60);
```

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:28739-28768](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L28739-L28768)

```text
guard "token_not_consumed" { !self.live_websocket_consumed_tokens.contains(token) }
```

  The accepted transition also requires the recorded channel to match and the token not to be expired, then inserts it into the consumed set.

- [meerkat-contracts/src/wire/supervisor_bridge.rs:3137-3149](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/supervisor_bridge.rs#L3137-L3149)

```text
pub bootstrap_token: BridgeBootstrapToken,
```

  The legacy member-bind payload retains the acknowledged bearer field; the correction does not conceal it.

## A13-012: Fence limitation summaries incorrectly include supervisor epoch checks that existed at the plan baseline

**Candidate severity:** low. **Independent verdict:** reject.

A22 and section 17.8 merge two separate authority facts into an unqualified absence claim, while section 20.2 correctly distinguishes them. Because the epoch check is provable in da467fca8 itself, this is a factual inconsistency in the historical design record, not a demand to update every v1 limitation to current HEAD.

### Baseline documentation evidence

- [docs/architecture/multi-host-mobs-plan.md:105-105](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L105)

```text
nothing validates fence/epoch member-side on delivery in v1
```

- [docs/architecture/multi-host-mobs-plan.md:885-885](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L885)

```text
nothing validates fence/epoch member-side on delivery (§6.4)
```

- [docs/architecture/multi-host-mobs-plan.md:1194-1194](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1194)

```text
a stale supervisor is rejected by epoch, but delivery commands carry no member-fence check
```

### Implementation proof

- Read-only reproduction:

```text
git show da467fca8:meerkat-runtime/src/comms_drain.rs | sed -n '2668,2690p'
```

  The DeliverMemberInput arm constructs BridgeSupervisorPayload with epoch: payload.epoch, calls resolve_authorized_supervisor_with_response_route, and returns a bridge failure without delivering when that admission returns Err.

  This check was already present in the exact historical source baseline named in the plan; the mismatch is not caused by later fencing enhancements.

- Read-only reproduction:

```text
git show da467fca8:meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs | sed -n '20259,20267p'
```

  transition ResolveSupervisorBridgeCommandAdmissionAccepted {
              per_phase [Idle, Attached, Running]
              on input ResolveSupervisorBridgeCommandAdmission { supervisor_peer_id, supervisor_epoch, sender_peer_id }
              guard "supervisor_bound" { self.supervisor_binding_kind == SupervisorBindingKind::Bound }
              guard "supervisor_peer_id_matches_current" { self.supervisor_bound_peer_id == Some(supervisor_peer_id) }
              guard "supervisor_epoch_matches_current" { self.supervisor_bound_epoch == Some(supervisor_epoch) }
              guard "sender_peer_id_matches_current" { sender_peer_id == self.supervisor_bound_peer_id }
              update {}
              to Idle

  Generated acceptance at the design-era baseline explicitly requires equality with the bound supervisor epoch.

- [meerkat-runtime/src/comms_drain.rs:1135-1157](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/comms_drain.rs#L1135-L1157)

```text
payload.epoch,
```

  Current supervisor admission still forwards the requested epoch to the generated authority; epoch and member-incarnation fencing are distinct checks.

### Independent challenge

The candidate conflates topology/member-incarnation fencing with supervisor-binding epoch admission. Section 6.4, explicitly referenced by the disputed surface summary, spells out 'fence/topology-epoch' and immediately says 'only supervisor identity+epoch'. Section 20.2 repeats that a stale supervisor is rejected by epoch. The original generated guards therefore confirm the distinction already recorded by the plan; they do not disprove its historical member-side fencing limitation. The abbreviated 'fence/epoch' could be expanded for readability, but it is not a proven assertion that supervisor epoch admission was absent. Do not retrofit later delivery-fencing behavior into the v1 record.

- [docs/architecture/multi-host-mobs-plan.md:187-187](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L187)

```text
Today **nothing validates fence/topology-epoch member-side on delivery** (only supervisor identity+epoch)
```

  The governing section expressly differentiates topology epoch from the admitted supervisor epoch; the candidate's contrary interpretation omits this qualifier.

- [docs/architecture/multi-host-mobs-plan.md:885-885](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L885)

```text
nothing validates fence/epoch member-side on delivery (§6.4)
```

  The shorthand points directly to the section that defines which epoch is meant.

- [docs/architecture/multi-host-mobs-plan.md:1194-1194](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L1194)

```text
a stale supervisor is rejected by epoch, but delivery commands carry no member-fence check
```

  The historical threat model already states the exact source-backed distinction requested by the finding.

- Read-only reproduction:

```text
git show da467fca8:meerkat-runtime/src/comms_drain.rs | nl -ba | sed -n '2668,2688p'
```

  DeliverMemberInput constructs BridgeSupervisorPayload with payload.epoch and runs resolve_authorized_supervisor_with_response_route before delivery.

  Independent design-era source proves supervisor admission, which the plan already acknowledges; it does not prove topology/member-fence validation.

- Read-only reproduction:

```text
git show da467fca8:meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs | nl -ba | sed -n '20259,20270p'
```

  ResolveSupervisorBridgeCommandAdmissionAccepted requires supervisor_peer_id_matches_current, supervisor_epoch_matches_current, and sender_peer_id_matches_current; the epoch guard is self.supervisor_bound_epoch == Some(supervisor_epoch).

  The exact epoch owner is the supervisor binding, not the topology/member-incarnation owner alleged to be missing.

**Disposition:** Rejected; no correction authorized for this claim.

## A13-013: A4 adjudication ledger still promises inherited-workgraph suppression instead of the recorded hard rejection

**Candidate severity:** medium. **Independent verdict:** confirm.

The top-level adjudication ledger claims a disabled-with-record outcome that the detailed R6/O5 sections expressly replace with rejection because the inherited authority witnesses are not portable. This is an unpropagated decision inside the historical plan, not evidence of an absent current feature. It matters because A4 presents itself as the definitive cross-chapter resolution.

### Baseline documentation evidence

- [docs/architecture/multi-host-mobs-plan.md:87-87](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L87)

```text
Inherited-OPEN workgraph resolves to disabled-with-typed-record; EXPLICIT assertion rejects.
```

- [docs/architecture/multi-host-mobs-plan.md:511-511](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L511)

```text
Consequently `non_portable_disabled` is an explicitly empty record in v1.
```

- [docs/architecture/multi-host-mobs-plan.md:977-977](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L977)

```text
a remote spawn carrying the non-serializable inherited tool-visibility authority is hard-denied first as `NonPortableInheritedToolFilter`
```

### Implementation proof

- [meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:6469-6483](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/mob_machine.rs#L6469-L6483)

```text
emit SpawnMemberAdmissionResolved { admission: MobSpawnMemberAdmissionKind::NonPortableInheritedToolFilter }
```

  The canonical generated admission rule rejects a remote placement with inherited_tool_filter_present; it does not materialize a member with an inherited workgraph category silently narrowed and recorded.

- [meerkat-mob/src/runtime/actor.rs:29893-29899](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L29893-L29899)

```text
non_portable_disabled: Vec::new(),
```

  The actual remote-spec compilation path supplies an empty disabled-resource record, agreeing with the later detailed rule rather than the ledger's suppression promise.

### Independent challenge

Confirm only an unmarked implementation supersession in the adjudication summary. Independent history shows A4 really was the original July 7 disabled-with-record decision, so it must not be rewritten as though hard rejection had always been the decision. The implementation commit changed the detailed R6/O5 contract to hard denial while leaving A4 unchanged. Unlike the budget record's explicit quoted replacement/erratum, the A4 row still presents the earlier outcome without identifying its supersession, although the ledger claims the chapter text incorporates its decisions. Current machine guards and empty compilation record establish which implemented outcome the annotation must identify.

- [docs/architecture/multi-host-mobs-plan.md:82-87](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L82-L87)

```text
Inherited-OPEN workgraph resolves to disabled-with-typed-record; EXPLICIT assertion rejects.
```

  The top-level final-adjudication summary still states the earlier suppression outcome.

- [docs/architecture/multi-host-mobs-plan.md:511-511](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/multi-host-mobs-plan.md?plain=1#L511)

```text
Consequently `non_portable_disabled` is an explicitly empty record in v1.
```

  The implemented detailed chapter states a different outcome and does not describe this as another still-available mode.

- Read-only reproduction:

```text
git show 85a9fb6f0f:docs/architecture/multi-host-mobs-plan.md | nl -ba | sed -n '83p;503p'
```

  Original v4.1 A4 (line 83) and the workgraph disposition (line 503) both select disabled-with-typed-record for inherited-open workgraph.

  This was a genuine historical decision, not text that may be silently erased.

- Read-only reproduction:

```text
git show 09680e1276:docs/architecture/multi-host-mobs-plan.md | nl -ba | sed -n '83p;507p'
```

  The implementation commit retains the old A4 at line 83 but changes the detailed workgraph row at line 507 to NonPortableInheritedToolFilter and an explicitly empty non_portable_disabled record.

  The exact source chronology supports adding an implementation supersession note rather than retroactively changing the original adjudication.

- [meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:6469-6483](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/mob_machine.rs#L6469-L6483)

```text
emit SpawnMemberAdmissionResolved { admission: MobSpawnMemberAdmissionKind::NonPortableInheritedToolFilter }
```

  Canonical remote-spawn admission rejects the non-serializable inherited authority; it does not merely suppress workgraph visibility.

- [meerkat-mob/src/runtime/actor.rs:29889-29899](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L29889-L29899)

```text
non_portable_disabled: Vec::new(),
```

  The actual remote compilation path agrees with the detailed hard-rejection disposition.

**Required correction:** Append a bounded implementation-supersession annotation to A4: the original disabled-with-record alternative was replaced during multi-host implementation by hard rejection of remote spawns carrying inherited tool-visibility authority; explicit workgraph assertions are separately rejected, and the corresponding v1 disabled-resource record remains empty. Cross-reference final R6/O5. Preserve the original decision as history rather than silently rewriting it to appear to have always required rejection.

### Correction

Appended an explicit implementation-supersession annotation to A4 while preserving its entire original v4.1 disabled-with-record decision verbatim. The note records that multi-host implementation replaced suppression with NonPortableInheritedToolFilter hard rejection for remote inherited authority, separately rejects explicit workgraph assertions, and leaves the v1 disabled-resource record empty. It cross-references the final section 15 R6 and section 18 O5 dispositions without retroactively rewriting the original decision.

- [docs/architecture/multi-host-mobs-plan.md:87-87](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L87)

```text
Implementation supersession: the original v4.1 disabled-with-record alternative above was replaced during multi-host implementation.
```

- [docs/architecture/multi-host-mobs-plan.md:87-87](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L87)

```text
Inherited-OPEN workgraph resolves to disabled-with-typed-record; EXPLICIT assertion rejects.
```

- [meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:6469-6483](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/mob_machine.rs#L6469-L6483)

```text
emit SpawnMemberAdmissionResolved { admission: MobSpawnMemberAdmissionKind::NonPortableInheritedToolFilter }
```

- [meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:6486-6501](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/mob_machine.rs#L6486-L6501)

```text
emit SpawnMemberAdmissionResolved { admission: MobSpawnMemberAdmissionKind::NonPortableWorkgraphTools }
```

- [meerkat-mob/src/runtime/actor.rs:29889-29899](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L29889-L29899)

```text
non_portable_disabled: Vec::new(),
```

### Final independent review

**PASS:** A4 retains its complete original disabled-with-record sentence and appends a clearly labeled implementation supersession. The replacement outcome is accurately scoped: inherited tool-visibility authority prevents remote spawning, explicit workgraph assertions have a separate denial, and v1 non_portable_disabled is empty. The final R6/O5 cross-references resolve the top-level contradiction without rewriting the original decision. Historical commits independently confirm that the original alternative really existed and that the detailed contract changed during implementation.

- [docs/architecture/multi-host-mobs-plan.md:87-87](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L87)

```text
Inherited-OPEN workgraph resolves to disabled-with-typed-record; EXPLICIT assertion rejects.
```

  The original A4 text remains verbatim as the prefix of the corrected ledger entry.

- [docs/architecture/multi-host-mobs-plan.md:87-87](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L87)

```text
Implementation supersession: the original v4.1 disabled-with-record alternative above was replaced during multi-host implementation.
```

- [docs/architecture/multi-host-mobs-plan.md:513-513](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/docs/architecture/multi-host-mobs-plan.md?plain=1#L513)

```text
Consequently `non_portable_disabled` is an explicitly empty record in v1.
```

  The referenced final R6 disposition agrees with the annotation; O5 repeats it at line 981.

- [meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:6469-6483](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/mob_machine.rs#L6469-L6483)

```text
emit SpawnMemberAdmissionResolved { admission: MobSpawnMemberAdmissionKind::NonPortableInheritedToolFilter }
```

  Canonical spawn-exec admission rejects the non-portable inherited authority rather than disabling workgraph and building anyway.

- [meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:6486-6501](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/mob_machine.rs#L6486-L6501)

```text
emit SpawnMemberAdmissionResolved { admission: MobSpawnMemberAdmissionKind::NonPortableWorkgraphTools }
```

  The separate workgraph-required arm is guarded after the inherited-authority refusal.

- [meerkat-mob/src/runtime/actor.rs:29889-29899](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L29889-L29899)

```text
non_portable_disabled: Vec::new(),
```

  The current remote compilation path agrees with the implemented empty disabled-resource record.

- Read-only reproduction:

```text
git show 85a9fb6f0f:docs/architecture/multi-host-mobs-plan.md | nl -ba | sed -n '83p;503p'; git show 09680e1276:docs/architecture/multi-host-mobs-plan.md | nl -ba | sed -n '83p;507p'
```

  The original v4.1 commit has disabled-with-record in both A4 and R6; the implementation commit retains A4 but changes R6 to NonPortableInheritedToolFilter and an empty non_portable_disabled record.

  Read-only history inspection independently supports an implementation-supersession note instead of silently replacing the original adjudication.

## Lane limitations

- **Audit:** Static repository audit only. No builds, tests, installations, provider calls, commits, nested agents, or repository edits were performed; the only written file is this requested audit artifact.
- **Audit:** All assigned documents were read in full and their SHA-256 hashes matched manifest-A13.json at 56208b9e6cee078f52c43af8f6b36660bc93eeb4. Source checks were targeted to material current/status claims and candidate contradictions, not an execution proof of every historical phase acceptance test.
- **Audit:** Local release tags establish tagged source contents, not present registry availability or the current status of external MobKit, HomeCore, OB3, and release-lane qualification. Status recommendations are deliberately limited to the Meerkat implementation evidence.
- **Audit:** The historical multi-host plan explicitly delegates current public behavior to current code and public guides. Its candidates concern conflicting statements retained inside its adjudicated record; historical line anchors, old non-goals, and subsequent protocol additions were not generally reported as stale-code defects.
- **Audit:** The dated deferred-ledger observations about parallel test failures, live provider configuration, and transient teardown flakes were not reproduced. Their dated historical framing was preserved.
- **Audit:** Live local Markdown destinations and pinned GitHub source targets were checked read-only; no external URL availability or remote GitHub PR metadata was fetched. Independent challenge, fixes, and final review remain the parent's separate stages.
- **Challenge:** Independent static challenge at repository HEAD 56208b9e6cee078f52c43af8f6b36660bc93eeb4. All 11 manifest document hashes matched on entry; review focused on every one of the 13 candidates and its relevant context/source rather than repeating the auditor's no-finding coverage.
- **Challenge:** No builds, tests, installations, provider calls, nested agents, commits, or repository-source edits were performed. The requested challenge artifact is the only authored output.
- **Challenge:** Local Git objects establish exact source contents and revision chronology, not registry availability, external MobKit/HomeCore/OB3 qualification, or remote CI status.
- **Challenge:** Confirmed historical-plan items are bounded missing qualifications or unresolved internal carrier/ownership contradictions. Repairs must retain dated decisions, rejected alternatives, old baseline anchors, and the document's explicit delegation of current public behavior to code and public guides.
- **Challenge:** Rejected A13-009, A13-010, and A13-012 were assessed in their full protocol/adjudication context. Ambiguous shorthand or retained wording already governed by an explicit historical correction was not promoted into a new live implementation or contract defect.
