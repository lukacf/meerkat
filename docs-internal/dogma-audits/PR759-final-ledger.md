# PR #759 — Canonical Dogma Ledger (one row, one verdict)

**PR:** `lukacf/meerkat#759` · **Head:** `2568493f831e3fb3561654f73f632cceae3e65b0` · **Baseline:** `/Users/luka/.codex/dogma-violations.md` (2026-06-09, 307 rows)

**This document supersedes the counts in `PR759-reverification-report.md`** and satisfies the reviewer's 7-point repair spec: every baseline row appears exactly once with a stable ID (R001–R307, codex order); one final verdict each; conflicts resolved by explicit tiebreak adjudication; all recalibrations folded in (not appended); headline recomputed from unique rows.

## Method (verification layers, in precedence order)

1. 531-agent default-active verification (adversarial refute on downgrades; 89 refuted back).
2. 183-row defender screen (design-intent hunt).
3. 24-row repair (13 never-verified + 11 duplicate-name tiebreaks; root cause: verifier index-misreads — later passes carry `row_index` making misattribution mechanically detectable).
4. **Corrected-criterion re-audits** (deliberate ≠ valid; functional justification required, drawn from the named retention categories: (1) durable-format stability, (2) wire-opaqueness, (3) external protocol we don't own, (4) divergence-free projection, (5) **fail-closed rejection machinery** — code whose job is to REJECT legacy persisted/wire shapes with a typed error is the policy-mandated disposal of legacy data, not retention debt, and is retained by design together with its negative tests; pre-1.0 self-compat and pinning tests NEVER justify): 28 prior by-designs → 13 overturned (46%); 62 screen by-designs → 42 overturned (68%); 59 screen already-fixed claims QA'd → 34 refuted (~58%, all sibling-residual shape); 5 misattribution holes closed + 2 conflict tiebreaks.
5. Lead/user adjudications (archive mirror; provider_params split verdict).
6. Independent post-fix validation of every keystone landed this PR (no self-certification).

## Headline (recomputed from unique final rows)

| Final verdict | Count | Meaning |
| --- | ---: | --- |
| `FIXED_THIS_PR` | 6 | fixed by this PR's keystones, independently validated at HEAD |
| `STALE_FIXED` | 123 | already fixed at audit HEAD (ledger row was stale) |
| `OVERFLAGGED_BY_DESIGN` | 35 | sanctioned, with a named FUNCTIONAL justification |
| `CONFIRMED_ACTIVE` | 28 | genuine, open |
| `NARROWED_PARTIAL` | 115 | genuinely narrowed but a named residual remains open |

**Genuine open backlog: 143 rows** (28 confirmed + 115 narrowed). Number trajectory across layers: 307 (codex) → 183 (default-active) → 61 (screen) → 147 (corrected criteria) → **143 open after this PR's fixes**. Every layer erred in a different direction; convergence came from opposing biases plus the corrected criterion.

## This PR's remediation (all independently verified, gates teeth-proven by planted reversions)

**Keystone fixes:** flow condition absent-root regression (679058616); terminal-fault launders REST SSE ×2 + MCP error/content (42fdc134d, 7 site-gates); inproc single-resolution namespace delivery (df2b353d9); canonical default-model ladder on REST+WASM (77ca23c17) and CLI (2568493f8); mcp-server event-serialization launder (2568493f8).

**Gates (rerun-and-diff/parity, lock the fixes):** typed-carrier-adoption AST ratchet (8852179e4); RPC signature parity beyond name-sets (df2b353d9); machine-authority CI lane verified already-running (codegen-diff + TLC per PR); machine-poster content-drift gate (b3ae3302b — immediately caught 2 deleted phases + 81 stale triggers the old gate missed).

**Legacy deletion (the add-owner-keep-legacy reversal):** wave 1 −238 (archived-bool fold, legacy_peer_label, split-wire Deserialize, dead from_legacy_value — 2a090e566); legacy-run command lane −913 via machine-schema cutover (6a2175998); wave 2 −1,834 (session_migrations.rs deleted, fail-closed v2-only persisted versions enforced at codegen level, clear_* split-wire cutover incl. SDKs — 10b9d46ac); BlockAssistant transcript unification −622 (b43750214); durability.rs admission mirror folded into machine-emitted typed reasons −103 hand-written (2568493f8).

## Net-LOC reckoning (honest)

- **This session: code net −915** (+4,472/−5,387 across rs/py/ts) while adding 2 new gates and ~30 regression tests. Total session delta +883 only due to regenerated TLA specs and audit documentation.
- **PR overall remains +36,764** (ex-bookkeeping/generated) vs merge-base — accumulated by the earlier waves' add-owner-beside-shadow pattern. The legacy-retention sweep identified and this session deleted ~3,900 of it; the remaining reversal is explicit follow-up work (below), not silently absorbed.

## Follow-up (named, not deferred-by-vagueness)

1. SessionDocumentMachine fold (LUC-524): ends the archive lifecycle mode-split (R004 residual) — the one remaining hard fold.
2. ~~Remaining FOLD_THEN_DELETE sweep items (~1.1k lines) + KEEP_FUNCTIONAL doc corrections (MemberRef carrier wording; fail-closed-rejection as an explicitly named retention category).~~ **DONE (w5-ftd):** all 26 FOLD_THEN_DELETE findings closed (folded+deleted or verified already-landed) plus the DELETE_NOW stragglers the waves missed (TrustStore cutover, comms-agent runtime deletion, MemberState mirror, MeerkatId rename, FlowSpec.root, input content-carrier unification, mandatory live tool_timeout, McpAuthTarget fold, legacy-bearer fold, SystemPromptOverride tri-state wire/persist retype, live-result boolean mirrors incl. the DSL-side `refresh_enqueued`/`closed`/`accepted` projections, and more); doc corrections landed (MemberRef carrier wording rewritten; fail-closed rejection machinery named as a retention category in the criterion list above; content_blocks_serde tripwire test added). Excluded by design: the archive-lifecycle/SessionDocumentMachine fold (item 1, sibling-owned).
3. Respawn-after-retire wiring defect (pre-existing, A/B-proven; task filed).
4. Scanner-conversion gates for already-folded clusters (machines.rs token scanners; ownership-ledger boundary resolution).

## The ledger (R001–R307)

| ID | Row | Verdict | Sev (codex) | Resolved by | Evidence (strongest layer) |
| --- | --- | --- | --- | --- | --- |
| R001 | Initial-turn runtime semantics still have split carriers | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: make the first-turn semantics bundle the single owner — remove render_metadata and skill_references from CreateSessionRequest top level (they live only in the initial-turn RuntimeTur |
| R002 | Persistent destroy can leave driver-side control shadow truth behind | `NARROWED_PARTIAL` | Medium |  | The main destroy protocol is genuinely fail-closed: EphemeralDriverRollbackSnapshot includes the DSL owner ('dsl_snapshot: mm_dsl::MeerkatMachineAuthoritySnapshot', meerkat-runtime/src/driver/ephemeral.rs:126) and restor |
| R003 | Control-plane lifecycle preflight/idempotence shadows DSL guards | `NARROWED_PARTIAL` | Medium |  | FIXED PRONG — Recycle is DSL-first at HEAD: meerkat-runtime/src/meerkat_machine/dispatch_control.rs:438-444 `self.apply_session_dsl_input(&session_id, ...MeerkatMachineInput::Recycle, "Recycle").await.map_err(...)` with  |
| R004 | Machine-authorized archive mirrors retired lifecycle into SessionStore compatibility metadata | `NARROWED_PARTIAL` | Medium | 2a090e566 (legacy fold deleted; SessionDocumentMachine fold = follow-up) | Cited lines drifted: the flagged write is now meerkat-session/src/persistent.rs:5253 (`session.set_lifecycle_terminal(SessionLifecycleTerminal::Archived)`) and :5261 (`self.save_compatibility_projection_only(session)`),  |
| R005 | Budget terminal truth still originates in shell-local counters | `OVERFLAGGED_BY_DESIGN` | Medium |  | VERDICT IS MACHINE-OWNED END-TO-END. (a) Loop-entry gate routes through machine authority: meerkat-core/src/agent/state.rs:1415-1432 "if let Some(exceeded) = self.budget.observe().exceeded() { ... self.apply_turn_input(T |
| R006 | LLM retry lifecycle remains shell-local and partly string-projected | `NARROWED_PARTIAL` | Medium |  | KEPT sub-claims: (1) Retry verdict is machine-owned, not shell-local. meerkat-core/src/agent/state.rs:577-581/640-644 route every failure through `self.classify_llm_failure_recovery(...)`, which (state.rs:783-788) drives |
| R007 | Provider params and tool defaults remain JSON semantic bags | `NARROWED_PARTIAL` | Medium |  | The headline violation is live at HEAD; one locus clause is stale. STILL TRUE: (1) meerkat-core/src/config.rs:779 `pub provider_params: Option<serde_json::Value>` (persisted, doc: "a generic JSON bag") and config.rs:790- |
| R008 | OAuth flow state registry owns login lifecycle outside AuthMachine | `OVERFLAGGED_BY_DESIGN` | Medium |  | Production path is machine-first on every operation the claim lists, with fail-closed typed faults on every divergence direction. (1) Payload admission: meerkat-runtime/src/handles/oauth_flow.rs:1440 'self.admit_browser( |
| R009 | Connection/auth wire contracts keep provider semantics stringly | `OVERFLAGGED_BY_DESIGN` | Medium |  | Cited line 84 (LoginStartParams.provider: String) is parse-at-ingress fail-closed: meerkat-rpc/src/handlers/auth.rs:1084-1096 routes through resolve_oauth_provider, meerkat-auth-core/src/oauth_flow.rs:262-266 "OAuthProvi |
| R010 | Token clear is not atomic with AuthMachine release | `CONFIRMED_ACTIVE` | Medium |  | The claim is verbatim true at HEAD. meerkat-core/src/auth/lifecycle.rs:262-299 `clear_tokens_and_publish_lifecycle_released` is a two-step shell saga, not an atomic boundary: :269 `let previous_lifecycle = handle.capture |
| R011 | Canonical source-identity registry is not authoritative at all skill load paths | `STALE_FIXED` | Medium |  | The auditor's narrowed claim is "Composite loading can still bypass registry resolution when no registry is attached." Current code does the OPPOSITE — it fails |
| R012 | LLM-driven hidden tool denial still downgrades typed truth | `STALE_FIXED` | Medium |  | Cited lines drifted (state.rs:2128/2133 now hold post-LLM hook-report handling). The live precheck path is typed: meerkat-core/src/agent/state.rs:2291-2306 — `if let Err(error) = precheck_visible_tool_call(...) { // Pres |
| R013 | Mob seam uses hand-authored generated-shape mirrors | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: close the completeness window, not the (sanctioned, gated) hand projection. (a) Replace the `_ => return None` wildcard in MobSeamEffect::routed (and lift_routed_signal) with an expl |
| R014 | Mob command admission shadows MobMachine guards | `STALE_FIXED` | Medium |  | Fix verified at HEAD (2a090e566); fix commit 49eb39330 "dogma(WAVE G2): 9 mob machine folds (#14,...)" is a verified ancestor. (1) Cited site: meerkat-mob/src/runtime/pending_spawn_lineage.rs:12-15 still defines the side |
| R015 | RPC catalog still leaves config/capability/skills contracts schema-light | `NARROWED_PARTIAL` | Medium |  | Minimal root fix in three steps: (1) catalog advertisement — flip `basic("capabilities/get",...)` to `result_only(..., "CapabilitiesResponse")` and `basic("skills/list",...)` to `result_only(..., "Ski |
| R016 | Web SDK lacks generated non-event wire parity | `CONFIRMED_ACTIVE` | Medium |  | Handwritten runtime/session/bootstrap shapes persist outside generated wire parity, with live drift in both directions. (1) sdks/web/src/types.ts:13-39 (RuntimeConfig, InitResult), :57-76 (SessionConfig), :129-140 (Sessi |
| R017 | Destroy-obligation seam audit uses variant-name folklore | `CONFIRMED_ACTIVE` | Low |  | xtask/src/seam_inventory.rs:486 `if !(variant.contains("Destroy") && variant.starts_with("Request")) { continue; }` and :507 `(variant.contains("IngressDetached") \|\| variant.contains("Detach"))` — the C-F3 destroy-obli |
| R018 | Mobpack/WASM capability manifest shape remains stringly | `NARROWED_PARTIAL` | Low |  | Cited sites fixed: meerkat-mob-pack/src/manifest.rs:16-20 stores typed `MobpackCapability { token, id: MobpackCapabilityId }` classified at deserialization, fail-closed on empty tokens (manifest.rs:59-67), with capabilit |
| R019 | Machine drift ratchets use source-string governance scanners | `NARROWED_PARTIAL` | Medium |  | Cited lines drifted: xtask/src/machines.rs:651 is now `&driver_code` inside collect_drift_mismatches' composition-driver compare, and :1117 is the peer-response-terminal `find_fn_item(&parsed, "peer_response_terminal_con |
| R020 | Machine/composition coverage anchors still depend on token matching | `CONFIRMED_ACTIVE` | Low |  | Minimal root fix: make the anchor→semantic-element relation declared and typed rather than lexically derived — extend CoverageAnchor (or the manifest constructors) with an explicit typed list of the t |
| R021 | Ownership ledger false-closes stale owner anchors | `NARROWED_PARTIAL` | Low |  | PARTIALLY FIXED: the claim lists `owner_shell` among unverified prose facts, but cited line xtask/src/ownership_ledger.rs:493 is now part of a live liveness check — `owner_shells_by_path` (:486-514) is built from AST-pro |
| R022 | Runtime admission policy is still inferred in shell paths | `STALE_FIXED` | Medium |  | Cited shape is gone at HEAD (2a090e566). (1) meerkat-runtime/src/policy_table.rs:53-68 is a generated-admission projection facade: it threads typed facts (input.handling_mode().map(mm_dsl::InputLane::from), kind, continu |
| R023 | Structured-output extraction keeps validation/result/retry prompt truth in agent shell | `NARROWED_PARTIAL` | Medium |  | All facts in the claim are present at current code, but most are the sanctioned shell-classify/machine-decide seam; one genuine ownership split survives.  MACHINE-OWNED (by design, disproving the broad claim): The genera |
| R024 | Provider/connection turn overrides still split at public/recovery surfaces | `FIXED_THIS_PR` | Medium | 10b9d46ac | FIXED HALF: the cited structs no longer carry split fields. meerkat-core/src/session_recovery.rs:29-30 — `pub provider_params: Option<TurnMetadataOverride<serde_json::Value>>, pub auth_binding: Option<TurnMetadataOverrid |
| R025 | Fatal failure reason downgraded before machine | `OVERFLAGGED_BY_DESIGN` | Medium |  | Cited lines drifted; FatalFailure handling is now meerkat-runtime/src/handles/turn_state.rs:243-251: `mm_dsl::MeerkatMachineInput::FatalFailure { run_id: ..., terminal_failure_source: mm_dsl::RunFailureSourceKind::from(f |
| R026 | Token acquisition is not atomic with AuthMachine acquire | `OVERFLAGGED_BY_DESIGN` | Medium |  | Claimed ordering is real and identical across all three surfaces: meerkat-rest/src/auth_endpoints.rs:274 `token_store.save(&key, tokens).await` precedes :280 `meerkat_core::publish_token_lifecycle_acquired(auth_lease, au |
| R027 | Model capability value domains are string-owned | `NARROWED_PARTIAL` | Low |  | Minimal root fix: introduce a typed `BetaFeature` enum (Compaction, StructuredOutput, InterleavedThinking) on `BetaHeader.feature`, change `catalog_beta_value` to take `BetaFeature`, and project to th |
| R028 | Tool-error transcript payload remains JSON-text mirror | `OVERFLAGGED_BY_DESIGN` | Medium |  | Cited lines drifted: ops.rs:256-260 is now `is_runtime_tool_timeout()`. The real site is `terminal_tool_outcome_for_error` (meerkat-core/src/ops.rs:275-291): `let terminal_cause = ToolDispatchTerminalCause::RuntimeToolEr |
| R029 | Composition consumer refusals collapse typed machine errors to strings | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: type the kernel→surface leg. Change dsl_authority::map_error to return a typed refusal (or construct ConsumerError directly with per-variant stable codes, e.g. dsl_no_matching_transi |
| R030 | Session cancel-after-boundary has a shell-local boolean authority | `STALE_FIXED` | Medium |  | The flagged AtomicBool no longer exists. Service path (meerkat-session/src/ephemeral.rs:2871-2895) now machine-authorizes then sends a typed command: `slot.authorize_cancel_after_boundary().map_err(...)?; let _ = cancel_ |
| R031 | Hook execution policy is engine-local | `STALE_FIXED` | Medium |  | meerkat-hooks/src/lib.rs:78-110 defines `HookExecutionPolicy` as the SINGLE typed owner of ordering/timeout/queue-pressure: `from_config` (l.88-94) resolves it |
| R032 | RPC/MCP/WASM stream IDs leak helper-local registries | `NARROWED_PARTIAL` | Medium |  | RPC portion FIXED (commit a1194d011 dogma wave1): meerkat-rpc/src/router.rs:324 `pub(crate) struct StreamRef(Uuid)` with design doc-comment at router.rs:314-323 ("Typed owner of an RPC event-stream identity... parse-at-i |
| R033 | WASM content-bearing APIs tunnel typed content through JSON/text strings | `NARROWED_PARTIAL` | Medium |  | The string-tunnel mechanism is still live at both cited APIs, but the claim's fail-open half was repaired in a prior dogma wave. STILL ACTIVE: sdks/web/src/session.ts:38-44 `serializePromptContentInput(prompt: string \|  |
| R034 | WASM bootstrap owns provider/model default policy at the surface | `FIXED_THIS_PR` | Medium | 77ca23c17 | meerkat-web-runtime/src/lib.rs:268-279 `build_bootstrap_config`: `config.agent.model = explicit_model.unwrap_or_default(); populate_realm_from_api_keys(&mut config, api_keys, base_urls); let model = meerkat::resolve_crea |
| R035 | Runtime binding helpers collapse control errors | `NARROWED_PARTIAL` | Medium |  | Cited site IS fixed: meerkat-runtime/src/meerkat_machine/session_management.rs:696-717 `set_session_silent_intents` returns Result<(), RuntimeDriverError>, propagates via `.map_err(MeerkatMachine::driver_error_from_comma |
| R036 | OAuth provider identity keeps an alias-string taxonomy | `NARROWED_PARTIAL` | Medium |  | FIXED HALVES (defender evidence verified): (1) Ingress parse-at-ingress is real: the only alias matcher is the typed owner's parse method — meerkat-core/src/oauth_identity.rs:42-50 `pub fn from_alias(alias: &str) -> Opti |
| R037 | Recovery runtime build mode uses optionality plus guard boolean | `CONFIRMED_ACTIVE` | Low |  | The flagged shape is fully present at HEAD and the by-design defense fails on its own factual premises. (1) The pair: meerkat-core/src/session_recovery.rs:184-185 `pub runtime_build_mode: Option<RuntimeBuildMode>, pub re |
| R038 | Provider clients re-check model capabilities with string helpers | `OVERFLAGGED_BY_DESIGN` | Medium |  | The string-helper half of the claim is dead: meerkat-core/src/model_profile/openai.rs:15-17 'pub fn supports_temperature(model: &str) -> bool { capabilities_for(Provider::OpenAI, model).is_some_and(\|caps\| caps.supports |
| R039 | Command credential source caches subprocess tokens outside AuthMachine | `STALE_FIXED` | Medium |  | Fix verified at HEAD (commit b56ba1bab, ancestor of HEAD 2a090e566). (1) meerkat-auth-core/src/resolver.rs:94-95 — the only production Command arm: `let runner = CommandCredentialRunner::new(spec); let tokens = resolve_c |
| R040 | Dynamic authorizer lease projects freshness but cannot refresh through lease | `OVERFLAGGED_BY_DESIGN` | Medium |  | (1) In-code design rationale at meerkat-llm-core/src/provider_runtime/binding.rs:351-359 explicitly forbids in-place refresh and mandates the typed fault: "A dynamic lease has no in-place refresh: the caller must re-reso |
| R041 | Memory search public contract loses owner/source handle | `NARROWED_PARTIAL` | Medium |  | Cited site IS fixed: meerkat-memory/src/tool.rs:144-152 emits the typed source handle (`obj.insert("source_range", json!({ "start": range.start(), "end": range.end() }))`), tests at tool.rs:304-317 assert `parsed[0].get( |
| R042 | OpenAI live adapter stages explicit-commit text turns in provider-local queues before commit | `NARROWED_PARTIAL` | Medium |  | CORE SHAPE SANCTIONED (functional, external protocol): meerkat-openai/src/live.rs:2710-2721 sends the text item to the provider at stage time ("send_raw(ClientEvent::ConversationItemCreate { ... id: Some(synthetic_item_i |
| R043 | OpenAI realtime response-nudge lifecycle is provider-local | `STALE_FIXED` | Medium |  | The central claim ("uses booleans/counters") no longer holds. meerkat-openai/src/live.rs:1221-1250 defines a typed `enum RealtimeResponseState { Idle, AwaitingP |
| R044 | Supervisor bridge trust installs before authorization terminality | `OVERFLAGGED_BY_DESIGN` | High |  | Cited lines have NOT drifted: meerkat-mob/src/runtime/actor.rs:2693 `self.supervisor_bridge.trust_recipient(peer).await?;` and meerkat-mob/src/runtime/provisioner.rs:2775 (identical) both execute before `send_bridge_comm |
| R045 | Mob event and flow status surfaces erase typed contracts into JSON values | `NARROWED_PARTIAL` | Medium |  | Cited site IS fixed: meerkat-contracts/src/wire/mob.rs:1742-1745 `pub run: Option<WireMobRun>` with typed WireMobRun (mob.rs:1726-1735: run_id/mob_id/flow_id/status: WireMobRunStatus + documented flatten kernel map), pro |
| R046 | Tool-call argument projection failure terminalizes through string error | `STALE_FIXED` | Low |  | Fixed at HEAD (2a090e566). (1) Cited site: meerkat-core/src/agent/state.rs:233-241 `fn tool_call_args_projection_error(tool_name: &str, error: ToolCallArgumentsError) -> AgentError { // Preserve the typed invalid_argumen |
| R047 | Peer response handling-mode inheritance is shell-map authority | `STALE_FIXED` | Medium |  | Production handling-mode resolution reads from the generated MeerkatMachine DSL, not a runtime HashMap. comms_runtime.rs:900-907 (PeerResponse, terminal-reply b |
| R048 | Realm profile contracts erase typed profile shape into JSON | `NARROWED_PARTIAL` | Low |  | FIXED portions confirmed: (a) handlers no longer use json!(stored) — zero hits workspace-wide; every surface (meerkat-rpc/src/handlers/mob.rs:1810,1826,1846-1849,1869; meerkat-mob-mcp/src/public_mcp.rs:862-902; meerkat-m |
| R049 | WASM API-key bootstrap fabricates realm binding policy from provider strings | `OVERFLAGGED_BY_DESIGN` | Medium |  | Lines drifted: the cited shapes are now meerkat-web-runtime/src/lib.rs:353-418 (populate_realm_from_api_keys) and lib.rs:367 (catalog provider_priority). The surface authors no policy fact: ordering is catalog-owned ("le |
| R050 | Direct session surfaces own model default policy | `FIXED_THIS_PR` | Medium | 77ca23c17 + 2568493f8 | Minimal root fix: in REST create_session_inner (and the help path that funnels into it), resolve the missing model via `meerkat::resolve_create_session_default_model(&snapshot.config)` from a live `st |
| R051 | Surface serialization failures become success fallbacks | `FIXED_THIS_PR` | Medium | 42fdc134d + 2568493f8 | All three cited residuals ARE fixed at HEAD (2a090e566): (1) meerkat-rpc/src/handlers/comms.rs:52-56 `pub(crate) fn send_receipt_json(...) -> Result<serde_json::Value, serde_json::Error>` with Result-matching callers (co |
| R052 | External dispatchers bypass typed tool-argument contract | `NARROWED_PARTIAL` | Medium |  | Apply the same ROW #63 shape to the MCP seam: replace the three `unwrap_or_else(Value::String)` parses with `ToolCallArguments::from_raw_json(call.args).map_err(\|e\| ToolError::invalid_arguments(call.n |
| R053 | Image generation builtin owns semantic alias parsing | `STALE_FIXED` | Low |  | The claimed shape ("accepts raw request JSON and parses intent/target/provider/model from it in the tool body") no longer exists. Current code: meerkat-tools/src/builtin/image_generation.rs:225 `request: GenerateImageToo |
| R054 | Per-turn skill activation failures are swallowed after consuming typed refs | `STALE_FIXED` | Medium |  | Fix verified at HEAD 2a090e566. meerkat-core/src/agent/runner.rs:1439-1460: Err arm of apply_skill_ref emits typed event — "// Fail-explicit: emit the typed resolution failure carrying the typed reason and the canonical  |
| R055 | Roster replay fabricates nil bridge session | `NARROWED_PARTIAL` | Medium |  | CITED FIX VERIFIED: commit 0fdbc45fd (dogma row #70) removed the exact fabrication `unwrap_or_else(\|\| MemberRef::from_bridge_session_id(SessionId::from_uuid(uuid::Uuid::nil())))` (confirmed in commit diff). Current mee |
| R056 | Self-hosted legacy connection synthesizes non-canonical realm binding fallback | `STALE_FIXED` | Medium |  | In meerkat/src/factory.rs the self-hosted connection resolution no longer synthesizes a transient realm. `resolve_self_hosted_connection` (factory.rs:2994-3045) |
| R057 | Factory/provider-runtime errors stringify typed auth/provider failures | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: stop stringifying at the ConnectionResolution seams — route provider_registry.resolve / build_client / build_image_generation_executor errors through the now-typed FactoryError (Prov |
| R058 | Mobpack manifest model/profile/surface selectors remain string authorities | `STALE_FIXED` | Medium |  | Fix verified at HEAD (2a090e566). meerkat-mob-pack/src/manifest.rs:74-79: `pub models: BTreeMap<ModelAlias, ModelRef>`, `pub surfaces: BTreeSet<SurfaceSelector>`; manifest.rs:114-120 ProfileSection: `model: Option<ModelA |
| R059 | Provider-executed server tool content is opaque JSON/string authority | `NARROWED_PARTIAL` | Medium |  | PRONG A (kind remains `name: String`) — STALE_FIXED, prior screen correct: meerkat-core/src/types.rs:594-604 defines typed `ServerToolKind { WebSearch, GoogleSearch, ProviderNative { name } }`; both seams carry it (meerk |
| R060 | Tool dispatchers collapse structured success into text | `NARROWED_PARTIAL` | Medium |  | FIXED HALF: (1) meerkat-tools/src/builtin/composite.rs:43-56 — `fn json_output_content(name: &str, value: &Value) -> Result<String, ToolError>` now fails closed: "Serialization failure is propagated as a typed [`ToolErro |
| R061 | Tool timeout ownership is split across core, dispatcher, and shell | `STALE_FIXED` | Medium |  | Single typed owner adopted and all old independent literals deleted. (1) Owner: meerkat-tools/src/timeout.rs:27-30 `pub struct ToolTimeoutPolicy { default_timeout: Duration }` with module doc (lines 1-16) naming the thre |
| R062 | REST schedule tool call is schema-light and handler-private | `CONFIRMED_ACTIVE` | Low |  | Minimal root fix: delete the private ScheduleToolCallRequest and deserialize the canonical contract type in schedule_call — `Json(req): Json<meerkat_contracts::ScheduleToolCallParams>` (re-exported at |
| R063 | OpenAI live provider tool-call arguments fall through to string JSON payloads | `STALE_FIXED` | Medium |  | Cited lines drifted (live.rs:1588 is now session-struct field init; live.rs:2100 is a lifecycle trace in InputAudioBufferCommitted). The alleged laundering no longer exists. (1) meerkat-openai/src/live.rs:1066-1085 `pars |
| R064 | Private peer-directory visibility is a router-local side channel | `STALE_FIXED` | Medium |  | The flat `private_peer_ids` side-map field no longer exists. The Router field is now `private_peer_sources: Arc<RwLock<TrustSourceMap>>` (meerkat-comms/src/rout |
| R065 | Per-turn skill activation success is folded into prompt text | `CONFIRMED_ACTIVE` | Medium |  | meerkat-core/src/agent/runner.rs:1464-1472 — apply_skill_ref returns the operational effect as a string fold: `format!("{}\n\n{user_input}", prefix_parts.join("\n\n"))`; the typed witness is fire-and-forget: runner.rs:14 |
| R066 | Transcript fork/edit admission bypasses machine authority | `STALE_FIXED` | Medium |  | Both cited sites now commit through generated machine authority. (1) meerkat-session/src/persistent.rs:2629 `fn authorize_transcript_edit(&self, id, directive: TranscriptEditKind)` drives the generated machine: `let mut  |
| R067 | Transcript fork replacement contract is schema-light across RPC and SDKs | `STALE_FIXED` | Medium |  | Both cited locations no longer carry a bare serde_json::Value / true for the fork-replace `replacement` field.  (1) meerkat-contracts/src/wire/session.rs:92-95 |
| R068 | Session fork SDK result parsers fabricate handles | `STALE_FIXED` | Medium |  | The genuine session-fork result parsers in both SDKs now fail closed on every required field (no empty-string/zero fabrication):  TypeScript `parseSessionForkRe |
| R069 | Resolved model capabilities are surface-rederived and SDK-defaulted | `NARROWED_PARTIAL` | Medium |  | PRONG 1 (web SDK) FIXED: sdks/web/src/mob.ts:210-214 `requireBooleanField` throws `if (typeof raw !== 'boolean')`; all 7 capability flags route through it (mob.ts:244-277) with explicit comment 'fail closed on absent/mal |
| R070 | Current-turn comms image refs are dispatch-local index handles | `OVERFLAGGED_BY_DESIGN` | Medium |  | The prior screen's doc-comment citation (meerkat-core/src/agent.rs:205-210) is circular and rejected; the drop survives on construction discipline instead. (1) Single-owner derivation, no divergence window: meerkat-core/ |
| R071 | Provider replay projection owns transcript semantics | `OVERFLAGGED_BY_DESIGN` | Medium |  | meerkat-llm-core/src/types.rs:45-52 — trait default FAILS CLOSED: `fn project_replay_messages(&self, _messages: &[Message]) -> Result<Vec<Message>, LlmError> { Err(LlmError::InvalidRequest { message: format!("{} client d |
| R072 | ToolGateway dynamic routing is name-only shadow authority | `NARROWED_PARTIAL` | Medium |  | Core claim FIXED by commit a5c50df84 ("dogma(WAVE C): ... #93 ToolGateway typed routing authority", ancestor of HEAD). The cited lines matched the OLD code exactly: old build() discarded ownership after collision check ( |
| R073 | Mobpack archive section/executable semantics use path folklore | `NARROWED_PARTIAL` | Medium |  | CITED SITES FIXED: meerkat-mob-pack/src/archive.rs:68 routes section classification through the typed owner ('match ArchiveSection::classify(path)'); exec_bits.rs:3-5 is a thin delegate ('ExecPolicy::classify(path, bytes |
| R074 | Peer lifecycle subject ignores typed peer_spec | `NARROWED_PARTIAL` | Medium |  | CORE DEFEATED (functional, not circular): (1) Subject selection incl. sender-name fallback is MACHINE-owned: meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:11060-11063 "lifecycle_peer: Some(if lifecycle_peer_p |
| R075 | Staged session registry owns pending session lifecycle | `OVERFLAGGED_BY_DESIGN` | Medium |  | Revert f3c7e5b42 (ancestor of HEAD): "#97 session_staged_present + staged-exists query (session_document.rs) [REVERTED] — StagedSessionRegistry.slots HashMap is the single owner of staged-session payloads; the machine bo |
| R076 | RPC session reads collapse store errors to partial success | `NARROWED_PARTIAL` | Medium |  | Cited lines drifted; list and read paths are FIXED with design-intent doc-comments: meerkat-rpc/src/session_runtime.rs:6938-6943 (`list_sessions`: "A store-layer failure is surfaced as a typed `RpcError` rather than coll |
| R077 | Executor attachment reports success after hidden failures | `STALE_FIXED` | Medium |  | meerkat-runtime/src/meerkat_machine/mod.rs:2620 `pub async fn register_session(&self, session_id: SessionId) -> Result<(), RuntimeControlPlaneError>` with body |
| R078 | Token persisted-auth mode is inferred from auth_method strings | `NARROWED_PARTIAL` | Medium |  | Cited sites ARE fixed: meerkat-auth-core/src/auth_store/mod.rs:47 `persisted_auth_mode_for_auth_method` is now a parse-at-boundary shim delegating to typed per-provider `*AuthMethod::persisted_auth_mode` (line 41 is its  |
| R079 | Core comms command path downgrades typed request contracts | `NARROWED_PARTIAL` | Medium |  | FIXED half: intent is typed across the cited public-wire->core-request seam. meerkat-contracts/src/wire/comms.rs:213 `intent: CommsPeerRequestIntent` (closed core-owned enum, serde fail-closed per meerkat-core/src/comms. |
| R080 | Core turn effects are shell-derived phase-delta truth | `STALE_FIXED` | High |  | At HEAD bca3c6ba2 (the exact cited commit), the disputed "terminal handle rejection warning-only continuation" no longer exists. meerkat-core/src/agent/state.rs |
| R081 | Run terminal output collapses assistant blocks to text | `OVERFLAGGED_BY_DESIGN` | Medium |  | RunResult.text is a String documented as a projection: meerkat-core/src/types.rs:2793-2795 `pub struct RunResult { /// Final committed text response from the main agentic turn. pub text: String,` — alongside typed `struc |
| R082 | JSONL session index can lag snapshot truth | `NARROWED_PARTIAL` | Medium |  | save_impl commits three durable artifacts in separate steps (meerkat-store/src/jsonl.rs): session file rename at :325 `fs::rename(&temp_path, &path).await?;`, sidecar rename at :335 `fs::rename(&meta_temp_path, &meta_pat |
| R083 | Completion waiter terminal details remain string-only | `STALE_FIXED` | Low |  | see field above |
| R084 | Peer ingress machine signal discards canonical peer id | `OVERFLAGGED_BY_DESIGN` | Medium |  | Claim is factually true but functionally benign. (1) Signal carries only the string: meerkat-runtime/src/handles/peer_comms.rs:242-244 `mm_dsl::MeerkatMachineSignal::ClassifyExternalEnvelope { item_id: facts.item_id.clon |
| R085 | Build-time initial metadata side channel can own session semantics | `STALE_FIXED` | Medium |  | The cited claim ("initial tool filters are still staged through reserved metadata") is contradicted by current code at every layer. (1) AgentBuildConfig owns tw |
| R086 | Explicit fallback web-search enable can silently produce no tool | `STALE_FIXED` | Medium |  | In meerkat/src/factory.rs, build_web_search_executor (line 1864) now takes `explicit: bool` and routes every cannot-provision branch through the `unavailable` c |
| R087 | WorkGraph tool/store authority is composed by factory fallback state | `NARROWED_PARTIAL` | Low |  | Cited factory site IS fixed: meerkat/src/factory.rs:4342-4353 fails closed when no dispatcher and no realm identity ("return Err(BuildAgentError::Config(\"WorkGraph tools enabled with no supplied dispatcher and no realm  |
| R088 | Runtime observability events stringify lifecycle terminal reasons | `STALE_FIXED` | Low |  | see above |
| R089 | OAuth refresh permanence is parsed from error text | `OVERFLAGGED_BY_DESIGN` | Medium |  | Permanence is structurally owned end-to-end; no production code parses error text to decide it. (1) meerkat-auth-core/src/auth_oauth/mod.rs:221-242 `OAuthError::refresh_permanence()` matches typed variants + status only: |
| R090 | MCP lifecycle mirror failures are swallowed after shell state changes | `CONFIRMED_ACTIVE` | Medium |  | The swallow is live at three sites. meerkat-mcp/src/router.rs:1030-1037: `if let Some(handle) = guard.as_deref() && let Err(error) = f(handle) { tracing::debug!(error = %error, "McpServerLifecycleHandle DSL apply rejecte |
| R091 | Live tool-dispatch failures collapse to provider error strings | `OVERFLAGGED_BY_DESIGN` | Medium |  | Typed owners exist and are the routing truth for every programmatic consumer; the String is a one-shot egress rendering. (1) Timeout: meerkat-live/src/host.rs:2407-2412 submits typed `LiveToolDispatchTimeout::new(timeout |
| R092 | Kickoff lifecycle member identity is string-owned | `STALE_FIXED` | Medium |  | All three assertions in the row desc are false at current code. (1) Machine state is typed, not Set<String>/Map<String,String>: meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:88-94 now reads `member_kickoff_pendin |
| R093 | Helper surfaces allocate mob member identity at the surface | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: give member-identity allocation a single typed owner. Either (a) extend the #115 fail-closed pattern (RPC's shape, with its comment and tests as the template) to REST, web-runtime, C |
| R094 | WorkGraph lifecycle authority is rebuilt from item projections | `OVERFLAGGED_BY_DESIGN` | High |  | The alleged failure mode (lifecycle authority rebuilt from item projections) is structurally refused at ingress on FUNCTIONAL grounds, not just doc-comment grounds. (1) meerkat-workgraph/src/types.rs:699-704 — WorkItem's |
| R095 | WorkGraph eligibility and topology are shell-computed booleans | `STALE_FIXED` | Medium |  | machine.rs:280 `pub fn validate_item_projection(item: &WorkItem) -> Result<(), WorkGraphError> { validate_item_machine_projection(item) }` — its doc says it "ne |
| R096 | WorkGraph mutating tool contracts bypass generated governance | `OVERFLAGGED_BY_DESIGN` | Medium |  | The narrowed residual is the hand-authored operation catalog `WorkGraphToolContract::ALL` (meerkat-workgraph/src/tools.rs:88-102). Re-adjudicated on the merits, NOT on the prior test-pin argument: (1) No generated tool-c |
| R097 | Legacy LoopState collapses terminal lifecycle truth | `OVERFLAGGED_BY_DESIGN` | Medium |  | The prior verdict leaned on the doc-comment (meerkat-core/src/state.rs:1-5 "this enum is the persisted/user-facing loop shape") — circular under the corrected criterion and discarded. Re-derived from current code, the ve |
| R098 | System-context appends are marked applied before turn terminality | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: reuse the persistent path's named conversion in ephemeral — replace the closure at ephemeral.rs:1517-1520 with the equivalent of control_error_into_session_error(err.into_control_err |
| R099 | LLM client overrides can mint provider identity outside the catalog | `CONFIRMED_ACTIVE` | Medium |  | The cited shape is live (lines drifted only). meerkat-core/src/agent.rs:75: `fn provider(&self) -> &'static str;` on AgentLlmClient. meerkat/src/factory.rs:2846: `return Ok((Provider::from_name(client.provider()), None)) |
| R100 | Provider runtime auth matrix is centralized in shared LLM core | `STALE_FIXED` | Medium |  | see above |
| R101 | OAuth provider policy is duplicated across auth core and provider runtimes | `NARROWED_PARTIAL` | Medium |  | Migrate meerkat-anthropic/src/runtime/oauth.rs to project claude_ai_endpoints()/console_endpoints() from oauth_provider_endpoints(OAuthProviderIdentity::AnthropicClaudeAi / AnthropicConsoleApiKey, red |
| R102 | Live WebRTC signaling failures are string-only | `OVERFLAGGED_BY_DESIGN` | Medium |  | meerkat-live/src/webrtc.rs:126-153 — LiveWebrtcError is a #[non_exhaustive] per-phase enum: `CodecRegistration { detail: String }`, `PeerCreation { detail }`, `SetRemoteDescription { detail }`, `CreateAnswer { detail }`, |
| R103 | Orchestrator lifecycle delivery is best-effort projection work | `STALE_FIXED` | Medium |  | meerkat-mob/src/runtime/actor.rs:4949 `async fn notify_orchestrator_lifecycle(&mut self, message: String) -> Result<(), MobError>` — typed fault contract replaces the old warn-and-drop shape. Each leg of the original cla |
| R104 | Flow terminalization erases failure cause into strings | `NARROWED_PARTIAL` | Medium |  | The cited fix is real but covers only one of the claim's three legs. Commit 4e1af6ee2 (dogma #126) retyped TerminalizationTarget::Failed { reason: String } -> { cause: FlowFailureCause } — confirmed at meerkat-mob/src/ru |
| R105 | Python WorkGraph helpers collapse malformed envelopes to empty success | `STALE_FIXED` | Medium |  | All three named WorkGraph list helpers in sdks/python/meerkat/client.py now fail closed via a shared typed helper. `list_workgraph_items` (workgraph/list, line |
| R106 | Empty assistant-output classification is shell-owned | `NARROWED_PARTIAL` | Low |  | MACHINE-OWNED VERDICT (real fix, verified): meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:19804-19820 'transition ClassifyAssistantOutputEmptyTerminal { ... guard "no_visible_or_actionable_output" { has_visib |
| R107 | External tool dispatch timeout is caller-shell terminal policy | `OVERFLAGGED_BY_DESIGN` | Medium |  | Cited runner.rs:528 drifted; the residue is meerkat-core/src/agent/runner.rs:568-633 `dispatch_external_tool_call_with_timeout_policy`. runner.rs:595-608: `let dispatch_result = match timeout_policy.timeout() { Some(time |
| R108 | Hook denial terminal policy is applied by the agent shell | `NARROWED_PARTIAL` | Medium |  | FIXED on primary path: meerkat-core/src/agent/runner.rs:1184-1197 in run_inner — doc-comment "Run-start hooks own the start veto. They must run — and be able to deny — BEFORE we publish `RunStarted`..." followed by `self |
| R109 | Persistent session event projection is best-effort after session behavior succeeds | `NARROWED_PARTIAL` | Medium |  | Cited site fixed: meerkat-session/src/persistent.rs:118-143 `async fn append_and_project_event(...) -> Result<u64, SessionError>` propagates durable append failure typed (`.map_err(\|err\| SessionError::Store(Box::new(er |
| R110 | Hook failure surfaces still expose string-only diagnostics | `NARROWED_PARTIAL` | Low |  | CITED SITES FIXED: meerkat-core/src/hooks.rs:101-129 defines typed owner `HookFailureReason` (serde tag "reason_code", variants Timeout{timeout_ms}/ExecutionFailed/ConfigInvalid/ObserveOnlyViolation). meerkat-core/src/ev |
| R111 | MCP skills provenance fabricates source identity records | `STALE_FIXED` | Medium |  | SEE current_evidence field above |
| R112 | Live adapter observation routing is host-local semantic authority | `OVERFLAGGED_BY_DESIGN` | Medium |  | `classify_observation` (host.rs:2869) is a total `match` over the TYPED enum `LiveAdapterObservation` (defined meerkat-core/src/live_adapter.rs:215) producing t |
| R113 | WireMemberRef opaque handle is a reversible public-ID tuple | `OVERFLAGGED_BY_DESIGN` | Medium |  | meerkat-contracts/src/wire/mob.rs:1368 `pub struct WireMemberRef(String);` with pure infallible encode (1375-1382: `let payload = serde_json::json!({ "m": mob_id, "a": agent_identity }); Self(base64_url_encode(...))`) an |
| R114 | Python SDK mob collection helpers collapse malformed envelopes to empty success | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: route wait_mob_kickoff and list_mob_members through the existing `_require_list_field` (one line each, mirroring wait_mob_ready at client.py:2099), validate list_mobs the same way, a |
| R115 | WorkGraph legacy deserialization seeds machine truth from projection fields | `STALE_FIXED` | High |  | At meerkat-workgraph/src/types.rs:698-704 (the `Deserialize for WorkItem` impl, exactly spanning the cited line 703), the code FAILS CLOSED instead of fabricati |
| R116 | WASM JS tool callback terminal/content truth is narrowed at boundary | `STALE_FIXED` | Medium |  | Rust callback parsing is NOT narrowed to a string. meerkat-web-runtime/src/lib.rs:1232-1238 defines `struct JsToolResult { content: meerkat_contracts::WireToolR |
| R117 | Peer ingress request semantics are intent-string classified | `NARROWED_PARTIAL` | Medium |  | SURVIVING PART (by-design): the handle classifier is genuine parse-at-ingress, not a residual. meerkat-runtime/src/handles/peer_comms.rs:171-179 `fn request_intent_class_for(intent: &str) -> mm_dsl::PeerIngressRequestCla |
| R118 | Run lifecycle hooks/events keep legacy prompt and error mirrors | `CONFIRMED_ACTIVE` | Medium |  | All three claim components hold at current code; cited lines merely drifted. (1) Stored text mirrors: meerkat-core/src/hooks.rs:360-362 "Text-only projection of `prompt_input` for legacy hooks. pub prompt: Option<String> |
| R119 | Background job completion emits legacy status beside typed terminal status | `STALE_FIXED` | Low |  | meerkat-core/src/event.rs:1859-1864 — the `BackgroundJobCompleted` variant carries ONLY the typed owner: `BackgroundJobCompleted { job_id: String, display_name: |
| R120 | Compaction failure events erase typed failure cause | `STALE_FIXED` | Low |  | The auditor's claimed shape — `AgentEvent::CompactionFailed` exposing only `error: String` — is gone. Current code: `meerkat-core/src/event.rs:1776` defines `Co |
| R121 | SessionProjector partial writes can duplicate derived event files | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: make the projection cursor own both facts of the derived commit — persist the checkpoint as {last_seq, events.jsonl byte length} and have resume truncate events.jsonl to that byte le |
| R122 | ModelProfile provider identity remains string-mediated | `NARROWED_PARTIAL` | Medium |  | ANCHOR FIXED: commit 74ed25e19 ("dogma(models): type ModelProfile.provider as Provider, not String") retyped the cited site; meerkat-core/src/model_profile/mod.rs:39-40 is now "/// Provider that serves this model." / "pu |
| R123 | Model operational defaults resolver is string-keyed | `STALE_FIXED` | Low |  | The resolver trait method is now typed, not string-keyed. meerkat-core/src/model_defaults.rs:25: `fn call_timeout_for(&self, provider: Provider, model: &str) -> |
| R124 | External auth resolver handles are string authority | `NARROWED_PARTIAL` | Medium |  | Cited site IS fixed: meerkat-llm-core/src/provider_runtime/registry.rs:53 `pub external_resolvers: BTreeMap<ExternalResolverId, Arc<dyn ExternalAuthResolverHandle>>`; meerkat-core/src/connection.rs:445 defines the `Exter |
| R125 | OpenAI realtime transcription model default bypasses model catalog | `STALE_FIXED` | Medium |  | The cited live.rs:957 has drifted; the relevant seam is now openai_realtime_transcription_model_for (meerkat-openai/src/live.rs:1029-1035), which resolves the t |
| R126 | OpenAI realtime websocket timeout classification parses error text | `CONFIRMED_ACTIVE` | Medium |  | Minimal root fix: match the typed tungstenite::Error discriminants in the WebSocket arm — Io(e) with ErrorKind::TimedOut → NetworkTimeout; Http(resp) → LlmError::from_http_status(resp.status().as_u16( |
| R127 | Compaction memory source identity remains turn-only shell metadata | `NARROWED_PARTIAL` | Medium |  | Cited legs ARE fixed: meerkat-core/src/memory.rs:35-39 defines `pub struct MessageRange { start: u64, end: u64 }` with fail-closed `new` (memory.rs:46-51, returns `MemoryStoreError::SourceRange` on inverted range), and m |
| R128 | Deleted realtime-attachment policy still leaks through model-routing contracts | `STALE_FIXED` | Medium |  | see above |
| R129 | Supervisor activation rollback still parses cleanup failure text | `STALE_FIXED` | Medium |  | The supervisor-activation rollback path no longer stringifies MobError or matches a cleanup phrase. The cleanup verdict is carried as a typed discriminant. meer |
| R130 | RPC mob member status fail-opens to empty success | `STALE_FIXED` | Medium |  | covered above |
| R131 | Public mob MCP/RPC contracts own status/profile/snapshot shape by hand | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: route the four sites through owners that already exist — (a/b) construct `meerkat_contracts::MobStatusResult`/`MobListResult` with `meerkat_mob_mcp::wire_mob_lifecycle_status(status) |
| R132 | REST OpenAPI schema emission owns manual shadow wire contracts | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: follow the workgraph precedent (meerkat-workgraph/src/rest_contract.rs) — move REST request/response wire structs (CreateSessionRequest, ContinueSessionRequest, SessionDetailsRespons |
| R133 | Web SDK member status fabricates `member_ref` client-side | `STALE_FIXED` | Medium |  | See above. |
| R134 | MCP event-stream read status is string-owned terminal truth | `STALE_FIXED` | Medium |  | see above |
| R135 | SDK capability helpers fabricate fallback status truth | `STALE_FIXED` | Medium |  | See current_evidence field above. |
| R136 | RMAT audit ratchets use source-token heuristics as governance truth | `NARROWED_PARTIAL` | Medium |  | Cited fix is real: pre-fix (5a5d31a4c^) xtask/src/rmat_audit.rs:618 was `if source.contains(banned)` and :655 was GuardedApplyVisitor matching `to_token_stream().to_string()` soup; at HEAD these are typed syn AST visitor |
| R137 | WASM mobpack initialization discards parsed mobpack/trust semantics | `NARROWED_PARTIAL` | Medium |  | Cited site IS fixed: meerkat-web-runtime/src/lib.rs:1344-1354 init_runtime calls extract_verify_and_parse_mobpack(bytes, TrustPolicy::Strict, &TrustedSigners::default()) which runs canonical meerkat_mob_pack::trust::veri |
| R138 | Durable event replay drops canonical stream envelope identity | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: give the wire envelope a constructor that projects the persisted identity (populate `mob_id` from StoredEvent.mob_id; if origin source is to be exposed, add a typed origin-identity f |
| R139 | Remote skill caches can outlive source authority | `STALE_FIXED` | Medium |  | All three cited transports now make stale-source health an authority gate, not a diagnostic. HTTP: meerkat-skills/src/source/http.rs:86-105 — refresh_if_needed comments "Authority gate: an Unhealthy source ... must NOT s |
| R140 | Tool execution policy is config-only | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: in AgentFactory::build_agent (meerkat/src/factory.rs, builder chain at ~4937-4946), add .with_tools_config(config.tools.clone()) (or project just the three policy fields if mcp_serve |
| R141 | Live config propagation is best-effort channel policy | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: make `close_live_channel_for_config_rejection` return a typed `Result<(), LiveChannelCloseFailure>` (variants: SignalFailed, CloseAuthorityRejected, CommitHandoffMissing, HostCommitF |
| R142 | Outbound peer request target is stringified then ignored by authority | `STALE_FIXED` | Medium |  | see above |
| R143 | MCP live reload stages partial operations without transaction | `CONFIRMED_ACTIVE` | Low |  | meerkat-mcp-server/src/lib.rs:2644-2651 (handler drifted from cited :2552): `None => { for name in adapter.active_server_names().await { adapter.stage_reload(McpReloadTarget::ServerName(name)).await.map_err(\|e\| format! |
| R144 | CLI mob registry/deploy is a surface-owned mob persistence path | `STALE_FIXED` | High |  | The cited lines drifted: main.rs:11764 is now `fn parse_run_flow_params` and :11915 is the end of the `MobCommands::Web` block — neither touches a registry side |
| R145 | Formal TLA composition route helpers fail open to sentinel strings | `STALE_FIXED` | Low |  | The cited `unknown_*` sentinel behavior has been fully replaced by typed fail-closed codegen. (1) `CompositionTlaCompiler::render()` runs `self.validate_routes( |
| R146 | Formal/oracle numeric width collapses `u32` into unbounded naturals | `NARROWED_PARTIAL` | Low |  | CITED FIX VERIFIED REAL at HEAD (2a090e566). (1) Codegen half: meerkat-machine-codegen/src/artifacts.rs:4132-4138 — "// dogma #172 (codegen half): U32 and U64 are distinct bounded domains" with `TypeRef::U32 => "U32Value |
| R147 | WASM fire-and-forget JS tools fabricate acknowledged success | `STALE_FIXED` | Medium |  | meerkat-web-runtime/src/lib.rs:1186-1202 (JsToolDispatcher::dispatch) no longer fabricates an "acknowledged" success. For fire-and-forget tools it now returns a |
| R148 | Generated kernel seeds semantic defaults outside DSL init authority | `STALE_FIXED` | Medium |  | The auditor's claim has two pillars, both refuted by current code.  PILLAR 1 ("schema validation only checks that listed initializers name real fields") is FALS |
| R149 | Generated-header audit treats marker text as provenance truth | `OVERFLAGGED_BY_DESIGN` | Low |  | xtask/src/audit_generated_headers.rs:196 `pub fn live_emit_paths()` derives the emit set from the TYPED schema authority, not a hard-coded list: lines 200-204 ` |
| R150 | Live WebSocket binary audio format is surface-owned | `STALE_FIXED` | Medium |  | The cited claim (live/open hardcodes pcm_24k_mono into the URL while LiveProjectionSnapshot.audio_config stays None) no longer holds in current code (worktree, |
| R151 | B2 compatibility re-export shims lack expiry | `NARROWED_PARTIAL` | Low |  | meerkat-models/src/lib.rs:1-19 is now ENTIRELY `pub use` aliases over meerkat_core::model_profile, with self-declared justification at lib.rs:3-6: "All model catalog / capability / profile data moved to `meerkat_core::mo |
| R152 | Provider backend/auth declarations live in core | `OVERFLAGGED_BY_DESIGN` | Medium |  | The flagged shape preserves singular typed ownership with zero divergence windows; the prior drop's doc-comment evidence was circular, but a genuine functional case exists. (1) One typed owner per fact, parse-at-ingress  |
| R153 | Web-search fallback reports unverified completion | `NARROWED_PARTIAL` | Medium |  | Port the meerkat-openai fix verbatim to both siblings: (1) native_events.is_empty() → WebSearchStatus::Unavailable with the unverified-search error witness; (2) replace the LlmError::Unknown map_err w |
| R154 | Mob generation monotonicity is shell-owned | `STALE_FIXED` | Medium |  | Machine owns the increment: meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:3055-3067 transition ComputeRespawnGeneration emits `RespawnGenerationComputed { next_generation: mob_machine_next_respawn_generation(self |
| R155 | Ops terminal payload truth is JSON-string authority | `NARROWED_PARTIAL` | Medium |  | Payload remains an opaque JSON string inside the canonical machine: meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:2603-2605 "// Terminal-outcome discriminant. Payload (result/error/reason) rides on the compan |
| R156 | Config patch semantics are reimplemented in public surfaces | `STALE_FIXED` | Medium |  | meerkat-core/src/config_store.rs:227 defines the single canonical `pub fn merge_patch(base, patch)` and its doc-comment (lines 222-226) explicitly states "This |
| R157 | Schedule-host mob delivery failure classes are handler-owned | `STALE_FIXED` | Medium |  | see above |
| R158 | Web mobpack bootstrap drops maxSessions policy | `STALE_FIXED` | Medium |  | All three sub-claims are refuted by current code with regression-guard tests.  1) WASM `Credentials` DOES deserialize max_sessions — meerkat-web-runtime/src/lib |
| R159 | DSL phase projection exhaustiveness can escape validation into generated panic | `STALE_FIXED` | Medium |  | The auditor's claim — that derived-phase validation "explicitly declines to fail when every projection rule is conditional" and generated code falls through to |
| R160 | Composition structural requirements are not checked by CI TLC profile | `STALE_FIXED` | Medium |  | The auditor claims structural requirements are only checked under the Deep profile while normal composition CI configs carry only behavioral/instance invariants |
| R161 | MCP tool payload serialization can fail open | `STALE_FIXED` | Medium |  | See above. |
| R162 | MCP/CLI callback-pending run state is a string success envelope | `STALE_FIXED` | Medium |  | Verified above |
| R163 | RPC/REST/MCP capabilities and model catalog fabricate default config | `STALE_FIXED` | Medium |  | See above |
| R164 | SDK session history parsers fabricate empty transcript truth | `CONFIRMED_ACTIVE` | Medium |  | Every coercion category in the claim is live in current code, in both SDKs. TypeScript (sdks/typescript/src/client.ts): parseSessionMessage:4453 `const role = String(data.role ?? "")` (missing role -> ""); :4458-4466 mis |
| R165 | SDKs mirror contract-version compatibility policy by hand | `STALE_FIXED` | Low |  | Fix verified: sdks/typescript/src/client.ts:3364-3369 `checkVersionCompatible` returns `isCompatibleWith(server, client)` imported from ./generated/version_compat.js (client.ts:44), comment names dogma row #193; sole cal |
| R166 | Completion events can precede terminal boundary authority | `STALE_FIXED` | Medium |  | Cited lines drifted; current sites verified at HEAD (fix commit 8b9237da6 is an ancestor of HEAD). (1) Fallible extraction setup now resolves BEFORE any success-shaped event: meerkat-core/src/agent/state.rs:2777-2796 "Co |
| R167 | Post-turn persistence and checkpoint failures are non-terminal | `OVERFLAGGED_BY_DESIGN` | Medium |  | Prong 1: meerkat-core/src/agent/state.rs:264-275 — save_session_best_effort prefers the injected checkpointer (state.rs:268-270) and only falls back to `if let Err(e) = self.store.save(&self.session).await { tracing::war |
| R168 | Resumed preloaded skills are silently dropped by surface availability | `NARROWED_PARTIAL` | Medium |  | CITED SITE FIXED: meerkat/src/factory.rs:4575-4652 — resume filter resolves persisted IDs via engine.canonical_skill_key and emits typed AgentEvent::SkillResolutionFailed { skill_key, reason: SkillResolutionFailureReason |
| R169 | Live terminal errors do not reach a session-owned terminal seam | `STALE_FIXED` | Medium |  | The original absence claim ("no canonical session terminal-error seam records the terminal cause for callers outside the live stream") is dead at HEAD. (1) Seam exists and is canonical: meerkat-core/src/service/mod.rs:17 |
| R170 | Missing live transcript identity becomes empty-string or direct-append truth | `STALE_FIXED` | Medium |  | The row's narrowed claim — "assistant transcript deltas still fall back missing response/delta/item IDs to empty strings at meerkat-rpc/src/live_projection_sink |
| R171 | Verified mobpack trust is not a typed archive contract | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: make the verified type the only road to archive truth. Demote `MobpackArchive::from_*` to pub(crate) so external consumers must construct via `VerifiedMobpackArchive::open/open_archi |
| R172 | Deferred tool-catalog control can accept loads through fallback visibility | `NARROWED_PARTIAL` | Medium |  | Cited fix verified at HEAD (commit 6e8528d03): meerkat-tools/src/control_plane.rs:24-32 defines typed `pub struct NoToolScope`; resolve_load_visibility() fails closed (line 125 `guard.as_ref().ok_or(NoToolScope)?`, line  |
| R173 | Tool schema generation fails open to null schemas | `NARROWED_PARTIAL` | Medium |  | Cited sites ARE fixed: meerkat-tools/src/schema.rs:14 now reads `let mut value = schema.to_value();` (schemars' infallible accessor, with `schema_for_is_never_null` test at schema.rs:54), and meerkat-tools/src/builtin/sk |
| R174 | BuildBuddy change detection skips governance-audit source changes | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: add `xtask/rmat-baseline.toml\|xtask/ownership-baseline.toml` to the governance case arm at scripts/machine-authority-changed:96, and extend the #203 block in scripts/tests/xtask_scri |
| R175 | WASM subscription lag is a handwritten in-band fault contract | `NARROWED_PARTIAL` | Medium |  | Minimal root fix is the one the code and audit doc already name: add a `SubscriptionLagged { kind: "lagged", skipped: u64 }` wire type to meerkat-contracts so it enters emit_all_schemas + SDK codegen; |
| R176 | Missing image blobs degrade into prompt text instead of terminal failure | `CONFIRMED_ACTIVE` | Medium |  | (1) Default is still the fail-open branch: meerkat-core/src/image_content.rs:15-16 `#[default] HistoricalPlaceholder`, and meerkat-core/src/agent/builder.rs:244 `missing_blob_behavior: crate::image_content::MissingBlobBe |
| R177 | Recovery realm defaults backfill durable session identity from context strings | `STALE_FIXED` | Medium |  | See above. |
| R178 | Google Code Assist onboarding owns project/tier policy in provider runtime | `OVERFLAGGED_BY_DESIGN` | Medium |  | meerkat-gemini/src/runtime/mod.rs:223-226 — `struct CodeAssistProjectResolution { project_id: String, tier: Option<String> }`, doc'd as "the single typed owner of the loadCodeAssist/onboardUser policy decision"; the doc  |
| R179 | Live snapshot prompt truth is inferred from seed history | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: in build_live_projection_snapshot_for_runtime (meerkat/src/session_runtime/live_orchestration.rs:143) read `open_config.system_prompt.clone()` (mirroring meerkat-rpc/src/handlers/liv |
| R180 | Flow task trackers own active-run lifecycle beside MobMachine | `OVERFLAGGED_BY_DESIGN` | Medium |  | Verified mechanically, not via the doc-comment. (1) Semantic verdict is machine-owned: DSL transition `CancelFlowRunning { on input CancelFlow { run_id } guard "run_known" { self.run_status.contains_key(run_id) == true } |
| R181 | Mob operator grants are post-hoc session effects | `NARROWED_PARTIAL` | High |  | Cited lines drifted: the grant sites are now meerkat-mob-mcp/src/agent_tools.rs:808-824 (dispatch_delegate) and :900-917 (dispatch_mob_create). What SURVIVES of the prior verdict: minting is genuinely singular — grant_ma |
| R182 | Kickoff lifecycle notice effects are shell-narrowed | `STALE_FIXED` | Medium |  | Fix verified at HEAD (2a090e566) via commit 6e8528d03 (ancestor, message: 'mob: #13/#210/#212 shell facts lowered to typed MobMachine inputs/effects'). Pre-fix shape confirmed via git show: kickoff_notice_intent returned |
| R183 | `mob/turn_start` reserializes typed params through `turn/start` JSON | `STALE_FIXED` | Medium |  | In current code, `handle_mob_turn_start` (meerkat-rpc/src/handlers/mob.rs:1924-2013) parses the typed `meerkat_contracts::MobTurnStartParams` and constructs the |
| R184 | RPC mob member status collapses future domain states to `unknown` | `STALE_FIXED` | Medium |  | The claimed collapse is gone. (1) meerkat-rpc/src/handlers/mob.rs:171-183 `project_member_status` now returns `Result<WireMobMemberStatus, String>`: every known variant is mapped explicitly and the wildcard arm FAILS — ` |
| R185 | WASM session APIs expose browser-local session handles | `STALE_FIXED` | Medium |  | All three sub-claims are refuted by current code.  (1) "Destroyed handles can remain addressable" — REFUTED. meerkat-web-runtime/src/lib.rs:1818-1822 `destroy_s |
| R186 | WASM mob lifecycle drops destroy report truth | `STALE_FIXED` | Medium |  | meerkat-web-runtime/src/lib.rs:1960 `pub async fn mob_lifecycle(mob_id: &str, action: &str) -> Result<JsValue, JsValue>` — NOT void/(). Lines 1970-1981 capture |
| R187 | WASM mob event cursors narrow and reset stream position | `STALE_FIXED` | Medium |  | Both legs fixed at HEAD. (1) meerkat-web-runtime/src/lib.rs:2024 `pub async fn mob_events(mob_id: &str, after_cursor: &str, limit: u32)` — cursor is an opaque string; parse_mob_event_cursor (lib.rs:2041-2052) maps empty→ |
| R188 | Explicit semantic memory enable can silently produce no memory tool | `NARROWED_PARTIAL` | Medium |  | FIXED HALF: meerkat/src/factory.rs:5028-5038 — on HnswMemoryStore::open failure, `if matches!(build_config.override_memory, ToolCategoryOverride::Enable) { return Err(BuildAgentError::CapabilityUnavailable { capability:  |
| R189 | Skill inventory failure is lowered into prompt text | `STALE_FIXED` | Medium |  | factory.rs:4565-4567 fails closed with a typed error instead of fabricating prompt text: `let inventory = engine.inventory_section().await.map_err(\|e\| { BuildAg |
| R190 | Filesystem and embedded skill sources silently drop invalid skills | `STALE_FIXED` | Low |  | Both cited files now record invalid/dropped entries into a typed source-health owner rather than silently dropping them. Filesystem source `meerkat-skills/src/s |
| R191 | BuildBuddy machine-authority lane can pass without TLC | `STALE_FIXED` | Medium |  | xtask/tests/machine_verify_all_tlc_test.sh now FAILS CLOSED when tlc is absent rather than silently falling back. Lines 31-40: ``` if ! command -v tlc >/dev/nul |
| R192 | Web SDK event parsers bypass generated event contracts | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: have parseEventPayload classify payload.type against the generated inventory via isKnownEvent (import from types.ts), mirroring the policy split the TS SDK's parseCoreEvent already u |
| R193 | WebRTC output-audio delivery truth is transport-local | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: give the D223 `dropped_packets` fact a production read path into a session-observable seam — e.g. fold the cumulative drop count into the existing generated live-channel status feedb |
| R194 | Machine peer endpoint authority accepts string-shaped identity/address | `STALE_FIXED` | Medium |  | The auditor's CORE claim — "validation/parsing happens later in runtime trust reconciliation AFTER machine mutation and effect emission" — is falsified. Orderin |
| R195 | Session active-capacity admission is service-local | `NARROWED_PARTIAL` | Medium |  | The drop rested solely on the staged_registry.rs:1-2 doc-comment ("the single typed owner of session materialization status and the global active-capacity admission seam") — circular under the corrected rule. Code verifi |
| R196 | TokenStore failures become ephemeral or missing auth truth | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: at the three status surfaces, replace `let Ok(Some(rehydrated)) = rehydrate_marked_tokens_for_status(...)` with a match that propagates Err as the surface's typed failure (anyhow bai |
| R197 | Bazel package-version metadata is outside the version parity gate | `STALE_FIXED` | Low |  | The auditor's two named uncovered facts are now BOTH bound by the same parity gate that runs in `make ci`.  Makefile:328 — `ci: ... verify-version-parity ...` ( |
| R198 | Interaction event tap delivery is best-effort shell truth | `NARROWED_PARTIAL` | Medium |  | CIRCULARITY OF PRIOR VERDICT: the by-design evidence was the doc-comment "Projection contract: best-effort observability, never authority" (meerkat-core/src/event_tap.rs:7-22), which was itself the fix for this row — com |
| R199 | Delegate helper wired truth is notification-owned | `STALE_FIXED` | Medium |  | Fix confirmed at meerkat-mob-mcp/src/agent_tools.rs: `wired` is now committed-edge truth, not notification truth. Lines 717-727 commit helper->parent via `mob_wire(...PeerTarget::External(parent_spec))` with fail-closed  |
| R200 | Realtime text request tunables fail open to provider defaults | `STALE_FIXED` | Medium |  | meerkat-openai/src/text_adapter.rs:65-72 `realtime_max_output_tokens` now fails closed on an explicit zero rather than reverting to the provider default: `if ma |
| R201 | Hook runtime adapter config remains opaque JSON | `STALE_FIXED` | Medium |  | meerkat-core/src/config.rs:1861-1867 defines the closed typed enum `#[serde(tag = "type", rename_all = "snake_case")] pub enum HookAdapterConfig { InProcess(Hoo |
| R202 | Semantic memory failures are string-only across store/tool boundary | `NARROWED_PARTIAL` | Medium |  | Cited sites ARE fixed (commit 7e782c59d): (1) MemoryStoreError is a typed 9-variant enum with stable error_code() — meerkat-core/src/memory.rs:441-498, doc: callers "preserve the typed distinction downstream without pars |
| R203 | Auth status projection collapses released, absent, and missing-credential truth | `STALE_FIXED` | Medium |  | meerkat-core/src/auth/status.rs:18-30 defines AuthStatusPhase with NO Unknown variant; the three states the auditor claims are collapsed are now distinct typed |
| R204 | Live control result contracts are untyped `Value` | `NARROWED_PARTIAL` | Medium |  | Cited sites ARE fixed: meerkat-contracts/src/rpc_catalog.rs:542-565 advertises RpcMethodDescriptor::typed for all four methods ("live/send_input"/"LiveSendInputResult", "live/commit_input"/"LiveCommitInputResult", "live/ |
| R205 | Mob reconcile failure cause is string-only at the public surface | `STALE_FIXED` | Medium |  | see above |
| R206 | Prompt source failures fall back to alternate prompts | `NARROWED_PARTIAL` | Medium |  | FIXED half: meerkat/src/prompt_assembly.rs:96-106 — configured system_prompt_file now fails closed: `Err(source) => { return Err(PromptAssemblyError::SystemPromptFileUnreadable { path: path.display().to_string(), source  |
| R207 | Factory builder uses stale config after store failure | `STALE_FIXED` | Medium |  | meerkat/src/service_factory.rs `resolve_config` (lines 643-653): on a configured store, it propagates the failure as a typed error and never falls back to the s |
| R208 | SQLite session-list metadata saturates invalid durable counters | `STALE_FIXED` | Low |  | At HEAD 67905861671, both cited paths now fail closed instead of laundering. WRITE (meerkat-store/src/sqlite_store.rs:82-93): `let message_count = i64::try_from |
| R209 | Skill diagnostics collection fails open | `NARROWED_PARTIAL` | Medium |  | Minimal root fix (already named in-code at state.rs:3086-3090): add a typed failure/fault variant to meerkat_core::skills::SkillRuntimeDiagnostics (or make the run-result field a typed enum of Collect |
| R210 | HNSW memory rollback leaves stale search candidates | `NARROWED_PARTIAL` | Medium |  | Cited fix is real: meerkat-memory/src/hnsw.rs:474-513 rolls back DB rows on partial HNSW failure then rebuilds the scoped live index from surviving committed rows via rebuild_scoped_index_from_db (hnsw.rs:137-185), teste |
| R211 | Plain event ingress body and fault semantics are transport-local | `STALE_FIXED` | Medium |  | Both halves fixed at HEAD (commit b56ba1bab). (1) Body contract: meerkat-comms/src/transport/plain_codec.rs:17-26 defines typed `pub enum PlainLine { Empty, Structured(Value), Text(String) }`; classify_plain_line (plain_ |
| R212 | Inproc peer address and namespace are ignored during trusted sends | `FIXED_THIS_PR` | High | df2b353d9 | FIX VERIFIED at the cited seam, plus a follow-up commit that closed the residual the QA sample predicts. (1) Cited site: meerkat-comms/src/router.rs:554-590 `send_inproc_in_namespace` — `match self.inproc_namespace.as_de |
| R213 | Live direct transports bypass the live input wire contract | `STALE_FIXED` | Medium |  | The cited bypass is fixed. meerkat-live/src/webrtc.rs:499-521 (data channel) deserializes the generated wire type, not core: `serde_json::from_slice::<LiveInput |
| R214 | SDK transport parsers silently drop malformed JSON-RPC frames | `STALE_FIXED` | Medium |  | Both SDK transport parsers now fail closed with a typed PROTOCOL_ERROR; the auditor's claimed silent-drop behavior is absent in current code.  Python sdks/pytho |
| R215 | SDK callback-tool registration is best-effort after local acceptance | `STALE_FIXED` | Medium |  | sdks/typescript/src/client.ts:553-592 `registerToolWithServer` no longer swallows the post-connect fault. On a genuine server rejection it constructs a typed `M |
| R216 | Create-session streaming admission is SDK-singleton state | `NARROWED_PARTIAL` | Low |  | Port the TS shape to Python: replace the singleton with `dict[int, asyncio.Queue]` keyed by request_id in _StdoutDispatcher; on response resolution bind only that request's queue to its session_id and |
| R217 | System-context append payload is text-only at surface seam | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: delete the five duplicate matches and call `append.content.render_text()` (run_primitive.rs:77) at each site; optionally hoist the shared ConversationContextAppend→PendingSystemConte |
| R218 | Schedule and occurrence identities are string-owned in canonical DSL | `NARROWED_PARTIAL` | Medium |  | Apply the already-declared newtypes: retype occurrence state/input fields to TargetBindingId/ClaimOwner/CorrelationId, register NamedTypeBinding::string("TargetBindingId"/"ClaimOwner"/"CorrelationId") |
| R219 | Machine posters are partial stale un-gated generated artifacts | `NARROWED_PARTIAL` | Low |  | Minimal root fix for the residual: regenerate the two posters at HEAD, add a deterministic-output mode to the generator (drop or pin the line-859 wall-clock generatedAt), and extend verify-machine-pos |
| R220 | ApplyPatch root containment is lexical | `STALE_FIXED` | High |  | meerkat-tools/src/builtin/utility/apply_patch.rs:645 `resolve_patch_path` is NOT lexical-only. After the lexical check at line 668 (`if !resolved.starts_with(pr |
| R221 | Blob file tools mint media-type/blob identity from strings | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: validate at the boundary — call MediaType::parse on the explicit media_type arg in BlobLoadFileTool::call (blob_file.rs:420-423) and store/return the canonical form; optionally tight |
| R222 | Session-create default model policy is surface-owned | `FIXED_THIS_PR` | High | 77ca23c17 + 2568493f8 | Minimal root fix: make `resolve_cli_default_agent_model` (and the `best_available_default_model` legacy-healing redirect) terminate in `meerkat::resolve_create_session_default_model(config)` instead o |
| R223 | RPC typed catalog advertises handler-local ghost contracts | `STALE_FIXED` | Medium |  | See above. |
| R224 | WorkGraph read SDK parity is handwritten outside generated contracts | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: extend WORKGRAPH_RPC_CONTRACT_TYPES/alias lists in tools/sdk-codegen/generate.py with the read filter/response types and emit named runtime closed unions for WorkEdgeKind/WorkGraphEv |
| R225 | Live response modality request is widened by SDK wrappers | `CONFIRMED_ACTIVE` | Low |  | Minimal root fix: narrow the helper parameter types to the typed distinction the contract already owns. TS: change `commitInput(responseModality?: string)` to `responseModality?: "audio" \| "text"` (or |
| R226 | Flow context expressions fail open into control truth | `NARROWED_PARTIAL` | Medium |  | FIXED half (conditions): meerkat-mob/src/runtime/conditions.rs:91-94 `pub(crate) fn evaluate_condition(expr: &ConditionExpr, ctx: &FlowContext) -> Result<bool, ConditionEvalError>` fails closed on structural faults; cond |
| R227 | Flow schema refs are executor-owned path strings | `STALE_FIXED` | Medium |  | The inline-vs-named distinction now has a single typed owner, parsed once at the boundary, not during execution:  definition.rs:264-270 — `pub enum FlowSchemaRe |
| R228 | Flow output parsing owns retry and failure truth | `NARROWED_PARTIAL` | Medium |  | CITED FIX VERIFIED: commit 49eb39330 (ancestor of HEAD) folded parse-fault retry-vs-terminal into MobMachine. Parse boundary is typed: `enum StepOutputFault { MalformedJson {..} }` (meerkat-mob/src/runtime/flow.rs:2097-2 |
| R229 | Supervisor escalation is prompt-timeout shell policy | `NARROWED_PARTIAL` | Medium |  | Fold #261 (commit 49eb39330, ancestor of HEAD) fixed part of the row: structural two-variant input (meerkat-machine-schema/src/catalog/dsl/mob_machine.rs:700-706) and typed fail-closed NoEligibleSupervisor (transition Es |
| R230 | Local bridge input provenance is session-string authority | `STALE_FIXED` | Medium |  | Fix verified at HEAD: meerkat-mob/src/runtime/local_bridge.rs:122-131 stamps provenance from the canonical typed identity — `let runtime_id = LogicalRuntimeId::for_session(&self.session_id); ... InputOrigin::Peer { peer_ |
| R231 | Recovered input lifecycle is shell-normalized before DSL recovery | `OVERFLAGGED_BY_DESIGN` | Medium |  | The narrowed residual ('shell still supplies observed phase into recovery') is the structural shell-to-machine observation feed, and every normalization decision is owned by generated DSL — verified independently of the  |
| R232 | Run boundary receipt sequence bypasses machine boundary counter | `CONFIRMED_ACTIVE` | Medium |  | Two producers of the same semantic fact (run boundary sequence), one machine-owned and one shell-fabricated, writing into one durable keyspace. (a) Machine owner exists: meerkat-machine-schema/src/catalog/dsl/meerkat_mac |
| R233 | EventStore schema version is inert | `STALE_FIXED` | Low |  | see above |
| R234 | Dogma skill doctrine mirror is ungated and legacy-number incomplete | `OVERFLAGGED_BY_DESIGN` | Low |  | see above |
| R235 | Live adapter tool-call identity is provider-string authority | `NARROWED_PARTIAL` | Medium |  | The routing half of the claim is genuinely fixed by commit f524516a4 (#270): the observation seam is typed — meerkat-core/src/live_adapter.rs:329-333 `ToolCallRequested { provider_call_id: ToolCallId, tool_name: ToolName |
| R236 | Realtime external attachment target is raw provider identity | `OVERFLAGGED_BY_DESIGN` | Medium |  | Cited line meerkat-openai/src/live.rs:2815 drifted; the live sites are: (1) meerkat-llm-core/src/realtime_session.rs:17-35 — "/// Advanced/internal target for attaching to an existing provider session. pub struct Realtim |
| R237 | Agent loop owns max-turn default policy | `STALE_FIXED` | Medium |  | see current_evidence field |
| R238 | WaitingForOps faults bypass turn terminalization | `STALE_FIXED` | Medium |  | The cited lines 2689/2699 drifted (now extraction code). The actual `WaitingForOps` arm is at meerkat-core/src/agent/state.rs:2897-2998. Every fault path in tha |
| R239 | Unsupported video tool results emit success events before nonterminal failure | `STALE_FIXED` | Medium |  | meerkat-core/src/agent/state.rs:2411-2425 rejects video tool results BEFORE any success events: `// Reject unsupported video tool results BEFORE any success-sha |
| R240 | Approval control handles remain string/JSON authority | `OVERFLAGGED_BY_DESIGN` | Medium |  | meerkat-core/src/approval.rs:155-173 — ApprovalOwnerRef variants carry real typed ids ('Session { session_id: SessionId }', 'Run { run_id: RunId }', 'ToolCall { tool_call_id: ToolCallId }'), refuting 'typed only at the o |
| R241 | Compaction cadence metadata is optional shadow truth | `NARROWED_PARTIAL` | Medium |  | FIXED PART (the screen's cited crux): the split-commit "persistence failure is only logged" tail is genuinely gone. meerkat-core/src/agent/state.rs:1101-1110: `crate::agent::compact::persist_compaction_cadence(self.sessi |
| R242 | Auto token store has split backend authority | `STALE_FIXED` | Medium |  | meerkat-auth-core/src/auth_store/auto.rs:73-97 `resolve_backend()` resolves ONE authoritative backend per TokenKey as a recorded fact (keyring-resident→Keyring; KeyringUnavailable→explicit File demotion at :83; file-resi |
| R243 | Production semantic memory ranking is store-local hashing policy | `STALE_FIXED` | Medium |  | A typed memory-policy + embedding-model contract now exists in meerkat-core/src/memory.rs and the store consumes it as an injected owner, not store-local consta |
| R244 | Live tool-dispatch timeout is host-local terminal policy | `STALE_FIXED` | Medium |  | Cited lines 2101/2111/2117 have drifted (they now sit inside `apply_observation` transcript routing). The actual tool-dispatch-timeout code is at meerkat-live/s |
| R245 | TS/Python stream event parsers bypass generated AgentEvent contracts | `STALE_FIXED` | Medium |  | The event-type inventory that gates both SDK parsers is generated from the canonical contract, not hand-maintained. Canonical owner: meerkat-contracts/src/event |
| R246 | Public AgentBuilder retry policy drops hard call-timeout policy | `STALE_FIXED` | Medium |  | Public facade `AgentBuilder::retry_policy` no longer drops `call_timeout`. meerkat/src/agent_builder.rs:143-156 copies every RetryPolicy field AND explicitly ca |
| R247 | Runtime effect-channel closure exits loop without terminalization | `NARROWED_PARTIAL` | Medium |  | FIXED HALF: drain disconnection now terminalizes canonically. control_plane.rs:97-108 defines typed EffectDrainOutcome{AppliedStop,Empty,ChannelClosed}; runtime_loop.rs:1267-1281 handles ChannelClosed: "Closure is NOT an |
| R248 | RuntimeStore bypasses session and input migrators | `STALE_FIXED` | Medium |  | All runtime-store reads of PERSISTED rows now route through the migrators, contradicting the claim. meerkat-runtime/src/store/sqlite.rs:78-83 defines `fn deseri |
| R249 | Skill frontmatter extensions are string-only policy blobs | `STALE_FIXED` | Low |  | The disputed semantic distinction (the extension KEY namespace/policy boundary) is now a typed owner, and the auditor's central `IndexMap<String, String>` descr |
| R250 | Flow topology policy is shell-owned role-dispatch authority | `STALE_FIXED` | Medium |  | meerkat-mob/src/runtime/topology.rs:39-47: `MobTopologyService::evaluate` returns `Option<PolicyDecision>` and the doc comment explicitly states "`None` (no spe |
| R251 | Staged callback tool results bypass turn authority | `NARROWED_PARTIAL` | Medium |  | Cited sites ARE fixed: meerkat-core/src/session.rs:1822-1859 stage_tool_results drives authority.stage_session_tool_results and only pushes after an accepted-count verdict (rejection returns 0 fail-closed); meerkat-core/ |
| R252 | In-memory RuntimeStore accepts non-Session snapshots without owner context | `STALE_FIXED` | Low |  | At meerkat-runtime/src/store/memory.rs:244-248, atomic_apply now fails closed in the no-owner-context case: `(Err(err), None) => { return Err(RuntimeStoreError: |
| R253 | Live projection errors erase typed session causes | `STALE_FIXED` | Medium |  | see above |
| R254 | Flow terminal status and event commit split | `NARROWED_PARTIAL` | High |  | SPLIT STILL PRESENT: flow.rs terminalize_with_machine_state first CAS-commits terminal status durably, then separately appends the terminal event — meerkat-mob/src/runtime/flow.rs:1820-1835: `let transitioned = self.run_ |
| R255 | Flow recovery re-derives scheduler truth | `CONFIRMED_ACTIVE` | Medium |  | Minimal root fix: make recovery.rs `loop_is_pending_body_frame` use the typed owner's predicate, identical to flow_frame_engine.rs:3540-3542 — `phase == Running && stage == LoopIterationStage::Awaitin |
| R256 | SDK codegen uses hand-curated contract rosters | `CONFIRMED_ACTIVE` | Medium |  | Minimal root fix: close the schema→roster direction. Either (a) derive the roster from the contract authority — emit an SDK contract-type inventory from meerkat-contracts (the precedent already exists |
| R257 | SDK wrapper freshness is substring parity | `STALE_FIXED` | Medium |  | Fix verified at HEAD 2a090e566 by execution and mutation testing. (1) The cited substring gate scripts/verify_sdk_wrapper_freshness.py still exists (lines 52-53: `missing_ts = [m for m in required_methods if m not in ts_ |
| R258 | Execution snapshot overflow erases runtime turn state | `STALE_FIXED` | Low |  | runner.rs:142 runtime_execution_snapshot returns Result, overflow -> Err(CounterOverflow); execution_snapshot returns Ok(None) only for no-handle; state() retur |
| R259 | Runtime-backed session load can promote SessionStore projection after RuntimeStore loss | `STALE_FIXED` | Medium |  | The recovery-source eligibility verdict is now owned by the canonical SessionDocumentMachine, not the shell. meerkat-session/src/persistent.rs:1683-1712 (the lo |
| R260 | Skill repository credentials are skill-config-owned strings | `NARROWED_PARTIAL` | Low |  | Config ingress (meerkat-core/src/skills_config.rs:105-132) still carries credentials as untagged optional strings: `Http { url, auth_header: Option<String>, auth_token: Option<String>, .. }` and `Git { .., auth_token: Op |
| R261 | Mob member peer-connectivity fault is erased as absent projection | `STALE_FIXED` | Medium |  | The auditor's claim that connectivity timeouts are "logged and returned as None" and that wire/Web SDK expose "an optional object rather than typed failure" is |
| R262 | Owner-provided route bindings bypass typed handoff validation | `STALE_FIXED` | Medium |  | Fix commit 73eb0c03f is an ancestor of HEAD (2a090e566). (1) Fail-closed check present: meerkat-machine-schema/src/composition.rs:1442-1471 — OwnerProvided bindings require coverage by a handoff protocol (producer_instan |
| R263 | Composition Rust codegen bindings are string-owned authority | `NARROWED_PARTIAL` | Medium |  | FIXED half: every emission-metadata kind now has a role-typed newtype owner — meerkat-machine-schema/src/composition.rs:66-106 (RustModulePath/RustTypePath/RustItemIdent/RustMethodName/RustUseStatement/RustArgAccessor/Ru |
| R264 | Auth test surfaces mutate credential freshness and lease truth | `NARROWED_PARTIAL` | Medium |  | FIXED HALF (verified): publish_resolved_auth_lease is deleted workspace-wide (0 grep hits); commit 3e174c4c3 (verified ancestor of HEAD via merge-base) says "#317 rest: test_auth_binding is read-only (deleted publish_res |
| R265 | Flow turn timeout/orphan disposition is executor-owned terminal policy | `NARROWED_PARTIAL` | Medium |  | FIXED ARM: executor-local orphan budgets/maps are gone. ActorFlowTurnExecutor is just {handle, provisioner} (meerkat-mob/src/runtime/actor_turn_executor.rs:23-27); on_timeout (actor_turn_executor.rs:406-455) drives MobMa |
| R266 | Skills availability is advertised by static surface catalogs | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: mirror the RPC pattern at the two residual sites. REST: `options.skills = state.skill_runtime.is_some()` in rest_runtime_host_surface_options (meerkat-rest/src/lib.rs:3371), matching |
| R267 | Per-turn tool overlay is shell-owned string policy | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: add a `ToolName(pub String)` bridging newtype to the DSL bridging-types section (meerkat_machine.rs:~47, beside OperationId), switch `turn_tool_overlay_allow_names`/`deny_names` (and |
| R268 | LLM call timeout terminal class is agent-loop-selected | `STALE_FIXED` | Medium |  | see above |
| R269 | Synthetic runtime notices mutate prompt truth from projections | `NARROWED_PARTIAL` | Medium |  | Live sites confirmed at meerkat-core/src/agent/state.rs (cited lines drifted). The main claim does NOT survive: each notice family is a genuine read-only projection of a typed owner rebuilt at every LLM ingress, provable |
| R270 | Skill identity overlays are last-writer-wins | `STALE_FIXED` | Low |  | meerkat-core/src/skills/identity.rs:140-147 fails closed on duplicate remap `from` instead of last-writer-wins HashMap::insert: `if let Some(existing) = remap_i |
| R271 | RPC surface alignment gate is source-string parity | `NARROWED_PARTIAL` | Medium |  | Minimal root fix for the residual: add a behavioral parity test in meerkat-rpc that constructs a fully-equipped router and dispatches every method from rpc_method_names(all-options catalog), asserting |
| R272 | mob/cancel_work public surface is wired before work tracking exists | `STALE_FIXED` | Medium |  | The auditor claims the runtime command arm is "hardcoded to WorkNotFound" returning a phantom ok-success shape. Current code does NOT do this. In meerkat-mob/sr |
| R273 | Live input frame rejection is transport-specific | `STALE_FIXED` | Medium |  | The auditor's claim is that a malformed live-input JSON frame closes the channel on WebSocket but only warns-and-keeps-alive on WebRTC, so the same fault termin |
| R274 | Public contract-version docs are stale mirrors | `CONFIRMED_ACTIVE` | Medium |  | Minimal root fix: stop hand-maintaining the version strings — either (a) have the regen pipeline (emit-schemas / a small docs-stamp step in scripts/release-hook.sh next to bump-sdk-versions.sh) rewrit |
| R275 | Hook tool-result text mirror can drive policy beside typed blocks | `STALE_FIXED` | Medium |  | HookToolResult stores only typed content_blocks; `content` is a serialize-only text_projection derived from content_blocks (hooks.rs:311, 345), never stored, ne |
| R276 | Mob member profile policy is string/JSON-owned before spawn | `NARROWED_PARTIAL` | Medium |  | VIOLATIONS SURVIVE on 2 of 4 legs. (1) output_schema: profile.rs:191 `pub output_schema: Option<serde_json::Value>` with doc justification at profile.rs:185-189 "Validation is deferred to spawn time ... MeerkatSchema doe |
| R277 | Supervisor bridge self-address is recipient-transport policy | `STALE_FIXED` | Medium |  | Cited lines 468/484/498 have drifted: they now sit inside `remove_bridge_trust` (DSL-driven trust-removal reconciliation: `apply_input_with_transition(... Remov |
| R278 | Realm store paths can alias requested realm identity before typed validation | `STALE_FIXED` | Medium |  | duplicate placeholder |
| R279 | Per-request system prompt override collapses set/disable prompt policy | `STALE_FIXED` | Medium |  | See above. |
| R280 | Live interrupt intent is rederived from admission projection after handling-mode normalization | `NARROWED_PARTIAL` | Medium |  | Cited site IS fixed: commit 49ef58e0e replaced the admitted-input projection scan with the machine-owned typed fact — meerkat-rpc/src/session_runtime.rs:3977-3979 now reads `input.live_interrupt_required && contributing_ |
| R281 | Model catalog public contracts advertise stale inference/catalog semantics | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: rewrite the two stale comments to project the owner's semantics — surface.rs:293-296 should mirror the already-fixed wire doc (config-backed ModelRegistry, merges compiled-in catalog |
| R282 | Comms classified drain failures become empty-inbox lifecycle truth | `STALE_FIXED` | Medium |  | The live drain loop in meerkat-runtime/src/comms_drain.rs no longer collapses the fallible drain. Lines 110-126: `let candidates = match comms_runtime.drain_cla |
| R283 | Comms pairing enrolls trust through raw JSON password protocol | `NARROWED_PARTIAL` | Medium |  | FIXED core (not circular): the pairing caller is parsed once at ingress, fail-closed — meerkat-comms/src/runtime/comms_runtime.rs:167-170 `enum PairingIdentityKind { #[serde(rename = "ed25519_public_key")] Ed25519PublicK |
| R284 | Runtime keep_alive override collapses false/preserve into true-only persistence policy | `NARROWED_PARTIAL` | Medium |  | Minimal root fix for the residual (not the claimed fix): feed the inbound tri-state to the machine — carry the operator's raw request (Enable/Disable/Preserve) alongside or instead of the policy-prese |
| R285 | Mob resume readiness timeouts report successful resume | `STALE_FIXED` | Medium |  | In meerkat-mob/src/runtime/actor.rs, ensure_autonomous_runtimes_from_roster() now treats both elapsed-timeout branches as typed terminal faults, NOT warn-and-co |
| R286 | RPC turn/interrupt bypasses generated interrupt public-result authority | `NARROWED_PARTIAL` | Medium |  | The bypass half of the claim is fixed: SessionRuntime::interrupt (meerkat-rpc/src/session_runtime.rs:6291-6401) routes every observation through the generated authority — e.g. 6305-6310 `meerkat_runtime::resolve_user_int |
| R287 | MCP OAuth token lifecycle bypasses realm/AuthMachine authority | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: mirror `publish_login_tokens_via_lease` on the refresh branch — after `exchange_refresh_token`, run the AuthMachine transition at the refreshed token's expiry on the already-reset si |
| R288 | Mob declarative member reconciliation is shell-owned | `STALE_FIXED` | Medium |  | See above. |
| R289 | Python mob-attributed event parser fabricates attribution truth | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: mirror the TS RPC SDK in Python — in `_parse_attributed_mob_event` (sdks/python/meerkat/client.py:2528), parse `source` via `_require_dict` then require non-empty `identity` and non- |
| R290 | Handoff feedback bindings are name/existence-only | `OVERFLAGGED_BY_DESIGN` | Medium |  | Validator layer is indeed name/existence-only: meerkat-machine-schema/src/composition.rs:1759-1766 checks obligation fields via `effect_variant_schema.field_named(field.as_str())` and :1817-1827 checks binding targets vi |
| R291 | Live open failure cleanup is best-effort after generated admission | `STALE_FIXED` | Medium |  | meerkat-rpc/src/handlers/live.rs:731-836 (commit 0fdbc45fd "dogma(WAVE C2)", ancestor of HEAD): close_live_channel_after_open_failure is fail-closed — comment line 737 "#355: open-failure cleanup is fail-closed, not best |
| R292 | Wire auth binding refs erase synthetic-origin identity | `OVERFLAGGED_BY_DESIGN` | Medium |  | The auditor's literal claim is true. `WireAuthBindingRef` omits origin (meerkat-contracts/src/wire/connection.rs:24-29: only `realm`, `binding`, `profile`), and |
| R293 | Skill function dispatch is string/JSON-owned | `NARROWED_PARTIAL` | Medium |  | functions.rs:17-24 declares `SkillInvokeFunctionArgs { ..., function_name: String, #[serde(default)] arguments: Value }`; lines 36-45 parse source_uuid/skill_name fail-closed via `SourceUuid::parse`/`SkillName::parse` bu |
| R294 | Deferred tool catalog owner witnesses are string-derived | `NARROWED_PARTIAL` | Medium |  | Minimal root fix: change `DeferredEligible { stable_owner_key: String }` to carry typed `ToolProvenance`, and drop `stable_owner_key` from `ToolVisibilityWitness`, making `last_seen_provenance` the so |
| R295 | Mobpack signing guide omits required signer identity | `CONFIRMED_ACTIVE` | Low |  | Minimal root fix: update docs/guides/mobpack.mdx:43 to add `--signer-id <identity>` (e.g. `--signer-id release@example.com`), matching docs/cli/commands.mdx:315 and example 028. Doc-only change; nothi |
| R296 | MCP wait-for readiness can time out into first-turn success | `CONFIRMED_ACTIVE` | Medium |  | meerkat/src/factory.rs:476-479 documents the contract: "When true, the surface should block after MCP tool loading until all servers finish connecting before starting the first agent turn." docs/reference/capability-matr |
| R297 | Retired hook patch delivery remains live compatibility surface | `NARROWED_PARTIAL` | Low |  | All cited machinery is still present. meerkat-core/src/hooks.rs:199-209: "Retired hook patch surface. ... this enum intentionally has no variants, so legacy patch payloads fail deserialization instead of being silently i |
| R298 | SDK event subscription close is client-local before stream-close authority | `CONFIRMED_ACTIVE` | Medium |  | TS sdks/typescript/src/subscription.ts:38-43: `async close() { if (this.closed) return; this.closed = true; this.queue.put(null); await this.closeRemote(this.streamId); }` — `isClosed` (line 30) flips true and the iterat |
| R299 | SDK JSON-RPC error parsers recover typed errors from message strings | `CONFIRMED_ACTIVE` | Medium |  | Minimal root fix: delete the message-string JSON parse in both SDKs (client.ts:3118-3132, streaming.py:150-158) and rely solely on the typed error.data projection {code,message,details} the server alr |
| R300 | Realtime guide treats WebSocket listener as live-surface gate | `NARROWED_PARTIAL` | Low |  | Minimal root fix: reword the warning in docs/guides/realtime.mdx:142 to mirror the router's owner truth (router.rs:1913-1915): live/* methods register when at least one live transport is configured — |
| R301 | Architecture doctrine advertises stale seven-machine catalog | `CONFIRMED_ACTIVE` | Low |  | Minimal root fix, following the precedent already in bcdbeb192: (a) collapse the hand-maintained machine lists in docs/reference/architecture.mdx:107-115 and the two skill files into pointers at the g |
| R302 | Composite dispatcher masks tool-name collisions | `CONFIRMED_ACTIVE` | Medium |  | meerkat-tools/src/builtin/composite.rs:465 "// Note: duplicates are silently skipped; builtins take precedence" — the live silent-mask. The winner decision is re-derived independently in four sites and they DISAGREE: too |
| R303 | Durable memory text corruption is lossy-decoded as content | `CONFIRMED_ACTIVE` | Low |  | Minimal root fix: replace the three `String::from_utf8_lossy(&bytes)` calls with `String::from_utf8(bytes)` mapped to a typed MemoryStoreError variant (a new e.g. `TextCorruption` with stable error_co |
| R304 | External MCP tool content is silently narrowed before ToolResult | `STALE_FIXED` | Medium |  | All three legs of the claim are fixed at HEAD by commit 42fdc134d ("fix(rest,mcp): propagate terminal faults instead of laundering into fabricated success", verified ancestor of HEAD), and the sibling sweep found no surv |
| R305 | Architecture doctrine assigns runtime semantics to surfaces | `NARROWED_PARTIAL` | Low |  | All four flagged doctrine lines verbatim name surfaces as owners of runtime semantics: docs/reference/session-contracts.mdx:13 "- **runtime-backed surfaces** own `keep_alive`, Queue/Steer routing, comms drain lifecycle,  |
| R306 | Embedded platform help advertises nonexistent live host type | `CONFIRMED_ACTIVE` | Low |  | Minimal fix: change `LiveHost` to `LiveAdapterHost` at meerkat/src/help_content/platform_skill.md:140. Root fix for the shape: eliminate or gate the handmirror — either restore single ownership by hav |
| R307 | MCP contributed tool lifecycles default outside feature-owned descriptors | `CONFIRMED_ACTIVE` | Medium |  | meerkat-mcp-server/src/lib.rs:1719-1721 "Default lifecycle for any MCP tool whose name is not declared in the typed [`base_tool_descriptors`] table (e.g. schedule/workgraph/mob/comms tools). const MCP_TOOL_DEFAULT_LIFECY |

---

# CAMPAIGN CLOSE-OUT (final, HEAD `ed3b51157`)

**The open set is ZERO.** Final disposition of all 307 baseline rows:

| Final verdict | Count |
| --- | ---: |
| `FIXED_THIS_PR` (independently validated at landed HEADs) | **147** |
| `STALE_FIXED` (already fixed before this PR's waves) | 123 |
| `OVERFLAGGED_BY_DESIGN` (named functional justification) | 37 |
| **OPEN** | **0** |

Trajectory: 307 codex rows → 183 default-active → 147 corrected-criterion genuine → elimination waves 1–3 (143 keystone-fixed across 6 lanes ×2 + residual round) → **wave 4 eliminated all 7 former deferrals** (no valid reason not to: K8a full tool-identity domain fold — one `ToolName` type machine-to-shell, TLC-verified; K8b `OpTerminalPayload` = the domain type, shell JSON codecs deleted; K21 inline-object promotion + fail-closed parser emission, workgraph baseline emptied; typed `EffectTeardownClass` with coherence rule; 93-element ownership-ledger migration with **zero exceptions**; RMAT token scans → syn-AST; typed coverage claims with byte-identical seeding).

**Also closed in this PR:** the pre-existing #34 respawn-after-retire defect (two-layer root cause: empty reaction prompt → provider 400 → silent live-session discard; respawn hard-fail fan-out over machine-owned restore edges — fixed at both roots, live-proven); a genuine browser-trust regression (Strict + hardcoded-empty signers = structurally dead verification — fixed with host-suppliable typed `MobpackTrustConfig`, fixture signed under strict); 4 stale live-lane scenario pins updated to landed contracts.

**Final verification:** workspace 8511/8511 (`--all-features`), clippy `-D warnings` clean, all machine/schema/parity/poster/alignment/governance gates green (several now structurally stronger: seam-inventory hard-fail with teardown coherence, ownership-ledger strict with empty baseline, coverage claims fail-closed), three SDK suites green, and the **full live e2e-smoke lane 41/41 — zero skips, zero failures**.

**Tracked follow-ups (not dogma-ledger rows):** #36 BuildBuddy Bazel external-fetch corruption (infra; cargo path used for the smoke lane); #37 post-discard member revival seam (design follow-up surfaced by the #34 fix).
