# Evidence group H

[Audit index](../AUDIT.md)

<a id="h01"></a>

### H01: The standalone lockfile prevents every locked build, test, and Docker build

**Severity:** high. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --manifest-path examples/035-mdm-tux-rs/Cargo.toml exited 101 before compiling: 'cannot update the lock file ... because --locked was passed'. The corresponding check --locked --offline --all-targets also exited 101. No lock was changed.

- [`examples/035-mdm-tux-rs/Cargo.lock:2129-2130`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/Cargo.lock#L2129-L2130) The facade and every other Meerkat package in this lockfile are version 0.8.24; the complete TOML was parsed (592 packages).
- [`Cargo.toml:56`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Cargo.toml#L56) Current workspace/package version is 0.8.40, inherited by the path dependencies.
- [`examples/035-mdm-tux-rs/Dockerfile:10-14`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/Dockerfile#L10-L14) Both Docker build branches explicitly use --locked against this stale standalone lockfile.

**Independent challenge:** Independently reproduced before compilation. The standalone workspace lock records the Meerkat path packages as 0.8.24 while the actual path packages inherit workspace 0.8.40. The locked offline all-target check exits 101 with 'cannot update the lock file ... because --locked was passed'. Both Docker build branches use that lock with --locked. This establishes locked resolution failure, not any further compiler errors or a claim that an unlocked build necessarily fails.

**Accepted correction:** Regenerate this standalone lock through the repository wrapper during the implementation phase, and include this standalone manifest in the existing release/version freshness and locked example validation mechanism. Do not hand-edit versions or change the workspace version to match the stale lock.

- [`examples/035-mdm-tux-rs/Cargo.lock:2129-2130`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/Cargo.lock#L2129-L2130) The meerkat package is locked at 0.8.24. Independent tomllib enumeration finds only 0.8.24 among source-less meerkat-prefixed packages.
- [`Cargo.toml:56`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Cargo.toml#L56) The workspace package version is 0.8.40.
- [`examples/035-mdm-tux-rs/Dockerfile:10-14`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/Dockerfile#L10-L14) Debug and release cargo build invocations both specify --locked.

**Implementation (fixed):** Standalone lock resolves current 0.8.40 path dependencies. Example Make targets use the wrapper, --locked, and its selected output directory. Parent-coordinated example-locks.py now validates standalone locks from verify-lock-consistency.sh and refreshes/stages them from release-hook.sh. H verified the real offline Cargo contract tests; H did not edit root lockfiles.
Changed: `examples/035-mdm-tux-rs/Cargo.lock`, `examples/035-mdm-tux-rs/Makefile`, `examples/035-mdm-tux-rs/README.md`, `scripts/example-locks.py`, `scripts/test_example_locks.py`, `scripts/verify-lock-consistency.sh`, `scripts/release-hook.sh`.
- `TMPDIR="$PWD/examples/035-mdm-tux-rs/.audit-tmp" CARGO_BUILD_JOBS=2 python3 scripts/test_example_locks.py`: **pass** 2 contract tests pass: release-version drift fails the read-only gate, refresh changes/stages only the standalone lock, missing locks fail without being created, and the release hook refresh precedes the sentinel.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo check --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --all-targets`: **pass** Current all-target check passes; every Meerkat lock row is 0.8.40.
- `make -C examples/035-mdm-tux-rs -n all`: **pass** Dry-run confirms wrapper-selected output paths. This was not codesigning or a Docker build.

**Independent fix review (fixed):** Standalone lock matches current path dependencies; local Make lanes are locked. TOML-discovered standalone locks now join the existing structural/locked-resolution gate and release refresh/stage hook. No second handwritten example inventory or silent check-time healing.
- [`scripts/example-locks.py:15-59`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/scripts/example-locks.py#L15-L59) Discovers actual standalone manifests; reuses structural validator; check requires existing lock and metadata --locked; refresh stages only each exact example lock.
- [`scripts/verify-lock-consistency.sh:105-109`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/scripts/verify-lock-consistency.sh#L105-L109) Existing gate propagates standalone failure.
- [`scripts/release-hook.sh:116-117`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/scripts/release-hook.sh#L116-L117) Refresh/stage before successful release sentinel.
- [`scripts/test_example_locks.py`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/scripts/test_example_locks.py) Real offline Cargo fixtures cover version drift, missing/stale lock, no healing, exact staging, spaces and non-standalone exclusion.
- `python3 scripts/test_example_locks.py with reviewer-owned project TMPDIR`: **pass** Independent2/2.
- `CARGO_NET_OFFLINE=true RUST_LANE_ID=review-h-lock-gate make verify-lock-consistency`: **pass** Independent root+034+035 resolution; all root/Bazel/example lock bytes unchanged. No actual release.

<a id="h02"></a>

### H02: Every TUX prompt sends a rejected turn/start parameter

**Severity:** high. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Precise static call chain: Enter -> RpcCommand::StartTurn -> request('turn/start', {session_id,prompt,handling_mode}) -> handle_start -> deny_unknown_fields parsing rejects handling_mode. audit-H-local-probes.py independently prints Unknown TUX fields: ['handling_mode']. This is a source-contract proof, not a compiled serde execution.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:493-520`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L493-L520) StartTurn unconditionally includes handling_mode in the turn/start parameter object, for both direct and Hive prompts.
- [`meerkat-rpc/src/handlers/turn.rs:31-84`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/handlers/turn.rs#L31-L84) StartTurnParams is serde(deny_unknown_fields) and has no handling_mode field.
- [`meerkat-rpc/src/handlers/turn.rs:155-177`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/handlers/turn.rs#L155-L177) The live handler parses StartTurnParams before admitting a turn.

**Independent challenge:** Both direct and Hive input paths construct RpcCommand::StartTurn; its only request constructor unconditionally adds handling_mode. The current turn/start handler deserializes into deny_unknown_fields StartTurnParams, which has no such field. Parsing precedes admission, so ordinary TUX prompts are rejected even when the selected session is otherwise valid. No unspecified generic runtime-input RPC is assumed as the replacement.

**Accepted correction:** Remove the invalid field from ordinary turn/start requests and use the current RPC contract. Either implement the mode UX using an actually supported and tested owner API or disable/remove the unsupported mode choices and update help/README. Do not silently discard the mode while continuing to promise steering, and do not widen the upstream RPC parser merely to tolerate the example.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:494-520`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L494-L520) Every StartTurn JSON object contains handling_mode and is submitted to turn/start.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs:2332-2369`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2332-L2369) Normal direct and Hive prompt submission both use this command.
- [`meerkat-rpc/src/handlers/turn.rs:39-84,155-177`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/handlers/turn.rs) The actual handler parses the closed StartTurnParams schema before start_turn_with_params; handling_mode is absent.
- [`examples/035-mdm-tux-rs/README.md:300-301`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md#L300-L301) The advertised /steer and /queue behaviors depend on the invalid field.

**Implementation (fixed):** TUX ordinary turn/start uses turn_params without handling_mode. /steer and /queue reject locally with accurate explanations. Real active-turn admission is exercised rather than implementing a competing TUI queue.
Changed: `examples/035-mdm-tux-rs/src/rpc_client.rs`, `examples/035-mdm-tux-rs/src/bin/tux.rs`, `examples/035-mdm-tux-rs/tests/runtime.rs`, `examples/035-mdm-tux-rs/README.md`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** Full coordinated run: 80/80. real_rpc_turn_start_uses_owner_admission_when_a_turn_is_active holds a synthetic stream, sends another actual RPC request, verifies bounded Busy refusal from no-turn materialization, then verifies both prompts in history. Unsupported modes emit no RPC command.

**Independent fix review (fixed):** Actual turn/start requests omit handling_mode. Unsupported steer/queue commands reject locally rather than promising ignored behavior; active-turn handling remains the runtime owner's contract.
- [`examples/035-mdm-tux-rs/src/rpc_client.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/rpc_client.rs) Ordinary request has session_id and prompt.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/tux.rs) Symbols: `RpcCommand::StartTurn`, `handle_slash_command`. Actual caller uses corrected shape; mode commands send no RPC.
- `Full88-test package; real_rpc_turn_start_uses_owner_admission_when_a_turn_is_active; unsupported_reset_and_modes_never_send_rpc_commands`: **pass** Parent raw output reviewed; ordinary and provider-gated active turns use the real RPC server.

<a id="h03"></a>

### H03: Kennel target boot creates its session in one runtime and asks another runtime to serve it

**Severity:** high. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Static current-implementation proof: a fresh session exists only in surface.runtime_adapter/service and its first in-memory store. The RPC runtime's second store is empty, with no staged/live session or registration. enable_autonomous_comms_drain therefore returns 'session not found', and run_kennel_mode exits before target registration. Runtime reproduction is blocked by H01.

- [`examples/035-mdm-tux-rs/src/bin/target.rs:536-552`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/target.rs#L536-L552) build_target_runtime_surface allocates its own InMemoryRuntimeStore and MeerkatMachine.
- [`examples/035-mdm-tux-rs/src/bin/target.rs:1602-1630`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/target.rs#L1602-L1630) The managed session and generated comms-trust router are created on that first surface.
- [`examples/035-mdm-tux-rs/src/bin/target.rs:1637-1713`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/target.rs#L1637-L1713) A second independent PersistenceBundle/InMemoryRuntimeStore/SessionRuntime is then constructed; enable_autonomous_comms_drain is invoked on it with the first surface's session ID, and the error is propagated from startup.
- [`meerkat-rpc/src/session_runtime.rs:6392-6402,6835-6879`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/session_runtime.rs) enable_autonomous_comms_drain calls ensure_runtime_executor. A session absent from that runtime's registration, staged sessions, live service, and persisted authoritative state returns SESSION_NOT_FOUND.
- [`meerkat/src/session_runtime/recovery.rs:125-139`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/recovery.rs#L125-L139) The RPC persisted loader reads service.load_authoritative_session, not arbitrary JSONL files shared by directory path.

**Independent challenge:** The two objects are genuinely independent authorities, not two service shells sharing the same backend. PersistenceBundle::new_with_subsystem_stores creates a fresh MeerkatMachine around exactly the supplied store. Target setup materializes the managed session on the first surface, then constructs a second InMemoryRuntimeStore and SessionRuntime. The second runtime has no staged session, live session, registration, or committed snapshot for the first session ID. Its enable_autonomous_comms_drain call reaches the explicit SESSION_NOT_FOUND branch and the caller propagates the error before kennel registration. This conclusion is conditional on the preceding setup succeeding; it does not claim the blocked full binary was run.

**Accepted correction:** Use one managed runtime/service and matching persistence bundle for target boot, RPC turns, scheduled delivery and generated comms trust. Materialize the target's session through that owner while preserving the explicit model/provider, system prompt and MCP dispatcher. Do not copy snapshots between owners or bypass the missing-session guard.

- [`examples/035-mdm-tux-rs/src/bin/target.rs:537-551,1605-1628,1652-1673,1705-1712`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/target.rs) First store/service owns create_or_resume_session and the trust router; the RPC service receives a second newly allocated memory store and then the first service's session ID.
- [`meerkat/src/persistence.rs:237-275`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/persistence.rs#L237-L275) The bundle keeps the injected store and creates its own persistent MeerkatMachine; a shared JSONL directory does not coalesce these owners.
- [`meerkat-rpc/src/session_runtime.rs:6392-6402,6835-6879`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/session_runtime.rs) Comms drain first ensures the executor; all four session-existence sources are checked and absence returns SESSION_NOT_FOUND.
- [`meerkat/src/session_runtime/recovery.rs:125-139`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/recovery.rs#L125-L139) RPC recovery loads the service's authoritative session, not arbitrary JSONL component rows.

**Implementation (fixed):** One ManagedRpcHost now supplies paired durable stores and the same SessionRuntime/service for RPC, comms/trust, schedules and MCP. The three REQUIRED architectural defects were corrected through parent-coordinated canonical recovery lowering, explicit SurfaceSessionRecoveryOverrides, exact prepared materialization/compensation and a per-ID registration fence acquired before observation. No manual metadata restore, snapshot-copy workaround, unrelated RPC method or live-channel opening was introduced. H adapted callers to the four-argument seam, preserving durable configuration while explicitly reauthorizing managed tool policy. The feature-owned exact scheduler catalog fixes nested contract erasure; six real detached-shell configurations pass, with production fleet features enabled. Model-facing catalog search/load verifies deferred mob/MCP availability. Live peer observation now uses the actual RPC session/history surface, rather than calling the cold recovery loader against an active actor; all original assertions remain, without retries swallowing errors. Three full runtime-suite repetitions and the final full 80-test suite pass. Historical active cold-loader invariant failures remain recorded as a separate limitation, not claimed repaired. A new root seam is justified because ordinary create_session only stages volatile state, RpcMobSessionService captures the mob-owned witness map rather than the session-owned publication slot, and start_turn_via_runtime executes work. The required root cancellation, failed-create/cold-resume preservation and same-ID winner tests were independently executed by H from the accepted existing binary: 11/11 pass.
Changed: `examples/035-mdm-tux-rs/src/runtime.rs`, `examples/035-mdm-tux-rs/src/lib.rs`, `examples/035-mdm-tux-rs/src/bin/target.rs`, `examples/035-mdm-tux-rs/src/bin/kennel.rs`, `examples/035-mdm-tux-rs/src/test_support.rs`, `examples/035-mdm-tux-rs/tests/runtime.rs`, `examples/035-mdm-tux-rs/tests/fixture_mcp.py`, `examples/035-mdm-tux-rs/tests/nested_tool_catalog.rs`, `examples/035-mdm-tux-rs/README.md`, `meerkat-rpc/src/session_runtime.rs`, `meerkat/src/session_runtime/recovery.rs`, `meerkat/src/surface/runtime_backed.rs`, `meerkat-schedule/src/tools.rs`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** FINAL current-source full run: 80/80, 0 ignored. .audit-final-required-test.log includes exact nondefault config plus committed-turn/rewrite-history reopen, capacity-one cleanup/retry, real RPC/streams/peer/schedule/restart, all six durable shell topologies and catalog preservation. Strict check and Clippy also pass in .audit-final-required-check.log and .audit-final-required-clippy.log.
- `cd examples/035-mdm-tux-rs && export TMPDIR="$PWD/.audit-tmp" RUST_MIN_STACK=8388608; binary='$HOME/Library/Caches/rust-workspaces/meerkat-7f595016ca/targets/v4/1.94.1-aarch64-apple-darwin-e408947bfd/examples-rpc-seam/debug/deps/meerkat_rpc-0d417844e7577294'; "$binary" no_turn_ --nocapture && "$binary" recovered_create_request_failure --nocapture && "$binary" realtime_materialization_create_failure --nocapture`: **pass** H directly executed the exact existing binary recorded in review-H-composition.json with the repository's unchanged .cargo/config.toml stack setting and project-local TMPDIR: 8 + 2 + 1 PASS. Deterministic winner/loser barrier proves registration, MCP and actor survive and a normal turn runs after winner publication. Fresh/cold failure and cancellation preserve capacity/predecessor. Log .audit-required-root-regressions.log; no duplicate root build was launched.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --test runtime cold_no_turn_resume_preserves_durable_build_state_without_resupply -- --exact cold_no_turn_resume_preserves_durable_build_state_without_resupply --nocapture`: **pass** Strengthened exact-history regression PASS: seven nondefault persisted config fields, real structured-output turn, real same-ID transcript rewrite, complete close/reopen, exact current-message equality, exact revision-list equality and exact retained-parent-revision equality. No persisted state or generated phase is manually altered. Log .audit-exact-resume-history.log.
- `for run in 1 2 3; do RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --test runtime --no-fail-fast || break; done`: **pass** FINAL public-RPC observation workload: all three runs PASS 8/8, including stronger history recovery. .audit-public-read-runtime-repeat.log. No error suppression, ignored case or success-shaped fallback.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** 80/80 pass in .audit-coordinated-full-test.log, including all six detached-shell topologies, nested typed catalog preservation, and eight real runtime tests. Detached controls inspect succeeded durable jobs and blob-backed stdout/exit codes. MCP/mob presence is verified after real tool_catalog_search/load calls. Scoped root architecture is accepted in review-H-composition.json: its final adjudication retires all three original findings and records independent 8+2+1 executable regressions. H independently compared all four final root source hashes and they match current files. This does not certify the separately reproduced peer-read invariant. The exploratory schema-after-turn negative control was rejected by the canonical owner itself; no bypass finding is claimed from it. The retained admission test checks actual provider-without-model rejection.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --test runtime no_turn_ -- --nocapture`: **fail** PRE-FIX proof: fresh failure left an anonymous Idle reservation and capacity-one retry returned Max sessions reached (1/1); cold resume replaced seven populated SessionBuildState fields with {}. Both assertions now pass in the full coordinated run. Historical logs .audit-seam-review-proof.log and .audit-seam-review-repros.log are retained.
- `for run in 1 2 3; do RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --test runtime --no-fail-fast || break; done`: **fail** Repeat 1: 8/8 pass. Repeat 2: 7/8, then stopped; managed_peer_ingress_and_schedule_use_the_rpc_session_owner fails at load_persisted_session with 'rewrite rejected: previous transcript revision head-v5-sha256:... did not match commit parent head-v5-sha256:...'. See .audit-runtime-repeat.log. The test now adds read-only runtime-state/active-input-count diagnostics on error, but no retry or success fallback.
- `for run in 1 2 3; do RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --test runtime --no-fail-fast || break; done`: **fail** Instrumented repeat independently reproduces the failure again on repeat 2 after repeat 1 passes. Immediately after the failed snapshot read, owner runtime_state = Ok(Running), active input count = Ok(0). See .audit-runtime-read-state.log; no error is suppressed.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --test runtime managed_peer_ingress_and_schedule_use_the_rpc_session_owner -- --exact managed_peer_ingress_and_schedule_use_the_rpc_session_owner --nocapture`: **pass** Isolated case passed once (2.83s). This did not resolve or erase the subsequently reproduced failure under the runtime suite.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 make -C examples mdm`: **pass** Parent final current-tree full suite:88Rusttests passed unignored/unfiltered plus2offline lock contracts; final-mdm-ownership-tests.log. Includes bounded shutdown during unanswered stream_open, reversed binding publication, all-fleet durable jobs, and real TCP registration-switch matrix plus original teardown tests.

**Independent fix review (fixed):** One ManagedRpcHost owns boot, RPC, comms/trust and schedules. The new session-owned no-turn seam now uses canonical recovery, exact prepared materialization and captured actor authority rather than a second runtime, snapshot import or mob-owned substitute. All three introduced seam findings are retired.
- [`examples/035-mdm-tux-rs/src/runtime.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/runtime.rs) Symbols: `ManagedRpcHost::open`, `managed_session`. One PersistenceBundle/SessionRuntime; generated trust and schedule delivery share its adapter/session.
- [`meerkat-rpc/src/session_runtime.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/meerkat-rpc/src/session_runtime.rs) Explicit recovery overrides, no staged registry, per-ID fence and exact compensation/publication.
- [`meerkat/src/session_runtime/recovery.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/meerkat/src/session_runtime/recovery.rs) Existing canonical lowering accepts the already-owned bindings; resource reinjection is separate from durable options.
- `Existing root binary: no_turn_, recovered_create_request_failure, realtime_materialization_create_failure`: **pass** Independent11/11; actual failure/cancellation/race/default-client/Busy and compatibility assertions.
- `Full03588/88 and public-RPC runtime repetitions3x8/8`: **pass** Actual target/Hive processes, MCP, signed peer ingress, schedule delivery and all-fleet detached jobs. Parent/owner raw output reviewed.

<a id="h04"></a>

### H04: Disk-backed session promises are implemented with ephemeral authoritative stores

**Severity:** high. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Static backend proof: authoritative session bodies/catalog/receipts and blobs are in process-local stores. Reconstructing the same bundle after process exit starts empty irrespective of the JSONL directory. target create_or_resume_session also lists JSONL component rows rather than the authoritative catalog. No process-restart execution was claimed because H01 blocks binaries.

- [`examples/035-mdm-tux-rs/src/bin/target.rs:540-545,746-751,1654-1659`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/target.rs) Every target PersistenceBundle uses InMemoryRuntimeStore and MemoryBlobStore despite a JSONL directory and persistent schedule database.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:185-195,255-273`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) The hive likewise uses an in-memory authoritative runtime store, then attempts to resume existing sessions after restart.
- [`meerkat-session/src/persistent.rs:4149-4175,8284-8391`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-session/src/persistent.rs) WholeBlob authoritative loads and committed snapshot writes use RuntimeStore; the SessionStore is not a second authoritative copy of the body.
- [`meerkat-runtime/src/store/memory.rs:73-101`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/store/memory.rs#L73-L101) Committed snapshots, authorities, and session catalog are stored in in-memory maps.
- [`examples/035-mdm-tux-rs/README.md:51-63,90-103`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md) The target is described as persisting sessions to disk; direct-mode instructions require a session saved by an earlier kennel run.

**Independent challenge:** The durability failure is proven for these exact mixed bundles, not inferred from the word ephemeral or from the service class name. The supplied InMemoryRuntimeStore explicitly uses WholeBlobV1 and holds session bodies, physical authorities and the listing catalog in process-local maps. The facade preserves it unchanged. PersistentSessionService's authoritative load and list use that store, and its snapshot commit writes to that store; there is no JSONL body fallback in this read path. Thus reconstructing the example's bundle cannot recover the committed sessions merely because the JSONL directory survived. MemoryBlobStore likewise cannot persist blobs. Identity files and SQLite schedule rows can survive; the finding does not mean all example state is ephemeral or that in-memory composition is intrinsically wrong.

**Accepted correction:** Use the existing coherent durable composition/provider for the example's explicit data directory, including matching session/runtime/blob stores, and perform discovery/resume through the authoritative service. Preserve optional in-memory APIs elsewhere; do not add a shadow JSONL save/import shortcut. Coordinate this with H03, which is a distinct same-process ownership bug.

- [`examples/035-mdm-tux-rs/src/bin/target.rs:540-545,746-751,961-985,1654-1659`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/target.rs) Both target modes supply memory runtime/blob stores; managed resume discovery separately lists raw JSONL rows rather than the authoritative service catalog.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:184-194,249-268`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Hive uses the same mixed composition while explicitly attempting restart resume.
- [`meerkat-runtime/src/store/memory.rs:73-99,1626-1631`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/store/memory.rs) The concrete backend stores bodies/authorities/catalog in maps and declares WholeBlobV1.
- [`meerkat-session/src/persistent.rs:3293-3300,4159-4175,8326-8359,11581-11590,12640-12648`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-session/src/persistent.rs) Authoritative bodies and catalog are loaded from RuntimeStore; snapshot commit is also RuntimeStore-owned. Store-only component rows are not promoted to session truth.
- [`examples/035-mdm-tux-rs/README.md:51-62,85-103`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md) Disk persistence and cross-process direct-mode resume are user-facing promises.

**Implementation (fixed):** Physical persistence uses the disk provider's matching SQLite session/runtime/schedule/job stores and durable blob store. Managed identity is discovered through canonical Session build-state app_context, not JSONL or an ID sidecar. Full shutdown/reopen and separate processes preserve identity, transcript, media, exact nondefault configuration and actual rewrite history; a persisted schedule fires after reopen. Historical misuse of the cold recovery loader for active observation is retained separately; current observation exercises RPC session/history without suppressing failures.
Changed: `examples/035-mdm-tux-rs/src/runtime.rs`, `examples/035-mdm-tux-rs/src/bin/target.rs`, `examples/035-mdm-tux-rs/src/bin/kennel.rs`, `examples/035-mdm-tux-rs/tests/runtime.rs`, `examples/035-mdm-tux-rs/tests/kennel_protocol.rs`, `examples/035-mdm-tux-rs/README.md`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** 80/80. managed_runtime_rpc_stream_and_restart_share_durable_authority verifies stable ID, prior transcript, blob bytes, real MCP/catalog access, and a persisted schedule after full reopen. Actual target/broker process restarts preserve their sessions. cold_no_turn_resume_preserves_durable_build_state_without_resupply verifies hooks, schema, budgets, instructions, shell_env, timeout and app_context without masking loss by resupply.

**Independent fix review (fixed):** Explicit Sqlite provider creates paired durable session/runtime authority and durable blob/schedule/job stores. Memory-pinned roots fail closed. Discovery/resume use canonical service content, not JSONL projections. Full reopen preserves identity, media, nondefault configuration and rewrite history.
- [`examples/035-mdm-tux-rs/src/runtime.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/runtime.rs) Explicit provider open and canonical managed identity discovery.
- [`meerkat/src/persistence.rs:1098-1123,1278-1300`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/meerkat/src/persistence.rs) Durability validation and matching SQLite pairing.
- [`examples/035-mdm-tux-rs/tests/runtime.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/tests/runtime.rs) Real structured turn, same-ID rewrite, full shutdown/drop/reopen; exact build-state/messages/revision-list/retained-parent equality.
- `Rich cold-rewrite preservation, process restart and persisted blob/schedule tests in current full package`: **pass** No retained Arc masquerading as restart. Final full88 and runtime repetitions pass.

<a id="h05"></a>

### H05: TUX does not own a scoped stream subscription for its bound session

**Severity:** medium. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Static event-order proof: KennelTargetDiscovered -> SessionBound changes Hive to Idle -> TargetConnected sees a non-disconnected target with Some(session_id), so never sends ResumeLatestOrCreate and never opens its stream. Independently, issuing /resume twice opens two server forwarders whose notifications are both accepted into the same target timeline; switching to a different session leaves the old stream accepted too.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:1385-1415,1875-1893,1917-1932`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs) Hive discovery pre-binds a session, which sets phase Idle; the later TargetConnected event neither clears it nor opens a stream. StreamEvent is routed solely by target_id, ignoring session_id and stream_id.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs:590-607,750-770,850-873`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs) Resume opens streams but discards each returned stream_id; no session/stream_close exists in the file. Hive /new binds another session without opening its stream.
- [`meerkat-rpc/src/router.rs:4244-4342`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/router.rs#L4244-L4342) Each stream_open mints a distinct stream and forwarder, and emits notifications carrying that stream/session identity.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:206-213`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs#L206-L213) The host runtime uses NotificationSink::noop; TUX cannot rely on a global unscoped agent-event sink instead of its explicit scoped stream.

**Independent challenge:** Initial Hive binding sets the view to Idle before the RPC-connected event; therefore that event neither clears the supplied session nor enters the only auto-stream-opening branch. /new likewise only emits SessionBound. Explicit resumes open additional independently minted streams but discard their IDs, and all stream events are accepted by target_id alone. I checked the apparent counter-path: turn/start's connection-local event channel forwards MCP lifecycle events, not execution events; the host runtime was built with NotificationSink::noop. It does not rescue normal Hive execution output. Multiple subscriptions and stale-session events remain independently real even when a stream has been opened manually.

**Accepted correction:** Give each RPC connection/bound session one explicit stream handle; subscribe initial Hive and each replacement session, close the previous stream, and filter by the current connection/session/stream identity. Handle session/event lifecycle notifications deliberately rather than treating them as arbitrary current-session execution events. If H07 disables Hive /new, no unsupported reset feature need be built merely for this finding.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:1387-1411,1875-1893,1917-1932`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs) Hive prebinding precedes Connect; SessionBound marks Idle; TargetConnected requests a stream only via the missing-session fallback, while event routing ignores session/stream identity.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs:592-607,750-767,856-869`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs) Resume paths discard stream_open results; Hive /new opens no stream. A whole-file search finds no session/stream_close.
- [`meerkat-rpc/src/router.rs:4244-4295,4331-4335`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/router.rs) Each open mints a distinct StreamRef, inserts a forwarder, and emits session- and stream-attributed events.
- [`meerkat-rpc/src/handlers/turn.rs:206-215`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/handlers/turn.rs#L206-L215) The per-request channel is explicitly MCP-lifecycle-only; execution events use the runtime executor's own channel.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:204-210`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs#L204-L210) That host runtime has a noop notification sink.

**Implementation (fixed):** Final review's late SessionBound race is fixed, not merely payload-filtered. RpcClient reserves a monotonic connection-owned selection token at command admission, before spawned binding or discovery I/O. StreamBinding carries a read-only witness checked again when the UI consumes it; older results/errors/events cannot replace newer selections. Superseded opened streams close before the next binding; stream-end invalidates delayed bound results. Connection identities fence obsolete lifecycle events, and connection-attempt generations reject late connect completions. Initial Hive subscription, scoped stream closure and reconnect behavior remain supported. Final follow-up: per-selection/connection revisions fence delayed publication; transport shutdown cancels pending requests before the stream lock, and pending insertion rechecks liveness under its lock. The new real-socket blocked-bind case proves bounded shutdown without a 30-second wait.
Changed: `examples/035-mdm-tux-rs/src/rpc_client.rs`, `examples/035-mdm-tux-rs/src/bin/tux.rs`, `examples/035-mdm-tux-rs/tests/runtime.rs`, `examples/035-mdm-tux-rs/README.md`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** Latest full suite:85/85, zero ignored. Actual TCP tests cover overlapping bind responses and exact stale-stream close ordering, reversed SessionBound publication, late stream end, replaced connection events, delayed latest discovery versus newer explicit resume, and reversed connect completion. H05-race-full-test.log; exact rebuilt binaries/source fingerprints in fix-H05-publication-race.json.
- `for run in 1 2 3; do RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --lib --bin mdm-tux || exit; done`: **pass** All three runs pass 33 library +17 TUX tests, including H11 epoch-projection countercases. H05-race-repeat.log. Current all-target check and strict Clippy pass in H05-final-check.log and H05-race-clippy.log.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** Real TCP stream tests cover initial binding, repeated same-session binding, another session, close/reconnect, and obsolete identities. initial_hive_binding_subscribes_and_obsolete_streams_cannot_paint exercises the actual TUI event reducer.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 make -C examples mdm`: **pass** Parent final current-tree full suite:88Rusttests passed unignored/unfiltered plus2offline lock contracts; final-mdm-ownership-tests.log. Includes bounded shutdown during unanswered stream_open, reversed binding publication, all-fleet durable jobs, and real TCP registration-switch matrix plus original teardown tests.

**Independent fix review (fixed):** Initial Hive subscribes. Admission-issued connection/selection witnesses fence binding completion and UI consumption; superseded opened streams are closed, stale lifecycle/error/data/end events cannot replace the current binding, and late connect attempts are rejected. Shutdown now cancels response waiters before taking the bind-held mutex; request admission rechecks disconnected under the pending lock.
- [`examples/035-mdm-tux-rs/src/rpc_client.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/rpc_client.rs) Symbols: `begin_binding`, `bind_selected`, `BindingSelection::is_current`, `shutdown`, `request`. Monotonic exact selection, cleanup of superseded streams, prompt disconnection and no post-shutdown pending admission.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/tux.rs) Symbols: `RpcTargetConnection`, `rpc_command_loop`, `process_event`. Attempt generations and consumption-time live witnesses; prompt submission also requires current binding.
- `Current compiled lib/TUX exact cases`: **pass** Independent six cases: unanswered-open shutdown under2s, overlapping binds with exact close order, reversed bound/end, stale connection, delayed latest versus explicit selection, obsolete connect completion. Full88 also passes; review-H-final-ownership-execution.log.

<a id="h06"></a>

### H06: The documented kennel-target /new command depends on a mob absent from the default topology

**Severity:** medium. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Static default configuration trace: documented kennel command/Compose -> no experimental flag -> no hive-fleet -> TUX /new sends mob/respawn for a nonexistent mob. This is separate from the explicitly documented unsupported fresh direct mode.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:788-818,2403-2424`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs) For a kennel target, /new always requests mob/respawn for mob_id hive-fleet and the target name.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:99,342-414,569-572`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) hive-fleet and external target membership exist only with --experimental-hive-mob; default startup sets hive_mob_id to None.
- [`examples/035-mdm-tux-rs/docker-compose.tux.yml:19-32`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/docker-compose.tux.yml#L19-L32) The default Compose kennel invocation does not opt into the experimental mob.
- [`examples/035-mdm-tux-rs/README.md:295`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md#L295) The slash-command table offers kennel target /new, only labeling direct target /new unsupported.

**Independent challenge:** Default kennel composition never creates hive-fleet: the entire mob creation and external membership path is gated by --experimental-hive-mob. The ordinary kennel-target /new path nevertheless always calls mob/respawn on hive-fleet, and the default Compose invocation does not enable it. The README accurately names the requested RPC operation but omits the essential default-topology limitation. This does not prove the experimental respawn implementation is otherwise functional.

**Accepted correction:** Either provide a tested managed reset through the default topology's owner, or locally disable/gate kennel-target /new with an accurate explanation and aligned help/docs. Do not enable an experimental mob globally merely to conceal the unsupported operation.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:788-815,2403-2424`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs) Kennel /new selects the Hive client and unconditionally submits mob/respawn for hive-fleet.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:98,342-405,572-588`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Mob existence and target membership are opt-in.
- [`examples/035-mdm-tux-rs/docker-compose.tux.yml:19-32`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/docker-compose.tux.yml#L19-L32) Default kennel command has no experimental-mob flag.
- [`examples/035-mdm-tux-rs/README.md:295`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md#L295) Only the direct-target variant is marked unsupported.

**Implementation (fixed):** Target /new is explicitly disabled locally: the default topology has no owner-coordinated reset transaction. No mob/respawn command or automatic experimental-mob enablement is used.
Changed: `examples/035-mdm-tux-rs/src/bin/tux.rs`, `examples/035-mdm-tux-rs/README.md`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --bin mdm-tux`: **pass** 13/13 pass, also included in the 80-test full run. unsupported_reset_and_modes_never_send_rpc_commands verifies accurate notices and an empty outgoing command channel.

**Independent fix review (fixed):** Target /new is disabled locally for all modes; no nonexistent default hive-fleet respawn request or implicit experimental enablement.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/tux.rs) Unsupported notice only; MobRespawn request path removed and README/help aligned.
- `unsupported_reset_and_modes_never_send_rpc_commands in final full suite`: **pass** No outgoing reset RPC in Direct/Hive modes.

<a id="h07"></a>

### H07: Hive /new leaves the kennel pointing at the archived hive session

**Severity:** medium. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Static lifecycle proof: successful Hive /new archives/unregisters S1 and binds S2 only in that TUX instance. Kennel still advertises S1 to later controllers and its trust router still addresses S1. A subsequent target registration calls direct_peer_endpoints(S1) and fails; the replacement has no kennel-owned drain/rebinding transaction.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:729-770`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L729-L770) Hive /new archives the kennel-created session and creates a replacement directly over generic RPC.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:319-338,434-435,537-544,677-681`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) The hive_session_id and ExampleGeneratedCommsTrustRouter are fixed once at kennel startup, cloned into connections, and reused for every new TuxRegistered reply and target registration.
- [`examples/035-mdm-tux-rs/src/generated_comms_trust.rs:12-16,88-108`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/generated_comms_trust.rs) The trust router retains the original SessionId and reads generated endpoints through it before adding peers.
- [`meerkat/src/session_runtime/runtime_state.rs:217-229`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/runtime_state.rs#L217-L229) Successful archive cleanup unregisters the old runtime session and aborts its comms drain.
- [`meerkat-runtime/src/meerkat_machine/comms_drain.rs:2391-2400`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/comms_drain.rs#L2391-L2400) Reading direct peer endpoints for an unregistered session fails with SessionNotRegistered.

**Independent challenge:** A successful generic archive/create sequence changes only TUX's current session. Kennel's advertised Hive session and generated trust router retain the startup SessionId for the process lifetime. Archive cleanup unregisters that old runtime session, and direct_peer_endpoints requires registration. Therefore later target registration fails through the stale router and later controllers are told the archived ID. A failed /new need not reach this state; the defect concerns the successful archive/reset path.

**Accepted correction:** Disable generic /new for the kennel-managed Hive unless a kennel-owned reset transaction exists. If implementing reset, publish the replacement identity only after its drain/trust/session ownership is ready, reconcile existing peers, and update subsequent registration replies through the same owner. Merely changing the TUI's ID is not a fix.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:730-767`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L730-L767) Archives current Hive through generic RPC, creates a deferred session, then rebinds only local TUI state.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:318-338,434-435,537-544,675-681`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Startup session ID and trust router are cloned unchanged into connections and reused for registration and TuxRegistered replies.
- [`examples/035-mdm-tux-rs/src/generated_comms_trust.rs:12-16,84-108`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/generated_comms_trust.rs) Trust mutations first read endpoints under the retained session ID.
- [`meerkat/src/session_runtime/runtime_state.rs:217-229`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/runtime_state.rs#L217-L229) Successful archive cleanup unregisters the session and tears down runtime resources.
- [`meerkat-runtime/src/meerkat_machine/comms_drain.rs:2391-2398`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/comms_drain.rs#L2391-L2398) Endpoint access for the unregistered old session returns SessionNotRegistered.

**Implementation (fixed):** Hive /new is disabled before archive/create. The UI cannot invent a replacement identity while the kennel retains an older session/trust/drain owner. Help and README match the adjudicated honest gate.
Changed: `examples/035-mdm-tux-rs/src/bin/tux.rs`, `examples/035-mdm-tux-rs/README.md`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --bin mdm-tux`: **pass** 13/13. Actual slash-command handling rejects Hive reset without any outgoing archive/create/reset command.

**Independent fix review (fixed):** Unsafe generic Hive archive/create reset is removed. /new cannot invalidate the kennel-owned session, advertised identity or generated trust/drain owner.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/tux.rs) No session/archive or session/create request remains in reset handling; documented owner-coordinated reset is unsupported.
- `unsupported_reset_and_modes_never_send_rpc_commands`: **pass** Checks actual command channel remains empty, not just help text.

<a id="h08"></a>

### H08: Available-list refresh silently claims every target and undoes explicit release

**Severity:** medium. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Static exact event sequence: initial Available list adds a Disconnected TargetView; second list automatically claims it without /claim. Likewise /release -> ClaimReleased -> Disconnected -> next available list -> UserClaimTarget -> SendClaimToKennel.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:2011-2050`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2011-L2050) An existing target with phase Disconnected triggers UserClaimTarget whenever it appears in the available list; there is no check that it was previously claimed or that release was intentional.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs:2108-2132`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2108-L2132) ClaimReleased marks the target Available and Disconnected, exactly satisfying the automatic reclaim branch on the next refresh.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs:1422-1448`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L1422-L1448) Available and mine lists are refreshed every five seconds.
- [`examples/035-mdm-tux-rs/src/machines/tux_claim.rs:117-132,262-281`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/tux_claim.rs) ClaimReleased returns to Available; Available + ClaimRequested immediately emits SendClaimToKennel.

**Independent challenge:** The automatic claim branch is driven solely by the view's RPC phase, not claim intent. A newly discovered target starts Disconnected; the second Available response for the same target issues UserClaimTarget without user action. Explicit ClaimReleased also sets Disconnected after the claim owner returns to Available, so a later Available response issues a new claim. This is not merely a harmless retry for a formerly owned target. 'Every target' is naturally limited to listed targets having an RPC address and an admissible local claim state.

**Accepted correction:** Do not translate mere discovery or RPC disconnection into claim intent. Remove automatic claiming of fresh/released targets; if transient reclaim is retained, drive it from explicit still-retained user ownership intent and the claim owner, not TargetPhase.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:84-93,1420-1437,2011-2050,2108-2126`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs) New views start Disconnected, list refresh is periodic, and existing disconnected views generate claims. Release puts a view back into that same condition.
- [`examples/035-mdm-tux-rs/src/machines/tux_claim.rs:117-131,267-280`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/tux_claim.rs) ClaimReleased restores Available; ClaimRequested from Available emits SendClaimToKennel.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs:1492-1511`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L1492-L1511) The network receive path applies ClaimReleased to the claim owner before notifying the TUI.

**Implementation (fixed):** Discovery and RPC disconnection no longer imply claim intent. ClaimTargets originates from explicit user intent; release remains released through later snapshots. Mine-based reconnect requires the currently owned matching lease.
Changed: `examples/035-mdm-tux-rs/src/bin/tux.rs`, `examples/035-mdm-tux-rs/src/machines/tux_kennel_control.rs`, `examples/035-mdm-tux-rs/README.md`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --bin mdm-tux`: **pass** discovery_and_release_do_not_send_claim_requests tests repeated discovery, explicit claim/grant/ack/release, and later snapshots against real reducers and command channels. Delayed UI projection tests prevent resurrection.

**Independent fix review (fixed):** Discovery and RPC disconnection no longer imply claim intent. Explicit release stays released; list-driven reconnection requires current owner-confirmed matching lease.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/tux.rs) Available only updates views; Mine/grant paths consult current claims; no automatic UserClaimTarget from disconnected views.
- `discovery_and_release_do_not_send_claim_requests; delayed projection controls`: **pass** Repeated discovery, explicit claim/release and later snapshots; old projections cannot create new ownership.

<a id="h09"></a>

### H09: An acknowledged lease never expires while its control connection remains open

**Severity:** medium. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** audit-H-claim-repros.rs includes the unmodified kennel_lease module. repro_claim_never_expires_when_control_remains_connected starts Claimed(expires_at_ms=1000), applies Tick(now_ms=1000000), and observes the same expired Claimed state with no effects. The symptom assertion passed.

- [`examples/035-mdm-tux-rs/src/machines/kennel_lease.rs:563-579`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/kennel_lease.rs#L563-L579) Claimed + Tick ignores now_ms and unconditionally retains the expired Claimed state.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:1055-1060,1407-1430`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Claims have explicit TTL-derived expires_at_ms and the janitor sends Tick once per second, so expiry information is available but unused in the active state.
- [`examples/035-mdm-tux-rs/README.md:64-65`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md#L64-L65) Kennel is described as managing claim/lease lifecycle including TTL.

**Independent challenge:** Independent execution of the original lease transition confirms that Claimed ignores Tick before, at, and far after its expiry. TTL does constrain RecoveringClaim, which is useful counterevidence: the defect is expiration while the acknowledged claim remains in Claimed, not every lease expiry path. With both sockets retained and no renewal, the janitor cannot free the reservation. Claims are coordination leases rather than authorization, so no security boundary is asserted.

**Accepted correction:** Add the expired-Claimed transition to the existing lease owner with correctly typed release reason/effects, leaving a connected target registered and available. Fence renewal/ack processing against already-terminal or expired leases using the same owner/clock facts; do not implement expiry by deleting records opportunistically in the shell.

- [`examples/035-mdm-tux-rs/src/machines/kennel_lease.rs:436-464,563-579,690-739`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/kennel_lease.rs) Renewal changes expires_at_ms, but Claimed Tick discards now_ms. Recovery Tick does enforce its deadline.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:1058-1060,1122-1154,1411-1425`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) TTL is returned as concrete expiry, renewals update it, and the broker ticks each target once per second.
- [`examples/035-mdm-tux-rs/README.md:64-79`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md#L64-L79) TTL lifecycle is promised; these are expressly coordination, not authorization, leases.

**Implementation (fixed):** The existing lease owner expires Claimed exactly at TTL, emits typed LeaseExpired notifications once, removes the lease and retains a connected target. Ack/renewal validate time; pre-expiry renewal extends TTL and late messages cannot revive it.
Changed: `examples/035-mdm-tux-rs/src/machines/kennel_lease.rs`, `examples/035-mdm-tux-rs/src/machines/kennel_target_control.rs`, `examples/035-mdm-tux-rs/src/bin/kennel.rs`, `examples/035-mdm-tux-rs/tests/kennel_protocol.rs`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** Boundary/renewal/late-ack/one-notification assertions pass in connected_claim_expires_and_late_ack_or_renew_cannot_revive_it. Real signed-socket expiry and retry pass with the TCP connection still alive.

**Independent fix review (fixed):** Claimed TTL expiration is owned by the existing lease transition, including connected sockets. Exact boundary expiration releases once; late ACK/renew cannot resurrect ownership; valid early renewal extends the deadline.
- [`examples/035-mdm-tux-rs/src/machines/kennel_lease.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/machines/kennel_lease.rs) Clock-aware Claimed Tick, ACK/renew guards and typed LeaseExpired effects.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/kennel.rs) Actual handlers/janitor feed clock facts through the owner.
- `connected_claim_expires_and_late_ack_or_renew_cannot_revive_it; connected_expiry_and_correlated_refusal_allow_retry_on_same_connection`: **pass** Boundary unit checks plus real signed TCP control connection retained across expiry.

<a id="h10"></a>

### H10: A reconnected claimed target is later deleted as if it were still disconnected

**Severity:** high. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Executed repro_reregistered_live_target_is_dropped_after_recovery in audit-H-claim-repros.rs: Claimed -> TargetDisconnected(100) -> Registered(None,200) produces outer connected=true but nested target_connected=false; Tick(1001) emits DropTargetRecord. The symptom assertion passed.

- [`examples/035-mdm-tux-rs/src/machines/kennel_lease.rs:538-561,690-739`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/kennel_lease.rs) TargetDisconnected sets the recovering lease's target_connected=false; its expiry emits DropTargetRecord whenever that nested flag stays false.
- [`examples/035-mdm-tux-rs/src/machines/kennel_target_control.rs:133-160`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/kennel_target_control.rs#L133-L160) Registered with attached_tux_id=None only flips the outer connected flag; it does not update the nested recovery state.
- [`examples/035-mdm-tux-rs/src/machines/target_kennel_session.rs:22-25,88-104`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/target_kennel_session.rs) Ordinary target startup/registration carries no attached controller hint. The RPC claim path does not establish the legacy attachment protocol.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:1395-1398`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs#L1395-L1398) DropTargetRecord unconditionally removes the now-live registration; no re-register request is sent to the connected target.

**Independent challenge:** The pure composition reproduces the alleged stale evidence: TargetDisconnected sets nested target_connected=false; Registered(None) sets only outer connected=true; the old recovery deadline then emits DropTargetRecord for that live target. I also ran the countercase with the correct attached_tux_id: it does not drop the live target. The accepted scope is the ordinary RPC-only no-attachment registration path, not all re-registration. In that path the target's session machine starts without an attachment hint and the simplified RPC claim ack does not install one.

**Accepted correction:** Deliver successful connection/re-registration evidence to the lease owner even without the legacy attachment hint. Preserve the distinction between a live target and proven continuing claim ownership; it is sufficient for an expired old claim to become Available without deleting the live target. Do not blindly revive an old lease or infer ownership from connection presence.

- [`examples/035-mdm-tux-rs/src/machines/kennel_target_control.rs:133-159`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/kennel_target_control.rs#L133-L159) The None registration arm leaves the nested lease entirely unchanged.
- [`examples/035-mdm-tux-rs/src/machines/kennel_lease.rs:538-561,590-634,690-739`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/kennel_lease.rs) Disconnect records false; only attachment-specific registration repairs it; expiration drops a target when that nested flag remains false.
- [`examples/035-mdm-tux-rs/src/machines/target_kennel_session.rs:18-20,71-76,87-102`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/target_kennel_session.rs) Normal initial registration has None and acknowledgments clear the hint.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:923-950,1395-1398`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Re-registration already replaces the sender with the live connection, yet DropTargetRecord unconditionally removes the same record.

**Implementation (fixed):** No-hint successful registration supplies TargetConnected evidence to the lease owner. It does not renew ownership, but old claim expiry makes the reconnected target Available instead of dropping it.
Changed: `examples/035-mdm-tux-rs/src/machines/kennel_target_control.rs`, `examples/035-mdm-tux-rs/src/machines/kennel_lease.rs`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** rpc_only_reregistration_preserves_connected_target_after_claim_expiry passes. recovery_expiry_drops_disconnected_target remains the passing genuinely disconnected control.

**Independent fix review (fixed):** No-hint registration now supplies TargetConnected to the lease owner without inventing continuing ownership. Old recovery expiry keeps a genuinely connected target registered and Available; actual disconnection still drops it.
- [`examples/035-mdm-tux-rs/src/machines/kennel_target_control.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/machines/kennel_target_control.rs) Connection evidence reaches nested lease for None attachment as well as attachment path.
- `rpc_only_reregistration_preserves_connected_target_after_claim_expiry; recovery_expiry_drops_disconnected_target`: **pass** Positive reconnect path and genuine-disconnection control retained.

<a id="h11"></a>

### H11: TUX cannot reconcile a refused claim or a broker restart

**Severity:** medium. **Verdict:** partial. **Examples:** 035.

**Original proof:** Two executed harness tests: repro_denied_claim_has_no_retry_path applies requested -> empty grant -> empty mine -> available list, then confirms state is still ClaimRequested and retry is rejected. repro_old_claim_survives_kennel_restart_without_reclaim applies disconnected/connected -> empty mine -> available to a Claimed entry, and confirms its obsolete lease remains and reclaim is rejected. Both symptom assertions passed.

- [`examples/035-mdm-tux-rs/src/machines/tux_kennel_control.rs:132-187,214-241`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/tux_kennel_control.rs) An empty ClaimGranted changes nothing; available-list reconciliation ignores any non-Available claim, and an empty mine list never invalidates requested/claimed entries.
- [`examples/035-mdm-tux-rs/src/machines/tux_claim.rs:186-230,282-305,343-360`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/tux_claim.rs) There is no refused-claim/authoritative-absence transition; claimed entries deliberately survive KennelDisconnected, while another ClaimRequested from ClaimRequested or Claimed is rejected.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:109-116,661-681,1063-1081`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Broker state is recreated empty on restart; TuxRegister ignores attached_target_ids. A conflicting/unavailable target is omitted from ClaimGranted rather than returning a refusal that clears the request.

**Independent challenge:** Two narrower failures are real: on one healthy broker connection a refused claim receives an empty grant and remains ClaimRequested even after Mine is empty and the target is positively Available; and after an actual in-memory broker restart a previously Claimed client retains an obsolete lease with no reconciliation route. However, 'cannot recover without restarting TUX' is too broad: original ClaimRequested + KennelDisconnected returns Available, so reconnect alone clears an ungranted request. More importantly, ordinary empty Mine snapshots are deliberately non-authoritative for downgrading live/pending claims, enforced by the existing mine_list_omission_does_not_downgrade_live_claims test. A blanket empty-list reset would regress that contract.

**Accepted correction:** Add correlated refusal/claim-request completion so a losing request can retry on the same connection. Add an explicit trustworthy reconciliation boundary for genuinely lost broker leases after restart, such as broker incarnation plus authoritative lease reconciliation or equivalently ordered/correlated positive state evidence. Preserve live claims through transport-only loss and uncorrelated/stale/empty Mine responses. Do not make empty snapshots a universal release signal.

- [`examples/035-mdm-tux-rs/src/machines/tux_kennel_control.rs:132-188,214-237,308-336`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/tux_kennel_control.rs) Empty grants are no-ops; available reconciliation ignores non-Available claims; empty Mine is intentionally tested not to downgrade Claimed or ClaimRequested.
- [`examples/035-mdm-tux-rs/src/machines/tux_claim.rs:221-233,282-298,354-365`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/tux_claim.rs) Disconnect clears ClaimRequested, countering the broad permanence claim, but preserves Claimed and rejects a second ClaimRequested from it.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:115,661-681,1063-1081`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Broker restart reconstructs empty state, registration ignores attached_target_ids, and failed claim attempts are omitted from the returned grants.

**Implementation (fixed):** Correlated refusal/retry and restart reconciliation remain, without treating transport loss or empty Mine as universal release evidence. Final-review projection gaps are now closed by a typed BrokerEpoch (authenticated incarnation plus local connection identity) on EVERY post-registration UI event: lists, grants, releases, reconciliation, Hive discovery/session advertisement and legacy Hive output/errors. The UI holds the claim owner's read guard from epoch validation through projection and RPC-command enqueue. Old epochs cannot alter state or reconnect RPC. Grant discovery is emitted only from a currently owned matching grant; same-epoch stale release cannot disconnect a newer lease. Same-incarnation reconnect changes projection identity only, preserving live claims.
Changed: `examples/035-mdm-tux-rs/src/kennel.rs`, `examples/035-mdm-tux-rs/src/bin/kennel.rs`, `examples/035-mdm-tux-rs/src/bin/tux.rs`, `examples/035-mdm-tux-rs/src/machines/tux_claim.rs`, `examples/035-mdm-tux-rs/src/machines/tux_kennel_control.rs`, `examples/035-mdm-tux-rs/tests/kennel_protocol.rs`, `examples/035-mdm-tux-rs/README.md`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** LATEST full suite:88/88, zero ignored. every_old_broker_projection_is_rejected_after_restart_or_reconnect exercises ten projection kinds across both restart and same-incarnation reconnect, asserting exact unchanged UI/owner snapshots and zero RPC commands. Same-epoch release/newer-claim, empty-Mine and positive grant controls pass. H05 race and coordinated shutdown tests also pass. Logs H11-epoch-full-test.log; fresh binaries/source hashes in fix-H11-epoch-projections.json.
- `for run in 1 2 3; do RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --lib --bin mdm-tux || break; done`: **pass** Three consecutive runs each pass34 library +19 TUX tests. All-target check and strict Clippy pass. H11-epoch-{check,clippy,repeat}.log.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** Fresh-grant test explicitly leaves the emitted ACK undelivered before an empty Mine; the claim survives. Refusal/retry, reconnect/no-downgrade, stale-lease and delayed-UI tests pass. A real broker process restart changes incarnation but retains the durable Hive ID.

**Independent fix review (fixed):** Correlated refusal completes losing requests; signed broker incarnation distinguishes restart from transport loss; empty Mine does not revoke live/pending claims. Owner mutations occur in socket order. The final epoch superset additionally rejects all old connection projections at UI consumption under a stable owner read guard; same-incarnation reconnect preserves claims and stale same-epoch releases cannot erase newer leases.
- [`examples/035-mdm-tux-rs/src/machines/tux_kennel_control.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/machines/tux_kennel_control.rs) Symbols: `BrokerEpoch`, `BrokerRegistered`. Only broker incarnation change resets claim state; connection UUID changes projection freshness, not canonical lease ownership.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs:2076-2085`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/tux.rs#L2076-L2085) Projection is admitted against current broker epoch while holding owner read guard; projection helper does not mutate owner.
- `Real refusal/restart protocol tests; existing empty-Mine controls`: **pass** Final full88.
- `every_old_broker_projection_is_rejected_after_restart_or_reconnect; same_epoch_release_cannot_disconnect_a_newer_claim_and_empty_mine_preserves_it`: **pass** Independent current-binary execution; all post-registration UI variants fenced for restart and same-incarnation reconnect, current-epoch controls retained.

<a id="h12"></a>

### H12: Definitively removed targets remain in the hive and peer comms catalogs

**Severity:** medium. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Static normal lifecycle: register available target -> hive add_trusted_peer + mesh PeerWire -> target disconnect -> DropTargetRecord. The only realization removes the target map entry, leaving both trust catalogs unchanged. No hypothetical malicious participant is needed.

- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:542-544,614-649`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Registration adds the target to hive generated trust and broadcasts PeerWire to both sides of every target pair.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:826-870,1395-1398,1432-1448`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Disconnect/removal retires an optional experimental mob member and removes records, but never removes generated hive trust or emits PeerUnwire. There is no PeerUnwire construction anywhere in this binary.
- [`examples/035-mdm-tux-rs/src/bin/target.rs:2051-2055`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/target.rs#L2051-L2055) Targets already support PeerUnwire, but the broker never sends it.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:280-291`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs#L280-L291) The hive prompt tells the agent to use peers as the source of truth for connected/available targets.

**Independent challenge:** Definitive removal changes only the broker's target map. Registration adds the Hive peer through generated authority and sends mesh PeerWire, but neither DropTargetRecord nor janitor removal performs the inverse. The binary contains no PeerUnwire, remove_trusted_peer or replace_named_trusted_peer call. Optional mob retirement does not repair the default non-mob topology. The positive proof is stale trust/discovery entries; exact behavior of a later same-name/different-key enrollment may depend on duplicate-name checks, so no particular coexistence result is required.

**Accepted correction:** At definitive target retirement/removal, remove the exact identity through the Hive generated trust owner and send PeerUnwire to surviving targets. Preserve explicitly recoverable transient cases and fence stale teardown against re-enrollment. Do not mutate TrustStore directly or equate socket loss with permanent removal in every state.

- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:542-544,613-647,850-858,1395-1398,1428-1441`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Adds both Hive and mesh peers, but permanent removal only deletes target records; experimental retirement is separate.
- [`examples/035-mdm-tux-rs/src/machines/kennel_lease.rs:220-226`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/machines/kennel_lease.rs#L220-L226) Available target disconnection is an immediate definitive DropTargetRecord path, not a retained recovery lease.
- [`examples/035-mdm-tux-rs/src/generated_comms_trust.rs:33-48,73-81,122-140`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/generated_comms_trust.rs) The existing adapter supports generated-owner peer removal, but registration's add operation only replaces matching identity, not all stale names.
- [`examples/035-mdm-tux-rs/src/bin/target.rs:2051-2055`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/target.rs#L2051-L2055) The receiving side already lowers PeerUnwire into generated trust removal.

**Implementation (fixed):** Definitive removal records a retryable retirement obligation, removes the exact peer through generated Hive trust, and sends PeerUnwire to survivors. Serialized registration/reconciliation and identity checks protect re-enrollment; transient recovery remains distinct.
Changed: `examples/035-mdm-tux-rs/src/bin/kennel.rs`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** Real handle_connection regression checks both generated endpoints and Hive trust projection, survivor PeerUnwire, obsolete-socket protection and a retired name re-enrolled with a fresh identity. Transient lease recovery controls also pass.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 make -C examples mdm`: **pass** Current final-mdm-ownership-tests.log88Rusttests+2locktests passed. The original exact survivor PeerUnwire assertion is unchanged and passes. Initial parent test extension injected extra unrelated teardown notices before that assertion; registration-switch cases now run afterward as an independent phase, preserving both original assertions and new negative controls. Independent review-H marks H12fixed on the same current handler.

**Independent fix review (fixed):** Definitive retirement removes exact generated Hive trust and sends PeerUnwire while preserving recoverable transient cases and re-enrollment fencing. The later unapproved false-premise retirement experiment was removed by parent, restoring the exact accepted file; fresh full88 verification passes. Its snapshot/failing log remain historical, not current code or a hidden ignored test.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/kennel.rs) Symbols: `dispatch_effects`, `reconcile_retired_targets`, `run_janitor`. Owner DropTargetRecord feeds serialized exact identity cleanup rather than raw TrustStore mutation.
- `teardown_is_unconditional_incarnation_fenced_and_unwires_retired_peers`: **pass** Independent actual TCP handler test retains original malformed/signature/replacement/RST/EOF, generated endpoint, TrustStore and survivor PeerUnwire assertions. Current source restored byte-identically.
- `Fresh parent restored-source full package`: **pass** final-mdm-restored.log88/88, including both approved kennel tests. Withdrawn invalid fixture preserved separately, not ignored in shipping tests.

<a id="h13"></a>

### H13: Control read/verification failures bypass connection cleanup

**Severity:** medium. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Precise static failure trace: register -> receive a transport reset, malformed JSON line, or invalid signature -> ? returns from handle_connection -> JoinHandle is dropped without abort, record disconnect transition is skipped, and the retained sender/receiver keep writer and registration alive. EOF follows a different, correctly cleaned-up path.

- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:484-500`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs#L484-L500) The writer task is spawned, then read_envelope and verify_envelope use ? inside the connection loop.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:826-856`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs#L826-L856) writer_task.abort and the machine-owned target/TUX disconnect handling are only reached after a normal loop break.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:1432-1448`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs#L1432-L1448) Janitor phantom cleanup depends on a closed target sender. A detached writer blocked on rx.recv keeps that receiver open while the record itself retains a sender.

**Independent challenge:** The spawned writer waits on a channel whose sender is retained by the registration. An error from read_envelope, verify_envelope, or other ? inside the loop returns before writer_task.abort and the disconnect transition. Dropping a Tokio JoinHandle detaches, not cancels, that task. A bounded malformed frame after successful registration is enough: the reader exits while the retained record keeps the receiver alive, so the janitor's is_closed condition is false. Clean EOF does execute cleanup and is not the failing case. Existing registration replacement also lacks an exact-connection teardown fence.

**Accepted correction:** Centralize unconditional teardown after a result-returning read/dispatch loop or use an equivalent owned guard. Preserve the original error, stop/join the writer, and apply the appropriate disconnect only if that exact connection still owns the record. Keep cleanup outside held locks when it awaits.

- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:481-500,826-848`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Writer spawn precedes fallible reads; cleanup is reachable only after normal loop termination.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:930-946,1428-1441`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/kennel.rs) Same-ID replacement overwrites tx; teardown is keyed only by participant ID; janitor tests sender closedness.

**Implementation (fixed):** Result-returning read/dispatch flows into unconditional teardown. Writer is aborted/joined, disconnect applies only to the exact owning channel, awaited cleanup occurs outside state locks, and the original error survives. Final follow-up: before any registration mutation, an already-bound socket must retain both participant kind and identity. Same-kind/same-identity repeat remains valid. Four real TCP identity/kind switch refusals and two valid re-registration/EOF controls prove no registry or trust orphan.
Changed: `examples/035-mdm-tux-rs/src/bin/kennel.rs`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --no-fail-fast`: **pass** Actual handlers terminate on obsolete-socket malformed JSON, current-socket signature failure, a real owned-client SO_LINGER reset, and clean EOF. Replacement registration is not disconnected by its predecessor.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 make -C examples mdm`: **pass** Parent final current-tree full suite:88Rusttests passed unignored/unfiltered plus2offline lock contracts; final-mdm-ownership-tests.log. Includes bounded shutdown during unanswered stream_open, reversed binding publication, all-fleet durable jobs, and real TCP registration-switch matrix plus original teardown tests.

**Independent fix review (fixed):** Reader/verification/dispatch errors always abort/join the writer and tear down only the exact owning connection. Registration is now pinned to its first participant kind+identity before any mutation; same-kind/same-ID repetition remains allowed. The independently reproduced multi-registration orphan is corrected.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs:466-480`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/kennel.rs#L466-L480) Cross-kind/identity re-registration rejected before broker/trust mutation; original session_kind retained for normal cleanup.
- [`examples/035-mdm-tux-rs/src/bin/kennel.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/kennel.rs) Unconditional writer join and same_channel ownership check preserve replacement connections.
- `teardown_is_unconditional_incarnation_fenced_and_unwires_retired_peers`: **pass** Independent rebuilt binary execution includes six real TCP registration cases: four identity/role switches rejected with records/trust absent, two idempotent-repeat controls accepted then cleaned on EOF. Matrix runs after original fixture teardown so original assertions are unchanged. Historical failing Node probe retained.

<a id="h14"></a>

### H14: Docker smoke error reporting loses its Python dictionary-key quotes

**Severity:** low. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** shellcheck reports SC2026 at line 102. Executed audit-H-local-probes.py extracts the exact function and uses a synthetic loopback RPC server: a successful session/list prints audit-session and exits 0; an error response exits 1 with NameError: name 'error' is not defined, rather than the RPC error message.

- [`examples/035-mdm-tux-rs/scripts/docker-smoke.sh:77-108`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/scripts/docker-smoke.sh#L77-L108) The Python program is a shell single-quoted string, but line 102 contains message['error']; shell quote processing strips the inner quotes to message[error].

**Independent challenge:** Independently executed the exact helper, without starting the script's Docker path. A synthetic successful session/list response exits zero. A synthetic error response produces NameError for message[error], because Bash removes the dictionary-key single quotes from the single-quoted Python command. bash -n passes, demonstrating that shell syntax validity does not disprove the runtime quoting bug.

**Accepted correction:** Preserve the literal string key through a quoted heredoc or correctly quoted Python formatting; retain the nonzero exit and the actual server error in diagnostics.

- [`examples/035-mdm-tux-rs/scripts/docker-smoke.sh:77-108`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/scripts/docker-smoke.sh#L77-L108) The embedded Python single-quoted shell argument is interrupted by the inner quotes in message['error'] at line 102.

**Implementation (fixed):** Quoted heredoc preserves the literal Python error key, original server diagnostic and nonzero exit.
Changed: `examples/035-mdm-tux-rs/scripts/docker-smoke.sh`, `examples/035-mdm-tux-rs/scripts/test-scripts.py`.
- `python3 examples/035-mdm-tux-rs/scripts/test-scripts.py`: **pass** Actual sourced helper over loopback preserves -32001/synthetic server failure without NameError; success still prints synthetic-session.
- `shellcheck examples/035-mdm-tux-rs/scripts/*.sh`: **pass** Original SC2026 is gone; all scripts pass.

**Independent fix review (fixed):** Quoted Python heredoc preserves the literal error key and actual RPC error diagnostic, retaining nonzero failure status.
- [`examples/035-mdm-tux-rs/scripts/docker-smoke.sh`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/scripts/docker-smoke.sh) No shell quote loss in message['error'].
- `python3 examples/035-mdm-tux-rs/scripts/test-scripts.py`: **pass** Independent actual sourced helper: synthetic success and server error code/text, no NameError.

<a id="h15"></a>

### H15: Architecture restart readiness can pass entirely on pre-restart logs

**Severity:** low. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** audit-H-local-probes.py executes the exact wait_for_log function with a non-mutating docker shell mock returning only '[OLD PROCESS] session ready' and '[OLD PROCESS] added hive as trusted peer'. Both post-restart checks immediately succeed, even with timeout 0.

- [`examples/035-mdm-tux-rs/scripts/tmux-architecture-suite.sh:196-224`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/scripts/tmux-architecture-suite.sh#L196-L224) wait_for_log scans all docker compose logs; restart_target_b restarts the same container then searches for strings already emitted during initial smoke startup, without a time/cursor boundary.

**Independent challenge:** The original wait_for_log helper accepts either readiness string from the complete container log history. restart_target_b restarts the existing container and invokes the same helper with no time/cursor boundary. An independent stale-only mock satisfies both post-restart checks with timeout zero. A missing-pattern control correctly fails, so this is specifically acceptance of the wrong process epoch, not an always-success helper. Later suite checks may still fail; the entire suite is not alleged to pass falsely.

**Accepted correction:** Fence readiness to evidence from the restarted process, using a properly bounded log cursor/start timestamp or a fresh identity/session-aware registration/RPC readiness check. Do not just clear or suppress old logs without an epoch boundary.

- [`examples/035-mdm-tux-rs/scripts/tmux-architecture-suite.sh:190-216`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/scripts/tmux-architecture-suite.sh#L190-L216) logs --no-color reads all logs; restart readiness has no --since, new-process marker or fresh RPC condition.

**Implementation (fixed):** Both restart readiness probes use Docker logs --since the restarted container's inspected StartedAt epoch.
Changed: `examples/035-mdm-tux-rs/scripts/tmux-architecture-suite.sh`, `examples/035-mdm-tux-rs/scripts/test-scripts.py`.
- `python3 examples/035-mdm-tux-rs/scripts/test-scripts.py`: **pass** Actual sourced restart function rejects stale-only evidence, accepts fresh evidence, and waits for delayed fresh readiness. This is fixture coverage, not a real Docker restart.

**Independent fix review (fixed):** Both post-restart readiness checks filter logs by the restarted container's State.StartedAt; historical readiness cannot satisfy the fresh epoch.
- [`examples/035-mdm-tux-rs/scripts/tmux-architecture-suite.sh`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/scripts/tmux-architecture-suite.sh) Symbols: `restart_target_b`, `wait_for_log`. Exact inspect timestamp passed to logs --since.
- `python3 examples/035-mdm-tux-rs/scripts/test-scripts.py`: **pass** Independent stale-only failure, fresh success and delayed-success fixtures. No actual Docker daemon run claimed.

<a id="h16"></a>

### H16: Ctrl+L clears an unused Hive view instead of the displayed timeline

**Severity:** low. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Static actual selection proof: populate app.targets[hive].history, switch Mode::Hive, invoke the Ctrl+L branch; only fallback hive_view is emptied, while render_timeline still selects the unchanged target entry.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:2271-2282`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2271-L2282) In Hive mode Ctrl+L always clears app.hive_view.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs:2725-2732,1928-1932`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs) The normal connected Hive timeline and incoming RPC events use the target entry with target_id=hive; app.hive_view is only a fallback.
- [`examples/035-mdm-tux-rs/README.md:366`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md#L366) Ctrl+L is documented as clearing the timeline.

**Independent challenge:** In Hive mode the renderer and RPC event path select the TargetView identified by target_id=hive. Ctrl+L instead clears only the fallback app.hive_view. The fallback can be cleared correctly when no Hive target exists, but this does not affect the normal connected timeline. This is a concrete wrong-view mutation, not a stylistic request to refactor all selection logic.

**Accepted correction:** Resolve the same active target/fallback for clear as for render, and clear only its history and streaming text while preserving other targets. A small shared selection helper is appropriate but not required.

- [`examples/035-mdm-tux-rs/src/bin/tux.rs:1928-1931,2271-2280,2725-2732`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs) Events populate the real target; rendering prefers that target; keyboard clear touches only the fallback in Hive mode.
- [`examples/035-mdm-tux-rs/README.md:366`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md#L366) Ctrl+L is documented to clear the timeline.

**Implementation (fixed):** Rendering and Ctrl+L share active-view selection. Connected Hive, fallback, and direct target clear history and streaming text without clearing other views.
Changed: `examples/035-mdm-tux-rs/src/bin/tux.rs`.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 ./scripts/repo-cargo test --locked --offline --manifest-path examples/035-mdm-tux-rs/Cargo.toml --bin mdm-tux`: **pass** 13/13 tests pass, including all three active-view selections through the actual key handler; no physical keyboard input.

**Independent fix review (fixed):** Render and Ctrl+L share the same active target/fallback selection. Clear removes displayed history/streaming text only, leaving unrelated targets intact.
- [`examples/035-mdm-tux-rs/src/bin/tux.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/tux.rs) Symbols: `App::active_view`, `App::active_view_mut`, `handle_key`, `render_timeline`. One selection rule for connected Hive, fallback and Direct mode.
- `clear_uses_rendered_hive_and_preserves_other_targets`: **pass** Current full package covers all three selection cases.

<a id="h-w4-01"></a>

### H-W4-01: Non-exact schedule leaf causes nested fleet composition to erase a sibling detached execution contract

**Severity:** high. **Verdict:** confirmed. **Examples:** 035.

**Original proof:** Independently reproduced exit 101 using the already-built test, without compilation. A synthetic exact Fast+Detached sibling plus the real ScheduleToolDispatcher retains its complete contract in the real inner DynamicToolComposite. A real outer DynamicToolComposite changes it to supported_modes={Fast}, detached_policy=None, detached_restart_classes={}. No dispatch is executed. Production managed-shell control fails with mob+schedule enabled; no-mob and no-schedule controls reach durable job output.

- [`meerkat-schedule/src/tools.rs:61-110,114-160,555-627`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/src/tools.rs) schedule_tools_list defines exactly eight fixed names. Constructor builds a private immutable tool_defs array once; tools returns that complete array; dispatch first requires a name in the same array and lowers to the matching eight-name handler. No catalog-capability override is present at baseline.
- [`meerkat-core/src/agent.rs:1214-1235`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent.rs#L1214-L1235) Exact means the precedence-resolved winner registry for the dispatcher's owned plane. Default capabilities are non-exact, but default tool_catalog faithfully maps every tools entry into a callable session-inline catalog entry. For this complete static leaf, that default catalog already satisfies exactness.
- [`meerkat-schedule/src/tools.rs:388-413`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/src/tools.rs#L388-L413) CurrentSessionScheduleToolDispatcher forwards inner capability and clones each full catalog entry before rewriting only its ToolDef. It preserves execution metadata and callability rather than synthesizing replacement contracts.
- [`meerkat/src/factory.rs:6247-6286,6459-6465`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs) The production factory composes schedule around the base dispatcher, then adds another DynamicToolComposite for mob tools. All production fleet features remain enabled.
- [`meerkat-core/src/gateway.rs:151-163,810-822,1000-1025`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/gateway.rs) The inner composite reports exact only when every child does. Its non-exact schedule child makes the combined catalog non-exact. The outer composite's live-catalog adapter then maps tools() to session_inline defaults, erasing the exact sibling's execution metadata.
- [`meerkat-core/src/agent.rs:1317-1340`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent.rs#L1317-L1340) The mandatory final validator checks the resolved execution mode against the outer advertised contract. The correctly resolved Detached plan is rejected against the erased Fast-only contract. This guard is correct and must stay intact.

**Independent challenge:** FIXED AND ACCEPTED. The truthful seven-line feature-owned exact-catalog declaration is unchanged; no generic gateway weakening. Parent owning tests2/2 and strict lint pass, current035 full88 tests include real all-fleet durable shell and nested catalog, and reviewer independently executed both owning tests from the fresh binary. Original failing proof remains below as history.

**Accepted correction:** owner: meerkat-schedule; production_change: Add ScheduleToolDispatcher::tool_catalog_capabilities returning exact_catalog=true and may_require_catalog_control_plane=false for its existing complete static catalog.; scope_limit: No generic gateway/core changes authorized. Do not claim arbitrary non-exact catalogs exact, weaken resolved-plan validation, disable mob/schedule/comms in production, or replace durable shell with process-local background work.; regressions: Schedule-owned leaf contract: declared capability is exact, tools and catalog names/definitions/provenance/callability match, unknown names still fail NotFound, and no control plane is required.; Schedule-owned nested composition with an exact sibling carrying a complete Fast+Detached execution contract: assert equality of the full sibling entry metadata through inner and outer composites, not just the tool name or advertised mode count.; Repeat nested metadata preservation through the real CurrentSessionScheduleToolDispatcher wrapper; existing rewrite and live-exact-callability regressions must remain green.; Actual example035 managed background shell with all production fleet features enabled must reach the durable job terminal result and persisted output; ordinary-create comparison must also pass.; Run the full standalone 035 suite and targeted owning-crate tests/lint, reporting any remaining failures rather than substituting a narrower green selector.

- [`meerkat-schedule/src/tools.rs:61-110,114-160,555-627`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/src/tools.rs) schedule_tools_list defines exactly eight fixed names. Constructor builds a private immutable tool_defs array once; tools returns that complete array; dispatch first requires a name in the same array and lowers to the matching eight-name handler. No catalog-capability override is present at baseline.
- [`meerkat-core/src/agent.rs:1214-1235`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent.rs#L1214-L1235) Exact means the precedence-resolved winner registry for the dispatcher's owned plane. Default capabilities are non-exact, but default tool_catalog faithfully maps every tools entry into a callable session-inline catalog entry. For this complete static leaf, that default catalog already satisfies exactness.
- [`meerkat-schedule/src/tools.rs:388-413`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/src/tools.rs#L388-L413) CurrentSessionScheduleToolDispatcher forwards inner capability and clones each full catalog entry before rewriting only its ToolDef. It preserves execution metadata and callability rather than synthesizing replacement contracts.
- [`meerkat/src/factory.rs:6247-6286,6459-6465`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs) The production factory composes schedule around the base dispatcher, then adds another DynamicToolComposite for mob tools. All production fleet features remain enabled.
- [`meerkat-core/src/gateway.rs:151-163,810-822,1000-1025`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/gateway.rs) The inner composite reports exact only when every child does. Its non-exact schedule child makes the combined catalog non-exact. The outer composite's live-catalog adapter then maps tools() to session_inline defaults, erasing the exact sibling's execution metadata.
- [`meerkat-core/src/agent.rs:1317-1340`](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent.rs#L1317-L1340) The mandatory final validator checks the resolved execution mode against the outer advertised contract. The correctly resolved Detached plan is rejected against the erased Fast-only contract. This guard is correct and must stay intact.

**Implementation (fixed):** The schedule-owned static catalog explicitly declares its existing complete registry exact, with no deferred control-plane requirement. Generic gateway semantics and mandatory plan validation remain unchanged. All-fleet detached shell execution now reaches real durable job terminal/output. Owning tests preserve every sibling catalog field across three nesting levels with both plain and current-session-wrapped schedules.
Changed: `meerkat-schedule/src/tools.rs`, `meerkat-schedule/tests/exact_catalog.rs`, `examples/035-mdm-tux-rs/tests/nested_tool_catalog.rs`, `examples/035-mdm-tux-rs/src/bin/target.rs`, `examples/Makefile`.
- `RUST_LANE_ID=examples-audit-H ./scripts/repo-cargo test --locked -p meerkat-schedule --test exact_catalog`: **pass** 2/2 owning regressions pass: complete/unique/fast-only catalog and unknown-name refusal; exact sibling definition/execution/callability/plane/deferred metadata preserved through three layers, including CurrentSessionSchedule wrapper. final-schedule-catalog-tests.log.
- `RUST_LANE_ID=examples-audit-H ./scripts/repo-cargo clippy --locked -p meerkat-schedule --test exact_catalog --no-deps -- -D warnings`: **pass** final-schedule-catalog-clippy.log.
- `RUST_LANE_ID=examples-audit-H CARGO_BUILD_JOBS=2 make -C examples mdm`: **pass** Parent final current-tree full suite:88Rusttests passed unignored/unfiltered plus2offline lock contracts; final-mdm-ownership-tests.log. Includes bounded shutdown during unanswered stream_open, reversed binding publication, all-fleet durable jobs, and real TCP registration-switch matrix plus original teardown tests.

**Independent fix review (fixed):** The schedule leaf truthfully declares its complete static catalog exact. Nested composition therefore preserves sibling execution/callability/deferred metadata; no generic gateway weakening or disabled production fleet feature. Owning static/plain/wrapped regressions and real all-fleet durable shell execution now pass.
- [`meerkat-schedule/src/tools.rs:145-151`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/meerkat-schedule/src/tools.rs#L145-L151) Only authorized exact_catalog=true/controlplane=false production declaration.
- [`meerkat-schedule/tests/exact_catalog.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/meerkat-schedule/tests/exact_catalog.rs) Complete/unique static catalog, Fast-only leaf, unknown-name NotFound; plain and CurrentSession schedule leaf through three nested levels preserves every ToolCatalogEntry field.
- [`examples/035-mdm-tux-rs/src/bin/target.rs`](https://github.com/lukacf/meerkat/blob/185182c73c554a262ac7f4f32acbc09f2963eb3b/examples/035-mdm-tux-rs/src/bin/target.rs) All fleet features enabled during actual durable shell terminal/blob assertions.
- `Existing exact_catalog-4cef86b3ef6f4652 binary`: **pass** Independent owning2/2; parent owning tests and strict lint also pass.
- `Current full03588-test suite`: **pass** Nested catalog regression and all six detached-shell topology cases pass; no provider calls.
