#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

# Cargo passes the subcommand name through to external cargo-* commands.
if [[ "${1-}" == "rct" ]]; then
  shift
fi

exec "${script_dir}/cargo-fast-nextest" "$@"
