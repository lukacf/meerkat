// @generated - session realtime transcript authority for `SessionRealtimeTranscriptAuthorityMachine`
// Generated by `xtask protocol-codegen` from `SessionRealtimeTranscriptAuthorityMachine` transitions.
#![allow(clippy::bool_comparison)]
#![allow(clippy::too_many_arguments)]

use std::collections::{BTreeMap, BTreeSet};
use std::fmt;

use crate::{
    realtime_transcript::{
        RealtimeTranscriptApplyOutcome, RealtimeTranscriptEvent,
        RealtimeTranscriptMaterializedMessage, RealtimeTranscriptRole, TranscriptLane,
    },
    types::{
        AssistantBlock, BlockAssistantMessage, ContentInput, Message, StopReason, TranscriptSource,
        Usage, UserMessage,
    },
};

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
pub enum RealtimeTranscriptEventKind {
    #[default]
    ItemObserved,
    ItemSkipped,
    UserTranscriptFinal,
    AssistantTextDelta,
    AssistantTranscriptDelta,
    AssistantTranscriptTruncated,
    AssistantTranscriptFinalText,
    AssistantTurnCompleted,
    AssistantTurnInterrupted,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
pub enum RealtimeTranscriptRoleKind {
    #[default]
    User,
    Assistant,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
pub enum RealtimeTranscriptLaneKind {
    #[default]
    Display,
    Spoken,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
pub enum RealtimeTranscriptStopReasonKind {
    Cancelled,
    ToolUse,
    #[default]
    Other,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
pub enum RealtimeTranscriptMaterializeDecision {
    #[default]
    Wait,
    MarkSkipped,
    MaterializeUser,
    MaterializeAssistant,
}

impl From<&RealtimeTranscriptEvent> for RealtimeTranscriptEventKind {
    fn from(event: &RealtimeTranscriptEvent) -> Self {
        match event {
            RealtimeTranscriptEvent::ItemObserved { .. } => Self::ItemObserved,
            RealtimeTranscriptEvent::ItemSkipped { .. } => Self::ItemSkipped,
            RealtimeTranscriptEvent::UserTranscriptFinal { .. } => Self::UserTranscriptFinal,
            RealtimeTranscriptEvent::AssistantTextDelta { .. } => Self::AssistantTextDelta,
            RealtimeTranscriptEvent::AssistantTranscriptDelta { .. } => {
                Self::AssistantTranscriptDelta
            }
            RealtimeTranscriptEvent::AssistantTranscriptTruncated { .. } => {
                Self::AssistantTranscriptTruncated
            }
            RealtimeTranscriptEvent::AssistantTranscriptFinalText { .. } => {
                Self::AssistantTranscriptFinalText
            }
            RealtimeTranscriptEvent::AssistantTurnCompleted { .. } => Self::AssistantTurnCompleted,
            RealtimeTranscriptEvent::AssistantTurnInterrupted { .. } => {
                Self::AssistantTurnInterrupted
            }
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
pub enum SessionRealtimeTranscriptAuthorityPhase {
    #[default]
    Ready,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct SessionRealtimeTranscriptAuthorityMachineState {
    lifecycle_phase: SessionRealtimeTranscriptAuthorityPhase,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum SessionRealtimeTranscriptAuthorityInput {
    ResolveItemObserved {
        role: RealtimeTranscriptRoleKind,
        response_discarded: bool,
    },
    ResolveItemSkipped,
    ResolveUserTranscriptFinal {
        text_present: bool,
        segment_empty: bool,
        segment_matches: bool,
    },
    ResolveAssistantDelta {
        response_id_valid: bool,
        response_discarded: bool,
        delta_id_present: bool,
        delta_id_seen: bool,
        item_has_text: bool,
        current_lane: RealtimeTranscriptLaneKind,
        requested_lane: RealtimeTranscriptLaneKind,
        response_completed: bool,
        text_after_write_present: bool,
    },
    ResolveAssistantTextReplacement {
        response_id_valid: bool,
        response_discarded: bool,
        item_materialized: bool,
        item_has_text: bool,
        current_lane: RealtimeTranscriptLaneKind,
        requested_lane: RealtimeTranscriptLaneKind,
        response_completed: bool,
        text_after_replace_present: bool,
    },
    ResolveAssistantTurnCompleted {
        response_id_valid: bool,
        response_discarded: bool,
        stop_reason: RealtimeTranscriptStopReasonKind,
    },
    ResolveAssistantTurnInterrupted {
        response_id_valid: bool,
    },
    ResolveMaterializeCandidate {
        item_materialized: bool,
        predecessor_materialized: bool,
        item_skipped: bool,
        item_ready: bool,
        item_text_present: bool,
        role: RealtimeTranscriptRoleKind,
        response_id_present: bool,
        completion_present: bool,
        completion_usage_consumed: bool,
    },
    RestoreRealtimeTranscriptState {
        item_count: u64,
        first_seen_count: u64,
        first_seen_unique_count: u64,
        every_item_has_order_entry: bool,
        every_order_entry_has_item: bool,
        all_identity_fields_valid: bool,
        all_delta_ids_valid: bool,
        all_completion_response_ids_valid: bool,
        all_discarded_response_ids_valid: bool,
        all_materialized_items_were_ready_or_skipped: bool,
        all_assistant_items_have_response_unless_skipped: bool,
        all_ready_assistant_items_have_completion_or_are_skipped: bool,
        all_materialized_assistant_completions_consumed: bool,
        all_completed_assistant_text_items_are_ready_or_materialized_or_skipped: bool,
        all_discarded_assistant_items_are_skipped_or_materialized: bool,
    },
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum SessionRealtimeTranscriptAuthorityEffect {
    RealtimeTranscriptEventResolved {
        observe_item: bool,
        observe_skipped: bool,
        write_user_segment: bool,
        append_assistant_segment: bool,
        replace_assistant_segment: bool,
        promote_lane: bool,
        mark_item_ready: bool,
        record_delta_id: bool,
        remove_completion: bool,
        record_completion: bool,
        discard_response: bool,
        discard_response_by_lane: bool,
        mark_response_ready: bool,
        materialize_ready_items: bool,
    },
    MaterializeCandidateResolved {
        decision: RealtimeTranscriptMaterializeDecision,
        consume_usage: bool,
    },
    SnapshotRestoreAuthorized,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SessionRealtimeTranscriptAuthorityError {
    op: &'static str,
}

impl fmt::Display for SessionRealtimeTranscriptAuthorityError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(
            f,
            "generated session realtime transcript authority rejected {}",
            self.op
        )
    }
}

impl std::error::Error for SessionRealtimeTranscriptAuthorityError {}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct SessionRealtimeTranscriptAuthorityMachineAuthority {
    state: SessionRealtimeTranscriptAuthorityMachineState,
}

impl SessionRealtimeTranscriptAuthorityMachineAuthority {
    #[must_use]
    pub fn new() -> Self {
        let state = SessionRealtimeTranscriptAuthorityMachineState {
            lifecycle_phase: SessionRealtimeTranscriptAuthorityPhase::Ready,
        };
        Self { state }
    }

    #[must_use]
    pub fn state(&self) -> &SessionRealtimeTranscriptAuthorityMachineState {
        &self.state
    }

    fn apply_input(
        &mut self,
        input: SessionRealtimeTranscriptAuthorityInput,
    ) -> Result<
        Vec<SessionRealtimeTranscriptAuthorityEffect>,
        SessionRealtimeTranscriptAuthorityError,
    > {
        match input {
            SessionRealtimeTranscriptAuthorityInput::ResolveItemObserved {
                role,
                response_discarded,
            } => {
                let _ = (&role, &response_discarded);
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((role == RealtimeTranscriptRoleKind::Assistant) && (response_discarded))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: true,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((role != RealtimeTranscriptRoleKind::Assistant)
                        || (response_discarded == false))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: true,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                Err(SessionRealtimeTranscriptAuthorityError {
                    op: "ResolveItemObserved",
                })
            }
            SessionRealtimeTranscriptAuthorityInput::ResolveItemSkipped => {
                if self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: true,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                Err(SessionRealtimeTranscriptAuthorityError {
                    op: "ResolveItemSkipped",
                })
            }
            SessionRealtimeTranscriptAuthorityInput::ResolveUserTranscriptFinal {
                text_present,
                segment_empty,
                segment_matches,
            } => {
                let _ = (&text_present, &segment_empty, &segment_matches);
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && (text_present == false)
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: true,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: true,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((text_present) && (segment_empty))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: true,
                            observe_skipped: false,
                            write_user_segment: true,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: true,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((text_present) && (segment_empty == false))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: true,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: true,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                Err(SessionRealtimeTranscriptAuthorityError {
                    op: "ResolveUserTranscriptFinal",
                })
            }
            SessionRealtimeTranscriptAuthorityInput::ResolveAssistantDelta {
                response_id_valid,
                response_discarded,
                delta_id_present,
                delta_id_seen,
                item_has_text,
                current_lane,
                requested_lane,
                response_completed,
                text_after_write_present,
            } => {
                let _ = (
                    &response_id_valid,
                    &response_discarded,
                    &delta_id_present,
                    &delta_id_seen,
                    &item_has_text,
                    &current_lane,
                    &requested_lane,
                    &response_completed,
                    &text_after_write_present,
                );
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid == false)
                        || (delta_is_duplicate(delta_id_present, delta_id_seen)))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid) && (response_discarded))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: true,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid)
                        && (response_discarded == false)
                        && (delta_is_duplicate(delta_id_present, delta_id_seen) == false)
                        && (lane_accepts(item_has_text, current_lane, requested_lane) == false))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: true,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: delta_id_present,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid)
                        && (response_discarded == false)
                        && (delta_is_duplicate(delta_id_present, delta_id_seen) == false)
                        && (lane_accepts(item_has_text, current_lane, requested_lane)))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: true,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: true,
                            replace_assistant_segment: false,
                            promote_lane: true,
                            mark_item_ready: should_mark_ready_after_write(
                                response_completed,
                                text_after_write_present,
                            ),
                            record_delta_id: delta_id_present,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                Err(SessionRealtimeTranscriptAuthorityError {
                    op: "ResolveAssistantDelta",
                })
            }
            SessionRealtimeTranscriptAuthorityInput::ResolveAssistantTextReplacement {
                response_id_valid,
                response_discarded,
                item_materialized,
                item_has_text,
                current_lane,
                requested_lane,
                response_completed,
                text_after_replace_present,
            } => {
                let _ = (
                    &response_id_valid,
                    &response_discarded,
                    &item_materialized,
                    &item_has_text,
                    &current_lane,
                    &requested_lane,
                    &response_completed,
                    &text_after_replace_present,
                );
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && (response_id_valid == false)
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid) && (response_discarded))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: true,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid) && (response_discarded == false) && (item_materialized))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: true,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid)
                        && (response_discarded == false)
                        && (item_materialized == false)
                        && (lane_accepts(item_has_text, current_lane, requested_lane) == false))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: true,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid)
                        && (response_discarded == false)
                        && (item_materialized == false)
                        && (lane_accepts(item_has_text, current_lane, requested_lane)))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: true,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: true,
                            promote_lane: true,
                            mark_item_ready: should_mark_ready_after_write(
                                response_completed,
                                text_after_replace_present,
                            ),
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                Err(SessionRealtimeTranscriptAuthorityError {
                    op: "ResolveAssistantTextReplacement",
                })
            }
            SessionRealtimeTranscriptAuthorityInput::ResolveAssistantTurnCompleted {
                response_id_valid,
                response_discarded,
                stop_reason,
            } => {
                let _ = (&response_id_valid, &response_discarded, &stop_reason);
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && (response_id_valid == false)
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid)
                        && ((response_discarded) || (stop_reason_discards(stop_reason))))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: true,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid)
                        && (response_discarded == false)
                        && (stop_reason_removes_completion(stop_reason)))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: true,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((response_id_valid)
                        && (response_discarded == false)
                        && (stop_reason_records_completion(stop_reason)))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: true,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: true,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                Err(SessionRealtimeTranscriptAuthorityError {
                    op: "ResolveAssistantTurnCompleted",
                })
            }
            SessionRealtimeTranscriptAuthorityInput::ResolveAssistantTurnInterrupted {
                response_id_valid,
            } => {
                let _ = &response_id_valid;
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && (response_id_valid == false)
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: false,
                            discard_response: false,
                            discard_response_by_lane: false,
                            mark_response_ready: false,
                            materialize_ready_items: false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && (response_id_valid)
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                            observe_item: false,
                            observe_skipped: false,
                            write_user_segment: false,
                            append_assistant_segment: false,
                            replace_assistant_segment: false,
                            promote_lane: false,
                            mark_item_ready: false,
                            record_delta_id: false,
                            remove_completion: false,
                            record_completion: true,
                            discard_response: false,
                            discard_response_by_lane: true,
                            mark_response_ready: true,
                            materialize_ready_items: true,
                        },
                    ]);
                }
                Err(SessionRealtimeTranscriptAuthorityError {
                    op: "ResolveAssistantTurnInterrupted",
                })
            }
            SessionRealtimeTranscriptAuthorityInput::ResolveMaterializeCandidate {
                item_materialized,
                predecessor_materialized,
                item_skipped,
                item_ready,
                item_text_present,
                role,
                response_id_present,
                completion_present,
                completion_usage_consumed,
            } => {
                let _ = (
                    &item_materialized,
                    &predecessor_materialized,
                    &item_skipped,
                    &item_ready,
                    &item_text_present,
                    &role,
                    &response_id_present,
                    &completion_present,
                    &completion_usage_consumed,
                );
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && (item_materialized)
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::MaterializeCandidateResolved {
                            decision: RealtimeTranscriptMaterializeDecision::Wait,
                            consume_usage: false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((item_materialized == false) && (predecessor_materialized == false))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::MaterializeCandidateResolved {
                            decision: RealtimeTranscriptMaterializeDecision::Wait,
                            consume_usage: false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((item_materialized == false)
                        && (predecessor_materialized)
                        && (item_skipped))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::MaterializeCandidateResolved {
                            decision: RealtimeTranscriptMaterializeDecision::MarkSkipped,
                            consume_usage: false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((item_materialized == false)
                        && (predecessor_materialized)
                        && (item_skipped == false)
                        && ((item_ready == false) || (item_text_present == false)))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::MaterializeCandidateResolved {
                            decision: RealtimeTranscriptMaterializeDecision::Wait,
                            consume_usage: false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((item_materialized == false)
                        && (predecessor_materialized)
                        && (item_skipped == false)
                        && (item_ready)
                        && (item_text_present)
                        && (role == RealtimeTranscriptRoleKind::User))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::MaterializeCandidateResolved {
                            decision: RealtimeTranscriptMaterializeDecision::MaterializeUser,
                            consume_usage: false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((item_materialized == false)
                        && (predecessor_materialized)
                        && (item_skipped == false)
                        && (item_ready)
                        && (item_text_present)
                        && (role == RealtimeTranscriptRoleKind::Assistant)
                        && (response_id_present)
                        && (completion_present))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::MaterializeCandidateResolved {
                            decision: RealtimeTranscriptMaterializeDecision::MaterializeAssistant,
                            consume_usage: completion_usage_consumed == false,
                        },
                    ]);
                }
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((item_materialized == false)
                        && (predecessor_materialized)
                        && (item_skipped == false)
                        && (item_ready)
                        && (item_text_present)
                        && (role == RealtimeTranscriptRoleKind::Assistant)
                        && ((response_id_present == false) || (completion_present == false)))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::MaterializeCandidateResolved {
                            decision: RealtimeTranscriptMaterializeDecision::Wait,
                            consume_usage: false,
                        },
                    ]);
                }
                Err(SessionRealtimeTranscriptAuthorityError {
                    op: "ResolveMaterializeCandidate",
                })
            }
            SessionRealtimeTranscriptAuthorityInput::RestoreRealtimeTranscriptState {
                item_count,
                first_seen_count,
                first_seen_unique_count,
                every_item_has_order_entry,
                every_order_entry_has_item,
                all_identity_fields_valid,
                all_delta_ids_valid,
                all_completion_response_ids_valid,
                all_discarded_response_ids_valid,
                all_materialized_items_were_ready_or_skipped,
                all_assistant_items_have_response_unless_skipped,
                all_ready_assistant_items_have_completion_or_are_skipped,
                all_materialized_assistant_completions_consumed,
                all_completed_assistant_text_items_are_ready_or_materialized_or_skipped,
                all_discarded_assistant_items_are_skipped_or_materialized,
            } => {
                let _ = (
                    &item_count,
                    &first_seen_count,
                    &first_seen_unique_count,
                    &every_item_has_order_entry,
                    &every_order_entry_has_item,
                    &all_identity_fields_valid,
                    &all_delta_ids_valid,
                    &all_completion_response_ids_valid,
                    &all_discarded_response_ids_valid,
                    &all_materialized_items_were_ready_or_skipped,
                    &all_assistant_items_have_response_unless_skipped,
                    &all_ready_assistant_items_have_completion_or_are_skipped,
                    &all_materialized_assistant_completions_consumed,
                    &all_completed_assistant_text_items_are_ready_or_materialized_or_skipped,
                    &all_discarded_assistant_items_are_skipped_or_materialized,
                );
                if (self.state.lifecycle_phase == SessionRealtimeTranscriptAuthorityPhase::Ready)
                    && ((item_count == first_seen_count)
                        && (first_seen_count == first_seen_unique_count)
                        && (every_item_has_order_entry)
                        && (every_order_entry_has_item)
                        && (all_identity_fields_valid)
                        && (all_delta_ids_valid)
                        && (all_completion_response_ids_valid)
                        && (all_discarded_response_ids_valid)
                        && (all_materialized_items_were_ready_or_skipped)
                        && (all_assistant_items_have_response_unless_skipped)
                        && (all_ready_assistant_items_have_completion_or_are_skipped)
                        && (all_materialized_assistant_completions_consumed)
                        && (all_completed_assistant_text_items_are_ready_or_materialized_or_skipped)
                        && (all_discarded_assistant_items_are_skipped_or_materialized))
                {
                    self.state.lifecycle_phase = SessionRealtimeTranscriptAuthorityPhase::Ready;
                    return Ok(vec![
                        SessionRealtimeTranscriptAuthorityEffect::SnapshotRestoreAuthorized,
                    ]);
                }
                Err(SessionRealtimeTranscriptAuthorityError {
                    op: "RestoreRealtimeTranscriptState",
                })
            }
        }
    }
}

fn delta_is_duplicate(delta_id_present: bool, delta_id_seen: bool) -> bool {
    (delta_id_present) && (delta_id_seen)
}

fn lane_accepts(
    item_has_text: bool,
    current_lane: RealtimeTranscriptLaneKind,
    requested_lane: RealtimeTranscriptLaneKind,
) -> bool {
    (current_lane == requested_lane) || (item_has_text == false)
}

fn should_mark_ready_after_write(response_completed: bool, text_after_write_present: bool) -> bool {
    (response_completed) && (text_after_write_present)
}

fn stop_reason_discards(stop_reason: RealtimeTranscriptStopReasonKind) -> bool {
    stop_reason == RealtimeTranscriptStopReasonKind::Cancelled
}

fn stop_reason_removes_completion(stop_reason: RealtimeTranscriptStopReasonKind) -> bool {
    stop_reason == RealtimeTranscriptStopReasonKind::ToolUse
}

fn stop_reason_records_completion(stop_reason: RealtimeTranscriptStopReasonKind) -> bool {
    stop_reason == RealtimeTranscriptStopReasonKind::Other
}

impl Default for SessionRealtimeTranscriptAuthorityMachineAuthority {
    fn default() -> Self {
        Self::new()
    }
}

fn realtime_transcript_authority_effect(
    input: SessionRealtimeTranscriptAuthorityInput,
    expected: &'static str,
) -> Result<Vec<SessionRealtimeTranscriptAuthorityEffect>, SessionRealtimeTranscriptAuthorityError>
{
    let mut authority = SessionRealtimeTranscriptAuthorityMachineAuthority::new();
    let effects = authority.apply_input(input)?;
    if effects.iter().any(|effect| {
        matches!(
            (expected, effect),
            (
                "RealtimeTranscriptEventResolved",
                SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved { .. }
            ) | (
                "MaterializeCandidateResolved",
                SessionRealtimeTranscriptAuthorityEffect::MaterializeCandidateResolved { .. }
            ) | (
                "SnapshotRestoreAuthorized",
                SessionRealtimeTranscriptAuthorityEffect::SnapshotRestoreAuthorized
            )
        )
    }) {
        Ok(effects)
    } else {
        Err(SessionRealtimeTranscriptAuthorityError { op: expected })
    }
}

#[derive(Debug, Clone, Copy)]
struct RealtimeTranscriptEventDecision {
    observe_item: bool,
    observe_skipped: bool,
    write_user_segment: bool,
    append_assistant_segment: bool,
    replace_assistant_segment: bool,
    promote_lane: bool,
    mark_item_ready: bool,
    record_delta_id: bool,
    remove_completion: bool,
    record_completion: bool,
    discard_response: bool,
    discard_response_by_lane: bool,
    mark_response_ready: bool,
    materialize_ready_items: bool,
}

#[derive(Debug, Clone, Copy)]
struct MaterializeCandidateDecision {
    decision: RealtimeTranscriptMaterializeDecision,
    consume_usage: bool,
}

fn resolve_realtime_event(
    input: SessionRealtimeTranscriptAuthorityInput,
) -> Result<RealtimeTranscriptEventDecision, SessionRealtimeTranscriptAuthorityError> {
    let effects = realtime_transcript_authority_effect(input, "RealtimeTranscriptEventResolved")?;
    effects
        .into_iter()
        .find_map(|effect| match effect {
            SessionRealtimeTranscriptAuthorityEffect::RealtimeTranscriptEventResolved {
                observe_item,
                observe_skipped,
                write_user_segment,
                append_assistant_segment,
                replace_assistant_segment,
                promote_lane,
                mark_item_ready,
                record_delta_id,
                remove_completion,
                record_completion,
                discard_response,
                discard_response_by_lane,
                mark_response_ready,
                materialize_ready_items,
            } => Some(RealtimeTranscriptEventDecision {
                observe_item,
                observe_skipped,
                write_user_segment,
                append_assistant_segment,
                replace_assistant_segment,
                promote_lane,
                mark_item_ready,
                record_delta_id,
                remove_completion,
                record_completion,
                discard_response,
                discard_response_by_lane,
                mark_response_ready,
                materialize_ready_items,
            }),
            _ => None,
        })
        .ok_or(SessionRealtimeTranscriptAuthorityError {
            op: "RealtimeTranscriptEventResolved",
        })
}

fn resolve_materialize_candidate(
    input: SessionRealtimeTranscriptAuthorityInput,
) -> Result<MaterializeCandidateDecision, SessionRealtimeTranscriptAuthorityError> {
    let effects = realtime_transcript_authority_effect(input, "MaterializeCandidateResolved")?;
    effects
        .into_iter()
        .find_map(|effect| match effect {
            SessionRealtimeTranscriptAuthorityEffect::MaterializeCandidateResolved {
                decision,
                consume_usage,
            } => Some(MaterializeCandidateDecision {
                decision,
                consume_usage,
            }),
            _ => None,
        })
        .ok_or(SessionRealtimeTranscriptAuthorityError {
            op: "MaterializeCandidateResolved",
        })
}

#[derive(Debug, Clone, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
pub struct SessionRealtimeTranscriptState {
    #[serde(default)]
    items: BTreeMap<String, RealtimeTranscriptItemState>,
    #[serde(default)]
    first_seen_order: Vec<String>,
    #[serde(default)]
    seen_delta_ids: BTreeSet<String>,
    #[serde(default)]
    assistant_completions: BTreeMap<String, RealtimeAssistantCompletion>,
    #[serde(default, skip_serializing_if = "BTreeSet::is_empty")]
    discarded_assistant_response_ids: BTreeSet<String>,
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
struct RealtimeTranscriptItemState {
    role: RealtimeTranscriptRole,
    #[serde(default)]
    previous_item_id: Option<String>,
    #[serde(default)]
    response_id: Option<String>,
    #[serde(default)]
    content_segments: BTreeMap<u32, String>,
    #[serde(default)]
    skipped: bool,
    #[serde(default)]
    ready: bool,
    #[serde(default)]
    materialized: bool,
    #[serde(default)]
    lane: TranscriptLane,
}

impl RealtimeTranscriptItemState {
    fn new(
        role: RealtimeTranscriptRole,
        previous_item_id: Option<String>,
        response_id: Option<String>,
    ) -> Self {
        Self {
            role,
            previous_item_id,
            response_id,
            content_segments: BTreeMap::new(),
            skipped: false,
            ready: false,
            materialized: false,
            lane: TranscriptLane::Display,
        }
    }

    fn skipped(previous_item_id: Option<String>) -> Self {
        Self {
            role: RealtimeTranscriptRole::Assistant,
            previous_item_id,
            response_id: None,
            content_segments: BTreeMap::new(),
            skipped: true,
            ready: true,
            materialized: false,
            lane: TranscriptLane::Display,
        }
    }

    fn text(&self) -> String {
        self.content_segments.values().cloned().collect()
    }
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
struct RealtimeAssistantCompletion {
    stop_reason: StopReason,
    usage: Usage,
    usage_consumed: bool,
}

#[derive(Debug, Clone, Default)]
pub struct RealtimeTranscriptApplyCommit {
    pub outcome: RealtimeTranscriptApplyOutcome,
    pub messages: Vec<Message>,
    pub usage: Usage,
}

pub fn restore_realtime_transcript_state(
    state: SessionRealtimeTranscriptState,
) -> Result<SessionRealtimeTranscriptState, SessionRealtimeTranscriptAuthorityError> {
    let first_seen_unique_count = state
        .first_seen_order
        .iter()
        .cloned()
        .collect::<BTreeSet<_>>()
        .len();
    realtime_transcript_authority_effect(
        SessionRealtimeTranscriptAuthorityInput::RestoreRealtimeTranscriptState {
            item_count: usize_to_u64(state.items.len()),
            first_seen_count: usize_to_u64(state.first_seen_order.len()),
            first_seen_unique_count: usize_to_u64(first_seen_unique_count),
            every_item_has_order_entry: state
                .items
                .keys()
                .all(|item_id| state.first_seen_order.iter().any(|seen| seen == item_id)),
            every_order_entry_has_item: state
                .first_seen_order
                .iter()
                .all(|item_id| state.items.contains_key(item_id)),
            all_identity_fields_valid: realtime_transcript_state_identity_fields_valid(&state),
            all_delta_ids_valid: realtime_transcript_delta_ids_valid(&state),
            all_completion_response_ids_valid: realtime_transcript_completion_ids_valid(&state),
            all_discarded_response_ids_valid: realtime_transcript_discarded_ids_valid(&state),
            all_materialized_items_were_ready_or_skipped:
                realtime_transcript_materialized_items_were_ready_or_skipped(&state),
            all_assistant_items_have_response_unless_skipped:
                realtime_transcript_assistant_items_have_response_unless_skipped(&state),
            all_ready_assistant_items_have_completion_or_are_skipped:
                realtime_transcript_ready_assistant_items_have_completion_or_are_skipped(&state),
            all_materialized_assistant_completions_consumed:
                realtime_transcript_materialized_assistant_completions_consumed(&state),
            all_completed_assistant_text_items_are_ready_or_materialized_or_skipped:
                realtime_transcript_completed_assistant_text_items_are_ready_or_materialized_or_skipped(
                    &state,
                ),
            all_discarded_assistant_items_are_skipped_or_materialized:
                realtime_transcript_discarded_assistant_items_are_skipped_or_materialized(&state),
        },
        "SnapshotRestoreAuthorized",
    )?;
    Ok(state)
}

pub fn apply_realtime_transcript_event(
    state: &mut SessionRealtimeTranscriptState,
    event: RealtimeTranscriptEvent,
) -> Result<RealtimeTranscriptApplyCommit, SessionRealtimeTranscriptAuthorityError> {
    let commit = match event {
        RealtimeTranscriptEvent::ItemObserved {
            item_id,
            previous_item_id,
            role,
            response_id,
        } => {
            let response_id = normalize_realtime_optional_response_id(response_id);
            let decision = resolve_realtime_event(
                SessionRealtimeTranscriptAuthorityInput::ResolveItemObserved {
                    role: role_kind(role),
                    response_discarded: role == RealtimeTranscriptRole::Assistant
                        && response_id
                            .as_ref()
                            .is_some_and(|id| state.discarded_assistant_response_ids.contains(id)),
                },
            )?;
            apply_item_observation_decision(
                state,
                decision,
                item_id,
                previous_item_id,
                role,
                response_id,
            )?
        }
        RealtimeTranscriptEvent::ItemSkipped {
            item_id,
            previous_item_id,
        } => {
            let decision = resolve_realtime_event(
                SessionRealtimeTranscriptAuthorityInput::ResolveItemSkipped,
            )?;
            apply_item_observation_decision(
                state,
                decision,
                item_id,
                previous_item_id,
                RealtimeTranscriptRole::Assistant,
                None,
            )?
        }
        RealtimeTranscriptEvent::UserTranscriptFinal {
            item_id,
            previous_item_id,
            content_index,
            text,
        } => apply_user_transcript_final(state, item_id, previous_item_id, content_index, text)?,
        RealtimeTranscriptEvent::AssistantTextDelta {
            response_id,
            delta_id,
            item_id,
            previous_item_id,
            content_index,
            delta,
        } => apply_assistant_delta(
            state,
            response_id,
            delta_id,
            item_id,
            previous_item_id,
            content_index,
            delta,
            TranscriptLane::Display,
        )?,
        RealtimeTranscriptEvent::AssistantTranscriptDelta {
            response_id,
            delta_id,
            item_id,
            previous_item_id,
            content_index,
            delta,
        } => apply_assistant_delta(
            state,
            response_id,
            delta_id,
            item_id,
            previous_item_id,
            content_index,
            delta,
            TranscriptLane::Spoken,
        )?,
        RealtimeTranscriptEvent::AssistantTranscriptTruncated {
            response_id,
            item_id,
            content_index,
            text,
        } => apply_assistant_text_replacement(
            state,
            response_id,
            item_id,
            content_index,
            text,
            "AssistantTranscriptTruncated",
        )?,
        RealtimeTranscriptEvent::AssistantTranscriptFinalText {
            response_id,
            item_id,
            content_index,
            text,
        } => apply_assistant_text_replacement(
            state,
            response_id,
            item_id,
            content_index,
            text,
            "AssistantTranscriptFinalText",
        )?,
        RealtimeTranscriptEvent::AssistantTurnCompleted {
            response_id,
            stop_reason,
            usage,
        } => apply_assistant_turn_completed(state, response_id, stop_reason, usage)?,
        RealtimeTranscriptEvent::AssistantTurnInterrupted { response_id } => {
            apply_assistant_turn_interrupted(state, response_id)?
        }
    };
    Ok(commit)
}

fn apply_item_observation_decision(
    state: &mut SessionRealtimeTranscriptState,
    decision: RealtimeTranscriptEventDecision,
    item_id: String,
    previous_item_id: Option<String>,
    role: RealtimeTranscriptRole,
    response_id: Option<String>,
) -> Result<RealtimeTranscriptApplyCommit, SessionRealtimeTranscriptAuthorityError> {
    if decision.observe_skipped {
        observe_realtime_skipped_item(state, item_id, previous_item_id);
    } else if decision.observe_item {
        observe_realtime_item(state, item_id, previous_item_id, role, response_id);
    }
    finish_realtime_event(state, decision)
}

fn apply_user_transcript_final(
    state: &mut SessionRealtimeTranscriptState,
    item_id: String,
    previous_item_id: Option<String>,
    content_index: u32,
    text: String,
) -> Result<RealtimeTranscriptApplyCommit, SessionRealtimeTranscriptAuthorityError> {
    let existing_segment = state
        .items
        .get(&item_id)
        .and_then(|item| item.content_segments.get(&content_index));
    let segment_empty = existing_segment.is_none_or(String::is_empty);
    let segment_matches = existing_segment.is_some_and(|segment| segment == &text);
    let decision = resolve_realtime_event(
        SessionRealtimeTranscriptAuthorityInput::ResolveUserTranscriptFinal {
            text_present: !text.is_empty(),
            segment_empty,
            segment_matches,
        },
    )?;

    if let Some(item) = if decision.observe_item {
        observe_realtime_item(
            state,
            item_id,
            previous_item_id,
            RealtimeTranscriptRole::User,
            None,
        )
    } else {
        None
    } {
        if decision.write_user_segment {
            item.content_segments.insert(content_index, text);
        } else if !text.is_empty() && !segment_empty && !segment_matches {
            tracing::warn!(
                content_index,
                "ignoring conflicting realtime user transcript segment replay"
            );
        }
        if decision.mark_item_ready {
            item.ready = true;
        }
    }
    finish_realtime_event(state, decision)
}

fn apply_assistant_delta(
    state: &mut SessionRealtimeTranscriptState,
    response_id: String,
    delta_id: String,
    item_id: String,
    previous_item_id: Option<String>,
    content_index: u32,
    delta: String,
    requested_lane: TranscriptLane,
) -> Result<RealtimeTranscriptApplyCommit, SessionRealtimeTranscriptAuthorityError> {
    let response_id = normalize_realtime_response_id(response_id);
    let delta_id_present = !delta_id.trim().is_empty();
    let delta_id_seen = delta_id_present && state.seen_delta_ids.contains(&delta_id);
    let response_discarded = response_id
        .as_ref()
        .is_some_and(|id| state.discarded_assistant_response_ids.contains(id));
    let item_has_text = state
        .items
        .get(&item_id)
        .is_some_and(|item| !item.text().is_empty());
    let current_lane = state
        .items
        .get(&item_id)
        .map(|item| item.lane)
        .unwrap_or_default();
    let text_after_write_present = item_has_text || !delta.is_empty();
    let response_completed = response_id
        .as_ref()
        .is_some_and(|id| state.assistant_completions.contains_key(id));
    let decision = resolve_realtime_event(
        SessionRealtimeTranscriptAuthorityInput::ResolveAssistantDelta {
            response_id_valid: response_id.is_some(),
            response_discarded,
            delta_id_present,
            delta_id_seen,
            item_has_text,
            current_lane: lane_kind(current_lane),
            requested_lane: lane_kind(requested_lane),
            response_completed,
            text_after_write_present,
        },
    )?;

    if decision.observe_skipped {
        observe_realtime_skipped_item(state, item_id, previous_item_id);
    } else if let Some(response_id) = response_id {
        if decision.record_delta_id {
            state.seen_delta_ids.insert(delta_id);
        }
        let item = if decision.observe_item {
            observe_realtime_item(
                state,
                item_id,
                previous_item_id,
                RealtimeTranscriptRole::Assistant,
                Some(response_id),
            )
        } else {
            None
        };
        if let Some(item) = item {
            if decision.promote_lane {
                item.lane = requested_lane;
            }
            if decision.append_assistant_segment {
                item.content_segments
                    .entry(content_index)
                    .or_default()
                    .push_str(&delta);
            } else if decision.observe_item && current_lane != requested_lane && item_has_text {
                tracing::warn!(
                    "realtime assistant delta routed to an incompatible transcript lane; dropping delta to preserve generated lane decision"
                );
            }
            if decision.mark_item_ready {
                item.ready = true;
            }
        }
    }
    finish_realtime_event(state, decision)
}

fn apply_assistant_text_replacement(
    state: &mut SessionRealtimeTranscriptState,
    response_id: String,
    item_id: String,
    content_index: u32,
    text: String,
    op: &'static str,
) -> Result<RealtimeTranscriptApplyCommit, SessionRealtimeTranscriptAuthorityError> {
    let response_id = normalize_realtime_response_id(response_id);
    let response_discarded = response_id
        .as_ref()
        .is_some_and(|id| state.discarded_assistant_response_ids.contains(id));
    let item_has_text = state
        .items
        .get(&item_id)
        .is_some_and(|item| !item.text().is_empty());
    let current_lane = state
        .items
        .get(&item_id)
        .map(|item| item.lane)
        .unwrap_or_default();
    let item_materialized = state
        .items
        .get(&item_id)
        .is_some_and(|item| item.materialized);
    let text_after_replace_present =
        text_after_replacing_segment_present(state.items.get(&item_id), content_index, &text);
    let response_completed = response_id
        .as_ref()
        .is_some_and(|id| state.assistant_completions.contains_key(id));
    let decision = resolve_realtime_event(
        SessionRealtimeTranscriptAuthorityInput::ResolveAssistantTextReplacement {
            response_id_valid: response_id.is_some(),
            response_discarded,
            item_materialized,
            item_has_text,
            current_lane: lane_kind(current_lane),
            requested_lane: lane_kind(TranscriptLane::Spoken),
            response_completed,
            text_after_replace_present,
        },
    )?;

    if decision.observe_skipped {
        observe_realtime_skipped_item(state, item_id, None);
    } else if let Some(response_id) = response_id {
        let item_id_for_log = item_id.clone();
        let item = if decision.observe_item {
            observe_realtime_item(
                state,
                item_id,
                None,
                RealtimeTranscriptRole::Assistant,
                Some(response_id.clone()),
            )
        } else {
            None
        };
        if let Some(item) = item {
            if item_materialized {
                tracing::warn!(
                    target: "meerkat::session",
                    item_id = %item_id_for_log,
                    response_id = %response_id,
                    "{op} arrived after item already materialized; canonical message is locked, late replacement dropped",
                );
            } else if decision.promote_lane {
                item.lane = TranscriptLane::Spoken;
            } else if decision.observe_item
                && current_lane != TranscriptLane::Spoken
                && item_has_text
            {
                tracing::warn!(
                    "{op} routed to a Display-lane item; dropping replacement to preserve generated lane decision"
                );
            }
            if decision.replace_assistant_segment {
                item.content_segments.insert(content_index, text);
            }
            if decision.mark_item_ready {
                item.ready = true;
            }
        }
    }
    finish_realtime_event(state, decision)
}

fn apply_assistant_turn_completed(
    state: &mut SessionRealtimeTranscriptState,
    response_id: String,
    stop_reason: StopReason,
    usage: Usage,
) -> Result<RealtimeTranscriptApplyCommit, SessionRealtimeTranscriptAuthorityError> {
    let response_id = normalize_realtime_response_id(response_id);
    let response_discarded = response_id
        .as_ref()
        .is_some_and(|id| state.discarded_assistant_response_ids.contains(id));
    let decision = resolve_realtime_event(
        SessionRealtimeTranscriptAuthorityInput::ResolveAssistantTurnCompleted {
            response_id_valid: response_id.is_some(),
            response_discarded,
            stop_reason: stop_reason_kind(stop_reason),
        },
    )?;

    if let Some(response_id) = response_id {
        if decision.discard_response {
            discard_realtime_assistant_response(state, &response_id);
        }
        if decision.remove_completion {
            state.assistant_completions.remove(&response_id);
        }
        if decision.record_completion {
            state
                .assistant_completions
                .entry(response_id.clone())
                .or_insert(RealtimeAssistantCompletion {
                    stop_reason,
                    usage,
                    usage_consumed: false,
                });
        }
        if decision.mark_response_ready {
            mark_realtime_assistant_response_ready(state, &response_id);
        }
    }
    finish_realtime_event(state, decision)
}

fn apply_assistant_turn_interrupted(
    state: &mut SessionRealtimeTranscriptState,
    response_id: String,
) -> Result<RealtimeTranscriptApplyCommit, SessionRealtimeTranscriptAuthorityError> {
    let response_id = normalize_realtime_response_id(response_id);
    let decision = resolve_realtime_event(
        SessionRealtimeTranscriptAuthorityInput::ResolveAssistantTurnInterrupted {
            response_id_valid: response_id.is_some(),
        },
    )?;

    if let Some(response_id) = response_id {
        if decision.discard_response_by_lane {
            discard_realtime_assistant_response_by_lane(state, &response_id);
        }
        if decision.record_completion {
            state
                .assistant_completions
                .entry(response_id.clone())
                .or_insert(RealtimeAssistantCompletion {
                    stop_reason: StopReason::Cancelled,
                    usage: Usage::default(),
                    usage_consumed: false,
                });
        }
        if decision.mark_response_ready {
            mark_realtime_assistant_response_ready(state, &response_id);
        }
    }
    finish_realtime_event(state, decision)
}

fn finish_realtime_event(
    state: &mut SessionRealtimeTranscriptState,
    decision: RealtimeTranscriptEventDecision,
) -> Result<RealtimeTranscriptApplyCommit, SessionRealtimeTranscriptAuthorityError> {
    if decision.materialize_ready_items {
        materialize_realtime_transcript_ready_items(state)
    } else {
        Ok(RealtimeTranscriptApplyCommit::default())
    }
}

#[must_use]
pub fn in_flight_realtime_assistant_response_ids(
    state: &SessionRealtimeTranscriptState,
) -> Vec<String> {
    let mut seen: BTreeSet<String> = BTreeSet::new();
    let mut out: Vec<String> = Vec::new();
    for item_id in &state.first_seen_order {
        let Some(item) = state.items.get(item_id) else {
            continue;
        };
        if item.role != RealtimeTranscriptRole::Assistant {
            continue;
        }
        if item.materialized || item.skipped {
            continue;
        }
        let Some(response_id) = item.response_id.as_ref() else {
            continue;
        };
        if state.discarded_assistant_response_ids.contains(response_id) {
            continue;
        }
        if seen.insert(response_id.clone()) {
            out.push(response_id.clone());
        }
    }
    out
}

fn materialize_realtime_transcript_ready_items(
    state: &mut SessionRealtimeTranscriptState,
) -> Result<RealtimeTranscriptApplyCommit, SessionRealtimeTranscriptAuthorityError> {
    let mut materialized = Vec::new();
    let mut messages = Vec::new();
    let mut committed_usage = Usage::default();
    let mut pending_blocks: Vec<AssistantBlock> = Vec::new();
    let mut pending_response_id: Option<String> = None;
    let mut pending_stop_reason: StopReason = StopReason::EndTurn;
    let mut pending_usage: Usage = Usage::default();

    loop {
        let order = realtime_transcript_order(state);
        let mut skipped_batch = Vec::new();
        let mut batch = Vec::new();
        for item_id in order {
            let Some(item) = state.items.get(&item_id) else {
                continue;
            };
            let text = item.text();
            let completion = item
                .response_id
                .as_ref()
                .and_then(|response_id| state.assistant_completions.get(response_id));
            let decision = resolve_materialize_candidate(
                SessionRealtimeTranscriptAuthorityInput::ResolveMaterializeCandidate {
                    item_materialized: item.materialized,
                    predecessor_materialized: realtime_predecessor_materialized(
                        state,
                        item.previous_item_id.as_deref(),
                    ),
                    item_skipped: item.skipped,
                    item_ready: item.ready,
                    item_text_present: !text.is_empty(),
                    role: role_kind(item.role),
                    response_id_present: item.response_id.is_some(),
                    completion_present: completion.is_some(),
                    completion_usage_consumed: completion
                        .is_some_and(|completion| completion.usage_consumed),
                },
            )?;
            match decision.decision {
                RealtimeTranscriptMaterializeDecision::Wait => {}
                RealtimeTranscriptMaterializeDecision::MarkSkipped => {
                    skipped_batch.push(item_id.clone());
                }
                RealtimeTranscriptMaterializeDecision::MaterializeUser => {
                    batch.push(ResolvedMaterialization::User {
                        item_id: item_id.clone(),
                        text,
                    });
                }
                RealtimeTranscriptMaterializeDecision::MaterializeAssistant => {
                    let Some(response_id) = item.response_id.as_ref() else {
                        continue;
                    };
                    let Some(completion) = completion else {
                        continue;
                    };
                    let usage = if decision.consume_usage {
                        completion.usage.clone()
                    } else {
                        Usage::default()
                    };
                    batch.push(ResolvedMaterialization::Assistant {
                        item_id: item_id.clone(),
                        response_id: response_id.clone(),
                        text,
                        stop_reason: completion.stop_reason,
                        usage,
                        lane: item.lane,
                        consume_usage: decision.consume_usage,
                    });
                }
            }
        }
        if skipped_batch.is_empty() && batch.is_empty() {
            break;
        }
        for item_id in skipped_batch {
            if let Some(item) = state.items.get_mut(&item_id) {
                item.materialized = true;
            }
        }

        for resolved in batch {
            match resolved {
                ResolvedMaterialization::User { item_id, text } => {
                    flush_pending_assistant_blocks(
                        &mut messages,
                        &mut committed_usage,
                        &mut pending_blocks,
                        pending_stop_reason,
                        &mut pending_usage,
                    );
                    pending_response_id = None;
                    if let Some(item) = state.items.get_mut(&item_id) {
                        item.materialized = true;
                    }
                    messages.push(Message::User(UserMessage::with_blocks(
                        ContentInput::Text(text.clone()).into_blocks(),
                    )));
                    materialized
                        .push(RealtimeTranscriptMaterializedMessage::User { item_id, text });
                }
                ResolvedMaterialization::Assistant {
                    item_id,
                    response_id,
                    text,
                    stop_reason,
                    usage,
                    lane,
                    consume_usage,
                } => {
                    if pending_response_id
                        .as_ref()
                        .is_some_and(|existing| existing != &response_id)
                        && !pending_blocks.is_empty()
                    {
                        flush_pending_assistant_blocks(
                            &mut messages,
                            &mut committed_usage,
                            &mut pending_blocks,
                            pending_stop_reason,
                            &mut pending_usage,
                        );
                        pending_response_id = None;
                    }
                    if let Some(item) = state.items.get_mut(&item_id) {
                        item.materialized = true;
                    }
                    if consume_usage
                        && let Some(completion) = state.assistant_completions.get_mut(&response_id)
                    {
                        completion.usage_consumed = true;
                    }
                    let block = match lane {
                        TranscriptLane::Display => AssistantBlock::Text {
                            text: text.clone(),
                            meta: None,
                        },
                        TranscriptLane::Spoken => AssistantBlock::Transcript {
                            text: text.clone(),
                            source: TranscriptSource::Spoken,
                            meta: None,
                        },
                    };
                    if pending_response_id.is_none() {
                        pending_response_id = Some(response_id.clone());
                        pending_stop_reason = stop_reason;
                        pending_usage = usage.clone();
                    }
                    pending_blocks.push(block);
                    materialized.push(RealtimeTranscriptMaterializedMessage::Assistant {
                        item_id,
                        response_id,
                        text,
                        stop_reason,
                        usage,
                        lane,
                    });
                }
            }
        }
    }

    flush_pending_assistant_blocks(
        &mut messages,
        &mut committed_usage,
        &mut pending_blocks,
        pending_stop_reason,
        &mut pending_usage,
    );

    Ok(RealtimeTranscriptApplyCommit {
        outcome: RealtimeTranscriptApplyOutcome {
            materialized_messages: materialized,
        },
        messages,
        usage: committed_usage,
    })
}

enum ResolvedMaterialization {
    User {
        item_id: String,
        text: String,
    },
    Assistant {
        item_id: String,
        response_id: String,
        text: String,
        stop_reason: StopReason,
        usage: Usage,
        lane: TranscriptLane,
        consume_usage: bool,
    },
}

fn flush_pending_assistant_blocks(
    messages: &mut Vec<Message>,
    committed_usage: &mut Usage,
    pending_blocks: &mut Vec<AssistantBlock>,
    pending_stop_reason: StopReason,
    pending_usage: &mut Usage,
) {
    if pending_blocks.is_empty() {
        *pending_usage = Usage::default();
        return;
    }
    let blocks = std::mem::take(pending_blocks);
    messages.push(Message::BlockAssistant(BlockAssistantMessage::new(
        blocks,
        pending_stop_reason,
    )));
    if *pending_usage != Usage::default() {
        committed_usage.add(pending_usage);
        *pending_usage = Usage::default();
    }
}

fn observe_realtime_item(
    state: &mut SessionRealtimeTranscriptState,
    item_id: String,
    previous_item_id: Option<String>,
    role: RealtimeTranscriptRole,
    response_id: Option<String>,
) -> Option<&mut RealtimeTranscriptItemState> {
    if item_id.trim().is_empty() {
        return None;
    }
    if !state.items.contains_key(&item_id) {
        state.first_seen_order.push(item_id.clone());
        state.items.insert(
            item_id.clone(),
            RealtimeTranscriptItemState::new(role, previous_item_id, response_id),
        );
    } else if let Some(item) = state.items.get_mut(&item_id) {
        if item.previous_item_id.is_none() {
            item.previous_item_id = previous_item_id;
        }
        if item.response_id.is_none() {
            item.response_id = response_id;
        }
        if item.role != role {
            tracing::warn!("ignoring conflicting realtime item role replay");
        }
    }
    state.items.get_mut(&item_id)
}

fn observe_realtime_skipped_item(
    state: &mut SessionRealtimeTranscriptState,
    item_id: String,
    previous_item_id: Option<String>,
) {
    if item_id.trim().is_empty() {
        return;
    }
    if !state.items.contains_key(&item_id) {
        state.first_seen_order.push(item_id.clone());
        state.items.insert(
            item_id,
            RealtimeTranscriptItemState::skipped(previous_item_id),
        );
    } else if let Some(item) = state.items.get_mut(&item_id) {
        if item.previous_item_id.is_none() {
            item.previous_item_id = previous_item_id;
        }
        item.skipped = true;
        item.ready = true;
    }
}

fn mark_realtime_assistant_response_ready(
    state: &mut SessionRealtimeTranscriptState,
    response_id: &str,
) {
    for item in state.items.values_mut() {
        if item.role == RealtimeTranscriptRole::Assistant
            && item.response_id.as_deref() == Some(response_id)
            && !item.text().is_empty()
        {
            item.ready = true;
        }
    }
}

fn discard_realtime_assistant_response(
    state: &mut SessionRealtimeTranscriptState,
    response_id: &str,
) {
    state
        .discarded_assistant_response_ids
        .insert(response_id.to_string());
    for item in state.items.values_mut() {
        if item.role == RealtimeTranscriptRole::Assistant
            && item.response_id.as_deref() == Some(response_id)
            && !item.materialized
        {
            item.skipped = true;
            item.ready = true;
        }
    }
}

fn discard_realtime_assistant_response_by_lane(
    state: &mut SessionRealtimeTranscriptState,
    response_id: &str,
) {
    state
        .discarded_assistant_response_ids
        .insert(response_id.to_string());
    for item in state.items.values_mut() {
        if item.role != RealtimeTranscriptRole::Assistant
            || item.response_id.as_deref() != Some(response_id)
            || item.materialized
        {
            continue;
        }
        let has_content = item
            .content_segments
            .values()
            .any(|segment| !segment.is_empty());
        if item.lane == TranscriptLane::Display && has_content {
            continue;
        }
        item.content_segments.clear();
        item.skipped = true;
        item.ready = true;
    }
}

fn realtime_transcript_order(state: &SessionRealtimeTranscriptState) -> Vec<String> {
    let mut out = Vec::new();
    let mut emitted = BTreeSet::new();
    loop {
        let mut progressed = false;
        for item_id in &state.first_seen_order {
            if emitted.contains(item_id) {
                continue;
            }
            let Some(item) = state.items.get(item_id) else {
                continue;
            };
            if item
                .previous_item_id
                .as_ref()
                .is_some_and(|prev| state.items.contains_key(prev) && !emitted.contains(prev))
            {
                continue;
            }
            emitted.insert(item_id.clone());
            out.push(item_id.clone());
            progressed = true;
        }
        if !progressed {
            for item_id in &state.first_seen_order {
                if emitted.insert(item_id.clone()) {
                    out.push(item_id.clone());
                }
            }
            break;
        }
        if emitted.len() >= state.items.len() {
            break;
        }
    }
    out
}

fn realtime_predecessor_materialized(
    state: &SessionRealtimeTranscriptState,
    previous_item_id: Option<&str>,
) -> bool {
    match previous_item_id {
        None => true,
        Some(previous_item_id) => state
            .items
            .get(previous_item_id)
            .is_some_and(|item| item.materialized),
    }
}

fn text_after_replacing_segment_present(
    item: Option<&RealtimeTranscriptItemState>,
    content_index: u32,
    text: &str,
) -> bool {
    if !text.is_empty() {
        return true;
    }
    item.is_some_and(|item| {
        item.content_segments
            .iter()
            .any(|(index, segment)| *index != content_index && !segment.is_empty())
    })
}

fn normalize_realtime_response_id(response_id: String) -> Option<String> {
    let trimmed = response_id.trim();
    if trimmed.is_empty() {
        None
    } else {
        Some(trimmed.to_string())
    }
}

fn normalize_realtime_optional_response_id(response_id: Option<String>) -> Option<String> {
    response_id.and_then(normalize_realtime_response_id)
}

fn role_kind(role: RealtimeTranscriptRole) -> RealtimeTranscriptRoleKind {
    match role {
        RealtimeTranscriptRole::User => RealtimeTranscriptRoleKind::User,
        RealtimeTranscriptRole::Assistant => RealtimeTranscriptRoleKind::Assistant,
    }
}

fn lane_kind(lane: TranscriptLane) -> RealtimeTranscriptLaneKind {
    match lane {
        TranscriptLane::Display => RealtimeTranscriptLaneKind::Display,
        TranscriptLane::Spoken => RealtimeTranscriptLaneKind::Spoken,
    }
}

fn stop_reason_kind(stop_reason: StopReason) -> RealtimeTranscriptStopReasonKind {
    match stop_reason {
        StopReason::Cancelled => RealtimeTranscriptStopReasonKind::Cancelled,
        StopReason::ToolUse => RealtimeTranscriptStopReasonKind::ToolUse,
        _ => RealtimeTranscriptStopReasonKind::Other,
    }
}

fn realtime_transcript_state_identity_fields_valid(state: &SessionRealtimeTranscriptState) -> bool {
    state.items.iter().all(|(item_id, item)| {
        !item_id.trim().is_empty()
            && item
                .previous_item_id
                .as_ref()
                .is_none_or(|previous| !previous.trim().is_empty())
            && item
                .response_id
                .as_ref()
                .is_none_or(|response| !response.trim().is_empty())
    })
}

fn realtime_transcript_delta_ids_valid(state: &SessionRealtimeTranscriptState) -> bool {
    state
        .seen_delta_ids
        .iter()
        .all(|delta_id| !delta_id.trim().is_empty())
}

fn realtime_transcript_completion_ids_valid(state: &SessionRealtimeTranscriptState) -> bool {
    state
        .assistant_completions
        .keys()
        .all(|response_id| !response_id.trim().is_empty())
}

fn realtime_transcript_discarded_ids_valid(state: &SessionRealtimeTranscriptState) -> bool {
    state
        .discarded_assistant_response_ids
        .iter()
        .all(|response_id| !response_id.trim().is_empty())
}

fn realtime_transcript_materialized_items_were_ready_or_skipped(
    state: &SessionRealtimeTranscriptState,
) -> bool {
    state
        .items
        .values()
        .all(|item| !item.materialized || item.ready || item.skipped)
}

fn realtime_transcript_assistant_items_have_response_unless_skipped(
    state: &SessionRealtimeTranscriptState,
) -> bool {
    state.items.values().all(|item| {
        item.role != RealtimeTranscriptRole::Assistant
            || item.skipped
            || item.response_id.is_some()
            || (!item.ready && !item.materialized && item.text().is_empty())
    })
}

fn realtime_transcript_ready_assistant_items_have_completion_or_are_skipped(
    state: &SessionRealtimeTranscriptState,
) -> bool {
    state.items.values().all(|item| {
        if item.role != RealtimeTranscriptRole::Assistant || item.skipped || !item.ready {
            return true;
        }
        item.response_id
            .as_ref()
            .is_some_and(|response_id| state.assistant_completions.contains_key(response_id))
    })
}

fn realtime_transcript_materialized_assistant_completions_consumed(
    state: &SessionRealtimeTranscriptState,
) -> bool {
    state.items.values().all(|item| {
        if item.role != RealtimeTranscriptRole::Assistant || !item.materialized || item.skipped {
            return true;
        }
        item.response_id.as_ref().is_some_and(|response_id| {
            state
                .assistant_completions
                .get(response_id)
                .is_some_and(|completion| completion.usage_consumed)
        })
    })
}

fn realtime_transcript_completed_assistant_text_items_are_ready_or_materialized_or_skipped(
    state: &SessionRealtimeTranscriptState,
) -> bool {
    state.items.values().all(|item| {
        if item.role != RealtimeTranscriptRole::Assistant || item.text().is_empty() {
            return true;
        }
        let Some(response_id) = item.response_id.as_ref() else {
            return false;
        };
        if !state.assistant_completions.contains_key(response_id) {
            return true;
        }
        item.ready || item.materialized || item.skipped
    })
}

fn realtime_transcript_discarded_assistant_items_are_skipped_or_materialized(
    state: &SessionRealtimeTranscriptState,
) -> bool {
    state.items.values().all(|item| {
        if item.role != RealtimeTranscriptRole::Assistant {
            return true;
        }
        let Some(response_id) = item.response_id.as_ref() else {
            return true;
        };
        if !state.discarded_assistant_response_ids.contains(response_id) {
            return true;
        }
        item.skipped || item.materialized || item.lane == TranscriptLane::Display
    })
}

fn usize_to_u64(value: usize) -> u64 {
    u64::try_from(value).unwrap_or(u64::MAX)
}
