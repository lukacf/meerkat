# A02: Internal architecture and dogma skills plus canonical doctrine

This ledger preserves the baseline evidence and independent disposition. See [the audit index](README.md) for scope, commits, and publication boundaries.

## A02-001: Durable-tail guidance advertises a removed caller-assembled recovery request instead of the store-bound recovery seam

**Candidate severity:** high. **Independent verdict:** confirm.

These are current implementation and imperative integration instructions, not merely a historical description. They direct callers to a nonexistent constructor and, more importantly, describe an authority handoff the current implementation intentionally forbids. Backend authors could design the wrong recovery seam or omit the opaque store-bound preparation/fencing contract.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/SKILL.md:312-318](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L312-L318)

```text
`DurableTailRecoveryRequest` has private fields and its only
  constructor, `from_classification`, requires the classifier's own
  `DurableTailClassified` effect.
```

- [.claude/skills/meerkat-architecture/SKILL.md:308-309](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L308-L309)

```text
REALIZES one fenced `atomic_apply` boundary.
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:236-236](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L236)

```text
`DurableTailRecoveryRequest` | Sealed recovery request; only constructor `from_classification` requires the classifier's `DurableTailClassified` effect
```

- [.claude/skills/meerkat-architecture/references/gotchas.md:59-59](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/gotchas.md?plain=1#L59)

```text
`DurableTailRecoveryRequest::from_classification`
```

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:101-112](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L101-L112)

```text
`DurableTailRecoveryRequest::from_classification` (meerkat-runtime/src/recovery.rs)
is the only constructor and requires the classifier's `DurableTailClassified`
effect.
```

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:300-300](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L300)

```text
`DurableTailRecoveryRequest`
```

### Implementation proof

- [meerkat-runtime/src/recovery.rs:1559-1579](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/recovery.rs#L1559-L1579)

```text
pub async fn recover_durable_tail(
    store: &dyn RuntimeStore,
    session_id: &SessionId,
) -> Result<DurableTailRecoveryOutcome, DurableTailRecoveryError>
```

  The current public seam takes only a store and stable session identity; its contract explicitly forbids caller-supplied documents, heads, classifications, identities, receipts, or CAS tokens.

- [meerkat-runtime/src/recovery.rs:401-409](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/recovery.rs#L401-L409)

```text
// There is intentionally no parameter through which a caller can inject a
    // `SessionDocumentEffect` or any of the shape projections below.
```

  Classification is driven and consumed internally, not accepted as a public caller-assembled capability.

- [meerkat-runtime/src/recovery.rs:1899-1926](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/recovery.rs#L1899-L1926)

```text
.commit_prepared_session_boundary(&runtime_id, request)
```

  Recovery realizes an opaque PreparedRuntimeSessionCommit through the prepared boundary store contract, with separate WholeBlob and HeadCanonical preparation, not the advertised direct atomic_apply request seam.

- Read-only reproduction:

```text
git --no-pager grep -n 'DurableTailRecoveryRequest' -- '*.rs'
```

  No matches in tracked Rust source.

  Corroborates removal of the old name; the current recovery API and internal classifier above supply the positive authority evidence.

### Independent challenge

Confirmed for the current recovery instructions and API index. I checked the public entry point, internal classifier, profile-specific preparation, and store handoff: callers supply a RuntimeStore and SessionId, not a caller-assembled classifier result. The atomicity/fencing requirement remains correct; the named atomic_apply integration seam is outdated. Narrowing: the dated 0.8.9 additions are historical evidence, not proof that the release never used the old request type. Preserve that history and annotate its supersession rather than silently replacing the historical claim.

- [meerkat-runtime/src/recovery.rs:1559-1581](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/recovery.rs#L1559-L1581)

```text
pub async fn recover_durable_tail(
    store: &dyn RuntimeStore,
    session_id: &SessionId,
) -> Result<DurableTailRecoveryOutcome, DurableTailRecoveryError>
```

  The concrete public function accepts only the store and stable session identity, and begins by loading recovery preparation from that store.

- [meerkat-runtime/src/recovery.rs:401-430](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/recovery.rs#L401-L430)

```text
let mut classifier = SessionDocumentMachineAuthority::new();
```

  The private generated_recovery_classification function drives the classifier itself and consumes its exact-candidate DurableTailClassified effect; the caller does not inject that effect.

- [meerkat-runtime/src/recovery.rs:1898-1928](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/recovery.rs#L1898-L1928)

```text
.commit_prepared_session_boundary(&runtime_id, request)
```

  The immediately preceding match prepares distinct WholeBlob and HeadCanonical recovery commits, then realizes the selected sealed request through the prepared boundary method.

- [meerkat-runtime/src/store/mod.rs:7916-7927](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/store/mod.rs#L7916-L7927)

```text
PreparedRuntimeSessionCommitExecution::ordinary(request)
```

  RuntimeStore forwards the opaque prepared request to its required session-authority carrier, rather than exposing the removed caller request contract.

- Read-only reproduction:

```text
git --no-pager grep -n -E 'DurableTailRecoveryRequest|RawPeerKind' -- '*.rs'
```

  No matches; git grep exited 1.

  Independent tracked-source search corroborates absence of the old recovery type. The positive public and store APIs above establish the replacement, rather than relying on this negative search alone.

**Required correction:** Replace the current runtime-control-plane recovery request/constructor instructions and key-file label with recover_durable_tail(store, session_id), internally derived and classified store-bound evidence, profile-specific PreparedRuntimeSessionCommit preparation, and RuntimeStore::commit_prepared_session_boundary. Update the recovery portion of gotchas rule 45 without weakening sealed authority, exact fences, or typed holds. In SKILL.md's dated 0.8.9 delta and crate_map.md's 0.8.9 additions, retain historical context and explicitly identify the old request/atomic_apply description as superseded by the current store-bound prepared boundary seam. Coordinate the shared gotchas paragraph with A02-002; no runtime or store behavior change.

### Correction

Replaced current caller-assembled recovery instructions and API index with recover_durable_tail(store, session_id), internally proved/classified store-bound evidence, profile-specific PreparedRuntimeSessionCommit preparation, and commit_prepared_session_boundary. Preserved SessionDocumentMachine classification, MeerkatMachine authorization, exact transactional fences, never-discard behavior, and typed holds. Retained the dated 0.8.9 descriptions with explicit historical/supersession notices instead of rewriting release history.

- [.claude/skills/meerkat-architecture/SKILL.md:294-296](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L294-L296)

```text
The recovery/request API names and eager snapshot/session pairing in the first
two bullets record the original 0.8.9 implementation. Use the current-design
supersession note immediately afterward for integration.
```

- [.claude/skills/meerkat-architecture/SKILL.md:323-328](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L323-L328)

```text
- **Current recovery/commit API supersession** — recovery now enters through
  `recover_durable_tail(store, session_id)`. It proves and classifies
  store-bound evidence internally, prepares a sealed
  `PreparedRuntimeSessionCommit` for WholeBlob or HeadCanonical, and realizes
  it through `RuntimeStore::commit_prepared_session_boundary`, superseding
  the caller-assembled request and direct `atomic_apply` handoff above.
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:249-250](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L249-L250)

```text
The first two entries record their original 0.8.9 API shapes; they are
superseded by the current-design note below the table.
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:261-266](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L261-L266)

```text
**Current recovery/commit design:** `recover_durable_tail(store, session_id)`
derives, proves, and classifies store-bound evidence internally, then prepares a
profile-specific `PreparedRuntimeSessionCommit` and realizes it through
`RuntimeStore::commit_prepared_session_boundary`. This supersedes the old
caller-assembled `DurableTailRecoveryRequest`/`from_classification` and direct
`atomic_apply` recovery handoff. `BoundSessionCommit` is a disjoint carrier for
```

- [.claude/skills/meerkat-architecture/references/gotchas.md:69-74](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/gotchas.md?plain=1#L69-L74)

```text
    the carrier sealed across the handoff. Recovery enters through
    `recover_durable_tail(store, session_id)`: evidence is proved and classified
    internally, then a profile-specific `PreparedRuntimeSessionCommit` is
    realized by `RuntimeStore::commit_prepared_session_boundary`. Do not inject
    caller-assembled classification, receipts, or CAS tokens; preserve the
    exact store fences and typed hold/quarantine outcomes.
```

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:105-110](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L105-L110)

```text
- `RuntimeStore` realizes a sealed `PreparedRuntimeSessionCommit` through
  `commit_prepared_session_boundary`: one atomic boundary for the recovered
  profile-specific session state, receipt, and input terminalization, fenced
  on the observed lifecycle-row version and per-input-row digests
  (`expected_row_digest` MUST be enforced inside the writing transaction;
  typed `InputRowVersionConflict` / `MachineLifecycleVersionConflict`).
```

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:112-122](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L112-L122)

```text
The public preparation seam is `recover_durable_tail(store, session_id)` in
`meerkat-runtime/src/recovery.rs`. It loads opaque store-bound evidence,
derives and proves the exact candidate, and drives/consumes the generated
classification internally. Callers supply only the `RuntimeStore` and stable
`SessionId`, never documents, heads, classifications, receipts, or CAS tokens.
WholeBlob and HeadCanonical recovery have distinct preparation paths that
produce the sealed `PreparedRuntimeSessionCommit` handed to the store.
While a tail is held or evidence is quarantined, resume fails typed
(`SessionError::DurableTailHeldForRecovery` / `DurableEvidenceQuarantined`)
with content retained. Read-triggered recovery runs under an exclusive
per-session fence and converges idempotently when a competing process wins.
```

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:327-327](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L327)

```text
- `meerkat-runtime/src/recovery.rs` — store-bound durable-tail preparation, classification, authorization, and realization (`recover_durable_tail(store, session_id)`)
```

### Final independent review

**PASS:** The current recovery instructions, gotcha, and API index now name the actual store-plus-session entry point. I verified that it loads preparation from the store, classifies internally, prepares distinct WholeBlob/HeadCanonical commits, and calls the prepared boundary store seam. The two dated 0.8.9 passages are explicitly historical and superseded, rather than silently rewritten. All assigned occurrences are addressed without transferring classification, recovery authorization, or physical commit authority to callers.

- [.claude/skills/meerkat-architecture/SKILL.md:294-296](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L294-L296)

```text
The recovery/request API names and eager snapshot/session pairing in the first
two bullets record the original 0.8.9 implementation. Use the current-design
supersession note immediately afterward for integration.
```

- [.claude/skills/meerkat-architecture/SKILL.md:323-328](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L323-L328)

```text
`RuntimeStore::commit_prepared_session_boundary`, superseding
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:261-266](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L261-L266)

```text
This supersedes the old
caller-assembled `DurableTailRecoveryRequest`/`from_classification` and direct
`atomic_apply` recovery handoff.
```

- [.claude/skills/meerkat-architecture/references/gotchas.md:69-74](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/gotchas.md?plain=1#L69-L74)

```text
`recover_durable_tail(store, session_id)`: evidence is proved and classified
```

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:112-122](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L112-L122)

```text
Callers supply only the `RuntimeStore` and stable
`SessionId`, never documents, heads, classifications, receipts, or CAS tokens.
```

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:327-327](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L327)

```text
store-bound durable-tail preparation, classification, authorization, and realization (`recover_durable_tail(store, session_id)`)
```

- [meerkat-runtime/src/recovery.rs:401-430](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/recovery.rs#L401-L430)

```text
let mut classifier = SessionDocumentMachineAuthority::new();
```

- [meerkat-runtime/src/recovery.rs:1574-1579](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/recovery.rs#L1574-L1579)

```text
pub async fn recover_durable_tail(
    store: &dyn RuntimeStore,
    session_id: &SessionId,
) -> Result<DurableTailRecoveryOutcome, DurableTailRecoveryError>
```

- [meerkat-runtime/src/recovery.rs:1899-1926](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/recovery.rs#L1899-L1926)

```text
.commit_prepared_session_boundary(&runtime_id, request)
```

  The preceding match constructs machine_terminal_whole_blob_recovery for WholeBlob and machine_terminal_recovery for HeadCanonical; the caller cannot assemble these from a public classification argument.

- [meerkat-runtime/src/store/mod.rs:7916-7927](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/store/mod.rs#L7916-L7927)

```text
PreparedRuntimeSessionCommitExecution::ordinary(request)
```

## A02-002: BoundSessionCommit is documented as an eagerly serialized pair with a removed snapshot_bytes reader

**Candidate severity:** medium. **Independent verdict:** confirm.

The invariant that typed authority and bytes cannot be re-paired remains valid, but the representation, cost/failure timing, reader, and producer choices have changed. Following the current text can reintroduce eager whole-document work or lose a prepared HeadCanonical proof by reminting from only a Session.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/SKILL.md:312-316](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L312-L316)

```text
`with_session` mints the sealed pair by
  serializing the session itself
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:235-235](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L235)

```text
Sealed snapshot+typed-session pair on `CoreApplyOutput` (one private `committed` field; `with_session` is the only typed mint)
```

- [.claude/skills/meerkat-architecture/references/gotchas.md:59-59](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/gotchas.md?plain=1#L59)

```text
read via `committed()` / `snapshot_bytes()` / `session()` / `into_parts()`
```

### Implementation proof

- [meerkat-core/src/lifecycle/core_executor.rs:715-731](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/lifecycle/core_executor.rs#L715-L731)

```text
/// This mint intentionally does not serialize.
```

  BoundSessionCommit::sealed installs a OnceLock and defers WholeBlob encoding; eager serialization is explicitly no longer the constructor contract.

- [meerkat-core/src/lifecycle/core_executor.rs:697-705](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/lifecycle/core_executor.rs#L697-L705)

```text
ProvisionalPromotion {
        receipt: crate::RunCheckpointReceipt,
    }
```

  The carrier is a disjoint persistence-profile representation. A provisional promotion deliberately carries neither Session nor whole blob nor HeadCanonical delta, disproving the universal pair description.

- [meerkat-core/src/lifecycle/core_executor.rs:1436-1459](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/lifecycle/core_executor.rs#L1436-L1459)

```text
pub fn with_bound_session(mut self, committed: BoundSessionCommit) -> Self
```

  The current profile-aware path preserves an already prepared HeadCanonical mutation and its exact predecessor/suffix proof; with_session is not the only typed carrier installation.

- [meerkat-core/src/lifecycle/core_executor.rs:1455-1461](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/lifecycle/core_executor.rs#L1455-L1461)

```text
pub fn whole_blob_bytes(&self) -> Result<Option<&[u8]>, SessionBoundaryEncodeError>
```

  The current reader is fallible and lazy, not the advertised snapshot_bytes API.

### Independent challenge

The current gotcha is concretely wrong about both API and timing: with_session installs a lazy WholeBlob carrier, whole_blob_bytes is fallible, and with_bound_session installs an already sealed profile-specific carrier. HeadCanonical and provisional-promotion variants cannot yield a universal Session/bytes pair. The original no-repairing-authority invariant is still valid and must not be removed. The dated addition sections require a supersession note, not a claim that their original historical implementation was impossible.

- [meerkat-core/src/lifecycle/core_executor.rs:675-705](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/lifecycle/core_executor.rs#L675-L705)

```text
ProvisionalPromotion {
        receipt: crate::RunCheckpointReceipt,
    },
```

  The complete private representation has WholeBlobTyped, WholeBlobUntyped, HeadCanonical, and ProvisionalPromotion cases. The promotion case contains only its exact receipt, disproving a universal eagerly serialized pair.

- [meerkat-core/src/lifecycle/core_executor.rs:715-735](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/lifecycle/core_executor.rs#L715-L735)

```text
whole_blob: std::sync::Arc::new(std::sync::OnceLock::new()),
```

  sealed stores the typed Session and an empty OnceLock. It does not serialize while minting the carrier.

- [meerkat-core/src/lifecycle/core_executor.rs:970-1008](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/lifecycle/core_executor.rs#L970-L1008)

```text
.get_or_init(|| {
```

  whole_blob_bytes performs lazy serialization here; the same method explicitly errors for HeadCanonical and provisional-promotion carriers.

- [meerkat-core/src/lifecycle/core_executor.rs:1421-1461](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/lifecycle/core_executor.rs#L1421-L1461)

```text
pub fn with_bound_session(mut self, committed: BoundSessionCommit) -> Self
```

  CoreApplyOutput has a profile-aware sealed-carrier installation path in addition to with_session, and exposes whole_blob_bytes rather than the advertised snapshot_bytes reader.

**Required correction:** Correct gotchas rule 45 to describe the sealed disjoint persistence carrier, lazy/fallible whole_blob_bytes, and with_bound_session for an already prepared carrier. Do not describe HeadCanonical or provisional promotion as carrying an accumulated Session plus snapshot. Preserve committed/session/into_parts where applicable and the prohibition on re-pairing authority. Add explicit current-design supersession notes to the dated SKILL.md and crate_map.md entries instead of rewriting the 0.8.9 history as a current universal constructor contract. Coordinate the recovery sentence in the same paragraph with A02-001.

### Correction

Documented the disjoint WholeBlob, prepared HeadCanonical, and receipt-only provisional-promotion carrier. Replaced snapshot_bytes guidance with lazy/fallible whole_blob_bytes and with_bound_session preservation of prepared predecessor/suffix proof. Retained committed, typed-WholeBlob session, and intact into_parts reads and the no-repairing-authority invariant. The original eager 0.8.9 pair remains explicitly historical, with current-design supersession notes coordinated with A02-001.

- [.claude/skills/meerkat-architecture/SKILL.md:329-334](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L329-L334)

```text
  `BoundSessionCommit` is now a disjoint profile-aware carrier: typed/untyped
  WholeBlob, a prepared HeadCanonical boundary, or receipt-only provisional
  promotion. `with_session` seals lazily; `whole_blob_bytes()` performs
  fallible on-demand encoding for WholeBlob. `with_bound_session` preserves an
  already prepared carrier and its exact predecessor/suffix proof rather than
  reminting it from a `Session`. No path may re-pair typed authority with
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:266-272](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L266-L272)

```text
`atomic_apply` recovery handoff. `BoundSessionCommit` is a disjoint carrier for
typed/untyped WholeBlob, a prepared HeadCanonical boundary, or receipt-only
provisional promotion, not a universal snapshot/session pair. `with_session`
seals without eager serialization; `whole_blob_bytes()` is lazy and fallible.
Use `with_bound_session` to preserve an already prepared carrier and its exact
predecessor/suffix proof. Typed authority and unrelated bytes must never be
re-paired.
```

- [.claude/skills/meerkat-architecture/references/gotchas.md:59-69](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/gotchas.md?plain=1#L59-L69)

```text
45. **`CoreApplyOutput`'s commit is sealed and profile-aware; never re-pair authority.**
    Its private `BoundSessionCommit` is a disjoint carrier: typed/untyped
    WholeBlob, a prepared HeadCanonical boundary, or receipt-only provisional
    promotion. `with_session` seals a typed WholeBlob lazily, replacing any
    uncertified bytes; `whole_blob_bytes()` performs fallible on-demand
    encoding and rejects HeadCanonical/promotion carriers, which have no
    whole-blob representation. `with_bound_session` installs an already sealed
    carrier and preserves its exact predecessor CAS and suffix proof; do not
    discard that proof by reminting from only a `Session`. Read through
    `committed()` and, for a typed WholeBlob, `session()`; `into_parts()` keeps
    the carrier sealed across the handoff. Recovery enters through
```

### Final independent review

**PASS:** Gotcha 45 now describes the disjoint carrier and the actual lazy/fallible WholeBlob reader. The guidance does not claim that HeadCanonical or promotion contains a universal Session/bytes pair. It preserves the already-prepared carrier through with_bound_session and accurately limits session() to typed WholeBlob. Both historical tables have current-design supersession notes. The no-re-pairing invariant is retained without pretending with_session still serializes eagerly or is the only profile-aware installation path.

- [.claude/skills/meerkat-architecture/references/gotchas.md:59-69](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/gotchas.md?plain=1#L59-L69)

```text
`with_bound_session` installs an already sealed
    carrier and preserves its exact predecessor CAS and suffix proof
```

- [.claude/skills/meerkat-architecture/SKILL.md:329-336](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L329-L336)

```text
`with_session` seals lazily; `whole_blob_bytes()` performs
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:266-272](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L266-L272)

```text
not a universal snapshot/session pair. `with_session`
seals without eager serialization; `whole_blob_bytes()` is lazy and fallible.
```

- [meerkat-core/src/lifecycle/core_executor.rs:675-705](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/lifecycle/core_executor.rs#L675-L705)

```text
ProvisionalPromotion {
        receipt: crate::RunCheckpointReceipt,
    }
```

  The closed private carrier enum separately represents WholeBlobTyped, WholeBlobUntyped, HeadCanonical, and receipt-only ProvisionalPromotion.

- [meerkat-core/src/lifecycle/core_executor.rs:715-735](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/lifecycle/core_executor.rs#L715-L735)

```text
whole_blob: std::sync::Arc::new(std::sync::OnceLock::new())
```

- [meerkat-core/src/lifecycle/core_executor.rs:970-1008](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/lifecycle/core_executor.rs#L970-L1008)

```text
"head-canonical boundary has no whole-blob representation"
```

  whole_blob_bytes returns Result, rejects both non-WholeBlob carriers, and only encodes the typed Session inside get_or_init.

- [meerkat-core/src/lifecycle/core_executor.rs:1443-1485](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/lifecycle/core_executor.rs#L1443-L1485)

```text
pub fn with_bound_session(mut self, committed: BoundSessionCommit) -> Self
```

## A02-003: Production machine guidance points to the feature-gated generic test oracle

**Candidate severity:** medium. **Independent verdict:** confirm.

The navigator sends production work to the legacy oracle and advertises a symbol unavailable in ordinary builds. Catalog-owned DSL invocation by production bridges is still real; the error is specifically conflating that typed generated architecture with the generic test-only interpreter.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/SKILL.md:634-634](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L634)

```text
| `meerkat-machine-kernels` | Generated kernel interpreter for all machines/compositions | `GeneratedMachineKernel` |
```

- [.claude/skills/meerkat-architecture/SKILL.md:663-663](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L663)

```text
`meerkat-machine-kernels/src/runtime.rs` — `GeneratedMachineKernel` interpreter
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:56-56](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L56)

```text
meerkat-machine-kernels       (generated kernel interpreter — centralized, no owner-crate re-exports)
```

- [.claude/skills/meerkat-architecture/references/machine-system.md:301-301](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L301)

```text
`meerkat-machine-kernels/src/runtime.rs` — `GeneratedMachineKernel` interpreter
```

### Implementation proof

- [meerkat-machine-kernels/src/lib.rs:1-3](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-kernels/src/lib.rs#L1-L3)

```text
pub mod generated;
#[cfg(feature = "test-oracle")]
mod runtime;
```

  Typed generated modules are the ordinary public surface; runtime.rs is compiled only for test-oracle.

- [meerkat-machine-kernels/src/lib.rs:71-77](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-kernels/src/lib.rs#L71-L77)

```text
#[cfg(feature = "test-oracle")]
pub mod test_oracle
```

  GeneratedMachineKernel and its generic KernelValue family are exported under this explicitly gated oracle namespace.

- [meerkat-machine-kernels/Cargo.toml:15-17](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-kernels/Cargo.toml#L15-L17)

```text
[features]
default = []
test-oracle = []
```

  The interpreter is not a default production feature.

- [meerkat-machine-codegen/tests/render_contracts.rs:525-545](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/tests/render_contracts.rs#L525-L545)

```text
"GeneratedMachineKernel::new",
```

  The typed production kernel contract expressly forbids the legacy generic interpreter surface in generated modules.

### Independent challenge

Confirmed with a qualification: GeneratedMachineKernel has not vanished and can be used when test-oracle is enabled. However, it is neither the ordinary public kernel surface nor the production interpreter implied by the crate-role table. The implementation distinguishes always-exported typed generated modules, catalog-owned production macro bodies, and a separately gated generic oracle. Correct that navigation without rejecting the documented two-compilation architecture.

- [meerkat-machine-kernels/src/lib.rs:1-3](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-kernels/src/lib.rs#L1-L3)

```text
pub mod generated;
#[cfg(feature = "test-oracle")]
mod runtime;
```

  The ordinary generated module and optional generic runtime have different compilation/export conditions.

- [meerkat-machine-kernels/src/lib.rs:71-77](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-kernels/src/lib.rs#L71-L77)

```text
pub mod test_oracle
```

  GeneratedMachineKernel is publicly re-exported through the feature-gated test_oracle namespace, not as an unconditional root API.

- [meerkat-machine-kernels/Cargo.toml:15-17](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-kernels/Cargo.toml#L15-L17)

```text
default = []
test-oracle = []
```

  The optional interpreter is not enabled by this crate's defaults.

- [meerkat-runtime/src/meerkat_machine/dsl.rs:4658-4662](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/dsl.rs#L4658-L4662)

```text
meerkat_machine_schema::meerkat_catalog_machine_dsl!("meerkat-runtime", "meerkat_machine::dsl");
```

  The actual production bridge invokes the catalog-owned typed machine body. It is not an invocation of GeneratedMachineKernel.

- [meerkat-machine-codegen/tests/render_contracts.rs:524-545](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/tests/render_contracts.rs#L524-L545)

```text
"GeneratedMachineKernel::new",
```

  The inspected render contract explicitly forbids that generic interpreter surface in a typed generated module. This is source evidence of the distinction, not a claim that the test was run.

**Required correction:** Update the machine-kernels crate-role descriptions and runtime.rs key-file pointers in SKILL.md, crate_map.md, and machine-system.md. Describe typed modules under src/generated as the ordinary generated kernel surface and runtime.rs / test_oracle::GeneratedMachineKernel as the optional test-oracle implementation. Point production authority work to catalog-owned DSL and its bridge modules; preserve the valid catalog-macro production model and do not claim the oracle type is wholly removed.

### Correction

Separated the ordinary typed src/generated kernel surface from runtime.rs and test_oracle::GeneratedMachineKernel behind the optional test-oracle feature. Updated both crate-role descriptions and every assigned key-file pointer. Kept catalog-owned DSL and production bridge invocation as the production semantic path; did not claim the oracle disappeared or replace the valid two-compilation model.

- [.claude/skills/meerkat-architecture/SKILL.md:656-656](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L656)

```text
| `meerkat-machine-kernels` | Typed generated kernel modules in `src/generated`; optional generic test oracle | `generated::*`; `test_oracle::GeneratedMachineKernel` only with `test-oracle` |
```

- [.claude/skills/meerkat-architecture/SKILL.md:685-686](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L685-L686)

```text
- `meerkat-machine-kernels/src/generated/` — ordinary typed generated kernel surface; production authority changes originate in catalog DSL and its bridge modules
- `meerkat-machine-kernels/src/runtime.rs` — optional generic interpreter, exported as `test_oracle::GeneratedMachineKernel` only with `test-oracle`
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:58-58](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L58)

```text
meerkat-machine-kernels       (typed generated kernel modules; optional generic test-oracle)
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:85-89](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L85-L89)

```text
`meerkat-machine-kernels/src/generated/` is the ordinary typed kernel surface.
The generic interpreter in `src/runtime.rs` is available as
`test_oracle::GeneratedMachineKernel` only with the `test-oracle` feature.
Production authority work belongs in the catalog-owned DSL and the production
bridge modules that invoke its bodies, not in that optional oracle.
```

- [.claude/skills/meerkat-architecture/references/machine-system.md:310-311](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L310-L311)

```text
- `meerkat-machine-kernels/src/generated/` — ordinary typed generated kernel modules
- `meerkat-machine-kernels/src/runtime.rs` — optional generic interpreter, exported as `test_oracle::GeneratedMachineKernel` only with `test-oracle`; production bridges invoke catalog-owned DSL bodies instead
```

### Final independent review

**PASS:** The crate-role rows and all assigned runtime.rs navigation entries now separate the always-exported typed generated modules from the optional generic oracle. The test-oracle feature gate is real and defaults off. The text correctly keeps production authority work in catalog DSL and its invoking bridges; it neither removes the still-available oracle nor replaces the valid two-compilation model.

- [.claude/skills/meerkat-architecture/SKILL.md:656-656](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L656)

```text
`test_oracle::GeneratedMachineKernel` only with `test-oracle`
```

- [.claude/skills/meerkat-architecture/SKILL.md:685-686](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L685-L686)

```text
ordinary typed generated kernel surface; production authority changes originate in catalog DSL and its bridge modules
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:85-89](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L85-L89)

```text
`meerkat-machine-kernels/src/generated/` is the ordinary typed kernel surface.
```

- [.claude/skills/meerkat-architecture/references/machine-system.md:310-311](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L310-L311)

```text
optional generic interpreter, exported as `test_oracle::GeneratedMachineKernel` only with `test-oracle`; production bridges invoke catalog-owned DSL bodies instead
```

- [meerkat-machine-kernels/src/lib.rs:1-3](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-kernels/src/lib.rs#L1-L3)

```text
pub mod generated;
#[cfg(feature = "test-oracle")]
mod runtime;
```

- [meerkat-machine-kernels/src/lib.rs:71-77](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-kernels/src/lib.rs#L71-L77)

```text
#[cfg(feature = "test-oracle")]
pub mod test_oracle
```

- [meerkat-machine-kernels/Cargo.toml:15-17](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-kernels/Cargo.toml#L15-L17)

```text
[features]
default = []
test-oracle = []
```

- [meerkat-runtime/src/meerkat_machine/dsl.rs:4658-4662](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/meerkat_machine/dsl.rs#L4658-L4662)

```text
meerkat_machine_schema::meerkat_catalog_machine_dsl!("meerkat-runtime", "meerkat_machine::dsl");
```

## A02-004: WorkGraphStore is incorrectly classified as a meerkat-core trait

**Candidate severity:** medium. **Independent verdict:** confirm.

The ownership heading encourages moving a feature-domain store contract into foundational core, contradicting the actual feature-owned contract boundary.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/crate_map.md:114-114](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L114)

```text
### Core traits (defined in meerkat-core)
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:132-132](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L132)

```text
| `WorkGraphStore` | Durable realm-scoped work item, edge, claim, event, and snapshot storage | `MemoryWorkGraphStore`, `SqliteWorkGraphStore` (meerkat-workgraph) |
```

### Implementation proof

- [meerkat-workgraph/src/store.rs:74-80](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-workgraph/src/store.rs#L74-L80)

```text
pub trait WorkGraphStore: Send + Sync
```

  The trait definition itself, not just its implementations, is owned by the workgraph feature crate, with target-specific async_trait attributes.

### Independent challenge

This is an explicit definition-location error in a table headed 'Core traits (defined in meerkat-core)', not a reason to weaken the normative principle that core owns foundational contracts. WorkGraphStore is a feature-domain contract defined and exported by meerkat-workgraph on both native and wasm targets. Its implementations being feature-owned does not account for the misplaced definition row.

- [meerkat-workgraph/src/store.rs:74-85](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-workgraph/src/store.rs#L74-L85)

```text
pub trait WorkGraphStore: Send + Sync
```

  The trait body is physically defined in the workgraph crate; the surrounding target-specific async_trait attributes do not move ownership to core.

- [meerkat-workgraph/src/lib.rs:42-48](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-workgraph/src/lib.rs#L42-L48)

```text
WorkGraphStore, WorkGraphStoreKind,
```

  The feature crate exports its own store contract alongside its implementations. This establishes the correct import/ownership location independently of architecture prose.

**Required correction:** Move the WorkGraphStore row out of the 'defined in meerkat-core' table into a clearly feature-owned WorkGraph contract section identifying meerkat-workgraph as the definition owner. Keep core's foundational traits and the existing feature-owned jobs/runtime contract sections distinct; no change to product crate ownership is warranted.

### Correction

Moved WorkGraphStore out of the defined-in-meerkat-core table into its own feature-owned WorkGraph contract section. Kept foundational core, runtime, and detached-job contract sections distinct; no product ownership or native/wasm contract change.

- [.claude/skills/meerkat-architecture/references/crate_map.md:145-149](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L145-L149)

```text
### WorkGraph traits (defined in meerkat-workgraph)

| Trait | Purpose | Implementors |
|-------|---------|-------------|
| `WorkGraphStore` | Feature-owned durable realm-scoped work item, edge, claim, event, and snapshot storage | `MemoryWorkGraphStore`, `SqliteWorkGraphStore` (meerkat-workgraph) |
```

### Final independent review

**PASS:** WorkGraphStore is no longer under the core-definition heading. The new feature-owned section names the actual definition/export owner while preserving separate foundational, runtime, and job contracts. I checked both target-specific async-trait attributes: the trait remains WorkGraph-owned on native and wasm. The separate native-only SqliteWorkGraphStore export does not move or gate the contract itself.

- [.claude/skills/meerkat-architecture/references/crate_map.md:141-153](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L141-L153)

```text
### WorkGraph traits (defined in meerkat-workgraph)
```

- [meerkat-workgraph/src/store.rs:74-79](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-workgraph/src/store.rs#L74-L79)

```text
#[cfg_attr(target_arch = "wasm32", async_trait(?Send))]
#[cfg_attr(not(target_arch = "wasm32"), async_trait)]
pub trait WorkGraphStore: Send + Sync
```

- [meerkat-workgraph/src/lib.rs:43-48](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-workgraph/src/lib.rs#L43-L48)

```text
WorkGraphStore, WorkGraphStoreKind,
```

## A02-005: Comms reference names removed/mislocated ingress types and obscures the generated phase projection

**Candidate severity:** medium. **Independent verdict:** confirm.

Readers are sent to nonexistent definitions and may mistake a generated lifecycle projection for freely maintained shell bookkeeping. The existing statement that the projection is not itself authority should remain, but its provenance and sole writer need to be precise.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/comms-model.md:17-17](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/comms-model.md?plain=1#L17)

```text
Shell-side data types live in `meerkat-comms/src/peer_types.rs` (`PeerId`, `RawPeerKind`, `ContentShape`, `PeerIngressState`).
```

- [.claude/skills/meerkat-architecture/references/comms-model.md:17-17](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/comms-model.md?plain=1#L17)

```text
per-peer phase (`PeerIngressState`) is shell mechanics — concurrency plumbing, not lifecycle truth.
```

- [.claude/skills/meerkat-architecture/references/comms-model.md:33-33](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/comms-model.md?plain=1#L33)

```text
`meerkat-comms/src/peer_types.rs` — pure data types (PeerId, RawPeerKind, ContentShape, PeerIngressState)
```

### Implementation proof

- [meerkat-core/src/comms.rs:79-79](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/comms.rs#L79)

```text
pub struct PeerId(#[cfg_attr(feature = "schema", schemars(with = "String"))] pub Uuid);
```

  Canonical PeerId is defined in core, not the comms shell's peer_types module.

- [meerkat-comms/src/classify.rs:9-14](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-comms/src/classify.rs#L9-L14)

```text
PeerIngressKind, PeerIngressPlainEventFacts, PeerInputClass, TerminalityClass,
```

  The live classifier imports the core-owned PeerIngressKind vocabulary, not RawPeerKind.

- [meerkat-core/src/interaction.rs:836-840](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/interaction.rs#L836-L840)

```text
pub enum PeerIngressKind
```

  This is the current authoritative enum definition.

- [meerkat-comms/src/peer_types.rs:18-25](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-comms/src/peer_types.rs#L18-L25)

```text
/// Projection of the generated peer-ingress authority phase.
```

  PeerIngressState is specifically a generated-authority phase projection, not independently chosen queue/concurrency state.

- [meerkat-comms/src/inbox.rs:354-382](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-comms/src/inbox.rs#L354-L382)

```text
self.phase = authority.authority_phase.into();
```

  The queue updates its phase only from the resolved authority effect; missing authority fails admission closed.

- Read-only reproduction:

```text
git --no-pager grep -n 'RawPeerKind' -- '*.rs'
```

  No matches in tracked Rust source.

  Corroborates removal; the actual current enum and classifier provide positive source authority.

### Independent challenge

The type-location portion is unambiguous: PeerId is core-owned and the classifier uses core's PeerIngressKind, while peer_types.rs defines ContentShape and PeerIngressState. Narrowing the authority portion: the document is right that the local phase copy is not itself authority. The correction should identify its generated receive/dequeue provenance, not reclassify the cache as an independent lifecycle owner or weaken mechanical-shell rules.

- [meerkat-core/src/comms.rs:66-79](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/comms.rs#L66-L79)

```text
pub struct PeerId(#[cfg_attr(feature = "schema", schemars(with = "String"))] pub Uuid);
```

  This is the canonical PeerId definition, not a shell-local type in meerkat-comms.

- [meerkat-core/src/interaction.rs:831-843](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/interaction.rs#L831-L843)

```text
pub enum PeerIngressKind
```

  The actual current ingress-kind enum is defined here.

- [meerkat-comms/src/classify.rs:5-14](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-comms/src/classify.rs#L5-L14)

```text
PeerIngressKind, PeerIngressPlainEventFacts, PeerInputClass, TerminalityClass,
```

  The live classifier imports PeerIngressKind from meerkat_core while importing only ContentShape from peer_types.

- [meerkat-comms/src/peer_types.rs:18-25](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-comms/src/peer_types.rs#L18-L25)

```text
/// Projection of the generated peer-ingress authority phase.
```

  The remaining phase type is explicitly a generated-authority projection, not an independently determined per-peer state machine.

- [meerkat-comms/src/inbox.rs:354-386](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-comms/src/inbox.rs#L354-L386)

```text
self.phase = authority.authority_phase.into();
```

  Admission obtains a receive-authority result before updating the phase; absent authority returns a dropped decision rather than locally deciding phase.

- [meerkat-comms/src/inbox.rs:448-466](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-comms/src/inbox.rs#L448-L466)

```text
self.phase = authority.authority_phase.into();
```

  Dequeue likewise projects the machine's phase, providing an independent second write-path check.

**Required correction:** Correct the type inventory and key-file entry in comms-model.md: PeerId belongs to meerkat-core/src/comms.rs, PeerIngressKind to meerkat-core/src/interaction.rs, and peer_types.rs contains ContentShape and PeerIngressState. Replace RawPeerKind. Describe PeerIngressState as the queue's local projection of PeerIngressAuthorityPhase updated from machine receive/dequeue results, while retaining the accurate non-authoritative/mechanical status of queue storage and locking.

### Correction

Replaced removed RawPeerKind and the wrong PeerId location with core-owned PeerIngressKind and PeerId definitions. Limited peer_types.rs inventory to ContentShape and PeerIngressState. Explained the latter as the queue-local projection of generated PeerIngressAuthorityPhase updated from receive/dequeue authority results, while storage, queue ordering, and locking remain mechanical.

- [.claude/skills/meerkat-architecture/references/comms-model.md:17-25](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/comms-model.md?plain=1#L17-L25)

```text
Canonical `PeerId` lives in `meerkat-core/src/comms.rs`; the current ingress-kind
vocabulary is `PeerIngressKind` in `meerkat-core/src/interaction.rs`.
`meerkat-comms/src/peer_types.rs` contains the shell-side `ContentShape` and
`PeerIngressState` types, not independent state machine authorities. The trust
state on `ClassifiedInboxQueue` is the shared `Arc<RwLock<TrustStore>>`.
`PeerIngressState` is the queue's local projection of
`PeerIngressAuthorityPhase`, updated only from machine receive/dequeue
authority results through `PeerCommsHandle`. Queue storage, order, and locking
remain mechanical; the local phase copy does not choose lifecycle truth.
```

- [.claude/skills/meerkat-architecture/references/comms-model.md:41-43](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/comms-model.md?plain=1#L41-L43)

```text
- `meerkat-core/src/comms.rs` — canonical `PeerId`
- `meerkat-core/src/interaction.rs` — `PeerIngressKind`, `PeerIngressAuthorityPhase`
- `meerkat-comms/src/peer_types.rs` — `ContentShape` and the non-authoritative `PeerIngressState` projection
```

### Final independent review

**PASS:** Both the inventory paragraph and key-file list now locate PeerId and PeerIngressKind in core and leave ContentShape/PeerIngressState in peer_types.rs. The queue's receive and dequeue paths assign the projected phase from generated authority results. The replacement correctly identifies that provenance without making the cached phase or queue mechanics independent semantic authority.

- [.claude/skills/meerkat-architecture/references/comms-model.md:17-25](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/comms-model.md?plain=1#L17-L25)

```text
`PeerIngressState` is the queue's local projection of
`PeerIngressAuthorityPhase`, updated only from machine receive/dequeue
authority results through `PeerCommsHandle`.
```

- [.claude/skills/meerkat-architecture/references/comms-model.md:41-43](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/comms-model.md?plain=1#L41-L43)

```text
`meerkat-core/src/interaction.rs` — `PeerIngressKind`, `PeerIngressAuthorityPhase`
```

- [meerkat-core/src/comms.rs:77-79](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/comms.rs#L77-L79)

```text
pub struct PeerId(#[cfg_attr(feature = "schema", schemars(with = "String"))] pub Uuid);
```

- [meerkat-core/src/interaction.rs:831-842](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/interaction.rs#L831-L842)

```text
pub enum PeerIngressKind
```

- [meerkat-core/src/interaction.rs:1248-1254](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/interaction.rs#L1248-L1254)

```text
pub enum PeerIngressAuthorityPhase
```

- [meerkat-comms/src/peer_types.rs:10-43](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-comms/src/peer_types.rs#L10-L43)

```text
impl From<meerkat_core::PeerIngressAuthorityPhase> for PeerIngressState
```

- [meerkat-comms/src/inbox.rs:354-383](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-comms/src/inbox.rs#L354-L383)

```text
self.phase = authority.authority_phase.into();
```

- [meerkat-comms/src/inbox.rs:458-466](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-comms/src/inbox.rs#L458-L466)

```text
self.phase = authority.authority_phase.into();
```

## A02-006: Runtime bindings still advertise an uncertified raw tool-visibility trait object

**Candidate severity:** medium. **Independent verdict:** confirm.

This is not a harmless Arc spelling difference. The binding inventory teaches a host-installable trait object where the implementation requires a runtime-minted certified owner for semantic visibility changes.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:45-45](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L45)

```text
`tool_visibility_owner: Arc<dyn ToolVisibilityOwner>` — tool visibility projection
```

### Implementation proof

- [meerkat-core/src/runtime_epoch.rs:172-174](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/runtime_epoch.rs#L172-L174)

```text
tool_visibility_owner: GeneratedToolVisibilityOwner,
```

  The actual binding field is the generated-authority carrier.

- [meerkat-core/src/runtime_epoch.rs:313-315](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/runtime_epoch.rs#L313-L315)

```text
pub fn tool_visibility_owner(&self) -> &GeneratedToolVisibilityOwner
```

  The public accessor preserves that certified type rather than exposing a raw installable trait object.

- [meerkat-core/src/tool_scope.rs:278-293](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/tool_scope.rs#L278-L293)

```text
/// This is the only owner shape accepted by `ToolScope`/`AgentBuilder` for
/// semantic visibility changes and durable persistence.
```

  The wrapper is an intentional authority boundary; its constructor is crate-private, and raw trait objects are not interchangeable with it.

### Independent challenge

The inventory gives a concrete field type, so this is more than incomplete explanation. Runtime bindings carry GeneratedToolVisibilityOwner, whose public accessor preserves the wrapper. A raw Arc<dyn ToolVisibilityOwner> does exist inside that wrapper, but that is not the binding's exposed installation type. Test/generated bridge construction allowances do not make the two public shapes interchangeable.

- [meerkat-core/src/runtime_epoch.rs:168-176](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/runtime_epoch.rs#L168-L176)

```text
tool_visibility_owner: GeneratedToolVisibilityOwner,
```

  This is the actual SessionRuntimeBindings field, without a native/wasm alternative raw-Arc field.

- [meerkat-core/src/runtime_epoch.rs:313-315](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/runtime_epoch.rs#L313-L315)

```text
pub fn tool_visibility_owner(&self) -> &GeneratedToolVisibilityOwner
```

  The current read API exposes the certified carrier by reference.

- [meerkat-core/src/tool_scope.rs:278-297](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/tool_scope.rs#L278-L297)

```text
pub(crate) fn from_generated_authority(owner: Arc<dyn ToolVisibilityOwner>) -> Self
```

  The underlying trait object is wrapped at a crate-private generated-authority handoff, not accepted as a substitute for the binding carrier.

**Required correction:** Replace tool_visibility_owner: Arc<dyn ToolVisibilityOwner> in runtime-control-plane.md with GeneratedToolVisibilityOwner and, if an accessor is shown, tool_visibility_owner() -> &GeneratedToolVisibilityOwner. Distinguish the underlying ToolVisibilityOwner delegation trait from the generated-authority carrier expected by ordinary runtime-backed composition. Do not assert that special test/generated bridge constructors are nonexistent.

### Correction

Corrected the binding's concrete type and accessor to GeneratedToolVisibilityOwner. Distinguished the underlying delegation trait from the runtime-minted installation carrier without denying special test/generated bridge constructors.

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:45-49](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L45-L49)

```text
- `tool_visibility_owner: GeneratedToolVisibilityOwner` — generated-authority
  carrier for tool visibility; `tool_visibility_owner()` returns
  `&GeneratedToolVisibilityOwner`. The underlying `ToolVisibilityOwner` trait
  delegates behind that carrier; a raw `Arc<dyn ToolVisibilityOwner>` is not a
  substitute for the runtime-minted installation authority.
```

### Final independent review

**PASS:** The field and accessor now have the exact GeneratedToolVisibilityOwner type used by bindings and consumed by the factory. The underlying Arc trait object is correctly described as delegation inside the certified carrier, not an interchangeable installation input. I checked the standalone branch as well: it obtains its own generated standalone authority bundle, so the correction does not falsely require SessionOwned for every build or deny test/generated-bridge constructors.

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:45-49](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L45-L49)

```text
`tool_visibility_owner: GeneratedToolVisibilityOwner`
```

- [meerkat-core/src/runtime_epoch.rs:173-174](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/runtime_epoch.rs#L173-L174)

```text
tool_visibility_owner: GeneratedToolVisibilityOwner,
```

- [meerkat-core/src/runtime_epoch.rs:313-315](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/runtime_epoch.rs#L313-L315)

```text
pub fn tool_visibility_owner(&self) -> &GeneratedToolVisibilityOwner
```

- [meerkat-core/src/tool_scope.rs:278-297](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/tool_scope.rs#L278-L297)

```text
pub(crate) fn from_generated_authority(owner: Arc<dyn ToolVisibilityOwner>) -> Self
```

- [meerkat/src/factory.rs:7153-7159](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L7153-L7159)

```text
builder.with_tool_visibility_owner(bindings.tool_visibility_owner().clone());
```

- [meerkat/src/factory.rs:7175-7194](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L7175-L7194)

```text
let authorities = meerkat_runtime::standalone_session_runtime_authorities(
```

## A02-007: Completion-wake documentation presents EpochCursorState as the cursor owner/read path

**Candidate severity:** medium. **Independent verdict:** reject.

Epoch scoping is still correct, but the guide omits the current semantic owner and gives a stale direct-field description. That encourages treating a cache as the authoritative wake watermark and bypassing fail-closed machine cursor reads/advances.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:161-161](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L161)

```text
the runtime loop tracks `EpochCursorState.runtime_observed_seq` and `runtime_last_injected_seq`
```

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:203-203](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L203)

```text
Consumer cursors are epoch-owned via `EpochCursorState` on `SessionRuntimeBindings`.
```

### Implementation proof

- [meerkat-core/src/runtime_epoch.rs:62-74](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/runtime_epoch.rs#L62-L74)

```text
/// The generated ops-lifecycle authority owns cursor truth. This struct is a
/// lock-free cache updated only after that authority emits cursor-advanced
/// feedback, then read by consumers that need a local starting watermark.
```

  EpochCursorState is a projection with private atomics, not the semantic cursor owner.

- [meerkat-runtime/src/runtime_loop.rs:4925-4950](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/runtime_loop.rs#L4925-L4950)

```text
let obs = match registry.completion_cursor(
```

  The actual loop seeds observed/injected cursors through generated registry authority and refuses authority errors rather than reading the documented fields.

- [meerkat-runtime/src/ops_lifecycle.rs:3546-3595](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L3546-L3595)

```text
projection.project_authorized_completion_cursor(consumer, advanced);
```

  advance_completion_cursor drives a generated cursor transition, consumes its advanced effect, and only then updates the projection.

### Independent challenge

The candidate overreads epoch ownership as semantic authority and 'tracks' as direct atomic reads. The referenced fields still exist, and the runtime loop really passes the epoch projection to registry advancement, which updates those fields after generated feedback. The surrounding OpsLifecycleRegistry paragraph explicitly says the DSL is the sole authority. Nothing in the cited prose directs a caller to read or mutate the private atomics instead of the registry. Naming the cache more explicitly could improve precision, but the asserted competing-owner/stale-read-path defect is not established by this evidence.

- [meerkat-core/src/runtime_epoch.rs:62-74](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/runtime_epoch.rs#L62-L74)

```text
runtime_last_injected_seq: AtomicU64,
```

  Both named runtime cursor fields remain present as epoch-scoped projected values; the document is not referring to removed state.

- [meerkat-runtime/src/runtime_loop.rs:5245-5254](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/runtime_loop.rs#L5245-L5254)

```text
epoch_cursor_state.as_deref(),
```

  The completion-wake path actually carries the epoch projection alongside the registry and local sequence variables.

- [meerkat-runtime/src/runtime_loop.rs:5480-5505](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/runtime_loop.rs#L5480-L5505)

```text
.advance_completion_cursor(consumer, cursor, epoch_cursor_state)
```

  The runtime tracks the projection through the owning registry and fails closed without that authority; tracking does not entail direct-field reads.

- [meerkat-runtime/src/ops_lifecycle.rs:3584-3596](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L3584-L3596)

```text
projection.project_authorized_completion_cursor(consumer, advanced);
```

  Generated feedback is used to update the exact EpochCursorState the documentation names.

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:192-196](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L192-L196)

```text
DSL is the sole authority; the registry exposes:
```

  Target-document context limits 'epoch-owned' to scoping rather than assigning a second semantic owner. The implementation evidence above independently shows the actual ownership path.

**Disposition:** Rejected; no correction authorized for this claim.

## A02-008: Operations persistence specifies the wrong queue type and omits durable-result acknowledgement

**Candidate severity:** medium. **Independent verdict:** confirm.

The queue-capacity statement is false, and omitting the acknowledgement obscures when a terminal transition may report success. This channel must not be confused with the separate best-effort durable event projection queue or with the bounded public completion feed.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:205-205](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L205)

```text
queue it to a bounded mpsc channel. A dedicated persistence task drains to `RuntimeStore::persist_ops_lifecycle()`.
```

### Implementation proof

- [meerkat-runtime/src/ops_lifecycle.rs:2273-2278](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L2273-L2278)

```text
tx: crate::tokio::sync::mpsc::UnboundedSender<OpsLifecyclePersistenceRequest>,
```

  The public wiring method explicitly requires an unbounded request channel.

- [meerkat-runtime/src/meerkat_machine/session_management.rs:4439-4445](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/session_management.rs#L4439-L4445)

```text
crate::tokio::sync::mpsc::unbounded_channel::<
```

  Ordinary production registration creates that unbounded channel.

- [meerkat-runtime/src/ops_lifecycle.rs:1886-1919](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L1886-L1919)

```text
let (result_tx, result_rx) = std::sync::mpsc::sync_channel(1);
```

  Each request has a separate durable-result acknowledgement; maybe_persist waits with result_rx.recv and propagates queue/worker/storage failure. The one-slot response channel is not the bounded snapshot queue claimed in the reference.

- [meerkat-runtime/src/meerkat_machine/session_management.rs:1115-1136](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/session_management.rs#L1115-L1136)

```text
request.complete(result);
```

  The worker reports the actual persist_ops_lifecycle result to the waiting transition.

### Independent challenge

Confirmed after checking both queue directions. The request queue is unbounded; the bounded sync_channel(1) is the per-request reply, not the snapshot queue. The terminal persistence path waits for that reply and propagates worker/store failure. Preserve the existing 'when wired' qualification: an optional persistence composition is not an unconditional durability guarantee. Calling a native thread-backed worker a task is not the principal defect.

- [meerkat-runtime/src/ops_lifecycle.rs:2273-2278](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L2273-L2278)

```text
tx: crate::tokio::sync::mpsc::UnboundedSender<OpsLifecyclePersistenceRequest>,
```

  The concrete wiring API requires an unbounded request sender.

- [meerkat-runtime/src/meerkat_machine/session_management.rs:4437-4447](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/session_management.rs#L4437-L4447)

```text
let (persist_tx, persist_rx) = crate::tokio::sync::mpsc::unbounded_channel::<
```

  The production path creates that queue only inside the optional runtime-store composition.

- [meerkat-runtime/src/ops_lifecycle.rs:1886-1920](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L1886-L1920)

```text
let (result_tx, result_rx) = std::sync::mpsc::sync_channel(1);
```

  maybe_persist submits the snapshot, then blocks on result_rx.recv and propagates send, receive, and durable-store errors. The bounded channel is the acknowledgement, not the work queue.

- [meerkat-runtime/src/meerkat_machine/session_management.rs:1115-1137](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/session_management.rs#L1115-L1137)

```text
request.complete(result);
```

  The worker passes the actual persist_ops_lifecycle result back to the waiting producer.

**Required correction:** Correct the runtime-control-plane.md persistence-channel paragraph to an unbounded OpsLifecyclePersistenceRequest queue with a per-request durable-result acknowledgement awaited by terminal persistence. State that queue closure, worker loss, and persistence failure propagate rather than becoming successful durable writes. Keep this conditional on the channel/store being wired and distinct from bounded completion-feed retention and best-effort event projection.

### Correction

Documented the unbounded OpsLifecyclePersistenceRequest work queue and separate one-slot durable-result reply, including the terminal persistence wait and propagated closure/worker/store failures. Kept persistence conditional on wiring and separate from completion-feed retention and best-effort event projection.

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:214-223](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L214-L223)

```text
Persistence channel: when wired via `set_persistence_channel()`, terminal
persistence captures a `PersistedOpsSnapshot` (DSL op state + completion
entries + cursor values) and sends it over an unbounded mpsc queue of
`OpsLifecyclePersistenceRequest`. A dedicated worker calls
`RuntimeStore::persist_ops_lifecycle()` and acknowledges the actual durable
result through each request's separate one-slot reply channel. Terminal
persistence waits for that acknowledgement; queue closure, worker loss, and
store failures propagate rather than becoming successful durable writes.
This optional persistence path is distinct from bounded completion-feed
retention and the best-effort event projector.
```

### Final independent review

**PASS:** The replacement distinguishes the unbounded work queue from its one-slot per-request result channel and correctly describes the synchronous durable-result wait and error propagation. Both the explicit 'when wired' qualification and the distinction from bounded completion retention/best-effort event projection remain. The worker wording accommodates the native thread-backed and wasm task-backed implementations rather than inventing a universal thread requirement.

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:214-223](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L214-L223)

```text
Terminal
persistence waits for that acknowledgement; queue closure, worker loss, and
store failures propagate rather than becoming successful durable writes.
```

- [meerkat-runtime/src/meerkat_machine/session_management.rs:4440-4443](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/meerkat_machine/session_management.rs#L4440-L4443)

```text
let (persist_tx, persist_rx) = crate::tokio::sync::mpsc::unbounded_channel::<
```

- [meerkat-runtime/src/ops_lifecycle.rs:1891-1906](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/ops_lifecycle.rs#L1891-L1906)

```text
let (result_tx, result_rx) = std::sync::mpsc::sync_channel(1);
```

- [meerkat-runtime/src/ops_lifecycle.rs:1908-1919](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/ops_lifecycle.rs#L1908-L1919)

```text
result_rx.recv().map_err(|_| {
```

- [meerkat-runtime/src/meerkat_machine/session_management.rs:1115-1135](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/meerkat_machine/session_management.rs#L1115-L1135)

```text
request.complete(result);
```

  The acknowledged result is the mapped RuntimeStore::persist_ops_lifecycle result, including failure, not merely successful enqueue.

## A02-009: Operations recovery incorrectly says every nonterminal operation is stripped

**Candidate severity:** medium. **Independent verdict:** confirm.

A reader implementing restart behavior could drop a durable detached-job wait or misclassify its still-running binding as corruption. The live job itself remains feature-owned; retaining its operation wait binding does not make jobs synonymous with ops entries.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:207-207](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L207)

```text
Non-terminal operations are stripped.
```

### Implementation proof

- [meerkat-runtime/src/ops_lifecycle.rs:2366-2385](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L2366-L2385)

```text
/// Rebuilds DSL state, retaining terminal operations and running
    /// `DetachedJobWait` bindings while stripping every other non-terminal
    /// operation.
```

  The ordinary from_recovered contract deliberately retains running detached-job wait bindings. The nearby exact ReloadRequired preservation API provides a second, separately authorized exception.

- [meerkat-runtime/src/ops_lifecycle.rs:4762-4771](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L4762-L4771)

```text
assert_eq!(wait.status, OperationStatus::Running);
```

  The explicit recovery test verifies that the running wait binding, owner session, and typed source survive without minting a terminal outcome.

### Independent challenge

The universal stripping statement is disproved by a generated retention guard and an ordinary from_recovered test, not merely a comment. Running DetachedJobWait bindings are deliberately recoverable without a terminal outcome. There is also a separate exact ReloadRequired preservation path; that must not be generalized into retaining arbitrary volatile operations. This concerns operation wait bindings, not moving detached-job lifecycle authority into the registry.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:22663-22694](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L22663-L22694)

```text
|| (kind == OperationKind::DetachedJobWait
                    && status == OperationStatus::Running
```

  The generated recovery classifier explicitly retains a running job-wait record with absent terminal witnesses.

- [meerkat-runtime/src/ops_lifecycle.rs:2433-2450](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L2433-L2450)

```text
let disposition = ShellState::recovered_operation_record_disposition(
```

  Actual recovery uses the generated classification for each persisted operation instead of stripping all nonterminal rows.

- [meerkat-runtime/src/ops_lifecycle.rs:4737-4783](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L4737-L4783)

```text
assert_eq!(wait.status, OperationStatus::Running);
```

  The inspected recovery test constructs a running DetachedJobWait, recovers with the ordinary API, and requires its status/source to survive without a completion entry.

- [meerkat-runtime/src/ops_lifecycle.rs:2379-2423](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/ops_lifecycle.rs#L2379-L2423)

```text
pub(crate) fn from_recovered_preserving_operation(
```

  The separate internal ReloadRequired path requires an exact persisted identity, kind, and source match. It is not ordinary retain-all startup policy.

**Required correction:** Replace 'Non-terminal operations are stripped' with generated-classification recovery that retains valid terminal records and running DetachedJobWait bindings, discarding other volatile nonterminal records by default. Briefly qualify the separate exact typed ReloadRequired preservation path if describing recovery exceptions. Preserve detached jobs as their own feature-owned lifecycle and do not imply restoration mints a terminal outcome for a running wait.

### Correction

Replaced universal nonterminal stripping with generated recovery classification retaining valid terminal records and running DetachedJobWait bindings, without minting terminal outcomes. Qualified the separate exact typed ReloadRequired preservation path and retained feature-owned job lifecycle.

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:225-234](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L225-L234)

```text
Recovery: `MeerkatMachine::recover_or_create_ops_state()` loads persisted
snapshots via `RuntimeOpsLifecycleRegistry::from_recovered()`. Generated
recovery classification retains valid terminal records and running
`DetachedJobWait` bindings, discarding other volatile nonterminal operations
by default. Recovering a running wait does not mint a terminal outcome or
completion entry; detached-job lifecycle remains feature-owned. The separate
internal `from_recovered_preserving_operation` path handles an exact typed
`ReloadRequired` preservation request matching persisted identity, kind, and
source, not a blanket retain-all exception. The feed buffer is pre-seeded
with persisted completion entries, and consumer cursors are restored.
```

### Final independent review

**PASS:** The recovered-operation description now matches generated classification, including the Running DetachedJobWait exception without terminal outcome or completion evidence. The separate ReloadRequired path is limited to exact identity/kind/source and is not advertised as a blanket volatile-operation restoration mechanism. The wording leaves job lifecycle authority in its feature rather than equating a durable job with its wait binding.

- [.claude/skills/meerkat-architecture/references/runtime-control-plane.md:225-234](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/runtime-control-plane.md?plain=1#L225-L234)

```text
Recovering a running wait does not mint a terminal outcome or
completion entry; detached-job lifecycle remains feature-owned.
```

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:22663-22694](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L22663-L22694)

```text
|| (kind == OperationKind::DetachedJobWait
```

  The generated retention guard separately accepts Running DetachedJobWait with all three terminal witnesses absent; terminal records require their witnesses.

- [meerkat-runtime/src/ops_lifecycle.rs:2374-2386](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/ops_lifecycle.rs#L2374-L2386)

```text
Self::from_recovered_with_preservation(snapshot, None)
```

- [meerkat-runtime/src/ops_lifecycle.rs:2447-2464](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/ops_lifecycle.rs#L2447-L2464)

```text
expected.operation_id() == &op_id
```

  The following conjunction requires the exact kind and source plus a nonterminal status and absent terminal outcome/sequence.

- [meerkat-runtime/src/ops_lifecycle.rs:4737-4783](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/ops_lifecycle.rs#L4737-L4783)

```text
"recovery of a wait binding must not mint completion"
```

  The ordinary from_recovered test verifies Running status, preserved source, no terminal outcome, and an empty completion feed.

## A02-010: The architecture skill describes retired Cargo-only CI while current CI calls BuildBuddy and hosted dense-mob lanes

**Candidate severity:** medium. **Independent verdict:** confirm.

This current architecture section gives the opposite CI routing and falsely labels a live workflow inert. The local Cargo-default/optional-BuildBuddy contract is separate and was not disproved.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/SKILL.md:48-55](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L48-L55)

```text
`.github/workflows/ci.yml` runs a single Cargo lane on free GitHub-hosted
  runners
```

- [.claude/skills/meerkat-architecture/SKILL.md:52-55](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L52-L55)

```text
The GCP BuildBuddy CI
  lane was retired 2026-07-03 (`buildbuddy.yml` is inert, pending deletion)
```

### Implementation proof

- [.github/workflows/ci.yml:32-49](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.github/workflows/ci.yml#L32-L49)

```text
uses: ./.github/workflows/buildbuddy.yml
```

  The current main CI workflow actively calls the BuildBuddy reusable workflow.

- [.github/workflows/ci.yml:46-57](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.github/workflows/ci.yml#L46-L57)

```text
uses: ./.github/workflows/mob-dense-topology.yml
```

  The second required lane is GitHub-hosted dense Mob topology, not the claimed single cargo.yml route.

- [.github/workflows/ci.yml:51-72](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.github/workflows/ci.yml#L51-L72)

```text
needs:
      - gcp-buildbuddy
      - github-hosted-dense-topology
```

  The gate requires both actual lanes and rejects either non-success result.

### Independent challenge

The checkout's executable workflow contradicts the current CI-routing paragraph: top-level CI calls BuildBuddy plus the hosted dense-topology workflow, and its gate requires both. The reusable workflow is not inert. This does not refute Cargo being the default local developer backend, nor does it authorize unrelated release/nightly claims to be rewritten without their own evidence.

- [.github/workflows/ci.yml:31-49](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.github/workflows/ci.yml#L31-L49)

```text
uses: ./.github/workflows/buildbuddy.yml
```

  The top-level CI job actively calls the BuildBuddy workflow in changed-paths mode.

- [.github/workflows/ci.yml:46-57](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.github/workflows/ci.yml#L46-L57)

```text
uses: ./.github/workflows/mob-dense-topology.yml
```

  The other required lane is the separately hosted dense Mob topology workflow.

- [.github/workflows/ci.yml:51-73](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.github/workflows/ci.yml#L51-L73)

```text
if [[ "${BUILDBUDDY_RESULT}" != "success" ]]; then
```

  The gate checks both required job results and fails on either non-success; BuildBuddy is not an optional CI decoration.

**Required correction:** Correct the current CI-routing portion of SKILL.md's Build And Test Architecture section to name the BuildBuddy reusable lane, hosted dense Mob topology lane, and their aggregate gate. Remove or explicitly supersede the claim that buildbuddy.yml is currently inert. Preserve the separate, accurate local Cargo-default/BuildBuddy-opt-in description; do not treat the dated retirement as permanently current or expand this candidate into an unverified release/nightly rewrite.

### Correction

Corrected current CI routing to the changed-paths BuildBuddy reusable workflow and GitHub-hosted dense Mob topology workflow, both required by the aggregate gate. Removed the current inert-workflow claim. Preserved local Cargo-default/BuildBuddy-opt-in guidance and did not expand into release/nightly rewrites.

- [.claude/skills/meerkat-architecture/SKILL.md:48-51](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L48-L51)

```text
- `.github/workflows/ci.yml` calls `buildbuddy.yml` in `changed-paths` mode
  (`gcp-buildbuddy`) and `mob-dense-topology.yml`
  (`github-hosted-dense-topology`). The aggregate `gate` requires both lanes
  to succeed. This CI routing is separate from local backend selection.
```

### Final independent review

**PASS:** The revised CI-routing paragraph describes the executable checkout rather than the stale retirement narrative: both reusable workflows are called and both successful job results are required. Local Cargo-default/BuildBuddy-opt-in routing remains separate and matches the developer-lane switch. The fix does not expand the candidate into unrelated release/nightly claims.

- [.claude/skills/meerkat-architecture/SKILL.md:48-55](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L48-L55)

```text
The aggregate `gate` requires both lanes
  to succeed. This CI routing is separate from local backend selection.
```

- [.github/workflows/ci.yml:31-48](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.github/workflows/ci.yml#L31-L48)

```text
mode: changed-paths
```

  gcp-buildbuddy calls buildbuddy.yml and github-hosted-dense-topology calls mob-dense-topology.yml.

- [.github/workflows/ci.yml:50-73](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.github/workflows/ci.yml#L50-L73)

```text
DENSE_TOPOLOGY_RESULT: ${{ needs.github-hosted-dense-topology.result }}
```

  The gate depends on both jobs and explicitly exits unsuccessfully if either result is not success.

- [.github/workflows/mob-dense-topology.yml:22-26](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.github/workflows/mob-dense-topology.yml#L22-L26)

```text
runs-on: ubuntu-latest
```

- [scripts/run-build-backend-lane:139-151](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/run-build-backend-lane#L139-L151)

```text
if meerkat_buildbuddy_enabled; then
```

  The branch executes buildbuddy-dev only when enabled; the ordinary fallthrough executes the repo-cargo backend.

## A02-011: The agent mob tool inventory omits durable fork_off and council, hiding their distinct lifecycle contracts

**Candidate severity:** medium. **Independent verdict:** confirm.

The reference presents an exhaustive base tool roster plus an explicit conditional profile-tool extension, but omits two major shipping base tools. Because it already explains prompt-context helpers, the omission also leaves readers without the critical durable-fork versus rendered-context distinction.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:68-74](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L68-L74)

```text
`delegate`, `conclude_objective`, `mob_create`, `mob_destroy`,
`mob_spawn_member`, `mob_retire_member`, `mob_check_member`,
`mob_list_members`, `mob_list`, `mob_wire`, and `mob_unwire`.
```

### Implementation proof

- [meerkat-mob-mcp/src/agent_tools.rs:59-60](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L59-L60)

```text
const TOOL_FORK_OFF: &str = "fork_off";
```

  The tool has a shipping registration name, not just an internal helper.

- [meerkat-mob-mcp/src/agent_tools.rs:2211-2220](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L2211-L2220)

```text
TOOL_FORK_OFF,
```

  The ordinary tool-definition list registers a real durable transcript fork that retains the child in the roster, distinct from prompt-context fork_helper.

- [meerkat-mob-mcp/src/agent_tools.rs:2218-2233](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L2218-L2233)

```text
TOOL_COUNCIL,
```

  The same list registers the bounded council flow: source-owner forks retain native tools/auth/realm/filesystem and are seated in a short-lived mob with cleanup.

- [meerkat-mob-mcp/src/agent_tools.rs:1180-1208](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L1180-L1208)

```text
fork_off requires the current session to be a durable mob member
```

  The dispatcher resolves the current durable member source before fork construction; this is not an interchangeable fresh prompt helper.

### Independent challenge

Both tools are present in the base definition vector and live dispatch match, independent of the optional profile-store additions. I also checked a counterexample to unconditional exposure: the factory returns an empty surface without generated authority, and tool execution has additional source/scope checks. Thus the missing inventory entries are confirmed only for the authorized/composed AgentMobToolSurface, not as tools automatically available to every agent.

- [meerkat-mob-mcp/src/agent_tools.rs:2211-2233](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L2211-L2233)

```text
TOOL_FORK_OFF,
```

  The ordinary definition vector registers fork_off and, immediately afterward, council before optional profile-store tool additions.

- [meerkat-mob-mcp/src/agent_tools.rs:1875-1883](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L1875-L1883)

```text
TOOL_COUNCIL => self.dispatch_council_boxed(call).await,
```

  Both definitions have real dispatch arms; these are not unused constants or proposed tools.

- [meerkat-mob-mcp/src/agent_tools.rs:1922-1931](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L1922-L1931)

```text
return Ok(Arc::new(EmptyAgentToolSurface));
```

  The factory gates composition on generated authority. The corrected inventory must preserve this optionality.

- [meerkat-mob-mcp/src/agent_tools.rs:1183-1209](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L1183-L1209)

```text
"fork_off requires the current session to be a durable mob member",
```

  fork_off resolves the current session's durable member before building the child; it is not a generic prompt-context helper.

- [meerkat-mob-mcp/src/agent_tools.rs:1230-1253](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L1230-L1253)

```text
.fork_member_then_run_bounded(
```

  The implementation uses the real durable-fork bounded-run path with CallerTurn admission rather than the prompt-context helper path.

**Required correction:** Add fork_off and council to the authorized AgentMobToolSurface base-tool inventory in mob-orchestration.md. Briefly distinguish a retained durable continuation of the current durable member from rendered-context fork_helper and from bounded temporary council participants cleaned up afterward. Retain the generated operator-authority and source/scope prerequisites already described nearby; do not make availability unconditional or attribute these two tools to profile-store-only composition.

### Correction

Added fork_off and council to the generated-authority-composed base inventory, not the profile-store-only extension. Distinguished retained durable continuation from rendered-context fork_helper and temporary source-owner council participants. Preserved current-durable-member, authority, and source/scope prerequisites.

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:68-75](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L68-L75)

```text
When composed with generated operator authority, `AgentMobToolSurface`
(`meerkat-mob-mcp/src/agent_tools.rs`) provides
`delegate`, `conclude_objective`, `fork_off`, `council`, `mob_create`, `mob_destroy`,
`mob_spawn_member`, `mob_retire_member`, `mob_check_member`,
`mob_list_members`, `mob_list`, `mob_wire`, and `mob_unwire`. When a realm
profile store is present it also exposes `mob_profile_create`,
`mob_profile_get`, `mob_profile_list`, `mob_profile_update`,
`mob_profile_delete`, and `mob_profile_list_sources`.
```

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:77-86](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L77-L86)

```text
- `fork_off` requires the current session to be a durable mob member. It
  creates a real child session from an exact committed transcript prefix,
  runs one bounded task, and retains the child in the normal roster. It is
  not `fork_helper`, which renders history into a fresh helper's context and
  tears that helper down afterward.
- `council` forks existing participants at their source execution owners,
  preserving their transcript prefixes, tools, auth, realm, and filesystem
  boundaries. The forks run bounded rounds in a short-lived mob and are
  cleaned up afterward. Both tools remain subject to operator authority and
  source/scope checks; neither depends on the optional profile-store tools.
```

### Final independent review

**PASS:** Both missing tools are now in the authorized base inventory, not the optional profile extension. I followed live dispatch and the lower implementations: fork_off resolves the current member, checks generated spawn admission, persists a real child, resumes it, and waits for one exact bounded task without helper retirement. fork_helper still renders history and attempts teardown. Council uses source-owner capabilities/profile witnesses and explicit cleanup custody, preserving tool/auth/realm boundaries instead of introducing another agent factory or lifecycle owner.

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:68-86](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L68-L86)

```text
`delegate`, `conclude_objective`, `fork_off`, `council`, `mob_create`, `mob_destroy`
```

- [meerkat-mob-mcp/src/agent_tools.rs:1922-1931](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-mcp/src/agent_tools.rs#L1922-L1931)

```text
if !authority_context.is_generated_authority_context()
```

- [meerkat-mob-mcp/src/agent_tools.rs:1183-1221](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-mcp/src/agent_tools.rs#L1183-L1221)

```text
"fork_off requires the current session to be a durable mob member"
```

  The dispatch path also resolves manage/profile scope and refuses generated spawn admission denial.

- [meerkat-mob/src/runtime/handle.rs:12687-12728](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/handle.rs#L12687-L12728)

```text
Ok(ForkMemberBoundedRunOutcome { fork, turn })
```

  The path forks and submits/waits for bounded work, retaining the child instead of calling helper retirement.

- [meerkat-mob/src/runtime/actor.rs:29823-29852](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L29823-L29852)

```text
Some(render_fork_context(&source_member_id, &rows))
```

  The distinct Fork launch path renders source history and prepends the text to a new kickoff prompt.

- [meerkat-mob/src/runtime/handle.rs:12982-12992](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/handle.rs#L12982-L12992)

```text
.retire(identity)
```

  The helper completion path attempts retirement and preserves any retirement_error beside the result.

- [meerkat-mob-mcp/src/agent_tools.rs:1277-1314](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-mcp/src/agent_tools.rs#L1277-L1314)

```text
self.ensure_mob_scope_authority(call.name, &source_mob_id)
```

- [meerkat-mob/src/runtime/actor.rs:33011-33042](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L33011-L33042)

```text
self.create_placed_forked_participant(&entry, host_id, &request)
```

  Source placement selects the source execution owner; the local branch separately issues a Local owner route with its realm.

- [meerkat-mob/src/forked_participant/service.rs:815-829](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/forked_participant/service.rs#L815-L829)

```text
if child.execution.auth_binding != source.auth_binding
```

  The adjacent checks require source tool-access policy and realm equality as well.

- [meerkat-mob-mcp/src/temporary_council.rs:3309-3368](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-mcp/src/temporary_council.rs#L3309-L3368)

```text
handle.retire(participant.target_identity.clone())
```

  Council cleanup has explicit participant-retirement attempts, bounded cleanup, and debt tracking, unlike retained fork_off children.

## A02-012: Mob lifecycle references use nonexistent MobHandle spawn/retire method names

**Candidate severity:** medium. **Independent verdict:** confirm.

The lifecycle diagram and control inventory use code-like public API names that no longer match the Rust handle. The lower provisioner method must not be conflated with the user-facing handle or the mob_retire_member tool.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/crate_map.md:321-321](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L321)

```text
MobHandle::spawn(spec)
```

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:86-86](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L86)

```text
`retire_member(id)`
```

### Implementation proof

- [meerkat-mob/src/runtime/handle.rs:9178-9183](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/handle.rs#L9178-L9183)

```text
pub async fn spawn_spec(&self, spec: SpawnMemberSpec) -> Result<SpawnResult, MobError>
```

  The public fully specified member-spawn method is spawn_spec.

- [meerkat-mob/src/runtime/handle.rs:9537-9544](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/handle.rs#L9537-L9544)

```text
pub async fn retire(&self, identity: AgentIdentity) -> Result<(), MobError>
```

  The public MobHandle lifecycle operation is retire, taking canonical AgentIdentity.

- [meerkat-mob/src/runtime/provisioner.rs:1407-1415](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L1407-L1415)

```text
async fn retire_member(
        &self,
        member_ref: &MemberRef,
```

  retire_member is a lower provisioner contract with a different parameter, not the identity-taking MobHandle API advertised alongside force_cancel_member and respawn.

### Independent challenge

Confirmed for the production public API, but the title's blanket 'nonexistent spawn' wording is too broad. MobHandle does have a crate-private cfg(test) spawn helper with three different arguments. That is not the documented spawn(spec) API. Production spec-based spawning is spawn_spec, and canonical-identity retirement is retire; retire_member exists on the lower provisioner/bridge contracts rather than as that handle method.

- [meerkat-mob/src/runtime/handle.rs:9081-9091](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/handle.rs#L9081-L9091)

```text
#[cfg(test)]
    pub(crate) async fn spawn(
```

  This independently found counterexample narrows the claim: the name exists only as a test helper taking profile, identity, and initial message, not a production SpawnMemberSpec.

- [meerkat-mob/src/runtime/handle.rs:9178-9185](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/handle.rs#L9178-L9185)

```text
pub async fn spawn_spec(&self, spec: SpawnMemberSpec) -> Result<SpawnResult, MobError>
```

  This is the actual public fully specified spawn entry point.

- [meerkat-mob/src/runtime/handle.rs:9537-9547](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/handle.rs#L9537-L9547)

```text
pub async fn retire(&self, identity: AgentIdentity) -> Result<(), MobError>
```

  The public handle exposes retire with canonical identity and lowers to MobMachineCommand::Retire.

- [meerkat-mob/src/runtime/provisioner.rs:1407-1415](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L1407-L1415)

```text
member_ref: &MemberRef,
```

  The existing lower-layer retire_member operation takes MemberRef and returns typed disposal; it is not the identity-taking handle operation in the prose.

**Required correction:** Use MobHandle::spawn_spec(spec) in crate_map.md's lifecycle diagram and MobHandle::retire(identity) in mob-orchestration.md's handle lifecycle inventory. Preserve the distinct mob_retire_member tool and provisioner/bridge retire_member names when discussing those layers. Do not claim or attempt to remove the cfg(test) spawn helper.

### Correction

Used the production public MobHandle::spawn_spec(spec) and MobHandle::retire(identity) names. Kept lower MobProvisioner::retire_member and mob_retire_member tool vocabulary distinct, including the different return contracts. Did not remove or make blanket absence claims about the cfg(test) spawn helper.

- [.claude/skills/meerkat-architecture/references/crate_map.md:353-353](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L353)

```text
MobHandle::spawn_spec(spec)
```

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:98-107](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L98-L107)

```text
- `MobHandle::retire(identity)` — terminal disposal barrier for this mob
  incarnation's owned work, removing the roster anchor and runtime binding.
  Mob-owned sessions are archived through their session service: persistent
  services archive durably, while explicitly enabled ephemeral services archive
  only in memory. Adopted host-owned sessions only
  release/unregister their member runtime and binding, not the host's durable
  document. The lower `MobProvisioner::retire_member` contract reports
  `MemberSessionDisposal::Archived` or `RuntimeReleasedOnlyHostOwned`; the
  handle returns `Result<(), MobError>`. This is not a drain guarantee for
  separate event-log projection tasks.
```

### Final independent review

**PASS:** Both production handle names are corrected: the spec-based public API is spawn_spec and identity-based retirement is retire. The lower provisioner/tool names remain distinct. The actual cfg(test), crate-private three-argument spawn helper is neither denied nor removed. Independent follow-up inspection confirms that the shared paragraph now also distinguishes persistent archival, explicitly enabled in-memory ephemeral archival, and adopted-host release-only disposal. It preserves the public Result<(), MobError> versus lower MemberSessionDisposal distinction and the separate event-projection caveat; the earlier A02-013 regression is resolved.

- [.claude/skills/meerkat-architecture/references/crate_map.md:353-353](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L353)

```text
MobHandle::spawn_spec(spec)
```

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:98-107](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L98-L107)

```text
`MobHandle::retire(identity)`
```

- [meerkat-mob/src/runtime/handle.rs:9081-9091](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/handle.rs#L9081-L9091)

```text
#[cfg(test)]
    pub(crate) async fn spawn(
```

- [meerkat-mob/src/runtime/handle.rs:9178-9185](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/handle.rs#L9178-L9185)

```text
pub async fn spawn_spec(&self, spec: SpawnMemberSpec) -> Result<SpawnResult, MobError>
```

- [meerkat-mob/src/runtime/handle.rs:9537-9547](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/handle.rs#L9537-L9547)

```text
pub async fn retire(&self, identity: AgentIdentity) -> Result<(), MobError>
```

- [meerkat-mob/src/runtime/provisioner.rs:1412-1415](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/provisioner.rs#L1412-L1415)

```text
Result<crate::machines::mob_machine::MemberSessionDisposal, MobError>
```

## A02-013: Mob retirement is inaccurately described as always archiving the session

**Candidate severity:** medium. **Independent verdict:** confirm.

The unconditional wording attributes archive authority to the mob for documents owned by another host. Integrators can mistake successful member retirement for a durable document archive or close a host-owned lifecycle they do not own.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:86-86](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L86)

```text
archive session, remove from roster
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:328-330](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L328-L330)

```text
retire existing member (archive session, remove from roster)
```

### Implementation proof

- [meerkat-mob/src/runtime/handle.rs:9517-9537](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/handle.rs#L9517-L9537)

```text
A host-owned adopted session is released
    /// and unregistered without archiving the host's durable document.
```

  The public handle's lifecycle contract explicitly distinguishes host-owned adopted sessions from mob-owned sessions.

- [meerkat-mob/src/runtime/provisioner.rs:3616-3649](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L3616-L3649)

```text
if !archive_authority_owned
```

  The actual disposal branch checks ownership, retires/unregisters the runtime binding, and returns RuntimeReleasedOnlyHostOwned without invoking the subsequent archive path.

- [meerkat-mob/src/runtime/provisioner.rs:1407-1415](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L1407-L1415)

```text
`RuntimeReleasedOnlyHostOwned` when
    /// the session's durable record belongs to another host and only the
    /// runtime session + binding were released.
```

  The distinction is a typed provisioner disposal contract, not an accidental implementation exception.

### Independent challenge

An adopted host-owned session is a supported counterexample to unconditional archival, not an implementation violation of retirement. The disposal code reads ownership before retirement and can return the typed RuntimeReleasedOnlyHostOwned result without entering the session-archive path. Successful member disposal still fences work and removes the member/runtime binding; it does not grant authority over another host's durable document or promise event-projector draining.

- [meerkat-mob/src/runtime/provisioner.rs:3609-3630](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L3609-L3630)

```text
if !archive_authority_owned
```

  Actual disposal branches on ownership obtained from session_known_to_archive_authority before changing the runtime.

- [meerkat-mob/src/runtime/provisioner.rs:3637-3649](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L3637-L3649)

```text
disposal: crate::machines::mob_machine::MemberSessionDisposal::RuntimeReleasedOnlyHostOwned,
```

  The non-owning branch retires and unregisters the runtime, then returns without running the following archive operation.

- [meerkat-mob/src/runtime/handle.rs:9517-9528](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/handle.rs#L9517-L9528)

```text
/// and unregistered without archiving the host's durable document.
```

  The public handle contract explicitly recognizes the host-owned exception and distinguishes the disposal barrier from event-log projection.

- [meerkat-mob/src/runtime/provisioner.rs:1407-1415](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/provisioner.rs#L1407-L1415)

```text
Result<crate::machines::mob_machine::MemberSessionDisposal, MobError>
```

  The contract exposes a typed disposal distinction rather than representing all successful retirement as an archive.

**Required correction:** Qualify both mob-orchestration.md's retirement summary and crate_map.md's respawn-retirement diagram: mob-owned sessions are archived, while adopted host-owned sessions release/unregister only the member runtime and binding. Describe successful retirement as the owned-work/member disposal barrier with the typed disposal distinction, not universal durable archive authority. Coordinate the method-name correction with A02-012; do not promise that unrelated event projection has drained.

### Correction

Qualified both retirement summaries by archive ownership: mob-owned durable sessions archive; adopted host-owned sessions release/unregister only the member runtime and binding. Defined successful retirement as the mob incarnation's owned-work disposal barrier, with lower-level Archived versus RuntimeReleasedOnlyHostOwned outcomes and no promise to drain unrelated event projection. Final-review follow-up: Qualified archival durability by the selected session service: persistent services archive durably, explicitly enabled ephemeral services archive in memory, and adopted host-owned sessions retain the release-only distinction.

- [.claude/skills/meerkat-architecture/references/crate_map.md:361-362](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L361-L362)

```text
  ├── retire existing member and remove its roster/runtime binding
  │     (archive mob-owned session; release/unregister adopted host-owned runtime only)
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:375-379](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L375-L379)

```text
- Successful retirement is the disposal barrier for this mob incarnation's
  owned work. The provisioner's `MemberSessionDisposal` distinguishes
  `Archived` from `RuntimeReleasedOnlyHostOwned`; the latter does not archive
  the host's durable document. Neither promises a drain of separate event-log
  projection tasks.
```

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:101-101](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L101)

```text
services archive durably, while explicitly enabled ephemeral services archive
```

### Final independent review

**PASS:** The follow-up resolves the previously identified durability regression without weakening the confirmed ownership correction. The reference now qualifies archive durability by the session service: successful persistent archival uses the machine/runtime-store lifecycle protocol, while the explicitly opted-in first-party ephemeral service archives in memory. The builder's opt-in guard and both concrete service implementations support that distinction. The provisioner separately retires and unregisters adopted host-owned runtimes without archiving the host's document. Both documentation occurrences retain the owned-work disposal barrier, public versus lower-layer return contracts, and no-event-projector-drain caveat. The persistent SessionService archive fallback fails closed without its required machine protocol; the corrected wording does not advertise a standalone persistent archive bypass. No new inaccuracy was found in the changed surrounding sentences. The initial failure remains preserved in review-history/V02-initial.json.

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:98-107](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L98-L107)

```text
- `MobHandle::retire(identity)` — terminal disposal barrier for this mob
  incarnation's owned work, removing the roster anchor and runtime binding.
  Mob-owned sessions are archived through their session service: persistent
  services archive durably, while explicitly enabled ephemeral services archive
  only in memory. Adopted host-owned sessions only
  release/unregister their member runtime and binding, not the host's durable
  document. The lower `MobProvisioner::retire_member` contract reports
  `MemberSessionDisposal::Archived` or `RuntimeReleasedOnlyHostOwned`; the
  handle returns `Result<(), MobError>`. This is not a drain guarantee for
  separate event-log projection tasks.
```

  The corrected paragraph independently distinguishes archive ownership, persistence capability, API return layers, and the boundary of the retirement guarantee.

- [.claude/skills/meerkat-architecture/references/mob-orchestration.md:9-13](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/mob-orchestration.md?plain=1#L9-L13)

```text
.allow_ephemeral_sessions(true)
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:360-364](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L360-L364)

```text
(archive mob-owned session; release/unregister adopted host-owned runtime only)
```

  This occurrence correctly qualifies ownership without adding the unconditional durability promise.

- [meerkat-mob/src/runtime/builder.rs:6687-6693](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/builder.rs#L6687-L6693)

```text
pub fn allow_ephemeral_sessions(mut self, allow: bool) -> Self
```

- [meerkat-mob/src/runtime/builder.rs:6875-6885](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/builder.rs#L6875-L6885)

```text
if !allow_ephemeral_sessions && !session_service.supports_persistent_sessions()
```

- [meerkat-mob/src/runtime/session_service.rs:1902-1904](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/session_service.rs#L1902-L1904)

```text
fn supports_persistent_sessions(&self) -> bool {
        false
    }
```

  This is the first-party EphemeralSessionService implementation, not a hypothetical custom backend.

- [meerkat-mob/src/runtime/session_service.rs:1994-2005](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/session_service.rs#L1994-L2005)

```text
<Self as SessionService>::archive(self, session_id).await
```

  Mob-authorized ephemeral archive delegates to the ephemeral SessionService archive after optional runtime retirement.

- [meerkat-session/src/ephemeral.rs:5944-5971](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-session/src/ephemeral.rs#L5944-L5971)

```text
archived_views.insert(id.clone(), archived_view);
```

  The method authorizes the lifecycle transition, removes the live in-memory handle, inserts an in-memory archived view, and shuts down the handle; it performs no durable archive write.

- [meerkat-mob/src/runtime/session_service.rs:2739-2755](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/session_service.rs#L2739-L2755)

```text
return meerkat_session::PersistentSessionService::<B>::archive_with_machine_protocol(
```

  The persistent mob service routes archival through the shared machine archive protocol when composed with the runtime adapter.

- [meerkat-session/src/persistent.rs:2939-2967](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-session/src/persistent.rs#L2939-L2967)

```text
.retire_session_with_archive_lease_terminalizing_model_routing_handoffs_before(
```

  Persistent archive realization awaits the runtime-owned transition using the exact archive lease and propagates its error; it does not treat an in-memory archived view as durable authority.

- [meerkat-session/src/persistent.rs:28854-28891](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-session/src/persistent.rs#L28854-L28891)

```text
Some(RuntimeState::Retired),
```

  The existing source test asserts the stored runtime lifecycle terminal after successful archive and separately rejects putting archive authority into the Session body or a second SessionStore authority. Inspected, not executed.

- [meerkat-session/src/persistent.rs:11658-11661](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-session/src/persistent.rs#L11658-L11661)

```text
runtime-backed archive for session {id} requires MachineSessionArchiveProtocol
```

  Without the machine archive protocol the persistent SessionService fallback is Unsupported, not a silent standalone or in-memory substitute for successful durable archive.

- [meerkat-mob/src/runtime/tests.rs:12917-12931](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/tests.rs#L12917-L12931)

```text
.expect("create should allow ephemeral sessions when explicitly enabled");
```

  The repository pins this as a supported explicit opt-in using EphemeralSessionService and in-memory mob storage.

- [meerkat-mob/src/runtime/provisioner.rs:3609-3649](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/provisioner.rs#L3609-L3649)

```text
MemberSessionDisposal::RuntimeReleasedOnlyHostOwned
```

  The separately corrected host-owned exception is supported: it retires/unregisters the binding without entering the durable-document archive branch.

## A02-014: Provider client ownership conflates LlmClient implementations with the AgentLlmClient adapter

**Candidate severity:** medium. **Independent verdict:** confirm.

The same crate map elsewhere correctly lists LlmClientAdapter, but its dependency graph and the navigator ownership table send provider implementors to the wrong trait layer. The distinction matters for provider wire logic versus core request/error adaptation.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/SKILL.md:614-614](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L614)

```text
Implements `AgentLlmClient` via provider-specific crates
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:30-32](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L30-L32)

```text
OpenAI client, including realtime transport — implements AgentLlmClient
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:30-32](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L30-L32)

```text
Gemini client, including inline video — implements AgentLlmClient
```

### Implementation proof

- [meerkat-anthropic/src/client.rs:1325-1330](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-anthropic/src/client.rs#L1325-L1330)

```text
impl LlmClient for AnthropicClient
```

  The provider-native client implements the wire-client abstraction.

- [meerkat-openai/src/client.rs:2220-2226](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-openai/src/client.rs#L2220-L2226)

```text
impl LlmClient for OpenAiClient
```

  OpenAI has the same lower-level contract.

- [meerkat-gemini/src/client.rs:2076-2083](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-gemini/src/client.rs#L2076-L2083)

```text
impl LlmClient for GeminiClient
```

  Gemini likewise does not directly implement the agent-loop abstraction.

- [meerkat-llm-core/src/adapter.rs:549-557](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-llm-core/src/adapter.rs#L549-L557)

```text
impl AgentLlmClient for LlmClientAdapter
```

  The agent-facing implementation belongs to the shared adapter in llm-core.

### Independent challenge

The provider-native text clients implement the wire-facing LlmClient trait. The shared LlmClientAdapter is a distinct struct implementing the core-facing AgentLlmClient trait, and the facade explicitly wraps raw clients with it. 'Through llm-core' is an acceptable high-level description only if it names this adaptation rather than asserting the provider client types themselves implement the core trait. Other AgentLlmClient wrappers/test doubles do not change these concrete client implementations.

- [meerkat-anthropic/src/client.rs:1325-1330](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-anthropic/src/client.rs#L1325-L1330)

```text
impl LlmClient for AnthropicClient
```

  Anthropic's native client implements the lower-level wire contract.

- [meerkat-openai/src/client.rs:2220-2228](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-openai/src/client.rs#L2220-L2228)

```text
impl LlmClient for OpenAiClient
```

  The OpenAI native client has the same trait boundary.

- [meerkat-gemini/src/client.rs:2076-2083](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-gemini/src/client.rs#L2076-L2083)

```text
impl LlmClient for GeminiClient
```

  The Gemini native client likewise implements LlmClient.

- [meerkat-llm-core/src/adapter.rs:549-557](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-llm-core/src/adapter.rs#L549-L557)

```text
impl AgentLlmClient for LlmClientAdapter
```

  The agent-loop contract implementation belongs to the shared adapter.

- [meerkat/src/factory.rs:3966-3973](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L3966-L3973)

```text
LlmClientAdapter::new(client, model.into())
```

  The production facade's build_llm_adapter accepts Arc<dyn LlmClient> and returns the separate adapter, demonstrating the real composition path.

- [meerkat-client/src/lib.rs:14-21](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-client/src/lib.rs#L14-L21)

```text
BlockAssembler, BlockKey, FactoryError, LlmClient, LlmClientAdapter, LlmDoneOutcome, LlmError,
```

  The compatibility client crate re-exports the trait/adapter and conditionally re-exports provider clients; it does not become their implementation owner.

**Required correction:** Correct the provider rows in SKILL.md and crate_map.md to say the native clients implement LlmClient and the agent loop sees them through meerkat-llm-core::LlmClientAdapter implementing AgentLlmClient. Make the three provider descriptions consistent. Preserve meerkat-client's compatibility/re-export role and do not relocate provider-native wire behavior or claim that the adapter is the only possible AgentLlmClient implementation.

### Correction

Made all three native provider descriptions consistently implement LlmClient, with meerkat_llm_core::LlmClientAdapter implementing the agent-facing AgentLlmClient boundary. Preserved provider-native implementation ownership and the meerkat-client compatibility shim; did not claim no other AgentLlmClient implementations can exist.

- [.claude/skills/meerkat-architecture/SKILL.md:636-636](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L636)

```text
| `meerkat-anthropic` / `meerkat-openai` / `meerkat-gemini` | Provider-specific client/runtime implementations | Native clients implement `LlmClient`; `meerkat_llm_core::LlmClientAdapter` adapts them to `AgentLlmClient` |
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:30-32](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L30-L32)

```text
  ├── meerkat-anthropic       (Anthropic streaming client — native client implements LlmClient)
  ├── meerkat-openai          (OpenAI client, including realtime transport — native client implements LlmClient)
  ├── meerkat-gemini          (Gemini client, including inline video — native client implements LlmClient)
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:80-83](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L80-L83)

```text
The provider-native clients implement `LlmClient`. The facade adapts them to
the agent loop through `meerkat_llm_core::LlmClientAdapter`, which implements
`AgentLlmClient`; the `meerkat-client` crate remains a compatibility re-export
shim.
```

### Final independent review

**PASS:** All three provider rows now agree with the actual native client implementations, and the added facade explanation correctly inserts LlmClientAdapter at the core-facing boundary. The compatibility shim still re-exports these surfaces. The correction neither moves provider wire behavior into core nor claims that the adapter is the only possible AgentLlmClient implementor; the target-specific async-trait attributes also remain consistent with wasm use.

- [.claude/skills/meerkat-architecture/SKILL.md:636-636](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L636)

```text
Native clients implement `LlmClient`; `meerkat_llm_core::LlmClientAdapter` adapts them to `AgentLlmClient`
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:30-32](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L30-L32)

```text
native client implements LlmClient
```

- [.claude/skills/meerkat-architecture/references/crate_map.md:80-83](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L80-L83)

```text
the agent loop through `meerkat_llm_core::LlmClientAdapter`, which implements
`AgentLlmClient`
```

- [meerkat-anthropic/src/client.rs:1325-1330](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-anthropic/src/client.rs#L1325-L1330)

```text
impl LlmClient for AnthropicClient
```

- [meerkat-openai/src/client.rs:2220-2226](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/src/client.rs#L2220-L2226)

```text
impl LlmClient for OpenAiClient
```

- [meerkat-gemini/src/client.rs:2076-2081](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-gemini/src/client.rs#L2076-L2081)

```text
impl LlmClient for GeminiClient
```

- [meerkat-llm-core/src/adapter.rs:549-555](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-llm-core/src/adapter.rs#L549-L555)

```text
impl AgentLlmClient for LlmClientAdapter
```

- [meerkat/src/factory.rs:3966-3973](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L3966-L3973)

```text
LlmClientAdapter::new(client, model.into())
```

- [meerkat-client/src/lib.rs:14-21](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-client/src/lib.rs#L14-L21)

```text
BlockAssembler, BlockKey, FactoryError, LlmClient, LlmClientAdapter, LlmDoneOutcome, LlmError,
```

## A02-015: Canonical thin-surface examples use nonexistent REST and RPC operation spellings

**Candidate severity:** low. **Independent verdict:** reject.

The paragraph illustrates actual Meerkat transport syntax, but two names do not match the shipping catalogs. This is a presentation correction only; the normative rule that syntax lowers into shared runtime operations remains unchanged.

### Baseline documentation evidence

- [docs/architecture/meerkat-dogma-commentary.md:1227-1229](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/meerkat-dogma-commentary.md?plain=1#L1227-L1229)

```text
`POST /sessions/{id}/turns`, receive `sessions/create`
```

- [.claude/skills/meerkat-dogma-inquisition/references/meerkat-dogma-commentary.md:1227-1229](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-dogma-inquisition/references/meerkat-dogma-commentary.md?plain=1#L1227-L1229)

```text
`POST /sessions/{id}/turns`, receive `sessions/create`
```

### Implementation proof

- [meerkat-contracts/src/rest_catalog.rs:210-218](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/rest_catalog.rs#L210-L218)

```text
"/sessions/{id}/messages",
```

  The authoritative continuation endpoint is POST /sessions/{id}/messages.

- [meerkat-contracts/src/rpc_catalog.rs:205-212](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/rpc_catalog.rs#L205-L212)

```text
"session/create",
```

  The RPC catalog uses singular session/create.

- [scripts/sync-meerkat-dogma-skill-docs.sh:21-28](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/sync-meerkat-dogma-skill-docs.sh#L21-L28)

```text
CANONICAL_DIR="$ROOT/docs/architecture"
SKILL_REF_DIR="$ROOT/.claude/skills/meerkat-dogma-inquisition/references"
```

  The canonical commentary owns the mirror; the script copies the canonical file into the skill reference.

### Independent challenge

The actual routers do use /sessions/{id}/messages and session/create, but that establishes different shipping spellings, not a false claim in this normative paragraph. Under 'What The Rule Forbids', the cited sentence says a surface 'may' parse or route its own syntax and then requires that syntax to lower to shared authority. It is an illustrative permission, not an endpoint inventory, runnable request, or promise that Meerkat currently serves each example. Treating any permitted/hypothetical syntax as an implementation contract would improperly rewrite doctrine to match today's surface. No factual correction is compelled; no canonical or mirror edit is authorized by this candidate.

- [meerkat-rest/src/lib.rs:2289-2301](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rest/src/lib.rs#L2289-L2301)

```text
.route("/sessions/{id}/messages", post(continue_session))
```

  Independent implementation inspection confirms the real REST continuation route, rather than using another document as proof.

- [meerkat-rpc/src/router.rs:2094-2106](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/router.rs#L2094-L2106)

```text
"session/create" => {
```

  The live RPC dispatch arm confirms the singular shipping name; the rejection does not deny that spelling fact.

- [docs/architecture/meerkat-dogma-commentary.md:1226-1231](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/architecture/meerkat-dogma-commentary.md?plain=1#L1226-L1231)

```text
A surface may classify its own syntax. It may parse `--resume`, route
```

  The target's modal, generic wording establishes an allowable syntax-to-authority boundary. It does not assert that the following example strings form the current public catalog.

- [scripts/sync-meerkat-dogma-skill-docs.sh:21-28](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/sync-meerkat-dogma-skill-docs.sh#L21-L28)

```text
CANONICAL_DIR="$ROOT/docs/architecture"
SKILL_REF_DIR="$ROOT/.claude/skills/meerkat-dogma-inquisition/references"
```

  Canonical/mirror ownership was independently verified. Rejection leaves both copies alone rather than prescribing direct mirror edits.

**Disposition:** Rejected; no correction authorized for this claim.

## A02-016: The concrete prompt-admission trace uses a wrong Input shape and assigns input identity creation to the driver

**Candidate severity:** medium. **Independent verdict:** confirm.

Even allowing schematic call notation, the concrete trace describes the wrong input representation and identity owner. That can cause adapters to regenerate an externally supplied input identity or reimplement admission defaults in the shell.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/machine-system.md:181-182](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L181-L182)

```text
`MeerkatMachine::accept_input_with_completion(session_id, Input::Prompt { content: "Hello" })`
```

- [.claude/skills/meerkat-architecture/references/machine-system.md:182-182](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L182)

```text
**Driver** generates `input_id`, resolves policy
```

### Implementation proof

- [meerkat-runtime/src/input.rs:120-125](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/input.rs#L120-L125)

```text
Prompt(PromptInput),
```

  The canonical Input variant wraps PromptInput; it is not the documented struct variant.

- [meerkat-runtime/src/input.rs:326-344](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/input.rs#L326-L344)

```text
pub fn new(text: impl Into<String>, turn_metadata: Option<RuntimeTurnMetadata>) -> Self
```

  PromptInput::new constructs the header and its InputId before admission, along with typed ContentInput.

- [meerkat-runtime/src/driver/ephemeral.rs:3678-3684](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/driver/ephemeral.rs#L3678-L3684)

```text
let input_id = input.id().clone();
```

  The driver preserves the already supplied identity rather than generating a new one.

- [meerkat-runtime/src/driver/ephemeral.rs:3592-3613](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/driver/ephemeral.rs#L3592-L3613)

```text
let effects = self.dsl_preview(
            Self::resolve_admission_plan_input(&authority),
            "ResolveAdmissionPlan",
        )?;
```

  Admission policy is resolved through the generated machine before queue realization; it is not independently invented by the driver.

### Independent challenge

Even if the call is read as schematic rather than compilable Rust, the accompanying assertion that admission generates input_id is false. PromptInput already carries its identity before admission, and accept_resolved_input clones that identity. The enum also uses a tuple payload, not the displayed struct variant. Narrowing: 'driver resolves policy' need not mean the driver independently owns policy; retain its orchestration role and make the generated ResolveAdmissionPlan feedback explicit instead of alleging a product authority violation.

- [meerkat-runtime/src/input.rs:120-126](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/input.rs#L120-L126)

```text
Prompt(PromptInput),
```

  The public Input enum requires a PromptInput payload rather than Input::Prompt { content: ... }.

- [meerkat-runtime/src/input.rs:324-344](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/input.rs#L324-L344)

```text
id: meerkat_core::lifecycle::InputId::new(),
```

  PromptInput::new creates the input identity with the header before the input reaches the admission driver.

- [meerkat-runtime/src/driver/ephemeral.rs:3678-3687](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/driver/ephemeral.rs#L3678-L3687)

```text
let input_id = input.id().clone();
```

  The actual accepting driver preserves the supplied identity.

- [meerkat-runtime/src/driver/ephemeral.rs:3592-3613](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/driver/ephemeral.rs#L3592-L3613)

```text
Self::resolve_admission_plan_input(&authority),
```

  Admission planning previews the generated ResolveAdmissionPlan input and builds the resolved admission from emitted machine effects.

- [meerkat-runtime/src/meerkat_machine/runtime_control.rs:12763-12771](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/runtime_control.rs#L12763-L12771)

```text
session_id: &SessionId,
```

  The real machine method receives a session reference and already constructed Input, then delegates to its boxed admission implementation.

**Required correction:** Correct steps 3-4 of machine-system.md's concrete Hello trace. Use the actual tuple variant, for example machine.accept_input_with_completion(&session_id, Input::Prompt(PromptInput::new("Hello", None))), and distinguish construction-time input identity from admission-time consumption of that identity. Describe the driver as realizing generated ResolveAdmissionPlan feedback before the queue-lane transition. Keep deliberately abbreviated DSL input notation clearly schematic and preserve the existing machine-owned admission rule.

### Correction

Corrected the Hello trace to Input::Prompt(PromptInput::new("Hello", None)) with a session reference. Separated construction-time identity creation from admission-time identity preservation and generated ResolveAdmissionPlan feedback. Explicitly labeled abbreviated generated-input payloads as schematic and preserved machine-owned admission.

- [.claude/skills/meerkat-architecture/references/machine-system.md:177-178](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L177-L178)

```text
Generated-input payloads below are deliberately abbreviated schematic notation,
not copyable Rust constructors.
```

- [.claude/skills/meerkat-architecture/references/machine-system.md:184-191](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L184-L191)

```text
3. **Accept input**:
   `machine.accept_input_with_completion(&session_id, Input::Prompt(PromptInput::new("Hello", None))).await`
   routes through dispatch → driver. `PromptInput::new` creates the header's
   input identity before admission.
4. **Driver** preserves that supplied identity (`input.id().clone()`) and
   realizes admission policy from generated `ResolveAdmissionPlan` feedback.
   For the authorized queue lane in this trace, it then applies
   `QueueAccepted { input_id, ... }` through `self.dsl_apply(...)`.
```

### Final independent review

**PASS:** The prompt call now uses the real tuple variant, constructor, session reference, and async method. Construction creates the InputHeader identity; admission clones and consumes it rather than minting a replacement. The driver realizes generated admission feedback before the queue-lane transition. The explicit schematic-notation label prevents the abbreviated DSL payloads from being advertised as compilable constructors. The surrounding factory/runtime-backed construction discussion is preserved rather than broadened into a claim that standalone builds require runtime bindings.

- [.claude/skills/meerkat-architecture/references/machine-system.md:177-191](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L177-L191)

```text
`machine.accept_input_with_completion(&session_id, Input::Prompt(PromptInput::new("Hello", None))).await`
```

- [meerkat-runtime/src/input.rs:120-126](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/input.rs#L120-L126)

```text
Prompt(PromptInput)
```

- [meerkat-runtime/src/input.rs:326-342](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/input.rs#L326-L342)

```text
id: meerkat_core::lifecycle::InputId::new(),
```

- [meerkat-runtime/src/driver/ephemeral.rs:3592-3613](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/driver/ephemeral.rs#L3592-L3613)

```text
self.resolved_admission_from_machine_effects(input, authority, effects, false)
```

  The preceding preview is ResolveAdmissionPlan, not a handwritten policy owner.

- [meerkat-runtime/src/driver/ephemeral.rs:3678-3687](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/driver/ephemeral.rs#L3678-L3687)

```text
let input_id = input.id().clone();
```

- [meerkat-runtime/src/driver/ephemeral.rs:1651-1667](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/driver/ephemeral.rs#L1651-L1667)

```text
self.dsl_apply(admission_input, admission_label)?;
```

- [meerkat-runtime/src/meerkat_machine/runtime_control.rs:12763-12771](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/meerkat_machine/runtime_control.rs#L12763-L12771)

```text
pub async fn accept_input_with_completion(
```

## A02-017: The dependency map places meerkat-schedule downstream of the facade even though the facade depends on it

**Candidate severity:** low. **Independent verdict:** confirm.

In the explicitly labeled dependency map, other descendants such as mob and web runtime consume the facade, while schedule is drawn in the same direction despite the reverse manifest dependency. This mislocates the scheduler's reusable feature-owned contract.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/references/crate_map.md:59-69](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L59-L69)

```text
├── meerkat-schedule         (scheduler: once/interval/calendar triggers, occurrence lifecycle,
```

### Implementation proof

- [meerkat/Cargo.toml:28-33](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/Cargo.toml#L28-L33)

```text
meerkat-schedule = { workspace = true }
```

  The facade directly depends on the scheduler.

- [meerkat-schedule/Cargo.toml:15-38](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/Cargo.toml#L15-L38)

```text
meerkat-core = { workspace = true }
```

  The scheduler's complete dependency sections include core, capabilities, optional skills, and machine schema, but no facade dependency. Its domain is a lower-level feature input to facade composition.

### Independent challenge

The section explicitly presents dependency order, and its other child relations place dependents below their substrates. In that convention schedule is on the wrong side of the facade: meerkat directly consumes meerkat-schedule, while the scheduler's complete manifest has no dependency on meerkat. This is a diagram-only correction, not a claim that every listed crate relation is exhaustive or that optional scheduler assets should move into core.

- [meerkat/Cargo.toml:28-35](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/Cargo.toml#L28-L35)

```text
meerkat-schedule = { workspace = true }
```

  The facade directly depends on the feature crate.

- [meerkat-schedule/Cargo.toml:15-46](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/Cargo.toml#L15-L46)

```text
meerkat-core = { workspace = true }
```

  The full ordinary, target-specific, feature, and dev dependency sections contain core/capabilities/schema and optional skills, but no facade dependency. Target or feature selection does not invert this relation.

- [meerkat-mob/Cargo.toml:17-26](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/Cargo.toml#L17-L26)

```text
meerkat = { workspace = true, features = ["comms", "skills", "session-compaction", "memory-store", "anthropic", "openai", "gemini"] }
```

  The neighboring mob descendant really does depend on the facade, confirming the diagram's existing direction rather than assuming indentation means arbitrary grouping.

**Required correction:** Move meerkat-schedule from the facade-dependent branch of crate_map.md's dependency diagram into the lower feature layer consumed by the facade, or replace that edge with an explicit correctly directed dependency arrow. Preserve scheduler ownership and optional assets; no Cargo or product code changes.

### Correction

Moved meerkat-schedule from the facade-dependent branch to the feature layer consumed by the facade, explicitly labeling that direction. Preserved the scheduler's feature-owned lifecycle/tools and all Cargo/product behavior.

- [.claude/skills/meerkat-architecture/references/crate_map.md:43-45](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L43-L45)

```text
  ├── meerkat-workgraph       (realm-scoped durable WorkGraph service, stores, tools, read surface)
  ├── meerkat-schedule        (scheduler: once/interval/calendar triggers, occurrence lifecycle,
                                delivery, schedule tools — consumed by the facade)
```

### Final independent review

**PASS:** The scheduler is now in the feature layer consumed by the facade, with an explicit direction label, and is removed from the facade-dependent branch. The manifests establish that direction: meerkat depends on meerkat-schedule, while the complete schedule manifest depends on core/capabilities/schema and optional skills, not the facade. The diagram correction preserves schedule-owned semantics and optional assets without altering dependencies.

- [.claude/skills/meerkat-architecture/references/crate_map.md:43-45](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/crate_map.md?plain=1#L43-L45)

```text
delivery, schedule tools — consumed by the facade
```

- [meerkat/Cargo.toml:28-35](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/Cargo.toml#L28-L35)

```text
meerkat-schedule = { workspace = true }
```

- [meerkat-schedule/Cargo.toml:15-46](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-schedule/Cargo.toml#L15-L46)

```text
meerkat-skills = { workspace = true, optional = true }
```

  The complete dependency/target/feature inventory has no meerkat facade dependency; skills is explicitly optional.

## A02-018: The claimed runtime_schema_parity coverage exceeds its explicit Phase 1 case inventory

**Candidate severity:** medium. **Independent verdict:** confirm.

The factual assertion about what this named suite already checks is broader than its actual inventory. This does not prove that the omitted machines lack other generated-source or crate-local tests, and it is not a reason to weaken the normative requirement that every canonical implementation match its catalog.

### Baseline documentation evidence

- [.claude/skills/meerkat-architecture/SKILL.md:508-508](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L508)

```text
`runtime_schema_parity` asserts catalog/production schema equality for all canonical machines.
```

- [.claude/skills/meerkat-architecture/references/machine-system.md:101-103](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L101-L103)

```text
catalog/production schemas must match shape for all canonical machines;
```

### Implementation proof

- [meerkat-machine-codegen/tests/runtime_schema_parity.rs:138-191](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/tests/runtime_schema_parity.rs#L138-L191)

```text
fn phase1_schema_parity_cases() -> Vec<SchemaParityCase>
```

  The full authoritative case table enumerates Meerkat, Auth, Mob, Schedule, Occurrence, WorkGraph, WorkAttention, ForkedParticipant, and TemporaryCouncil, rather than deriving all cases from the canonical registry.

- [meerkat-machine-codegen/tests/runtime_schema_parity.rs:193-202](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/tests/runtime_schema_parity.rs#L193-L202)

```text
for case in phase1_schema_parity_cases()
```

  The schema drift report iterates only that explicit table.

- [meerkat-machine-codegen/tests/runtime_schema_parity.rs:414-421](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/tests/runtime_schema_parity.rs#L414-L421)

```text
let failures = phase1_schema_drift_report();
```

  The production schema-shape assertion consumes the bounded Phase 1 report.

- [meerkat-machine-schema/src/catalog/mod.rs:41-58](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/mod.rs#L41-L58)

```text
dsl::dsl_approval_lifecycle_machine(),
        dsl::dsl_detached_job_machine(),
        dsl::dsl_runtime_delivery_machine(),
        dsl::dsl_session_document_machine(),
        dsl::dsl_session_turn_admission_machine(),
```

  These are canonical catalog machines absent from the named suite's case table; WorkExecutionLifecycle is another such entry.

### Independent challenge

Confirmed only for SKILL.md's descriptive assertion that this named test already checks all canonical machines. The file compares production schemas for a closed, explicitly enumerated Phase 1 table, while the canonical registry includes additional machines. Its other canonical-registry use audits projection-module classification, not full production schema equality. Narrowing: machine-system.md says schemas 'must' match for all canonical machines under required ratchets. That is a valid normative requirement and is not disproved by narrower implementation coverage. Do not weaken it or infer that omitted machines have no other tests.

- [meerkat-machine-codegen/tests/runtime_schema_parity.rs:138-191](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/tests/runtime_schema_parity.rs#L138-L191)

```text
fn phase1_schema_parity_cases() -> Vec<SchemaParityCase>
```

  The complete table names Meerkat, Auth, Mob, Schedule, Occurrence, WorkGraph, WorkAttention, ForkedParticipant, and TemporaryCouncil production schema pairs. It is not constructed by iterating the canonical registry.

- [meerkat-machine-codegen/tests/runtime_schema_parity.rs:193-202](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/tests/runtime_schema_parity.rs#L193-L202)

```text
for case in phase1_schema_parity_cases()
```

  The schema drift report iterates only that explicit case table.

- [meerkat-machine-codegen/tests/runtime_schema_parity.rs:413-421](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/tests/runtime_schema_parity.rs#L413-L421)

```text
let failures = phase1_schema_drift_report();
```

  The production schema equality assertion consumes the bounded report, not an independently complete canonical inventory.

- [meerkat-machine-schema/src/catalog/mod.rs:41-58](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/mod.rs#L41-L58)

```text
dsl::dsl_session_document_machine(),
        dsl::dsl_session_turn_admission_machine(),
```

  These canonical entries, among others, have no production pair in the named table, furnishing concrete counterexamples to 'all'.

- [.claude/skills/meerkat-architecture/references/machine-system.md:95-103](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L95-L103)

```text
catalog/production schemas must match shape for all canonical machines;
```

  This cited occurrence is normative rather than an assertion about completed implementation coverage; preserve it while correcting the separate factual overclaim.

**Required correction:** Correct SKILL.md's all-canonical coverage assertion to state that runtime_schema_parity checks the explicitly enumerated Phase 1 production-schema pairs in phase1_schema_parity_cases(). Point to that source inventory instead of creating another hard-coded machine roster. Preserve all-canonical parity as the architectural requirement, including the 'must match' sentence in machine-system.md; that sentence does not require a fix. Do not imply omitted machines are wholly untested or request product/test expansion as part of this documentation-only candidate.

### Correction

Narrowed the descriptive runtime_schema_parity coverage claim to phase1_schema_parity_cases() and linked the actual source inventory without copying another roster. Preserved the all-canonical normative requirement, including machine-system.md's unchanged 'must match' sentence; made no claim that omitted machines lack other tests and requested no test expansion.

- [.claude/skills/meerkat-architecture/SKILL.md:526-530](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L526-L530)

```text
- `runtime_schema_parity` checks the explicitly enumerated Phase 1
  production-schema pairs in `phase1_schema_parity_cases()`
  (`meerkat-machine-codegen/tests/runtime_schema_parity.rs`). Equality remains
  required for all canonical machines; this suite's case table is not an
  exhaustive catalog-coverage claim.
```

### Final independent review

**PASS:** The descriptive coverage claim now points to the exact phase1_schema_parity_cases inventory rather than asserting exhaustive canonical-machine coverage or duplicating a roster. The actual test obtains its comparisons from that table. The all-canonical normative parity rule remains explicitly required in SKILL.md and unchanged in machine-system.md, as the narrowed verdict demanded. Nothing implies that omitted machines lack other tests or requires unrelated product/test expansion.

- [.claude/skills/meerkat-architecture/SKILL.md:526-530](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/SKILL.md?plain=1#L526-L530)

```text
Equality remains
  required for all canonical machines; this suite's case table is not an
  exhaustive catalog-coverage claim.
```

- [.claude/skills/meerkat-architecture/references/machine-system.md:101-103](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-architecture/references/machine-system.md?plain=1#L101-L103)

```text
catalog/production schemas must match shape for all canonical machines
```

- [meerkat-machine-codegen/tests/runtime_schema_parity.rs:138-191](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-codegen/tests/runtime_schema_parity.rs#L138-L191)

```text
fn phase1_schema_parity_cases() -> Vec<SchemaParityCase>
```

- [meerkat-machine-codegen/tests/runtime_schema_parity.rs:193-202](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-codegen/tests/runtime_schema_parity.rs#L193-L202)

```text
for case in phase1_schema_parity_cases()
```

- [meerkat-machine-codegen/tests/runtime_schema_parity.rs:413-421](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-codegen/tests/runtime_schema_parity.rs#L413-L421)

```text
let failures = phase1_schema_drift_report();
```

- [meerkat-machine-schema/src/catalog/mod.rs:41-59](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/mod.rs#L41-L59)

```text
dsl::dsl_session_document_machine(),
```

  The canonical registry contains additional machines beyond the test's explicit production-schema pair table; the fix does not turn that observation into an absence-of-all-testing claim.

## Lane limitations

- **Audit:** This is the AUDIT cohort artifact only. Findings are candidates awaiting independent challenge; no self-adjudication, implementation, product changes, or repository validation builds were performed.
- **Audit:** Coverage means the complete assigned prose was read and relevant concrete claims were traced to inspected source, manifests, generated catalogs, and test definitions. It does not claim execution of every advertised feature, platform, concurrency interleaving, provider, or CI lane.
- **Audit:** The historical meerkat-mobkit source/test claims in gotchas.md concern an external repository not present in this checkout. They were not treated as missing local files or disproved without external evidence.
- **Audit:** Normative doctrine and hypothetical violation/repair examples were distinguished from shipping API assertions. Apparent implementation departures from rules such as core purity or singular authority were not converted into documentation changes that normalize a code bug.
- **Audit:** The canonical dogma/commentary are docs/architecture/meerkat-dogma.md and docs/architecture/meerkat-dogma-commentary.md. Their skill references are byte-identical mirrors maintained by scripts/sync-meerkat-dogma-skill-docs.sh; any confirmed correction must be canonical-source-first.
- **Audit:** No assigned file is a symlink. Assigned hashes matched the supplied manifest when inspected. Concrete repository references were checked as applicable; the assigned files contain no ordinary inline Markdown links of the [label](relative/path) form.
- **Audit:** Release-delta and addition subsections have historical context inside a current navigator. Corrections involving removed APIs should preserve that context by marking supersession and naming the current seam, rather than rewriting a historical release claim without qualification.
- **Challenge:** Independent source-only challenge of all 18 supplied candidates at git HEAD 56208b9e6cee078f52c43af8f6b36660bc93eeb4: 16 confirmed, 2 rejected (A02-007 and A02-015). No new findings were added. All 13 assigned document hashes matched manifest-A02.json before writing this artifact.
- **Challenge:** No builds, test executions, dependency installations, provider calls, nested agents, commits, or repository edits were performed. Test source is cited as an inspected contract, never as an executed result. Only challenge-A02.json was written.
- **Challenge:** Loaded skill/doctrine instructions were not treated as implementation proof. Decisions use directly inspected Rust definitions, call paths, cfg/features, Cargo manifests, workflow wiring, and target-document context.
- **Challenge:** A02-001 and A02-002 are confirmed for current integration guidance, with historical delta entries restricted to explicit supersession annotations. A02-012 is narrowed because a cfg(test), crate-private spawn helper exists. A02-018 is narrowed to the descriptive SKILL.md claim; the normative all-canonical requirement remains intact.
- **Challenge:** Optional compositions remain optional: ops durability acknowledgement applies when persistence is wired; mob tool inventory applies to a surface composed with generated authority; the generic machine interpreter remains available under test-oracle. Host-owned adoption does not transfer durable archive authority.
- **Challenge:** The commentary mirror is byte-identical to its canonical source. scripts/sync-meerkat-dogma-skill-docs.sh owns canonical docs/architecture to repo-local skill synchronization. A02-015 is rejected, so no mirror or canonical commentary change is authorized by this ledger.
