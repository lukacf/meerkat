# A17: Imported MobKit documentation and its publication provenance

This ledger preserves the baseline evidence and independent disposition. See [the audit index](README.md) for scope, commits, and publication boundaries.

## A17-001: Three pages describe the already-fixed empty roster-context bug as current behavior

**Candidate severity:** medium. **Independent verdict:** confirm.

The notes direct integrators to work around a bug that is not present in the pinned gateway/SDK combination, contradicting the later typed-context guidance. A Python RosterContext is also not the raw empty dict claimed here. The valid requirement to declare members separately from profiles should remain.

### Baseline documentation evidence

- [docs/mobkit/quickstart.mdx:191-195](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/quickstart.mdx?plain=1#L191-L195)

```text
The roster callback's `context` argument is
empty on both SDKs (the gateway sends the definition, the SDK dispatchers do
not pass it through)
```

- [docs/mobkit/concepts/roster.mdx:44-54](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/roster.mdx?plain=1#L44-L54)

```text
The roster callback does not hand you the definition.
```

- [docs/mobkit/api/rpc.mdx:314-320](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L314-L320)

```text
provider `params.context`, which the gateway does not send, so the provider's
`context` argument is empty on both SDKs
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/identity_first/gateway_bridges.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '390,403p'
```

  Lines 392-398 serialize the supplied RosterContext, build `let params = json!({ "context": context });`, and call `callback/roster_provider/roster` with that nested object.

  The pinned gateway sends exactly the context envelope these paragraphs say is absent.

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/sdk/python/meerkat_mobkit/agent_builder.py?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '749,758p'; gh api 'repos/lukacf/meerkat-mobkit/contents/sdk/typescript/src/agent-builder.ts?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '517,525p'
```

  Python lines 753-757 construct `RosterContext.from_dict(params.get("context") or {})` and pass it to the provider. TypeScript lines 521-524 call `parseRosterContext(params.context ?? {})` and pass the parsed context.

  Both SDKs consume the actual envelope. This is a completed wire fix at the documented release, not a proposed future capability.

### Independent challenge

The alleged missing context envelope is already fixed in the pinned gateway. Both exact-version SDK dispatchers read the nested context that it sends; Python constructs RosterContext rather than handing the callback an empty dictionary. The source also contains a regression checking populated definition and previous identities. Preserve the separate, valid rule that profiles do not automatically instantiate members, and do not promise that every arbitrary host-provided context contains a definition.

- [meerkat-mobkit/src/identity_first/gateway_bridges.rs:392-403](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/identity_first/gateway_bridges.rs#L392-L403)

```text
let params = json!({ "context": context });
```

  The active roster callback uses the exact envelope the docs say is absent.

- [sdk/python/meerkat_mobkit/agent_builder.py:753-758](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/sdk/python/meerkat_mobkit/agent_builder.py#L753-L758)

```text
context = RosterContext.from_dict(params.get("context") or {})
```

  Python consumes the supplied nested context and passes the typed value to the provider.

- [sdk/typescript/src/agent-builder.ts:517-525](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/sdk/typescript/src/agent-builder.ts#L517-L525)

```text
const context = parseRosterContext(params.context ?? {});
```

  TypeScript likewise forwards the parsed context, disproving the two-SDK limitation.

**Required correction:** Remove the obsolete empty-context/KeyError claim and mandatory host-side workaround from all three canonical pages. Document the actual typed roster context, definition and previous-identities fields, while retaining explicit member declaration and any optional host-controlled roster strategy.

### Correction

Removed all three obsolete empty-context claims. Documented the nested callback context, typed Python and TypeScript field spellings, booted definition and optional-definition custom-host case. Independently reviewed follow-up qualifies previous_identities as call-site-dependent in both prose and the existing field table: topology snapshots supply registered identities, but bootstrap/full roster refresh/reconcile/reset-adoption pass an empty list even if identities exist. Kept explicit member declaration and the host-owned parsed-TOML roster as an optional strategy.

- [docs/quickstart.mdx:189-190](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/quickstart.mdx#L189-L190)

```text
The roster callback receives a typed
`RosterContext`: Python exposes `mob_definition` and `previous_identities`;
```

- [docs/concepts/roster.mdx:44-45](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/roster.mdx#L44-L45)

```text
nested under `params.context`, and both SDK dispatchers pass the parsed
context to the provider:
```

- [docs/api/rpc.mdx:314-314](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L314-L314)

```text
under `params.context` and expects a `DurableAgentSpec[]` reply.
```

- [meerkat-mobkit/src/identity_first/gateway_bridges.rs:395-395](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/identity_first/gateway_bridges.rs#L395-L395)

```text
let params = json!({ "context": context });
```

- [docs/concepts/roster.mdx:56-56](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/roster.mdx#L56-L56)

```text
An empty list is not proof that no identities are registered,
```

- [docs/concepts/roster.mdx:466-466](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/roster.mdx#L466-L466)

```text
| `previous_identities` | Call-site-dependent context, not a complete membership snapshot.
```

- [meerkat-mobkit/src/identity_first/runtime.rs:1280-1285](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/identity_first/runtime.rs#L1280-L1285)

```text
let roster = match self
            .roster_provider
            .roster(&RosterContext {
                mob_definition: self.mob_definition.clone(),
                previous_identities: Vec::new(),
            })
```

### Final independent review

**PASS:** The initial final-review failure is resolved. Independent reinspection of the final text and scoped diff confirms that the early roster paragraph, existing roster field-table row and RPC paragraph now distinguish populated topology-query/edge-reconciliation snapshots from empty bootstrap, full roster refresh/reconcile and reset-time roster-adoption contexts. Those distinctions match the current call sites: topology_snapshot_inputs collects registered identities, while the other cited paths explicitly construct an empty vector. Both paragraphs warn against inferring an empty runtime from an empty list. Quickstart only names the typed fields and makes no inventory-completeness promise. The corrected nested SDK envelope, optional-definition qualification and explicit member declaration remain intact; the cited implementation files are unchanged.

- [docs/quickstart.mdx:189-192](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/quickstart.mdx#L189-L192)

```text
one member per profile for you. The roster callback receives a typed
`RosterContext`: Python exposes `mob_definition` and `previous_identities`;
TypeScript exposes `mobDefinition` and `previousIdentities`. The bundled
`rpc_gateway` supplies its booted definition, while custom hosts may omit it.
```

- [docs/concepts/roster.mdx:50-57](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/roster.mdx#L50-L57)

```text
The bundled gateway supplies its booted, compiled definition as a JSON object,
including `profiles`. The definition is optional for custom hosts and test
harnesses (`None` in Python, `null` in TypeScript). The `previous_identities`
list depends on the callback path: topology-query/edge-reconciliation
snapshots supply registered identities, while bootstrap, full roster
refresh/reconcile, and reset-time roster adoption currently pass an empty
list. An empty list is not proof that no identities are registered, and the
context does not instruct the host to spawn one member per profile.
```

- [docs/concepts/roster.mdx:466-466](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/roster.mdx#L466-L466)

```text
| `previous_identities` | Call-site-dependent context, not a complete membership snapshot. Topology-query/edge-reconciliation snapshots supply registered identities; bootstrap, full roster refresh/reconcile, and reset-time roster adoption currently pass an empty list, even if identities are already registered. |
```

- [docs/api/rpc.mdx:318-324](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L318-L324)

```text
booted definition; a custom host may supply no definition.
`previous_identities` depends on the callback path: topology-query and
edge-reconciliation snapshots supply registered identities, while bootstrap,
full roster refresh/reconcile, and reset-time roster adoption currently pass
an empty list even if identities are already registered. Do not treat an
empty list as proof of an empty runtime. The host still declares members
explicitly, whether it uses this context or its own parsed configuration.
```

- [meerkat-mobkit/src/identity_first/gateway_bridges.rs:393-398](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/identity_first/gateway_bridges.rs#L393-L398)

```text
        let context = serde_json::to_value(context)
            .map_err(|e| RosterError::Io(format!("serialize roster context: {e}")))?;
        let params = json!({ "context": context });
        let result = self
            .bridge
            .call("callback/roster_provider/roster", params)
```

- [sdk/python/meerkat_mobkit/agent_builder.py:756-758](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/python/meerkat_mobkit/agent_builder.py#L756-L758)

```text
            context = RosterContext.from_dict(params.get("context") or {})
            specs = await provider.roster(context)
            return [s.to_dict() for s in specs]
```

- [sdk/typescript/src/agent-builder.ts:523-525](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/typescript/src/agent-builder.ts#L523-L525)

```text
      const context = parseRosterContext(params.context ?? {});
      const specs = await this._rosterProvider.roster(context);
      return specs.map(durableAgentSpecToDict);
```

- [meerkat-mobkit/src/identity_first/runtime.rs:1050-1056](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/identity_first/runtime.rs#L1050-L1056)

```text
        let previous_identities = self.runtime.registered_identities().await;
        let roster = self
            .roster_provider
            .roster(&RosterContext {
                mob_definition: self.mob_definition.clone(),
                previous_identities,
            })
```

- [meerkat-mobkit/src/identity_first/runtime.rs:1280-1285](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/identity_first/runtime.rs#L1280-L1285)

```text
        let roster = match self
            .roster_provider
            .roster(&RosterContext {
                mob_definition: self.mob_definition.clone(),
                previous_identities: Vec::new(),
            })
```

- [meerkat-mobkit/src/unified_runtime/mod.rs:1214-1224](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/unified_runtime/mod.rs#L1214-L1224)

```text
    pub async fn refresh_desired_topology(
        &self,
    ) -> Result<
        Option<crate::identity_first::RestoreFlowResult>,
        crate::identity_first::IdentityRuntimeError,
    > {
        match self.identity_first_context.as_ref() {
            Some(ctx) => ctx.refresh_desired_topology_tracked().await.map(Some),
            None => Ok(None),
        }
    }
```

- [meerkat-mobkit/src/identity_first/runtime.rs:6669-6673](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/identity_first/runtime.rs#L6669-L6673)

```text
        match roster_provider
            .roster(&RosterContext {
                mob_definition,
                previous_identities: Vec::new(),
            })
```

- [meerkat-mobkit/src/bin/rpc_gateway.rs:13038-13041](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/bin/rpc_gateway.rs#L13038-L13041)

```text
            .roster(&RosterContext {
                mob_definition: Some(mob_definition.clone()),
                previous_identities: Vec::new(),
            })
```

## A17-002: The TypeScript access-control example calls nonexistent builder methods

**Candidate severity:** medium. **Independent verdict:** confirm.

The access-control setup fails TypeScript type checking and cannot run as written even with valid configuration paths.

### Baseline documentation evidence

- [docs/mobkit/concepts/access-control.mdx:117-122](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/access-control.mdx?plain=1#L117-L122)

```text
const mobkit = await MobKit.builder()
  .mobConfig("config/mob.toml")
  .accessControl("config/access.toml") // auto-discovered if the file exists
  .start();
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/sdk/typescript/src/builder.ts?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '202,209p;270,280p;787,793p'
```

  MobKitBuilder defines `mob(configPath: string): this` at line 206, `accessControl(configPath: string): this` at line 274, and `async build(): Promise<MobKitRuntime>` at line 787. Inspection of the complete class and its method inventory found no mobConfig or start methods; it does not inherit another builder.

  The authoritative builder class exposes mob/build, not the two names in the example.

### Independent challenge

The complete exported builder class exposes mob, accessControl and build, not mobConfig or start; MobKit.builder() returns that class directly. No inheritance, declaration-merging method, or alias in the pinned TypeScript source rescues the snippet. A working launch should also show the gateway prerequisite instead of silently constructing an unconnected runtime.

- [sdk/typescript/src/builder.ts:202-209](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/sdk/typescript/src/builder.ts#L202-L209)

```text
mob(configPath: string): this {
```

  This is the concrete configuration method; mobConfig is only a field-name concept, not a callable alias.

- [sdk/typescript/src/builder.ts:787-793](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/sdk/typescript/src/builder.ts#L787-L793)

```text
async build(): Promise<MobKitRuntime> {
```

  build is the terminal builder method.

- [sdk/typescript/src/builder.ts:856-860](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/sdk/typescript/src/builder.ts#L856-L860)

```text
return new MobKitBuilder();
```

  There is no alternate builder returned by the advertised factory.

**Required correction:** Change the canonical TypeScript access-control snippet to .mob(...).accessControl(...).gateway(...).build(), or explicitly establish an equivalent gateway prerequisite; keep the valid accessControl spelling.

### Correction

Replaced nonexistent TypeScript mobConfig/start methods with mob/accessControl/gateway/build and linked the gateway binary prerequisite.

- [docs/concepts/access-control.mdx:118-122](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/access-control.mdx#L118-L122)

```text
const mobkit = await MobKit.builder()
  .mob("config/mob.toml")
  .accessControl("config/access.toml") // auto-discovered if the file exists
  .gateway("/path/to/rpc_gateway")
  .build();
```

- [sdk/typescript/src/builder.ts:799-799](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/typescript/src/builder.ts#L799-L799)

```text
async build(): Promise<MobKitRuntime> {
```

### Final independent review

**PASS:** The TypeScript example now uses the exported concrete builder's actual mob, accessControl, gateway and build methods, with the correct argument and return shapes. Each fluent setter returns this, and build returns the runtime promise. The explicit gateway path removes the unconnected-runtime prerequisite gap; the added /quickstart#install link resolves to the installation section that explains obtaining that binary. The valid accessControl spelling and file-discovery behavior are preserved.

- [docs/concepts/access-control.mdx:118-122](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/access-control.mdx#L118-L122)

```text
const mobkit = await MobKit.builder()
  .mob("config/mob.toml")
  .accessControl("config/access.toml") // auto-discovered if the file exists
  .gateway("/path/to/rpc_gateway")
  .build();
```

- [sdk/typescript/src/builder.ts:208-211](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/typescript/src/builder.ts#L208-L211)

```text
  mob(configPath: string): this {
    this._config.mobConfigPath = configPath;
    return this;
  }
```

- [sdk/typescript/src/builder.ts:276-279](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/typescript/src/builder.ts#L276-L279)

```text
  accessControl(configPath: string): this {
    this._config.accessConfigPath = configPath;
    return this;
  }
```

- [sdk/typescript/src/builder.ts:729-732](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/typescript/src/builder.ts#L729-L732)

```text
  gateway(binPath: string): this {
    this._config.gatewayBin = binPath;
    return this;
  }
```

- [sdk/typescript/src/builder.ts:799-804](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/typescript/src/builder.ts#L799-L804)

```text
  async build(): Promise<MobKitRuntime> {
    this._validateConfig();
    this._applyConventionDefaults();
    // Dynamic import to break circular dep (runtime imports from builder config type)
    const { MobKitRuntime } = await import("./runtime.js");
    return MobKitRuntime._create(this._config);
```

## A17-003: The gating/evaluate RPC reference documents the wrong request and response contract

**Candidate severity:** medium. **Independent verdict:** confirm.

Both directions of the advertised API are incompatible with the exact pinned implementation. The accurate gating concept page does not repair the wrong RPC reference for clients using its parameter and response tables.

### Baseline documentation evidence

- [docs/mobkit/api/rpc.mdx:653-656](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L653-L656)

```text
| `action_id` | `string` | yes | Unique action identifier |
```

- [docs/mobkit/api/rpc.mdx:655-656](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L655-L656)

```text
| `action_type` | `string` | yes | Action category (e.g. `"delivery.send"`) |
```

- [docs/mobkit/api/rpc.mdx:661-665](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L661-L665)

```text
  "risk_tier": "safe_dispatch",
  "explanation": "Low-risk internal delivery",
  "recommended_handling": "auto_approve"
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/rpc/gating_methods.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '91,113p;261,270p'
```

  The live parser requires nonempty `action`, nonempty `actor_id`, and `risk_tier`; the tier parser accepts r0, r1, r2, or r3. Missing action is an ActionRequired error before evaluation.

  Requests assembled using only the published required fields do not satisfy the actual parser.

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '882,932p'
```

  GatingEvaluateRequest has action/actor_id/risk_tier plus rationale, requested_approver, approval routing/timeout, entity, and topic options. GatingEvaluateResult has action_id/action/actor_id/risk_tier/outcome/pending_id/fallback_reason. GatingOutcome serializes as allowed, allowed_with_audit, pending_approval, or safe_draft.

  safe_dispatch, explanation, and recommended_handling are not this endpoint's result contract; action_id belongs to the generated result.

### Independent challenge

The active RPC parsers require action and actor_id, plus risk_tier after any configured gateway override. Neither action_type nor caller action_id substitutes for action. The serializable result has a generated action_id and typed outcome, not explanation/recommended_handling or a safe_dispatch tier. Narrow the risk_tier wording to acknowledge that a configured gateway can supply it for a matching action.

- [meerkat-mobkit/src/rpc/gating_methods.rs:91-113](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/rpc/gating_methods.rs#L91-L113)

```text
.ok_or(GatingParamsError::ActionRequired)?;
```

  The parser reads action, actor_id and risk_tier directly; the documented alternative keys cannot satisfy it.

- [meerkat-mobkit/src/runtime.rs:882-932](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime.rs#L882-L932)

```text
pub outcome: GatingOutcome,
```

  The complete request/result types and snake-case enums define r0/r1/r2/r3 and allowed/allowed_with_audit/pending_approval/safe_draft.

**Required correction:** Replace the evaluate request table and response example with the pinned GatingEvaluateRequest/GatingEvaluateResult contract, including real optional inputs and the generated action_id. Explain configured gateway tier overrides without implying a missing action is inferred.

### Correction

Replaced gating/evaluate request fields with action, actor_id, risk_tier and all seven supported optional inputs. Added the complete seven-field result example, generated action_id and real outcome vocabulary. Scoped configured tier overrides to matching actions without inventing an action.

- [docs/api/rpc.mdx:657-657](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L657-L657)

```text
| `action` | `string` | yes | Non-empty action name (e.g. `"delivery.send"`) |
```

- [docs/api/rpc.mdx:677-683](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L677-L683)

```text
"action_id": "gate-action-000001",
  "action": "delivery.send",
  "actor_id": "agent-1",
  "risk_tier": "r0",
  "outcome": "allowed",
  "pending_id": null,
  "fallback_reason": null
```

- [meerkat-mobkit/src/runtime.rs:931-936](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime.rs#L931-L936)

```text
pub struct GatingEvaluateResult {
    pub action_id: String,
    pub action: String,
    pub actor_id: String,
    pub risk_tier: GatingRiskTier,
    pub outcome: GatingOutcome,
```

### Final independent review

**PASS:** The evaluate table now has the parser's action and actor_id inputs, the r0/r1/r2/r3 tier vocabulary, and all seven optional request fields with matching types. The seven-field JSON result uses the serialized result type, including nullable pending_id and fallback_reason and a runtime-generated action_id. The outcome vocabulary matches the snake-case enum. The separate configured-policy explanation correctly makes a supplied tier optional only for a matching gateway action policy and does not infer an absent action.

- [docs/api/rpc.mdx:657-666](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L657-L666)

```text
| `action` | `string` | yes | Non-empty action name (e.g. `"delivery.send"`) |
| `actor_id` | `string` | yes | Non-empty identifier of the agent or user performing the action |
| `risk_tier` | `string` | yes, unless supplied by configured gateway policy | `"r0"`, `"r1"`, `"r2"`, or `"r3"` |
| `rationale` | `string` | no | Explanation for the proposed action |
| `requested_approver` | `string` | no | Approver required for a pending approval |
| `approval_recipient` | `string` | no | Recipient for routing an approval request |
| `approval_channel` | `string` | no | Channel for routing an approval request |
| `approval_timeout_ms` | `u64` | no | Requested approval timeout in milliseconds; runtime bounds apply |
| `entity` | `string` | no | Entity for the memory-conflict check, paired with `topic` |
| `topic` | `string` | no | Topic for the memory-conflict check, paired with `entity` |
```

- [docs/api/rpc.mdx:677-683](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L677-L683)

```text
  "action_id": "gate-action-000001",
  "action": "delivery.send",
  "actor_id": "agent-1",
  "risk_tier": "r0",
  "outcome": "allowed",
  "pending_id": null,
  "fallback_reason": null
```

- [meerkat-mobkit/src/runtime.rs:901-919](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime.rs#L901-L919)

```text
pub struct GatingEvaluateRequest {
    pub action: String,
    pub actor_id: String,
    pub risk_tier: GatingRiskTier,
    #[serde(default)]
    pub rationale: Option<String>,
    #[serde(default)]
    pub requested_approver: Option<String>,
    #[serde(default)]
    pub approval_recipient: Option<String>,
    #[serde(default)]
    pub approval_channel: Option<String>,
    #[serde(default)]
    pub approval_timeout_ms: Option<u64>,
    #[serde(default)]
    pub entity: Option<String>,
    #[serde(default)]
    pub topic: Option<String>,
}
```

- [meerkat-mobkit/src/runtime.rs:922-941](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime.rs#L922-L941)

```text
#[serde(rename_all = "snake_case")]
pub enum GatingOutcome {
    Allowed,
    AllowedWithAudit,
    PendingApproval,
    SafeDraft,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct GatingEvaluateResult {
    pub action_id: String,
    pub action: String,
    pub actor_id: String,
    pub risk_tier: GatingRiskTier,
    pub outcome: GatingOutcome,
    #[serde(default)]
    pub pending_id: Option<String>,
    #[serde(default)]
    pub fallback_reason: Option<String>,
}
```

- [meerkat-mobkit/src/runtime/gating.rs:159-160](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/gating.rs#L159-L160)

```text
        let action_sequence = self.next_gating_sequence();
        let action_id = format!("gate-action-{action_sequence:06}");
```

## A17-004: The gating/decide reference uses decided_by and deny instead of the accepted contract

**Candidate severity:** medium. **Independent verdict:** confirm.

Following the table fails to identify an approver, and the advertised rejection spelling is not accepted. The supported escalation decision is also omitted.

### Baseline documentation evidence

- [docs/mobkit/api/rpc.mdx:676-678](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L676-L678)

```text
| `decision` | `string` | yes | `"approve"` or `"deny"` |
| `decided_by` | `string` | yes | Who made the decision |
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/rpc/gating_methods.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '198,235p;273,281p'
```

  The parser requires pending_id and approver_id, reads optional reason, and accepts decision values approve, reject, and escalate. `deny` falls into UnknownDecision; `decided_by` does not supply approver_id.

  This is the live RPC parser, not merely an internal enum that may differ from the wire.

### Independent challenge

The decide parser explicitly reads approver_id and accepts approve/reject/escalate after case/whitespace normalization. That normalization does not make deny an alias, and decided_by cannot populate approver_id. The optional reason is supported.

- [meerkat-mobkit/src/rpc/gating_methods.rs:198-235](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/rpc/gating_methods.rs#L198-L235)

```text
.get("approver_id")
```

  pending_id and approver_id are required; reason is optional.

- [meerkat-mobkit/src/rpc/gating_methods.rs:273-281](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/rpc/gating_methods.rs#L273-L281)

```text
"reject" => Ok(GatingDecision::Reject),
```

  The exhaustive string parser supplies the positive accepted set and rejects deny.

**Required correction:** Use approver_id, document approve/reject/escalate and optional reason, and remove deny as a claimed wire value.

### Correction

Corrected gating/decide to approver_id, approve/reject/escalate and optional reason; removed deny and decided_by from the advertised wire contract.

- [docs/api/rpc.mdx:701-703](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L701-L703)

```text
| `decision` | `string` | yes | `"approve"`, `"reject"`, or `"escalate"` |
| `approver_id` | `string` | yes | Non-empty identifier of the decision maker |
| `reason` | `string` | no | Explanation for the decision |
```

- [meerkat-mobkit/src/rpc/gating_methods.rs:278-278](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/rpc/gating_methods.rs#L278-L278)

```text
"escalate" => Ok(GatingDecision::Escalate),
```

### Final independent review

**PASS:** The decide table now requires pending_id, approver_id and decision, exposes optional reason, and advertises exactly the parser's approve/reject/escalate vocabulary. The parser's case and whitespace normalization does not provide a deny alias; that obsolete value and decided_by were removed from this method's contract. Both the wire parser and request type support the replacement.

- [docs/api/rpc.mdx:700-703](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L700-L703)

```text
| `pending_id` | `string` | yes | ID of the pending entry |
| `decision` | `string` | yes | `"approve"`, `"reject"`, or `"escalate"` |
| `approver_id` | `string` | yes | Non-empty identifier of the decision maker |
| `reason` | `string` | no | Explanation for the decision |
```

- [meerkat-mobkit/src/runtime.rs:973-979](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime.rs#L973-L979)

```text
pub struct GatingDecideRequest {
    pub pending_id: String,
    pub approver_id: String,
    pub decision: GatingDecision,
    #[serde(default)]
    pub reason: Option<String>,
}
```

- [meerkat-mobkit/src/rpc/gating_methods.rs:273-282](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/rpc/gating_methods.rs#L273-L282)

```text
fn parse_gating_decision(raw: &str) -> Result<GatingDecision, GatingParamsError> {
    let normalized = raw.trim().to_ascii_lowercase();
    match normalized.as_str() {
        "approve" => Ok(GatingDecision::Approve),
        "reject" => Ok(GatingDecision::Reject),
        "escalate" => Ok(GatingDecision::Escalate),
        "" => Err(GatingParamsError::DecisionRequired),
        _ => Err(GatingParamsError::UnknownDecision(raw.to_string())),
    }
}
```

## A17-005: Configuration reference presents authoritative gating tiers as missing-value defaults

**Candidate severity:** medium. **Independent verdict:** confirm.

The runtime-options contract omits the materially important override behavior, suggesting that an explicit caller value is retained. This affects policy expectations and interpreting evaluation results.

### Baseline documentation evidence

- [docs/mobkit/reference/configuration.mdx:103-103](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/configuration.mdx?plain=1#L103)

```text
supplies a configured `risk_tier` for `mobkit/gating/evaluate` requests that omit `risk_tier`.
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/bin/rpc_gateway.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '6734,6771p'
```

  For a configured matching action, apply_gateway_runtime_config_to_request unconditionally inserts the configured risk_tier at line 6770. Lines 6757-6768 warn when a supplied tier differs before replacing it. There is no missing-field-only condition.

  The configuration binds even callers that provide a different tier; it is not merely a defaulting rule.

### Independent challenge

Narrow to a material omission, not a literally false claim that omitted tiers are supplied. The documented omission case does work, but matching configured action tiers also replace explicit caller tiers. The source and its regression distinguish that binding policy from mere defaulting, while unknown actions retain a supplied tier.

- [meerkat-mobkit/src/bin/rpc_gateway.rs:6743-6771](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/bin/rpc_gateway.rs#L6743-L6771)

```text
params.insert("risk_tier".to_string(), Value::String(risk_tier.clone()));
```

  A matching configured action unconditionally inserts its tier and logs a differing string claim.

- [meerkat-mobkit/src/bin/rpc_gateway.rs:857-889](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/bin/rpc_gateway.rs#L857-L889)

```text
"configured policy must win over a caller-supplied tier"
```

  The checked regression source covers explicit override, omission, and unknown-action preservation; it was inspected, not executed.

**Required correction:** State in the canonical configuration row that a matching configured action risk tier overrides caller input as well as supplying an omitted value. Do not claim every action is overridden.

### Correction

Clarified that a matching configured action tier overrides caller input as well as supplying an omitted tier, while unmatched actions still require the caller's tier.

- [docs/reference/configuration.mdx:101-101](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/configuration.mdx#L101-L101)

```text
For a matching `action` in `mobkit/gating/evaluate`, the configured `risk_tier` overrides a caller-supplied tier and supplies an omitted tier.
```

- [meerkat-mobkit/src/bin/rpc_gateway.rs:6707-6707](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/bin/rpc_gateway.rs#L6707-L6707)

```text
params.insert("risk_tier".to_string(), Value::String(risk_tier.clone()));
```

### Final independent review

**PASS:** The configuration row now distinguishes a matching action's binding policy from a missing-value default. This follows the gateway's action lookup followed by unconditional tier insertion, including when the caller supplied another tier. The unmatched-action qualification is also correct: no matching policy supplies a tier, so the evaluate parser still requires one. The implementation's inspected regression covers explicit override, omission and preservation for an unknown action.

- [docs/reference/configuration.mdx:101-101](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/configuration.mdx#L101-L101)

```text
| `gating_config_path` | String | Loads JSON or TOML action policies from an `actions` object. For a matching `action` in `mobkit/gating/evaluate`, the configured `risk_tier` overrides a caller-supplied tier and supplies an omitted tier. Actions without a matching policy still require a caller-supplied tier. |
```

- [meerkat-mobkit/src/bin/rpc_gateway.rs:6682-6687](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/bin/rpc_gateway.rs#L6682-L6687)

```text
            if let Some(params) = params
                && let Some(action) = params
                    .get("action")
                    .and_then(Value::as_str)
                    .map(|action| action.trim().to_string())
                && let Some(risk_tier) = gating.action_risk_tiers.get(action.as_str())
```

- [meerkat-mobkit/src/bin/rpc_gateway.rs:6707-6707](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/bin/rpc_gateway.rs#L6707-L6707)

```text
                params.insert("risk_tier".to_string(), Value::String(risk_tier.clone()));
```

- [meerkat-mobkit/src/rpc/gating_methods.rs:109-113](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/rpc/gating_methods.rs#L109-L113)

```text
    let risk_tier = object
        .get("risk_tier")
        .and_then(Value::as_str)
        .ok_or(GatingParamsError::RiskTierRequired)?;
    let risk_tier = parse_gating_risk_tier(risk_tier)?;
```

## A17-006: Routing resolve documents destination and a nonconforming resolution object

**Candidate severity:** medium. **Independent verdict:** confirm.

The request fails at the parser, and using the example resolution for delivery cannot satisfy the complete trusted-resolution contract.

### Baseline documentation evidence

- [docs/mobkit/api/rpc.mdx:798-799](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L798-L799)

```text
| `destination` | `string` | yes | Logical destination identifier |
```

- [docs/mobkit/api/rpc.mdx:804-809](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L804-L809)

```text
  "route_id": "mob.member.lead-1.notification",
  "channel": "notification",
  "sink_adapter": "mob_member",
  "target_module": "delivery"
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/rpc/routing_delivery_methods.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '180,191p;368,379p'
```

  Routing resolve requires `recipient`; omission returns RecipientRequired. Delivery send deserializes the supplied resolution into the actual RoutingResolution type.

  destination is not an alias, and the resolution example must conform to the type clients subsequently pass to delivery.

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '682,721p'; gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime/routing.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '359,373p'
```

  RoutingResolution fields are route_id, recipient, channel, sink, target_module, retry_max, backoff_ms, and rate_limit_per_minute. It has no sink_adapter field. Runtime routing mints ids with `format!("route-{route_sequence:06}")`.

  The documented response omits required resolution data, uses the wrong sink field, and presents an id shape the pinned runtime does not produce.

### Independent challenge

The wire parser requires recipient with no destination alias. RoutingResolution has eight required fields and no sink_adapter alias. The runtime issues route-NNNNNN ids and records the complete resolution for later trusted comparison. Both the example request and reusable resolution object therefore need correction, not just a cosmetic field rename.

- [meerkat-mobkit/src/rpc/routing_delivery_methods.rs:180-200](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/rpc/routing_delivery_methods.rs#L180-L200)

```text
.get("recipient")
```

  The required request field is recipient; channel is optional.

- [meerkat-mobkit/src/runtime.rs:710-720](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime.rs#L710-L720)

```text
pub sink: String,
```

  The complete serialized resolution also requires recipient, retry_max, backoff_ms and rate_limit_per_minute; no serde alias makes the abbreviated example valid.

- [meerkat-mobkit/src/runtime/routing.rs:359-371](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/routing.rs#L359-L371)

```text
let route_id = format!("route-{route_sequence:06}");
```

  The route id is runtime-issued, and the full resolution is retained.

**Required correction:** Use recipient in the parameter table, describe supported optional overrides, and show a complete returned RoutingResolution using sink and an issued route id. Instruct clients to pass back the actual returned resolution rather than manufacture one.

### Correction

Documented recipient and supported resolve overrides; replaced the invented resolution with all eight RoutingResolution fields using sink and an issued route-NNNNNN id. Required passing the complete actual returned resolution unchanged to delivery.

- [docs/api/rpc.mdx:823-823](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L823-L823)

```text
| `recipient` | `string` | yes | Non-empty logical recipient identifier |
```

- [docs/api/rpc.mdx:833-840](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L833-L840)

```text
"route_id": "route-000001",
  "recipient": "ops@example.com",
  "channel": "notification",
  "sink": "email",
  "target_module": "delivery",
  "retry_max": 1,
  "backoff_ms": 250,
  "rate_limit_per_minute": 2
```

- [docs/api/rpc.mdx:846-847](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L846-L847)

```text
Pass the complete, actual returned resolution unchanged to
`mobkit/delivery/send`;
```

- [meerkat-mobkit/src/runtime/routing.rs:360-360](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/routing.rs#L360-L360)

```text
let route_id = format!("route-{route_sequence:06}");
```

### Final independent review

**PASS:** The resolve request now uses recipient and every supported optional override. The documented integer types and limits match parsing and runtime validation. The response contains all eight RoutingResolution fields with sink rather than sink_adapter, and its email/default policy values are realizable. The runtime issues route-NNNNNN ids and retains the complete resolution; delivery compares supplied and trusted values. The new instruction to return the actual unchanged resolution therefore fixes the important trust-boundary problem, not just the example's spelling.

- [docs/api/rpc.mdx:823-827](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L823-L827)

```text
| `recipient` | `string` | yes | Non-empty logical recipient identifier |
| `channel` | `string` | no | Delivery channel (default: `"notification"`) |
| `retry_max` | `u32` | no | Requested retry override, at most `10` |
| `backoff_ms` | `u64` | no | Requested retry backoff override in milliseconds |
| `rate_limit_per_minute` | `u32` | no | Requested rate-limit override, greater than zero |
```

- [docs/api/rpc.mdx:833-840](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L833-L840)

```text
  "route_id": "route-000001",
  "recipient": "ops@example.com",
  "channel": "notification",
  "sink": "email",
  "target_module": "delivery",
  "retry_max": 1,
  "backoff_ms": 250,
  "rate_limit_per_minute": 2
```

- [docs/api/rpc.mdx:844-848](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L844-L848)

```text
This is an example `RoutingResolution`; router-module and registered-route
policies can override the resolved values. The runtime issues and retains the
`route_id`. Pass the complete, actual returned resolution unchanged to
`mobkit/delivery/send`; do not manufacture a resolution or substitute a
registered route's `route_key`.
```

- [meerkat-mobkit/src/runtime/routing.rs:359-371](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/routing.rs#L359-L371)

```text
        let route_sequence = self.next_routing_sequence();
        let route_id = format!("route-{route_sequence:06}");
        let resolution = RoutingResolution {
            route_id,
            recipient,
            channel: resolved_channel,
            sink,
            target_module,
            retry_max,
            backoff_ms,
            rate_limit_per_minute,
        };
        self.remember_routing_resolution(resolution.clone());
```

- [meerkat-mobkit/src/runtime/delivery.rs:55-60](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/delivery.rs#L55-L60)

```text
        let Some(trusted) = self.routing_resolutions.get(route_id) else {
            return Err(DeliverySendError::UnknownRouteId(route_id.to_string()));
        };
        if trusted != provided {
            return Err(DeliverySendError::ForgedResolution);
        }
```

## A17-007: Route mutation reference confuses route keys with issued resolution ids

**Candidate severity:** medium. **Independent verdict:** confirm.

The route-add table omits recipient, gives two nonexistent field names, and makes an optional field required. The adjacent deletion entry does not document the required identifier. Registered route keys and runtime-issued route_ids are different contracts.

### Baseline documentation evidence

- [docs/mobkit/api/rpc.mdx:824-827](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L824-L827)

```text
| `route_id` | `string` | yes | Unique route identifier |
| `channel` | `string` | yes | Delivery channel |
| `sink_adapter` | `string` | yes | Physical delivery backend |
```

- [docs/mobkit/api/rpc.mdx:830-832](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L830-L832)

```text
### `mobkit/routing/routes/delete`

Remove a registered route.
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/rpc/routing_delivery_methods.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '258,301p;341,365p'
```

  Route-add accepts a direct object or nested `route` object and requires route_key, recipient, sink, and target_module. channel is optional. Route-delete requires a nonempty route_key.

  These are the complete wire parsing authorities for the two mutation operations.

### Independent challenge

The route-add parser accepts either a direct object or a nested route object, but both use route_key/recipient/sink/target_module. channel is optional. Delete reads route_key. Nested-object support does not rescue the published field names or conflate registered route keys with issued route ids.

- [meerkat-mobkit/src/rpc/routing_delivery_methods.rs:258-301](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/rpc/routing_delivery_methods.rs#L258-L301)

```text
let route_value = object.get("route").unwrap_or(params);
```

  Both accepted envelope forms use the same actual fields and optional channel.

- [meerkat-mobkit/src/rpc/routing_delivery_methods.rs:353-365](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/rpc/routing_delivery_methods.rs#L353-L365)

```text
.get("route_key")
```

  Deletion requires the registration key, not a runtime resolve id.

**Required correction:** Replace route-add fields with route_key, recipient, sink and target_module; make channel optional and document supported retry/backoff/rate-limit overrides. Document deletion by route_key and distinguish it from RoutingResolution.route_id.

### Correction

Corrected registered-route add/delete to route_key, recipient, sink and target_module, optional channel and retry/backoff/rate-limit overrides. Described direct/nested add params and distinguished registry keys from issued resolution IDs.

- [docs/api/rpc.mdx:857-858](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L857-L858)

```text
Register a route, or replace the route with the same `route_key`. Fields can
be supplied directly in `params` or nested under `params.route`.
```

- [docs/api/rpc.mdx:881-882](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L881-L882)

```text
`route_key` is a required non-empty string, not the `route_id` issued by
`mobkit/routing/resolve`.
```

- [meerkat-mobkit/src/rpc/routing_delivery_methods.rs:264-264](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/rpc/routing_delivery_methods.rs#L264-L264)

```text
let route_value = object.get("route").unwrap_or(params);
```

### Final independent review

**PASS:** Both accepted add envelopes now use route_key, recipient, sink and target_module, with channel and the retry/backoff/rate-limit fields optional. Runtime canonicalization validates nonempty strings, and inserting by canonical.route_key really replaces an existing registration. Delete requires a trimmed nonempty route_key. The response-issued route_id is explicitly distinguished from this registration key, so the original conflation is corrected in both adjacent methods.

- [docs/api/rpc.mdx:857-870](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L857-L870)

```text
Register a route, or replace the route with the same `route_key`. Fields can
be supplied directly in `params` or nested under `params.route`.

<Accordion title="Parameters">
| Param | Type | Required | Description |
|-------|------|----------|-------------|
| `route_key` | `string` | yes | Non-empty key identifying the registered route |
| `recipient` | `string` | yes | Non-empty recipient match |
| `channel` | `string` | no | Channel match; must be non-empty when supplied |
| `sink` | `string` | yes | Non-empty physical delivery backend |
| `target_module` | `string` | yes | Non-empty module ID that handles delivery |
| `retry_max` | `u32` | no | Retry override, at most `10` |
| `backoff_ms` | `u64` | no | Retry backoff override in milliseconds |
| `rate_limit_per_minute` | `u32` | no | Rate-limit override, greater than zero |
```

- [docs/api/rpc.mdx:875-882](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L875-L882)

````text
Remove a registered route by `route_key`:

```json
{"route_key": "ops-notifications"}
```

`route_key` is a required non-empty string, not the `route_id` issued by
`mobkit/routing/resolve`.
````

- [meerkat-mobkit/src/runtime.rs:704-717](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime.rs#L704-L717)

```text
pub struct RuntimeRoute {
    pub route_key: String,
    pub recipient: String,
    #[serde(default)]
    pub channel: Option<String>,
    pub sink: String,
    pub target_module: String,
    #[serde(default)]
    pub retry_max: Option<u32>,
    #[serde(default)]
    pub backoff_ms: Option<u64>,
    #[serde(default)]
    pub rate_limit_per_minute: Option<u32>,
}
```

- [meerkat-mobkit/src/rpc/routing_delivery_methods.rs:264-267](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/rpc/routing_delivery_methods.rs#L264-L267)

```text
    let route_value = object.get("route").unwrap_or(params);
    let route_object = route_value
        .as_object()
        .ok_or(RoutingDeliveryParamsError::RouteFieldMustBeObject)?;
```

- [meerkat-mobkit/src/runtime/routing.rs:227-229](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/routing.rs#L227-L229)

```text
        self.runtime_routes
            .insert(canonical.route_key.clone(), canonical.clone());
        Ok(canonical)
```

- [meerkat-mobkit/src/rpc/routing_delivery_methods.rs:359-366](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/rpc/routing_delivery_methods.rs#L359-L366)

```text
    let route_key = object
        .get("route_key")
        .and_then(Value::as_str)
        .map(str::trim)
        .filter(|value| !value.is_empty())
        .ok_or(RoutingDeliveryParamsError::RouteKeyRequired)?;
    Ok(route_key.to_string())
}
```

## A17-008: Delivery parameter table incorrectly requires an idempotency key and restricts payload to objects

**Candidate severity:** low. **Independent verdict:** confirm.

Generated client validation based on this table would reject valid endpoint requests. A particular external delivery module can impose its own payload schema, but that is not an RPC-wide object-only rule.

### Baseline documentation evidence

- [docs/mobkit/api/rpc.mdx:906-907](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L906-L907)

```text
| `payload` | `object` | yes | Message payload |
| `idempotency_key` | `string` | yes | Key for idempotent delivery |
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/rpc/routing_delivery_methods.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '380,403p'
```

  payload is cloned as a serde_json::Value without an object check. `match object.get("idempotency_key")` explicitly maps absence to None; present keys must be nonempty strings.

  The endpoint's parser does not impose the two restrictions asserted by the table.

### Independent challenge

The RPC parser accepts any present JSON payload and permits an absent idempotency key. Downstream send_delivery also conditions replay on an optional key and forwards the serialized request to the module rather than enforcing an object-only payload. Retain the caveat that a particular delivery module may apply its own schema, and that a present key must be a nonempty string.

- [meerkat-mobkit/src/rpc/routing_delivery_methods.rs:380-403](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/rpc/routing_delivery_methods.rs#L380-L403)

```text
let idempotency_key = match object.get("idempotency_key") {
```

  payload is cloned as Value; absent idempotency_key maps to None.

- [meerkat-mobkit/src/runtime/delivery.rs:126-164](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/delivery.rs#L126-L164)

```text
if let Some(idempotency_key) = request.idempotency_key.as_deref()
```

  The implementation does not introduce a later mandatory-key restriction.

**Required correction:** Mark idempotency_key optional and payload a JSON value at the RPC boundary. Preserve nonempty-string validation for a supplied key and module-specific payload requirements.

### Correction

Marked delivery payload as any present JSON value and idempotency_key optional, retaining nonempty-string validation when supplied and delivery-module-specific payload schemas.

- [docs/api/rpc.mdx:956-957](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L956-L957)

```text
| `payload` | JSON value | yes | Message payload; the delivery module may impose its own schema |
| `idempotency_key` | `string` | no | Non-empty key for idempotent delivery when supplied |
```

- [meerkat-mobkit/src/runtime.rs:732-736](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime.rs#L732-L736)

```text
pub struct DeliverySendRequest {
    pub resolution: RoutingResolution,
    pub payload: Value,
    #[serde(default)]
    pub idempotency_key: Option<String>,
```

### Final independent review

**PASS:** The table correctly describes payload as any present JSON value, including non-object values, and idempotency_key as optional. The live parser clones payload without an object check, maps an absent key to None, and rejects a supplied key unless it is a nonempty string after trimming. Delivery also gates replay on an optional key rather than adding a later mandatory-key requirement. The module-specific schema caveat is retained.

- [docs/api/rpc.mdx:956-957](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L956-L957)

```text
| `payload` | JSON value | yes | Message payload; the delivery module may impose its own schema |
| `idempotency_key` | `string` | no | Non-empty key for idempotent delivery when supplied |
```

- [meerkat-mobkit/src/rpc/routing_delivery_methods.rs:380-397](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/rpc/routing_delivery_methods.rs#L380-L397)

```text
    let payload = object
        .get("payload")
        .cloned()
        .ok_or(RoutingDeliveryParamsError::PayloadRequired)?;
    let idempotency_key = match object.get("idempotency_key") {
        None => None,
        Some(value) => {
            let key = value
                .as_str()
                .ok_or(RoutingDeliveryParamsError::IdempotencyKeyMustBeString)?
                .trim()
                .to_string();
            if key.is_empty() {
                return Err(RoutingDeliveryParamsError::IdempotencyKeyMustBeString);
            }
            Some(key)
        }
    };
```

- [meerkat-mobkit/src/runtime.rs:732-737](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime.rs#L732-L737)

```text
pub struct DeliverySendRequest {
    pub resolution: RoutingResolution,
    pub payload: Value,
    #[serde(default)]
    pub idempotency_key: Option<String>,
}
```

## A17-009: The JSON session adapter is a single file, not a per-session directory store

**Candidate severity:** medium. **Independent verdict:** confirm.

The Rust literal cannot construct the adapter, and the operational guidance points users to nonexistent per-session files and locks. This adapter is not Meerkat's current canonical session store.

### Baseline documentation evidence

- [docs/mobkit/concepts/sessions.mdx:24-29](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L24-L29)

```text
Each session gets its own file
```

- [docs/mobkit/concepts/sessions.mdx:27-29](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L27-L29)

```text
JsonFileSessionStore {
    base_dir: "/var/mobkit/sessions",
}
```

- [docs/mobkit/concepts/sessions.mdx:32-32](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L32)

```text
the store acquires a lock file (`{session_id}.lock`)
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime/session_store.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '53,58p;153,166p;182,211p'
```

  JsonFileSessionStore has private data_path, lock_path, and stale_lock_threshold fields, no base_dir. `new(data_path)` derives `data_path.with_extension("lock")`. append_rows reads and extends the existing row vector, writes one .tmp file, and renames it to data_path; read_rows decodes one Vec<SessionPersistenceRow> from that path.

  Both the storage layout and construction mechanism are directly contradicted by the adapter implementation.

### Independent challenge

The exact MobKit adapter stores all appended rows in one JSON array at a configured file path. Its fields are private, the constructor derives one file-level .lock path, and writes replace the shared file. No base_dir field or per-session file layout exists in this type. The surrounding distinction from Meerkat's canonical session persistence remains correct.

- [meerkat-mobkit/src/runtime/session_store.rs:53-58](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L53-L58)

```text
data_path: PathBuf,
```

  The private fields rule out the documented base_dir literal.

- [meerkat-mobkit/src/runtime/session_store.rs:153-166](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L153-L166)

```text
let lock_path = data_path.with_extension("lock");
```

  The public constructor takes a file path and permits an explicit lock-path override.

- [meerkat-mobkit/src/runtime/session_store.rs:182-211](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L182-L211)

```text
persisted.extend(rows.iter().cloned());
```

  append_rows reads, extends and rewrites one vector; read_rows deserializes that same shared file.

**Required correction:** Describe one JSON array file per adapter, show JsonFileSessionStore::new with a file path, and document the file-level derived/overridden lock path. Remove the per-session directory/file/lock claims without substituting unrelated Meerkat storage APIs.

### Correction

Replaced per-session directory/file claims and the private-field literal with one JSON array file per adapter, JsonFileSessionStore::new(file_path), and the derived or overridden file-level lock path.

- [docs/concepts/sessions.mdx:22-23](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L22-L23)

```text
This adapter stores rows for all sessions in one JSON array at the configured
file path.
```

- [docs/concepts/sessions.mdx:29-29](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L29-L29)

```text
let store = JsonFileSessionStore::new(".mobkit/session-rows.json");
```

- [meerkat-mobkit/src/runtime/session_store.rs:156-156](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L156-L156)

```text
let lock_path = data_path.with_extension("lock");
```

### Final independent review

**PASS:** The actual diff replaces the private-field struct literal and per-session file/lock claims with the public file-path constructor and one shared JSON array. Current source has private data_path/lock_path fields, derives the lock by replacing the extension, permits a lock-path override, and reads, extends, serializes and renames the shared row vector on append. The surrounding distinction from Meerkat's canonical session authority is preserved.

- [docs/concepts/sessions.mdx:22-30](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L22-L30)

````text
This adapter stores rows for all sessions in one JSON array at the configured
file path. Appending rows reads the existing array and atomically replaces the
shared data file.

```rust
use meerkat_mobkit::JsonFileSessionStore;

let store = JsonFileSessionStore::new(".mobkit/session-rows.json");
```
````

- [meerkat-mobkit/src/runtime/session_store.rs:53-58](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L53-L58)

```text
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct JsonFileSessionStore {
    data_path: PathBuf,
    lock_path: PathBuf,
    stale_lock_threshold: Duration,
}
```

- [meerkat-mobkit/src/runtime/session_store.rs:153-167](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L153-L167)

```text
impl JsonFileSessionStore {
    pub fn new(data_path: impl AsRef<Path>) -> Self {
        let data_path = data_path.as_ref().to_path_buf();
        let lock_path = data_path.with_extension("lock");
        Self {
            data_path,
            lock_path,
            stale_lock_threshold: Duration::from_secs(30),
        }
    }

    pub fn with_lock_path(mut self, lock_path: impl AsRef<Path>) -> Self {
        self.lock_path = lock_path.as_ref().to_path_buf();
        self
    }
```

- [meerkat-mobkit/src/runtime/session_store.rs:192-202](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L192-L202)

```text
        let mut persisted = self.read_rows()?;
        persisted.extend(rows.iter().cloned());

        let tmp_path = self.data_path.with_extension("tmp");
        let json = serde_json::to_vec_pretty(&persisted)
            .map_err(|err| JsonFileSessionStoreError::Serialize(err.to_string()))?;
        fs::write(&tmp_path, json).map_err(|err| JsonFileSessionStoreError::Io(err.to_string()))?;
        fs::rename(&tmp_path, &self.data_path)
            .map_err(|err| JsonFileSessionStoreError::Io(err.to_string()))?;
        Ok(())
    }
```

## A17-010: The BigQuery adapter example constructs private, incomplete fields

**Candidate severity:** medium. **Independent verdict:** confirm.

The production adapter example cannot compile independently of credentials or cloud availability.

### Baseline documentation evidence

- [docs/mobkit/concepts/sessions.mdx:41-44](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L41-L44)

```text
BigQuerySessionStoreAdapter {
    dataset: "mobkit_sessions",
    table: "session_rows",
}
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime/session_store.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '84,93p;318,350p'
```

  All adapter fields are private; the type also contains project_id, api_base_url, access_token, http_timeout, and client. Public `new_native(dataset: impl Into<String>, table: impl Into<String>)` initializes the complete adapter. Public with_project_id and with_access_token setters exist.

  The published struct literal is not usable by library consumers; the source supplies the actual public constructor.

### Independent challenge

The public type has private dataset/table fields and additional private connection state. The two-field literal therefore fails independently of authentication or cloud availability. A compatibility constructor new also exists, but neither it nor new_native makes the literal valid; new_native is the direct current constructor.

- [meerkat-mobkit/src/runtime/session_store.rs:84-93](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L84-L93)

```text
pub struct BigQuerySessionStoreAdapter {
```

  The complete definition contains seven private fields, not two public literal fields.

- [meerkat-mobkit/src/runtime/session_store.rs:318-350](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L318-L350)

```text
pub fn new_native(dataset: impl Into<String>, table: impl Into<String>) -> Self {
```

  This constructor initializes the complete adapter; project and token configuration have explicit setters.

**Required correction:** Replace the canonical example with BigQuerySessionStoreAdapter::new_native("mobkit_sessions", "session_rows"). Keep operation-specific project/auth prerequisites accurate rather than implying that constructing an adapter provisions credentials.

### Correction

Used the public BigQuerySessionStoreAdapter::new_native constructor and distinguished construction from remote-operation project/token prerequisites and table provisioning.

- [docs/concepts/sessions.mdx:53-53](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L53-L53)

```text
let store = BigQuerySessionStoreAdapter::new_native("mobkit_sessions", "session_rows");
```

- [docs/concepts/sessions.mdx:56-56](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L56-L56)

```text
Constructing the adapter does not provision the table or credentials.
```

- [meerkat-mobkit/src/runtime/session_store.rs:326-326](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L326-L326)

```text
pub fn new_native(dataset: impl Into<String>, table: impl Into<String>) -> Self {
```

### Final independent review

**PASS:** The replacement calls the actual public new_native constructor rather than attempting to initialize seven private fields. Its Into<String> parameters accept both literals. I separately checked remote-operation configuration: construction leaves project_id/access_token unset and performs no table provisioning; row operations resolve the project from the explicit setting or BIGQUERY_PROJECT_ID and the token from the explicit setting or the three documented environment variables. The new prerequisite paragraph accurately separates construction from cloud access.

- [docs/concepts/sessions.mdx:50-56](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L50-L56)

````text
```rust
use meerkat_mobkit::BigQuerySessionStoreAdapter;

let store = BigQuerySessionStoreAdapter::new_native("mobkit_sessions", "session_rows");
```

Constructing the adapter does not provision the table or credentials. Remote
````

- [meerkat-mobkit/src/runtime/session_store.rs:84-93](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L84-L93)

```text
#[derive(Debug, Clone)]
pub struct BigQuerySessionStoreAdapter {
    dataset: String,
    table: String,
    project_id: Option<String>,
    api_base_url: String,
    access_token: Option<String>,
    http_timeout: Duration,
    client: reqwest::Client,
}
```

- [meerkat-mobkit/src/runtime/session_store.rs:326-336](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L326-L336)

```text
    pub fn new_native(dataset: impl Into<String>, table: impl Into<String>) -> Self {
        Self {
            dataset: dataset.into(),
            table: table.into(),
            project_id: None,
            api_base_url: Self::DEFAULT_API_BASE_URL.to_string(),
            access_token: None,
            client: shared_bigquery_http_client(),
            http_timeout: Duration::from_secs(30),
        }
    }
```

- [meerkat-mobkit/src/runtime/session_store.rs:546-559](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L546-L559)

```text
    fn resolve_project_id(&self) -> Result<String, BigQuerySessionStoreError> {
        if let Some(project_id) = self.project_id.as_deref() {
            let project = project_id.trim();
            if !project.is_empty() {
                return Ok(project.to_string());
            }
        }

        if let Ok(project_id) = std::env::var("BIGQUERY_PROJECT_ID") {
            let project = project_id.trim();
            if !project.is_empty() {
                return Ok(project.to_string());
            }
        }
```

- [meerkat-mobkit/src/runtime/session_store.rs:567-586](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L567-L586)

```text
    fn resolve_access_token(&self) -> Result<String, BigQuerySessionStoreError> {
        if let Some(token) = self.access_token.as_deref() {
            let token = token.trim();
            if !token.is_empty() {
                return Ok(token.to_string());
            }
        }

        for key in [
            "BIGQUERY_ACCESS_TOKEN",
            "GOOGLE_OAUTH_ACCESS_TOKEN",
            "GOOGLE_ACCESS_TOKEN",
        ] {
            if let Ok(token) = std::env::var(key) {
                let token = token.trim();
                if !token.is_empty() {
                    return Ok(token.to_string());
                }
            }
        }
```

## A17-011: SessionPersistenceRow field table describes a different serialized record

**Candidate severity:** medium. **Independent verdict:** confirm.

Integrations written to this schema produce or read the wrong persistence record, including failing to populate payload and missing deletion semantics.

### Baseline documentation evidence

- [docs/mobkit/concepts/sessions.mdx:62-65](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L62-L65)

```text
| `turn_number` | `u64` | Current turn count |
| `state` | `Value` | Serialized session state (messages, tool results, context) |
| `created_at_ms` | `u64` | Creation timestamp |
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime/session_store.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '24,36p'
```

  The complete SessionPersistenceRow definition has session_id: String, updated_at_ms: u64, deleted: bool, payload: Value, and labels: BTreeMap<String, String>, all with serde defaults. There is no turn_number, state, or created_at_ms field.

  The serialized type itself is authoritative, and the documentation omits tombstone and label data while renaming the actual payload.

### Independent challenge

The complete serialized SessionPersistenceRow has exactly session_id, updated_at_ms, deleted, payload and labels, all serde-defaulted. There are no aliases that map state, turn_number or created_at_ms to those fields. Defaulting does not rescue the table: a supplied state would not become payload.

- [meerkat-mobkit/src/runtime/session_store.rs:24-36](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L24-L36)

```text
pub labels: BTreeMap<String, String>,
```

  The full struct and serde attributes establish the actual five-field wire record and the absence of the table's three invented fields.

**Required correction:** Replace the canonical field table with the actual five fields, types and serde defaults; identify payload as content, deleted as the tombstone flag, and labels as string metadata.

### Correction

Replaced the invented row schema with session_id, updated_at_ms, deleted, payload and labels, including exact Rust types and all serde defaults.

- [docs/concepts/sessions.mdx:75-79](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L75-L79)

```text
| `session_id` | `String` | `""` | Session identifier |
| `updated_at_ms` | `u64` | `0` | Timestamp used to choose the latest row for a session |
| `deleted` | `bool` | `false` | Tombstone flag |
| `payload` | `serde_json::Value` | `null` | Serialized session content |
| `labels` | `BTreeMap<String, String>` | `{}` | String key-value metadata |
```

- [meerkat-mobkit/src/runtime/session_store.rs:25-25](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L25-L25)

```text
pub struct SessionPersistenceRow {
```

### Final independent review

**PASS:** The replacement is an exact five-field inventory of the current SessionPersistenceRow, not a renamed approximation of the old table. String, u64, bool, serde_json::Value and BTreeMap<String, String> have the documented empty-string, zero, false, null and empty-map defaults under the actual field-level serde(default) annotations. payload, deleted and labels now describe their real purposes, and updated_at_ms really is the latest-row selection key.

- [docs/concepts/sessions.mdx:73-82](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L73-L82)

```text
| Field | Type | Serde default | Description |
|-------|------|---------------|-------------|
| `session_id` | `String` | `""` | Session identifier |
| `updated_at_ms` | `u64` | `0` | Timestamp used to choose the latest row for a session |
| `deleted` | `bool` | `false` | Tombstone flag |
| `payload` | `serde_json::Value` | `null` | Serialized session content |
| `labels` | `BTreeMap<String, String>` | `{}` | String key-value metadata |

Every field is `#[serde(default)]`: an omitted field receives the default
above when the row is deserialized.
```

- [meerkat-mobkit/src/runtime/session_store.rs:24-36](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L24-L36)

```text
#[derive(Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize)]
pub struct SessionPersistenceRow {
    #[serde(default)]
    pub session_id: String,
    #[serde(default)]
    pub updated_at_ms: u64,
    #[serde(default)]
    pub deleted: bool,
    #[serde(default)]
    pub payload: Value,
    #[serde(default)]
    pub labels: BTreeMap<String, String>,
}
```

- [meerkat-mobkit/src/runtime/session_store.rs:134-141](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L134-L141)

```text
    for row in rows {
        let should_replace = match latest_by_session.get(&row.session_id) {
            Some(existing) => row.updated_at_ms >= existing.updated_at_ms,
            None => true,
        };
        if should_replace {
            latest_by_session.insert(row.session_id.clone(), row.clone());
        }
```

## A17-012: SessionStoreContract is capability metadata, not a CRUD interface

**Candidate severity:** medium. **Independent verdict:** confirm.

The reference directs embedders toward an interface that the public type does not represent and conflates descriptive adapter guarantees with backend operations.

### Baseline documentation evidence

- [docs/mobkit/concepts/sessions.mdx:69-76](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L69-L76)

```text
The `SessionStoreContract` defines the operations every backend must implement:
```

- [docs/mobkit/concepts/sessions.mdx:73-76](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L73-L76)

```text
| `write` | Persist a session row (create or update) |
| `read` | Load a session row by ID |
| `list` | List all session IDs |
| `delete` | Remove a session row |
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime/session_store.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '12,22p;182,221p'
```

  SessionStoreContract is a serializable struct containing store, latest_row_per_session, tombstones_supported, dedup_read_path, file_locking, crash_recovery, bigquery_dataset, and bigquery_table. The JSON adapter exposes append_rows, read_rows, read_latest_rows, and read_live_rows rather than an implementation of a SessionStoreContract CRUD trait.

  This is the exact named type and adapter API; a same-named CRUD contract is not hidden behind the prose.

### Independent challenge

SessionStoreContract is a concrete serializable capability record, not a trait or CRUD API. The complete pinned symbol search finds no competing public contract definition. Concrete adapters expose row-oriented methods, with tombstone materialization, rather than the table's write/read/list/delete trait. This verdict does not separately assert that all claimed integration tests are absent.

- [meerkat-mobkit/src/runtime/session_store.rs:12-22](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L12-L22)

```text
pub latest_row_per_session: bool,
```

  The named contract contains backend/guarantee metadata, not operation signatures.

- [meerkat-mobkit/src/runtime/session_store.rs:204-223](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L204-L223)

```text
pub fn read_rows(&self) -> Result<Vec<SessionPersistenceRow>, JsonFileSessionStoreError> {
```

  The actual adapter methods read all rows or materialize latest/live rows; append_rows is defined immediately above.

**Required correction:** Describe SessionStoreContract as capability/guarantee metadata and list its actual fields. Separately describe real adapter methods and tombstone-based row deletion semantics; do not present the invented CRUD list as a trait.

### Correction

Identified SessionStoreContract as capability metadata, listed all eight fields, and documented real row-oriented adapter methods and tombstone deletion separately. Preserved the rejected A17-013 materialization phrase and the existing integration-test statement.

- [docs/concepts/sessions.mdx:86-87](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L86-L87)

```text
`SessionStoreContract` is serializable capability and guarantee metadata, not
a CRUD trait.
```

- [docs/concepts/sessions.mdx:110-111](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L110-L111)

```text
Logical deletion appends a newer row for the same `session_id` with
`deleted: true`.
```

- [meerkat-mobkit/src/runtime/session_store.rs:13-22](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L13-L22)

```text
pub struct SessionStoreContract {
    pub store: SessionStoreKind,
    pub latest_row_per_session: bool,
    pub tombstones_supported: bool,
    pub dedup_read_path: bool,
    pub file_locking: bool,
    pub crash_recovery: bool,
    pub bigquery_dataset: Option<String>,
    pub bigquery_table: Option<String>,
}
```

### Final independent review

**PASS:** The subsection now correctly distinguishes the serializable SessionStoreContract descriptor from concrete adapter methods. All eight fields and their types match the complete struct. The JSON adapter's append/read/latest/live methods and BigQuery's asynchronous stream-insert/read/latest/live methods exist. Latest-row selection followed by deleted=false filtering supports the stated logical tombstone behavior without claiming physical erasure. The actual diff leaves the rejected A17-013 wording and the expressly excluded integration-test sentence unchanged.

- [docs/concepts/sessions.mdx:86-99](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L86-L99)

```text
`SessionStoreContract` is serializable capability and guarantee metadata, not
a CRUD trait. `session_store_contracts(&decisions)` returns these descriptors
for the BigQuery and JSON-file adapters:

| Field | Type | Meaning |
|-------|------|---------|
| `store` | `SessionStoreKind` | Backend kind |
| `latest_row_per_session` | `bool` | Supports latest-row materialization |
| `tombstones_supported` | `bool` | Supports rows marked deleted |
| `dedup_read_path` | `bool` | Supports deduplicated session reads |
| `file_locking` | `bool` | Uses file-level locking |
| `crash_recovery` | `bool` | Supports stale-lock recovery |
| `bigquery_dataset` | `Option<String>` | Configured dataset for the BigQuery descriptor |
| `bigquery_table` | `Option<String>` | Configured table for the BigQuery descriptor |
```

- [docs/concepts/sessions.mdx:101-112](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L101-L112)

```text
The concrete adapters expose row-oriented methods (BigQuery's are async):

| Method | Description |
|--------|-------------|
| JSON `append_rows` / BigQuery `stream_insert_rows` | Append session rows |
| `read_rows` | Read stored rows, including older versions and tombstones |
| `read_latest_rows` | Read the latest row per session |
| `read_live_rows` | Read latest rows whose `deleted` flag is false |

Logical deletion appends a newer row for the same `session_id` with
`deleted: true`. Live-row materialization omits that session; the tombstone
does not itself erase historical rows.
```

- [meerkat-mobkit/src/runtime/session_store.rs:12-22](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L12-L22)

```text
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct SessionStoreContract {
    pub store: SessionStoreKind,
    pub latest_row_per_session: bool,
    pub tombstones_supported: bool,
    pub dedup_read_path: bool,
    pub file_locking: bool,
    pub crash_recovery: bool,
    pub bigquery_dataset: Option<String>,
    pub bigquery_table: Option<String>,
}
```

- [meerkat-mobkit/src/runtime/session_store.rs:146-151](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L146-L151)

```text
pub fn materialize_live_session_rows(rows: &[SessionPersistenceRow]) -> Vec<SessionPersistenceRow> {
    materialize_latest_session_rows(rows)
        .into_iter()
        .filter(|row| !row.deleted)
        .collect()
}
```

- [meerkat-mobkit/src/runtime/session_store.rs:204-224](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L204-L224)

```text
    pub fn read_rows(&self) -> Result<Vec<SessionPersistenceRow>, JsonFileSessionStoreError> {
        if !self.data_path.exists() {
            return Ok(vec![]);
        }
        let bytes = fs::read(&self.data_path)
            .map_err(|err| JsonFileSessionStoreError::Io(err.to_string()))?;
        serde_json::from_slice::<Vec<SessionPersistenceRow>>(&bytes)
            .map_err(|err| JsonFileSessionStoreError::InvalidStoreData(err.to_string()))
    }

    pub fn read_latest_rows(
        &self,
    ) -> Result<Vec<SessionPersistenceRow>, JsonFileSessionStoreError> {
        let rows = self.read_rows()?;
        Ok(materialize_latest_session_rows(&rows))
    }

    pub fn read_live_rows(&self) -> Result<Vec<SessionPersistenceRow>, JsonFileSessionStoreError> {
        let rows = self.read_rows()?;
        Ok(materialize_live_session_rows(&rows))
    }
```

- [meerkat-mobkit/src/runtime/session_store.rs:453-465](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L453-L465)

```text
    pub async fn read_live_rows(
        &self,
    ) -> Result<Vec<SessionPersistenceRow>, BigQuerySessionStoreError> {
        let fq_table = self.fully_qualified_table()?;
        let query = format!(
            "SELECT session_id, updated_at_ms, deleted, payload, labels_json FROM (\
               SELECT session_id, updated_at_ms, deleted, payload, labels_json \
               FROM `{fq_table}` \
               QUALIFY ROW_NUMBER() OVER (PARTITION BY session_id ORDER BY updated_at_ms DESC) = 1\
             ) WHERE deleted = false"
        );
        self.execute_query(&query).await
    }
```

## A17-013: Live session-row materialization means non-tombstoned, not currently running

**Candidate severity:** low. **Independent verdict:** reject.

The wording can cause an operational consumer to count retained but idle/completed records as actively running sessions.

### Baseline documentation evidence

- [docs/mobkit/concepts/sessions.mdx:85-85](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L85)

```text
**`materialize_live_session_rows`** -- returns rows for currently active sessions
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime/session_store.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '130,150p'
```

  materialize_latest_session_rows picks the greatest updated_at_ms per session, with the later input winning ties. materialize_live_session_rows only applies `.filter(|row| !row.deleted)` to that result.

  The function does not consult execution, residency, or session-service activity.

### Independent challenge

The implementation does return the latest non-deleted rows, but the audit strengthens the document's 'currently active sessions' into 'currently running' without evidence. The page explicitly describes storage adapters rather than canonical runtime lifecycle, and this materialization subsection is about persisted-row inspection. In that context active/live can mean non-tombstoned; there is no explicit running/executing predicate or promised runtime-status API to disprove. More exact wording could be an editorial improvement, but this candidate does not establish a distinct factual defect under the challenge contract. The separately confirmed wrong row schema and invented CRUD interface remain actionable.

- [meerkat-mobkit/src/runtime/session_store.rs:130-150](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L130-L150)

```text
.filter(|row| !row.deleted)
```

  This establishes storage liveness, but does not by itself prove the prose asserted execution liveness.

- [docs/mobkit/concepts/sessions.mdx:9-13](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L9-L13)

```text
This page describes MobKit-specific storage adapters. It does not replace Meerkat's own runtime-backed session durability model.
```

  The page explicitly narrows its domain to the adapter rather than the canonical runtime lifecycle.

- [docs/mobkit/concepts/sessions.mdx:80-87](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L80-L87)

```text
**`materialize_live_session_rows`** -- returns rows for currently active sessions
```

  The actual claim says active, not running or executing, in a row-materialization subsection. The audit's stronger interpretation is not established.

**Disposition:** Rejected; no correction authorized for this claim.

## A17-014: Governance examples and Rust reference use the wrong validator signatures

**Candidate severity:** medium. **Independent verdict:** confirm.

Both examples fail to type-check, and simply adding an argument would still be wrong if raw state text is supplied instead of a document. The validators operate on governance documents.

### Baseline documentation evidence

- [docs/mobkit/reference/governance.mdx:22-22](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/governance.mdx?plain=1#L22)

```text
validate_governance_state("realignment_in_progress")?;
```

- [docs/mobkit/reference/governance.mdx:42-42](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/governance.mdx?plain=1#L42)

```text
validate_traceability_statuses(&["TYPED", "WIRED", "VALIDATED"])?;
```

- [docs/mobkit/sdks/rust.mdx:282-283](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/sdks/rust.mdx?plain=1#L282-L283)

```text
fn validate_governance_state(state: &str) -> Result<(), GovernanceValidationError>
fn validate_traceability_statuses(statuses: &[&str]) -> Result<(), GovernanceValidationError>
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/governance.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '67,100p;175,197p;227,229p'
```

  validate_governance_state takes `(file_name: &str, content: &str)` and searches content for a governance_state line. validate_traceability_statuses takes one `markdown: &str`, parses Markdown tables or YAML rows, and checks row evidence as well as statuses.

  The documented scalar state and slice-of-statuses inputs are not the accepted representations or Rust signatures.

### Independent challenge

The exact exported validators operate on document strings. validate_governance_state needs both a file label and content containing governance_state:, while traceability validation takes a Markdown/YAML document and validates row evidence as well as status names. No one-argument scalar-state or slice-of-statuses overload exists.

- [meerkat-mobkit/src/governance.rs:67-100](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/governance.rs#L67-L100)

```text
pub fn validate_traceability_statuses(markdown: &str) -> Result<(), GovernanceValidationError> {
```

  The two public signatures and document parsing disprove both snippets and the Rust API inventory.

- [meerkat-mobkit/src/governance.rs:175-197](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/governance.rs#L175-L197)

```text
return Err(GovernanceValidationError::MissingTraceabilityEvidence { line: index + 1 });
```

  A replacement example must include a real traceability representation with evidence where required, not merely wrap status strings.

**Required correction:** Correct the signatures in the Rust reference. Replace governance examples with a file label plus governance_state document content and a valid Markdown/YAML traceability document including evidence for TYPED/WIRED/VALIDATED rows.

### Correction

Corrected both validator signatures in the Rust reference. Governance examples now pass a diagnostic file label plus governance_state document text and a valid YAML traceability document with evidence for TYPED/WIRED/VALIDATED rows.

- [docs/reference/governance.mdx:23-23](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L23-L23)

```text
validate_governance_state("spec.yaml", "governance_state: realignment_in_progress\n")?;
```

- [docs/reference/governance.mdx:51-54](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L51-L54)

```text
  - status: VALIDATED
    evidence: ["meerkat-mobkit/tests/governance_contracts.rs"]
"#;
validate_traceability_statuses(traceability)?;
```

- [docs/sdks/rust.mdx:280-281](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/rust.mdx#L280-L281)

```text
fn validate_governance_state(file_name: &str, content: &str) -> Result<(), GovernanceValidationError>
fn validate_traceability_statuses(markdown: &str) -> Result<(), GovernanceValidationError>
```

### Final independent review

**PASS:** Both required occurrences are corrected: the governance guide supplies a diagnostic file label plus actual governance_state content, and the Rust SDK lists the exact two-string and one-document signatures. The YAML example matches TraceabilityDocument/TraceabilityRow, uses recognized statuses and provides non-placeholder evidence for every evidence-requiring row; each referenced repository path exists. The added explanation accurately describes Markdown/YAML inputs and evidence checking without promising that the validator executes tests.

- [docs/reference/governance.mdx:17-24](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L17-L24)

````text
`validate_governance_state` takes a file label for diagnostics and the document
content. It requires a `governance_state:` line with the value shown above:

```rust
use meerkat_mobkit::validate_governance_state;

validate_governance_state("spec.yaml", "governance_state: realignment_in_progress\n")?;
```
````

- [docs/reference/governance.mdx:45-54](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L45-L54)

```text
let traceability = r#"
rows:
  - status: TYPED
    evidence: ["meerkat-mobkit/src/governance.rs"]
  - status: WIRED
    evidence: ["meerkat-mobkit/src/bin/governance_check.rs"]
  - status: VALIDATED
    evidence: ["meerkat-mobkit/tests/governance_contracts.rs"]
"#;
validate_traceability_statuses(traceability)?;
```

- [docs/sdks/rust.mdx:280-281](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/rust.mdx#L280-L281)

```text
fn validate_governance_state(file_name: &str, content: &str) -> Result<(), GovernanceValidationError>
fn validate_traceability_statuses(markdown: &str) -> Result<(), GovernanceValidationError>
```

- [meerkat-mobkit/src/governance.rs:67-100](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/governance.rs#L67-L100)

```text
pub fn validate_governance_state(
    file_name: &str,
    content: &str,
) -> Result<(), GovernanceValidationError> {
    let line = content
        .lines()
        .find(|line| line.trim_start().starts_with("governance_state:"))
        .ok_or_else(|| GovernanceValidationError::MissingGovernanceState {
            file: file_name.to_string(),
        })?;

    let found = line
        .split_once(':')
        .map(|(_, value)| value.trim())
        .unwrap_or_default()
        .to_string();

    if found != REQUIRED_GOVERNANCE_STATE {
        return Err(GovernanceValidationError::InvalidGovernanceState {
            file: file_name.to_string(),
            found,
        });
    }

    Ok(())
}

pub fn validate_traceability_statuses(markdown: &str) -> Result<(), GovernanceValidationError> {
    if looks_like_markdown_table(markdown) {
        return validate_markdown_traceability_statuses(markdown);
    }

    validate_yaml_traceability_statuses(markdown)
}
```

- [meerkat-mobkit/src/governance.rs:54-65](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/governance.rs#L54-L65)

```text
#[derive(Debug, Deserialize)]
struct TraceabilityDocument {
    #[serde(default)]
    rows: Vec<TraceabilityRow>,
}

#[derive(Debug, Deserialize)]
struct TraceabilityRow {
    status: String,
    #[serde(default)]
    evidence: Vec<String>,
}
```

- [meerkat-mobkit/src/governance.rs:190-194](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/governance.rs#L190-L194)

```text
        if status_requires_evidence(&row.status)
            && row.evidence.iter().all(|entry| is_missing_evidence(entry))
        {
            return Err(GovernanceValidationError::MissingTraceabilityEvidence { line: index + 1 });
        }
```

## A17-015: GovernanceValidationError table contains nonexistent variants

**Candidate severity:** medium. **Independent verdict:** confirm.

Consumers cannot match or diagnose errors using this inventory, and the documented baseline failure is from a different error family.

### Baseline documentation evidence

- [docs/mobkit/reference/governance.mdx:83-86](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/governance.mdx?plain=1#L83-L86)

```text
| `InvalidState` | Governance state not recognized |
```

- [docs/mobkit/reference/governance.mdx:85-86](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/governance.mdx?plain=1#L85-L86)

```text
| `BaselineVerificationFailed` | Required Meerkat symbols missing |
| `ContractNotTraced` | Contract lacks traceability annotation |
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/governance.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '18,26p'
```

  The complete enum is MissingGovernanceState, InvalidGovernanceState, NoTraceabilityRows, InvalidTraceabilityStatus, MissingTraceabilityEvidence, and InvalidTraceabilityRow. None of InvalidState, BaselineVerificationFailed, or ContractNotTraced exists.

  The complete enum provides positive/exhaustive authority for the negative variant claim.

### Independent challenge

The exhaustive error enum contains six variants. InvalidTraceabilityStatus is real, but InvalidState, BaselineVerificationFailed and ContractNotTraced are not variants of this error type. The baseline scanner has its own BaselineVerificationError, so combining the two families is not a valid simplification of the Rust inventory.

- [meerkat-mobkit/src/governance.rs:18-26](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/governance.rs#L18-L26)

```text
InvalidGovernanceState { file: String, found: String },
```

  The full variant set is MissingGovernanceState, InvalidGovernanceState, NoTraceabilityRows, InvalidTraceabilityStatus, MissingTraceabilityEvidence and InvalidTraceabilityRow.

- [meerkat-mobkit/src/baseline.rs:31-37](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/baseline.rs#L31-L37)

```text
pub enum BaselineVerificationError {
```

  Baseline failures belong to a separate error family.

**Required correction:** Replace the erroneous governance error table with the actual six variants and payload meanings; describe baseline failures separately if retained.

### Correction

Replaced nonexistent GovernanceValidationError variants with the six actual variants and payload meanings, including Markdown-line/YAML-row position semantics. Kept baseline errors in their separate family.

- [docs/reference/governance.mdx:108-108](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L108-L108)

```text
| `MissingGovernanceState { file }` | No `governance_state:` line; `file` is the supplied diagnostic label |
```

- [docs/reference/governance.mdx:113-113](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L113-L113)

```text
| `InvalidTraceabilityRow { line }` | Malformed document, table header, or row |
```

- [meerkat-mobkit/src/governance.rs:19-26](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/governance.rs#L19-L26)

```text
pub enum GovernanceValidationError {
    MissingGovernanceState { file: String },
    InvalidGovernanceState { file: String, found: String },
    NoTraceabilityRows,
    InvalidTraceabilityStatus { line: usize, status: String },
    MissingTraceabilityEvidence { line: usize },
    InvalidTraceabilityRow { line: usize },
}
```

### Final independent review

**PASS:** The table now enumerates exactly the six variants in the current GovernanceValidationError, retaining the real InvalidTraceabilityStatus and removing all invented variants. Payload labels and the distinction between Markdown source-line positions, YAML row indices and YAML parse-failure position 1 match the actual error construction sites. BaselineVerificationError is explicitly separate and is a different enum in source.

- [docs/reference/governance.mdx:108-117](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L108-L117)

```text
| `MissingGovernanceState { file }` | No `governance_state:` line; `file` is the supplied diagnostic label |
| `InvalidGovernanceState { file, found }` | State differs from `realignment_in_progress`; includes the file label and found value |
| `NoTraceabilityRows` | No traceability data rows were found |
| `InvalidTraceabilityStatus { line, status }` | Status is outside the strict set; includes the reported position and value |
| `MissingTraceabilityEvidence { line }` | A status requiring evidence has only empty or placeholder evidence |
| `InvalidTraceabilityRow { line }` | Malformed document, table header, or row |

For Markdown, `line` is a one-based source line; for parsed YAML rows it is
the one-based row index (a YAML parse failure reports `1`). Baseline scan
failures belong to `BaselineVerificationError`, not this enum.
```

- [meerkat-mobkit/src/governance.rs:19-26](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/governance.rs#L19-L26)

```text
pub enum GovernanceValidationError {
    MissingGovernanceState { file: String },
    InvalidGovernanceState { file: String, found: String },
    NoTraceabilityRows,
    InvalidTraceabilityStatus { line: usize, status: String },
    MissingTraceabilityEvidence { line: usize },
    InvalidTraceabilityRow { line: usize },
}
```

- [meerkat-mobkit/src/governance.rs:154-165](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/governance.rs#L154-L165)

```text
        seen_rows = true;
        let status = columns[status_column].trim_matches('`');
        if !STRICT_TRACEABILITY_STATUSES.contains(&status) {
            return Err(GovernanceValidationError::InvalidTraceabilityStatus {
                line: idx + 1,
                status: status.to_string(),
            });
        }
        let evidence = columns[evidence_or_link_column].trim_matches('`').trim();
        if status_requires_evidence(status) && is_missing_evidence(evidence) {
            return Err(GovernanceValidationError::MissingTraceabilityEvidence { line: idx + 1 });
        }
```

- [meerkat-mobkit/src/governance.rs:175-194](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/governance.rs#L175-L194)

```text
fn validate_yaml_traceability_statuses(yaml: &str) -> Result<(), GovernanceValidationError> {
    let document: TraceabilityDocument = serde_yaml::from_str(yaml)
        .map_err(|_| GovernanceValidationError::InvalidTraceabilityRow { line: 1 })?;

    if document.rows.is_empty() {
        return Err(GovernanceValidationError::NoTraceabilityRows);
    }

    for (index, row) in document.rows.iter().enumerate() {
        if !STRICT_TRACEABILITY_STATUSES.contains(&row.status.as_str()) {
            return Err(GovernanceValidationError::InvalidTraceabilityStatus {
                line: index + 1,
                status: row.status.clone(),
            });
        }
        if status_requires_evidence(&row.status)
            && row.evidence.iter().all(|entry| is_missing_evidence(entry))
        {
            return Err(GovernanceValidationError::MissingTraceabilityEvidence { line: index + 1 });
        }
```

- [meerkat-mobkit/src/baseline.rs:32-37](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/baseline.rs#L32-L37)

```text
pub enum BaselineVerificationError {
    RepoNotConfigured { env_var: &'static str },
    RepoMissing(PathBuf),
    RepoUnreadable(PathBuf),
    MissingSymbols(BaselineVerificationReport),
}
```

## A17-016: Aggregate governance validation does not run baseline symbol verification

**Candidate severity:** medium. **Independent verdict:** confirm.

A CI pipeline following this guarantee may run only governance_check while assuming Meerkat source prerequisites were also checked.

### Baseline documentation evidence

- [docs/mobkit/reference/governance.mdx:71-77](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/governance.mdx?plain=1#L71-L77)

```text
1. Governance state validation
2. Traceability status validation
3. Baseline symbol verification
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/governance.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '231,242p'; gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/bin/governance_check.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '90,103p'
```

  validate_governance_contracts validates governance_state in three supplied document strings, calls validate_traceability_statuses on the fourth, and returns Ok. governance_check invokes that function. There is no baseline verifier call in the aggregate implementation.

  The entire implementation of the advertised aggregate enumerates the checks it actually performs.

### Independent challenge

The entire aggregate function performs three governance-state checks and one traceability check, then returns. The standalone governance_check binary calls that aggregate and does not invoke the separately implemented baseline scanner. A broader host CI could run both binaries, but that cannot make this function's advertised third check true.

- [meerkat-mobkit/src/governance.rs:231-242](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/governance.rs#L231-L242)

```text
validate_traceability_statuses(traceability_markdown)?;
```

  This complete function body contains no baseline verification.

- [meerkat-mobkit/src/bin/governance_check.rs:95-101](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/bin/governance_check.rs#L95-L101)

```text
if let Err(err) = validate_governance_contracts(&spec, &plan, &checklist, &traceability) {
```

  The advertised binary is not an implicit wrapper around both verification families.

**Required correction:** Remove baseline verification from the aggregate's enumerated checks; identify baseline_check or verify_meerkat_baseline_symbols as a separate optional invocation.

### Correction

Removed baseline scanning from aggregate governance checks and explicitly separated optional baseline_check/verify_meerkat_baseline_symbols invocation from validate_governance_contracts and governance_check.

- [docs/reference/governance.mdx:100-101](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L100-L101)

```text
Neither the aggregate function nor that binary runs baseline symbol
verification.
```

- [meerkat-mobkit/src/governance.rs:237-241](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/governance.rs#L237-L241)

```text
    validate_governance_state(".rct/spec.yaml", spec_yaml)?;
    validate_governance_state(".rct/plan.yaml", plan_yaml)?;
    validate_governance_state(".rct/checklist.yaml", checklist_yaml)?;
    validate_traceability_statuses(traceability_markdown)?;
    Ok(())
```

### Final independent review

**PASS:** The aggregate's promised checks now exactly match its complete implementation: governance_state checks over spec, plan and checklist content, then traceability validation. The guide explicitly says neither the aggregate nor governance_check performs baseline verification and identifies both separate entry points. I inspected the binary's entire control flow as well as the distinct baseline_check call; there is no hidden baseline invocation that the revised text overlooks.

- [docs/reference/governance.mdx:94-102](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L94-L102)

```text
`validate_governance_contracts` validates the supplied document contents:

1. Governance state in the spec, plan, and checklist documents
2. Traceability row statuses and required evidence

This is available as a standalone binary (`governance_check`) for CI integration.
Neither the aggregate function nor that binary runs baseline symbol
verification. Invoke `baseline_check` or `verify_meerkat_baseline_symbols`
separately when that optional text scan is useful.
```

- [meerkat-mobkit/src/governance.rs:231-242](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/governance.rs#L231-L242)

```text
pub fn validate_governance_contracts(
    spec_yaml: &str,
    plan_yaml: &str,
    checklist_yaml: &str,
    traceability_markdown: &str,
) -> Result<(), GovernanceValidationError> {
    validate_governance_state(".rct/spec.yaml", spec_yaml)?;
    validate_governance_state(".rct/plan.yaml", plan_yaml)?;
    validate_governance_state(".rct/checklist.yaml", checklist_yaml)?;
    validate_traceability_statuses(traceability_markdown)?;
    Ok(())
}
```

- [meerkat-mobkit/src/bin/governance_check.rs:95-101](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/bin/governance_check.rs#L95-L101)

```text
    if let Err(err) = validate_governance_contracts(&spec, &plan, &checklist, &traceability) {
        eprintln!("governance validation failed: {err}");
        process::exit(1);
    }

    println!("governance validation passed");
}
```

- [meerkat-mobkit/src/bin/baseline_check.rs:24-31](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/bin/baseline_check.rs#L24-L31)

```text
fn main() {
    let report = match verify_meerkat_baseline_symbols(None) {
        Ok(report) => report,
        Err(err) => {
            eprintln!("baseline validation failed: {err}");
            process::exit(1);
        }
    };
```

## A17-017: Baseline scanner is incorrectly advertised as an export and version-compatibility verifier

**Candidate severity:** medium. **Independent verdict:** confirm.

A directory containing matching text can satisfy the scan without being a usable, compatible Meerkat library. The current wording overstates what this CI check proves.

### Baseline documentation evidence

- [docs/mobkit/reference/governance.mdx:60-63](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/governance.mdx?plain=1#L60-L63)

```text
The baseline check ensures that:
- Required Meerkat crates are present
- Expected API symbols are exported
- Version compatibility is maintained
```

- [docs/mobkit/reference/governance.mdx:67-67](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/governance.mdx?plain=1#L67)

```text
The `REQUIRED_MEERKAT_SYMBOLS` constant lists every Meerkat API entry point that MobKit depends on.
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/baseline.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '8,23p;62,92p;117,145p;161,200p'
```

  REQUIRED_MEERKAT_SYMBOLS is a fixed 14-string prerequisite list. The verifier checks that a directory exists, recursively reads text, and removes entries when contains_symbol finds substrings or loose token combinations. Text such as Markdown/comments is eligible. It neither inspects Rust visibility/exports nor resolves Cargo crate versions or compatibility.

  The complete verifier and scanner show that success is textual symbol evidence, not a compiler or dependency-compatibility guarantee.

### Independent challenge

The complete baseline scanner reads arbitrary eligible text and searches for a fixed prerequisite list by substrings/token combinations. Comments or Markdown can satisfy it; it does not resolve Rust exports or Cargo versions. Its list is not every used API: for example, the pinned WorkGraph composition actually calls mob_realm_id, which is absent from that list. Narrow the correction to guarantees this heuristic does not provide, not removal of its legitimate limited utility.

- [meerkat-mobkit/src/baseline.rs:8-23](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/baseline.rs#L8-L23)

```text
pub const REQUIRED_MEERKAT_SYMBOLS: &[&str] = &[
```

  The fixed 14-entry selected list is not a generated full dependency inventory.

- [meerkat-mobkit/src/baseline.rs:117-173](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/baseline.rs#L117-L173)

```text
missing.retain(|symbol| !contains_symbol(&content, symbol));
```

  Readable eligible file contents, including prose, supply the evidence; this is not compiler export checking.

- [meerkat-mobkit/src/workgraph_wiring.rs:107-123](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/workgraph_wiring.rs#L107-L123)

```text
let realm = match meerkat_core::mob_realm_id(mob_id) {
```

  An actual dependency API outside the selected baseline list independently disproves the 'every entry point' claim.

**Required correction:** Describe the check as a heuristic textual scan for selected prerequisite symbols. Remove guarantees of required crate presence, exported API visibility, version compatibility, and exhaustive API dependency coverage; distinguish those from build/dependency validation.

### Correction

Described baseline verification as a heuristic text scan over selected prerequisites, not proof of crate presence, export visibility, version compatibility, or exhaustive dependency/API coverage.

- [docs/reference/governance.mdx:80-82](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L80-L82)

```text
text files. Comments or Markdown can satisfy a match: success does not prove
that a required crate exists, an API is publicly exported, or dependency
versions are compatible.
```

- [docs/reference/governance.mdx:87-88](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L87-L88)

```text
`REQUIRED_MEERKAT_SYMBOLS` is the scanner's selected prerequisite list, not an
exhaustive inventory of every Meerkat API MobKit uses.
```

- [meerkat-mobkit/src/baseline.rs:145-145](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/baseline.rs#L145-L145)

```text
missing.retain(|symbol| !contains_symbol(&content, symbol));
```

### Final independent review

**PASS:** The current prose correctly limits baseline verification to heuristic text matching over a selected prerequisite list. The implementation scans eligible readable files, accepts direct substrings and loose token combinations, and does not examine Rust visibility or resolve dependency versions. Markdown is not an excluded extension. The fixed list is demonstrably not exhaustive: current WorkGraph composition calls meerkat_core::mob_realm_id, which is absent from that list. The document retains the scanner's limited utility while removing all four unsupported guarantees.

- [docs/reference/governance.mdx:79-90](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/governance.mdx#L79-L90)

```text
The scanner matches substrings and selected token combinations in eligible
text files. Comments or Markdown can satisfy a match: success does not prove
that a required crate exists, an API is publicly exported, or dependency
versions are compatible. Use dependency resolution and compilation to check
those properties.

### Required symbols

`REQUIRED_MEERKAT_SYMBOLS` is the scanner's selected prerequisite list, not an
exhaustive inventory of every Meerkat API MobKit uses. Missing textual matches
produce `BaselineVerificationError::MissingSymbols`; repository configuration
and read failures also use this separate error type.
```

- [meerkat-mobkit/src/baseline.rs:8-23](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/baseline.rs#L8-L23)

```text
pub const REQUIRED_MEERKAT_SYMBOLS: &[&str] = &[
    "MobEventRouter",
    "send_message(id, msg)",
    "subscribe_agent_events(id)",
    "subscribe_all_agent_events()",
    "SpawnPolicy trait",
    "respawn(id, msg)",
    "AttributedEvent",
    "Roster::session_id(id)",
    "Roster::find_by_label(k, v)",
    "SessionBuildOptions.app_context",
    "SessionBuildOptions.additional_instructions",
    "CreateSessionRequest.labels",
    "RosterEntry.labels",
    "SpawnMemberSpec.resume_session_id",
];
```

- [meerkat-mobkit/src/baseline.rs:140-147](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/baseline.rs#L140-L147)

```text
        let content = match fs::read_to_string(&entry_path) {
            Ok(content) => content,
            Err(_) => continue,
        };

        missing.retain(|symbol| !contains_symbol(&content, symbol));
        if missing.is_empty() {
            return Ok(());
```

- [meerkat-mobkit/src/baseline.rs:161-184](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/baseline.rs#L161-L184)

```text
fn should_skip_file(path: &Path) -> bool {
    let Some(extension) = path.extension().and_then(|ext| ext.to_str()) else {
        return false;
    };
    matches!(
        extension,
        "png" | "jpg" | "jpeg" | "gif" | "pdf" | "wasm" | "lock"
    )
}

fn contains_symbol(content: &str, symbol: &str) -> bool {
    if content.contains(symbol) {
        return true;
    }

    match symbol {
        "MobEventRouter" => content.contains("MobEventRouter"),
        "subscribe_agent_events(id)" => content.contains("subscribe_agent_events"),
        "subscribe_all_agent_events()" => content.contains("subscribe_all_agent_events"),
        "SpawnPolicy trait" => content.contains("trait SpawnPolicy"),
        "SessionBuildOptions.app_context" => {
            content.contains("SessionBuildOptions")
                && (content.contains("app_context") || content.contains(".app_context"))
        }
```

- [meerkat-mobkit/src/workgraph_wiring.rs:111-112](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/workgraph_wiring.rs#L111-L112)

```text
    let realm = match meerkat_core::mob_realm_id(mob_id) {
        Ok(realm) => realm.as_str().to_string(),
```

## A17-018: ConsolePolicy example and field inventory omit real struct fields

**Candidate severity:** medium. **Independent verdict:** confirm.

The guide's Rust literal cannot compile. The reference also gives an incomplete public struct shape, hiding how the console UI config is carried.

### Baseline documentation evidence

- [docs/mobkit/guides/console.mdx:464-467](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/guides/console.mdx?plain=1#L464-L467)

```text
ConsolePolicy {
    require_app_auth: true,   // the default; set false only for local development
    read_only: false,         // set true for view-only deployments
}
```

- [docs/mobkit/reference/configuration.mdx:829-833](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/configuration.mdx?plain=1#L829-L833)

```text
ConsolePolicy {
    require_app_auth: bool,
    read_only: bool,
    fetch_timeout_ms: Option<u64>,
}
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/decisions.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '123,142p'
```

  ConsolePolicy has require_app_auth, read_only, fetch_timeout_ms, and ui: ConsoleUiConfig. Default initializes all four, including None and ConsoleUiConfig::default().

  The concrete example lacks two fields and no struct update is present; the configuration inventory omits ui.

### Independent challenge

ConsolePolicy has four fields. Serde defaults do not permit omitting fields from a Rust literal, so the guide's two-field construction is invalid without struct update syntax. The configuration reference is a schema illustration rather than runnable code, but its field inventory still omits ui. Its actual default is available and preserves the stated auth/read-only values.

- [meerkat-mobkit/src/decisions.rs:123-142](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/decisions.rs#L123-L142)

```text
pub ui: ConsoleUiConfig,
```

  The complete type and Default implementation include fetch_timeout_ms and ui as well as the two documented booleans.

**Required correction:** Use ..ConsolePolicy::default() in the concrete guide literal. Add ui: ConsoleUiConfig and its default/purpose to the configuration shape and field table, without treating the type-inventory illustration as a runnable constructor.

### Correction

Completed the concrete ConsolePolicy literal with struct update syntax and added ui: ConsoleUiConfig with its real default and view-only purpose to the configuration inventory.

- [docs/guides/console.mdx:611-612](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/guides/console.mdx#L611-L612)

```text
    read_only: false,         // set true for view-only deployments
    ..ConsolePolicy::default()
```

- [docs/reference/configuration.mdx:840-840](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/configuration.mdx#L840-L840)

```text
| `ui` | `ConsoleUiConfig` | `ConsoleUiConfig::default()` | Stock-console presentation configuration, projected through `/console/experience` as `console_config`; does not control runtime authorization |
```

- [meerkat-mobkit/src/decisions.rs:131-131](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/decisions.rs#L131-L131)

```text
    pub ui: ConsoleUiConfig,
```

### Final independent review

**PASS:** The concrete ConsolePolicy literal now supplies the omitted fields through its real Default implementation without changing the two explicitly shown policy values. The configuration shape and table include ui with the correct ConsoleUiConfig type and default. The stated presentation role is backed by the experience projection reading console_policy.ui and serializing console_config; it is not presented as an authorization setting. The reference's type inventory remains distinct from a runnable constructor.

- [docs/guides/console.mdx:609-613](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/guides/console.mdx#L609-L613)

```text
ConsolePolicy {
    require_app_auth: true,   // the default; set false only for local development
    read_only: false,         // set true for view-only deployments
    ..ConsolePolicy::default()
}
```

- [docs/reference/configuration.mdx:827-832](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/configuration.mdx#L827-L832)

```text
ConsolePolicy {
    require_app_auth: bool,
    read_only: bool,
    fetch_timeout_ms: Option<u64>,
    ui: ConsoleUiConfig,
}
```

- [docs/reference/configuration.mdx:840-840](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/configuration.mdx#L840-L840)

```text
| `ui` | `ConsoleUiConfig` | `ConsoleUiConfig::default()` | Stock-console presentation configuration, projected through `/console/experience` as `console_config`; does not control runtime authorization |
```

- [meerkat-mobkit/src/decisions.rs:124-143](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/decisions.rs#L124-L143)

```text
pub struct ConsolePolicy {
    pub require_app_auth: bool,
    #[serde(default)]
    pub read_only: bool,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fetch_timeout_ms: Option<u64>,
    #[serde(default, skip_serializing_if = "ConsoleUiConfig::is_default")]
    pub ui: ConsoleUiConfig,
}

impl Default for ConsolePolicy {
    fn default() -> Self {
        Self {
            require_app_auth: true,
            read_only: false,
            fetch_timeout_ms: None,
            ui: ConsoleUiConfig::default(),
        }
    }
}
```

- [meerkat-mobkit/src/runtime/console_ingress.rs:791-795](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/console_ingress.rs#L791-L795)

```text
    let console_title = console_config.title.as_deref().unwrap_or("Mob Console");
    let mut body = serde_json::json!({
        "contract_version": MOBKIT_CONTRACT_VERSION,
        "runtime_id": live_snapshot.runtime_id,
        "console_config": console_config,
```

## A17-019: Rust HTTP bind-policy example passes a string where GatewaySurface is required

**Candidate severity:** medium. **Independent verdict:** confirm.

An embedder following the published fail-closed binding recipe cannot call the function as shown. The surrounding auth-or-allow_remote policy explanation is otherwise supported by for_gateway.

### Baseline documentation evidence

- [docs/mobkit/sdks/rust.mdx:314-315](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/sdks/rust.mdx?plain=1#L314-L315)

```text
Run `validate_http_bind_policy("my-host", addr,
HttpBindPolicy::for_gateway(allow_remote, &decisions))`
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/gateway_composition.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '508,518p;615,624p'
```

  GatewaySurface is a closed enum with RpcGateway and MobkitGateway. validate_http_bind_policy takes `(surface: GatewaySurface, listen: SocketAddr, policy: HttpBindPolicy)`, not an arbitrary host-name string.

  The exact public helper signature makes the example a Rust type mismatch.

### Independent challenge

The function requires the closed GatewaySurface enum, not an Into/string-like parameter. A free-form host name cannot type-check. The surrounding non-loopback auth-or-explicit-permission explanation is supported by HttpBindPolicy::for_gateway and should not be rewritten as a different policy.

- [meerkat-mobkit/src/gateway_composition.rs:508-518](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/gateway_composition.rs#L508-L518)

```text
pub enum GatewaySurface {
```

  Only RpcGateway and MobkitGateway identify the bundled surfaces.

- [meerkat-mobkit/src/gateway_composition.rs:615-624](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/gateway_composition.rs#L615-L624)

```text
surface: GatewaySurface,
```

  The public signature has no string conversion seam.

**Required correction:** Use an imported, appropriate GatewaySurface variant in the Rust call and explain the argument's bundled-surface meaning instead of an arbitrary host label. Preserve the supported bind-policy explanation.

### Correction

Used imported GatewaySurface::RpcGateway in the bind-policy call, explained the closed bundled-surface diagnostic vocabulary, and retained auth-or-explicit-remote-acknowledgement policy.

- [docs/sdks/rust.mdx:325-329](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/rust.mdx#L325-L329)

```text
validate_http_bind_policy(
    GatewaySurface::RpcGateway,
    addr,
    HttpBindPolicy::for_gateway(allow_remote, &decisions),
)?;
```

- [meerkat-mobkit/src/gateway_composition.rs:615-618](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/gateway_composition.rs#L615-L618)

```text
pub fn validate_http_bind_policy(
    surface: GatewaySurface,
    listen: SocketAddr,
    policy: HttpBindPolicy,
```

### Final independent review

**PASS:** The call now imports and passes GatewaySurface::RpcGateway rather than an arbitrary string, matching the exact function signature. The accompanying explanation accurately names the two closed bundled-gateway variants. HttpBindPolicy::for_gateway still allows non-loopback binding only through explicit acknowledgment or enforced console authentication; the correction preserves that policy rather than weakening the typed API. Host-supplied addr, allow_remote and decision state are identified as prerequisites, and the deployment link resolves.

- [docs/sdks/rust.mdx:321-329](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/rust.mdx#L321-L329)

```text
use meerkat_mobkit::gateway_composition::{
    GatewaySurface, HttpBindPolicy, validate_http_bind_policy,
};

validate_http_bind_policy(
    GatewaySurface::RpcGateway,
    addr,
    HttpBindPolicy::for_gateway(allow_remote, &decisions),
)?;
```

- [meerkat-mobkit/src/gateway_composition.rs:513-518](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/gateway_composition.rs#L513-L518)

```text
pub enum GatewaySurface {
    /// The SDK stdin JSON-RPC gateway.
    RpcGateway,
    /// The standalone console/HTTP gateway.
    MobkitGateway,
}
```

- [meerkat-mobkit/src/gateway_composition.rs:615-624](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/gateway_composition.rs#L615-L624)

```text
pub fn validate_http_bind_policy(
    surface: GatewaySurface,
    listen: SocketAddr,
    policy: HttpBindPolicy,
) -> Result<(), HttpBindPolicyError> {
    if policy.allow_remote || listen.ip().is_loopback() {
        return Ok(());
    }
    Err(HttpBindPolicyError::RemoteBindRequiresExplicitAllow { surface, listen })
}
```

- [meerkat-mobkit/src/gateway_composition.rs:501-505](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/gateway_composition.rs#L501-L505)

```text
    pub fn for_gateway(allow_remote: bool, decisions: &RuntimeDecisionState) -> Self {
        Self {
            allow_remote: allow_remote || ConsoleAuthPosture::of(decisions).is_enforced(),
        }
    }
```

## A17-020: Memory guide incorrectly limits MCP trust classification to qualified tool names

**Candidate severity:** medium. **Independent verdict:** confirm.

The stated release limitation and mandatory manual listing of unqualified MCP tool names are obsolete. The correct distinction is typed dispatch-time attribution with a name-based fallback when provenance is absent.

### Baseline documentation evidence

- [docs/mobkit/concepts/memory.mdx:186-186](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/memory.mdx?plain=1#L186)

```text
In this release classification is by tool *name* (tool events do not carry provenance yet), so MCP tools are attributable to a server only when their names are qualified as `mcp__<server>__<tool>`; list unqualified MCP tool names in `untrusted_tools`.
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/memory/taint.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '193,233p'
```

  classify_tool_with_provenance checks explicit name policies, then recognizes ToolSourceKind::Mcp and classifies using provenance.source_id, regardless of tool-name qualification. The mcp__ name path is a fallback. Unallowlisted MCP servers are classified Untrusted.

  The active classifier supports typed server attribution in the documented release.

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/memory/dispatch_taint.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '217,230p'; gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/mob_handle_runtime.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '720,726p'
```

  The LLM-client decorator joins tool results to `tools[*].provenance` and calls observe_dispatched_tool_result. Member build handling invokes attach_member_taint_decorator on the request.

  This is wired production behavior, not an unused richer classifier. It does not require claiming that every asynchronous event carries provenance.

### Independent challenge

Typed MCP attribution is already active through the request ToolDef.provenance catalog even for unqualified names. The asynchronous observer still has a name-only path, so the subclaim that those events lack provenance need not be replaced by a false claim that every event is richer. The defect is the blanket release-wide limitation and mandatory unqualified-name workaround, not the existence of the fallback.

- [meerkat-mobkit/src/memory/taint.rs:193-233](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/memory/taint.rs#L193-L233)

```text
return self.classify_mcp_server(provenance.source_id.as_str(), name);
```

  After non-overridable web and explicit name rules, typed MCP provenance supplies server identity; name parsing remains the fallback.

- [meerkat-mobkit/src/memory/dispatch_taint.rs:217-230](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/memory/dispatch_taint.rs#L217-L230)

```text
tracker.observe_dispatched_tool_result(&self.identity, name, provenance);
```

  The live decorator joins catalog provenance to tool results and passes it into classification.

- [meerkat-mobkit/src/memory/dispatch_taint.rs:428-457](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/memory/dispatch_taint.rs#L428-L457)

```text
let tools = vec![mcp_tool("scrape_page", "scraper")];
```

  The inspected regression specifically attributes an unqualified tool to its MCP server without an observer; it was not executed in this audit.

**Required correction:** Describe typed dispatch-time MCP source_id attribution, name-based fallback when provenance is unavailable, and existing precedence of web/explicit-tool/server rules. Remove the blanket name-only limitation; do not claim observe-stream events universally carry provenance.

### Correction

Documented typed dispatch-time MCP source_id attribution even for unqualified names, fallback name classification when unavailable, the remaining name-only observation path, and real web/tool/server precedence.

- [docs/concepts/memory.mdx:188-190](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L188-L190)

```text
Dispatch-time classification joins tool results with the request's
`ToolDef.provenance` catalog. Typed MCP provenance attributes a tool to its
`source_id` even when the tool name is unqualified;
```

- [meerkat-mobkit/src/memory/taint.rs:211-214](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/memory/taint.rs#L211-L214)

```text
if let Some(provenance) = provenance
            && provenance.kind == ToolSourceKind::Mcp
        {
            return self.classify_mcp_server(provenance.source_id.as_str(), name);
```

### Final independent review

**PASS:** The revised guide distinguishes the two real paths instead of claiming every event has provenance. Dispatch-time marking joins tool-use names/results to the request ToolDef catalog, then the classifier uses ToolSourceKind::Mcp and source_id even for unqualified names. The complete decision order is built-in web/fetch names, explicit untrusted names, explicit trusted names, typed MCP attribution, qualified-name fallback, then trusted; the server step consults trusted_mcp_servers. The asynchronous tool-event observer still calls the name-only classifier. These implementation facts support both the correction and its explicit fallback limitations.

- [docs/concepts/memory.mdx:184-184](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L184-L184)

```text
- **Content-trust classification** (`agent_memory.content_trust`): built-in web/fetch tools and provider-native search are always untrusted. Tool classification checks those built-in names first, then `untrusted_tools`, then `trusted_tools`, then MCP server attribution against `trusted_mcp_servers`. Explicit tool rules therefore take precedence over server rules, but cannot make the built-in web/fetch tools trusted.
```

- [docs/concepts/memory.mdx:188-195](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L188-L195)

```text
Dispatch-time classification joins tool results with the request's
`ToolDef.provenance` catalog. Typed MCP provenance attributes a tool to its
`source_id` even when the tool name is unqualified; servers are untrusted
unless allowlisted. When MCP provenance is unavailable, classification falls
back to the `mcp__<server>__<tool>` name shape and the explicit tool lists.
Otherwise-unclassified tools default to trusted. The asynchronous
observe-stream tool-event path remains name-based; it does not universally
carry typed provenance.
```

- [meerkat-mobkit/src/memory/taint.rs:193-234](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/memory/taint.rs#L193-L234)

```text
    pub fn classify_tool_with_provenance(
        &self,
        name: &str,
        provenance: Option<&ToolProvenance>,
    ) -> ToolContentTrust {
        if ALWAYS_UNTRUSTED_TOOL_NAMES.contains(&name) {
            return ToolContentTrust::Untrusted {
                source: format!("web tool '{name}'"),
            };
        }
        if self.untrusted_tools.iter().any(|tool| tool == name) {
            return ToolContentTrust::Untrusted {
                source: format!("configured untrusted tool '{name}'"),
            };
        }
        if self.trusted_tools.iter().any(|tool| tool == name) {
            return ToolContentTrust::Trusted;
        }
        if let Some(provenance) = provenance
            && provenance.kind == ToolSourceKind::Mcp
        {
            return self.classify_mcp_server(provenance.source_id.as_str(), name);
        }
        if let Some(rest) = name.strip_prefix(MCP_QUALIFIED_PREFIX) {
            let server = rest.split("__").next().unwrap_or(rest);
            return self.classify_mcp_server(server, name);
        }
        ToolContentTrust::Trusted
    }

    fn classify_mcp_server(&self, server: &str, name: &str) -> ToolContentTrust {
        if self
            .trusted_mcp_servers
            .iter()
            .any(|trusted| trusted == server)
        {
            return ToolContentTrust::Trusted;
        }
        ToolContentTrust::Untrusted {
            source: format!("MCP server '{server}' (tool '{name}')"),
        }
    }
```

- [meerkat-mobkit/src/memory/dispatch_taint.rs:223-230](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/memory/dispatch_taint.rs#L223-L230)

```text
                        let Some(name) = names.get(result.tool_use_id.as_str()) else {
                            continue;
                        };
                        let provenance = tools
                            .iter()
                            .find(|tool| tool.name.as_ref() == *name)
                            .and_then(|tool| tool.provenance.as_ref());
                        tracker.observe_dispatched_tool_result(&self.identity, name, provenance);
```

- [meerkat-mobkit/src/memory/taint.rs:384-389](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/memory/taint.rs#L384-L389)

```text
            AgentEvent::ToolResultReceived { name, .. }
            | AgentEvent::ToolExecutionCompleted { name, .. } => {
                if let ToolContentTrust::Untrusted { source } = self.config.classify_tool(name) {
                    self.mark_identity_tainted(identity, source);
                }
            }
```

## A17-021: Memory guide says the first-ingestion race is still awaiting a future fix

**Candidate severity:** medium. **Independent verdict:** confirm.

The guide recommends a more restrictive posture on the false premise that the pinned release still relies exclusively on asynchronous first-ingestion observation. This finding does not claim universal prompt-injection safety or cover arbitrary custom compositions.

### Baseline documentation evidence

- [docs/mobkit/concepts/memory.mdx:188-188](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/memory.mdx?plain=1#L188)

```text
Choose `"quarantined"` if you cannot accept the known race: a write issued in the same turn as the session's *first* untrusted ingestion can beat the observe-stream taint signal (closed in a later phase by dispatch-time taint visibility).
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/memory/dispatch_taint.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '130,151p;253,275p'
```

  attach_member_taint_decorator composes TaintObservingLlmClient into SessionBuildOptions. Before calling the inner client it marks request tool ingestions; after receiving a result it marks typed server-tool content before returning that result to the loop.

  The pinned release has the dispatch-ordered join that this paragraph postpones to a later phase.

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/unified_runtime/builder.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '1430,1441p'; gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/bin/rpc_gateway.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '12965,12976p'; gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/mob_handle_runtime.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '720,726p'
```

  The full-stack Rust builder fills the dispatch taint slot with stack.taint. rpc_gateway fills the same slot with its tracker, while member builds attach the decorator. The asynchronous observer remains in addition to this join.

  Both relevant full-stack composition paths actually install the fix; its existence is not inferred from a future plan or unused type.

### Independent challenge

Narrow to the shipped dispatch-ordered protection for LLM-authored writes derived from ingested content in the bundled/full-stack memory composition. The wrapper marks request ingestions before the inner LLM call and server-tool blocks before returning its response. Both bundled gateway and full-stack builder fill the shared slot, and member builds attach the wrapper. The old observer-only first-ingestion warning is therefore stale. An unfilled custom slot remains a pass-through, and this evidence does not establish universal race freedom or prompt-injection safety.

- [meerkat-mobkit/src/memory/dispatch_taint.rs:253-275](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/memory/dispatch_taint.rs#L253-L275)

```text
self.mark_request_ingestions(&tracker, messages, tools);
```

  Taint updates occur before the inner request and before the decorated response returns to the loop.

- [meerkat-mobkit/src/mob_handle_runtime.rs:720-726](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/mob_handle_runtime.rs#L720-L726)

```text
crate::memory::dispatch_taint::attach_member_taint_decorator(&mut req, slot);
```

  Member preparation actually installs the decorator.

- [meerkat-mobkit/src/unified_runtime/builder.rs:1430-1441](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/unified_runtime/builder.rs#L1430-L1441)

```text
dispatch_taint_slot.fill(stack.taint.clone());
```

  The full-stack builder binds the live tracker rather than leaving the slot empty.

- [meerkat-mobkit/src/bin/rpc_gateway.rs:12967-12978](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/bin/rpc_gateway.rs#L12967-L12978)

```text
dispatch_taint_slot.fill(tracker.clone());
```

  The gateway installs the same synchronous join in addition to the async observer.

- [meerkat-mobkit/tests/memory_dispatch_taint.rs:283-310](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/tests/memory_dispatch_taint.rs#L283-L310)

```text
requests[2].contains("QUARANTINED"),
```

  The inspected regression covers first ingestion and a content-derived write in the same turn without an async observer. The neighboring unfilled-slot control was also inspected.

**Required correction:** Replace the future-phase statement with the implemented dispatch-time ordering and its bundled/full-stack scope. Retain quarantined as the stricter all-LLM-writes policy, not as a required workaround for this already-addressed observer race. Avoid universal safety claims.

### Correction

Replaced the future-fix/first-ingestion-race warning with the shipped dispatch-ordered request/server-tool classification. Scoped protection to the bundled/full-stack memory composition with a filled tracker slot; kept quarantined as a stricter all-LLM-writes posture and disclaimed universal prompt-injection safety.

- [docs/concepts/memory.mdx:199-201](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L199-L201)

```text
It marks tool-result ingestions before the next LLM call, and marks typed
provider-native server-tool content before returning the response to the
agent loop.
```

- [docs/concepts/memory.mdx:203-204](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L203-L204)

```text
A custom composition with an
unfilled slot is a pass-through and does not gain this ordering protection.
```

- [meerkat-mobkit/src/unified_runtime/builder.rs:1475-1475](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/unified_runtime/builder.rs#L1475-L1475)

```text
dispatch_taint_slot.fill(stack.taint.clone());
```

- [meerkat-mobkit/src/bin/rpc_gateway.rs:12908-12908](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/bin/rpc_gateway.rs#L12908-L12908)

```text
dispatch_taint_slot.fill(tracker.clone());
```

### Final independent review

**PASS:** The obsolete future-fix warning is replaced with the implemented, bounded ordering claim. Member preparation attaches the LLM-client decorator; the decorator marks classified request ingestions before the inner LLM call and typed server-tool response blocks before returning the response. Full-stack assembly installs a gate backed by the same tracker, and both the SDK gateway and UnifiedRuntime full-stack path fill the shared slot. The SQLite commit path consults that gate. I also inspected the no-observer same-turn regression and its unfilled-slot control without executing them. The text correctly retains quarantined as the stricter first-pass write posture, limits the protection to the filled-slot bundled/full-stack composition and classified content, and explicitly disclaims universal safety. It does not incorrectly extend the guarantee to the plain provider-only builder path or an unfilled custom composition.

- [docs/concepts/memory.mdx:186-186](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L186-L186)

```text
- **Write posture** (`agent_memory.llm_writes`): `"observed"` (default) quarantines only tainted-session writes; `"quarantined"` is the stricter policy that quarantines *every* LLM-authored write pending review.
```

- [docs/concepts/memory.mdx:197-206](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L197-L206)

```text
The bundled SDK gateway and `UnifiedRuntime` full-stack memory composition
fill a shared `DispatchTaintSlot` and attach the member LLM-client decorator.
It marks tool-result ingestions before the next LLM call, and marks typed
provider-native server-tool content before returning the response to the
agent loop. LLM-authored memory writes derived from that classified content
therefore reach the store gate after taint is marked, rather than racing the
asynchronous observer's first-ingestion signal. A custom composition with an
unfilled slot is a pass-through and does not gain this ordering protection.
This is a scoped ingestion/write ordering guarantee, not universal
prompt-injection safety.
```

- [meerkat-mobkit/src/mob_handle_runtime.rs:724-726](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/mob_handle_runtime.rs#L724-L726)

```text
        if let Some(slot) = self.dispatch_taint.as_ref() {
            crate::memory::dispatch_taint::attach_member_taint_decorator(&mut req, slot);
        }
```

- [meerkat-mobkit/src/memory/dispatch_taint.rs:253-276](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/memory/dispatch_taint.rs#L253-L276)

```text
    async fn stream_response(
        &self,
        messages: &[Message],
        tools: &[Arc<ToolDef>],
        max_tokens: u32,
        temperature: Option<f32>,
        provider_params: Option<&ProviderParamsOverride>,
    ) -> Result<LlmStreamResult, AgentError> {
        if let Some(tracker) = self.slot.tracker() {
            self.mark_request_ingestions(&tracker, messages, tools);
        }
        let result = self
            .inner
            .stream_response(messages, tools, max_tokens, temperature, provider_params)
            .await?;
        if let Some(tracker) = self.slot.tracker() {
            for block in result.blocks() {
                if let AssistantBlock::ServerToolContent { kind, .. } = block {
                    tracker.observe_dispatched_server_tool(&self.identity, kind);
                }
            }
        }
        Ok(result)
    }
```

- [meerkat-mobkit/src/memory_wiring.rs:184-188](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/memory_wiring.rs#L184-L188)

```text
    let taint = SessionTaintTracker::new(config.content_trust.clone());
    taintable.set_llm_write_gate(Arc::new(TaintLlmWriteGate::new(
        Some(taint.clone()),
        config.llm_writes,
    )));
```

- [meerkat-mobkit/src/unified_runtime/builder.rs:1415-1420](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/unified_runtime/builder.rs#L1415-L1420)

```text
        // Full-stack path (persistent_agent_memory_stack): firewall + engines
        // + observer over the pre-opened stack provider.
        if let (Some(provider), Some(engines)) =
            (stack_provider, self.agent_memory_engines.as_ref())
        {
            let config = self.agent_memory_config.clone().unwrap_or_default();
```

- [meerkat-mobkit/src/unified_runtime/builder.rs:1475-1475](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/unified_runtime/builder.rs#L1475-L1475)

```text
            dispatch_taint_slot.fill(stack.taint.clone());
```

- [meerkat-mobkit/src/bin/rpc_gateway.rs:12908-12915](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/bin/rpc_gateway.rs#L12908-L12915)

```text
                    dispatch_taint_slot.fill(tracker.clone());
                    // Observe-stream feed lives for the gateway process;
                    // forgetting the guard keeps the task running.
                    std::mem::forget(meerkat_mobkit::spawn_member_event_observer(
                        runtime.mob_handle(),
                        sinks,
                    ));
                    agent_memory_taint = Some(tracker);
```

- [meerkat-mobkit/src/memory/taint.rs:784-797](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/memory/taint.rs#L784-L797)

```text
        if !author.is_llm() {
            return None;
        }
        if self.llm_writes == AgentMemoryLlmWrites::Quarantined
            && kind != StagedBatchKind::ReviewVerdict
        {
            return Some("llm_writes=quarantined policy".to_string());
        }
        let tracker = self.tracker.as_ref()?;
        if let MemoryAuthor::Agent { identity } = author
            && let Some(state) = tracker.identity_taint(identity)
        {
            return Some(format!("session tainted by {}", state.source));
        }
```

- [meerkat-mobkit/src/memory/sqlite_store.rs:2924-2925](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/memory/sqlite_store.rs#L2924-L2925)

```text
    let quarantine =
        gate.and_then(|gate| gate.quarantine_reason(&batch.author, batch.kind, &evidence));
```

## A17-022: Rust memory examples supply borrowed strings to owned String fields

**Candidate severity:** low. **Independent verdict:** confirm.

These concrete Rust construction examples do not type-check against the documented release.

### Baseline documentation evidence

- [docs/mobkit/concepts/memory.mdx:52-55](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/memory.mdx?plain=1#L52-L55)

```text
LocalJsonMemoryBackendConfig {
    state_path: "/var/mobkit/state/memory-ledger-state.json",
    health_check_endpoint: Some("http://localhost:3000"),
}
```

- [docs/mobkit/concepts/memory.mdx:233-236](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/memory.mdx?plain=1#L233-L236)

```text
MemoryStoreInfo {
    store: "knowledge_graph",
    record_count: 42,
}
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '556,561p;768,772p'
```

  LocalJsonMemoryBackendConfig fields are state_path: String and health_check_endpoint: Option<String>. MemoryStoreInfo.store is String.

  Rust struct field assignment does not implicitly convert the illustrated &str literals into String or Option<String>.

### Independent challenge

The concrete Rust examples assign string literals to String and Option<String> fields. Serde configuration support and constructors elsewhere do not provide implicit conversion inside Rust struct literals. The fix is limited to the three owned-string values, not the backend behavior.

- [meerkat-mobkit/src/runtime.rs:556-561](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime.rs#L556-L561)

```text
pub health_check_endpoint: Option<String>,
```

  state_path and the optional endpoint own their strings.

- [meerkat-mobkit/src/runtime.rs:768-772](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime.rs#L768-L772)

```text
pub store: String,
```

  MemoryStoreInfo also requires an owned store string.

**Required correction:** Add .into() or .to_string() to state_path, the string inside Some(health_check_endpoint), and MemoryStoreInfo.store in the canonical examples.

### Correction

Added owned String conversions for LocalJsonMemoryBackendConfig.state_path, its optional health_check_endpoint, and MemoryStoreInfo.store.

- [docs/concepts/memory.mdx:51-52](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L51-L52)

```text
    state_path: "/var/mobkit/state/memory-ledger-state.json".into(),
    health_check_endpoint: Some("http://localhost:3000".into()),
```

- [docs/concepts/memory.mdx:252-252](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L252-L252)

```text
    store: "knowledge_graph".into(),
```

### Final independent review

**PASS:** All three required owned-string corrections appear in the actual diff: state_path, the string inside Some(health_check_endpoint), and MemoryStoreInfo.store now use .into(). Both complete current struct definitions have exactly the illustrated fields; the conversions satisfy String and Option<String>, while the integer literal is valid for record_count: usize. No backend behavior was changed to accommodate the examples.

- [docs/concepts/memory.mdx:50-53](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L50-L53)

```text
LocalJsonMemoryBackendConfig {
    state_path: "/var/mobkit/state/memory-ledger-state.json".into(),
    health_check_endpoint: Some("http://localhost:3000".into()),
}
```

- [docs/concepts/memory.mdx:251-254](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/memory.mdx#L251-L254)

```text
MemoryStoreInfo {
    store: "knowledge_graph".into(),
    record_count: 42,
}
```

- [meerkat-mobkit/src/runtime.rs:566-570](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime.rs#L566-L570)

```text
pub struct LocalJsonMemoryBackendConfig {
    pub state_path: String,
    #[serde(default)]
    pub health_check_endpoint: Option<String>,
}
```

- [meerkat-mobkit/src/runtime.rs:778-781](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime.rs#L778-L781)

```text
pub struct MemoryStoreInfo {
    pub store: String,
    pub record_count: usize,
}
```

## A17-023: Python structural-event reference hard-codes a stale 25-variant count

**Candidate severity:** low. **Independent verdict:** confirm.

Clients may incorrectly treat an obsolete variant inventory as exhaustive.

### Baseline documentation evidence

- [docs/mobkit/sdks/python.mdx:239-242](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/sdks/python.mdx?plain=1#L239-L242)

```text
every meerkat `MobEventKind` (25 variants) into a typed envelope
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/unified_runtime/mob_events.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '551,635p'
```

  The exhaustive event_kind_label match has 63 distinct MobEventKind arms, counted from its body. It includes MobDefinitionUpdated, multiple remote/placed lifecycle variants, ObjectiveOwnerBound, and ObjectiveConcluded in addition to basic mob/member/flow events.

  The documented MobKit release's own projection already handles substantially more than 25 variants; no current-checkout Meerkat enum was substituted.

### Independent challenge

Independently counted 63 distinct MobEventKind arms in the pinned MobKit event_kind_label match, with no wildcard. Thus 25 is not the pinned release's inventory. This proof does not substitute a newer Meerkat checkout's enum and does not require freezing another manually maintained count.

- [meerkat-mobkit/src/unified_runtime/mob_events.rs:551-635](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/unified_runtime/mob_events.rs#L551-L635)

```text
MobEventKind::ObjectiveConcluded { .. } => "objective_concluded",
```

  The complete exhaustive label mapper has 63 distinct variant arms, including placed/remote lifecycle and objective events.

**Required correction:** Remove the stale '(25 variants)' from the canonical Python SDK reference rather than replacing it with another ungenerated fixed inventory count.

### Correction

Removed the stale manually maintained 25-variant count without replacing it with another frozen number.

- [docs/sdks/python.mdx:257-257](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/python.mdx#L257-L257)

```text
every meerkat `MobEventKind` into a typed envelope
```

- [meerkat-mobkit/src/unified_runtime/mob_events.rs:551-551](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/unified_runtime/mob_events.rs#L551-L551)

```text
fn event_kind_label(kind: &MobEventKind) -> &'static str {
```

### Final independent review

**PASS:** The stale parenthetical count was removed without replacing it with another frozen count or closed client-side inventory. Independent inspection of the current event_kind_label implementation finds 63 explicit variant arms and no wildcard, confirming that 25 was not a valid current inventory. The surrounding typed-envelope description is retained; this bounded correction neither changes projection behavior nor narrows event compatibility.

- [docs/sdks/python.mdx:256-259](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/python.mdx#L256-L259)

```text
`MobHandle.query_mob_events()` and `subscribe_mob_events()` project
every meerkat `MobEventKind` into a typed envelope
preserving `mob_id`, `run_id`, `step_id`, `agent_identity`, and the
full payload.
```

- [meerkat-mobkit/src/unified_runtime/mob_events.rs:551-552](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/unified_runtime/mob_events.rs#L551-L552)

```text
fn event_kind_label(kind: &MobEventKind) -> &'static str {
    match kind {
```

- [meerkat-mobkit/src/unified_runtime/mob_events.rs:628-633](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/unified_runtime/mob_events.rs#L628-L633)

```text
        MobEventKind::TopologyViolation { .. } => "topology_violation",
        MobEventKind::SupervisorEscalation { .. } => "supervisor_escalation",
        MobEventKind::SupervisorEscalationFailed { .. } => "supervisor_escalation_failed",
        MobEventKind::OperatorActionRecorded { .. } => "operator_action_recorded",
        MobEventKind::ObjectiveOwnerBound { .. } => "objective_owner_bound",
        MobEventKind::ObjectiveConcluded { .. } => "objective_concluded",
```

## A17-024: Current dependency guidance names Meerkat 0.8.32 although MobKit pins 0.8.38

**Candidate severity:** low. **Independent verdict:** confirm.

These are present-tense compatibility claims, not historical release notes. The stale future-version instruction is particularly misleading because the implementation still intentionally skips that injection mode.

### Baseline documentation evidence

- [docs/mobkit/reference/configuration.mdx:942-942](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/reference/configuration.mdx?plain=1#L942)

```text
With the current Meerkat 0.8.32 pairing, ambient per-turn memory requires `runtime_mode = "turn_driven"`; remove this restriction after the Meerkat 0.8.33 carrier change lands.
```

- [docs/mobkit/sdks/python.mdx:669-671](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/sdks/python.mdx?plain=1#L669-L671)

```text
meerkat 0.8.32 (the pinned release)
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/Cargo.toml?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '59,70p'
```

  The pinned MobKit source exact-pins meerkat-core, meerkat-client, meerkat-comms, meerkat-contracts, meerkat-mob, meerkat-mob-mcp, meerkat-mcp, meerkat-models, and meerkat itself at =0.8.38.

  The allegedly current/pinned 0.8.32 version is not the dependency declared by this documented release.

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/identity_first/runtime.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '8398,8413p'
```

  The actual MobKit runtime still branches on MobRuntimeMode::AutonomousHost, records RuntimeModeAutonomousHost, and returns an empty injected-context vector.

  Correcting the stale version must not falsely remove the still-implemented runtime-mode restriction just because 0.8.33 has been surpassed.

### Independent challenge

The documented MobKit release exact-pins Meerkat =0.8.38, not 0.8.32. These are present-tense pairing claims, not historical changelog text. Crucially, MobKit still explicitly skips autonomous-host ambient injection; reaching a dependency version above 0.8.33 does not justify deleting that restriction. The Anthropic caching-default claim is outside the proven correction and must not be silently changed.

- [meerkat-mobkit/Cargo.toml:59-69](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/Cargo.toml#L59-L69)

```text
meerkat-core = { version = "=0.8.38" }
```

  The exact dependency declarations establish the pinned pairing independently of documentation.

- [meerkat-mobkit/src/identity_first/runtime.rs:8398-8413](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/identity_first/runtime.rs#L8398-L8413)

```text
if runtime_mode == meerkat_mob::MobRuntimeMode::AutonomousHost {
```

  The active mode branch records RuntimeModeAutonomousHost and returns no injected context despite the newer dependency.

**Required correction:** Correct or remove the stale 0.8.32 pairing statements in both canonical pages and remove the obsolete instruction to drop the restriction after 0.8.33. Preserve the implemented turn_driven restriction; do not alter provider-default guidance without separate exact-version evidence.

### Correction

Removed stale present-tense Meerkat 0.8.32 pairing claims and the obsolete promise to drop the ambient-memory restriction at 0.8.33. Preserved current turn_driven enforcement and the unchallenged provider-default guidance; did not replace release-stamped installation versions.

- [docs/reference/configuration.mdx:942-942](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/configuration.mdx#L942-L942)

```text
Ambient per-turn memory requires `runtime_mode = "turn_driven"`: MobKit still explicitly skips recall for `autonomous_host` delivery with the typed reason `runtime_mode_autonomous_host`.
```

- [docs/sdks/python.mdx:687-687](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/python.mdx#L687-L687)

```text
caching is such a knob; Meerkat defaults it to
```

- [meerkat-mobkit/Cargo.toml:75-75](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/Cargo.toml#L75-L75)

```text
meerkat-memory = { version = "=0.8.40" }
```

- [meerkat-mobkit/src/identity_first/runtime.rs:8528-8528](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/identity_first/runtime.rs#L8528-L8528)

```text
crate::memory::coordinator::TurnInjectionSkip::RuntimeModeAutonomousHost,
```

### Final independent review

**PASS:** Both obsolete present-tense 0.8.32 attributions and the instruction to remove the restriction after 0.8.33 are gone. Current manifests exact-pin Meerkat 0.8.40, while MobKit still explicitly skips ambient recall on the autonomous-host work-delivery path and reports runtime_mode_autonomous_host. The replacement preserves that implemented restriction without making dependency advancement its removal condition. A baseline comparison confirms the Anthropic provider-parameter guidance is otherwise byte-for-byte unchanged; it was not re-adjudicated outside the narrowed scope.

- [docs/reference/configuration.mdx:942-942](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/reference/configuration.mdx#L942-L942)

```text
Ambient per-turn memory requires `runtime_mode = "turn_driven"`: MobKit still explicitly skips recall for `autonomous_host` delivery with the typed reason `runtime_mode_autonomous_host`. A dependency version change alone does not remove that runtime restriction.
```

- [docs/sdks/python.mdx:687-688](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/python.mdx#L687-L688)

```text
caching is such a knob; Meerkat defaults it to
`disabled` on every backend, so a long-lived identity opts in here:
```

- [meerkat-mobkit/Cargo.toml:59-66](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/Cargo.toml#L59-L66)

```text
meerkat-core = { version = "=0.8.40" }
meerkat-client = { version = "=0.8.40" }
meerkat-comms = { version = "=0.8.40" }
meerkat-contracts = { version = "=0.8.40" }
meerkat-mob = { version = "=0.8.40" }
meerkat-mob-mcp = { version = "=0.8.40" }
meerkat-mcp = { version = "=0.8.40" }
meerkat-models = { version = "=0.8.40" }
```

- [meerkat-mobkit/src/identity_first/runtime.rs:8522-8531](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/identity_first/runtime.rs#L8522-L8531)

```text
                if matches!(
                    preparation,
                    DeliveryPreparation::Work(meerkat_mob::MobRuntimeMode::AutonomousHost)
                ) {
                    injector.note_turn_skip(
                        identity,
                        crate::memory::coordinator::TurnInjectionSkip::RuntimeModeAutonomousHost,
                    );
                    return Ok((defanged, Vec::new()));
                }
```

- [meerkat-mobkit/src/memory/coordinator.rs:348-348](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/memory/coordinator.rs#L348-L348)

```text
            Self::RuntimeModeAutonomousHost => "runtime_mode_autonomous_host",
```

## A17-025: Console endpoint guides label configured module ids as the loaded runtime module set

**Candidate severity:** low. **Independent verdict:** confirm.

Operators should not treat this endpoint as proof that a module was started successfully or remains loaded/running. The REST reference already makes the more precise distinction.

### Baseline documentation evidence

- [docs/mobkit/guides/console.mdx:377-377](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/guides/console.mdx?plain=1#L377)

```text
| `/console/modules` | GET | Loaded modules list (JSON) |
```

- [docs/mobkit/guides/unified-runtime.mdx:174-174](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/guides/unified-runtime.mdx?plain=1#L174)

```text
| `/console/modules` | GET | Loaded modules JSON |
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime/console_ingress.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '274,298p'
```

  The modules response is constructed by mapping `decisions.modules` to module.id. The separate live_snapshot is used for console experience, not to determine the modules array returned by /console/modules.

  The endpoint projects configured decision-state declarations, not observed process/startup health or the live module roster.

### Independent challenge

Confirm the data-source label correction, without upgrading it into a new runtime-health defect. /console/modules derives ids from decision-state declarations and is exposed even by a router with no runtime or module runtime. A live snapshot does not determine that response's array. Calling it a loaded-runtime inventory is therefore misleading; it is a configured-module projection.

- [meerkat-mobkit/src/runtime/console_ingress.rs:277-298](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/console_ingress.rs#L277-L298)

```text
.map(|module| module.id.clone())
```

  The modules array comes from decisions.modules; live_snapshot is separately used for the experience contract.

- [meerkat-mobkit/src/http_console.rs:251-273](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/http_console.rs#L251-L273)

```text
module_runtime: None,
```

  The public decisions-only router supplies a concrete counterexample to interpreting the route as an observed loaded-module set.

**Required correction:** Change both canonical guide route descriptions to configured/declared module ids from RuntimeDecisionState, not proof of successful startup or current module health.

### Correction

Corrected all three route-table occurrences in both guides to configured module IDs from RuntimeDecisionState, explicitly not proof of module startup or live health.

- [docs/guides/console.mdx:46-46](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/guides/console.mdx#L46-L46)

```text
| `GET /console/modules` | Configured module IDs from `RuntimeDecisionState`, not startup or health confirmation. |
```

- [docs/guides/console.mdx:522-522](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/guides/console.mdx#L522-L522)

```text
| `/console/modules` | GET | Configured module IDs (JSON), not a loaded-runtime inventory |
```

- [docs/guides/unified-runtime.mdx:172-172](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/guides/unified-runtime.mdx#L172-L172)

```text
| `/console/modules` | GET | Configured module IDs from `RuntimeDecisionState` (JSON), not startup or health confirmation |
```

- [meerkat-mobkit/src/runtime/console_ingress.rs:277-281](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/console_ingress.rs#L277-L281)

```text
let modules: Vec<String> = decisions
        .modules
        .iter()
        .map(|module| module.id.clone())
        .collect();
```

### Final independent review

**PASS:** All three affected guide rows now call /console/modules a configured-module ID projection rather than a loaded-runtime inventory. The handler constructs the array exclusively from decisions.modules; the live snapshot is handled separately for /console/experience. The decisions-only public router also has runtime and module_runtime set to None, establishing why the endpoint cannot prove successful startup or current health. No module authority or endpoint behavior was changed.

- [docs/guides/console.mdx:46-46](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/guides/console.mdx#L46-L46)

```text
| `GET /console/modules` | Configured module IDs from `RuntimeDecisionState`, not startup or health confirmation. |
```

- [docs/guides/console.mdx:522-522](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/guides/console.mdx#L522-L522)

```text
| `/console/modules` | GET | Configured module IDs (JSON), not a loaded-runtime inventory |
```

- [docs/guides/unified-runtime.mdx:172-172](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/guides/unified-runtime.mdx#L172-L172)

```text
| `/console/modules` | GET | Configured module IDs from `RuntimeDecisionState` (JSON), not startup or health confirmation |
```

- [meerkat-mobkit/src/runtime/console_ingress.rs:277-281](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/console_ingress.rs#L277-L281)

```text
    let modules: Vec<String> = decisions
        .modules
        .iter()
        .map(|module| module.id.clone())
        .collect();
```

- [meerkat-mobkit/src/runtime/console_ingress.rs:291-298](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/console_ingress.rs#L291-L298)

```text
    let mut body = if base_path == CONSOLE_EXPERIENCE_ROUTE {
        build_console_experience_contract(&modules, &live_snapshot, &decisions.console)
    } else {
        serde_json::json!({
            "contract_version": MOBKIT_CONTRACT_VERSION,
            "modules": modules
        })
    };
```

- [meerkat-mobkit/src/http_console.rs:251-255](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/http_console.rs#L251-L255)

```text
pub fn console_json_router(decisions: RuntimeDecisionState) -> Router {
    console_json_router_with_state(ConsoleJsonState {
        decisions,
        runtime: None,
        module_runtime: None,
```

## A17-026: WorkGraph RPC reference states the bare mob id instead of the canonical mob realm

**Candidate severity:** medium. **Independent verdict:** confirm.

For mob id home, the service scope is mob.home, not home. Bare-id guidance misidentifies grant/store scope and conflicts with the implemented canonicalization.

### Baseline documentation evidence

- [docs/mobkit/api/rpc.mdx:931-934](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/api/rpc.mdx?plain=1#L931-L934)

```text
agent is issued by MobKit from the wired service (realm = the mob id,
namespace `default`)
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/workgraph_wiring.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '107,124p'
```

  scoped_workgraph_service passes mob_id through meerkat_core::mob_realm_id and constructs WorkGraphService::with_scope using the returned realm plus WorkNamespace::default(). The raw id is only an error fallback that logs a refusal warning.

  The normal authority is the canonical realm helper, not the bare input mob id.

- Read-only reproduction:

```text
gh api repos/lukacf/meerkat/git/tags/d0d1535fe69d3764f8bd1847ce42ebcb2d02f075 --jq '{tag: .tag, object: .object}'; gh api 'repos/lukacf/meerkat/contents/meerkat-core/src/connection.rs?ref=b9b7ffcb46d896dcf410f02ebfac9f7bd169b043' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '410,412p'
```

  Meerkat's public v0.8.38 annotated tag resolves to b9b7ffcb46d896dcf410f02ebfac9f7bd169b043. At that release, mob_realm_id returns `RealmId::parse(format!("mob.{mob_id}"))`. MobKit's pinned Cargo.toml requires =0.8.38; the dependency release's root Cargo.toml declares 0.8.38.

  The one downstream helper needed for this claim was inspected at the exact dependency release, not at the unrelated current worktree revision.

### Independent challenge

For a valid mob definition id, MobKit derives the WorkGraph realm through the pinned dependency's mob_realm_id helper, whose form is mob.<id>, then uses the default namespace. The raw-id branch is an error fallback intended to surface an upstream build refusal, not the normal grant scope. It does not rescue the bare-id documentation.

- [meerkat-mobkit/src/workgraph_wiring.rs:107-123](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/workgraph_wiring.rs#L107-L123)

```text
WorkGraphService::with_scope(store, realm, WorkNamespace::default())
```

  The service scope normally uses the successful canonical helper result, not the unmodified argument.

- [meerkat-core/src/connection.rs:410-412](https://github.com/lukacf/meerkat/blob/b9b7ffcb46d896dcf410f02ebfac9f7bd169b043/meerkat-core/src/connection.rs#L410-L412)

```text
RealmId::parse(format!("mob.{mob_id}"))
```

  This is the independently resolved Meerkat v0.8.38 dependency, not the audit checkout.

**Required correction:** State that the automatically issued grant is scoped to canonical realm mob.<mob definition id> and namespace default; retain the prohibition on caller-supplied realm_id.

### Correction

Corrected auto-issued WorkGraph grant scope to canonical mob.<mob definition id> and namespace default; retained caller realm_id rejection.

- [docs/api/rpc.mdx:982-983](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L982-L983)

```text
agent is issued by MobKit from the wired service (canonical realm
`mob.<mob definition id>`, namespace `default`)
```

- [meerkat-mobkit/src/workgraph_wiring.rs:111-111](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/workgraph_wiring.rs#L111-L111)

```text
let realm = match meerkat_core::mob_realm_id(mob_id) {
```

### Final independent review

**PASS:** The grant scope now states canonical mob.<mob definition id> and namespace default. The current composition calls the canonical mob_realm_id helper, constructs the scoped service with WorkNamespace::default(), and copies that service's grant into the factory rather than inventing a separate scope. Repository regression assertions pin the mob. prefix. The exact Meerkat 0.8.40 helper and WorkNamespace Default implementation were also inspected and match those values. The prohibition on caller-supplied realm_id remains, matching the shared RPC rejection before method dispatch.

- [docs/api/rpc.mdx:982-986](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/api/rpc.mdx#L982-L986)

```text
agent is issued by MobKit from the wired service (canonical realm
`mob.<mob definition id>`, namespace `default`) for every member whose profile sets
`tools.workgraph = true`; hosts do not issue it. `realm_id` is rejected
(`-32602`) on every method for the same reason: the service is realm-scoped at
construction and agent tool calls are scope-pinned to it.
```

- [meerkat-mobkit/src/workgraph_wiring.rs:111-112](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/workgraph_wiring.rs#L111-L112)

```text
    let realm = match meerkat_core::mob_realm_id(mob_id) {
        Ok(realm) => realm.as_str().to_string(),
```

- [meerkat-mobkit/src/workgraph_wiring.rs:123-123](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/workgraph_wiring.rs#L123-L123)

```text
    WorkGraphService::with_scope(store, realm, WorkNamespace::default())
```

- [meerkat-mobkit/src/workgraph_wiring.rs:160-163](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/workgraph_wiring.rs#L160-L163)

```text
    meerkat::surface::set_default_workgraph_namespace_grant(
        builder,
        Some(service.namespace_grant().clone()),
    );
```

- [meerkat-mobkit/src/workgraph_wiring.rs:643-644](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/workgraph_wiring.rs#L643-L644)

```text
        assert_eq!(service.default_realm_id(), "mob.wiring-realm");
        assert_eq!(service.store().kind(), WorkGraphStoreKind::Sqlite);
```

- [meerkat-mobkit/src/rpc/workgraph_methods.rs:574-578](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/rpc/workgraph_methods.rs#L574-L578)

```text
    if object.contains_key("realm_id") {
        return Err(invalid_params(
            "realm_id is not accepted; workgraph is scoped to this runtime's realm",
        ));
    }
```

## A17-027: Python quick start sends to agent-1 without declaring or creating that member

**Candidate severity:** medium. **Independent verdict:** confirm.

With a fresh valid profile-only mob definition, the standalone quick-start sequence addresses a member that has never been established. This claim is scoped to that shown fresh-launch setup, not custom hosts that separately populate membership.

### Baseline documentation evidence

- [docs/mobkit/sdks/python.mdx:36-45](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/sdks/python.mdx?plain=1#L36-L45)

```text
    async with await MobKit.builder().mob("config/mob.toml").gateway("/path/to/rpc_gateway").build() as rt:
        handle = rt.mob_handle()

        # Send a message (comms)
        await handle.send("agent-1", "Hello")
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/sdk/python/meerkat_mobkit/builder.py?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '40,54p;898,922p'; gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/bin/rpc_gateway.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '808,824p;11079,11089p;11101,11104p'
```

  SDK defaults have modules=[], persistent_state=None, and roster_provider=None. Convention defaults add only gating/access/routing file paths. The gateway builds pre_spawn from supplied module specs and separately reads has_roster_provider; the shown builder config supplies neither a roster nor any module pre-spawn.

  A fresh definition containing profiles does not declare a concrete agent-1 through this launch configuration.

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/sdk/python/meerkat_mobkit/runtime.py?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '1899,1904p'; gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/mob_handle_runtime.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '9412,9419p'
```

  Python send calls mobkit/send_message without an ensure/spawn step. The MobKit delivery helper resolves `handle.member(&mid).await?` before sending; it does not create a missing member.

  send is not an implicit member-construction API that could make the omitted prerequisite harmless.

### Independent challenge

Narrow to the advertised fresh quick-start path. The shown Python builder config supplies no roster provider or members, and reading mob.toml supplies a definition rather than a member declaration. The raw unified runtime has no discovery source by default; the explicit discovery path only spawns when a provider is installed. send does not ensure a missing member. Existing custom/persisted hosts may already have agent-1, but the example neither declares that precondition nor creates it.

- [sdk/python/meerkat_mobkit/builder.py:40-54](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/sdk/python/meerkat_mobkit/builder.py#L40-L54)

```text
roster_provider: Any | None = None
```

  The shown builder begins with no modules, persistent-state override or roster provider.

- [meerkat-mobkit/src/unified_runtime/mod.rs:474-488](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/unified_runtime/mod.rs#L474-L488)

```text
discovery: None,
```

  The raw runtime does not silently install a profile-to-members discovery policy.

- [meerkat-mobkit/src/unified_runtime/builder.rs:1492-1498](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/unified_runtime/builder.rs#L1492-L1498)

```text
&& let Some(ref discovery) = runtime.discovery
```

  The alternative classic discovery path is conditional on an actual discovery provider.

- [sdk/python/meerkat_mobkit/runtime.py:1766-1791](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/sdk/python/meerkat_mobkit/runtime.py#L1766-L1791)

```text
params: dict[str, Any] = {"role": role, "agent_identity": member_id}
```

  ensure_member is the explicit supported creation seam and takes a profile name as role.

- [meerkat-mobkit/src/mob_handle_runtime.rs:9412-9419](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/mob_handle_runtime.rs#L9412-L9419)

```text
.member(&mid)
```

  The send helper resolves an existing member and propagates failure rather than creating one.

**Required correction:** Make the canonical Python quick start self-contained about membership: include or explicitly identify a valid profile and call ensure_member("agent-1", that_profile) before send, or install a roster that declares agent-1. Do not imply every custom host lacks that member or that profiles auto-spawn.

### Correction

Made quick-start membership explicit with a concrete worker profile and ensure_member("agent-1", "worker") before sending, without implying profiles auto-spawn or custom hosts cannot already contain that member.

- [docs/sdks/python.mdx:37-42](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/python.mdx#L37-L42)

```text
[profiles.worker]
model = "gpt-5.5"
runtime_mode = "turn_driven"

[profiles.worker.tools]
comms = true
```

- [docs/sdks/python.mdx:55-55](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/python.mdx#L55-L55)

```text
        await handle.ensure_member("agent-1", "worker")
```

- [sdk/python/meerkat_mobkit/runtime.py:1768-1770](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/python/meerkat_mobkit/runtime.py#L1768-L1770)

```text
async def ensure_member(
        self, member_id: str, role: str, **kwargs: Any
    ) -> MemberSnapshot:
```

### Final independent review

**PASS:** The fresh-launch example now declares a concrete worker profile and calls ensure_member("agent-1", "worker") before send. The profile includes required comms support, explicitly selects turn_driven, and uses the model accepted by the current gateway's inspected catalog regression. The SDK signature takes member_id then role, and its RPC payload maps these to agent_identity and role as used by the gateway. Send still resolves an existing member rather than creating one. Gateway-binary installation and provider credentials are stated prerequisites; the TOML and Python snippets parse successfully in read-only checks.

- [docs/sdks/python.mdx:34-42](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/python.mdx#L34-L42)

```text
[mob]
id = "quickstart"

[profiles.worker]
model = "gpt-5.5"
runtime_mode = "turn_driven"

[profiles.worker.tools]
comms = true
```

- [docs/sdks/python.mdx:54-58](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/sdks/python.mdx#L54-L58)

```text
        handle = rt.mob_handle()
        await handle.ensure_member("agent-1", "worker")

        # Send a message (comms)
        await handle.send("agent-1", "Hello")
```

- [sdk/python/meerkat_mobkit/runtime.py:1768-1770](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/python/meerkat_mobkit/runtime.py#L1768-L1770)

```text
    async def ensure_member(
        self, member_id: str, role: str, **kwargs: Any
    ) -> MemberSnapshot:
```

- [sdk/python/meerkat_mobkit/runtime.py:1783-1783](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/python/meerkat_mobkit/runtime.py#L1783-L1783)

```text
        params: dict[str, Any] = {"role": role, "agent_identity": member_id}
```

- [sdk/python/meerkat_mobkit/runtime.py:1792-1793](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/sdk/python/meerkat_mobkit/runtime.py#L1792-L1793)

```text
        raw = await self._runtime._rpc("mobkit/ensure_member", params)
        return MemberSnapshot.from_dict(raw)
```

- [meerkat-mobkit/src/mob_handle_runtime.rs:9596-9600](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/mob_handle_runtime.rs#L9596-L9600)

```text
    let _receipt = handle
        .member(&mid)
        .await?
        .send(content, handling_mode)
        .await?;
```

## A17-028: JSON session lock recovery omits the age gate before dead-PID eviction

**Candidate severity:** low. **Independent verdict:** confirm.

The crash-recovery recipe promises behavior earlier than the adapter permits, which can mislead restart/retry diagnosis.

### Baseline documentation evidence

- [docs/mobkit/concepts/sessions.mdx:34-34](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/docs/mobkit/concepts/sessions.mdx?plain=1#L34)

```text
On the next access attempt, the store checks whether the PID in the lock record is still alive. Dead PIDs trigger automatic lock recovery
```

### Implementation proof

- Read-only reproduction:

```text
gh api 'repos/lukacf/meerkat-mobkit/contents/meerkat-mobkit/src/runtime/session_store.rs?ref=426ebdfc4101e69abb4c6749f14d15dc14bbd524' -H 'Accept: application/vnd.github.raw+json' | nl -ba | sed -n '153,171p;276,312p'
```

  The default stale_lock_threshold is 30 seconds. For a valid record, is_lock_stale returns false without a PID probe while age_ms is below the threshold; only afterward does it test process liveness. If liveness probing cannot run, it conservatively treats the process as alive. Malformed records use file age.

  Dead owner alone does not cause recovery on the immediate next attempt.

### Independent challenge

A valid lock record younger than the configurable threshold is not probed for process liveness and remains held even when its owner has died. The default threshold is 30 seconds. Only an old enough valid record reaches the PID check; malformed records use file age, and inability to execute the probe conservatively preserves the lock. The document's immediate-next-access account omits this operationally significant gate.

- [meerkat-mobkit/src/runtime/session_store.rs:153-171](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L153-L171)

```text
stale_lock_threshold: Duration::from_secs(30),
```

  The default and public threshold override are explicit.

- [meerkat-mobkit/src/runtime/session_store.rs:276-312](https://github.com/lukacf/meerkat-mobkit/blob/426ebdfc4101e69abb4c6749f14d15dc14bbd524/meerkat-mobkit/src/runtime/session_store.rs#L276-L312)

```text
if age_ms < stale_threshold_ms {
```

  Young valid records return false before PID probing; old valid records require a dead process, malformed records use modification age, and probe-execution failure returns alive.

**Required correction:** Describe recovery on a lock-acquisition attempt after the age threshold, default 30 seconds, followed by the valid-record PID test. Mention the configurable threshold and conservative unavailable-probe behavior; do not promise immediate recovery on the next access.

### Correction

Documented lock-acquisition recovery only after the configurable age threshold (30-second default), followed by the valid-record PID check. Included malformed-record file-age fallback and conservative retention when the liveness probe cannot execute.

- [docs/concepts/sessions.mdx:39-40](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L39-L40)

```text
the stale-age threshold (30 seconds by default, configurable with
`with_stale_lock_threshold(Duration)`). Only then is its PID probed:
```

- [docs/concepts/sessions.mdx:42-43](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L42-L43)

```text
If the liveness probe cannot be executed, the lock is conservatively
retained.
```

- [meerkat-mobkit/src/runtime/session_store.rs:282-285](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L282-L285)

```text
if age_ms < stale_threshold_ms {
                return Ok(false);
            }
            return Ok(!is_process_alive(record.owner_pid));
```

### Final independent review

**PASS:** The new operational guidance follows the real ordering rather than treating a dead owner as immediately recoverable. The default is 30 seconds and the setter changes it; valid records return non-stale before any PID probe when younger than the threshold. Only old-enough valid records reach the liveness check, malformed records use file modification age, and failure to execute the probe conservatively reports alive. Recovery/removal/retry occurs inside acquisition, whereas read_rows does not acquire the lock. The revised text no longer promises recovery on the next read or immediately after a crash.

- [docs/concepts/sessions.mdx:37-44](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/docs/concepts/sessions.mdx#L37-L44)

```text
**Stale lock recovery.** Recovery is attempted during lock acquisition, not on
every read or immediately after a crash. A valid lock record must first reach
the stale-age threshold (30 seconds by default, configurable with
`with_stale_lock_threshold(Duration)`). Only then is its PID probed: a dead
owner allows the lock to be removed and acquisition retried, while a live owner
retains it. If the liveness probe cannot be executed, the lock is conservatively
retained. Malformed lock records use the lock file's modification age instead
of a PID test. A lock that cannot yet be recovered returns `LockHeld`.
```

- [meerkat-mobkit/src/runtime/session_store.rs:157-172](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L157-L172)

```text
        Self {
            data_path,
            lock_path,
            stale_lock_threshold: Duration::from_secs(30),
        }
    }

    pub fn with_lock_path(mut self, lock_path: impl AsRef<Path>) -> Self {
        self.lock_path = lock_path.as_ref().to_path_buf();
        self
    }

    pub fn with_stale_lock_threshold(mut self, threshold: Duration) -> Self {
        self.stale_lock_threshold = threshold;
        self
    }
```

- [meerkat-mobkit/src/runtime/session_store.rs:259-269](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L259-L269)

```text
                    if self.is_lock_stale()? {
                        fs::remove_file(&self.lock_path).map_err(|remove_err| {
                            JsonFileSessionStoreError::StaleLockRecoveryFailed(
                                remove_err.to_string(),
                            )
                        })?;
                        continue;
                    }
                    return Err(JsonFileSessionStoreError::LockHeld {
                        lock_path: self.lock_path.display().to_string(),
                    });
```

- [meerkat-mobkit/src/runtime/session_store.rs:276-295](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L276-L295)

```text
    fn is_lock_stale(&self) -> Result<bool, JsonFileSessionStoreError> {
        let bytes = fs::read(&self.lock_path)
            .map_err(|err| JsonFileSessionStoreError::Io(err.to_string()))?;
        let stale_threshold_ms = self.stale_lock_threshold.as_millis() as u64;
        if let Ok(record) = serde_json::from_slice::<JsonStoreLockRecord>(&bytes) {
            let age_ms = current_time_ms().saturating_sub(record.created_at_ms);
            if age_ms < stale_threshold_ms {
                return Ok(false);
            }
            return Ok(!is_process_alive(record.owner_pid));
        }

        let modified = fs::metadata(&self.lock_path)
            .and_then(|meta| meta.modified())
            .map_err(|err| JsonFileSessionStoreError::Io(err.to_string()))?;
        let age = SystemTime::now()
            .duration_since(modified)
            .unwrap_or_default();
        Ok(age >= self.stale_lock_threshold)
    }
```

- [meerkat-mobkit/src/runtime/session_store.rs:298-313](https://github.com/lukacf/meerkat-mobkit/blob/01435e7ccda5e925fc2cf9f462327bdae9584d07/meerkat-mobkit/src/runtime/session_store.rs#L298-L313)

```text
fn is_process_alive(pid: u32) -> bool {
    if pid == 0 {
        return false;
    }
    let status = Command::new("kill")
        .arg("-0")
        .arg(pid.to_string())
        .stdout(Stdio::null())
        .stderr(Stdio::null())
        .status();
    match status {
        Ok(exit_status) => exit_status.success(),
        // If liveness probing is unavailable, avoid evicting potentially active locks.
        Err(_) => true,
    }
}
```

## Lane limitations

- **Audit:** Ownership/provenance dependency: every finding concerns faithfully imported upstream content. Canonical owners are the corresponding docs/*.mdx pages in lukacf/meerkat-mobkit. Correct those upstream, publish an appropriate immutable release, and use scripts/sync-mobkit-docs.py / .github/workflows/publish-mobkit-docs.yml. Do not directly edit docs/mobkit pages or fabricate/restamp _source.json to incorporate unproven changes.
- **Audit:** Provenance was established from docs/mobkit/_source.json, the sync and validation implementations, and publish-mobkit-docs.yml before using implementation evidence. The public v0.8.35 tag object 440ce577f50c4a850e03696dd2cb5de6569f9f9b resolves to 426ebdfc4101e69abb4c6749f14d15dc14bbd524. Release metadata reported a published, non-draft, non-prerelease release on 2026-09-15T04:37:38Z. The manifest says source_docs_dirty=false and content_sha256=21ff341959057c1960e582d7d89d8fb6f50e9cbfa2e499cba162751b230d63f4.
- **Audit:** Exact public MobKit source access succeeded: the recursive tree was complete, the pinned archive was read in memory without extraction, and all 27 canonical pages were compared through the repository's transformations. There was no unresolved public-source access blocker. Source-only/private host modules, deployment secrets, provider behavior, and unpublished integrations were not available or assumed.
- **Audit:** Cross-repository proof was deliberately bounded. MobKit exact-pins Meerkat =0.8.38, not the current audit worktree version. For A17-026 only, the public v0.8.38 dependency tag was resolved to b9b7ffcb46d896dcf410f02ebfac9f7bd169b043 and the canonical realm helper was read there. Registry packages, registry checksums, and compiled release binaries were not downloaded/installed or compared with Git source.
- **Audit:** This is a static documentation audit. No nested agents, builds, installs, provider calls, application processes, cloud operations, commits, or imported-source modifications were performed. Source-level type/shape mismatches were not reproduced with a compiler or live server. The read-only local validator ran successfully: `python3 -B scripts/validate-mintlify-docs.py` => `docs-check: ok (116 public pages)`.
- **Audit:** Every assigned page was read completely, but not every runtime branch, SDK example, external URL, frontend rendering condition, or platform-specific deployment was executed. Python top-level imports in parseable fenced snippets were checked against pinned exports; that is not a claim of full example executability.
- **Audit:** The pinned release being older than a later possible upstream release is not itself a defect. No recommendation changes snapshot identity without its normal publication flow. No finding uses another documentation page as the sole proof.
- **Audit:** Rejected tentative alarms are not findings: remote binds can be allowed by enforced console auth through HttpBindPolicy::for_gateway; the SDK gateway's default persistent mob store differs from the plain Rust builder; autonomous-host ambient-memory injection is still explicitly skipped; the inspected Python spawn_member and memory_query methods match their documented forms. Apparent masked Bearer strings were tool-output redaction, not bad source examples.
- **Audit:** All 27 assigned file hashes still match manifest-A17.json and git status was clean immediately before creating this audit artifact. Only audit-A17.json was written.
- **Challenge:** Static read-only challenge only: no nested agents, builds, installations, provider calls, live gateway execution, commits or repository modifications. Test bodies were inspected as supporting evidence, not executed or reported passing.
- **Challenge:** Public source retrieval used immutable GitHub tags/commits and HTTPS archives in memory. No repository content was uploaded. SDK/registry package contents and compiled release binaries were not independently compared to Git source.
- **Challenge:** All 27 imported pages were independently byte-compared through the existing transformations and their candidate contexts were reread. This adjudicates the 28 submitted candidates; it does not add new findings or claim exhaustive execution of every documentation example.
- **Challenge:** A17-005 is confirmed as a material missing policy detail; A17-020/A17-021 are scoped to typed dispatch attribution and the wired full-stack composition; A17-024 preserves the still-implemented autonomous-host restriction; A17-027 is scoped to the shown fresh-launch example. These narrowed scopes must survive implementation and final review.
- **Challenge:** A17-013 stays rejected: no repair is requested for its speculative running-session interpretation. Other independently confirmed changes still legitimately affect the same canonical Sessions page.
- **Challenge:** The canonical-owner dependency is unresolved by this read-only task. The generator's commit-origin fallback is real, but it is not an accepted publication mode under current docs-check and workflow contracts. A new verified upstream release is required without a separately authorized publication-contract change.
- **Challenge:** Only challenge-A17.json was intentionally written. No canonical MobKit docs, imported pages, source manifest, sync script or workflow was edited.
