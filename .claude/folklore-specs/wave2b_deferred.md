# Wave 2b deferred / completion work (read verbatim)


## LANE: N2 (LlmClient::provider() -> Provider) + S6 (dead gemini=google image-executor alias)  (wire_impact=False)

### sites_fixed in-lane (done):

- N2 root carrier: meerkat-llm-core/src/types.rs:60 — LlmClient::provider() retyped from -> &'static str to -> meerkat_core::Provider; default project_replay_messages body now uses self.provider().as_str() for the display label; added Provider to the meerkat_core import
- N2 adapter: meerkat-llm-core/src/adapter.rs:119 apply_generic_provider_overrides rewritten to `match self.client.provider() { Provider::Anthropic if .. => .., Provider::Gemini if .. => .., Provider::Anthropic|Gemini|OpenAI|SelfHosted|Other => tag }` — the old "gemini"|"google" string alias is folded into Provider::Gemini, and the prior silent `_ => tag` fall-through is replaced by explicit no-op arms (no fail-open)
- N2 adapter error/log sites: meerkat-llm-core/src/adapter.rs:195/342/351 AgentError::llm(self.client.provider().as_str(), ..) — display label preserved (AgentError::llm still takes &'static str, out of lane)
- N2 adapter delegating impl: meerkat-llm-core/src/adapter.rs:382 AgentLlmClient::provider() (out-of-lane trait stays &'static str) now delegates via self.client.provider().as_str()
- N2 adapter in-lane test doubles: meerkat-llm-core/src/adapter.rs ProjectionClient/ScriptedClient provider() -> meerkat_core::Provider { Provider::Other } (were "projection-test"/"scripted-test")
- N2 anthropic: meerkat-anthropic/src/client.rs:1438 -> Provider { Provider::Anthropic }; test asserts at :2306/:2313 now assert_eq!(client.provider(), Provider::Anthropic)
- N2 gemini: meerkat-gemini/src/client.rs:1661 -> Provider { Provider::Gemini }
- N2 openai (client.rs only): meerkat-openai/src/client.rs:2163 -> meerkat_core::Provider { Provider::OpenAI }
- N2 factory: meerkat/src/factory.rs:2634 (LlmClient override) — dropped Provider::from_name(client.provider()) round-trip, now returns client.provider() directly; factory.rs:2637 (AgentLlmClient override) keeps Provider::from_name since that trait still returns &'static str
- N2 factory in-lane test doubles: meerkat/src/factory.rs NeverLlmClient (~5079) + PromptTestClient (~8865) provider() -> meerkat_core::Provider { Provider::Other } (were "mock")
- S6: meerkat/src/factory.rs:1047 deleted the dead `(provider == "gemini").then(|| self.executors.get("google")).flatten()` fallback (map is only ever keyed by provider_key(Provider)=as_str(), never "google"); now plain self.executors.get(provider)

### new_types introduced:


### deferred_cross_crate (TO DO):

- DESIGN DECISION (read first): I retyped LlmClient::provider() -> Provider (the N2 root carrier the spec names). I deliberately did NOT change the SEPARATE meerkat_core::AgentLlmClient::provider() trait (declared meerkat-core/src/agent.rs:75), which still returns &'static str — that trait is out of my lane and has ~40 implementors workspace-wide. The adapter's AgentLlmClient::provider() impl bridges by calling self.client.provider().as_str(). Net cross-crate ripple is ONLY: every `impl LlmClient` whose provider() returned &'static str. Lead: if you ALSO want AgentLlmClient::provider() -> Provider, that is a second, much larger pass (change agent.rs:75 + all AgentLlmClient impls + all agent.provider() callers in runtime/rpc/mob); not required for N2 and not done here.
- PRODUCTION, MUST fix to compile — meerkat-openai/src/client_compatible.rs:632 (OpenAiCompatibleClient): change `fn provider(&self) -> &'static str { "self_hosted" }` to `fn provider(&self) -> meerkat_core::Provider { meerkat_core::Provider::SelfHosted }`. (Out of my lane: only meerkat-openai/src/client.rs is mine.)
- PRODUCTION, MUST fix to compile — meerkat-openai/src/text_adapter.rs:414 (OpenAiRealtimeTextAdapter): change `fn provider(&self) -> &'static str { "openai" }` to `fn provider(&self) -> meerkat_core::Provider { meerkat_core::Provider::OpenAI }` (keep the comment about sharing the openai identity). (Out of lane.)
- PRODUCTION (always-compiled `pub mod test_client`), MUST fix to compile — meerkat-llm-core/src/test_client.rs:49 (TestClient): change `fn provider(&self) -> &'static str { "test" }` to `fn provider(&self) -> meerkat_core::Provider { meerkat_core::Provider::Other }`. (Same crate as my lane but NOT in my allowed file list — types.rs/adapter.rs only.)
- meerkat-cli/src/main.rs:14875 (CapturingLlmClient test double): `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (was "mock").
- meerkat-mcp-server/src/lib.rs:4233 (MockLlmClient test double): `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (was "mock").
- meerkat-mob/src/build.rs:475 (CaptureClient, delegating wrapper over inner LlmClient): change return type to `-> meerkat_core::Provider` and keep the body `self.inner.provider()` (no .as_str()).
- meerkat-rest/src/lib.rs:7038,7081,7108 (MockLlmClient/BlockingMockLlmClient/ErrorLlmClient): each `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (were "mock").
- meerkat-rest/tests/runtime_backed_ingress.rs:70 (FirstTurnOnlyClient): `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (was "first-turn-only").
- meerkat-rpc/src/server.rs:863 (MockLlmClient): `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (was "mock").
- meerkat-rpc/src/router.rs:3538,3615 (MockLlmClient/RecordingMockLlmClient): each `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (were "mock").
- meerkat-rpc/src/session_runtime.rs:8659,8714,8817,9040,9087,9143 (MockLlmClient family + Slow/Blocking/BlockAfterFirst/FailAfterFirst): each `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (were "mock"). ALSO session_runtime.rs:8746 (RecordingMessagesLlmClient, delegating wrapper): change return type to `-> meerkat_core::Provider` and keep body `self.inner.provider()`.
- meerkat-rpc/tests/runtime_backed_ingress.rs:51, meerkat-rpc/tests/integration_server.rs:60,114, meerkat-rpc/tests/regression_rpc.rs:58 (MockLlmClient/RecordingToolClient): each `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (were "mock").
- meerkat-client/tests/rct_contracts.rs:36 (MockClient): `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (was "mock").
- meerkat/src/service_factory.rs:888,1092 (CaptureToolClient/CountingAgentLlmClient delegating wrappers — verify inner is dyn LlmClient): change return type to `-> meerkat_core::Provider`, keep body self.inner.provider(); service_factory.rs:1059 (MockLlmClient): `-> meerkat_core::Provider { meerkat_core::Provider::Other }`. (Out of lane: only meerkat/src/factory.rs is mine.)
- meerkat/src/surface/runtime_backed.rs:1182,1236,1291 (BlockingClient/OneToolThenOkClient/TerminalLlmFailureClient): each `-> meerkat_core::Provider { meerkat_core::Provider::Other }` (were "blocking-test"/"one-tool-then-ok-test"/"terminal-failure-test").
- meerkat/src/surface/embedded.rs:104 (CaptureToolClient delegating wrapper): change return type to `-> meerkat_core::Provider`, keep body self.inner.provider().
- meerkat/tests/factory_build_agent.rs:71,109,2869 (MockLlmClient/CaptureClient/ParamsCaptureClient): literal mocks → Provider::Other; :2869 returns "anthropic" → Provider::Anthropic. ALSO :139,:242 are delegating wrappers (self.inner.provider()) — change return type, keep delegating body. NOTE this file also has impl AgentLlmClient (MockAgentLlmClient:85, CountingAgentLlmClient:124): if those call an inner LlmClient.provider() in a &'static str context they need .as_str(); the AgentLlmClient trait return type itself is unchanged.
- meerkat/tests/sdk_agentfactory.rs:83 (MockLlmClient -> Provider::Other), :152 ("anthropic" -> Provider::Anthropic), :183 ("custom-provider" -> Provider::Other), :295 ("anthropic" -> Provider::Anthropic). (:121/:164 are AgentLlmClient impls — unchanged trait; only retype the LlmClient ones.)
- meerkat/tests/sdk_structured_output.rs:77,139,210 (Mock* LlmClient): each -> Provider::Other (were "mock").
- meerkat/tests/live_meerkat_regression.rs:270 (ScriptedImageToolCallClient): -> Provider::Other (was "scripted-image-tool-call").
- meerkat/tests/persistence_contract.rs:122 (CatalogLoadClient): -> Provider::Other (was "mock").
- meerkat/tests/agent_factory_auth_binding.rs:160 (MockLlmClient): -> Provider::Other (was "mock").
- LOCAL AgentLlmClient test adapters that wrap a dyn LlmClient and use self.client.provider() in &'static str contexts (these need `.as_str()` added, NOT a return-type change, because they impl the unchanged AgentLlmClient trait): meerkat/tests/live_meerkat_comms.rs:106,118 (AgentError::llm) + :161 (fn provider body); meerkat/tests/smoke_meerkat_sdk.rs:112,124 + :166; meerkat/tests/live_meerkat_user_flows.rs:104,116 + :158; meerkat/examples/comms_verbose.rs:143,152 (+ its fn provider body around :161). Add `.as_str()` to each self.client.provider() call.
- examples/035-mdm-tux-rs/src/test_support.rs:53 (CaptureClient delegating wrapper): change return type to `-> meerkat_core::Provider`, keep body self.inner.provider(). examples/035-mdm-tux-rs/src/bin/target.rs:2243 (ScriptedClient): -> meerkat_core::Provider { meerkat_core::Provider::Other } (was "test"). (Verify these example crates are in the workspace build/CI.)
- Re Provider::from_name: it maps unknown strings (incl. the old "custom-provider", "mock", "first-turn-only", etc.) to Provider::Other, so any test that previously asserted a specific provider STRING from an AgentLlmClient override path now sees Provider::Other — semantics preserved for the factory inference path (factory.rs:2637 still parses the AgentLlmClient label). No behavior change expected for real providers.

### notes:
N2 + S6 fully done in-lane; behavior preserved. KEY SCOPING NOTE for the lead: there are TWO distinct provider() methods. (1) LlmClient::provider() in meerkat-llm-core/src/types.rs — I retyped this to -> meerkat_core::Provider (the N2 root carrier). (2) AgentLlmClient::provider() in meerkat-core/src/agent.rs:75 — I left this as -> &'static str (out of my lane; ~40 implementors). The adapter bridges via .as_str(). AgentError::llm(&'static str) is a legitimate display sink and was kept (lane instruction), now fed .as_str(). The adapter match folds the old \"gemini\"|\"google\" alias into Provider::Gemini and replaces the silent `_ => tag` fall-through with explicit no-op arms (Anthropic|Gemini|OpenAI|SelfHosted|Other => tag) — fail-open eliminated. S6: the gemini=google image-executor alias was provably dead (map keyed only by provider_key(Provider).as_str(), never \"google\") and is deleted. wire_impact=false: no meerkat-contracts wire/schema type changed (Provider already serializes snake_case; this is an internal trait return type). No new types introduced. Build the lane crates + apply the deferred_cross_crate edits (mostly mechanical `-> &'static str { \"x\" }` => `-> meerkat_core::Provider { Provider::Variant }`; delegating wrappers just change the signature; the local AgentLlmClient test adapters need `.as_str()`). NO cargo/build run per write-only rule.


## LANE: N6 (mob-wire action discriminator) + N7-status (mob-result DTO status/members) + N8 (revision/initial_turn/current_session sentinels)  (wire_impact=True)

### sites_fixed in-lane (done):

- N6: WireActionArgs.action String->WireMobWireAction; action=="wire" guard ->matches!(.., Wire); match action.as_str() dispatch -> exhaustive typed match (catch-all deleted) at meerkat-mob-mcp/src/lib.rs:3169/3264/3546
- N7-status: MobStatusResult.status String->WireMobLifecycleStatus (meerkat-contracts/src/wire/mob.rs:596)
- N7-status: MobRespawnResult.status String->WireMobRespawnOutcome (mob.rs:867); web SDK respawn parse + producer literals now typed
- N7-status: MobAppendSystemContextResult.status String->WireAppendSystemContextStatus (mob.rs:1602)
- N7-status: MobSnapshotResult.status String->WireMobLifecycleStatus + members Vec<Value>->Vec<MobMemberListEntryWire> (mob.rs:1871/1872)
- N7-status producers: mob-mcp mob_list/mob_status emit wire_mob_lifecycle_status(MobState); mob_respawn emits WireMobRespawnOutcome::{Completed,TopologyRestoreFailed} instead of bare string literals
- N8(a): meerkat-session/src/persistent.rs:5451 revision=="current" -> RevisionSelector::parse(query.revision) matched on enum (Current/Specific)
- N8(b): meerkat-contracts/src/request_lifecycle.rs:40 value["initial_turn"].as_str()=="deferred" raw Value match -> typed WireInitialTurn deserialize via SessionCreateInitialTurnView (same fail-open default)
- N8(c): meerkat-schedule/src/tools.rs:434 target["target_kind"]/target["type"] raw-Value string poke -> typed HostTargetProbe/HostSessionTargetProbe deserialize classifying the current_session pseudo-variant

### new_types introduced:

- meerkat-contracts WireMobWireAction { Wire, Unwire } (#[serde(rename_all=snake_case)]) in wire/mob.rs
- meerkat-contracts WireMobLifecycleStatus { Creating, Running, Stopped, Completed, Destroyed } (PascalCase default serialization to match MobState::as_str() wire output) in wire/mob.rs
- meerkat-contracts WireMobRespawnOutcome { Completed, TopologyRestoreFailed } (snake_case) in wire/mob.rs
- meerkat-contracts WireAppendSystemContextStatus { Applied, Staged, Duplicate } (snake_case) + From<meerkat_core::AppendSystemContextStatus> in wire/mob.rs
- meerkat-contracts RevisionId(pub String) newtype + RevisionSelector { Current, Specific(RevisionId) } with RevisionSelector::parse(impl Into<String>) in wire/session.rs
- meerkat-contracts WireInitialTurn { RunImmediately, Deferred } (snake_case, Deserialize) + private SessionCreateInitialTurnView in request_lifecycle.rs
- meerkat-mob-mcp module-private fn wire_mob_lifecycle_status(MobState)->WireMobLifecycleStatus
- meerkat-schedule tools.rs module-private HostTargetProbe + HostSessionTargetProbe deserialize-probe enums (current_session classifier)
- web SDK WIRE_MOB_RESPAWN_OUTCOMES readonly array (typed-enum validation list)

### deferred_cross_crate (TO DO):

- N8(a) DEEPER FIX (optional, dogma-purest): retype meerkat-core/src/service/mod.rs:1390 SessionTranscriptRevisionQuery.revision from String to meerkat_contracts::RevisionSelector, then have meerkat-rpc/src/router.rs:2110 build it via RevisionSelector::parse(params.revision). I fixed the actual string-oracle at the persistent.rs consumer (parse-at-boundary), so the violation is closed; the wire stays String (no SDK break). Router needs NO change for current behavior.
- N8(b) FOLD (optional): meerkat-rpc/src/handlers/session.rs:45 enum InitialTurn { RunImmediately, Deferred } is a divergent second copy of the same wire contract now owned by meerkat_contracts::WireInitialTurn. Lead may replace the rpc-local enum's usage (CreateSessionParams.initial_turn deserialize at handlers/session.rs) with meerkat_contracts::WireInitialTurn to collapse to one owner. Not required for compile; my change only touched the contracts classifier.
- N8(c) DEEPER FIX (out-of-lane, recommended): add SessionTargetBinding::CurrentSession { action: ScheduledSessionAction } arm to meerkat-schedule/src/types.rs:1128 (#[serde(tag=type)] enum) so the host pseudo-variant is first-class in the persisted domain, and have tools.rs deserialize TargetBinding directly instead of pre-rewriting raw Value. I did an in-lane typed-probe parse that already removes the raw target["target_kind"]/target["type"] string oracle; the schema-injection (add_current_session_to_schema) and the resumable_session rewrite remain as the host shim. Lead must edit types.rs for the full domain promotion.
- N7 follow-up (not in my assigned scope): MobWaitMembersResult.members (wire/mob.rs:2006) and MobListMembersMatchingResult.members (wire/mob.rs:2226) are still Vec<Value> collapsing the typed MobMemberListEntryWire roster — same shape as the MobSnapshotResult.members I fixed, but not named in the N7-status spec. Candidate for a follow-up retype.

### notes:
LEAD MUST RUN `make regen-schemas` + `make verify-version-parity`. Wire shapes changed for 4 mob-result DTOs (MobStatusResult/MobRespawnResult/MobAppendSystemContextResult/MobSnapshotResult): status fields go from open string to closed enum, and MobSnapshotResult.members from Vec<Value> to typed roster entries. CRITICAL serialization detail: WireMobLifecycleStatus uses DEFAULT (PascalCase) serde — NOT snake_case as the synthesis sketch suggested — because the live producers emit MobState::as_str() ("Running","Creating",...) and tests assert status==\"Running\"; a snake_case enum would have broken the wire contract. WireMobRespawnOutcome + WireAppendSystemContextStatus are snake_case (matching existing "completed"/"topology_restore_failed" and AppendSystemContextStatus snake_case output). The four DTOs are catalog/emit/SDK-codegen mirrors only (no Rust producer constructs them — grep-confirmed), so retyping fields does NOT break out-of-lane compilation; only the emitted schema + generated SDK types change. The mob-mcp producers emit ad-hoc json! and now build the typed enums (compiler-enforced agreement with the wire shape). N8(b) fail-open behavior preserved exactly (verified against rpc_catalog.rs:1007-1019 in-lane tests: deferred->InlineObservation, run_immediately->LongRunningPublishOnSuccess). serde caveat to verify at build: HostTargetProbe/HostSessionTargetProbe use #[serde(other)] unit variants inside #[serde(tag=...)] internally-tagged enums (supported, but the lead should confirm clean compile). No .unwrap/.expect/panic introduced. RevisionId/RevisionSelector/WireInitialTurn deliberately have NO JsonSchema/Serialize derives (not embedded in any schema-emitted struct; wire stays String) so they add nothing to regen.


## LANE: M10 (PeerResponseTerminalFact string-flatten/re-parse) + N5-live (typed Provider laundered through String across the live projection boundary)  (wire_impact=True)

### sites_fixed in-lane (done):

- M10 consumer: meerkat-openai/src/live.rs openai_realtime_terminal_peer_response_summary (was live.rs:368-399) no longer re-derives the typed fact via starts_with("peer_response_terminal:") + split_once("Payload:") + serde_json re-parse; it now reads the typed PendingSystemContextAppend.peer_response_terminal fact directly (context_key() + render_payload_value()). The string parse survives ONLY as an explicitly-marked TRANSITIONAL fallback (openai_realtime_terminal_peer_response_payload) pending the cross-crate producer wiring, to keep the live path correct until the lead lands the carrier.
- M10 carrier: meerkat-core/src/session.rs PendingSystemContextAppend gained typed `peer_response_terminal: Option<PeerResponseTerminalFact>` (serde default+skip_if_none), mirroring the source_kind/RuntimeSteer precedent that retired the `runtime:steer:` string prefix.
- M10 typed-truth: meerkat-core/src/handles.rs PeerResponseTerminalFact + PeerResponseTerminalSource + the 4 identity newtypes + PeerResponseTerminalRenderPayload now derive Serialize/Deserialize so the typed fact can persist on PendingSystemContextAppend and survive the durable round-trip to the realtime consumer; added render_payload_value() accessor.
- M10 producer (partial): meerkat-runtime/src/input.rs context_append_to_pending_system_context_append + the runtime-steer producer now construct the new field (=None today; the peer-terminal stamp seam is marked for the deferred carrier wiring).

### new_types introduced:

- meerkat-core/src/session.rs: PendingSystemContextAppend.peer_response_terminal: Option<crate::handles::PeerResponseTerminalFact> (new typed field; serde default + skip_serializing_if=Option::is_none, so durable rows are back-read-safe)
- meerkat-core/src/handles.rs: added serde::Serialize+serde::Deserialize derives to PeerResponseTerminalFact, PeerResponseTerminalSource, PeerResponseTerminalTransportIdentity, PeerResponseTerminalRouteIdentity, PeerResponseTerminalDisplayIdentity, PeerResponseTerminalCorrelationId, PeerResponseTerminalRenderPayload (single-field newtypes use #[serde(transparent)]); added PeerResponseTerminalFact::render_payload_value() accessor

### deferred_cross_crate (TO DO):

- N5-live FULLY DEFERRED (all-or-nothing 3-file retype; 2 of 3 files are out of my literal lane and the live.rs portion cannot compile without them, so I touched none of it). NOTE the lane spec named the wrong crate paths: the real files are meerkat-core/src/live_adapter.rs (NOT meerkat-openai) and meerkat/src/session_runtime/live_orchestration.rs (NOT meerkat-openai). Edits: (1) meerkat-core/src/live_adapter.rs:622 retype LiveProjectionSnapshot.provider_id: String -> meerkat_core::Provider (model_id stays String). (2) meerkat-core/src/live_adapter.rs:512-514 RefreshProviderSwap{from_provider,to_provider}: String -> Provider (match its sibling ChannelIdentitySwap at :481-483 which is already Provider); update the Display arm at :571-576 to use {from_provider:?}/{to_provider:?} or .as_str(). Tests at live_adapter.rs:1126/1151/1674 set provider_id:"openai".into() -> Provider::OpenAI; :1980-1982 RefreshProviderSwap String -> Provider. (3) meerkat/src/session_runtime/live_orchestration.rs:145 drop .as_str().to_string() (build provider_id: open_config.llm_identity.provider directly). (4) meerkat-openai/src/live.rs:1299 current_provider_id: Option<String> -> Option<Provider>; :1362 set_current_identity provider_id: impl Into<String> -> provider: Provider; :2811 and :2854 stamp open_config.llm_identity.provider directly (drop .as_str().to_string()); :3826 guard becomes Provider != Provider; :3829-3830 RefreshProviderSwap carries Provider; :3626 OpenAiLiveCommandError::RefreshProviderSwap{from_provider,to_provider} String -> Provider (and the Display arm at :3641-3647); classify_command_error :3696-3707 moves Provider. live.rs tests at :7605/:7758/:7882/:7997/:8109/:8182 set provider_id String -> Provider; :7736/:7854/:7976/:8090/:8162 set_current_identity(model, Provider::OpenAI.as_str()) -> set_current_identity(model, Provider::OpenAI).
- M10 PRODUCER CARRIER (coordinated set — finishes the typed carry so the live.rs transitional fallback can be DELETED): (a) meerkat-runtime/src/ingress_types.rs:91 add field `pub peer_response_terminal: Option<meerkat_core::PeerResponseTerminalFact>` to RuntimeInputProjection (populate it in meerkat-runtime/src/input.rs runtime_input_projection_for_machine_batch from peer_response_terminal_fact(peer), which already builds the typed fact). (b) meerkat-core/src/service/mod.rs:1187 add `#[serde(default, skip_serializing_if=Option::is_none)] pub peer_response_terminal: Option<crate::handles::PeerResponseTerminalFact>` to AppendSystemContextRequest. (c) meerkat-session/src/persistent.rs:3982 (stage_live_system_context_boundary_snapshot) thread `peer_response_terminal: append.peer_response_terminal` when reducing PendingSystemContextAppend -> AppendSystemContextRequest. (d) meerkat-core/src/session.rs:2162 (system_context_authority::stage_append) change `peer_response_terminal: None` -> `req.peer_response_terminal.clone()` (seam already commented). (e) meerkat-runtime/src/input.rs context_append_to_pending_system_context_append: stamp the typed fact (seam already commented) — thread it via the RuntimeInputProjection field from (a), NOT by re-probing append.key/kind. (f) THEN delete the transitional fallback branch in meerkat-openai/src/live.rs openai_realtime_terminal_peer_response_payload (the `source.starts_with("peer_response_terminal:")` + split_once block) so only the typed path remains.
- M10 MECHANICAL FAN-OUT: adding the required `peer_response_terminal` field to PendingSystemContextAppend breaks ~33 struct-literal construction sites in 13 out-of-lane files; each full literal needs `peer_response_terminal: None,` added (struct-update `..spread` literals are exempt). Files + literal counts: meerkat-core/src/live_adapter.rs (1, test), meerkat/src/surface/runtime_backed.rs (1), meerkat/src/session_runtime/staged_promotion.rs (1), meerkat-cli/src/main.rs (1), meerkat-mcp-server/src/runtime_ingress.rs (1), meerkat-web-runtime/src/lib.rs (2), examples/035-mdm-tux-rs/src/bin/target.rs (1), meerkat-rest/src/lib.rs (1), meerkat-rpc/src/session_executor.rs (1), meerkat-rpc/src/session_runtime.rs (3), meerkat-session/src/ephemeral.rs (8), meerkat-session/src/persistent.rs (13), meerkat-mob/src/runtime/provisioner.rs (1). Suitable for a single codemob gemini-flash bulk pass.

### notes:
M10 is fundamentally a cross-crate carry, NOT confined to the 5 listed lane files: the typed fact must survive producer (input.rs) -> RuntimeInputProjection (ingress_types.rs) -> PendingSystemContextAppend (session.rs) -> AppendSystemContextRequest reduction (persistent.rs:3982) -> realtime_projection_appends() -> live.rs consumer. I completed the in-lane foundation (serde on the fact family, the typed field, the canonical consumer read, in-lane producer/test-fixture updates, a serde round-trip test) and left the live.rs string parse as an EXPLICITLY-MARKED transitional bridge so the live path stays correct until the lead lands the producer carrier (deferred set above). I did NOT introduce a silent fallback as a permanent path — it is documented for deletion in step (f). WIRE/DURABLE IMPACT: PendingSystemContextAppend is Serialize/Deserialize and persisted in session state + carried in meerkat-core LiveProjectionSnapshot.runtime_system_context; the new field is serde-default/skip-if-none so old rows back-read as None. PendingSystemContextAppend does NOT currently derive JsonSchema (live_adapter.rs:635 confirms), so it is NOT in the contracts emit set — if the lead later adds JsonSchema or the AppendSystemContextRequest/RuntimeInputProjection field crosses the SDK schema, run `make regen-schemas` + `make verify-version-parity`. I set wire_impact=true conservatively because the durable session-state JSON shape changed and N5-live's LiveProjectionSnapshot.provider_id is a serialized field. N5-live: touched NOTHING (all-or-nothing 3-file retype, 2 files out of lane); exact edits are in deferred_cross_crate. Strictly stayed within handles.rs/session.rs/input.rs/live.rs; did NOT touch meerkat-openai/src/client.rs (provider-identity lane) nor any */generated/ nor service/mod.rs/persistent.rs/ingress_types.rs/live_adapter.rs/live_orchestration.rs. No .unwrap()/.expect()/panic! added to non-test code (the new helper + accessor are panic-free; .expect() appears only in the new #[cfg(test)] helper). Behavior preserved: the transitional fallback reproduces the exact prior summary output for legacy flattened appends; two live.rs summary tests were upgraded to exercise the canonical typed path.
