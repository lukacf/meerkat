name: CI

# BuildBuddy RBE is the broad CI lane. The exact dense Mob topology stress runs
# from a prebuilt unit archive on GitHub-hosted Linux, so release evidence does
# not mistake BuildBuddy/macOS timing for the hosted scalability signal.

on:
  push:
    branches:
      - main
      - ci/**
      - feat/**
      - feature/**
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
  workflow_dispatch: {}

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read
  id-token: write

jobs:
  gcp-buildbuddy:
    name: GCP BuildBuddy
    uses: ./.github/workflows/buildbuddy.yml
    with:
      mode: changed-paths
      machine_authority_base_sha: >-
        ${{
          github.event_name == 'pull_request' && github.event.pull_request.base.sha ||
          github.event_name == 'push' && github.event.before ||
          ''
        }}
      machine_authority_head_sha: ${{ github.sha }}
    secrets: inherit

  github-hosted-dense-topology:
    name: GitHub-hosted dense Mob topology
    uses: ./.github/workflows/mob-dense-topology.yml

  gate:
    name: CI gate
    if: ${{ always() }}
    needs:
      - gcp-buildbuddy
      - github-hosted-dense-topology
    runs-on: ubuntu-latest
    steps:
      - name: Check CI result
        shell: bash
        env:
          BUILDBUDDY_RESULT: ${{ needs.gcp-buildbuddy.result }}
          DENSE_TOPOLOGY_RESULT: ${{ needs.github-hosted-dense-topology.result }}
        run: |
          echo "GCP BuildBuddy result: ${BUILDBUDDY_RESULT}"
          echo "GitHub-hosted dense Mob topology result: ${DENSE_TOPOLOGY_RESULT}"
          if [[ "${BUILDBUDDY_RESULT}" != "success" ]]; then
            echo "::error::BuildBuddy CI did not complete successfully."
            exit 1
          fi
          if [[ "${DENSE_TOPOLOGY_RESULT}" != "success" ]]; then
            echo "::error::GitHub-hosted dense Mob topology did not complete successfully."
            exit 1
          fi

      - name: Enforce push-to-terminal budget
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          MAX_SECONDS: "1200"
        run: |
          set -euo pipefail
          created_at="$(gh api "/repos/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" --jq .created_at)"
          elapsed="$(python3 - "${created_at}" <<'PY'
          import datetime
          import sys

          started = datetime.datetime.fromisoformat(sys.argv[1].replace("Z", "+00:00"))
          now = datetime.datetime.now(datetime.timezone.utc)
          print(int((now - started).total_seconds()))
          PY
          )"
          echo "Push-to-full-CI terminal budget: ${elapsed}/${MAX_SECONDS} seconds"
          if ((elapsed > MAX_SECONDS)); then
            echo "::error::Full CI exceeded the 20-minute push-to-terminal SLO (${elapsed}s)."
            exit 1
          fi

      - name: Checkout attested source tree
        uses: actions/checkout@v6

      - name: Build exact-tree CI attestation
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        id: attestation
        shell: bash
        env:
          BUILDBUDDY_RESULT: ${{ needs.gcp-buildbuddy.result }}
          DENSE_TOPOLOGY_RESULT: ${{ needs.github-hosted-dense-topology.result }}
        run: |
          set -euo pipefail
          destination="${RUNNER_TEMP}/meerkat-ci-attestation"
          mkdir -p "${destination}"
          tree_sha="$(git rev-parse 'HEAD^{tree}')"
          jq -n \
            --arg repository "${GITHUB_REPOSITORY}" \
            --arg commit_sha "${GITHUB_SHA}" \
            --arg tree_sha "${tree_sha}" \
            --arg workflow "${GITHUB_WORKFLOW}" \
            --arg workflow_run_id "${GITHUB_RUN_ID}" \
            --arg workflow_run_attempt "${GITHUB_RUN_ATTEMPT}" \
            --arg event "${GITHUB_EVENT_NAME}" \
            --arg ref "${GITHUB_REF}" \
            --arg validation_result "${BUILDBUDDY_RESULT}" \
            --arg dense_topology_result "${DENSE_TOPOLOGY_RESULT}" \
            '{
              schema_version: 3,
              repository: $repository,
              commit_sha: $commit_sha,
              tree_sha: $tree_sha,
              workflow: $workflow,
              workflow_run_id: $workflow_run_id,
              workflow_run_attempt: $workflow_run_attempt,
              event: $event,
              ref: $ref,
              validation_backend: "gcp-buildbuddy+github-hosted-dense-mob",
              validation_result: (
                if $validation_result == "success" and $dense_topology_result == "success"
                then "success"
                else "failure"
                end
              ),
              component_results: {
                gcp_buildbuddy: $validation_result,
                github_hosted_dense_mob: $dense_topology_result
              }
            }' > "${destination}/attestation.json"
          echo "tree_sha=${tree_sha}"
          echo "path=${destination}" >> "${GITHUB_OUTPUT}"

      - name: Upload exact-tree CI attestation
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        uses: actions/upload-artifact@v7
        with:
          name: meerkat-ci-attestation-${{ github.sha }}-${{ github.run_attempt }}
          path: ${{ steps.attestation.outputs.path }}/attestation.json
          if-no-files-found: error
          retention-days: 14
