[package]
name = "public-live-rtc-fixture"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
meerkat = { path = "../../../meerkat", default-features = false, features = [
    "openai-live",
    "live-webrtc",
    "sqlite-store",
    "session-store",
] }

[lib]
path = "../public-live-shared/lib.rs"
