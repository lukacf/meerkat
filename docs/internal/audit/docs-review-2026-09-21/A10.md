# A10: Runnable examples and repository-local supporting documentation

This ledger preserves the baseline evidence and independent disposition. See [the audit index](README.md) for scope, commits, and publication boundaries.

## A10-001: The examples index assigns all Rust examples to the facade manifest

**Candidate severity:** low. **Independent verdict:** confirm.

The unqualified manifest claim is false for multiple indexed Rust examples and directs readers toward the wrong package/target selection. It is unrelated to the intentional numbering gaps.

### Baseline documentation evidence

- [examples/README.md:40-41](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/README.md?plain=1#L40-L41)

```text
Rust examples in this folder are wired into `meerkat/Cargo.toml` and can be run
directly from the workspace root.
```

### Implementation proof

- [meerkat-mob/Cargo.toml:174-184](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/Cargo.toml#L174-L184)

```text
name = "017-mob-coding-swarm"
path = "../examples/017-mob-coding-swarm-rs/main.rs"
```

  017-019 are example targets of meerkat-mob, not meerkat.

- [examples/034-codemob-mcp/Cargo.toml:7-11](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/034-codemob-mcp/Cargo.toml#L7-L11)

```text
[workspace]  # standalone, not part of the main workspace

[[bin]]
name = "codemob-mcp"
```

  034 is a standalone binary package, not a facade example target.

- [examples/035-mdm-tux-rs/Cargo.toml:7-19](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/Cargo.toml#L7-L19)

```text
[[bin]]
name = "mdm-target"
path = "src/bin/target.rs"
```

  035 likewise declares a standalone workspace with three binaries.

### Independent challenge

The workspace-root execution advice and the specific 001 command are valid; the unqualified manifest ownership statement is not. The actual Cargo targets put 017-019 in meerkat-mob and make 034/035 standalone binary packages. Independently enumerating examples found 35 directories; numbering gaps 016 and 020 are not defects. Limit this correction to target ownership, not the count, numbering, or the existing valid 001 command.

- [meerkat-mob/Cargo.toml:174-184](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/Cargo.toml#L174-L184)

```text
path = "../examples/017-mob-coding-swarm-rs/main.rs"
```

  This manifest registers all three numbered mob examples, 017, 018, and 019.

- [examples/034-codemob-mcp/Cargo.toml:1-18](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/034-codemob-mcp/Cargo.toml#L1-L18)

```text
[workspace]  # standalone, not part of the main workspace
```

  034 has its own binary target and path dependencies; depending on the facade does not register it as a facade example.

- [examples/035-mdm-tux-rs/Cargo.toml:7-22](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/Cargo.toml#L7-L22)

```text
name = "mdm-tux"
```

  035 is a standalone workspace containing mdm-target, mdm-tux, and mdm-kennel binaries, not an example in meerkat/Cargo.toml.

**Required correction:** Replace the blanket sentence at examples/README.md:40-41 with the actual split: most numbered Rust examples are facade targets, 017-019 are meerkat-mob example targets selected with -p meerkat-mob, and 034/035 are standalone packages selected with their Cargo.toml --manifest-path and binary names. Keep the working 001 command and the correct 35-example inventory; direct readers to each example's own run instructions.

### Correction

Replaced blanket facade ownership with the actual Cargo split: most numbered examples belong to meerkat, 017-019 belong to meerkat-mob, and 034/035 are standalone packages selected by --manifest-path and --bin. Preserved the valid 001 command, all example rows, numbering gaps, and the 35-directory count.

- [examples/README.md:48-53](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/README.md?plain=1#L48-L53)

```text
Most numbered Rust examples are registered in `meerkat/Cargo.toml`; 017-019
are registered in `meerkat-mob/Cargo.toml` and use `-p meerkat-mob`. Examples
034 and 035 are standalone packages: select their `Cargo.toml` with
`--manifest-path` and the desired binary with `--bin`.
```

- [meerkat-mob/Cargo.toml:174-184](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/Cargo.toml#L174-L184)

```text
path = "../examples/017-mob-coding-swarm-rs/main.rs"
```

- [examples/034-codemob-mcp/Cargo.toml:7-11](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/034-codemob-mcp/Cargo.toml#L7-L11)

```text
[workspace]  # standalone, not part of the main workspace
```

- [examples/035-mdm-tux-rs/Cargo.toml:7-19](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/Cargo.toml#L7-L19)

```text
name = "mdm-tux"
path = "src/bin/tux.rs"
```

### Final independent review

**PASS:** The replacement describes the actual target ownership without changing the valid 001 command or example inventory. I parsed the manifests and counted 35 numbered example directories: 11 numbered facade targets, three meerkat-mob targets, and the two stated standalone Rust packages. All registered source paths exist. The 001 command selects the correct package, example name, and required feature.

- [examples/README.md:48-56](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/README.md?plain=1#L48-L56)

```text
Most numbered Rust examples are registered in `meerkat/Cargo.toml`; 017-019
are registered in `meerkat-mob/Cargo.toml` and use `-p meerkat-mob`.
```

  The following lines distinguish the standalone packages and retain the exact working manifest selector for 001.

- [meerkat/Cargo.toml:265-268](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/Cargo.toml#L265-L268)

```text
name = "001-hello-meerkat"
path = "../examples/001-hello-meerkat-rs/main.rs"
required-features = ["jsonl-store"]
```

  This is the target and feature used by the retained README command.

- [meerkat-mob/Cargo.toml:174-184](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/Cargo.toml#L174-L184)

```text
name = "017-mob-coding-swarm"
```

  These three adjacent example entries register 017, 018, and 019 in meerkat-mob.

- [examples/034-codemob-mcp/Cargo.toml:7-11](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/034-codemob-mcp/Cargo.toml#L7-L11)

```text
[workspace]  # standalone, not part of the main workspace
```

  The adjacent bin entry is codemob-mcp, supporting --manifest-path and --bin rather than a facade example selector.

- [examples/035-mdm-tux-rs/Cargo.toml:7-19](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/Cargo.toml#L7-L19)

```text
name = "mdm-tux"
path = "src/bin/tux.rs"
```

  This standalone workspace declares mdm-target, mdm-tux, and mdm-kennel.

## A10-002: Anthropic-only shell prerequisites do not satisfy the scripts' unpinned CLI model

**Candidate severity:** medium. **Independent verdict:** confirm.

On a fresh checkout/configuration, setting only ANTHROPIC_API_KEY does not make these model-unpinned CLI calls use Anthropic. In 010 the redirected roots additionally prevent relying on the operator's ordinary model configuration. The root quick start repeats the insufficient setup. This is a credential/model prerequisite mismatch, not a claim that every existing configured installation fails.

### Baseline documentation evidence

- [examples/004-cli-one-liners-sh/README.md:13-17](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/004-cli-one-liners-sh/README.md?plain=1#L13-L17)

```text
export ANTHROPIC_API_KEY=sk-...
./scripts/repo-cargo build -p rkat --bin rkat
```

- [examples/010-mcp-tool-server-sh/README.md:29-34](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/010-mcp-tool-server-sh/README.md?plain=1#L29-L34)

```text
export ANTHROPIC_API_KEY=sk-...
./scripts/repo-cargo build -p rkat --bin rkat
```

- [examples/README.md:8-10](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/README.md?plain=1#L8-L10)

```text
# Set your API key
export ANTHROPIC_API_KEY=sk-...
```

- [examples/README.md:35-37](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/README.md?plain=1#L35-L37)

```text
# Run a shell example
(cd examples/010-mcp-tool-server-sh && ./setup.sh)
```

- [examples/README.md:207-211](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/README.md?plain=1#L207-L211)

```text
export OPENAI_API_KEY=sk-...        # Optional (examples 021, 034, 035 live suite, 036)
```

### Implementation proof

- [examples/004-cli-one-liners-sh/examples.sh:55-65](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/004-cli-one-liners-sh/examples.sh#L55-L65)

```text
$RKAT run "List three benefits of Rust. Be concise."
```

  The script invokes run without selecting a model or provider.

- [examples/010-mcp-tool-server-sh/setup.sh:108-121](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/010-mcp-tool-server-sh/setup.sh#L108-L121)

```text
run_in_project run \
  --wait-for-mcp \
  --verbose \
```

  The MCP script also omits model/provider selection, despite redirecting state/context/user roots and checking ANTHROPIC_API_KEY at lines 59-62.

- [meerkat-cli/src/main.rs:4826-4842](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L4826-L4842)

```text
model: explicit_model,
            provider: explicit_provider.map(Provider::as_core),
            auth_binding,
```

  CLI model selection delegates the actual hints to the canonical create-session resolver; presence of an Anthropic key is not supplied as a provider hint.

- [meerkat/src/factory.rs:1589-1606](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L1589-L1606)

```text
(meerkat_models::global_default_model().to_string(), None)
```

  With no explicit hints or configured model, the resolver chooses the catalog global default rather than selecting from available environment keys.

- [meerkat-models/src/catalog.rs:34-41](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/catalog.rs#L34-L41)

```text
const DEFAULT_OPENAI: &str = "gpt-6-astra";
```

  The current OpenAI default is gpt-6-astra.

- [meerkat-models/src/catalog.rs:151-155](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/catalog.rs#L151-L155)

```text
global_default_model: DEFAULT_OPENAI,
```

  That OpenAI model is also the no-hint global default.

- [meerkat-core/src/config_template.toml:4-8](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/config_template.toml#L4-L8)

```text
# No model is pinned here: when `agent.model` is empty, surfaces resolve the
# default through the injected model catalog (meerkat-models)
```

  A fresh template does not supply an Anthropic pin that would rescue the documented setup.

### Independent challenge

An existing explicit model/configuration can rescue either script, so this is not an unconditional failure on every installation. On the documented fresh configuration, however, neither script selects Anthropic, and exporting its key supplies credentials rather than a model-selection hint. The 010 wrapper forwards only filesystem roots and then performs an unpinned run; it also explicitly refuses a missing ANTHROPIC_API_KEY. A docs-only fix that merely replaces that export with OPENAI_API_KEY would therefore still fail 010's guard.

- [examples/004-cli-one-liners-sh/examples.sh:53-84](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/004-cli-one-liners-sh/examples.sh#L53-L84)

```text
$RKAT run "List three benefits of Rust. Be concise."
```

  The executable is resolved but no model/provider arguments are injected; the fresh, isolated, verbose, and streaming runs are also unpinned.

- [examples/010-mcp-tool-server-sh/setup.sh:59-76](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/010-mcp-tool-server-sh/setup.sh#L59-L76)

```text
if [[ -z "${ANTHROPIC_API_KEY:-}" ]]; then
```

  The script requires that exact environment key and forwards state/context/user roots only; its wrapper does not add Anthropic selection.

- [examples/010-mcp-tool-server-sh/setup.sh:105-120](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/010-mcp-tool-server-sh/setup.sh#L105-L120)

```text
run_in_project run \
```

  The actual live invocation supplies --wait-for-mcp and --verbose but neither a model nor a provider.

- [meerkat-cli/src/main.rs:4061-4082](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L4061-L4082)

```text
let resolution = resolve_cli_create_session_model(&config, model, provider, auth_binding)?;
```

  The CLI routes the actual explicit hints and effective config to shared model selection.

- [meerkat/src/factory.rs:1589-1607](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L1589-L1607)

```text
(meerkat_models::global_default_model().to_string(), None)
```

  Without an explicit/binding/configured model, selection falls back to the catalog, not whichever provider key is present.

- [meerkat-models/src/catalog.rs:34-41](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/catalog.rs#L34-L41)

```text
const DEFAULT_OPENAI: &str = "gpt-6-astra";
```

  This establishes the model whose access is needed for the fresh unpinned OpenAI path.

- [meerkat-models/src/catalog.rs:150-155](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/catalog.rs#L150-L155)

```text
global_default_model: DEFAULT_OPENAI,
```

  The OpenAI default is also the global no-hint default.

- [meerkat-core/src/config_template.toml:4-9](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/config_template.toml#L4-L9)

```text
# No model is pinned here: when `agent.model` is empty, surfaces resolve the
```

  A fresh template does not implicitly supply the missing Anthropic pin.

**Required correction:** Make the prerequisites truthful for the unmodified scripts: document that fresh unpinned runs select gpt-6-astra and require usable OpenAI credentials/model access; use OPENAI_API_KEY in 004's fresh-run prerequisites, and add it alongside the still-required ANTHROPIC_API_KEY guard in 010. Add the same qualification/export to the index quick start and include 004/010 in its OpenAI-key explanation. Explain that an existing explicit provider/model configuration can change the credential requirement and that merely setting an API key does not choose that provider. Do not prescribe an ordinary-realm-only Anthropic pin as sufficient for 010's redirected roots or 004's isolated run.

### Correction

004 now exports OPENAI_API_KEY for fresh unpinned runs. 010 and the index quick start export it alongside ANTHROPIC_API_KEY, which 010 still explicitly checks. All three explain gpt-6-astra access, credential versus provider selection, and the actual isolated/redirected roots. The index API-key inventory now includes 004/010. No script or product behavior was changed.

- [examples/004-cli-one-liners-sh/README.md:15-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/004-cli-one-liners-sh/README.md?plain=1#L15-L24)

```text
configuration it selects `gpt-6-astra`, so the OpenAI key must have access to
that model.
```

- [examples/010-mcp-tool-server-sh/README.md:32-43](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/010-mcp-tool-server-sh/README.md?plain=1#L32-L43)

```text
export ANTHROPIC_API_KEY=sk-...  # Required by the script's guard
export OPENAI_API_KEY=sk-...     # Fresh unpinned run: needs gpt-6-astra access
```

- [examples/README.md:9-12](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/README.md?plain=1#L9-L12)

```text
# Set API keys: 010 checks ANTHROPIC_API_KEY, but its fresh unpinned run
# uses OpenAI's gpt-6-astra and needs access to that model.
export ANTHROPIC_API_KEY=sk-...
export OPENAI_API_KEY=sk-...
```

- [examples/README.md:42-46](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/README.md?plain=1#L42-L46)

```text
010 redirects its roots into `.work/`, and 004 includes an `--isolated` run.
An ordinary-realm-only model pin is not sufficient for every run.
```

- [examples/README.md:222-222](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/README.md?plain=1#L222)

```text
export OPENAI_API_KEY=sk-...        # 004/010 fresh defaults; also 021, 034, 035 live suite, 036
```

- [examples/010-mcp-tool-server-sh/setup.sh:59-62](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/010-mcp-tool-server-sh/setup.sh#L59-L62)

```text
if [[ -z "${ANTHROPIC_API_KEY:-}" ]]; then
```

- [meerkat/src/factory.rs:1589-1607](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L1589-L1607)

```text
(meerkat_models::global_default_model().to_string(), None)
```

- [meerkat-models/src/catalog.rs:34-41](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-models/src/catalog.rs#L34-L41)

```text
const DEFAULT_OPENAI: &str = "gpt-6-astra";
```

- [meerkat-models/src/catalog.rs:150-155](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-models/src/catalog.rs#L150-L155)

```text
global_default_model: DEFAULT_OPENAI,
```

### Final independent review

**PASS:** All required occurrences are corrected: 004's prerequisite, 010's two-key prerequisite and guard explanation, the index quick-start exports and shell-run context, and the index API-key inventory. The text distinguishes credential availability from model selection and does not suggest an ordinary-realm-only pin rescues isolated or redirected runs. The scripts are unchanged: 004 submits unpinned runs including --isolated; 010 still explicitly guards ANTHROPIC_API_KEY and submits an unpinned run under redirected roots. The documented fresh-default OpenAI access requirement follows the actual CLI-to-facade resolver and catalog, not the presence of an environment key.

- [examples/004-cli-one-liners-sh/README.md:13-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/004-cli-one-liners-sh/README.md?plain=1#L13-L24)

```text
export OPENAI_API_KEY=sk-...
```

  The paragraph immediately below names gpt-6-astra access, the distinction between API keys and provider selection, and the isolated-realm caveat.

- [examples/010-mcp-tool-server-sh/README.md:29-43](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/010-mcp-tool-server-sh/README.md?plain=1#L29-L43)

```text
export ANTHROPIC_API_KEY=sk-...  # Required by the script's guard
export OPENAI_API_KEY=sk-...     # Fresh unpinned run: needs gpt-6-astra access
```

  The added explanation retains the Anthropic guard and qualifies any alternate model configuration by the script's redirected roots.

- [examples/README.md:9-12](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/README.md?plain=1#L9-L12)

```text
export ANTHROPIC_API_KEY=sk-...
export OPENAI_API_KEY=sk-...
```

  The quick start now supplies both keys before its existing 010 setup.sh command at line 39.

- [examples/README.md:42-46](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/README.md?plain=1#L42-L46)

```text
010 redirects its roots into `.work/`, and 004 includes an `--isolated` run.
An ordinary-realm-only model pin is not sufficient for every run.
```

  The shared quick-start qualification covers both distinct scoping cases.

- [examples/README.md:219-223](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/README.md?plain=1#L219-L223)

```text
export OPENAI_API_KEY=sk-...        # 004/010 fresh defaults; also 021, 034, 035 live suite, 036
```

  The second, separately listed API-key occurrence now includes 004 and 010.

- [examples/010-mcp-tool-server-sh/setup.sh:59-77](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/010-mcp-tool-server-sh/setup.sh#L59-L77)

```text
if [[ -z "${ANTHROPIC_API_KEY:-}" ]]; then
```

  The guard remains, and BASE_ARGS forwards state, context, and user-config roots. The live run at lines 110-123 adds no model/provider flag.

- [examples/004-cli-one-liners-sh/examples.sh:55-73](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/004-cli-one-liners-sh/examples.sh#L55-L73)

```text
$RKAT run --isolated "This session lives in its own isolated realm."
```

  Both ordinary runs and the isolated run leave model/provider selection to configuration/defaults.

- [meerkat-cli/src/main.rs:4826-4842](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L4826-L4842)

```text
model: explicit_model,
            provider: explicit_provider.map(Provider::as_core),
```

  CLI passes actual selection hints into the canonical resolver; an Anthropic key is not a model hint.

- [meerkat/src/factory.rs:1589-1607](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L1589-L1607)

```text
(meerkat_models::global_default_model().to_string(), None)
```

  Without an explicit/configured selection, the resolver chooses the catalog default.

- [meerkat-models/src/catalog.rs:34-41](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-models/src/catalog.rs#L34-L41)

```text
const DEFAULT_OPENAI: &str = "gpt-6-astra";
```

  canonical() assigns this constant to global_default_model at line 153.

## A10-003: TurnCompleted is not emitted once per internal tool-loop iteration

**Candidate severity:** medium. **Independent verdict:** confirm.

Readers building progress counters or per-call accounting from this table will overinterpret TurnCompleted. Intermediate tool-loop calls are not paired with that event.

### Baseline documentation evidence

- [examples/005-streaming-events-rs/README.md:20-20](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/005-streaming-events-rs/README.md?plain=1#L20)

```text
| `TurnCompleted` | One agent loop iteration finished |
```

### Implementation proof

- [meerkat-core/src/agent/usage_accounting_tests.rs:268-281](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/usage_accounting_tests.rs#L268-L281)

```text
assert_eq!(client.calls_made(), 3, "the run made three provider calls");
```

  The regression constructs a run with three provider calls, including two tool calls.

- [meerkat-core/src/agent/usage_accounting_tests.rs:277-281](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/usage_accounting_tests.rs#L277-L281)

```text
observed_first.turn_rows.len(),
        1,
        "a tool-using run publishes one turn row, not one per provider call"
```

  The asserted TurnCompleted observation is one row, not three loop-iteration completions.

- [meerkat-core/src/agent/state.rs:6897-6904](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L6897-L6904)

```text
emit_phase_event!(self, ctx, AgentEvent::TurnCompleted { stop_reason, usage });
```

  The emitter is on the terminal assistant/no-tool path, after terminal hooks and boundary side effects, rather than on every intermediate tool iteration.

### Independent challenge

The adjacent example happens to use an empty tool dispatcher, so its two simple runs cannot reveal this distinction. The table nevertheless describes the generic event. The actual terminal path emits TurnCompleted, and an existing multi-call regression explicitly observes one such row after three provider calls. It is not the completion counterpart for every internal tool-loop iteration. Do not replace this with a guarantee that every run outcome emits it or conflate it with post-run structured extraction completion.

- [examples/005-streaming-events-rs/main.rs:44-49](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/005-streaming-events-rs/main.rs#L44-L49)

```text
.build(Arc::new(llm), Arc::new(EmptyToolDispatcher), store)
```

  The runnable demo's no-tool case is compatible with both descriptions and is not sufficient evidence for the broader table claim.

- [meerkat-core/src/agent/usage_accounting_tests.rs:268-291](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/usage_accounting_tests.rs#L268-L291)

```text
"a tool-using run publishes one turn row, not one per provider call"
```

  The regression asserts three calls, a single TurnCompleted row, and accounting from the closing call.

- [meerkat-core/src/agent/state.rs:6897-6904](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L6897-L6904)

```text
emit_phase_event!(self, ctx, AgentEvent::TurnCompleted { stop_reason, usage });
```

  This emission belongs to the terminal no-tool assistant phase, after accepting terminal hooks and boundary side effects.

**Required correction:** Change the TurnCompleted table row to describe completion of the terminal assistant turn of the agentic loop, not each internal iteration/provider call. Keep it distinct from RunCompleted and structured-output extraction; if accounting is mentioned, its optional usage describes the closing call, not an aggregate of all calls.

### Correction

Changed TurnCompleted from an internal-iteration event to completion of the terminal assistant turn of the agentic loop. The row does not promise delivery for every run outcome, equate it with RunCompleted/extraction completion, or advertise aggregate usage.

- [examples/005-streaming-events-rs/README.md:20-20](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/005-streaming-events-rs/README.md?plain=1#L20)

```text
| `TurnCompleted` | Terminal assistant turn of the agentic loop completed, not each internal provider call |
```

- [meerkat-core/src/agent/usage_accounting_tests.rs:268-291](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/usage_accounting_tests.rs#L268-L291)

```text
"a tool-using run publishes one turn row, not one per provider call"
```

- [meerkat-core/src/agent/state.rs:6897-6904](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L6897-L6904)

```text
emit_phase_event!(self, ctx, AgentEvent::TurnCompleted { stop_reason, usage });
```

### Final independent review

**PASS:** The table now describes the terminal assistant turn of the agentic loop, not every provider/tool iteration. It does not promise the event on every failure or cancellation and does not equate it with separate structured extraction. I checked both normal and extraction terminal emitters plus the existing multi-call accounting regression. The example's run selector and jsonl-store feature agree with its Cargo target; no live example or regression test was executed.

- [examples/005-streaming-events-rs/README.md:20-20](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/005-streaming-events-rs/README.md?plain=1#L20)

```text
| `TurnCompleted` | Terminal assistant turn of the agentic loop completed, not each internal provider call |
```

- [meerkat-core/src/agent/state.rs:6897-6904](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L6897-L6904)

```text
emit_phase_event!(self, ctx, AgentEvent::TurnCompleted { stop_reason, usage });
```

  The ordinary terminal path emits after terminal hooks, not on every loop iteration.

- [meerkat-core/src/agent/state.rs:6803-6815](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L6803-L6815)

```text
// The main agentic turn has committed. Extraction
            // is a separate post-run validation phase.
```

  The extraction branch preserves the distinction made by the revised description.

- [meerkat-core/src/agent/usage_accounting_tests.rs:268-291](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/usage_accounting_tests.rs#L268-L291)

```text
"a tool-using run publishes one turn row, not one per provider call"
```

  This source regression expects one closing row after three provider calls and attributes that row to the closing call.

- [meerkat/Cargo.toml:270-273](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/Cargo.toml#L270-L273)

```text
name = "005-streaming-events"
path = "../examples/005-streaming-events-rs/main.rs"
required-features = ["jsonl-store"]
```

## A10-004: BudgetWarning currently reports exhaustion, not an approaching-limit threshold

**Candidate severity:** medium. **Independent verdict:** confirm.

The table promises advance warning that the current emitters do not provide. A consumer waiting for an approaching-limit event may receive nothing until the budget has already been reached or exceeded.

### Baseline documentation evidence

- [examples/005-streaming-events-rs/README.md:21-21](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/005-streaming-events-rs/README.md?plain=1#L21)

```text
| `BudgetWarning` | Warning emitted when approaching a token/time/tool-call budget limit |
```

### Implementation proof

- [meerkat-core/src/agent/state.rs:400-412](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L400-L412)

```text
fn budget_warning_event(exceeded: BudgetExceeded) -> AgentEvent {
```

  The event constructor accepts an already-exceeded budget fact and emits percent: 1.0.

- [meerkat-core/src/agent/state.rs:4239-4244](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L4239-L4244)

```text
if let Some(exceeded) = self.budget.observe().exceeded() {
                emit_event!(budget_warning_event(exceeded));
```

  Loop-entry emission is gated by exhaustion, not a near-limit threshold.

- [meerkat-core/src/agent/state.rs:5811-5819](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L5811-L5819)

```text
if let Some(exceeded) = self.budget.observe().exceeded() {
            emit_phase_event!(self, ctx, budget_warning_event(exceeded));
```

  The post-response emission is also gated by exhaustion after usage is recorded.

### Independent challenge

The BudgetWarning name and the configuration template's warning-threshold field make the README plausible, but they are not producer evidence. The ordinary loop constructs this event from an already-exceeded budget fact, with percent 1.0; the entry and post-response emitters are both exhaustion-gated. The correction should describe that actual loop behavior rather than promise an advance warning or change the product to match the prose.

- [meerkat-core/src/agent/state.rs:400-411](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L400-L411)

```text
fn budget_warning_event(exceeded: BudgetExceeded) -> AgentEvent {
```

  The constructor consumes BudgetExceeded and sets percent to 1.0.

- [meerkat-core/src/agent/state.rs:4241-4244](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L4241-L4244)

```text
if let Some(exceeded) = self.budget.observe().exceeded() {
```

  The loop-entry emission occurs only after the budget observation reports exhaustion.

- [meerkat-core/src/agent/state.rs:5811-5819](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L5811-L5819)

```text
emit_phase_event!(self, ctx, budget_warning_event(exceeded));
```

  The post-response emission likewise follows usage recording and an exceeded observation, not a near-limit percentage test.

**Required correction:** Replace the approaching-limit description with a statement that the current agent loop reports a reached/exceeded token, time, or tool-call budget. Do not promise pre-exhaustion delivery or a percentage threshold.

### Correction

Described the current loop's BudgetWarning as a reached/exceeded token, time, or tool-call budget, removing the advance-warning promise.

- [examples/005-streaming-events-rs/README.md:21-21](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/005-streaming-events-rs/README.md?plain=1#L21)

```text
| `BudgetWarning` | The current agent loop reports a reached/exceeded token, time, or tool-call budget |
```

- [meerkat-core/src/agent/state.rs:400-411](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L400-L411)

```text
fn budget_warning_event(exceeded: BudgetExceeded) -> AgentEvent {
```

- [meerkat-core/src/agent/state.rs:5811-5819](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L5811-L5819)

```text
if let Some(exceeded) = self.budget.observe().exceeded() {
```

### Final independent review

**PASS:** The only required BudgetWarning table occurrence now describes exhaustion instead of an advance threshold. The code constructs it from a typed BudgetExceeded and gates both the entry and post-accounting emitters on an exceeded observation. The replacement neither promises pre-exhaustion delivery nor invents a percentage threshold.

- [examples/005-streaming-events-rs/README.md:21-21](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/005-streaming-events-rs/README.md?plain=1#L21)

```text
| `BudgetWarning` | The current agent loop reports a reached/exceeded token, time, or tool-call budget |
```

- [meerkat-core/src/agent/state.rs:400-411](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L400-L411)

```text
fn budget_warning_event(exceeded: BudgetExceeded) -> AgentEvent {
```

  The helper maps the three exceeded dimensions and sets percent to 1.0.

- [meerkat-core/src/agent/state.rs:4241-4243](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L4241-L4243)

```text
if let Some(exceeded) = self.budget.observe().exceeded() {
```

  The entry emitter is exhaustion-gated.

- [meerkat-core/src/agent/state.rs:5811-5819](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L5811-L5819)

```text
emit_phase_event!(self, ctx, budget_warning_event(exceeded));
```

  The post-provider emitter is also inside an exceeded observation, after measured usage has been recorded.

- [meerkat-core/src/budget.rs:263-273](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/budget.rs#L263-L273)

```text
if used >= limit {
```

  Reached as well as exceeded token totals qualify.

## A10-005: Cumulative token budgets are accounting stop conditions, not exact provider spend caps

**Candidate severity:** medium. **Independent verdict:** confirm.

A provider request can consume more input/output tokens than the remaining budget. The runtime then stops further work, but the already-consumed total can exceed max_tokens. 'Hard cap' incorrectly suggests exact prepaid admission or an in-stream ceiling on billed usage.

### Baseline documentation evidence

- [examples/009-budget-and-retry-rs/README.md:7-7](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/009-budget-and-retry-rs/README.md?plain=1#L7)

```text
- `BudgetLimits` — hard caps on tokens, tool calls, and duration
```

- [examples/009-budget-and-retry-rs/README.md:16-16](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/009-budget-and-retry-rs/README.md?plain=1#L16)

```text
| `max_tokens` | Hard cap on cumulative token usage |
```

### Implementation proof

- [meerkat-core/src/agent/state.rs:5004-5010](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L5004-L5010)

```text
let configured_max_tokens = self.config.resolved_max_tokens_per_turn();
```

  Request output size is prepared from the per-turn setting and model output limit; it is not clamped to the remaining cumulative token budget here.

- [meerkat-core/src/agent/state.rs:5811-5817](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L5811-L5817)

```text
self.budget.record_turn_usage(turn_usage);
```

  Actual provider usage is charged after the completed response, before checking whether the budget was exceeded.

- [meerkat-core/src/budget.rs:263-274](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/budget.rs#L263-L274)

```text
if used >= limit {
```

  Budget observation detects already-accounted usage reaching or crossing the limit. It cannot prevent the completed request from crossing it.

### Independent challenge

The example separately configures a cumulative token budget and a per-call output request limit. Those are not interchangeable: output is prepared using max_tokens_per_turn/model limits, while provider-accounted usage is charged after the call. The cumulative guard can stop subsequent work but cannot guarantee that the just-completed call stayed under the remaining total, particularly because input tokens also count. Narrow the correction to this spend-cap implication, without inventing different tool-call or wall-clock semantics.

- [examples/009-budget-and-retry-rs/main.rs:45-55](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/009-budget-and-retry-rs/main.rs#L45-L55)

```text
.max_tokens_per_turn(512)
```

  The runnable example sets the separate 2000 cumulative and 512 per-response controls.

- [meerkat-core/src/agent/state.rs:5004-5010](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L5004-L5010)

```text
let configured_max_tokens = self.config.resolved_max_tokens_per_turn();
```

  The provider request's output limit derives from the per-turn configuration and model cap, not the remaining cumulative token budget.

- [meerkat-core/src/agent/state.rs:5811-5817](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L5811-L5817)

```text
self.budget.record_turn_usage(turn_usage);
```

  Usage is charged after provider response accounting, before testing whether the budget was exceeded.

- [meerkat-core/src/budget.rs:263-273](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/budget.rs#L263-L273)

```text
if used >= limit {
```

  The budget observes already-used tokens; crossing the limit is an explicit supported observation.

**Required correction:** Remove the cumulative-token hard-cap guarantee from both the Concepts bullet and max_tokens row. Describe max_tokens as a cumulative accounted-usage stopping limit checked around provider calls, with possible overshoot by the final call. Distinguish max_tokens_per_turn as the requested response-output bound. Preserve the existing tool-count and duration descriptions.

### Correction

Removed the exact cumulative-token cap claim from both Concepts and the max_tokens row. Explained accounted-usage checks around provider calls, final-call overshoot, and the separate requested-output max_tokens_per_turn control. Preserved the tool-count and duration rows.

- [examples/009-budget-and-retry-rs/README.md:7-7](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/009-budget-and-retry-rs/README.md?plain=1#L7)

```text
- `BudgetLimits` — cumulative token-usage stopping limits, plus caps on tool calls and duration
```

- [examples/009-budget-and-retry-rs/README.md:16-23](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/009-budget-and-retry-rs/README.md?plain=1#L16-L23)

```text
The cumulative token limit is checked around provider calls using accounted
usage, so a completed call can exceed the remaining budget before further work
is stopped. Separately, `max_tokens_per_turn` bounds the requested response
output (512 in this example), not cumulative input and output usage.
```

- [meerkat-core/src/agent/state.rs:5004-5010](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L5004-L5010)

```text
let configured_max_tokens = self.config.resolved_max_tokens_per_turn();
```

- [meerkat-core/src/agent/state.rs:5811-5817](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L5811-L5817)

```text
self.budget.record_turn_usage(turn_usage);
```

- [meerkat-core/src/budget.rs:263-273](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/budget.rs#L263-L273)

```text
if used >= limit {
```

### Final independent review

**PASS:** Both required hard-cap occurrences were replaced: the Concepts bullet and max_tokens row. The new explanation correctly distinguishes cumulative measured usage, which can overshoot in the final call, from the requested per-response output limit. It preserves the independently scoped tool-count and duration rows. The adjacent source sets 512 for the first agent and separately configures its cumulative budget; the README command selects the registered 009 example with jsonl-store.

- [examples/009-budget-and-retry-rs/README.md:7-7](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/009-budget-and-retry-rs/README.md?plain=1#L7)

```text
- `BudgetLimits` — cumulative token-usage stopping limits, plus caps on tool calls and duration
```

- [examples/009-budget-and-retry-rs/README.md:16-23](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/009-budget-and-retry-rs/README.md?plain=1#L16-L23)

```text
| `max_tokens` | Cumulative accounted-token stopping limit; the final provider call may overshoot |
```

  The subsequent paragraph explains accounted usage and the separate max_tokens_per_turn response-output bound.

- [examples/009-budget-and-retry-rs/main.rs:45-56](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/009-budget-and-retry-rs/main.rs#L45-L56)

```text
.max_tokens_per_turn(512)
```

  The first agent combines this output request bound with its separately built 2000-token cumulative budget.

- [meerkat-core/src/agent/state.rs:5004-5010](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L5004-L5010)

```text
let configured_max_tokens = self.config.resolved_max_tokens_per_turn();
```

  The request limit is derived from configured/model output limits, not the remaining cumulative token balance.

- [meerkat-core/src/agent/state.rs:5805-5819](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L5805-L5819)

```text
self.budget.record_turn_usage(turn_usage);
```

  Provider accounting is charged before the exceeded-budget observation, so that completed call can cross the cumulative threshold.

- [meerkat/Cargo.toml:280-283](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/Cargo.toml#L280-L283)

```text
name = "009-budget-and-retry"
path = "../examples/009-budget-and-retry-rs/main.rs"
required-features = ["jsonl-store"]
```

## A10-006: The retry diagram omits jitter, server hints and rate-limit delay selection

**Candidate severity:** low. **Independent verdict:** confirm.

The three configured base delays are correct, but the wording presents them as the actual schedule for every retryable failure.

### Baseline documentation evidence

- [examples/009-budget-and-retry-rs/README.md:22-30](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/009-budget-and-retry-rs/README.md?plain=1#L22-L30)

```text
When the provider returns a typed retryable failure, the configured policy
uses this schedule.
```

- [examples/009-budget-and-retry-rs/README.md:27-30](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/009-budget-and-retry-rs/README.md?plain=1#L27-L30)

```text
Attempt 1 → fail → wait 500ms →
Attempt 2 → fail → wait 1s →
Attempt 3 → fail → wait 2s →
Attempt 4 → give up
```

### Implementation proof

- [meerkat-core/src/retry.rs:248-253](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/retry.rs#L248-L253)

```text
let jitter = 1.0 + (rand_jitter() * 0.2 - 0.1);
```

  Computed backoff applies plus/minus ten percent jitter before its max-delay cap.

- [meerkat-core/src/retry.rs:279-287](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/retry.rs#L279-L287)

```text
select_retry_delay(hint, computed, error.is_rate_limited());
```

  Actual retry scheduling incorporates the server retry hint/rate-limit classification and caps the selected delay by remaining time budget.

- [meerkat-core/src/retry.rs:310-317](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/retry.rs#L310-L317)

```text
Some(h) if h > computed => (h, false),
        _ if is_rate_limited => {
            let floor = Duration::from_secs(30);
```

  Server hints can dominate the computed delay; rate-limit handling can select a thirty-second floor rather than the diagram's sub-second/second waits.

### Independent challenge

The diagram is illustrative policy documentation, not purported captured output; the README correctly says no live provider failure is forced. Its 500 ms, 1 s, 2 s progression and three-retry count match the configured nominal exponential policy. The narrower defect is the unqualified statement that a retryable failure uses those waits: the actual scheduler jitters them and may select a substantially different hint/rate-limit delay. Preserve the illustration and qualify it rather than treating the demo as a failed retry test.

- [examples/009-budget-and-retry-rs/main.rs:69-76](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/009-budget-and-retry-rs/main.rs#L69-L76)

```text
.with_initial_delay(Duration::from_millis(500))
```

  The shown base progression is correct for the policy actually constructed and printed.

- [meerkat-core/src/retry.rs:237-254](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/retry.rs#L237-L254)

```text
let jitter = 1.0 + (rand_jitter() * 0.2 - 0.1);
```

  Computed waits vary by plus/minus ten percent before the max-delay cap.

- [meerkat-core/src/retry.rs:279-287](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/retry.rs#L279-L287)

```text
select_retry_delay(hint, computed, error.is_rate_limited());
```

  Actual scheduling selects using hints/rate-limit classification and then caps the wait by remaining duration.

- [meerkat-core/src/retry.rs:310-319](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/retry.rs#L310-L319)

```text
Some(h) if h > computed => (h, false),
```

  A larger hint wins; otherwise a rate-limited error selects at least 30 seconds before the remaining-duration cap. The floor must not be described as unconditional even when a hint wins.

**Required correction:** Label the diagram as nominal exponential backoff, not exact observed waits. Briefly state that actual waits include plus/minus ten-percent jitter, can be selected from provider retry hints or the rate-limit policy, and can be shortened by the remaining duration budget. Retain the three-retry example and its explicit no-forced-live-failure disclaimer; avoid presenting the 30-second rate-limit floor as unconditional when an overriding hint is selected.

### Correction

Kept the configured three-retry illustration but labeled it nominal exponential backoff before jitter/selection. Added the plus/minus ten-percent computed jitter, provider hints/rate-limit selection, and remaining-duration cap, without making the 30-second floor unconditional. Retained the no-forced-live-failure disclaimer.

- [examples/009-budget-and-retry-rs/README.md:27-29](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/009-budget-and-retry-rs/README.md?plain=1#L27-L29)

```text
For typed retryable provider failures, the configured policy has this nominal
exponential backoff, before jitter and delay selection.
```

- [examples/009-budget-and-retry-rs/README.md:38-40](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/009-budget-and-retry-rs/README.md?plain=1#L38-L40)

```text
The computed backoff includes ±10% jitter. Actual waits may instead be
selected from provider retry hints or the rate-limit policy, and can be
shortened by the remaining duration budget.
```

- [meerkat-core/src/retry.rs:248-253](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/retry.rs#L248-L253)

```text
let jitter = 1.0 + (rand_jitter() * 0.2 - 0.1);
```

- [meerkat-core/src/retry.rs:279-287](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/retry.rs#L279-L287)

```text
Some(remaining) => selected.min(remaining),
```

- [meerkat-core/src/retry.rs:310-319](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/retry.rs#L310-L319)

```text
Some(h) if h > computed => (h, false),
```

### Final independent review

**PASS:** The existing four-attempt illustration is now explicitly nominal rather than observed output or a universal timing promise. The surrounding text covers computed plus/minus ten-percent jitter, provider hints, rate-limit selection, and remaining-duration capping. It keeps the disclaimer that this example prints the policy without forcing a provider failure and does not turn the rate-limit floor into an unconditional rule.

- [examples/009-budget-and-retry-rs/README.md:27-40](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/009-budget-and-retry-rs/README.md?plain=1#L27-L40)

```text
For typed retryable provider failures, the configured policy has this nominal
exponential backoff, before jitter and delay selection.
```

  The diagram retains 500ms/1s/2s while the added paragraph qualifies actual selected waits.

- [examples/009-budget-and-retry-rs/main.rs:69-77](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/009-budget-and-retry-rs/main.rs#L69-L77)

```text
.with_max_retries(3)
```

  The source sets 500ms initial delay and multiplier 2.0, and prints the policy rather than supplying scripted failures.

- [meerkat-core/src/retry.rs:237-254](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/retry.rs#L237-L254)

```text
let jitter = 1.0 + (rand_jitter() * 0.2 - 0.1);
```

  The scheduler computes exponential delay, applies jitter, and caps at max_delay.

- [meerkat-core/src/retry.rs:277-300](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/retry.rs#L277-L300)

```text
Some(remaining) => selected.min(remaining),
```

  The selected wait is capped by remaining duration and the plan records whether it was capped.

- [meerkat-core/src/retry.rs:307-320](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/retry.rs#L307-L320)

```text
Some(h) if h > computed => (h, false),
```

  A larger provider hint takes precedence; the 30-second rate-limit floor is only the subsequent branch.

## A10-007: Scope the eight-hook overview to agent-loop hooks instead of the full HookPoint enum

**Candidate severity:** low. **Independent verdict:** reject.

The listed eight names remain valid; the error is presenting that subset as the entire defined HookPoint vocabulary. Replacing it with an unqualified claim that the standalone example exercises all fourteen would also be wrong.

### Baseline documentation evidence

- [examples/011-hooks-guardrails-rs/README.md:3-4](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/011-hooks-guardrails-rs/README.md?plain=1#L3-L4)

```text
Intercept and control agent behavior at 8 defined hook points.
```

- [examples/011-hooks-guardrails-rs/README.md:8-8](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/011-hooks-guardrails-rs/README.md?plain=1#L8)

```text
- `HookPoint` - 8 interception points in the agent lifecycle
```

- [examples/README.md:97-97](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/README.md?plain=1#L97)

```text
Intercept agent behavior at 8 hook points for audit, filtering, gating
```

### Implementation proof

- [meerkat-core/src/hooks.rs:49-64](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/hooks.rs#L49-L64)

```text
RuntimeInputAccepted,
    RuntimeInputRejected,
    RuntimeInputDeduplicated,
    PeerIngressCommitted,
    PeerEgressCommitted,
    InteractionCompleted,
```

  The canonical HookPoint enum has these six additional runtime/interaction points after the eight listed loop hooks.

### Independent challenge

The candidate treats this example overview as an exhaustive inventory of every HookPoint variant. Its stated subject is interception of the agent lifecycle, and the adjacent runnable source explicitly says agent loop at eight defined points. Those eight listed variants remain exactly the ordinary loop interception set. The six extra variants observe already-committed runtime/interaction facts and are explicitly observe-only; they are not six additional loop guardrail points. Neither the index row nor the README says that runtime observation variants do not exist, or that the demo instantiates every hook. Adding an explicit 'agent-loop' qualifier could be editorially useful, but the alleged numerical/API mismatch is not established in this context.

- [examples/011-hooks-guardrails-rs/main.rs:1-9](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/011-hooks-guardrails-rs/main.rs#L1-L9)

```text
//! Hooks let you observe and guard the agent loop at 8 defined points.
```

  The immediately adjacent example source explicitly defines the intended scope of the eight-point overview.

- [examples/011-hooks-guardrails-rs/README.md:3-22](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/011-hooks-guardrails-rs/README.md?plain=1#L3-L22)

```text
- `HookPoint` - 8 interception points in the agent lifecycle
```

  This is the example's agent-lifecycle overview and lists the eight real loop hooks; it does not expressly inventory all runtime observation points.

- [meerkat-core/src/hooks.rs:49-64](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/hooks.rs#L49-L64)

```text
RuntimeInputAccepted,
```

  The enum indeed contains fourteen variants, but the additional six concern runtime admission, peer commits, and completed interactions.

- [meerkat-core/src/hooks.rs:91-105](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/hooks.rs#L91-L105)

```text
pub fn is_observe_only(self) -> bool {
```

  The owner distinguishes precisely those six additional variants as observations of already-committed/terminal facts, not policy-authoritative interception points.

**Disposition:** Rejected; no correction authorized for this claim.

## A10-008: Compaction retains a bounded number of complete turns, not exactly N message pairs

**Candidate severity:** medium. **Independent verdict:** confirm.

The rule gets both the unit and the guarantee wrong. A complete turn can contain several tool cycles, and pressure/small-history conditions can cause fewer than N turns to remain verbatim.

### Baseline documentation evidence

- [examples/013-context-compaction-rs/README.md:19-23](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/013-context-compaction-rs/README.md?plain=1#L19-L23)

```text
- The N most recent message pairs are preserved
```

### Implementation proof

- [meerkat-session/src/compactor.rs:367-389](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-session/src/compactor.rs#L367-L389)

```text
if matches!(msg, Message::User(u) if u.transcript_role.is_conversational()) {
```

  Retention boundaries are conversational user turns, with attached injected context and all intervening assistant/tool messages, not user/assistant pairs.

- [meerkat-session/src/compactor.rs:398-405](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-session/src/compactor.rs#L398-L405)

```text
.min(turn_starts.len().saturating_sub(1))
```

  The recent-turn budget is an upper bound: at least the oldest live turn must still be summarized.

- [meerkat-session/src/compactor.rs:426-432](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-session/src/compactor.rs#L426-L432)

```text
retain_turn_count -= 1;
```

  The compactor can reduce retained turns further to fit its retained-history byte budget.

### Independent challenge

The high-level flow diagram is appropriately schematic, but the separate Preservation Rules bullet states an unconditional guarantee and the wrong retention unit. The example sets recent_turn_budget to two. The implementation finds conversational user-turn boundaries, retains their full sequences and attached injected context, reserves at least one old live turn for compaction, and can retain still fewer turns under byte pressure. This is not exactly N user/assistant pairs.

- [examples/013-context-compaction-rs/main.rs:47-53](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/013-context-compaction-rs/main.rs#L47-L53)

```text
recent_turn_budget: 2,
```

  This is the actual retention control selected by the runnable example.

- [meerkat-session/src/compactor.rs:367-389](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-session/src/compactor.rs#L367-L389)

```text
if matches!(msg, Message::User(u) if u.transcript_role.is_conversational()) {
```

  Retention units are complete conversational turns, including preceding attached injected-context messages, not isolated message pairs.

- [meerkat-session/src/compactor.rs:391-432](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-session/src/compactor.rs#L391-L432)

```text
.min(turn_starts.len().saturating_sub(1))
```

  The configured count is an upper bound, and the subsequent loop decrements it until retained history fits its byte budget.

**Required correction:** Replace the exact-N message-pair rule with 'up to recent_turn_budget recent complete conversational turns', retaining their tool-call/result structure and attached context. State that fewer turns may be retained to compact actual old content and fit the retained-history budget. Do not recast the introductory schematic as runnable code or expand this correction to other preservation claims.

### Correction

Changed exact-N message pairs to up to recent_turn_budget complete conversational turns, including tool structure and attached injected context. Documented reduced retention to summarize real old content and satisfy the retained-history byte budget; left the schematic and other preservation bullets unchanged.

- [examples/013-context-compaction-rs/README.md:21-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/013-context-compaction-rs/README.md?plain=1#L21-L24)

```text
- Up to `recent_turn_budget` recent complete conversational turns are retained,
  including their tool-call/result structure and attached injected context.
  Fewer turns may be retained to summarize actual old content and fit the
  retained-history byte budget.
```

- [meerkat-session/src/compactor.rs:367-389](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-session/src/compactor.rs#L367-L389)

```text
if matches!(msg, Message::User(u) if u.transcript_role.is_conversational()) {
```

- [meerkat-session/src/compactor.rs:391-432](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-session/src/compactor.rs#L391-L432)

```text
.min(turn_starts.len().saturating_sub(1))
```

- [meerkat-session/src/compactor.rs:426-432](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-session/src/compactor.rs#L426-L432)

```text
retain_turn_count -= 1;
```

### Final independent review

**PASS:** The retention rule now uses an upper bound in complete conversational turns, explicitly retaining their tool structure and attached injected context. It gives both reasons fewer turns may remain: removing real old content and meeting retained-history byte pressure. The schematic and other preservation bullets were not rewritten. The source's retention count and boundary walk support the correction, and the documented target includes both required Cargo features.

- [examples/013-context-compaction-rs/README.md:21-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/013-context-compaction-rs/README.md?plain=1#L21-L24)

```text
- Up to `recent_turn_budget` recent complete conversational turns are retained,
  including their tool-call/result structure and attached injected context.
  Fewer turns may be retained to summarize actual old content and fit the
  retained-history byte budget.
```

- [meerkat-session/src/compactor.rs:367-389](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-session/src/compactor.rs#L367-L389)

```text
if matches!(msg, Message::User(u) if u.transcript_role.is_conversational()) {
```

  The boundary scan excludes summary/injected rows as independent turns and walks backward over attached injected context.

- [meerkat-session/src/compactor.rs:391-432](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-session/src/compactor.rs#L391-L432)

```text
.min(turn_starts.len().saturating_sub(1))
```

  At least one old live turn is left for summarization; the loop further decrements retain_turn_count until retained bytes fit.

- [meerkat/Cargo.toml:295-298](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/Cargo.toml#L295-L298)

```text
required-features = ["jsonl-store", "session-compaction"]
```

  These are precisely the README run command's features for 013-context-compaction.

## A10-009: Persistent semantic memory does not provide default cross-session recall

**Candidate severity:** medium. **Independent verdict:** confirm.

Durability permits recall after resuming the same session, not automatic retrieval from unrelated sessions. Cross-session knowledge would require an explicitly designed host scope/integration rather than merely selecting HnswMemoryStore.

### Baseline documentation evidence

- [examples/014-semantic-memory-rs/README.md:5-6](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/014-semantic-memory-rs/README.md?plain=1#L5-L6)

```text
Production realms can use `HnswMemoryStore` with SQLite
for durable cross-session memory.
```

### Implementation proof

- [examples/014-semantic-memory-rs/main.rs:108-126](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/014-semantic-memory-rs/main.rs#L108-L126)

```text
MemoryIndexScope::for_session(memory_session_id.clone()),
```

  The example indexes against a canonical session scope and constructs its search dispatcher for that same session.

- [meerkat-core/src/memory.rs:441-445](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/memory.rs#L441-L445)

```text
owner: MemoryOwner::canonical_session(session_id),
```

  The standard search scope is owned by a particular session.

- [meerkat-memory/src/hnsw.rs:1526-1528](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-memory/src/hnsw.rs#L1526-L1528)

```text
if !scope.includes(&metadata) {
                    continue;
                }
```

  Persistent HNSW search enforces the selected scope; switching storage implementations does not remove scope filtering.

- [meerkat-memory/src/tool.rs:514-519](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-memory/src/tool.rs#L514-L519)

```text
"{surface} must not advertise '{claim}' recall: memory is session-scoped"
```

  A dedicated regression contract explicitly forbids previous-session/past-session/cross-session recall advertisements for the standard memory tool/skill.

### Independent challenge

Durability and visibility are independent. I checked the actual pre-seeding and dispatcher setup: both bind the same session ID, and HNSW preserves the same scope restriction. A host could deliberately build a different retrieval integration or address another session's store scope, so the supported correction is not 'cross-session retrieval is impossible in Rust'. It is that replacing the demo store with HNSW/SQLite does not grant an agent automatic cross-session memory.

- [examples/014-semantic-memory-rs/main.rs:100-126](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/014-semantic-memory-rs/main.rs#L100-L126)

```text
MemoryIndexScope::for_session(memory_session_id.clone()),
```

  The facts and the later memory_search dispatcher are deliberately scoped to the same resumed session.

- [meerkat-core/src/memory.rs:316-326](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/memory.rs#L316-L326)

```text
metadata.session_id == self.session_id
```

  The canonical owner accepts metadata only from its session.

- [meerkat-memory/src/tool.rs:96-106](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-memory/src/tool.rs#L96-L106)

```text
Self::new(store, MemorySearchScope::for_session(session_id))
```

  The standard tool is bound to that scope, independently of which MemoryStore implementation is supplied.

- [meerkat-memory/src/hnsw.rs:1526-1528](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-memory/src/hnsw.rs#L1526-L1528)

```text
if !scope.includes(&metadata) {
```

  The persistent implementation also enforces the search scope.

**Required correction:** Replace 'durable cross-session memory' with durable session-scoped recall across turns and resumes/restarts of the same session. Keep SimpleMemoryStore described as process-local and HNSW/SQLite as persistent. Do not claim that persistence alone shares indexed history between unrelated sessions, or that a custom host integration is categorically impossible.

### Correction

Replaced automatic durable cross-session recall with persistent session-scoped recall across turns and same-session resumes/restarts. Kept SimpleMemoryStore process-local and HNSW/SQLite persistent without ruling out custom host integrations.

- [examples/014-semantic-memory-rs/README.md:4-8](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/014-semantic-memory-rs/README.md?plain=1#L4-L8)

```text
for durable session-scoped recall across turns and resumes/restarts of the same
session. Persistence does not automatically share indexed history with unrelated
sessions.
```

- [examples/014-semantic-memory-rs/main.rs:100-126](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/014-semantic-memory-rs/main.rs#L100-L126)

```text
MemoryIndexScope::for_session(memory_session_id.clone()),
```

- [meerkat-memory/src/tool.rs:96-106](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-memory/src/tool.rs#L96-L106)

```text
Self::new(store, MemorySearchScope::for_session(session_id))
```

- [meerkat-memory/src/hnsw.rs:1526-1528](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-memory/src/hnsw.rs#L1526-L1528)

```text
if !scope.includes(&metadata) {
```

### Final independent review

**PASS:** The production-store sentence now promises durable recall only within the same session across turns and resumes/restarts. SimpleMemoryStore remains described as process-local. The correction does not forbid a custom host integration, but correctly says persistence alone does not share unrelated sessions. Both indexing and tool construction in the example use the same session ID; the persistent store also applies the session scope. The documented 014 Cargo target and both required features match its manifest.

- [examples/014-semantic-memory-rs/README.md:4-8](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/014-semantic-memory-rs/README.md?plain=1#L4-L8)

```text
for durable session-scoped recall across turns and resumes/restarts of the same
session. Persistence does not automatically share indexed history with unrelated
sessions.
```

- [examples/014-semantic-memory-rs/main.rs:100-126](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/014-semantic-memory-rs/main.rs#L100-L126)

```text
MemoryIndexScope::for_session(memory_session_id.clone()),
```

  The adjacent dispatcher is constructed with for_session and that same memory_session_id.

- [meerkat-memory/src/tool.rs:94-102](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-memory/src/tool.rs#L94-L102)

```text
Self::new(store, MemorySearchScope::for_session(session_id))
```

  The tool's selected owner is session-scoped.

- [meerkat-core/src/memory.rs:324-326](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/memory.rs#L324-L326)

```text
metadata.session_id == self.session_id
```

  Owner inclusion is an equality check, not a realm-wide persistent-memory grant.

- [meerkat-memory/src/hnsw.rs:1526-1528](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-memory/src/hnsw.rs#L1526-L1528)

```text
if !scope.includes(&metadata) {
```

  HNSW search applies the same visibility restriction.

- [meerkat/Cargo.toml:300-303](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/Cargo.toml#L300-L303)

```text
required-features = ["jsonl-store", "memory-store-session"]
```

## A10-010: The generated-file inventory places the demo signing key inside the mob source directory

**Candidate severity:** low. **Independent verdict:** confirm.

The earlier inventory contradicts the actual layout and the later correct .work/release.key note. Keeping signing material outside the packed source tree is a useful distinction, even for this intentional demo key.

### Baseline documentation evidence

- [examples/028-mobpack-release-triage-sh/README.md:32-38](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/README.md?plain=1#L32-L38)

```text
`examples.sh` generates a temporary mob source tree under `.work/release-triage/`
with:
```

- [examples/028-mobpack-release-triage-sh/README.md:38-38](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/README.md?plain=1#L38)

```text
- `release.key` - demo signing key for local verification
```

### Implementation proof

- [examples/028-mobpack-release-triage-sh/examples.sh:4-9](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/examples.sh#L4-L9)

```text
MOB_DIR="$WORK/release-triage"
PACK="$WORK/release-triage.mobpack"
KEY="$WORK/release.key"
```

  The key is a sibling of the mob source directory under .work, not a file inside .work/release-triage.

- [examples/028-mobpack-release-triage-sh/examples.sh:240-241](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/examples.sh#L240-L241)

```text
"$RKAT" mob pack "$MOB_DIR" -o "$PACK" --sign "$KEY" --signer-id "$SIGNER_ID"
```

  The packer receives that outside key separately from the packed directory.

### Independent challenge

The later Notes section already gives the correct key path, so this is only an inconsistent generated-file inventory, not evidence that the implementation packages its signing key. The script puts the source directory, artifact, and key alongside one another under the example's .work directory. Correct the earlier list; do not move the key into the packed tree or change deliberate fixture key bytes.

- [examples/028-mobpack-release-triage-sh/examples.sh:4-9](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/examples.sh#L4-L9)

```text
KEY="$WORK/release.key"
```

  MOB_DIR is $WORK/release-triage and PACK is $WORK/release-triage.mobpack; KEY is outside MOB_DIR.

- [examples/028-mobpack-release-triage-sh/README.md:32-38](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/README.md?plain=1#L32-L38)

```text
- `release.key` - demo signing key for local verification
```

  This bullet is incorrectly included under the explicitly named .work/release-triage source tree.

- [examples/028-mobpack-release-triage-sh/README.md:92-97](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/README.md?plain=1#L92-L97)

```text
- The signing key in `.work/release.key` is for local demonstration only.
```

  The existing later note narrows the required fix to consistency with an already-correct path.

**Required correction:** Remove release.key from the contents listed under .work/release-triage/. Describe .work/release.key and .work/release-triage.mobpack separately as sibling outputs, with paths relative to the example directory. Keep the demonstration-only warning and the key outside the packed source.

### Correction

Removed release.key from the packed source-tree inventory. Listed .work/release.key and .work/release-triage.mobpack as sibling outputs relative to the example directory, leaving the key and script bytes untouched.

- [examples/028-mobpack-release-triage-sh/README.md:32-41](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/028-mobpack-release-triage-sh/README.md?plain=1#L32-L41)

```text
It also writes sibling outputs outside that source tree:
- `.work/release.key` - demo signing key for local verification
- `.work/release-triage.mobpack` - the signed artifact
```

- [examples/028-mobpack-release-triage-sh/examples.sh:4-9](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/028-mobpack-release-triage-sh/examples.sh#L4-L9)

```text
MOB_DIR="$WORK/release-triage"
PACK="$WORK/release-triage.mobpack"
KEY="$WORK/release.key"
```

### Final independent review

**PASS:** The generated-file inventory no longer places a signing key inside the packed source tree. It explicitly anchors paths to the example directory and separately lists both sibling outputs, matching ROOT/WORK/MOB_DIR/PACK/KEY in the script. The later demo-key warning remains consistent. The baseline diff confirms the script and its deliberate demo key bytes were not changed.

- [examples/028-mobpack-release-triage-sh/README.md:32-41](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/028-mobpack-release-triage-sh/README.md?plain=1#L32-L41)

```text
It also writes sibling outputs outside that source tree:
- `.work/release.key` - demo signing key for local verification
- `.work/release-triage.mobpack` - the signed artifact
```

- [examples/028-mobpack-release-triage-sh/examples.sh:4-9](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/028-mobpack-release-triage-sh/examples.sh#L4-L9)

```text
MOB_DIR="$WORK/release-triage"
PACK="$WORK/release-triage.mobpack"
KEY="$WORK/release.key"
```

  All three paths are resolved under the script's own .work directory; the key is not a child of MOB_DIR.

- [examples/028-mobpack-release-triage-sh/README.md:98-101](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/028-mobpack-release-triage-sh/README.md?plain=1#L98-L101)

```text
- The fixed signing key in `.work/release.key` is publicly known, for local
  demonstration only, and unsuitable for production.
```

  The second key-path occurrence agrees with the corrected inventory.

## A10-011: Switching the mobpack demo to strict mode also requires signer trust enrollment

**Candidate severity:** medium. **Independent verdict:** confirm.

Moving the private key and changing the flag is not sufficient. Without registering the matching signer/public key, the recommended stricter flow fails with UnknownSignerStrict.

### Baseline documentation evidence

- [examples/028-mobpack-release-triage-sh/README.md:96-99](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/README.md?plain=1#L96-L99)

```text
- For a stricter production-like flow, switch the deploy step to
  `--trust-policy strict` and manage keys outside the example directory.
```

### Implementation proof

- [examples/028-mobpack-release-triage-sh/examples.sh:8-9](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/examples.sh#L8-L9)

```text
SIGNER_ID="release-triage-demo"
```

  The script signs with a demo signer identity but does not install it in a trust store.

- [meerkat-mob-pack/src/trust.rs:122-125](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-pack/src/trust.rs#L122-L125)

```text
if trust_policy == TrustPolicy::Strict {
        return Err(PackValidationError::UnknownSignerStrict(
```

  Even a cryptographically valid signature is rejected in strict mode if its signer is not trusted.

- [meerkat-cli/src/main.rs:17868-17885](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L17868-L17885)

```text
.join("trusted-signers.toml")
```

  CLI signer trust is loaded from the scope-resolved user/project .rkat/trusted-signers.toml files, independently of where the private key is stored.

- [meerkat-mob-pack/src/trust.rs:105-109](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-pack/src/trust.rs#L105-L109)

```text
if let Some(trusted_key) = trusted_signers.lookup(&pack_signature.signer_id) {
```

  Trust lookup is by signer ID and then checks that the enrolled public key matches the embedded key.

### Independent challenge

The preceding permissive-policy note already acknowledges that trust-store setup has been skipped, so the README is not wholly unaware of enrollment. Nevertheless its prescriptive strict-mode instruction omits the specific condition strict verification requires: the chosen signer ID must resolve to the matching public key. Moving a private key outside the example and selecting strict does not establish that trust. Narrow this to completing the strict-mode prerequisite, not asserting that the deliberately permissive demo is broken.

- [meerkat-mob-pack/src/trust.rs:21-29](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-pack/src/trust.rs#L21-L29)

```text
pub signers: BTreeMap<SignerId, Ed25519PublicKeyHex>,
```

  Enrollment is a signer-ID to public-key mapping, not the location of a signing private key.

- [meerkat-mob-pack/src/trust.rs:105-128](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-pack/src/trust.rs#L105-L128)

```text
return Err(PackValidationError::UnknownSignerStrict(
```

  A valid embedded signature with no enrolled signer still fails strict policy; an enrolled ID with the wrong key also fails.

- [meerkat-cli/src/main.rs:17868-17885](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L17868-L17885)

```text
.join("trusted-signers.toml")
```

  The CLI resolves separate user/project .rkat/trusted-signers.toml paths for trust, independently of the private-key path.

- [examples/028-mobpack-release-triage-sh/examples.sh:235-253](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/028-mobpack-release-triage-sh/examples.sh#L235-L253)

```text
"$RKAT" mob pack "$MOB_DIR" -o "$PACK" --sign "$KEY" --signer-id "$SIGNER_ID"
```

  The script signs with its fixed demonstration identity/key and subsequently validates/deploys permissively; it does not enroll that signer.

**Required correction:** Complete the Notes strict-mode advice: use an appropriate privately managed signing key and signer ID, enroll that ID's matching Ed25519 public key in the effective user/project .rkat/trusted-signers.toml [signers] mapping, and only then validate/deploy under strict policy. State that the public fixed demo key is not suitable for production and that signing a pack does not enroll its signer. Preserve the runnable permissive demo.

### Correction

Completed strict-mode prerequisites: a privately managed non-demo key and signer ID, enrollment of the matching Ed25519 public key in the effective user/project trusted-signers.toml [signers] mapping, then strict validation/deployment. Explicitly distinguished signing from trust enrollment and warned that the fixed public demonstration key is unsuitable for production. Preserved permissive demo execution.

- [examples/028-mobpack-release-triage-sh/README.md:98-107](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/028-mobpack-release-triage-sh/README.md?plain=1#L98-L107)

```text
- Signing a pack does not enroll its signer in a trust store.
- For a stricter production-like flow, use a privately managed signing key
  outside the example directory and choose its signer ID. Enroll that ID's
  matching Ed25519 public key in the `[signers]` mapping of the effective
  user/project `.rkat/trusted-signers.toml` trust store before validating or
  deploying with `--trust-policy strict`.
```

- [meerkat-mob-pack/src/trust.rs:21-29](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-pack/src/trust.rs#L21-L29)

```text
pub signers: BTreeMap<SignerId, Ed25519PublicKeyHex>,
```

- [meerkat-mob-pack/src/trust.rs:105-128](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-pack/src/trust.rs#L105-L128)

```text
return Err(PackValidationError::UnknownSignerStrict(
```

- [meerkat-cli/src/main.rs:17868-17885](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L17868-L17885)

```text
.join("trusted-signers.toml")
```

### Final independent review

**PASS:** Strict-mode guidance now includes every confirmed prerequisite: a privately managed non-demo key, its signer ID, enrollment of the matching Ed25519 public key in the effective user/project [signers] trust mapping, and only then strict validation/deployment. It explicitly separates pack signing from enrollment and preserves the runnable permissive demo. I checked both trust-store path construction and the verifier's key-match/unknown-signer branches; the correction does not claim private-key relocation alone establishes trust.

- [examples/028-mobpack-release-triage-sh/README.md:98-107](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/028-mobpack-release-triage-sh/README.md?plain=1#L98-L107)

```text
- Signing a pack does not enroll its signer in a trust store.
```

  The following complete strict-mode recipe requires the matching public key in [signers], while the preceding notes keep permissive mode and the public demo-key warning.

- [meerkat-mob-pack/src/trust.rs:21-29](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-pack/src/trust.rs#L21-L29)

```text
pub signers: BTreeMap<SignerId, Ed25519PublicKeyHex>,
```

  The trust file's actual schema maps signer IDs to Ed25519 public keys.

- [meerkat-mob-pack/src/trust.rs:39-65](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-pack/src/trust.rs#L39-L65)

```text
merged.signers.insert(signer_id, project_key);
```

  The verifier uses the effective user/project mapping, with project entries taking precedence.

- [meerkat-mob-pack/src/trust.rs:105-128](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-pack/src/trust.rs#L105-L128)

```text
return Err(PackValidationError::UnknownSignerStrict(
```

  A trusted signer must have a matching embedded key; otherwise strict policy rejects even a cryptographically valid unknown signature.

- [meerkat-cli/src/main.rs:17868-17886](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L17868-L17886)

```text
.join("trusted-signers.toml")
```

  Both user and context roots append .rkat/trusted-signers.toml, as the revised note specifies.

- [meerkat-cli/src/main.rs:16086-16103](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L16086-L16103)

```text
println!("{}", execute_mob_pack(dir, output, signing).await?);
```

  Pack dispatch only signs/writes the artifact; trust loading is a separate validation operation at lines 16843-16854.

## A10-012: TUX model selection is applied on the next turn but is not automatically reverted afterward

**Candidate severity:** medium. **Independent verdict:** confirm.

The word 'only' falsely promises a one-turn temporary model change. The TUI clears the pending field, but the RPC session identity changes persistently.

### Baseline documentation evidence

- [examples/035-mdm-tux-rs/README.md:298-298](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md?plain=1#L298)

```text
| `/model <name>` | Set the model override for the next turn only |
```

### Implementation proof

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2329-2343](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2329-L2343)

```text
let model = app.pending_model.take();
```

  The TUI consumes its pending request override once, which is not the same as restoring the server's previous session model.

- [examples/035-mdm-tux-rs/src/bin/tux.rs:516-524](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L516-L524)

```text
params["model"] = Value::String(m);
                        params["provider"] = Value::String(provider.into());
                    }
                    match client.request("turn/start", params).await {
```

  The pending choice is sent as a normal RPC turn/start model/provider reconfiguration.

- [meerkat-rpc/src/session_runtime.rs:26376-26384](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/session_runtime.rs#L26376-L26384)

```text
assert_eq!(info.model, "gpt-5.4");
        assert_eq!(info.provider, "openai");
```

  The RPC regression asserts the overridden identity remains on the session after that turn.

- [meerkat-rpc/src/session_runtime.rs:26417-26427](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/session_runtime.rs#L26417-L26427)

```text
assert_eq!(info.model, "gpt-5.4", "recovered model must match hot-swap");
```

  It further asserts that the hot-swapped model persists through recovery and a later run without another override.

### Independent challenge

The obvious counterargument is valid only for the UI field: pending_model is taken once, so the request option is sent only on the next submission. That does not revert the runtime session identity. The TUI sends ordinary turn/start model/provider overrides, and the RPC regression explicitly verifies their persistence both after the turn and after recovery without another override. 'Next turn only' confuses one-shot request transmission with one-shot model effect.

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2329-2343](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2329-L2343)

```text
let model = app.pending_model.take();
```

  This clears the local pending option but sends it in StartTurn; it is not a server-side revert operation.

- [examples/035-mdm-tux-rs/src/bin/tux.rs:516-524](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L516-L524)

```text
match client.request("turn/start", params).await {
```

  The model/provider values are forwarded to the canonical RPC turn override path.

- [meerkat-rpc/src/session_runtime.rs:26376-26427](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/session_runtime.rs#L26376-L26427)

```text
assert_eq!(info.model, "gpt-5.4", "recovered model must match hot-swap");
```

  The existing regression first reads the hot-swapped identity, discards/unregisters it, recovers without an override, and verifies the same model.

**Required correction:** Change the /model row to say that it stages a model change for the next submitted turn. Explain that a successfully applied change becomes the session's model for subsequent turns until changed again; clearing the pending UI option does not automatically restore the previous model.

### Correction

Described /model as staging a change for the next submission, with a successful change persisting as the session model until another change. Explicitly stated that clearing the pending UI choice does not restore the old model.

- [examples/035-mdm-tux-rs/README.md:298-298](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/README.md?plain=1#L298)

```text
| `/model <name>` | Stage a model change for the next submitted turn |
```

- [examples/035-mdm-tux-rs/README.md:307-309](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/README.md?plain=1#L307-L309)

```text
Once a `/model` change is successfully applied, it remains the session's model
for subsequent turns until changed again. Clearing the pending UI choice does
not restore the previous model.
```

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2329-2343](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/src/bin/tux.rs#L2329-L2343)

```text
let model = app.pending_model.take();
```

- [meerkat-rpc/src/session_runtime.rs:26417-26427](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-rpc/src/session_runtime.rs#L26417-L26427)

```text
assert_eq!(info.model, "gpt-5.4", "recovered model must match hot-swap");
```

### Final independent review

**PASS:** The slash-command row now correctly describes staging for the next submitted turn, and the added paragraph explicitly states that a successfully applied choice remains the session model until changed again. This matches the one-time consumption of the UI's pending field followed by an ordinary turn/start identity override. I inspected production reconfiguration and the existing recovery test rather than treating take() as an automatic model rollback.

- [examples/035-mdm-tux-rs/README.md:298-309](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/README.md?plain=1#L298-L309)

```text
Once a `/model` change is successfully applied, it remains the session's model
for subsequent turns until changed again. Clearing the pending UI choice does
not restore the previous model.
```

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2330-2343](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/src/bin/tux.rs#L2330-L2343)

```text
let model = app.pending_model.take();
```

  The pending selection is consumed for a submitted StartTurn, not paired with a request to restore the old model.

- [examples/035-mdm-tux-rs/src/bin/tux.rs:503-520](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/src/bin/tux.rs#L503-L520)

```text
params["model"] = Value::String(m);
```

  The chosen model/provider is sent through the normal turn/start RPC method.

- [meerkat-rpc/src/session_runtime.rs:5582-5619](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-rpc/src/session_runtime.rs#L5582-L5619)

```text
.reconfigure_session_llm_identity(session_id, request)
```

  Production code reconfigures session identity, not a temporary per-request-only client.

- [meerkat-rpc/src/session_runtime.rs:26417-26427](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-rpc/src/session_runtime.rs#L26417-L26427)

```text
assert_eq!(info.model, "gpt-5.4", "recovered model must match hot-swap");
```

  The existing test source requires the changed identity after recovery without a new override. This review did not execute that test.

## A10-013: TUX steer mode is cooperative work admission, not the interrupt command

**Candidate severity:** medium. **Independent verdict:** confirm.

The parenthetical conflates choosing the mode for subsequent content with interrupting an already-running turn. Particular live-provider steering paths can interrupt provider output, but this is not the general /steer command guarantee.

### Baseline documentation evidence

- [examples/035-mdm-tux-rs/README.md:300-302](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/README.md?plain=1#L300-L302)

```text
| `/steer` | Set handling mode to steer (interrupts current turn) |
```

### Implementation proof

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2501-2506](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2501-L2506)

```text
t.handling_mode = HandlingMode::Steer;
                t.push_notice("mode", "handling mode set to steer");
```

  The slash command only changes local handling mode; it does not send an interrupt request.

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2513-2525](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2513-L2525)

```text
let _ = rpc_tx.send(RpcCommand::Interrupt {
```

  Explicit interruption belongs to the separate /interrupt handler.

- [meerkat-core/src/types.rs:661-675](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/types.rs#L661-L675)

```text
/// `Steer` means inner-loop handling and requests injection at the earliest
/// admissible cooperative boundary, while remaining ordinary work rather than
/// an out-of-band control-plane command.
```

  The canonical HandlingMode contract does not equate steering with cancellation of the current turn.

### Independent challenge

The slash command does not itself send content or an interrupt; it changes the selected target's handling mode. Even read as a description of subsequent content, 'interrupts current turn' is too strong: the canonical meaning is cooperative inner-loop admission, distinct from the explicit interruption control plane. This does not deny that a particular live-provider steering implementation may interrupt provider output; it rejects that as an unconditional /steer guarantee.

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2501-2506](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2501-L2506)

```text
t.handling_mode = HandlingMode::Steer;
```

  The handler updates local state and a notice only.

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2513-2525](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/examples/035-mdm-tux-rs/src/bin/tux.rs#L2513-L2525)

```text
let _ = rpc_tx.send(RpcCommand::Interrupt {
```

  The separate /interrupt handler is the explicit current-turn control action.

- [meerkat-core/src/types.rs:661-675](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/types.rs#L661-L675)

```text
/// admissible cooperative boundary, while remaining ordinary work rather than
```

  HandlingMode::Steer requests inner-loop injection at a cooperative boundary and is expressly not an out-of-band control-plane command.

**Required correction:** Describe /steer as selecting cooperative inner-loop handling for subsequently submitted work, at the earliest admissible boundary. Remove the unconditional current-turn interruption parenthetical and retain /interrupt as the explicit interruption command.

### Correction

Changed /steer to cooperative inner-loop handling of subsequently submitted work at the earliest admissible boundary. Removed the unconditional current-turn interruption claim and retained the separate /interrupt command.

- [examples/035-mdm-tux-rs/README.md:300-302](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/README.md?plain=1#L300-L302)

```text
| `/steer` | Select cooperative inner-loop handling for subsequently submitted work, at the earliest admissible boundary |
```

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2501-2506](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/src/bin/tux.rs#L2501-L2506)

```text
t.handling_mode = HandlingMode::Steer;
```

- [meerkat-core/src/types.rs:661-675](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/types.rs#L661-L675)

```text
/// `Steer` means inner-loop handling and requests injection at the earliest
/// admissible cooperative boundary, while remaining ordinary work rather than
/// an out-of-band control-plane command.
```

### Final independent review

**PASS:** The revised /steer row is scoped to subsequently submitted ordinary work and the earliest admissible cooperative boundary. It no longer promises an immediate current-turn interruption. The distinct /interrupt row is preserved, matching the TUI's different state-selection and RPC-command branches.

- [examples/035-mdm-tux-rs/README.md:300-302](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/README.md?plain=1#L300-L302)

```text
| `/steer` | Select cooperative inner-loop handling for subsequently submitted work, at the earliest admissible boundary |
```

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2501-2506](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/src/bin/tux.rs#L2501-L2506)

```text
t.handling_mode = HandlingMode::Steer;
```

  The slash command changes handling mode; it does not itself send a turn or interrupt.

- [examples/035-mdm-tux-rs/src/bin/tux.rs:2513-2525](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/examples/035-mdm-tux-rs/src/bin/tux.rs#L2513-L2525)

```text
let _ = rpc_tx.send(RpcCommand::Interrupt {
```

  Explicit interruption is a separate slash-command branch.

- [meerkat-core/src/types.rs:661-675](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/types.rs#L661-L675)

```text
/// `Steer` means inner-loop handling and requests injection at the earliest
/// admissible cooperative boundary, while remaining ordinary work rather than
/// an out-of-band control-plane command.
```

  The corrected wording matches the owning typed contract.

## A10-014: The historical core-fixture guide still says the released-realm corpus is absent

**Candidate severity:** low. **Independent verdict:** confirm.

The dated compatibility narrative and historical vector classification need not be modernized. The final paragraph nevertheless makes a false present-state claim about a concrete repository artifact and test blocker. Hash verification does not assert the recovery test passed; it disproves the stated missing-corpus condition.

### Baseline documentation evidence

- [meerkat-core/tests/fixtures/README.md:100-102](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/tests/fixtures/README.md?plain=1#L100-L102)

```text
Until its real `corpus/` directory is populated from the HomeCore released
artifact, this directory remains historical digest-vector coverage only and
the released-realm recovery test fails as an explicit release blocker.
```

### Implementation proof

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import hashlib,json
root=Path('meerkat-runtime/tests/fixtures/v0_8_10_released_realm/corpus')
manifest=json.loads((root/'fixture-manifest.json').read_text())
assert all((root/f['path']).stat().st_size==f['bytes'] and hashlib.sha256((root/f['path']).read_bytes()).hexdigest()==f['sha256'] for f in manifest['files'])
print(f"verified {len(manifest['files'])} released-corpus payloads; sessions={len(manifest['expected']['sessions'])}; snapshots={len(manifest['expected']['runtime_snapshots'])}; consumed_inputs={len(manifest['expected']['consumed_inputs'])}")
PY
```

  verified 16 released-corpus payloads; sessions=1; snapshots=1; consumed_inputs=2

  The referenced corpus is committed and internally hash-consistent, not awaiting population.

- [meerkat-runtime/tests/recovery_contract.rs:2570-2587](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/tests/recovery_contract.rs#L2570-L2587)

```text
.join("tests/fixtures/v0_8_10_released_realm/corpus");
```

  The actual release-boundary test loads this committed directory and treats only absence/unreadability as that specific release blocker.

### Independent challenge

Most of this file is deliberately historical release-boundary documentation and should remain so. The final paragraph is different: it describes a still-missing repository corpus and a current release-blocking absence. The committed corpus now exists and the consuming test explicitly recognizes a synthetic published-rkat producer, not a required HomeCore capture. Independent hashes verify presence/integrity, not that the full recovery test passes or that the corpus proves every crash/pending-input condition in the older historical checklist.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import hashlib,json
root=Path('meerkat-runtime/tests/fixtures/v0_8_10_released_realm/corpus')
m=json.loads((root/'fixture-manifest.json').read_text())
assert all((root/f['path']).stat().st_size==f['bytes'] and hashlib.sha256((root/f['path']).read_bytes()).hexdigest()==f['sha256'] for f in m['files'])
print(f"verified {len(m['files'])} payloads; product={m['producer']['product']}; version={m['producer']['meerkat_version']}; sessions={len(m['expected']['sessions'])}; snapshots={len(m['expected']['runtime_snapshots'])}; consumed_inputs={len(m['expected']['consumed_inputs'])}")
PY
```

  verified 16 payloads; product=rkat; version=0.8.10; sessions=1; snapshots=1; consumed_inputs=2

  This read-only byte check independently disproves the missing-corpus premise without importing or activating any store.

- [meerkat-runtime/tests/recovery_contract.rs:2570-2607](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/tests/recovery_contract.rs#L2570-L2607)

```text
assert_eq!(manifest.producer.product, "rkat");
```

  The current consumer loads the committed corpus and requires the published synthetic rkat fixture, rather than requiring HomeCore deployment state.

**Required correction:** Replace or explicitly mark the final missing-HomeCore-corpus paragraph as superseded. State that the separate published-rkat 0.8.10 synthetic clean-shutdown realm is committed and consumed by recovery_contract.rs, while missing/corrupt corpus bytes remain a test failure. Preserve the historical digest-vector classification, old release discussion, and fixture payloads; do not claim a newly executed recovery pass or broaden this fixture to pending-input/crash evidence.

### Correction

Replaced only the obsolete missing-HomeCore-corpus status with the committed published-rkat 0.8.10 synthetic clean-shutdown corpus and its recovery_contract.rs consumer. Kept missing/corrupt bytes fail-closed and expressly avoided claiming crash/pending-input coverage or a newly executed recovery pass. All preceding historical discussion and fixture bytes were preserved.

- [meerkat-core/tests/fixtures/README.md:100-104](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/tests/fixtures/README.md?plain=1#L100-L104)

```text
The separate `corpus/` now contains a synthetic clean-shutdown realm captured
with the published `rkat` 0.8.10 binary. It is consumed by
`meerkat-runtime/tests/recovery_contract.rs`; missing or corrupt corpus bytes
remain a test failure.
```

- [meerkat-runtime/tests/recovery_contract.rs:2570-2607](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/tests/recovery_contract.rs#L2570-L2607)

```text
assert_eq!(manifest.producer.product, "rkat");
```

- [meerkat-runtime/tests/fixtures/v0_8_10_released_realm/corpus/provenance/capture-receipt.json:15-15](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/tests/fixtures/v0_8_10_released_realm/corpus/provenance/capture-receipt.json#L15)

```text
"clean_shutdown": true,
```

- [meerkat-runtime/tests/recovery_contract.rs:2732-2745](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/tests/recovery_contract.rs#L2732-L2745)

```text
"fixture file digest changed: {}"
```

### Final independent review

**PASS:** Only the obsolete final status paragraph changed; I verified lines 1-99 remain byte-for-byte identical to the audit baseline. The replacement correctly identifies the separate published-rkat 0.8.10 synthetic clean-shutdown corpus and its consumer, retains fail-closed missing/corrupt-byte behavior, and explicitly disclaims crash/pending-input proof. Independently reading the manifest and hashing the files found an exact 16-file inventory with matching lengths, SHA-256 values, and capture-receipt digest. That is file-integrity evidence only, not an executed recovery test.

- [meerkat-core/tests/fixtures/README.md:100-104](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/tests/fixtures/README.md?plain=1#L100-L104)

```text
The separate `corpus/` now contains a synthetic clean-shutdown realm captured
with the published `rkat` 0.8.10 binary.
```

  The remainder of the replacement names recovery_contract.rs and refuses to stretch this into historical-vector or crash-recovery evidence.

- [meerkat-runtime/tests/fixtures/v0_8_10_released_realm/corpus/provenance/capture-receipt.json:2-16](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/tests/fixtures/v0_8_10_released_realm/corpus/provenance/capture-receipt.json#L2-L16)

```text
"clean_shutdown": true,
```

  The same receipt records synthetic_non_production, version 0.8.10, published release provenance, and current_source_build=false.

- [meerkat-runtime/tests/recovery_contract.rs:2560-2607](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/tests/recovery_contract.rs#L2560-L2607)

```text
assert_eq!(manifest.producer.product, "rkat");
```

  The consumer loads this committed corpus and rejects an absent/unreadable manifest instead of generating replacement old bytes.

- [meerkat-runtime/tests/recovery_contract.rs:2732-2760](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/tests/recovery_contract.rs#L2732-L2760)

```text
"fixture file digest changed: {}"
```

  The test checks each file's size/digest and exact inventory before opening a copy. Its passing execution is not claimed.

- [meerkat-runtime/tests/fixtures/v0_8_10_released_realm/corpus/fixture-manifest.json:1-1](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/tests/fixtures/v0_8_10_released_realm/corpus/fixture-manifest.json#L1)

```text
"fixture_id":"meerkat-0.8.10-rkat-released-synthetic"
```

  This committed manifest binds the 16 files independently hash-checked during review; neither it nor any payload changed from the baseline.

## A10-015: The realtime mock README claims e2e-fast consumption that is not wired

**Candidate severity:** medium. **Independent verdict:** confirm.

The mock exists and its own deterministic tests exist, but the README turns intended e2e-fast applicability into a present-tense coverage claim. A maintainer running that lane will not exercise this mock through the advertised composite paths.

### Baseline documentation evidence

- [meerkat-openai/tests/mock_realtime_ws/README.md:3-5](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-openai/tests/mock_realtime_ws/README.md?plain=1#L3-L5)

```text
used by `e2e-fast` tests that need realtime lifecycle coverage without live
OpenAI credentials.
```

### Implementation proof

- [.cargo/config.toml:71-72](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.cargo/config.toml#L71-L72)

```text
e2e-fast = "nextest run -p meerkat-integration-tests --test e2e_fast_lane --no-fail-fast --show-progress none --status-level none --final-status-level fail"
```

  The canonical e2e-fast command selects the integration-test e2e_fast_lane binary, not the OpenAI integration self-test.

- [meerkat-openai/tests/mock_realtime_ws_selftest.rs:6-10](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-openai/tests/mock_realtime_ws_selftest.rs#L6-L10)

```text
#[path = "mock_realtime_ws/mod.rs"]
mod mock_realtime_ws;
```

  The real test entry point including the mock is the realtime-feature-gated OpenAI self-test.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import re
roots=['tests','meerkat-openai','meerkat-mob/tests','meerkat-mob-mcp/tests']
pattern=re.compile(r'mock_realtime_ws|RealtimeMockServer|RealtimeMockSessionFactory|ScriptedScenario')
for p in sorted({p for root in roots for p in Path(root).rglob('*.rs') if p.is_file()}):
    if pattern.search(p.read_text()):
        print(p)
PY
```

  meerkat-openai/tests/mock_realtime_ws/mod.rs
  meerkat-openai/tests/mock_realtime_ws/server.rs
  meerkat-openai/tests/mock_realtime_ws_selftest.rs

  The consumer inventory includes the entire integration-test tree and shared mob support trees that e2e_fast_lane imports, but finds no use there. Together with the authoritative lane selector, this establishes the wiring mismatch rather than merely relying on a failed search.

### Independent challenge

Confirmed only for this particular mock, not for all deterministic realtime coverage. The active e2e-fast binary includes live-plane tests using a different, facade-owned ScriptedRealtimeSessionFactory. The old mock is compiled through its feature-gated OpenAI self-test; the other repository name hits outside that module/self-test are comments about it. Thus the current-use statement is false, but replacing it with 'e2e-fast has no realtime coverage' would introduce a new error. Preserve the dated W1-C motivation as historical context.

- [.cargo/config.toml:71-72](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.cargo/config.toml#L71-L72)

```text
e2e-fast = "nextest run -p meerkat-integration-tests --test e2e_fast_lane --no-fail-fast --show-progress none --status-level none --final-status-level fail"
```

  The canonical lane selects the integration crate's e2e_fast_lane binary, not the OpenAI mock self-test.

- [meerkat-openai/tests/mock_realtime_ws_selftest.rs:6-10](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-openai/tests/mock_realtime_ws_selftest.rs#L6-L10)

```text
#[path = "mock_realtime_ws/mod.rs"]
```

  This actual module inclusion is gated on native targets and the realtime feature.

- Read-only reproduction:

```text
git grep -l -E 'mock_realtime_ws|RealtimeMockServer|RealtimeMockSessionFactory|ScriptedScenario' -- '*.rs'
```

  meerkat-mob/src/runtime/builder.rs
  meerkat-openai/tests/mock_realtime_ws/mod.rs
  meerkat-openai/tests/mock_realtime_ws/server.rs
  meerkat-openai/tests/mock_realtime_ws_selftest.rs
  meerkat/src/test_fixtures/realtime.rs

  All tracked Rust references were inspected: builder.rs and the facade fixture only mention this prior mock in comments; they do not include or re-export it.

- [tests/integration/tests/e2e_fast_lane.rs:13-22](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/integration/tests/e2e_fast_lane.rs#L13-L22)

```text
#[path = "../../../meerkat-mob/tests/support/live_plane.rs"]
```

  The fast lane does have separate live-plane wiring; its presence rules out broadening the finding into absence of all realtime fast coverage.

- [meerkat-mob/tests/support/live_plane.rs:23-34](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/tests/support/live_plane.rs#L23-L34)

```text
pub use meerkat::test_fixtures::realtime::ScriptedRealtimeSessionFactory;
```

  The current fast-lane fixture comes from the facade, not mock_realtime_ws.

**Required correction:** Replace the present-tense assertion that this mock is used by e2e-fast with its current feature-gated self-test usage. Give ./scripts/repo-cargo test -p meerkat-openai --features realtime --test mock_realtime_ws_selftest as the targeted command. Frame W1-C/composite-path integration as the original motivation or potential reuse, not demonstrated current consumption. If describing today's fast realtime coverage, name the separate facade-owned fixture; do not claim that e2e-fast has no realtime tests.

### Correction

Identified the native realtime-feature self-test as this mock's actual consumer and documented the exact targeted repo-cargo command. Framed W1-C/composite-path motivation historically and distinguished today's separate facade-owned ScriptedRealtimeSessionFactory fast-lane coverage. Mock behavior and fixtures were not changed.

- [meerkat-openai/tests/mock_realtime_ws/README.md:3-6](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/tests/mock_realtime_ws/README.md?plain=1#L3-L6)

```text
currently exercised by the native, `realtime`-feature-gated
`mock_realtime_ws_selftest` integration test without live OpenAI credentials.
```

- [meerkat-openai/tests/mock_realtime_ws/README.md:15-26](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/tests/mock_realtime_ws/README.md?plain=1#L15-L26)

```text
Current `e2e-fast` live-plane coverage uses the separate facade-owned
`meerkat::test_fixtures::realtime::ScriptedRealtimeSessionFactory`, composed in
`meerkat-mob/tests/support/live_plane.rs`.
```

- [meerkat-openai/tests/mock_realtime_ws/README.md:26-26](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/tests/mock_realtime_ws/README.md?plain=1#L26)

```text
./scripts/repo-cargo test -p meerkat-openai --features realtime --test mock_realtime_ws_selftest
```

- [meerkat-openai/tests/mock_realtime_ws_selftest.rs:6-10](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/tests/mock_realtime_ws_selftest.rs#L6-L10)

```text
#![cfg(all(not(target_arch = "wasm32"), feature = "realtime"))]
```

- [meerkat-mob/tests/support/live_plane.rs:23-34](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/tests/support/live_plane.rs#L23-L34)

```text
pub use meerkat::test_fixtures::realtime::ScriptedRealtimeSessionFactory;
```

### Final independent review

**PASS:** The README accurately names this mock's current native realtime-feature self-test and supplies the correct package/test/feature command. Its W1-C motivation is explicitly historical; it no longer implies the old mock is wired into e2e-fast or that current e2e-fast has no realtime coverage. I checked the Rust references repository-wide and traced the actual fast-lane root to its distinct facade-owned scripted factory. The added command is source-validated, not reported as executed.

- [meerkat-openai/tests/mock_realtime_ws/README.md:3-20](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/tests/mock_realtime_ws/README.md?plain=1#L3-L20)

```text
currently exercised by the native, `realtime`-feature-gated
`mock_realtime_ws_selftest` integration test without live OpenAI credentials.
```

  The subsequent Original motivation section distinguishes historical intent from the current separate fast-lane fixture.

- [meerkat-openai/tests/mock_realtime_ws/README.md:24-26](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/tests/mock_realtime_ws/README.md?plain=1#L24-L26)

```text
./scripts/repo-cargo test -p meerkat-openai --features realtime --test mock_realtime_ws_selftest
```

- [meerkat-openai/tests/mock_realtime_ws_selftest.rs:6-10](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/tests/mock_realtime_ws_selftest.rs#L6-L10)

```text
#![cfg(all(not(target_arch = "wasm32"), feature = "realtime"))]
```

  This integration target directly includes mock_realtime_ws/mod.rs. Its nested server module contains real deterministic tokio self-tests.

- [meerkat-openai/Cargo.toml:60-64](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/Cargo.toml#L60-L64)

```text
realtime = ["dep:oai-rt-rs", "dep:sha2", "dep:tokio-tungstenite"]
```

  The feature in the exact README command exists; the package auto-discovers the named tests/*.rs integration target.

- [tests/integration/tests/e2e_fast_lane.rs:13-22](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/integration/tests/e2e_fast_lane.rs#L13-L22)

```text
#[path = "../../../meerkat-mob/tests/support/live_plane.rs"]
```

  The authoritative fast-lane binary explicitly composes this support module and cross_host_live_member tests.

- [meerkat-mob/tests/support/live_plane.rs:32-36](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/tests/support/live_plane.rs#L32-L36)

```text
pub use meerkat::test_fixtures::realtime::ScriptedRealtimeSessionFactory;
```

  This is the separate factory identified in the corrected README.

## A10-016: The workspace semver fixture contains forty Finished lines, not thirty-nine

**Candidate severity:** low. **Independent verdict:** confirm.

The mismatch is in the fixture README's count. The historical version numbers and deliberate log bytes are not independently a defect, and this finding does not infer unverifiable capture provenance.

### Baseline documentation evidence

- [scripts/fixtures/semver-breaks/README.md:11-11](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/fixtures/semver-breaks/README.md?plain=1#L11)

```text
39 `Finished` lines.
```

### Implementation proof

- Read-only reproduction:

```text
grep -c '^[[:space:]]*Finished ' scripts/fixtures/semver-breaks/report-workspace-clean-0.8.23.txt
```

  40

  This counts the actual immutable fixture records rather than substituting today's workspace crate count.

- [scripts/test_check_semver_breaks.py:634-646](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/test_check_semver_breaks.py#L634-L646)

```text
self.assertEqual(sorted(parsed.finished_crates), sorted(self.scope.checkable))
```

  The report is an intentionally consumed parser/coverage fixture. Correct its description rather than deleting a record to make the old count true.

### Independent challenge

This is a statement about the checked-in report's contents, not an invitation to modernize historical versions or execute commands captured in the report. Counting the actual Finished records gives forty. The associated test treats the report as immutable parser/coverage input. Only its README description needs correction; deleting a report entry or replacing it with current command output would damage the fixture.

- Read-only reproduction:

```text
grep -c '^[[:space:]]*Finished ' scripts/fixtures/semver-breaks/report-workspace-clean-0.8.23.txt
```

  40

  The count comes directly from the committed report, independently of the current workspace crate count.

- [scripts/test_check_semver_breaks.py:634-646](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/test_check_semver_breaks.py#L634-L646)

```text
self.assertEqual(sorted(parsed.finished_crates), sorted(self.scope.checkable))
```

  The fixture is consumed by a coverage contract; its bytes should not be changed merely to match the stale caption.

**Required correction:** Change '39 Finished lines' to '40 Finished lines' in the workspace-report row. Leave the report files, historical versions, timestamps, captured commands, and unrelated capture-provenance claims unchanged.

### Correction

Changed only the workspace report's stated Finished-line count from 39 to 40. Verified the actual committed report count and kept all three report files, captured commands, versions, timestamps, and provenance prose untouched.

- [scripts/fixtures/semver-breaks/README.md:11-11](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/fixtures/semver-breaks/README.md?plain=1#L11)

```text
40 `Finished` lines.
```

- [scripts/test_check_semver_breaks.py:634-646](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/test_check_semver_breaks.py#L634-L646)

```text
self.assertEqual(sorted(parsed.finished_crates), sorted(self.scope.checkable))
```

### Final independent review

**PASS:** The README change is exactly 39 to 40 in the workspace-report row. Counting the committed report's lines whose trimmed contents begin with 'Finished ' produced 40, from line 12 through line 480. All three report payloads, captured commands, historical versions, and provenance prose remain unchanged from the baseline; I did not rerun captured semver commands or reinterpret the fixture as current output.

- [scripts/fixtures/semver-breaks/README.md:11-11](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/fixtures/semver-breaks/README.md?plain=1#L11)

```text
40 `Finished` lines.
```

  A read-only baseline comparison found this numeric replacement to be the entire README diff.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
p=Path('scripts/fixtures/semver-breaks/report-workspace-clean-0.8.23.txt')
rows=[i for i,l in enumerate(p.read_text().splitlines(),1) if l.lstrip().startswith('Finished ')]
print(f'Finished records: {len(rows)}; first line: {rows[0]}; last line: {rows[-1]}')
PY
```

  Finished records: 40; first line: 12; last line: 480

  This executed command counts committed report records read-only. No report content or captured command was changed.

- [scripts/test_check_semver_breaks.py:634-646](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/test_check_semver_breaks.py#L634-L646)

```text
self.assertEqual(sorted(parsed.finished_crates), sorted(self.scope.checkable))
```

  The owning test consumes the frozen report as coverage/parser evidence, not as commands to re-execute.

## A10-017: The poster drift gate documentation omits its HTML freshness check

**Candidate severity:** low. **Independent verdict:** reject.

Coverage and alphabet content can both pass while the same documented gate fails on stale generated HTML. Its advertised two-check contract is incomplete.

### Baseline documentation evidence

- [scripts/machine-posters/README.md:42-51](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/machine-posters/README.md?plain=1#L42-L51)

```text
Runs two checks against `canonical_machine_schemas()`:
```

### Implementation proof

- [scripts/machine-posters/generate-machine-posters.mjs:953-969](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/machine-posters/generate-machine-posters.mjs#L953-L969)

```text
if (onDisk !== fresh) {
      stale.push(path.relative(repoRoot, outPath));
```

  After coverage and content checks, --check renders each poster and compares it byte-for-byte with committed HTML.

- [scripts/machine-posters/generate-machine-posters.mjs:971-991](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/machine-posters/generate-machine-posters.mjs#L971-L991)

```text
machine-poster --check: FRESHNESS drift detected; the checked-in posters do not
```

  The index is checked too, and stale/missing generated files cause a failing exit with a regeneration instruction.

### Independent challenge

The third implementation check exists, but the alleged count contradiction drops a material qualifier from the README: it says two checks against canonical_machine_schemas(), not that the command performs exactly two total checks. Coverage and alphabet-content validation are precisely those two catalog checks. Byte-for-byte freshness is a separate render/output comparison. The README already instructs regeneration after machine/schema/spec changes and gives the generator command, so no missing runnable prerequisite is established either. Documenting freshness would be an optional completeness improvement, not a proven false contract in the cited wording.

- [scripts/machine-posters/README.md:42-51](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/machine-posters/README.md?plain=1#L42-L51)

```text
Runs two checks against `canonical_machine_schemas()`:
```

  The explicit scope is catalog checks, followed by the accurate coverage and content descriptions.

- [scripts/machine-posters/generate-machine-posters.mjs:924-941](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/machine-posters/generate-machine-posters.mjs#L924-L941)

```text
contentFailures = validateContent(canonicalMachineFns(), alphabetPath);
```

  runCheck performs checkCoverage and validateContent against the canonical machine inventory/alphabet, matching the two described checks.

- [scripts/machine-posters/generate-machine-posters.mjs:953-994](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/machine-posters/generate-machine-posters.mjs#L953-L994)

```text
if (onDisk !== fresh) {
```

  Freshness separately compares generated render bytes with checked-in poster/index HTML; it does not falsify the scoped catalog-check count.

- [scripts/machine-posters/README.md:27-34](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/machine-posters/README.md?plain=1#L27-L34)

```text
Regenerate after machine/schema/spec changes so the posters stay aligned with
```

  The existing instructions already give the regeneration action that the freshness diagnostic requires.

**Disposition:** Rejected; no correction authorized for this claim.

## A10-018: Both smoke-lane numeric examples select a removed scenario ID

**Candidate severity:** medium. **Independent verdict:** confirm.

The normal and prebuilt examples fail selection before executing the intended smoke scenario. The named mob-live-smoke examples in the same section remain valid.

### Baseline documentation evidence

- [tests/live_smoke/README.md:62-62](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/README.md?plain=1#L62)

```text
make e2e-smoke SCENARIO=62
```

- [tests/live_smoke/README.md:73-73](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/README.md?plain=1#L73)

```text
MEERKAT_E2E_EXECUTION_MODE=prebuilt make e2e-smoke SCENARIO=62
```

### Implementation proof

- [tests/integration/src/e2e_lanes.rs:166-241](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/integration/src/e2e_lanes.rs#L166-L241)

```text
scenario(e2e_smoke_s55_rpc_rest_callback_peer_storm_resume, 55);
            scenario(e2e_smoke_s71_live_adapter_channel_lifecycle_rpc_ws, 71);
```

  The authoritative, ordered smoke-entry macro jumps from 55 to 71 and contains no scenario 62; named suites are separate entries.

- [tests/integration/src/e2e_lanes.rs:677-689](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/integration/src/e2e_lanes.rs#L677-L689)

```text
return Err(format!("unknown catalog scenario id {id}"));
```

  The selector requires a registered smoke entry and rejects an unknown numeric scenario rather than treating it as a suite alias.

- [tests/integration/src/e2e_lanes.rs:172-177](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/integration/src/e2e_lanes.rs#L172-L177)

```text
scenario(e2e_smoke_s16_rpc_kitchen_sink, 16);
```

  16 is a concrete currently registered numeric smoke scenario that can replace the stale pedagogical selector.

### Independent challenge

A stale wrapper help example also mentions 62, but it is not an alias implementation. The Make target forwards the selector, and the actual read-only selector script rejects 62 before either execution mode starts. I ran that selector only: 62 exits 2 while the proposed replacement 16 returns a valid filter. There is no need to infer whether or when 62 was historically removed; it is simply invalid in the current authoritative catalog.

- [Makefile:88-90](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Makefile#L88-L90)

```text
@TEST="$(TEST)" SCENARIO="$(SCENARIO)" SUITE="$(SUITE)" scripts/run-build-backend-lane e2e-smoke
```

  Make passes the requested numeric value through rather than mapping 62 to a named suite.

- [scripts/e2e-smoke-lane:150-179](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/e2e-smoke-lane#L150-L179)

```text
selection_args=(--scenario "${SCENARIO}")
```

  The wrapper invokes the shared selector before distinguishing cargo from prebuilt execution, so both documented variants are affected.

- [tests/integration/src/e2e_lanes.rs:169-241](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/integration/src/e2e_lanes.rs#L169-L241)

```text
scenario(e2e_smoke_s16_rpc_kitchen_sink, 16);
```

  The exhaustive smoke-entry macro registers 16, jumps from 55 to 71, and has no 62 entry.

- Read-only reproduction:

```text
scripts/e2e-smoke-filter --scenario 62; printf 'scenario 62 exit=%s\n' "$?"; scripts/e2e-smoke-filter --scenario 16; printf 'scenario 16 exit=%s\n' "$?"
```

  error: unknown e2e-smoke scenario id '62'
  scenario 62 exit=2
  e2e_smoke_s16_
  scenario 16 exit=0

  This executed only the inspected read-only selector, which does not invoke Cargo, build anything, or contact providers.

**Required correction:** Replace SCENARIO=62 with SCENARIO=16 in both the ordinary and prebuilt command blocks in tests/live_smoke/README.md. Preserve the valid TEST and SUITE examples. Do not invent an alias or restore a scenario, and do not change unassigned wrapper files under this verdict.

### Correction

Replaced SCENARIO=62 with registered SCENARIO=16 in both normal and prebuilt Make examples. Preserved the TEST/SUITE examples and all other text; the read-only selector independently rejects 62 and accepts 16. No obsolete scenario or alias was introduced.

- [tests/live_smoke/README.md:62-62](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/README.md?plain=1#L62)

```text
make e2e-smoke SCENARIO=16
```

- [tests/live_smoke/README.md:73-73](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/README.md?plain=1#L73)

```text
MEERKAT_E2E_EXECUTION_MODE=prebuilt make e2e-smoke SCENARIO=16
```

- [tests/integration/src/e2e_lanes.rs:169-177](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/integration/src/e2e_lanes.rs#L169-L177)

```text
scenario(e2e_smoke_s16_rpc_kitchen_sink, 16);
```

### Final independent review

**PASS:** Both required numeric examples now select 16, with the ordinary and prebuilt forms otherwise preserved. I ran only the read-only selector: 16 succeeds with the exact prefix for the registered kitchen-sink test, 62 is rejected, and the retained TEST/SUITE examples both resolve correctly. Source inspection confirms Make forwards the selectors, the lane validates before choosing execution mode, and prebuilt selection reaches the same catalog. No alias was invented and the unassigned stale wrapper help text was not edited.

- [tests/live_smoke/README.md:59-64](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/README.md?plain=1#L59-L64)

```text
make e2e-smoke SCENARIO=16
```

  The ordinary example is corrected; neighboring TEST and SUITE examples are retained.

- [tests/live_smoke/README.md:70-75](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/README.md?plain=1#L70-L75)

```text
MEERKAT_E2E_EXECUTION_MODE=prebuilt make e2e-smoke SCENARIO=16
```

  The separately required prebuilt example is corrected as well.

- [Makefile:88-90](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/Makefile#L88-L90)

```text
SCENARIO="$(SCENARIO)"
```

  The public Make target forwards the selection to run-build-backend-lane, which invokes e2e-smoke-lane.

- [tests/integration/src/e2e_lanes.rs:169-174](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/integration/src/e2e_lanes.rs#L169-L174)

```text
scenario(e2e_smoke_s16_rpc_kitchen_sink, 16);
```

  The smoke test root expands this single catalog into ignored tests; the prefix e2e_smoke_s16_ selects the registered scenario rather than scenario 160.

- [scripts/e2e-smoke-lane:165-190](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/e2e-smoke-lane#L165-L190)

```text
filter="$("${root}/scripts/e2e-smoke-filter" "${selection_args[@]}")"
```

  The same selector validation precedes the cargo/prebuilt mode branches.

- Read-only reproduction:

```text
python3 - <<'PY'
import subprocess
for args in [('--scenario','16'),('--scenario','62'),('--test','e2e_smoke_mob_live_smoke'),('--suite','mob-live-smoke')]:
 p=subprocess.run(['./scripts/e2e-smoke-filter',*args],text=True,capture_output=True)
 print(' '.join(('./scripts/e2e-smoke-filter',*args)), '=>', 'exit',p.returncode, 'stdout='+repr(p.stdout.strip()),'stderr='+repr(p.stderr.strip()))
PY
```

  --scenario 16: exit 0, stdout 'e2e_smoke_s16_'; --scenario 62: exit 2, stderr "error: unknown e2e-smoke scenario id '62'"; --test e2e_smoke_mob_live_smoke and --suite mob-live-smoke: exit 0, stdout 'e2e_smoke_mob_live_smoke'.

  This executed command only reads the catalog; it does not build, initialize a live harness, or call providers.

## A10-019: The browser single-scenario command names a nonexistent BROWSER-RAW-MOB-003

**Candidate severity:** medium. **Independent verdict:** confirm.

Copying the first documented single-scenario command selects no implemented test and throws before scenario execution.

### Baseline documentation evidence

- [tests/live_smoke/browser/README.md:31-31](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/README.md?plain=1#L31)

```text
npm run smoke -- --scenario BROWSER-RAW-MOB-003
```

### Implementation proof

- [tests/live_smoke/browser/app/runner.mjs:356-367](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/app/runner.mjs#L356-L367)

```text
'BROWSER-MOBPACK-SESSION-003': scenarioMobpackSession003,
  'BROWSER-RAW-MOB-004': scenarioRawMob004,
```

  The exhaustive scenario map uses MOBPACK-SESSION for 003 and RAW-MOB only for 004.

- [tests/live_smoke/browser/app/runner.mjs:364-367](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/app/runner.mjs#L364-L367)

```text
assert(unknownIds.length === 0, `unknown scenario ids: ${unknownIds.join(', ')}`);
```

  An unknown ID is rejected; the misspelling does not fall back to another scenario.

### Independent challenge

The README's own Scenarios list already names 003 correctly, so the defect is isolated to the single-scenario command. The runner uses exact IDs and validates them before loading WASM; there is no RAW-MOB-003 alias. The adjacent RAW-MOB-004 command is valid and should remain unchanged.

- [tests/live_smoke/browser/app/runner.mjs:356-367](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/app/runner.mjs#L356-L367)

```text
'BROWSER-MOBPACK-SESSION-003': scenarioMobpackSession003,
```

  The exhaustive map assigns 003 to the mobpack-session scenario and RAW-MOB to 004; the following assertion rejects unknown IDs.

- [tests/live_smoke/browser/harness/run-live-smoke.mjs:19-25](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/harness/run-live-smoke.mjs#L19-L25)

```text
scenarioIds.push(value);
```

  Argument parsing forwards the supplied string unchanged, not a translated or normalized alias.

**Required correction:** Change the first single-scenario command to npm run smoke -- --scenario BROWSER-MOBPACK-SESSION-003. Keep BROWSER-RAW-MOB-004 and the already-correct scenario descriptions.

### Correction

Corrected the single-scenario command to BROWSER-MOBPACK-SESSION-003. Kept BROWSER-RAW-MOB-004 and the existing scenario descriptions; both documented IDs were checked against the exhaustive runner map.

- [tests/live_smoke/browser/README.md:49-50](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/README.md?plain=1#L49-L50)

```text
npm run smoke -- --scenario BROWSER-MOBPACK-SESSION-003
npm run smoke -- --scenario BROWSER-RAW-MOB-004
```

- [tests/live_smoke/browser/app/runner.mjs:356-367](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/app/runner.mjs#L356-L367)

```text
'BROWSER-MOBPACK-SESSION-003': scenarioMobpackSession003,
```

### Final independent review

**PASS:** The first direct single-scenario command now exactly names BROWSER-MOBPACK-SESSION-003; the second still correctly names BROWSER-RAW-MOB-004. I checked both against the complete four-entry runner map and traced npm's -- arguments through the Node harness to window.__liveSmoke.run. The invalid RAW-MOB-003 spelling is absent from the map, and no alias or fixture rewrite was introduced.

- [tests/live_smoke/browser/README.md:49-50](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/README.md?plain=1#L49-L50)

```text
npm run smoke -- --scenario BROWSER-MOBPACK-SESSION-003
npm run smoke -- --scenario BROWSER-RAW-MOB-004
```

- [tests/live_smoke/browser/app/runner.mjs:356-367](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/app/runner.mjs#L356-L367)

```text
'BROWSER-MOBPACK-SESSION-003': scenarioMobpackSession003,
```

  The complete SCENARIOS map also contains RAW-MOB-004 and validates selected IDs before loadRawWasm.

- [tests/live_smoke/browser/package.json:5-8](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/package.json#L5-L8)

```text
"smoke": "node ./harness/run-live-smoke.mjs",
```

  The documented npm command resolves to this actual checked-in script.

- [tests/live_smoke/browser/harness/run-live-smoke.mjs:10-28](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/harness/run-live-smoke.mjs#L10-L28)

```text
scenarioIds.push(value);
```

  The parser accepts repeated --scenario values verbatim and main forwards them to the browser runner.

## A10-020: The direct browser smoke setup omits the generated raw WASM bundle

**Candidate severity:** medium. **Independent verdict:** confirm.

Even with the documented Anthropic key and Playwright installed, the direct Run block is incomplete on a clean checkout: runtime file loading fails because npm install in the harness does not produce sdks/web/wasm. Merely mentioning the serving directory does not establish how to obtain it.

### Baseline documentation evidence

- [tests/live_smoke/browser/README.md:22-25](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/README.md?plain=1#L22-L25)

```text
cd tests/live_smoke/browser
npm install
npx playwright install chromium
npm run smoke
```

- [tests/live_smoke/browser/README.md:43-44](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/README.md?plain=1#L43-L44)

```text
- The harness serves the raw WASM export bundle from
  `MEERKAT_WEB_WASM_OUT_DIR` when set, otherwise `sdks/web/wasm/`.
```

### Implementation proof

- [tests/live_smoke/browser/package.json:5-10](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/package.json#L5-L10)

```text
"smoke": "node ./harness/run-live-smoke.mjs",
```

  The direct npm smoke script starts the runner; there is no prebuild or WASM build lifecycle script.

- [tests/live_smoke/browser/app/runner.mjs:98-101](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/app/runner.mjs#L98-L101)

```text
const module = await import('/runtime/meerkat_web_runtime.js');
```

  Every scenario loads the generated runtime JavaScript and its WASM module.

- [tests/live_smoke/browser/harness/server.mjs:120-124](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/harness/server.mjs#L120-L124)

```text
await serveFile(response, resolveWithin(RUNTIME_DIR, relative));
```

  The server reads existing runtime files and does not build them.

- Read-only reproduction:

```text
git ls-files sdks/web/wasm; git check-ignore sdks/web/wasm/meerkat_web_runtime.js sdks/web/wasm/meerkat_web_runtime_bg.wasm
```

  git ls-files returned no tracked files. git check-ignore returned both sdks/web/wasm/meerkat_web_runtime.js and sdks/web/wasm/meerkat_web_runtime_bg.wasm.

  A fresh checkout does not contain the bundle that the documented direct commands attempt to load.

- [tests/integration/src/e2e_lanes.rs:3552-3560](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/integration/src/e2e_lanes.rs#L3552-L3560)

```text
WEB_RUNTIME_BUILD_IF_STALE,
```

  The canonical Rust harness explicitly bootstraps this missing build before browser execution; direct npm invocation bypasses that bootstrap.

- [sdks/web/package.json:26-30](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/package.json#L26-L30)

```text
"build:wasm": "node ./scripts/build-wasm.mjs",
```

  There is an existing package build entry point that generates the required runtime.

- [sdks/web/scripts/build-wasm.mjs:19-27](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/scripts/build-wasm.mjs#L19-L27)

```text
process.env.RKAT_WASM_PACK_BIN || process.env.WASM_PACK || "wasm-pack";
```

  Building the bundle additionally requires the Rust/WASM toolchain and wasm-pack or a supplied executable, not only Playwright.

### Independent challenge

A user who already supplied a compatible generated bundle can run the debugging harness as documented; that does not make the clean-checkout setup complete. I checked the npm manifest and full direct runner/server: installing Playwright and running smoke neither builds nor downloads the raw runtime. The source imports existing generated JS/WASM from the serving directory, and those artifacts are untracked/ignored. The Notes identify where files are served, not how the preceding Run recipe obtains them. This is a missing build/prebuilt prerequisite, not a request to change fixtures or to claim all smoke entrypoints lack bootstrap.

- [tests/live_smoke/browser/package.json:5-10](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/package.json#L5-L10)

```text
"smoke": "node ./harness/run-live-smoke.mjs",
```

  There is no pre-smoke/build lifecycle or runtime dependency in the browser harness package that produces the bundle.

- [tests/live_smoke/browser/harness/run-live-smoke.mjs:40-65](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/harness/run-live-smoke.mjs#L40-L65)

```text
const server = await startBrowserSmokeServer();
```

  The direct runner starts the server/browser and invokes the page API; it has no raw-runtime build step.

- [tests/live_smoke/browser/harness/server.mjs:120-124](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/harness/server.mjs#L120-L124)

```text
await serveFile(response, resolveWithin(RUNTIME_DIR, relative));
```

  Runtime URLs are served from existing files, not generated on demand.

- [tests/live_smoke/browser/app/runner.mjs:98-102](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/live_smoke/browser/app/runner.mjs#L98-L102)

```text
const module = await import('/runtime/meerkat_web_runtime.js');
```

  The scenario runner loads the generated raw JS and initializes its WASM before executing scenarios.

- Read-only reproduction:

```text
git ls-files sdks/web/wasm; git check-ignore sdks/web/wasm/meerkat_web_runtime.js sdks/web/wasm/meerkat_web_runtime_bg.wasm
```

  The first command prints no tracked files. The second prints:
  sdks/web/wasm/meerkat_web_runtime.js
  sdks/web/wasm/meerkat_web_runtime_bg.wasm

  A fresh checkout does not carry the generated files that the direct Run block needs.

- [sdks/web/package.json:26-30](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/package.json#L26-L30)

```text
"build:wasm": "node ./scripts/build-wasm.mjs",
```

  The existing raw-bundle builder provides the narrowly appropriate setup command; the TypeScript wrapper build is not needed for these raw exports.

- [sdks/web/scripts/build-wasm.mjs:19-27](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/scripts/build-wasm.mjs#L19-L27)

```text
process.env.RKAT_WASM_PACK_BIN || process.env.WASM_PACK || "wasm-pack";
```

  The build requires the Rust/WASM toolchain and wasm-pack (or the documented executable override), and identifies the generated JS/WASM outputs.

**Required correction:** Before the browser-local Run block, require a compatible prebuilt raw bundle or show the repo-root command npm --prefix sdks/web run build:wasm, with Rust/wasm32-unknown-unknown and wasm-pack prerequisites. Then explicitly change into tests/live_smoke/browser for its existing npm/Playwright commands. Explain that a supplied bundle may instead be selected with an absolute MEERKAT_WEB_WASM_OUT_DIR inherited by the harness, and that npm run smoke does not build it. Keep the existing API-key requirement and canonical Rust-harness versus direct-debugging distinction; do not execute or rewrite signing fixtures for this correction.

### Correction

Added the missing raw WASM prerequisite, required JS/WASM filenames, Rust wasm32-unknown-unknown and wasm-pack tooling, and the repository-root npm --prefix sdks/web run build:wasm command. Documented an absolute inherited MEERKAT_WEB_WASM_OUT_DIR for compatible prebuilt bundles, explicitly stated npm smoke does not build the runtime, and preserved Anthropic credentials and canonical Rust-lane versus direct-debugging guidance.

- [tests/live_smoke/browser/README.md:24-31](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/README.md?plain=1#L24-L31)

```text
Before the direct run, provide a compatible raw WASM export bundle containing
`meerkat_web_runtime.js` and `meerkat_web_runtime_bg.wasm`. To build it, have
the Rust toolchain with the `wasm32-unknown-unknown` target and `wasm-pack`
available (or set `RKAT_WASM_PACK_BIN` / `WASM_PACK` to a wasm-pack executable):
```

- [tests/live_smoke/browser/README.md:31-40](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/README.md?plain=1#L31-L40)

```text
Alternatively, export an absolute `MEERKAT_WEB_WASM_OUT_DIR` pointing to a
compatible prebuilt bundle, and keep that export in the harness's environment.
`npm run smoke` serves existing runtime files; it does not build the bundle.
```

- [sdks/web/package.json:26-30](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/package.json#L26-L30)

```text
"build:wasm": "node ./scripts/build-wasm.mjs",
```

- [sdks/web/scripts/build-wasm.mjs:19-27](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/scripts/build-wasm.mjs#L19-L27)

```text
process.env.RKAT_WASM_PACK_BIN || process.env.WASM_PACK || "wasm-pack";
```

- [tests/live_smoke/browser/harness/server.mjs:120-124](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/harness/server.mjs#L120-L124)

```text
await serveFile(response, resolveWithin(RUNTIME_DIR, relative));
```

### Final independent review

**PASS:** The clean-checkout direct-debugging recipe now supplies the missing raw-bundle prerequisite before changing into the browser harness. The exact npm --prefix sdks/web run build:wasm command resolves to a Node-builtin-only build script, so it does not need an omitted SDK npm install merely to start; the documented Rust wasm target and wasm-pack prerequisites match that script. Its default output and both executable override names are correct. The absolute, inherited MEERKAT_WEB_WASM_OUT_DIR alternative matches server path resolution. npm smoke remains accurately described as consuming, not building, the bundle, while the Rust-owned lanes really have browser setup precommands. Existing Anthropic credentials and signing-fixture instructions were preserved, not executed.

- [tests/live_smoke/browser/README.md:21-43](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/README.md?plain=1#L21-L43)

```text
npm --prefix sdks/web run build:wasm
```

  The preceding prose names Rust/wasm32/wasm-pack; the following alternative requires an absolute inherited output directory, explicitly says npm smoke does not build, and then changes into the harness for npm/Playwright setup.

- [tests/live_smoke/browser/README.md:61-66](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/README.md?plain=1#L61-L66)

```text
- `ANTHROPIC_API_KEY` is required for the current browser matrix.
```

  The live-provider prerequisite remains present; this review does not claim a live browser run.

- [sdks/web/package.json:26-30](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/package.json#L26-L30)

```text
"build:wasm": "node ./scripts/build-wasm.mjs",
```

  The documented repository-root npm command exists and does not select the unrelated TypeScript build.

- [sdks/web/scripts/build-wasm.mjs:9-27](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/scripts/build-wasm.mjs#L9-L27)

```text
process.env.RKAT_WASM_PACK_BIN || process.env.WASM_PACK || "wasm-pack";
```

  The script resolves the output under sdks/web/wasm unless overridden and defines the required JS/WASM files. Its imports at lines 1-7 are all Node builtins.

- [sdks/web/scripts/build-wasm.mjs:354-373](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/scripts/build-wasm.mjs#L354-L373)

```text
"--target",
          "web",
          "--out-dir",
          OUT_DIR,
```

  The command builds the actual runtime crate as the raw web bundle and supplies the wasm rustflags itself.

- [tests/live_smoke/browser/harness/server.mjs:12-14](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/harness/server.mjs#L12-L14)

```text
process.env.MEERKAT_WEB_WASM_OUT_DIR || path.resolve(ROOT, '..', '..', '..', 'sdks', 'web', 'wasm'),
```

  An absolute override avoids dependence on the harness's changed current directory; the default is the same SDK output.

- [tests/live_smoke/browser/harness/server.mjs:120-124](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/live_smoke/browser/harness/server.mjs#L120-L124)

```text
await serveFile(response, resolveWithin(RUNTIME_DIR, relative));
```

  The server reads existing runtime files instead of building them. The full direct runner likewise contains no runtime build step.

- [tests/integration/src/e2e_lanes.rs:3543-3573](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/tests/integration/src/e2e_lanes.rs#L3543-L3573)

```text
WEB_RUNTIME_BUILD_IF_STALE,
```

  The Rust browser specs for 45-48 include npm prerequisites, runtime build-if-stale, and Playwright Chromium installation, supporting the new canonical-lane distinction.

## Lane limitations

- **Audit:** This is the first-wave audit only. Candidates have not been independently adjudicated or implemented; no documentation or product behavior was changed.
- **Audit:** No Cargo builds/tests, provider calls, server/browser/Docker/tmux execution, package installations, fixture mint/import commands, commits or nested agents were run. Runtime findings are established from source contracts and existing test assertions, not newly executed end-to-end tests.
- **Audit:** All 41 owned-file SHA-256 values matched manifest-A10.json during the audit, and git status --short was empty before the external audit artifact was written.
- **Audit:** Read-only fixture checks used byte hashing and SQLite mode=ro&immutable=1. They establish fixture integrity and recorded facts, not that current migration/recovery tests pass or that old release provenance was independently re-downloaded.
- **Audit:** Historical release boundaries, old semver output and phase-audit comments were intentionally not modernized. The 35-example count is correct despite absent 016 and 020; workgraph's internal sqlite_sequence was not confused with a fifth domain table.
- **Audit:** Relative Markdown-link targets in the assigned files were checked and no missing targets were found. External URLs, host-specific services and all possible ambient user configurations were not executed or exhaustively validated.
- **Challenge:** Independently adjudicated all 20 candidates: 18 confirmed (several explicitly narrowed) and 2 rejected (A10-007 and A10-017). No additional findings were added to the accepted set.
- **Challenge:** The repository HEAD was 56208b9e6cee078f52c43af8f6b36660bc93eeb4. All 41 manifest-owned document hashes matched manifest-A10.json during inspection. Numbered example directories, Cargo target ownership/dependencies, relevant adjacent runnable sources, wrapper arguments, and README context were checked.
- **Challenge:** No builds, installations, provider calls, browser/server runs, fixture imports, commits, repository edits, or nested agents were performed. Dynamic verification was limited to the inspected read-only smoke selector; the other behavior claims are source/test-contract evidence, not newly executed integration-test results.
- **Challenge:** Released-corpus hashing proves the committed payloads' presence and internal integrity, not independent download provenance, a passing current recovery suite, or coverage of absent crash/pending-input scenarios. Captured reports and fixture payloads were treated as data rather than instructions and must remain unchanged.
- **Challenge:** Some adjacent source comments and wrapper help text repeat claims found in owned documentation. They were inspected as context, not silently added as unassigned fix files. Confirmed fixes are restricted to the listed manifest-owned documents; no generator or sync-owner exception is required.
