# A03: Platform and exact CLI reference skills

This ledger preserves the baseline evidence and independent disposition. See [the audit index](README.md) for scope, commits, and publication boundaries.

## A03-001: Current Gemini default and recommendation still name 3.5 instead of 3.8

**Candidate severity:** low. **Independent verdict:** confirm.

These are explicitly current/default claims, not merely examples using an older supported model. They disagree with both independent catalog policies: the default ID and recommendation tier.

### Baseline documentation evidence

- [.claude/skills/meerkat-cli-reference/SKILL.md:163-171](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L163-L171)

```text
current recommended Gemini model is `gemini-3.5-flash`
```

- [.claude/skills/meerkat-platform/SKILL.md:257-261](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L257-L261)

```text
Anthropic `claude-opus-5`, and Gemini `gemini-3.5-flash`.
```

### Implementation proof

- [meerkat-models/src/catalog.rs:33-44](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/catalog.rs#L33-L44)

```text
const DEFAULT_GEMINI: &str = "gemini-3.8-flash";
```

  The canonical per-provider default table selects Gemini 3.8 Flash.

- [meerkat-models/src/capabilities/gemini.rs:23-29](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/capabilities/gemini.rs#L23-L29)

```text
tier: ModelTier::Recommended,
```

  This is the gemini-3.8-flash row. The 3.5 row at lines 124-128 is Supported, not Recommended.

### Independent challenge

The default and recommendation are independently owned facts, and both now select Gemini 3.8 Flash. I checked whether the older model had become invalid: it has not; Gemini 3.5 Flash remains supported. Confirm only the falsely current default/recommendation statements, not every example using 3.5.

- [meerkat-models/src/catalog.rs:33-44](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/catalog.rs#L33-L44)

```text
const DEFAULT_GEMINI: &str = "gemini-3.8-flash";
```

  The actual default-model table consumes this constant; this is product policy rather than a neighboring document's recommendation.

- [meerkat-models/src/capabilities/gemini.rs:23-29](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/capabilities/gemini.rs#L23-L29)

```text
tier: ModelTier::Recommended,
```

  This is the capability row for gemini-3.8-flash. The separately inspected gemini-3.5-flash row is Supported, so retaining deliberate 3.5 examples is legitimate.

**Required correction:** Change the CLI default-model row and platform Gemini recommendation to gemini-3.8-flash. Explicitly distinguish current default/recommended selection from supported alternatives; do not mechanically replace valid gemini-3.5-flash examples.

### Correction

Both current Gemini default/recommendation statements now identify gemini-3.8-flash and explicitly retain gemini-3.5-flash as supported, without replacing unrelated supported-model examples.

- [.claude/skills/meerkat-cli-reference/SKILL.md:169-169](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L169)

```text
current default/recommended Gemini model is `gemini-3.8-flash`; `gemini-3.5-flash` remains a supported alternative
```

- [.claude/skills/meerkat-platform/SKILL.md:268-269](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L268-L269)

```text
Anthropic `claude-opus-5`, and Gemini `gemini-3.8-flash` (default and
```

### Final independent review

**PASS:** Both current-default statements now distinguish Gemini 3.8 Flash's product default and Recommended tier from still-supported 3.5. No mass replacement of older valid model examples.

- [.claude/skills/meerkat-cli-reference/SKILL.md:167-170](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L167-L170)

```text
current default/recommended Gemini model is `gemini-3.8-flash`
```

- [.claude/skills/meerkat-platform/SKILL.md:266-269](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L266-L269)

```text
recommended; `gemini-3.5-flash` remains supported
```

- [meerkat-models/src/catalog.rs:33-44](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-models/src/catalog.rs#L33-L44)

```text
const DEFAULT_GEMINI: &str = "gemini-3.8-flash";
```

- [meerkat-models/src/capabilities/gemini.rs:23-29](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-models/src/capabilities/gemini.rs#L23-L29)

```text
tier: ModelTier::Recommended,
```

## A03-002: Facade feature inventory omits default Copilot and the public Live feature

**Candidate severity:** medium. **Independent verdict:** confirm.

Embedders using this section as the exact dependency/feature contract receive an incomplete description of default backend composition and cannot discover the actual feature required for public OpenAI Live. Copilot remains a backend, not a fourth Provider enum variant.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:866-875](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L866-L875)

```text
The `meerkat` facade crate defaults to the Anthropic, OpenAI, and Gemini
provider features.
```

- [.claude/skills/meerkat-platform/SKILL.md:888-888](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L888)

```text
Available facade features: `anthropic`, `openai`, `openai-realtime`, `gemini`, `all-providers`, `native-keyring`, `jsonl-store`, `memory-store`, `sqlite-store`, `session-store`, `session-compaction`, `memory-store-session`, `atif`, `comms`, `mcp`, `skills`, `schedule`, `workgraph`, `live`, and `live-webrtc`.
```

### Implementation proof

- [meerkat/Cargo.toml:132-134](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/Cargo.toml#L132-L134)

```text
default = ["anthropic", "openai", "gemini", "copilot"]
```

  Copilot is an enabled default feature, not an opt-in inferred from the three provider names.

- [meerkat/Cargo.toml:144-163](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/Cargo.toml#L144-L163)

```text
openai-live = ["openai", "live-webrtc", "session-store", "meerkat-openai/live"]
```

  The public GPT Live path has its own off-by-default feature; the manifest also separately declares experimental GPT Live gates.

- [meerkat/Cargo.toml:164-175](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/Cargo.toml#L164-L175)

```text
all-providers = ["anthropic", "openai", "gemini", "copilot"]
```

  The Copilot feature additionally wires OAuth and the three provider-specific Copilot backends.

### Independent challenge

The complete facade feature manifest contradicts the advertised exhaustive inventory. The three-provider vocabulary itself is still correct: Copilot is a backend/auth feature for those providers, not a fourth Provider enum value. Public openai-live is a separate, non-default feature and must not be conflated with the experimental/private GPT Live gates.

- [meerkat/Cargo.toml:132-134](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/Cargo.toml#L132-L134)

```text
default = ["anthropic", "openai", "gemini", "copilot"]
```

  Default feature composition includes Copilot; a dependencies example using default features does not exclude it.

- [meerkat/Cargo.toml:144-175](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/Cargo.toml#L144-L175)

```text
openai-live = ["openai", "live-webrtc", "session-store", "meerkat-openai/live"]
```

  The public Live feature exists independently of the experimental flags. The same manifest also puts copilot in all-providers and defines its provider/auth dependencies.

**Required correction:** Correct the facade default-features description and add copilot plus public openai-live to the feature inventory. Describe Copilot as the optional/default-enabled backend/auth integration across the three provider identities, and openai-live as opt-in public GPT Live with its WebRTC/session-store dependencies. Preserve distinctions from experimental-openai-gpt-live and private harness features.

### Correction

Corrected the default feature set to the three provider families plus Copilot backend/auth integration. Added public openai-live and its OpenAI/WebRTC/session-store/provider-live dependencies, and kept the actual deprecated experimental-gpt-live and private gate0 harness features separate.

- [.claude/skills/meerkat-platform/SKILL.md:971-977](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L971-L977)

```text
provider features plus the `copilot` backend/auth integration for those same
```

- [.claude/skills/meerkat-platform/SKILL.md:995-1006](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L995-L1006)

```text
`experimental-gpt-live-gate0-harness` separate from that public feature.
```

### Final independent review

**PASS:** The default dependency example, prose, all-providers caveat and feature inventory agree with the facade manifest. Copilot remains integration across three providers; public openai-live is opt-in and distinguished from the actual experimental-gpt-live/private harness names.

- [.claude/skills/meerkat-platform/SKILL.md:973-1008](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L973-L1008)

```text
`all-providers` includes `copilot`; disable defaults
```

- [meerkat/Cargo.toml:132-175](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/Cargo.toml#L132-L175)

```text
default = ["anthropic", "openai", "gemini", "copilot"]
```

- [meerkat/Cargo.toml:145-164](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/Cargo.toml#L145-L164)

```text
openai-live = ["openai", "live-webrtc", "session-store", "meerkat-openai/live"]
```

## A03-003: Storage guidance omits the supported explicit pre-0.8.10 SQLite bridge

**Candidate severity:** medium. **Independent verdict:** confirm.

The blanket instruction to obtain an older binary is no longer universally true. It hides an implemented operator recovery path and leaves the purported exact CLI syntax incomplete.

### Baseline documentation evidence

- [.claude/skills/meerkat-cli-reference/SKILL.md:413-415](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L413-L415)

```text
rkat storage migrate [--apply] [--json] [--root <PATH>]... [--adopt-root <PATH>] [--fence-wait-secs <SECS>]
```

- [.claude/skills/meerkat-cli-reference/SKILL.md:449-452](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L449-L452)

```text
compatibility floor is Meerkat 0.8.10; state from older releases must be
migrated offline with the older binary before repinning.
```

- [.claude/skills/meerkat-platform/SKILL.md:124-129](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L124-L129)

```text
Older state must be upgraded with the older
binary before repinning.
```

### Implementation proof

- [meerkat-cli/src/main.rs:2203-2225](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L2203-L2225)

```text
bridge_pre_0_8_10: bool,
```

  Clap declares --bridge-pre-0-8-10 with requires = "apply" and documents it as an explicit recovery path for exact authenticated pre-floor schemas.

- [meerkat-cli/src/storage_migrate.rs:481-493](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/storage_migrate.rs#L481-L493)

```text
if bridge_pre_0_8_10 && backend != "sqlite" {
```

  The exception is intentionally SQLite-only; it is not permission to open arbitrary old stores.

- [meerkat-cli/src/storage_migrate.rs:612-633](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/storage_migrate.rs#L612-L633)

```text
meerkat::bridge_pre_0_8_10_realm_storage_in(&bridge_root, &bridge_realm, &fence);
```

  The handler actually invokes the authenticated bridge under the maintenance fence before normal migration.

### Independent challenge

The historical 0.8.10 ordinary-open floor remains real, but it no longer implies that every older SQLite realm requires an older executable. I traced the actual Clap flag into the exclusive-fence migration path and its backend/refusal checks. Git commit c9a32ef07603374f136d5d3af341ac8b2f0e2e20 introduced this later explicit recovery route; that is a current exception, not grounds to rewrite historical migration guidance.

- [meerkat-cli/src/main.rs:2203-2225](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L2203-L2225)

```text
bridge_pre_0_8_10: bool,
```

  The actual Clap declaration exposes --bridge-pre-0-8-10 and requires --apply.

- [meerkat-cli/src/storage_migrate.rs:481-493](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/storage_migrate.rs#L481-L493)

```text
if bridge_pre_0_8_10 && backend != "sqlite" {
```

  This is deliberately not a generic importer for jsonl, memory, or arbitrary legacy layouts.

- [meerkat-cli/src/storage_migrate.rs:612-633](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/storage_migrate.rs#L612-L633)

```text
meerkat::bridge_pre_0_8_10_realm_storage_in(&bridge_root, &bridge_realm, &fence);
```

  The handler actually calls the bridge before ordinary strict migration, under the maintenance fence.

- [meerkat/src/persistence.rs:776-790](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/persistence.rs#L776-L790)

```text
Ordinary realm
/// opens remain strict and never invoke this bridge.
```

  The implementation contract preserves strict ordinary opens and authenticates each supported domain against its owning migration manifest.

**Required correction:** Add --bridge-pre-0-8-10 to the storage migrate command contract and document rkat storage migrate --apply --bridge-pre-0-8-10 as the explicit offline recovery path for exact supported pre-floor SQLite schemas. Replace the unconditional older-binary requirement with this qualified exception. Keep ordinary-open floor, unknown/malformed/future-state refusal, per-domain reporting, and the possibility of refused legacy records or partial domain progress explicit; do not promise arbitrary-vintage recovery.

### Correction

Added the bridge flag to migration command summaries and documented the apply-only, SQLite-only authenticated pre-floor bridge. Preserved the ordinary-open 0.8.10 floor, exact-schema admission, refusal cases, per-domain reporting, possible legacy-record refusal and partial domain progress, and older-binary migration for other old state.

- [.claude/skills/meerkat-cli-reference/SKILL.md:452-463](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L452-L463)

```text
recovery route is `rkat storage migrate --apply --bridge-pre-0-8-10`. It runs
```

- [.claude/skills/meerkat-platform/SKILL.md:124-137](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L124-L137)

```text
Inspect the per-domain report: legacy records may be refused and earlier
```

- [.claude/skills/meerkat-platform/references/api_reference.md:85-85](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L85)

```text
rkat storage migrate [--apply] [--bridge-pre-0-8-10] [--adopt-root PATH]
```

### Final independent review

**PASS:** CLI syntax, main storage discussion and API summary all expose the real pre-floor bridge. Apply-only and SQLite admission, exclusive fencing, the ordinary-open floor, exact-schema refusal, per-domain progress and refused records remain explicit. The useful older-binary alternative is qualified rather than erased.

- [.claude/skills/meerkat-cli-reference/SKILL.md:452-466](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L452-L466)

```text
`--bridge-pre-0-8-10` requires `--apply` and a realm pinned to `sqlite`.
```

- [.claude/skills/meerkat-platform/SKILL.md:124-137](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L124-L137)

```text
`rkat storage migrate --apply --bridge-pre-0-8-10`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:83-86](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L83-L86)

```text
rkat storage migrate [--apply] [--bridge-pre-0-8-10]
```

- [meerkat-cli/src/main.rs:2215-2226](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L2215-L2226)

```text
#[arg(long, requires = "apply")]
```

- [meerkat-cli/src/storage_migrate.rs:481-493](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/storage_migrate.rs#L481-L493)

```text
if bridge_pre_0_8_10 && backend != "sqlite"
```

- [meerkat/src/persistence.rs:776-790](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/persistence.rs#L776-L790)

```text
Ordinary realm
```

- [meerkat-cli/src/storage_migrate.rs:612-650](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/storage_migrate.rs#L612-L650)

```text
Every domain that committed is reported even when a later domain
```

## A03-004: Offline migration description incorrectly includes per-mob databases

**Candidate severity:** medium. **Independent verdict:** confirm.

An operator cannot infer from a successful --apply that all mob databases have already migrated. This exception is distinct from the compatibility-floor/bridge issue.

### Baseline documentation evidence

- [.claude/skills/meerkat-cli-reference/SKILL.md:438-445](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L438-L445)

```text
every store opens through its
normal constructor so guarded schema-ledger migrations converge files of any
vintage
```

### Implementation proof

- [meerkat-cli/src/main.rs:2196-2203](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L2196-L2203)

```text
Per-mob databases under
    /// `mobs/` are report-only in v1.
```

  The command's own contract explicitly excludes these databases from offline convergence.

- [meerkat-cli/src/storage_migrate.rs:418-452](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/storage_migrate.rs#L418-L452)

```text
LedgerBaselineAction::ReportOnly
```

  mob_report_only_entries reads ledger versions and emits ReportOnly entries; its note says the owning mob store converges each file on next open.

### Independent challenge

The migration command does not implement the claimed every-store convergence. This is not merely a stale help comment: the handler reads each per-mob ledger and produces ReportOnly entries rather than opening those stores for migration. I also checked the explicit pre-floor bridge's fixed domain inventory; it does not supply a hidden per-mob migration path.

- [meerkat-cli/src/main.rs:2196-2203](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L2196-L2203)

```text
Per-mob databases under
    /// `mobs/` are report-only in v1.
```

  The command's own declared contract excludes per-mob databases from offline convergence.

- [meerkat-cli/src/storage_migrate.rs:418-452](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/storage_migrate.rs#L418-L452)

```text
LedgerBaselineAction::ReportOnly
```

  The production report path records ledger information and delegates convergence to the owning mob store's next open; --apply does not turn these entries into migrations.

**Required correction:** Replace 'every store' and 'files of any vintage' with supported realm-store schema convergence. Explicitly state that per-mob databases under mobs/ are report-only in this command and converge through their owning store on open. Keep the explicit, schema-authenticated pre-floor bridge separate from any blanket vintage guarantee.

### Correction

Replaced every-store/arbitrary-vintage convergence with supported realm-store schema convergence. Explicitly identified per-mob databases as report-only during the offline sweep and converged by their owning store on a subsequent supported open.

- [.claude/skills/meerkat-cli-reference/SKILL.md:441-450](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L441-L450)

```text
`mobs/` are report-only in this command; their owning store converges supported
```

- [.claude/skills/meerkat-platform/SKILL.md:135-137](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L135-L137)

```text
`mobs/` are report-only in the offline sweep and converge through their owning
```

### Final independent review

**PASS:** The sweeping every-store/any-vintage claim is replaced with supported realm-store convergence, while both affected storage descriptions explicitly preserve the per-mob report-only exception and owning-store-on-open convergence.

- [.claude/skills/meerkat-cli-reference/SKILL.md:441-450](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L441-L450)

```text
Per-mob databases under
```

- [.claude/skills/meerkat-platform/SKILL.md:134-137](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L134-L137)

```text
`mobs/` are report-only in the offline sweep
```

- [meerkat-cli/src/storage_migrate.rs:418-452](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/storage_migrate.rs#L418-L452)

```text
LedgerBaselineAction::ReportOnly
```

## A03-005: Primary Rust persistent-session quickstart constructs a removed request shape

**Candidate severity:** high. **Independent verdict:** confirm.

The example cannot compile: it assigns an Option to SystemPromptOverride, sets a nonexistent field, and omits two required fields. This is a current quickstart using the current exact-pinned crate family, not historical migration code.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:799-810](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L799-L810)

```text
system_prompt: None,
```

- [.claude/skills/meerkat-platform/SKILL.md:799-810](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L799-L810)

```text
skill_references: None,
```

### Implementation proof

- [meerkat-core/src/service/mod.rs:450-488](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/service/mod.rs#L450-L488)

```text
pub system_prompt: crate::config::SystemPromptOverride,
```

  CreateSessionRequest requires the typed prompt override, injected_context, and deferred_prompt_policy. The complete definition has no request-level skill_references field.

- [meerkat-core/src/service/mod.rs:452-457](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/service/mod.rs#L452-L457)

```text
request-level `render_metadata` / `skill_references` duplicates are gone
```

  Initial-turn skill references have one current carrier: SessionBuildOptions.initial_turn_metadata.

- [meerkat/src/service_factory.rs:1597-1607](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/service_factory.rs#L1597-L1607)

```text
#[cfg(feature = "session-store")]
```

  The demonstrated persistent-service helper also needs the non-default session-store facade feature.

### Independent challenge

The quickstart is current executable guidance, not part of the historical 0.4x-to-0.5 guide. Its struct literal cannot match the complete current CreateSessionRequest: None is not the system-prompt override type, skill_references is gone, and injected_context/deferred_prompt_policy are required fields. The convenience builder also has a real session-store feature prerequisite.

- [meerkat-core/src/service/mod.rs:450-488](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/service/mod.rs#L450-L488)

```text
pub system_prompt: crate::config::SystemPromptOverride,
```

  The complete public struct also declares injected_context and deferred_prompt_policy, and has no skill_references member. Its preceding contract directs initial-turn metadata into the build payload.

- [meerkat/src/service_factory.rs:1597-1607](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/service_factory.rs#L1597-L1607)

```text
#[cfg(feature = "session-store")]
```

  The persistent-service convenience function used by the example is not supplied by provider-only facade defaults.

**Required correction:** Update the Rust literal to system_prompt: SystemPromptOverride::Inherit, injected_context: Vec::new(), and an explicit/default DeferredPromptPolicy; remove skill_references. Route any needed initial-turn skill/render metadata through SessionBuildOptions.initial_turn_metadata instead of resurrecting request-level fields. Import the current types and state that this persistent-service example requires the session-store facade feature.

### Correction

Rebuilt the persistent-service request with all ten current fields: injected_context, typed SystemPromptOverride::Inherit, explicit DeferredPromptPolicy::Discard, and realm-bearing build options. Removed request-level skill_references, directed initial-turn metadata to build.initial_turn_metadata, and documented the session-store feature prerequisite.

- [.claude/skills/meerkat-platform/SKILL.md:856-859](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L856-L859)

```text
Enable the facade's `session-store` feature for this persistent-service
```

- [.claude/skills/meerkat-platform/SKILL.md:897-914](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L897-L914)

```text
system_prompt: SystemPromptOverride::Inherit,
```

### Final independent review

**PASS:** Checked the complete ten-field request literal, imports, feature prerequisite and helper signature. The literal now supplies typed system policy, injected context, deferred policy and realm-bearing build options, without resurrecting removed request metadata.

- [.claude/skills/meerkat-platform/SKILL.md:858-919](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L858-L919)

```text
system_prompt: SystemPromptOverride::Inherit,
```

- [.claude/skills/meerkat-platform/SKILL.md:905-919](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L905-L919)

```text
deferred_prompt_policy: DeferredPromptPolicy::Discard,
```

- [meerkat-core/src/service/mod.rs:450-488](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/service/mod.rs#L450-L488)

```text
pub deferred_prompt_policy: DeferredPromptPolicy,
```

- [meerkat/src/service_factory.rs:1597-1609](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/service_factory.rs#L1597-L1609)

```text
#[cfg(feature = "session-store")]
```

## A03-006: Named-realm Rust bootstraps load ambient config instead of the selected realm chain

**Candidate severity:** medium. **Independent verdict:** confirm.

Selecting team-alpha's storage directory does not cause Config::load to read team-alpha/config.toml or its parents. An embedder following the 'realm-aware' recipe misses realm bindings, tools, and inherited credentials and can instead use an unrelated workspace configuration.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:768-787](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L768-L787)

```text
let config = Config::load().await?;
let realm = meerkat_store::realm_paths("team-alpha");
```

- [.claude/skills/meerkat-platform/SKILL.md:785-794](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L785-L794)

```text
let config = Config::load().await?;
```

### Implementation proof

- [meerkat-core/src/config.rs:134-171](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/config.rs#L134-L171)

```text
// 1. Load project config (if exists). Local config replaces global.
```

  Config::load uses the current directory and HOME and loads a project file or the global file. It takes no selected realm and does not perform named-realm parent-chain composition.

- [meerkat-store/src/realm.rs:324-330](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-store/src/realm.rs#L324-L330)

```text
ambient state-root resolution is deprecated; use the `_in` variant with an explicit root (see meerkat_core::StorageLayout)
```

  The recommended API-reference bootstrap also uses the deprecated ambient path selector instead of the explicit resolved root.

- [meerkat-cli/src/main.rs:4793-4825](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L4793-L4825)

```text
let reader = meerkat_core::EffectiveConfigReader::new(cli_realm_config_source(scope));
```

  The shipping named-realm path composes FilesystemRealmConfigSource through EffectiveConfigReader.effective_config for the selected realm, then applies environment overrides and validates it.

### Independent challenge

Narrowed: manually supplying an ambient/raw Config to an embedded Rust factory is legal and need not mimic CLI composition. The defect is advertising this as the recommended named-realm-aware bootstrap without explaining that it skips selected-realm inheritance. Config::load reads project-or-global config; the persistent-service helper does not subsequently replace it with the named realm's effective config. Selecting persistence for team-alpha is not config composition for team-alpha.

- [meerkat-core/src/config.rs:134-171](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/config.rs#L134-L171)

```text
// 1. Load project config (if exists). Local config replaces global.
```

  The complete Config::load implementation chooses an ambient file and applies environment overrides; it has no selected realm argument or parent-chain traversal.

- [meerkat/src/service_factory.rs:1198-1254](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/service_factory.rs#L1198-L1254)

```text
.effective_config_over_head(&inheritance.head, head_config)
```

  Effective realm configuration is an explicit builder facility. The factory's supplied snapshot is otherwise retained; the persistent convenience helper does not silently install this facility.

- [meerkat-cli/src/main.rs:4793-4825](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L4793-L4825)

```text
let reader = meerkat_core::EffectiveConfigReader::new(cli_realm_config_source(scope));
```

  Actual realm-aware composition uses a filesystem realm source and effective-config reader before environment overrides and validation.

- [meerkat-store/src/realm.rs:310-330](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-store/src/realm.rs#L310-L330)

```text
ambient state-root resolution is deprecated; use the `_in` variant with an explicit root (see meerkat_core::StorageLayout)
```

  The recommended reference bootstrap also uses the deprecated ambient realm_paths helper despite having an explicit-root alternative.

**Required correction:** Make the named-realm recipes actually compose the selected RealmId through FilesystemRealmConfigSource and EffectiveConfigReader, then apply environment overrides/validation, and use the same explicit state root for realm_paths_in and persistence. If retaining a short raw-Config embedding example instead, relabel it explicitly as manually configured standalone embedding, remove the realm-aware claim, and warn beside both examples that opening named persistence does not load that realm's config chain. Do not outlaw intentional raw-config embedding.

### Correction

Both named-realm recipes now parse RealmId, compose FilesystemRealmConfigSource through EffectiveConfigReader, apply environment overrides and catalog validation, and use explicit realm_paths_in roots. Persistent config and storage share the same root. The factory-only example is explicitly standalone, and deliberate raw-config embedding remains valid without a false inheritance claim.

- [.claude/skills/meerkat-platform/SKILL.md:873-891](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L873-L891)

```text
let reader = EffectiveConfigReader::new(Arc::new(FilesystemRealmConfigSource::new(
```

- [.claude/skills/meerkat-platform/references/api_reference.md:807-834](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L807-L834)

```text
let mut config = reader.effective_config(&realm_id).await?;
```

### Final independent review

**PASS:** Both named-realm recipes compose the selected RealmId using the filesystem source and effective reader, then apply environment overrides and validation. Config/persistence use the same explicit root; the factory-only recipe is accurately standalone, and intentional manually supplied Config remains permitted.

- [.claude/skills/meerkat-platform/SKILL.md:875-893](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L875-L893)

```text
let mut config = reader.effective_config(&realm_id).await?;
```

- [.claude/skills/meerkat-platform/references/api_reference.md:809-836](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L809-L836)

```text
let mut config = reader.effective_config(&realm_id).await?;
```

- [meerkat-core/src/config_store.rs:101-142](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/config_store.rs#L101-L142)

```text
pub async fn effective_config(
```

- [meerkat-store/src/realm.rs:2212-2239](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-store/src/realm.rs#L2212-L2239)

```text
realm_paths_in(&self.state_root, realm.as_str()).config_path
```

- [meerkat-store/src/realm.rs:310-323](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-store/src/realm.rs#L310-L323)

```text
let root = realms_root.join(safe);
```

## A03-007: AgentFactory.mob(true) example does not actually install or authorize mob tools

**Candidate severity:** medium. **Independent verdict:** confirm.

The purported opt-in leaves the built agent without mob orchestration tools. Merely changing an ambient boolean is deliberately not an authority grant.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:777-787](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L777-L787)

```text
.mob(true);  // opt-in mob orchestration tools
```

### Implementation proof

- [meerkat/src/factory.rs:3514-3527](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L3514-L3527)

```text
self.enable_mob = enabled;
```

  mob(true) changes an ambient default only. mob_tools_factory is the separate installer.

- [meerkat/src/factory.rs:5250-5260](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L5250-L5260)

```text
Ambient factory defaults must not do this,
```

  Generated create-only authority is minted for explicit build_config.override_mob == Enable, not for the ambient factory default used by the snippet.

- [meerkat/src/factory.rs:6369-6389](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L6369-L6389)

```text
if effective_mob && let Some(mob_factory) = mob_factory {
```

  Actual tool composition requires both generated authority and an installed MobToolsFactory; the shown code supplies neither.

### Independent challenge

I tested the possible ambient-default escape hatch in source. AgentFactory.mob(true) only changes a boolean: the default factory has no MobToolsFactory, and ambient defaults are explicitly excluded from minting mob authority. The final composition gate requires both actual infrastructure and generated authority. Thus the example's comment promises tools it has not installed or authorized.

- [meerkat/src/factory.rs:3514-3527](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L3514-L3527)

```text
self.enable_mob = enabled;
```

  The neighboring mob_tools_factory setter is a separate operation; mob(true) is not a complete mob-tools composition.

- [meerkat/src/factory.rs:5250-5260](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L5250-L5260)

```text
Ambient factory defaults must not do this,
```

  Create-only authority is minted for explicit request enablement, not merely for this ambient factory boolean.

- [meerkat/src/factory.rs:6369-6389](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L6369-L6389)

```text
if effective_mob && let Some(mob_factory) = mob_factory {
```

  The actual dispatcher composition still needs the installed factory, and the enclosed branch requires generated mob authority.

**Required correction:** Remove the misleading '.mob(true) // opt-in mob orchestration tools' from the otherwise incomplete bootstrap, or replace it with a complete composition that installs MobToolsFactory and explicitly requests generated create-only mob access. State both prerequisites, and distinguish create-only enablement from access to existing mobs; do not suggest the boolean alone supplies either infrastructure or authority.

### Correction

Removed the misleading mob(true) bootstrap activation. Documented the distinct MobToolsFactory and explicit per-build Enable prerequisites, create-only authority rather than existing-mob scope, and a complete with_mob_tools handoff that records supported generated-access intent without forging authority.

- [.claude/skills/meerkat-platform/references/api_reference.md:836-841](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L836-L841)

```text
operator authority. The factory's `.mob(true)` ambient default supplies
```

- [.claude/skills/meerkat-platform/references/mobs.md:230-247](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L230-L247)

```text
build.apply_generated_create_only_mob_operator_access(ToolCategoryOverride::Enable);
```

### Final independent review

**PASS:** The false .mob(true)-only promise is gone, not merely hidden. Its replacement explains both infrastructure and explicit intent; the linked with_mob_tools fragment supplies both through supported APIs and accurately leaves generated authority minting to the factory/runtime.

- [.claude/skills/meerkat-platform/references/api_reference.md:838-843](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L838-L843)

```text
neither prerequisite by itself and grants no scope over existing mobs.
```

- [.claude/skills/meerkat-platform/references/mobs.md:230-247](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L230-L247)

```text
build.apply_generated_create_only_mob_operator_access(ToolCategoryOverride::Enable);
```

- [meerkat-core/src/service/mod.rs:1423-1435](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/service/mod.rs#L1423-L1435)

```text
self.mob_tool_authority_context = None;
```

- [meerkat/src/factory.rs:5248-5260](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L5248-L5260)

```text
Ambient factory defaults must not do this,
```

- [meerkat/src/factory.rs:6368-6394](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L6368-L6394)

```text
if effective_mob && let Some(mob_factory) = mob_factory
```

## A03-008: Rust skill-introspection snippet matches Option before unwrapping Result

**Candidate severity:** medium. **Independent verdict:** confirm.

This current Rust example fails type checking. It also uses SkillFilter without importing it in the displayed import list.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:818-824](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L818-L824)

```text
if let Some(runtime) = factory.build_skill_runtime(&config).await {
```

### Implementation proof

- [meerkat/src/factory.rs:3653-3658](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L3653-L3658)

```text
) -> Result<Option<Arc<meerkat_core::skills::SkillRuntime>>, BuildAgentError> {
```

  The async method returns Result<Option<_>>, not Option<_>.

### Independent challenge

The invoked public function returns Result<Option<_>>, not Option<_>. No overload or wrapper in this call erases the Result. I also checked load_from_source: its optional argument really is a source name, so Some("company") is not itself an error and should not be changed merely because other skill identities use UUIDs.

- [meerkat/src/factory.rs:3653-3658](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L3653-L3658)

```text
) -> Result<Option<Arc<meerkat_core::skills::SkillRuntime>>, BuildAgentError> {
```

  The example must handle BuildAgentError before matching Some; the function is also gated by the skills feature.

- [meerkat-core/src/skills/mod.rs:1474-1481](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/mod.rs#L1474-L1481)

```text
source_name: Option<&str>,
```

  This independently limits the correction: the optional named-source argument is a real supported API.

**Subsequent adjudication:** The original endorsement of the `Some("company")` source selector above was overturned by [A03-036](A03.md). The scoped Result/import correction remains accepted; that aside is preserved as review history, not current API guidance.

**Original scoped correction:** Use factory.build_skill_runtime(&config).await? (or explicit error handling) before matching Some. Add the missing SkillFilter import to the self-contained snippet and identify the skills feature prerequisite. Preserve the supported named-source load_from_source call.

### Correction

Propagated build_skill_runtime's outer Result with .await? before matching Option, imported SkillFilter, and stated the skills feature requirement. The separate A03-036 adjudication subsequently corrected the selector to None; the earlier positive source-name assumption is not retained.

- [.claude/skills/meerkat-platform/references/api_reference.md:876-889](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L876-L889)

```text
if let Some(runtime) = factory.build_skill_runtime(&config).await? {
```

### Final independent review

**PASS:** Independently rechecked that .await? still unwraps Result before Some, SkillFilter remains imported, and the skills feature prerequisite remains explicit. The subsequent None selector repair is separately authorized by supplemental A03-036 after the parent explicitly reopened the original source-name endorsement; it does not silently reinterpret this ID's accepted Result/import scope. The stock-native embedded-key prerequisite now accompanies the example.

- [.claude/skills/meerkat-platform/references/api_reference.md:878-895](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L878-L895)

```text
if let Some(runtime) = factory.build_skill_runtime(&config).await? {
```

- [.claude/skills/meerkat-platform/references/api_reference.md:884-886](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L884-L886)

```text
use meerkat_core::skills::{SkillFilter, SkillKey, SkillName, SourceUuid};
```

- [meerkat/src/factory.rs:3653-3658](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L3653-L3658)

```text
Result<Option<Arc<meerkat_core::skills::SkillRuntime>>, BuildAgentError>
```

## A03-009: In-memory mob Rust example omits the required placement argument

**Candidate severity:** medium. **Independent verdict:** confirm.

The otherwise-current in-memory Rust example does not compile against the public method signature.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/mobs.md:190-198](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L190-L198)

```text
ProfileName::from("lead"),
            AgentIdentity::from("lead-1"),
            None,
            None,
```

### Implementation proof

- [meerkat-mob-mcp/src/lib.rs:1761-1774](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/lib.rs#L1761-L1774)

```text
placement: Option<meerkat_mob::machines::mob_machine::HostId>,
```

  mob_spawn takes mob ID, profile, identity, runtime_mode, backend, and placement. The example supplies only the first five.

### Independent challenge

The actual MobMcpState::mob_spawn method has a required sixth Rust argument even though that argument's type is Option. Rust does not supply omitted optional parameters. The sample is missing placement, not a runtime_mode or backend default.

- [meerkat-mob-mcp/src/lib.rs:1761-1774](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/lib.rs#L1761-L1774)

```text
placement: Option<meerkat_mob::machines::mob_machine::HostId>,
```

  The complete method signature orders placement after runtime_mode and backend.

**Required correction:** Append a third None after the existing runtime_mode/backend None arguments in the in-memory mob_spawn example, documenting it as no explicit host placement. Alternatively use the current complete SpawnMemberSpec API, but do not silently drop placement.

### Correction

Completed the in-memory MobMcpState::mob_spawn invocation with the third trailing None, explicitly documented as default/no explicit host placement; preserved the rest of the lifecycle example.

- [.claude/skills/meerkat-platform/references/mobs.md:193-201](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L193-L201)

```text
None, // no explicit host placement
```

### Final independent review

**PASS:** The high-level in-memory example now passes all six required arguments, with the final None explicitly documented as placement. Profile definitions, comms enablement and the surrounding lifecycle example remain intact.

- [.claude/skills/meerkat-platform/references/mobs.md:193-201](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L193-L201)

```text
None, // no explicit host placement
```

- [meerkat-mob-mcp/src/lib.rs:1761-1779](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-mcp/src/lib.rs#L1761-L1779)

```text
placement: Option<meerkat_mob::machines::mob_machine::HostId>,
```

## A03-010: TypeScript connect example combines mutually exclusive realm selection modes

**Candidate severity:** medium. **Independent verdict:** confirm.

Copying the options block fails immediately; the 'optional' comments do not make the simultaneously supplied values optional at runtime.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:675-683](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L675-L683)

```text
isolated: true,
```

- [.claude/skills/meerkat-platform/references/api_reference.md:675-683](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L675-L683)

```text
realmId: "team-alpha",
```

### Implementation proof

- [sdks/typescript/src/client.ts:990-994](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L990-L994)

```text
throw new MeerkatError("INVALID_ARGS", "realmId and isolated cannot both be set");
```

  The SDK rejects the exact combination in the example before starting or resolving the RPC binary.

### Independent challenge

The options do not mean 'isolate this named realm'. The client rejects their combination immediately, before resolving or launching a runtime binary. This is a deterministic client-side error, independent of server configuration.

- [sdks/typescript/src/client.ts:990-994](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L990-L994)

```text
throw new MeerkatError("INVALID_ARGS", "realmId and isolated cannot both be set");
```

  The actual connect method explicitly disallows the exact combination shown by the reference.

**Required correction:** Remove isolated: true from the named-realm connect example. Present isolated: true without realmId as a separate alternative and explicitly state the mutual exclusion.

### Correction

Removed isolated:true from named-realm TypeScript connection options and provided a separate isolated-only invocation with an explicit mutual-exclusion warning.

- [.claude/skills/meerkat-platform/references/api_reference.md:714-714](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L714)

```text
instead. `realmId` and `isolated: true` are mutually exclusive.
```

### Final independent review

**PASS:** The named connection no longer combines realmId with isolation. The isolated alternative is separate and the client-side mutual exclusion is explicit; no unsupported alternate connect overload was introduced.

- [.claude/skills/meerkat-platform/references/api_reference.md:702-714](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L702-L714)

```text
`realmId` and `isolated: true` are mutually exclusive.
```

- [sdks/typescript/src/client.ts:990-995](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/typescript/src/client.ts#L990-L995)

```text
realmId and isolated cannot both be set
```

## A03-011: Mob TypeScript example passes a request object where createSession requires a prompt

**Candidate severity:** medium. **Independent verdict:** confirm.

The example fails TypeScript checking and does not match the SDK's runtime request-lowering contract.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/mobs.md:434-436](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L434-L436)

```text
const result = await client.createSession({
  prompt: "Use mob_* tools to create a lead/worker mob and return status.",
});
```

### Implementation proof

- [sdks/typescript/src/client.ts:1202-1209](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L1202-L1209)

```text
prompt: string | ContentBlock[],
```

  The method takes the prompt as its first argument and optional SessionOptions as its second; it has no object-form overload.

### Independent challenge

The native TypeScript createSession API is prompt-first, not a wire-shaped request-object overload. The complete method signature takes string or ContentBlock[] followed by SessionOptions. The reference appears to have carried an RPC-shaped object into the SDK layer.

- [sdks/typescript/src/client.ts:1202-1209](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L1202-L1209)

```text
prompt: string | ContentBlock[],
```

  The request object in the example cannot satisfy this public SDK input; options are the second argument.

**Required correction:** Rewrite the mob example as client.createSession("<the existing prompt>", { enableMob: true }). Preserve the prompt text and coordinate the explicit enablement with A03-012.

### Correction

Converted the native TypeScript mob-execution example to createSession(prompt, options), preserving the existing prompt and placing enableMob:true in the second argument.

- [.claude/skills/meerkat-platform/references/mobs.md:461-464](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L461-L464)

```text
const result = await client.createSession(
  "Use mob_* tools to create a lead/worker mob and return status.",
  { enableMob: true },
);
```

### Final independent review

**PASS:** The execution example preserves its original prompt and now uses the native SDK's prompt-first signature with enableMob in the separate options argument. It is not confused with browser or RPC request-object APIs.

- [.claude/skills/meerkat-platform/references/mobs.md:461-464](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L461-L464)

```text
{ enableMob: true }
```

- [sdks/typescript/src/client.ts:1202-1214](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/typescript/src/client.ts#L1202-L1214)

```text
prompt: string | ContentBlock[],
```

## A03-012: Tool-driven mob examples omit the explicit enablement that grants operator tools

**Candidate severity:** medium. **Independent verdict:** confirm.

These examples ask an ordinary newly created agent to execute tools it has not been granted. They are not direct mob/create calls, and merely mentioning mob_* in a prompt does not install operator authority.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/mobs.md:392-393](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L392-L393)

```text
{"jsonrpc":"2.0","id":2,"method":"session/create","params":{"prompt":"Use mob_* tools to create a lead/worker mob and return status."}}
```

- [.claude/skills/meerkat-platform/references/mobs.md:399-410](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L399-L410)

```text
-d '{"prompt":"Use mob_* tools to create a lead/worker mob and return status."}'
```

- [.claude/skills/meerkat-platform/references/mobs.md:407-412](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L407-L412)

```text
"prompt": "Use mob_* tools to create a lead/worker mob and return status."
```

- [.claude/skills/meerkat-platform/references/mobs.md:434-436](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L434-L436)

```text
prompt: "Use mob_* tools to create a lead/worker mob and return status.",
```

### Implementation proof

- [meerkat-rpc/src/handlers/session.rs:584-587](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/handlers/session.rs#L584-L587)

```text
ToolCategoryOverride::from_override(params.enable_mob),
```

  RPC creation derives generated operator access from the explicit enable_mob override.

- [meerkat-rest/src/lib.rs:5333-5335](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rest/src/lib.rs#L5333-L5335)

```text
req.enable_mob,
```

  REST creation applies the same generated-access helper to its explicit enable_mob option.

- [meerkat-mcp-server/src/lib.rs:4231-4233](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mcp-server/src/lib.rs#L4231-L4233)

```text
ToolCategoryOverride::from_override(input.enable_mob),
```

  The MCP run handler also uses explicit enablement rather than interpreting prompt text as authority.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:10040-10070](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L10040-L10070)

```text
guard "not_explicit_enable" { request_kind != MobOperatorAccessRequestKind::Enable }
```

  The authoritative absent transition clears mob_operator_authority_present for inherited/omitted enablement. The explicit Enable transition grants create authority.

### Independent challenge

I followed request lowering into generated authority rather than assuming configuration defaults could rescue the examples. Each shipping native surface forwards explicit enable_mob; the machine grants create-only authority on Enable and clears it otherwise. These examples ask the model to execute mob operations, so tool enablement is a material prerequisite. The Python example that merely asks to design a topology need not be treated as executing those tools.

- [meerkat-rpc/src/handlers/session.rs:584-587](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rpc/src/handlers/session.rs#L584-L587)

```text
ToolCategoryOverride::from_override(params.enable_mob),
```

  RPC create explicitly carries the request's enable_mob value into the generated create-only access seam.

- [meerkat-rest/src/lib.rs:5333-5335](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-rest/src/lib.rs#L5333-L5335)

```text
req.enable_mob,
```

  REST uses the same explicit opt-in rather than treating the prompt as authority.

- [meerkat-mcp-server/src/lib.rs:4231-4233](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mcp-server/src/lib.rs#L4231-L4233)

```text
ToolCategoryOverride::from_override(input.enable_mob),
```

  The MCP run surface also consumes an explicit enable_mob input.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:10040-10077](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L10040-L10077)

```text
guard "explicit_enable" { request_kind == MobOperatorAccessRequestKind::Enable }
```

  The actual generated-authority rule distinguishes explicit enablement from omission/inheritance; the inspected non-enable branch removes mob authority.

**Required correction:** Add enable_mob: true to the RPC session/create, REST session-create, and MCP meerkat_run examples that ask for mob execution. Add enableMob: true in the TypeScript createSession options using the corrected prompt-first signature. Explain that this grants create-only operator capability, not arbitrary existing-mob scopes.

### Correction

Added enable_mob:true to RPC, REST, and MCP examples and enableMob:true to the corrected native TypeScript options. Explained host factory/support prerequisites and create-only rather than unrestricted authority; retained the design-only Python prompt.

- [.claude/skills/meerkat-platform/references/mobs.md:419-437](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L419-L437)

```text
"enable_mob":true
```

- [.claude/skills/meerkat-platform/references/mobs.md:461-473](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L461-L473)

```text
create-only operator capability. It does not grant arbitrary existing-mob
```

### Final independent review

**PASS:** All four execution examples, not just TypeScript, explicitly enable mob tools. RPC/REST/MCP lowering and generated authority agree with the create-only explanation. The design-only Python prompt is correctly preserved, and host/factory support and existing-mob scope limits are stated.

- [.claude/skills/meerkat-platform/references/mobs.md:419-437](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L419-L437)

```text
"enable_mob":true
```

- [.claude/skills/meerkat-platform/references/mobs.md:461-473](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L461-L473)

```text
create-only operator capability.
```

- [meerkat-rpc/src/handlers/session.rs:582-587](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-rpc/src/handlers/session.rs#L582-L587)

```text
ToolCategoryOverride::from_override(params.enable_mob)
```

- [meerkat-rest/src/lib.rs:5333-5335](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-rest/src/lib.rs#L5333-L5335)

```text
req.enable_mob
```

- [meerkat-mcp-server/src/lib.rs:4230-4234](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mcp-server/src/lib.rs#L4230-L4234)

```text
input.enable_mob
```

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:10040-10077](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L10040-L10077)

```text
self.mob_operator_managed_mob_scope = EmptySet;
```

## A03-013: Mob creation SDK signatures advertise unsupported positional forms and auth options

**Candidate severity:** medium. **Independent verdict:** confirm.

Python callers get argument errors; TypeScript callers either fail type checking or supply an object without the definition wrapper. Top-level auth settings advertised here cannot affect member credentials.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:605-606](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L605-L606)

```text
`create_mob(definition, ..., auth_binding=None)`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:694-695](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L694-L695)

```text
`createMob(definition, options?)` → `Mob` — accepts `authBinding`
```

### Implementation proof

- [sdks/python/meerkat/client.py:2470-2483](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L2470-L2483)

```text
definition: MobDefinitionInput | dict[str, Any],
```

  The full signature has a keyword-only separator and only definition; it has neither positional definition nor auth_binding.

- [sdks/typescript/src/client.ts:2069-2082](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L2069-L2082)

```text
async createMob(options: MobCreateOptions): Promise<Mob> {
```

  TypeScript accepts one options object and lowers options.definition.

- [sdks/typescript/src/types.ts:1433-1436](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/types.ts#L1433-L1436)

```text
definition: MobDefinition;
```

  The complete MobCreateOptions interface contains only definition, not authBinding.

- [sdks/python/meerkat/mob.py:327-352](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/mob.py#L327-L352)

```text
auth_binding: WireAuthBindingRef | dict[str, str] | None = None,
```

  Per-member spawning is an actual auth-binding override seam; top-level mob creation is not.

### Independent challenge

The Python and TypeScript public implementations agree on the semantic definition payload but deliberately have different call shapes. Neither current creation signature has a top-level auth override. Member/profile auth is not evidence that creation accepts auth_binding/authBinding.

- [sdks/python/meerkat/client.py:2470-2483](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L2470-L2483)

```text
definition: MobDefinitionInput | dict[str, Any],
```

  The complete signature places definition after *, making it keyword-only, and exposes no auth_binding parameter. Independent AST inspection confirmed no positional arguments after self.

- [sdks/typescript/src/client.ts:2069-2082](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L2069-L2082)

```text
async createMob(options: MobCreateOptions): Promise<Mob> {
```

  This is a single options-object method, not createMob(definition, options).

- [sdks/typescript/src/types.ts:1433-1436](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/types.ts#L1433-L1436)

```text
definition: MobDefinition;
```

  The complete MobCreateOptions declaration contains the definition field and no authBinding field.

**Required correction:** Document await client.create_mob(definition=definition) for Python and await client.createMob({ definition }) for native TypeScript. Remove auth_binding/authBinding from mob-creation signatures; point auth customization to supported role/profile or member-spawn paths instead.

### Correction

Documented keyword-only Python create_mob(definition=definition) and native TypeScript createMob({ definition }) with awaited examples. Neither mob creation nor mob role Profile declares an initial auth binding; use host/realm credentials or supported per-member spawn/helper binding overrides.

- [.claude/skills/meerkat-platform/references/api_reference.md:615-615](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L615)

```text
await client.create_mob(definition=definition)
```

- [.claude/skills/meerkat-platform/references/api_reference.md:724-724](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L724)

```text
await client.createMob({ definition })
```

- [.claude/skills/meerkat-platform/references/api_reference.md:685-685](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L685)

```text
`Profile` does not declare an initial auth binding either.
```

- [.claude/skills/meerkat-platform/references/api_reference.md:724-724](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L724)

```text
neither mob creation nor a mob role `Profile` declares an initial auth binding.
```

### Final independent review

**PASS:** Rechecked the Python keyword-only and native TypeScript definition-wrapper methods against their actual request builders. Both corrected create signatures are preserved. Both replacement-auth occurrences now explicitly deny an initial mob-role Profile binding and direct callers to host/realm credentials or supported per-member spawn/helper overrides. Profile's complete declared-field inventory, SpawnMemberSpec.auth_binding and its forwarding into AgentBuildConfig support the replacement; the initial V03-R01 regression is resolved.

- [.claude/skills/meerkat-platform/references/api_reference.md:615-615](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L615)

```text
`await client.create_mob(definition=definition)`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:684-687](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L684-L687)

```text
`Profile` does not declare an initial auth binding either.
```

- [.claude/skills/meerkat-platform/references/api_reference.md:724-724](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L724)

```text
neither mob creation nor a mob role `Profile` declares an initial auth binding.
```

- [sdks/python/meerkat/client.py:2470-2482](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/python/meerkat/client.py#L2470-L2482)

```text
async def create_mob(
```

- [sdks/typescript/src/client.ts:2069-2082](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/typescript/src/client.ts#L2069-L2082)

```text
async createMob(options: MobCreateOptions): Promise<Mob>
```

- [meerkat-mob/src/profile.rs:222-374](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/profile.rs#L222-L374)

```text
pub const FIELD_NAMES: &'static [&'static str] = &[
```

  The complete Profile and declared-field inventory have model/provider/tools/etc., but no auth_binding. No flattened auth-bearing carrier exists here.

- [meerkat-mob/src/definition.rs:798-846](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/definition.rs#L798-L846)

```text
every other unknown key
```

  Unknown TOML profile fields are warnings, not working auth controls; serde does not invent an override for them.

- [meerkat-mob/src/runtime/handle.rs:4797-4804](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/handle.rs#L4797-L4804)

```text
pub auth_binding: Option<meerkat_core::AuthBindingRef>,
```

- [meerkat-mob/src/runtime/actor.rs:28419-28422](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L28419-L28422)

```text
config.auth_binding = Some(cref.clone());
```

## A03-014: Python session and mob query return shapes do not match the reference

**Candidate severity:** medium. **Independent verdict:** confirm.

Consumers following the advertised object-versus-dictionary contract use the wrong access pattern. The list_sessions entry also loses the actual return type and its additional fields, although SessionSummary inherits SessionInfo.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:601-606](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L601-L606)

```text
`read_session(session_id)` → dict
```

- [.claude/skills/meerkat-platform/references/api_reference.md:601-606](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L601-L606)

```text
`list_mobs()` → `list[MobSummary]`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:601-601](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L601)

```text
`list_sessions()` → `list[SessionInfo]`
```

### Implementation proof

- [sdks/python/meerkat/client.py:1333-1365](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L1333-L1365)

```text
async def read_session(self, session_id: str) -> SessionDetails:
```

  read_session parses a SessionDetails object. list_sessions in the same range returns list[SessionSummary], the more specific public query type.

- [sdks/python/meerkat/client.py:2487-2501](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L2487-L2501)

```text
async def list_mobs(self) -> list[dict[str, Any]]:
```

  Mob listing returns validated dictionary entries, not MobSummary instances.

### Independent challenge

Confirmed with a narrower interpretation of list_sessions: SessionSummary inherits SessionInfo, so listing it as SessionInfo is imprecise rather than proof that ordinary attribute access fails. The concrete defects are read_session advertised as a dict despite returning a SessionDetails dataclass, and list_mobs advertised as typed MobSummary values despite returning dictionaries. Record the exact actual return types together.

- [sdks/python/meerkat/client.py:1333-1365](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L1333-L1365)

```text
async def read_session(self, session_id: str) -> SessionDetails:
```

  The neighboring list_sessions implementation returns SessionSummary objects, and read_session constructs SessionDetails rather than returning the raw JSON mapping.

- [sdks/python/meerkat/types.py:497-526](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/types.py#L497-L526)

```text
class SessionSummary(SessionInfo):
```

  The public dataclass hierarchy is the counterexample to overclaiming that SessionInfo-based access is intrinsically invalid.

- [sdks/python/meerkat/client.py:2487-2501](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L2487-L2501)

```text
async def list_mobs(self) -> list[dict[str, Any]]:
```

  Mob-list results are mappings in this SDK implementation, not a MobSummary dataclass API.

**Required correction:** Use the exact Python annotations list_sessions() -> list[SessionSummary], read_session(session_id) -> SessionDetails, and list_mobs() -> list[dict[str, Any]]. Preserve the SessionInfo inheritance relationship and avoid implying list_sessions lost its inherited fields.

### Correction

Corrected Python list_sessions, read_session, and list_mobs return annotations to list[SessionSummary], SessionDetails, and list[dict[str, Any]], respectively, and preserved the SessionInfo inheritance and shared fields for the typed session dataclasses.

- [.claude/skills/meerkat-platform/references/api_reference.md:611-616](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L611-L616)

```text
`read_session(session_id)` → `SessionDetails`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:681-683](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L681-L683)

```text
`SessionSummary` and `SessionDetails` inherit `SessionInfo` and its shared
```

### Final independent review

**PASS:** All three Python return annotations now match their implementations, and the added inheritance note correctly retains SessionInfo fields for SessionSummary/SessionDetails. Mob dictionaries are not laundered into typed summaries.

- [.claude/skills/meerkat-platform/references/api_reference.md:611-616](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L611-L616)

```text
`list_mobs()` → `list[dict[str, Any]]`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:681-683](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L681-L683)

```text
`SessionSummary` and `SessionDetails` inherit `SessionInfo`
```

- [sdks/python/meerkat/client.py:1333-1364](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/python/meerkat/client.py#L1333-L1364)

```text
-> list[SessionSummary]:
```

- [sdks/python/meerkat/client.py:2487-2498](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/python/meerkat/client.py#L2487-L2498)

```text
-> list[dict[str, Any]]:
```

- [sdks/python/meerkat/types.py:512-526](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/python/meerkat/types.py#L512-L526)

```text
class SessionDetails(SessionInfo):
```

## A03-015: Python live MCP mutation helpers are not single params-object methods

**Candidate severity:** medium. **Independent verdict:** confirm.

Passing one request dictionary, as the reference directs, leaves required positional arguments missing for add/remove and passes an invalid session_id object for reload.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:608-608](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L608)

```text
`mcp_add(params)` / `mcp_remove(params)` / `mcp_reload(params)`
```

### Implementation proof

- [sdks/python/meerkat/client.py:2287-2338](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L2287-L2338)

```text
server_config: McpServerConfig,
```

  mcp_add takes session_id and server_config with keyword-only persisted; mcp_remove takes session_id and server_name; mcp_reload takes session_id with keyword-only server_name and persisted. These full signatures are in this range.

### Independent challenge

These Python helpers are not raw JSON-RPC params-object adapters. Their complete function definitions and independent AST inspection show positional session/config/name inputs plus keyword-only persistence options. The similarly named TypeScript params-object API does not justify copying its shape into Python.

- [sdks/python/meerkat/client.py:2287-2338](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L2287-L2338)

```text
server_config: McpServerConfig,
```

  The complete three helper signatures are mcp_add(session_id, server_config, *, persisted=False), mcp_remove(session_id, server_name, *, persisted=False), and mcp_reload(session_id, *, server_name=None, persisted=False). Their bodies build the wire params.

**Required correction:** Replace the three Python '(params)' signatures with mcp_add(session_id, server_config, *, persisted=False), mcp_remove(session_id, server_name, *, persisted=False), and mcp_reload(session_id, *, server_name=None, persisted=False). Leave the independently valid TypeScript params-object signatures alone.

### Correction

Replaced all three Python MCP params-object signatures with their actual session/server positional arguments and keyword-only persisted/server_name options. Left valid native TypeScript params-object methods unchanged.

- [.claude/skills/meerkat-platform/references/api_reference.md:618-620](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L618-L620)

```text
`mcp_reload(session_id, *, server_name=None, persisted=False)`
```

### Final independent review

**PASS:** Checked all three Python definitions and their keyword-only separators: add/remove take required session/server arguments, reload takes session with optional named server, and persisted defaults false. The independently object-shaped TypeScript APIs were retained.

- [.claude/skills/meerkat-platform/references/api_reference.md:618-620](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L618-L620)

```text
`mcp_reload(session_id, *, server_name=None, persisted=False)`
```

- [sdks/python/meerkat/client.py:2287-2335](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/python/meerkat/client.py#L2287-L2335)

```text
persisted: bool = False,
```

- [.claude/skills/meerkat-platform/references/api_reference.md:727-728](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L727-L728)

```text
`mcpAdd(params)` / `mcpRemove(params)` / `mcpReload(params)`
```

## A03-016: Python live helpers document session IDs instead of channel IDs and omit audio format arguments

**Candidate severity:** medium. **Independent verdict:** confirm.

A valid session ID is not a live channel ID. The documented calls address the wrong resource, and the audio call fails before dispatch because required format arguments are absent.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:616-619](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L616-L619)

```text
`live_status(session_id)` / `live_close(session_id)`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:617-619](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L617-L619)

```text
`live_send_input_text(session_id, text)` / `live_send_input_audio(session_id, data)`
```

### Implementation proof

- [sdks/python/meerkat/client.py:4392-4419](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L4392-L4419)

```text
async def live_status(self, channel_id: str) -> dict[str, Any]:
```

  Status, close, and text input all use the channel_id returned by live_open, and encode that identity under channel_id.

- [sdks/python/meerkat/client.py:4429-4454](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L4429-L4454)

```text
sample_rate_hz: int,
        channels: int,
```

  Audio input additionally requires base64 data plus mandatory sample rate and channel count.

- [sdks/python/meerkat/client.py:4531-4578](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L4531-L4578)

```text
async def live_interrupt(self, channel_id: str) -> dict[str, Any]:
```

  Commit, interrupt, and truncate continue the same channel-scoped contract, not a session-scoped one.

### Independent challenge

Only opening a live channel is session-scoped. Subsequent helpers serialize channel_id, so calling them with the session ID does not resolve the documented channel. The audio arguments are not optional encoding metadata supplied by defaults: both sample rate and channel count are required. Truncation likewise has a distinct item/content-index/time shape.

- [sdks/python/meerkat/client.py:4392-4419](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L4392-L4419)

```text
async def live_status(self, channel_id: str) -> dict[str, Any]:
```

  Status, close, and text input use the returned channel identifier and construct channel_id wire fields.

- [sdks/python/meerkat/client.py:4429-4454](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L4429-L4454)

```text
sample_rate_hz: int,
        channels: int,
```

  The audio convenience method requires channel_id, data_base64, sample_rate_hz, and channels; no defaults fill the missing arguments.

- [sdks/python/meerkat/client.py:4531-4578](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L4531-L4578)

```text
async def live_interrupt(self, channel_id: str) -> dict[str, Any]:
```

  Commit/interrupt/truncate are channel-scoped; the complete truncate signature additionally requires item_id, content_index, and audio_played_ms.

- [sdks/python/meerkat/client.py:4608-4616](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/python/meerkat/client.py#L4608-L4616)

```text
async def live_refresh(self, channel_id: str) -> LiveRefreshResult:
```

  Refresh is also channel-scoped and config-only, not a new session-scoped open.

**Required correction:** Keep live_open(session_id, ...) but change subsequent helper signatures to channel_id and explain where the open result supplies it. Document live_send_input_audio(channel_id, data_base64, sample_rate_hz, channels), live_truncate(channel_id, item_id, content_index, audio_played_ms), and live_refresh(channel_id), with the current positional/keyword shapes for the remaining helpers.

### Correction

Kept live_open session-addressed, corrected its Python dictionary return shape and optional arguments, and made subsequent operations channel-addressed. Included mandatory audio format and truncate arguments, refresh(channel_id), and extraction of opened["channel_id"].

- [.claude/skills/meerkat-platform/references/api_reference.md:627-640](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L627-L640)

```text
`live_send_input_audio(channel_id, data_base64, sample_rate_hz, channels)`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:638-640](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L638-L640)

```text
`channel_id = opened["channel_id"]`.
```

### Final independent review

**PASS:** The full Python live helper list was compared with AST-extracted signatures. Opening remains session-addressed and returns a dictionary; subsequent calls use opened['channel_id']. Mandatory audio rate/channels, truncate item/index/time and refresh(channel_id) all match.

- [.claude/skills/meerkat-platform/references/api_reference.md:627-640](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L627-L640)

```text
`channel_id = opened["channel_id"]`
```

- [sdks/python/meerkat/client.py:4326-4340](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/python/meerkat/client.py#L4326-L4340)

```text
async def live_open(
```

- [sdks/python/meerkat/client.py:4429-4454](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/python/meerkat/client.py#L4429-L4454)

```text
sample_rate_hz: int,
```

- [sdks/python/meerkat/client.py:4562-4580](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/python/meerkat/client.py#L4562-L4580)

```text
audio_played_ms: int,
```

- [sdks/python/meerkat/client.py:4608-4614](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/python/meerkat/client.py#L4608-L4614)

```text
async def live_refresh(self, channel_id: str)
```

## A03-017: TypeScript image and video live helpers take positional arguments

**Candidate severity:** medium. **Independent verdict:** confirm.

The reference incorrectly generalizes the raw liveSendInput parameter-object form to two convenience methods. Its calls do not type-check.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:706-706](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L706)

```text
`liveSendInput(params)` / `liveSendInputImage(params)` / `liveSendInputVideoFrame(params)`
```

### Implementation proof

- [sdks/typescript/src/client.ts:4080-4095](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L4080-L4095)

```text
channelId: string,
    idempotencyKey: string,
    mime: string,
    dataBase64: string,
```

  The image convenience method takes four positional arguments, including the required idempotency key.

- [sdks/typescript/src/client.ts:4108-4127](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L4108-L4127)

```text
channelId: string,
    codec: string,
    dataBase64: string,
    timestampMs: number,
```

  The video-frame convenience method likewise takes four positional arguments.

### Independent challenge

The raw liveSendInput method does accept a params object, but the two convenience helpers do not. Their complete declarations take positional arguments and lower them into the raw shape. Confirm only the two misdocumented convenience signatures.

- [sdks/typescript/src/client.ts:4080-4095](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L4080-L4095)

```text
channelId: string,
    idempotencyKey: string,
    mime: string,
    dataBase64: string,
```

  liveSendInputImage requires these four positional inputs.

- [sdks/typescript/src/client.ts:4108-4127](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L4108-L4127)

```text
channelId: string,
    codec: string,
    dataBase64: string,
    timestampMs: number,
```

  liveSendInputVideoFrame also uses four positional inputs, with a different semantic shape.

**Required correction:** Document liveSendInputImage(channelId, idempotencyKey, mime, dataBase64) and liveSendInputVideoFrame(channelId, codec, dataBase64, timestampMs). Preserve liveSendInput(params) as the valid raw params-object method.

### Correction

Changed native TypeScript liveSendInputImage and liveSendInputVideoFrame to their source-verified positional signatures while retaining liveSendInput(params) as the raw object API.

- [.claude/skills/meerkat-platform/references/api_reference.md:737-737](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L737)

```text
`liveSendInputImage(channelId, idempotencyKey, mime, dataBase64)`
```

### Final independent review

**PASS:** The two convenience signatures have the actual exported names and positional parameter ordering. The raw liveSendInput remains a params-object method, preserving the important distinction.

- [.claude/skills/meerkat-platform/references/api_reference.md:736-738](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L736-L738)

```text
`liveSendInputVideoFrame(channelId, codec, dataBase64, timestampMs)`
```

- [sdks/typescript/src/client.ts:4060-4063](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/typescript/src/client.ts#L4060-L4063)

```text
async liveSendInput(params: LiveSendInputParams)
```

- [sdks/typescript/src/client.ts:4080-4127](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/typescript/src/client.ts#L4080-L4127)

```text
async liveSendInputImage(
```

## A03-018: TypeScript Mob.sendMessage is not a public method

**Candidate severity:** medium. **Independent verdict:** confirm.

The listed call fails to compile and is undefined at runtime. listMembers is valid; only the send entry is wrong.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:741-741](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L741)

```text
`Mob.listMembers()` / `Mob.sendMessage(agentIdentity, message)`
```

### Implementation proof

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
import re
s = Path('sdks/typescript/src/mob.ts').read_text().split('export class Mob {', 1)[1]
print(', '.join(re.findall(r'^  (?:async )?([A-Za-z]\w*)\(', s, re.M)))
PY
```

  constructor, status, lifecycle, spawn, spawnMany, retire, grantScopes, revokeScopes, grants, memberHistory, hosts, routeInstalls, bindHost, revokeHost, hardCancel, memberLiveOpen, memberLiveClose, memberLiveStatus, memberLiveControl, respawn, forceCancel, memberStatus, waitForKickoffComplete, waitForReady, spawnHelper, forkHelper, wire, wireMembersBatch, unwire, listMembers, member, appendSystemContext, listFlows, runFlow, run, flowStatus, runResult, cancelFlow, subscribeMemberEvents, subscribeEvents, readEvents

  This is a complete declaration inventory of the concrete exported Mob class, which has no superclass, not a single unsuccessful symbol search.

- [sdks/typescript/src/mob.ts:231-237](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/mob.ts#L231-L237)

```text
return this.client.sendMobMemberContent(this.mobId, this.agentIdentity, content, options);
```

  Member.send is the actual content submission API.

- [sdks/typescript/src/mob.ts:443-445](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/mob.ts#L443-L445)

```text
return new Member(this.client, this.mobId, agentIdentity);
```

  Mob.member(identity) obtains that handle.

### Independent challenge

I inspected the complete concrete Mob class and enumerated its methods, rather than relying on a failed sendMessage search. It has no superclass or sendMessage member. The actual member wrapper supplies send and delegates to the client's member-content route; listMembers remains valid.

- [sdks/typescript/src/mob.ts:243-493](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/mob.ts#L243-L493)

```text
export class Mob {
```

  The complete public class is the authority for this negative claim. It exposes member(), listMembers(), lifecycle/flow/wiring helpers, but no sendMessage method or inherited extension providing one.

- [sdks/typescript/src/mob.ts:231-237](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/mob.ts#L231-L237)

```text
return this.client.sendMobMemberContent(this.mobId, this.agentIdentity, content, options);
```

  This is the implementation of the real Member.send operation.

- [sdks/typescript/src/mob.ts:443-445](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/mob.ts#L443-L445)

```text
return new Member(this.client, this.mobId, agentIdentity);
```

  Mob.member constructs the supported member handle used to send content.

**Required correction:** Replace Mob.sendMessage with mob.member(agentIdentity).send(content, options?) in the native TypeScript handle summary. Keep the valid listMembers method and distinguish Mob-level operations from Member-level operations.

### Correction

Removed nonexistent native Mob.sendMessage and documented Mob.member(agentIdentity) returning Member, with Member.send(content, options). Preserved Mob.listMembers and distinguished handle responsibilities.

- [.claude/skills/meerkat-platform/references/api_reference.md:774-774](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L774)

```text
`Member.send(content, options?)` — call `mob.member(agentIdentity).send(content, options?)`
```

### Final independent review

**PASS:** The reference now accurately separates Mob.listMembers and Mob.member from Member.send. Both the Member factory and send implementation were checked, including optional options and the content union.

- [.claude/skills/meerkat-platform/references/api_reference.md:773-774](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L773-L774)

```text
`Member.send(content, options?)`
```

- [sdks/typescript/src/mob.ts:231-237](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/typescript/src/mob.ts#L231-L237)

```text
return this.client.sendMobMemberContent
```

- [sdks/typescript/src/mob.ts:439-445](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/typescript/src/mob.ts#L439-L445)

```text
member(agentIdentity: string): Member
```

## A03-019: Browser OAuth resolver returns an unsupported bearer_token envelope

**Candidate severity:** high. **Independent verdict:** confirm.

The featured host-auth recipe cannot type-check against registerExternalAuthResolver, and bypassing TypeScript still fails at the WASM boundary.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:584-591](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L584-L591)

```text
return { kind: 'bearer_token', token };
```

### Implementation proof

- [sdks/web/src/auth.ts:95-118](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/auth.ts#L95-L118)

```text
kind: 'inline_secret';
      secret: string;
      metadata: ExternalAuthMetadata;
```

  The closed ExternalAuthLease union supports inline_secret, static_headers, dynamic_authorizer, and none. It has no bearer_token variant.

- [meerkat-web-runtime/src/external_auth.rs:242-254](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/external_auth.rs#L242-L254)

```text
serde_json::from_str::<meerkat_core::ResolvedAuthEnvelope>(&raw)
```

  The bridge deserializes and validates the typed envelope and reports unsupported credential envelope on an unknown variant.

### Independent challenge

A JavaScript type assertion cannot rescue the example: the wrapper performs no bearer_token alias conversion, and the Rust boundary deserializes the returned JSON into a closed tagged enum. Raw bearer/OAuth token material is supported, but through InlineSecret with its real field names and metadata, not the invented variant.

- [sdks/web/src/auth.ts:95-118](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/auth.ts#L95-L118)

```text
kind: 'inline_secret';
```

  The complete TypeScript envelope union exposes inline_secret, static_headers, dynamic_authorizer, and none, not bearer_token.

- [meerkat-web-runtime/src/external_auth.rs:242-254](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/external_auth.rs#L242-L254)

```text
serde_json::from_str::<meerkat_core::ResolvedAuthEnvelope>(&raw)
```

  The runtime actually validates this shape with serde; unsupported tags fail rather than being treated as bearer-token shorthands.

- [meerkat-core/src/auth/lease.rs:79-108](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/auth/lease.rs#L79-L108)

```text
InlineSecret {
        secret: String,
        metadata: AuthMetadata,
```

  The cross-target enum explicitly supports raw secrets including bearer/OAuth tokens and optional expiry through this variant.

**Required correction:** Replace the callback result with a valid envelope, for example { kind: 'inline_secret', secret: token, metadata: {} }, adding the actual expiry/provider metadata when required by the selected binding. Do not invent a bearer_token alias. Coordinate the snippet's separate binding-provisioning prerequisite with A03-020.

### Correction

Replaced the nonexistent bearer_token envelope with inline_secret, secret, and metadata, plus binding-specific expiry/provider metadata guidance; preserved the separate custom-host provisioning prerequisite.

- [.claude/skills/meerkat-platform/SKILL.md:648-658](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L648-L658)

```text
return { kind: 'inline_secret', secret: token, metadata: {} };
```

### Final independent review

**PASS:** The callback now returns the exact inline_secret discriminant with secret and metadata. This matches both the TypeScript union and Rust serde enum; expiry/provider metadata and the separate provisioning prerequisite are retained.

- [.claude/skills/meerkat-platform/SKILL.md:648-658](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L648-L658)

```text
return { kind: 'inline_secret', secret: token, metadata: {} };
```

- [sdks/web/src/auth.ts:95-105](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/src/auth.ts#L95-L105)

```text
kind: 'inline_secret';
```

- [meerkat-core/src/auth/lease.rs:79-93](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/auth/lease.rs#L79-L93)

```text
InlineSecret {
```

- [meerkat-web-runtime/src/external_auth.rs:242-255](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/external_auth.rs#L242-L255)

```text
serde_json::from_str::<meerkat_core::ResolvedAuthEnvelope>
```

## A03-020: Registering the browser auth callback is not sufficient to make an authBinding resolvable

**Candidate severity:** medium. **Independent verdict:** confirm.

Even after correcting the returned lease in A03-019, the example omits the binding/backend/auth-profile setup that causes the resolver to run. The preceding normal Web SDK initialization does not expose arbitrary realm config, so the text currently presents a bridge hook as an end-to-end OAuth bootstrap.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:582-594](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L582-L594)

```text
register an external resolver instead of shipping bare API keys to the browser
```

- [.claude/skills/meerkat-platform/SKILL.md:590-591](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L590-L591)

```text
const session = runtime.createSession(withAuthBinding(authBinding, { model: 'claude-sonnet-4-6' }));
```

### Implementation proof

- [meerkat-web-runtime/src/external_auth.rs:171-194](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/external_auth.rs#L171-L194)

```text
`CredentialSourceSpec::ExternalResolver { handle: "wasm_host" }`
```

  The bridge is selected by an already-configured binding whose credential source names this exact resolver handle; a callback is not a catch-all for arbitrary authBinding values.

- [sdks/web/src/auth.ts:136-144](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/auth.ts#L136-L144)

```text
selected binding's credential source is `external_resolver`.
```

  The public callback type documents the same prerequisite.

- [meerkat-web-runtime/src/lib.rs:238-266](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/lib.rs#L238-L266)

```text
struct RuntimeConfig {
```

  The public runtime bootstrap input contains provider API keys/base URLs, model, max_sessions, and mobpack trust, not a general realm configuration field.

- [meerkat-web-runtime/src/lib.rs:283-293](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/lib.rs#L283-L293)

```text
populate_realm_from_api_keys(&mut config, api_keys, base_urls);
```

  Normal bootstrap synthesizes key-backed configuration; registration alone does not replace those sources or install the named external binding.

### Independent challenge

Narrowed to the stock browser bootstrap shown in the skill. Resolver registration installs a callback, not a binding definition or a catch-all credential source. I traced resolution and bootstrap: only an ExternalResolver binding with handle wasm_host reaches this callback, while init_runtime synthesizes inline-key realm bindings from its restricted RuntimeConfig. A preconfigured/custom host can legitimately use the hook; the finding is not a claim that external auth is impossible everywhere.

- [meerkat-web-runtime/src/external_auth.rs:171-194](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/external_auth.rs#L171-L194)

```text
CredentialSourceSpec::ExternalResolver
```

  This bridge is selected for the configured external-resolver source and wasm_host handle, not for arbitrary unknown authBinding strings.

- [meerkat-auth-core/src/resolver.rs:41-77](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-auth-core/src/resolver.rs#L41-L77)

```text
CredentialSourceSpec::ExternalResolver
```

  The actual resolver dispatch keeps inline-secret resolution separate and only invokes external handles in the ExternalResolver branch.

- [meerkat-web-runtime/src/lib.rs:238-293](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/lib.rs#L238-L293)

```text
struct RuntimeConfig {
```

  The complete bootstrap config exposes keys/base URLs/model/session limits/mobpack trust, not arbitrary realm backend/auth/binding configuration. Its composition starts from Config::default and populates bindings from API keys.

- [meerkat-web-runtime/src/lib.rs:372-454](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/lib.rs#L372-L454)

```text
RealmConfigSection::from_inline_api_keys
```

  Stock bootstrap bindings are deliberately synthesized as inline-key sources; registering a callback does not mutate their source type.

**Required correction:** Describe registerExternalAuthResolver as a preconfigured/custom-host hook requiring a real matching backend/auth/binding whose credential source is external_resolver with handle wasm_host. State that the stock public runtime bootstrap shown here only synthesizes API-key bindings and does not offer arbitrary realm-config injection, so registration plus an otherwise undefined authBinding is not a working stock-browser OAuth recipe. Do not invent a realmConfig option or imply callback registration rewrites inline bindings.

### Correction

Scoped external-resolver registration to custom/preconfigured hosts with real backend/auth/binding definitions and external_resolver handle wasm_host. Explained that registration neither creates bindings nor replaces inline sources; stock init synthesizes API-key bindings and lacks arbitrary realm-config injection, so its proxy path remains the stock recommendation.

- [.claude/skills/meerkat-platform/SKILL.md:636-645](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L636-L645)

```text
auth profile, and binding whose credential source is `external_resolver` with
```

- [.claude/skills/meerkat-platform/SKILL.md:967-967](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L967)

```text
`authBinding` plus `registerExternalAuthResolver` requires a custom/preconfigured
```

### Final independent review

**PASS:** Both the browser section and repeated auth summary distinguish stock key/proxy bootstrap from a preconfigured custom-host resolver seam. The text now names the actual backend/auth/binding prerequisite and wasm_host handle, and does not invent a realmConfig option or callback-driven conversion of inline sources.

- [.claude/skills/meerkat-platform/SKILL.md:636-645](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L636-L645)

```text
handle `wasm_host`. Registration installs only the callback
```

- [.claude/skills/meerkat-platform/SKILL.md:966-968](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L966-L968)

```text
host with a matching external-resolver binding
```

- [meerkat-web-runtime/src/external_auth.rs:171-194](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/external_auth.rs#L171-L194)

```text
CredentialSourceSpec::ExternalResolver { handle: "wasm_host" }
```

- [meerkat-web-runtime/src/lib.rs:238-293](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/lib.rs#L238-L293)

```text
struct RuntimeConfig {
```

- [meerkat-web-runtime/src/lib.rs:427-454](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/lib.rs#L427-L454)

```text
RealmConfigSection::from_inline_api_keys(&entries)
```

## A03-021: Browser mob.spawn does not accept the advertised authBinding override

**Candidate severity:** medium. **Independent verdict:** confirm.

This conflates native SDK spawn capability with the smaller browser spawn contract. A typed call fails; untyped extra fields are not a usable override, and raw WASM rejects them.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:594-594](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L594)

```text
set it on `runtime.createSession({...})`, `mob.spawn(...)`, etc.
```

### Implementation proof

- [sdks/web/src/types.ts:310-324](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/types.ts#L310-L324)

```text
export interface SpawnSpec {
```

  The complete plain browser SpawnSpec has no authBinding/auth_binding property.

- [sdks/web/src/mob.ts:96-109](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/mob.ts#L96-L109)

```text
function spawnSpecPayload(spec: SpawnSpec): Record<string, unknown> {
```

  The SDK explicitly constructs a payload from its supported spawn fields rather than forwarding arbitrary extra auth properties.

- [meerkat-web-runtime/src/lib.rs:2128-2167](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/lib.rs#L2128-L2167)

```text
#[serde(deny_unknown_fields)]
```

  The authoritative WASM SpawnSpecInput also lacks auth_binding and rejects unknown fields for direct raw calls.

- [sdks/web/src/types.ts:467-478](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/types.ts#L467-L478)

```text
authBinding?: AuthBindingRef;
```

  Browser helper options do have an explicit authBinding override, unlike plain spawn.

### Independent challenge

I checked both sides of the browser boundary. Plain spawn lacks authBinding in its public input type, its payload builder uses an explicit allowlist, and raw WASM SpawnSpecInput rejects unknown fields. Helper spawn/fork paths are a real counterexample to an overly broad 'no browser member auth' claim: they do carry auth overrides.

- [sdks/web/src/types.ts:310-324](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/types.ts#L310-L324)

```text
export interface SpawnSpec {
```

  The complete plain-spawn type has no authBinding field.

- [sdks/web/src/mob.ts:96-109](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/mob.ts#L96-L109)

```text
function spawnSpecPayload(spec: SpawnSpec): Record<string, unknown> {
```

  The explicit lowering omits authBinding even if an untyped caller adds it.

- [meerkat-web-runtime/src/lib.rs:2128-2167](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/lib.rs#L2128-L2167)

```text
#[serde(deny_unknown_fields)]
```

  The complete raw spawn input also lacks auth_binding, so bypassing the TypeScript wrapper does not enable the advertised override.

- [meerkat-web-runtime/src/lib.rs:2610-2619](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/lib.rs#L2610-L2619)

```text
options.auth_binding = Some(auth_binding.into());
```

  The helper path does lower a supported auth override, preserving a narrower valid browser capability.

**Required correction:** Remove plain mob.spawn from the authBinding-override promise. Limit it to verified session creation and helper spawn/fork APIs (or auth configured on the role/profile), and explicitly identify the current plain SpawnSpec limitation.

### Correction

Limited browser authBinding overrides to session creation and helper spawn/fork APIs. Plain mob.spawn uses host/runtime credential resolution without a per-spawn override, and a mob role Profile is not an alternative initial-auth carrier. Custom-host resolver prerequisites remain.

- [.claude/skills/meerkat-platform/SKILL.md:657-665](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L657-L665)

```text
`mob.forkHelper(...)`; plain `mob.spawn([...])` specs currently have no auth
```

- [.claude/skills/meerkat-platform/SKILL.md:661-661](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L661)

```text
override and use the host/runtime credential resolution.
```

- [.claude/skills/meerkat-platform/SKILL.md:662-662](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L662)

```text
does not declare an initial `auth_binding`; select a preconfigured binding
```

### Final independent review

**PASS:** Rechecked the public SpawnSpec, its explicit payload allowlist and the closed raw WASM input: plain browser spawn still has no per-spawn auth override. The replacement now states host/runtime credential resolution and expressly rules out an initial auth_binding on the mob role Profile. Session creation and helper authBinding support are retained, and helper lowering forwards the binding into HelperOptions. The adjacent custom-host prerequisite still requires an already provisioned external-resolver binding and does not promise stock bootstrap can create one. The initial V03-R01 regression is resolved.

- [.claude/skills/meerkat-platform/SKILL.md:657-666](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L657-L666)

```text
override and use the host/runtime credential resolution.
```

- [.claude/skills/meerkat-platform/SKILL.md:661-663](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L661-L663)

```text
does not declare an initial `auth_binding`; select a preconfigured binding
```

- [.claude/skills/meerkat-platform/SKILL.md:636-643](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L636-L643)

```text
Registration installs only the callback, not those
```

- [sdks/web/src/types.ts:310-324](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/src/types.ts#L310-L324)

```text
export interface SpawnSpec {
```

- [sdks/web/src/mob.ts:96-109](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/src/mob.ts#L96-L109)

```text
function spawnSpecPayload(spec: SpawnSpec)
```

- [meerkat-web-runtime/src/lib.rs:2128-2167](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/lib.rs#L2128-L2167)

```text
#[serde(deny_unknown_fields)]
```

- [meerkat-mob/src/profile.rs:222-374](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/profile.rs#L222-L374)

```text
pub const FIELD_NAMES: &'static [&'static str] = &[
```

  Complete mob-role Profile authority has no initial auth_binding field; a realm auth profile is a different type.

- [sdks/web/src/types.ts:467-481](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/src/types.ts#L467-L481)

```text
authBinding?: AuthBindingRef;
```

- [meerkat-web-runtime/src/lib.rs:2422-2463](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/lib.rs#L2422-L2463)

```text
auth_binding: Option<meerkat_contracts::WireAuthBindingRef>,
```

- [meerkat-web-runtime/src/lib.rs:2612-2617](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/lib.rs#L2612-L2617)

```text
options.auth_binding = Some(auth_binding.into());
```

- [meerkat-web-runtime/src/lib.rs:2658-2662](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/lib.rs#L2658-L2662)

```text
options.auth_binding = Some(auth_binding.into());
```

- [meerkat-web-runtime/src/lib.rs:3176-3205](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/lib.rs#L3176-L3205)

```text
omitted_auth_binding_resolves_synthesized_global_realm
```

  Existing source-level regression test for the stock bootstrap's global binding resolution; not executed by this reviewer.

- [meerkat-web-runtime/src/external_auth.rs:171-194](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/external_auth.rs#L171-L194)

```text
CredentialSourceSpec::ExternalResolver { handle: "wasm_host" }
```

## A03-022: Browser export list includes nonexistent mob_run_result

**Candidate severity:** medium. **Independent verdict:** confirm.

Native RPC/SDK run-result support does not imply a corresponding wasm_bindgen export. A host following this list calls an undefined function.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/mobs.md:384-384](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L384)

```text
mob_run_flow → run_id string  [async] / mob_flow_status / mob_run_result / mob_cancel_flow  [async]
```

### Implementation proof

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
import re
for path in sorted(Path('meerkat-web-runtime/src').rglob('*.rs')):
    names = re.findall(r'^pub (?:async )?fn (mob_[a-z0-9_]+)', path.read_text(), re.M)
    if names:
        print(str(path) + ': ' + ', '.join(names))
PY
```

  meerkat-web-runtime/src/lib.rs: mob_create, mob_status, mob_list, mob_lifecycle, mob_events, mob_spawn, mob_retire, mob_wire, mob_wire_peer, mob_member_peer_target, mob_unwire, mob_unwire_peer, mob_list_members, mob_append_system_context, mob_member_send, mob_member_status, mob_respawn, mob_force_cancel, mob_spawn_helper, mob_fork_helper, mob_run_flow, mob_flow_status, mob_cancel_flow, mob_member_subscribe, mob_subscribe_events

  The full public mob-function inventory across the WASM source has the run/status/cancel family but no mob_run_result.

- [sdks/web/src/mob.ts:64-94](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/mob.ts#L64-L94)

```text
mob_flow_status: (mobId: string, runId: string) => Promise<string>;
```

  The closed MobWasmBindings interface likewise supplies flow status and cancel, not run_result.

- [meerkat-web-runtime/src/lib.rs:2706-2721](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/lib.rs#L2706-L2721)

```text
Returns a generated `MobFlowStatusResult` JSON envelope whose `run` field
```

  The supported status export returns run state and ledgers; it is the actual browser observation seam.

### Independent challenge

I enumerated all public mob functions in the WASM runtime and read the complete MobWasmBindings interface. Neither has mob_run_result; native RPC/client run-result support does not imply a browser export. The implemented browser status export returns the run envelope, which remains available.

- [sdks/web/src/mob.ts:64-94](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/web/src/mob.ts#L64-L94)

```text
mob_flow_status: (mobId: string, runId: string) => Promise<string>;
```

  The complete browser binding inventory has mob_run_flow, mob_flow_status, and mob_cancel_flow, but no run-result export.

- [meerkat-web-runtime/src/lib.rs:2706-2721](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-web-runtime/src/lib.rs#L2706-L2721)

```text
Returns a generated `MobFlowStatusResult` JSON envelope whose `run` field
```

  The actual wasm_bindgen status function is implemented and serializes that envelope; it is not named mob_run_result.

**Required correction:** Remove mob_run_result from the WASM exports list. Document the implemented mob_flow_status run envelope/browser flow-status API without renaming it, and leave native mob/run_result or native SDK runResult support explicitly surface-qualified.

### Correction

Removed mob_run_result from the WASM export inventory. Documented mob_flow_status's run envelope and Web SDK flowStatus unwrapping, while explicitly preserving native RPC mob/run_result and native SDK result helpers as separate contracts.

- [.claude/skills/meerkat-platform/references/mobs.md:404-413](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L404-L413)

```text
There is also no browser `mob_run_result` export: `mob_flow_status` returns a
```

### Final independent review

**PASS:** The browser export inventory no longer invents mob_run_result. Checked the full wasm_bindgen mob-export inventory and the actual status serializer and SDK parser: raw status is a run envelope, whereas flowStatus unwraps FlowStatus|null. Native run-result support remains explicitly separate.

- [.claude/skills/meerkat-platform/references/mobs.md:404-413](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L404-L413)

```text
There is also no browser `mob_run_result` export
```

- [meerkat-web-runtime/src/lib.rs:2706-2721](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-web-runtime/src/lib.rs#L2706-L2721)

```text
serde_json::json!({ "run": status })
```

- [sdks/web/src/mob.ts:1626-1630](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/web/src/mob.ts#L1626-L1630)

```text
async flowStatus(runId: string): Promise<FlowStatus | null>
```

## A03-023: Frame-flow reference and TOML example use the wrong node discriminator and structure

**Candidate severity:** high. **Independent verdict:** confirm.

The displayed loop example cannot deserialize, even before runtime validation: it uses type instead of kind, omits required dependency modes and step IDs, places step payloads in the wrong object, and uses an invalid condition representation.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:874-877](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L874-L877)

```text
`type = "step"`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:874-877](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L874-L877)

```text
`type = "repeat_until"`
```

- [.claude/skills/meerkat-platform/references/mobs.md:480-482](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L480-L482)

```text
nodes.check_quality = { type = "repeat_until", loop_id = "quality_loop", body = { nodes = { run_tests = { type = "step", role = "tester", message = "Run tests" } } }, until = "all_pass", max_iterations = 5, depends_on = [] }
```

### Implementation proof

- [meerkat-mob/src/definition.rs:263-297](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L263-L297)

```text
#[serde(tag = "kind", rename_all = "snake_case")]
```

  FlowNodeSpec is tagged by kind. FrameStepSpec requires step_id, depends_on, and depends_on_mode; a node does not itself carry role/message. RepeatUntilSpec also requires depends_on_mode.

- [meerkat-mob/src/definition.rs:196-224](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L196-L224)

```text
#[serde(tag = "op", rename_all = "snake_case")]
```

  until is a typed ConditionExpr such as {op = "eq", path = ..., value = ...}, not the bare string "all_pass".

- [meerkat-mob/src/definition.rs:424-442](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L424-L442)

```text
pub role: ProfileName,
    pub message: ContentInput,
```

  Role and message belong in FlowStepSpec entries under flows.<id>.steps, referenced by frame-node step_id.

### Independent challenge

This is more than a discriminator typo. I parsed the actual TOML example read-only: it has no flow step definitions, embeds role/message inside frame nodes, omits dependency-mode fields, and supplies a string until condition. The authoritative serde definitions require a kind-tagged node referencing a separately defined step and an op-tagged condition. A JSON condition also requires the step's output to be parsed as JSON; merely changing 'until' would leave a broken example.

- [meerkat-mob/src/definition.rs:263-297](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L263-L297)

```text
#[serde(tag = "kind", rename_all = "snake_case")]
```

  FlowNodeSpec uses kind, and its complete step/repeat payloads require step_id or loop_id/body plus dependency fields.

- [meerkat-mob/src/definition.rs:196-224](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L196-L224)

```text
#[serde(tag = "op", rename_all = "snake_case")]
```

  ConditionExpr is a typed expression such as {op="eq", path="steps.run_tests.all_pass", value=true}, not the string "all_pass".

- [meerkat-mob/src/definition.rs:424-475](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L424-L475)

```text
pub role: ProfileName,
    pub message: ContentInput,
```

  These belong to FlowStepSpec definitions. The inspected effective_output_format implementation defaults schema-less steps to Text, so a JSON predicate needs output_format="json" or a schema.

- [meerkat-mob/src/runtime/path.rs:9-76](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/path.rs#L9-L76)

```text
Some("steps") => {
```

  The actual step-output path resolver accepts the steps.<step_id> path and optional output alias; the corrected condition must address the declared run_tests output.

**Required correction:** Replace type="step"/"repeat_until" with kind in both references. Define flows.release_flow.steps.run_tests and .ship with their role/message fields; make frame step nodes reference step_id and include depends_on plus depends_on_mode="all". Give the repeat node loop_id, body, dependency fields, max_iterations, and until={op="eq",path="steps.run_tests.all_pass",value=true}. Set run_tests output_format="json" (or a valid schema) and request the matching {"all_pass":boolean} output. Keep ship dependent on the loop node.

### Correction

Replaced type-tagged/inlined frame instructions with kind-tagged nodes referencing named step definitions and all required dependency fields. Added a complete minimal reference flow and a complete release loop using typed eq condition, JSON all_pass output, nested body, max_iterations, and ship depending on check_quality.

- [.claude/skills/meerkat-platform/references/api_reference.md:944-965](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L944-L965)

```text
`kind = "step"` — references a named `step_id`; supply `depends_on` and
```

- [.claude/skills/meerkat-platform/references/mobs.md:520-547](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L520-L547)

```text
until = { op = "eq", path = "steps.run_tests.all_pass", value = true }
```

### Final independent review

**PASS:** Both complete replacement flow fences parse as TOML. Checked every named step reference, required dependency/mode field, kind/op tag, loop body/id/bound and JSON output contract against the serde owners. The release loop's ship node still depends on check_quality and its cross-reference resolves.

- [.claude/skills/meerkat-platform/references/api_reference.md:955-976](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L955-L976)

```text
kind = "step"
```

- [.claude/skills/meerkat-platform/references/mobs.md:516-547](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L516-L547)

```text
until = { op = "eq", path = "steps.run_tests.all_pass", value = true }
```

- [meerkat-mob/src/definition.rs:196-224](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/definition.rs#L196-L224)

```text
#[serde(tag = "op", rename_all = "snake_case")]
```

- [meerkat-mob/src/definition.rs:263-297](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/definition.rs#L263-L297)

```text
#[serde(tag = "kind", rename_all = "snake_case")]
```

- [meerkat-mob/src/definition.rs:424-475](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/definition.rs#L424-L475)

```text
pub fn effective_output_format(&self) -> StepOutputFormat
```

## A03-024: Flow execution no longer switches between flat and frame engines

**Candidate severity:** low. **Independent verdict:** confirm.

Flat steps remain a supported authoring syntax, but not a second runtime engine. The versioned explanation presents an obsolete runtime branch and obscures where canonicalization occurs.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/mobs.md:457-457](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L457)

```text
When `root` is present, the `FlowFrameEngine` drives execution instead of flat topological-sort dispatch.
```

- [.claude/skills/meerkat-platform/references/api_reference.md:868-878](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L868-L878)

```text
When `[flows.<flow_id>].root` is present, the flow uses frame-based execution via `FlowFrameEngine`.
```

### Implementation proof

- [meerkat-mob/src/definition.rs:478-505](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L478-L505)

```text
let root = root.unwrap_or_else(|| FrameSpec::from_flat_steps(&steps));
```

  FlowSpec always contains a canonical FrameSpec. The constructor compiles omitted authored roots from flat steps.

- [meerkat-mob/src/definition.rs:509-527](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L509-L527)

```text
Ok(Self::new(de.description, de.steps, de.root))
```

  Deserialization goes through the canonicalizing constructor, so the runtime never receives a separate root-absent flat execution representation.

### Independent challenge

Flat authoring syntax remains supported, so it must not be 'fixed' by deleting it. The obsolete distinction is runtime engine selection. FlowSpec normalizes a missing authored root into a canonical FrameSpec once, and actual execution always constructs FlowFrameEngine. Explicit root precedence still holds.

- [meerkat-mob/src/definition.rs:478-527](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L478-L527)

```text
let root = root.unwrap_or_else(|| FrameSpec::from_flat_steps(&steps));
```

  Deserialization calls this constructor, so both authoring forms produce one mandatory canonical root.

- [meerkat-mob/src/runtime/flow.rs:139-175](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/flow.rs#L139-L175)

```text
.execute_frame(&run_id, &frame_id, root_spec, &context)
```

  The production execution path uses this engine for the normalized flow, with no alternative flat engine selected by authored-root presence.

**Required correction:** Describe flat steps and explicit root as two supported authoring forms that normalize to one frame-based execution engine. Remove 'root selects the frame engine instead of flat dispatch' wording, while retaining explicit-root precedence and valid flat examples. Do not modernize the separately labeled historical 0.4x-to-0.5 guide.

### Correction

Unified current flow guidance around one canonical frame execution engine. Flat authoring normalizes at decode/construction; explicit roots take precedence without selecting a different runtime. Preserved valid flat authoring and the unchanged historical 0.4x-to-0.5 migration guide.

- [.claude/skills/meerkat-platform/SKILL.md:680-680](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L680)

```text
Both flat step authoring and explicit `root` authoring normalize to one frame execution engine.
```

- [.claude/skills/meerkat-platform/references/api_reference.md:929-929](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L929)

```text
authoring normalizes once at decode/construction to the canonical root frame;
```

- [.claude/skills/meerkat-platform/references/mobs.md:491-495](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L491-L495)

```text
generated structure. These are two authoring forms, not separate engines.
```

### Final independent review

**PASS:** All three current flow summaries now describe two authoring forms and one engine. Explicit-root precedence and useful flat authoring are retained, and the separately historical migration reference is unchanged.

- [.claude/skills/meerkat-platform/SKILL.md:680-680](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L680)

```text
normalize to one frame execution engine
```

- [.claude/skills/meerkat-platform/references/api_reference.md:928-950](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L928-L950)

```text
different execution engine.
```

- [.claude/skills/meerkat-platform/references/mobs.md:491-495](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L491-L495)

```text
These are two authoring forms, not separate engines.
```

- [meerkat-mob/src/definition.rs:497-527](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/definition.rs#L497-L527)

```text
root.unwrap_or_else(|| FrameSpec::from_flat_steps(&steps))
```

- [meerkat-mob/src/runtime/flow.rs:166-175](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/flow.rs#L166-L175)

```text
FlowFrameEngine::new(
```

## A03-025: Topology mode permissive is not a valid domain value

**Candidate severity:** medium. **Independent verdict:** confirm.

Users copying the advertised topology value get a parse error. This appears to conflate topology policy with mobpack trust policy, where permissive is valid.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:608-608](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L608)

```text
topology rules (`strict|permissive`, `"*"` wildcard)
```

- [.claude/skills/meerkat-platform/references/api_reference.md:881-881](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L881)

```text
`[topology] mode = "strict"|"permissive"`
```

### Implementation proof

- [meerkat-mob/src/definition.rs:529-537](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L529-L537)

```text
pub enum PolicyMode {
    #[default]
    Advisory,
    Strict,
}
```

  The closed snake_case serde enum accepts advisory and strict; permissive is not a variant.

### Independent challenge

The closed domain enum accepts advisory and strict, not permissive. Narrow the proposed default claim: Advisory is the Rust Default variant, but TopologySpec.mode has no serde default annotation, so this alone does not establish that mode may be omitted from an explicitly supplied topology table. Also, mobpack trust-policy permissive is unrelated and remains valid.

- [meerkat-mob/src/definition.rs:529-553](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/definition.rs#L529-L553)

```text
pub enum PolicyMode {
    #[default]
    Advisory,
    Strict,
}
```

  The snake_case enum is exhaustive, and the adjacent TopologySpec declaration requires its mode field without a serde omission default.

**Required correction:** Change topology mode documentation to advisory | strict and use an explicit mode="advisory" where a permissive policy is intended. If mentioning a default, distinguish the Rust enum default from required TOML/JSON fields; do not claim supplied topology tables can omit mode without separate proof. Leave mobpack --trust-policy permissive unchanged.

### Correction

Corrected topology policy to advisory|strict in both current summaries, requiring explicit mode in a supplied topology table and distinguishing Rust enum defaulting. Left all unrelated permissive mobpack trust-policy examples intact.

- [.claude/skills/meerkat-platform/SKILL.md:680-680](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L680)

```text
topology rules (`advisory|strict`, `"*"` wildcard)
```

- [.claude/skills/meerkat-platform/references/api_reference.md:981-981](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L981)

```text
`[topology] mode = "advisory"|"strict"`
```

### Final independent review

**PASS:** Both topology occurrences use advisory|strict. The API reference correctly distinguishes the Rust enum's default from a required field in an explicitly supplied topology table. The unrelated permissive mobpack trust examples remain present.

- [.claude/skills/meerkat-platform/SKILL.md:680-680](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L680)

```text
topology rules (`advisory|strict`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:981-983](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L981-L983)

```text
that required serialized field optional.
```

- [meerkat-mob/src/definition.rs:529-553](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/definition.rs#L529-L553)

```text
pub mode: PolicyMode,
```

## A03-026: Agent-facing mob roster omits conclude_objective, fork_off, and council

**Candidate severity:** low. **Independent verdict:** confirm.

The reference explicitly claims a ten-tool roster and the other two guides reproduce that roster plus only six optional tools. This hides current agent-facing orchestration/terminality mechanisms rather than merely omitting operator-only administration.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:887-900](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L887-L900)

```text
`AgentMobToolSurface` provides 10 agent-internal mob tools:
```

- [.claude/skills/meerkat-platform/SKILL.md:376-376](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L376)

```text
Agent-facing delegation tools (`delegate`, `mob_create`, `mob_destroy`, `mob_spawn_member`, `mob_retire_member`, `mob_check_member`, `mob_list_members`, `mob_list`, `mob_wire`, `mob_unwire`)
```

- [.claude/skills/meerkat-platform/references/mobs.md:494-510](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L494-L510)

```text
When a realm profile store is configured, six additional profile-management tools are surfaced
```

### Implementation proof

- [meerkat-mob-mcp/src/agent_tools.rs:54-73](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L54-L73)

```text
const TOOL_CONCLUDE_OBJECTIVE: &str = "conclude_objective";
```

  The implementation defines conclude_objective, fork_off, and council in addition to all ten tools in the guide.

- [meerkat-mob-mcp/src/agent_tools.rs:3038-3089](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L3038-L3089)

```text
"agent-facing roster (minimal composition) must stay exactly this set"
```

  The complete roster contract test pins 13 base tools and 19 in the profile-enabled composition. Both lists include all three omitted tools.

### Independent challenge

The three names are implemented tools, not dead constants or aspirational documentation: production dispatch and definition construction include them, and the roster tests pin their presence. Narrow '13 tools' to the base composition inventory, not a promise that every ordinary agent receives or can exercise every tool. Their objective, fork-snapshot, and authority prerequisites matter.

- [meerkat-mob-mcp/src/agent_tools.rs:1856-1884](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L1856-L1884)

```text
TOOL_COUNCIL => self.dispatch_council_boxed(call).await,
```

  The same production dispatch match routes conclude_objective and fork_off; these are callable implementation paths rather than unused names.

- [meerkat-mob-mcp/src/agent_tools.rs:2088-2240](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L2088-L2240)

```text
TOOL_FORK_OFF
```

  The production tool-definition builder describes conclude_objective, durable committed-prefix fork_off, and temporary multi-specialist council semantics.

- [meerkat-mob-mcp/src/agent_tools.rs:3038-3089](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob-mcp/src/agent_tools.rs#L3038-L3089)

```text
agent-facing roster (minimal composition) must stay exactly this set
```

  The inspected test inventories pin 13 base definitions and 19 with profile tools; tests were read, not executed.

**Required correction:** Add conclude_objective, fork_off, and council to all three owned agent-tool rosters with concrete semantics: conclude a pre-addressed kickoff objective; fork a durable committed-prefix child through the resume path and retain the child member; run a bounded temporary specialist council with cleanup. Correct any inventory counts while qualifying composition/authority availability. Do not equate fork_off with the rendered-context launch mode in A03-027.

### Correction

All three rosters now list exactly thirteen base tools, adding conclude_objective, durable retained-child fork_off, and bounded temporary council with cleanup. Documented concrete arguments and objective/durable-member/source-scope admission; five profile-store tools plus a sixth only with snapshot support; visibility never bypasses generated authority or per-call checks.

- [.claude/skills/meerkat-platform/SKILL.md:391-405](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L391-L405)

```text
`mob_spawn_member`, `fork_off`, `council`, `mob_retire_member`,
```

- [.claude/skills/meerkat-platform/references/api_reference.md:978-1002](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L978-L1002)

```text
With generated authority, `AgentMobToolSurface` exposes thirteen base
```

- [.claude/skills/meerkat-platform/references/mobs.md:578-607](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L578-L607)

```text
`council` requires creation authority and scope over each source mob.
```

### Final independent review

**PASS:** Independently enumerated thirteen base definitions and checked all three repaired inventories. New-tool descriptions agree with their argument structs and dispatch: pre-addressed objective conclusion, durable retained child fork, and bounded council of source-member forks. Five profile tools and the separately snapshot-gated sixth are correctly distinguished.

- [.claude/skills/meerkat-platform/SKILL.md:391-405](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L391-L405)

```text
has thirteen base definitions
```

- [.claude/skills/meerkat-platform/references/api_reference.md:989-1013](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L989-L1013)

```text
`conclude_objective`
```

- [.claude/skills/meerkat-platform/references/mobs.md:556-608](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L556-L608)

```text
A profile store alone does not expose it.
```

- [meerkat-mob-mcp/src/agent_tools.rs:2088-2310](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-mcp/src/agent_tools.rs#L2088-L2310)

```text
TOOL_COUNCIL
```

- [meerkat-mob-mcp/src/agent_tools.rs:2449-2509](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-mcp/src/agent_tools.rs#L2449-L2509)

```text
struct CouncilArgs
```

- [meerkat-mob-mcp/src/agent_tools.rs:2403-2416](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-mcp/src/agent_tools.rs#L2403-L2416)

```text
if has_profile_store && has_snapshot_provider
```

## A03-027: API reference conflates mob fork context seeding with copy-on-write Session::fork

**Candidate severity:** medium. **Independent verdict:** confirm.

Consumers cannot rely on mob fork to preserve the exact source transcript structure, media blocks, session state, or copy-on-write semantics of the separate library primitive. The main platform guide already makes the correct distinction, but this assigned reference does not.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:841-841](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L841)

```text
`MemberLaunchMode::Fork` provides history branching via `Session::fork()`.
```

### Implementation proof

- [meerkat-mob/src/runtime/actor.rs:28363-28378](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L28363-L28378)

```text
Some(render_fork_context(&source_member_id, &rows))
```

  The mob fork branch reads source history rows and renders them as context rather than cloning the source Session.

- [meerkat-mob/src/runtime/actor.rs:28424-28460](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L28424-L28460)

```text
session_origin: super::provisioner::ProvisionSessionOrigin::Fresh,
```

  Rendered history is prepended to the initial prompt and provisioned as a fresh session.

- [meerkat-mob/src/runtime/actor.rs:4784-4795](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L4784-L4795)

```text
WireContentBlock::Text { text } => Some(text.as_str()),
```

  The renderer selects text from content blocks, illustrating why this is not an exact typed/multimodal transcript branch.

### Independent challenge

I followed the specific MemberLaunchMode::Fork path, rather than assuming every function named fork has the same semantics. It maps to fork_spec, reads source history, renders that history into prompt context, and provisions a Fresh session. That is not Session::fork copy-on-write canonical state. Separate real Session::fork/fork_at primitives and the newer durable fork_off tool remain valid.

- [meerkat-mob/src/runtime/actor.rs:27928-27940](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L27928-L27940)

```text
crate::launch::MemberLaunchMode::Fork {
```

  This actual launch-mode match produces the fork_spec consumed by the subsequent context-rendering path.

- [meerkat-mob/src/runtime/actor.rs:28363-28378](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L28363-L28378)

```text
Some(render_fork_context(&source_member_id, &rows))
```

  Source rows are rendered as context rather than installed as a forked canonical session.

- [meerkat-mob/src/runtime/actor.rs:28424-28460](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L28424-L28460)

```text
session_origin: super::provisioner::ProvisionSessionOrigin::Fresh,
```

  The rendered context is prepended to the new prompt, and provisioning expressly uses Fresh.

**Required correction:** Describe this MemberLaunchMode::Fork lane as a fresh member/session seeded with rendered source-history context, not an O(1) CoW transcript clone. Keep Session::fork/fork_at and durable fork_off semantics separately scoped so the correction does not deny genuine transcript-fork APIs.

### Correction

Corrected MemberLaunchMode::Fork/fork_helper to fresh-session rendered-history prompt seeding. Separately retained genuine low-level Session::fork/fork_at structural semantics and fork_off's durable committed-prefix resume path.

- [.claude/skills/meerkat-platform/references/api_reference.md:913-913](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L913)

```text
seeded with rendered source-history context, not an O(1) copy-on-write
```

- [.claude/skills/meerkat-platform/references/mobs.md:581-585](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L581-L585)

```text
`MemberLaunchMode::Fork` / `fork_helper`, which seed a fresh session's prompt
```

### Final independent review

**PASS:** The specific MemberLaunchMode::Fork/fork_helper lane is now described as fresh-session rendered context rather than structural CoW. Genuine Session fork primitives and durable fork_off remain explicitly supported and separate.

- [.claude/skills/meerkat-platform/references/api_reference.md:911-919](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L911-L919)

```text
seeded with rendered source-history context
```

- [meerkat-mob/src/runtime/actor.rs:28363-28378](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L28363-L28378)

```text
Some(render_fork_context(&source_member_id, &rows))
```

- [meerkat-mob/src/runtime/actor.rs:28424-28460](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L28424-L28460)

```text
ProvisionSessionOrigin::Fresh
```

- [meerkat-mob-mcp/src/agent_tools.rs:1230-1253](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob-mcp/src/agent_tools.rs#L1230-L1253)

```text
.fork_member_then_run_bounded(
```

## A03-028: Spawn startup policy omits the fresh autonomous member's automatic kickoff

**Candidate severity:** medium. **Independent verdict:** confirm.

Deferred session creation is accurate, but it does not mean an ordinary autonomous spawn remains idle until the caller sends a separate external turn or flow. Activation admits the kickoff automatically, potentially performing provider work before those later dispatches.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/mobs.md:313-318](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L313-L318)

```text
First model work is triggered by real dispatch (`external_turn`, peer message, or flow step).
```

### Implementation proof

- [meerkat-mob/src/runtime/actor.rs:28410-28438](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L28410-L28438)

```text
ContentInput::from(self.fallback_spawn_prompt(&profile_name, &agent_identity))
```

  Fresh spawn prepares a kickoff prompt even when initial_message is omitted; autonomous mode is carried into the pending spawn.

- [meerkat-mob/src/runtime/actor/spawn_activation.rs:2707-2731](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor/spawn_activation.rs#L2707-L2731)

```text
SpawnActivationStage::AutonomousKickoff,
```

  After runtime readiness, fresh autonomous activation dispatches the prepared kickoff itself. Only the explicit suppress_autonomous_initial_prompt recovery case skips it.

- [meerkat-mob/src/runtime_mode.rs:4-12](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime_mode.rs#L4-L12)

```text
#[default]
    AutonomousHost,
```

  This affects the default member mode, not an obscure opt-in mode.

### Independent challenge

Narrowed: deferred creation and 'real dispatch' are not false in themselves; automatic kickoff is itself a real admitted runtime prompt. What is misleading is enumerating only later external turns, peer messages, or flow steps as the source of first work. For a fresh runtime-backed AutonomousHost spawn, activation schedules its own kickoff, using a fallback prompt when initial_message is absent. TurnDriven and recovery-suppression paths must remain distinguished.

- [meerkat-mob/src/runtime_mode.rs:4-12](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime_mode.rs#L4-L12)

```text
#[default]
    AutonomousHost,
```

  AutonomousHost, not TurnDriven, is the default runtime mode.

- [meerkat-mob/src/runtime/actor.rs:28410-28438](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor.rs#L28410-L28438)

```text
ContentInput::from(self.fallback_spawn_prompt(&profile_name, &agent_identity))
```

  The absence of initial_message does not mean the activation state has no kickoff prompt.

- [meerkat-mob/src/runtime/actor/spawn_activation.rs:2650-2780](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mob/src/runtime/actor/spawn_activation.rs#L2650-L2780)

```text
SpawnActivationStage::AutonomousKickoff,
```

  The actual activation sequence reaches autonomous kickoff after readiness unless suppressed, and constructs Input::Prompt from state.prompt for runtime admission.

**Required correction:** Keep the deferred-create/readiness explanation, but explicitly add that fresh runtime-backed autonomous spawns automatically admit an initial kickoff after activation, using the supplied initial message or fallback prompt. Explain that this automatic prompt is real runtime dispatch and can cause provider work without a subsequent user call. Preserve separate TurnDriven behavior and recovery suppression; do not promise every deferred spawn stays idle.

### Correction

Preserved deferred creation and readiness while explicitly documenting automatic fresh autonomous kickoff using initial_message or fallback, which can call a provider without a later user action. Kept distinct TurnDriven initial-message behavior and resumed-transcript recovery suppression.

- [.claude/skills/meerkat-platform/SKILL.md:384-389](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L384-L389)

```text
runtime-backed autonomous members automatically admit a kickoff using the
```

- [.claude/skills/meerkat-platform/references/mobs.md:328-338](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L328-L338)

```text
automatically admit an initial kickoff after activation/readiness.
```

### Final independent review

**PASS:** The main guide and startup policy retain deferred registration but disclose fresh autonomous kickoff/provider work without a later user call. Supplied-versus-fallback prompt, turn-driven behavior and recovery-suppressed kickoff remain separate.

- [.claude/skills/meerkat-platform/SKILL.md:384-389](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L384-L389)

```text
invoke the provider without another user call.
```

- [.claude/skills/meerkat-platform/references/mobs.md:328-338](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/mobs.md?plain=1#L328-L338)

```text
authoritative transcript suppresses a manufactured autonomous kickoff.
```

- [meerkat-mob/src/runtime/actor.rs:28424-28438](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor.rs#L28424-L28438)

```text
self.fallback_spawn_prompt(&profile_name, &agent_identity)
```

- [meerkat-mob/src/runtime/actor/spawn_activation.rs:2707-2779](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mob/src/runtime/actor/spawn_activation.rs#L2707-L2779)

```text
if pending.state.suppress_autonomous_initial_prompt
```

## A03-029: Live guidance incorrectly requires starting a turn before opening an idle session

**Candidate severity:** medium. **Independent verdict:** confirm.

The instruction can cause unnecessary paid/model work and obstruct the supported defer-then-live-open lifecycle. Session existence and a valid live-capable identity are necessary; an active or already-completed turn is not universally required.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:440-440](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L440)

```text
Idle sessions can't host a channel — start a turn or spawn the member first.
```

### Implementation proof

- [meerkat/src/session_runtime/live_orchestration.rs:965-969](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/live_orchestration.rs#L965-L969)

```text
without running a turn, so realtime-open paths can find it.
```

  The live orchestration layer explicitly materializes staged/deferred sessions for open without running model work.

- [meerkat/src/session_runtime/live_orchestration.rs:1111-1126](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/live_orchestration.rs#L1111-L1126)

```text
Box::pin(self.materialize_staged_session_for_realtime_open(session_id)).await?;
```

  The actual recovery/open preparation invokes that promotion for staged sessions and also supports persisted-session recovery.

- [meerkat/src/session_runtime/live_orchestration.rs:1015-1025](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/live_orchestration.rs#L1015-L1025)

```text
a deferred session created with injected_context cannot be promoted by
```

  There is a narrower real exception: deferred injected context requires turn/start promotion, not a blanket ban on idle sessions.

### Independent challenge

I traced the ordinary realtime open projection, not just a helper comment. It actively recovers or materializes a staged session before reading its snapshot and identity, and the staged request uses InitialTurnPolicy::Defer. Thus no prior model turn is universally required. This does not imply every idle/deferred session is eligible: injected_context has an explicit start-turn restriction, and provider/transport/channel admission still applies.

- [meerkat/src/session_runtime/live_orchestration.rs:1176-1208](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/live_orchestration.rs#L1176-L1208)

```text
Box::pin(self.recover_live_session_for_realtime_open(session_id)).await?;
```

  The production realtime open path invokes recovery/promotion before exporting the provider seed and identity.

- [meerkat/src/session_runtime/live_orchestration.rs:1111-1126](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/live_orchestration.rs#L1111-L1126)

```text
Box::pin(self.materialize_staged_session_for_realtime_open(session_id)).await?;
```

  Recovery includes staged-session materialization without running an initial model turn.

- [meerkat/src/session_runtime/live_orchestration.rs:1015-1025](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/live_orchestration.rs#L1015-L1025)

```text
a deferred session created with injected_context cannot be promoted by
```

  This real exception prevents overcorrecting the documentation into a universal deferred-session guarantee.

**Required correction:** Replace 'idle sessions cannot host a live channel; start a turn first' with eligible existing idle/deferred sessions being recoverable/promotable during live/open without a model turn. Retain realtime/provider/transport and channel-ownership prerequisites, and explicitly note that deferred injected_context requires the documented turn-start path.

### Correction

Replaced the blanket idle-session refusal with live/open recovery/promotion for eligible existing idle/deferred sessions without a model turn. Preserved provider/transport/ownership admission and the deferred injected_context requirement to use turn/start first.

- [.claude/skills/meerkat-platform/SKILL.md:490-495](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L490-L495)

```text
`live/open` can recover an eligible existing idle session or promote a
```

### Final independent review

**PASS:** The blanket idle-session rejection is gone. Recovery/materialization before open is accurately described without promising universal eligibility; deferred injected_context is explicitly routed through turn/start rather than silently lost.

- [.claude/skills/meerkat-platform/SKILL.md:490-495](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L490-L495)

```text
deferred session without a prior model turn
```

- [meerkat/src/session_runtime/live_orchestration.rs:1111-1126](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/session_runtime/live_orchestration.rs#L1111-L1126)

```text
self.materialize_staged_session_for_realtime_open(session_id)
```

- [meerkat/src/session_runtime/live_orchestration.rs:1015-1025](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/session_runtime/live_orchestration.rs#L1015-L1025)

```text
if !deferred_injected_context.is_empty()
```

- [meerkat/src/session_runtime/live_orchestration.rs:1178-1208](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/session_runtime/live_orchestration.rs#L1178-L1208)

```text
self.recover_live_session_for_realtime_open(session_id)
```

## A03-030: Live catalog guidance misses GPT Live and its distinct admission prerequisites

**Candidate severity:** medium. **Independent verdict:** confirm.

The asserted catalog exclusivity is false. More importantly, treating every realtime=true row as the same legacy WebSocket flow would break integrations with the public GPT Live feature.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:381-381](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L381)

```text
today the only catalog realtime row is `gpt-realtime-2`
```

### Implementation proof

- [meerkat-models/src/capabilities/openai.rs:609-641](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/capabilities/openai.rs#L609-L641)

```text
id: "gpt-live-1",
```

  The canonical capabilities include a stable public GPT Live row with realtime = true, provider-managed turns, and no explicit-commit support.

- [meerkat-models/src/capabilities/openai.rs:565-582](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/capabilities/openai.rs#L565-L582)

```text
id: "gpt-live-1-codex",
```

  There is also a separately experimental realtime row, not an ordinary public Realtime model.

- [meerkat-models/src/catalog.rs:143-150](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/catalog.rs#L143-L150)

```text
capabilities: capabilities::capability_table(),
```

  These rows feed the canonical catalog rather than existing only in disconnected fixtures.

- [meerkat/src/session_runtime/live_orchestration.rs:2152-2176](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/live_orchestration.rs#L2152-L2176)

```text
if requested_transport != Some(LiveOpenTransport::Webrtc) {
```

  The execution-identity Live path is feature-gated openai-live and requires WebRTC; merely replacing a Realtime session's model is not the documented public Live admission path.

### Independent challenge

The canonical capability table now contains more realtime-capable rows than gpt-realtime-2. However, a realtime capability bit does not make all rows interchangeable in the existing WebSocket recipe. Public gpt-live-1 has its own feature/transport/registered execution-profile path; gpt-live-1-codex is an experimental route. I checked both catalog inclusion and actual admission wiring.

- [meerkat-models/src/capabilities/openai.rs:609-650](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/capabilities/openai.rs#L609-L650)

```text
id: "gpt-live-1",
```

  This canonical stable/supported row is realtime-capable and advertises provider-managed turn support.

- [meerkat-models/src/capabilities/openai.rs:565-598](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-models/src/capabilities/openai.rs#L565-L598)

```text
id: "gpt-live-1-codex",
```

  This additional realtime-capable row is explicitly experimental, not a default substitute for the public route.

- [meerkat/src/session_runtime/live_orchestration.rs:2152-2176](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/session_runtime/live_orchestration.rs#L2152-L2176)

```text
if requested_transport != Some(LiveOpenTransport::Webrtc) {
```

  The inspected execution-identity open implementation is feature-gated and requires the WebRTC path.

- [meerkat-contracts/src/wire/live.rs:126-182](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-contracts/src/wire/live.rs#L126-L182)

```text
pub profile_id: String,
```

  The versioned execution_identity selects a host-registered profile that owns model/provider/auth/capabilities; clients do not inject an arbitrary complete identity into this selector.

**Required correction:** Scope the existing gpt-realtime-2 example to the ordinary OpenAI Realtime path instead of claiming it is the only realtime catalog row. Add public gpt-live-1 with opt-in openai-live, WebRTC, supported provider-managed behavior, negotiated execution-identity capability, and a host-registered profile selected by execution_identity { version: 'v1', profile_id: ... }. Identify gpt-live-1-codex as experimental/private-path guidance rather than a drop-in replacement. Do not imply that changing only the model name enables GPT Live.

### Correction

Scoped gpt-realtime-2 to ordinary Realtime and separately described public gpt-live-1 and experimental gpt-live-1-codex. Added public openai-live/WebRTC/API-key backend/provider-managed-turn prerequisites, negotiated execution identity and host-registered profile selection, and warned that neither a model-name swap nor compiling the feature creates host authority.

- [.claude/skills/meerkat-platform/SKILL.md:410-430](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L410-L430)

```text
`execution_identity: { version: "v1", profile_id: "<host-registered-profile>" }`.
```

- [.claude/skills/meerkat-platform/SKILL.md:483-485](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L483-L485)

```text
GPT Live follows its advertised provider-managed turn policy instead.
```

### Final independent review

**PASS:** The live catalog correction includes actual admission constraints, not just extra model names: public feature, plain OpenAI API secret material, WebRTC, provider-managed turns, negotiated execution-profile authority and a typed selector. Ordinary Realtime and experimental Codex remain distinct, including adjacent commit guidance.

- [.claude/skills/meerkat-platform/SKILL.md:410-430](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L410-L430)

```text
`execution_identity: { version: "v1", profile_id: "<host-registered-profile>" }`
```

- [meerkat-models/src/capabilities/openai.rs:565-655](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-models/src/capabilities/openai.rs#L565-L655)

```text
id: "gpt-live-1",
```

- [meerkat/src/session_runtime/live_orchestration.rs:2152-2176](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/session_runtime/live_orchestration.rs#L2152-L2176)

```text
requested_transport != Some(LiveOpenTransport::Webrtc)
```

- [meerkat-rpc/src/handlers/live.rs:1053-1066](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-rpc/src/handlers/live.rs#L1053-L1066)

```text
experimental_live_open_authority.is_none()
```

- [meerkat-openai/src/public_live.rs:527-566](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/src/public_live.rs#L527-L566)

```text
NormalizedBackendKind::OpenAi(OpenAiBackendKind::OpenAiApi)
```

- [meerkat-contracts/src/wire/live.rs:126-182](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-contracts/src/wire/live.rs#L126-L182)

```text
pub profile_id: String,
```

## A03-031: Scheduler example targets a fresh random ID as if it were a resumable session

**Candidate severity:** medium. **Independent verdict:** confirm.

Nothing in the example creates or persists the randomly generated target session. Successfully creating a schedule does not make that ID resumable, so a normal delivery host will misfire instead of generating hourly reports.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:697-710](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L697-L710)

```text
target: TargetBinding::session(SessionTargetBinding::ResumableSession {
        session_id: SessionId::new(),
```

- [.claude/skills/meerkat-platform/SKILL.md:707-710](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L707-L710)

```text
missing_target_policy: MissingTargetPolicy::MarkMisfired,
```

### Implementation proof

- [meerkat/src/surface/schedule_host.rs:537-600](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/surface/schedule_host.rs#L537-L600)

```text
| SessionTargetBinding::ResumableSession { session_id, .. } => {
```

  Exact/resumable targets resolve the supplied session ID or recover its known identity. Session creation is a separate MaterializeOnDemandSession branch.

- [meerkat-schedule/src/driver.rs:2843-2852](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/src/driver.rs#L2843-L2852)

```text
OccurrencePhase::Misfired,
```

  The target-probe contract case maps Missing plus MarkMisfired to a misfired occurrence with TargetMissing.

### Independent challenge

Narrowed to an unusable executable target, not an immediate failure of ScheduleService.create. The shown code stores a schedule definition and does not itself run a driver. When delivered through the runtime-backed host, its newly generated SessionId names no existing session; ResumableSession retains or recovers an existing identity and does not mean 'create this random ID'. Only MaterializeOnDemandSession carries creation instructions.

- [meerkat/src/surface/runtime_schedule_host.rs:585-626](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/surface/runtime_schedule_host.rs#L585-L626)

```text
Err(meerkat_core::service::SessionError::NotFound { .. }) => {
```

  The production target probe maps authoritative service NotFound to Missing. The following resumable recovery attempts to load existing authoritative identity, not invent one.

- [meerkat/src/surface/schedule_host.rs:537-600](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/surface/schedule_host.rs#L537-L600)

```text
| SessionTargetBinding::ResumableSession { session_id, .. } => {
```

  Exact/resumable branches retain supplied IDs; session materialization occurs only in the separate MaterializeOnDemandSession branch.

- [meerkat-schedule/src/driver.rs:2843-2852](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/src/driver.rs#L2843-L2852)

```text
OccurrencePhase::Misfired,
```

  The inspected test case binds Missing plus MarkMisfired to a misfired occurrence through occurrence authority; it was not executed.

**Required correction:** Replace SessionId::new() in the ResumableSession recipe with the ID returned by a previously created/persisted session and show or clearly state that prerequisite. If demonstrating creation on first delivery instead, use MaterializeOnDemandSession with a complete SessionMaterializationSpec. Explicitly distinguish storing a schedule definition from running a driver and delivering it; do not claim the existing storage-only snippet immediately misfires.

### Correction

Replaced the fabricated resumable target ID with a previously created/persisted session's returned ID. Stated that prerequisite explicitly and separated schedule storage/planning from running a driver and host delivery; identified materialize-on-demand as the alternative without pretending this snippet executes it.

- [.claude/skills/meerkat-platform/SKILL.md:746-753](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L746-L753)

```text
First create/persist the target session in the runtime host's realm.
```

- [.claude/skills/meerkat-platform/SKILL.md:778-778](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L778)

```text
session_id: session_result.session_id.clone(),
```

### Final independent review

**PASS:** The schedule target uses an actual prior creation result rather than a fabricated UUID. The prerequisite and same-host/realm requirement are adjacent to the example, and storing/planning is explicitly distinguished from driver delivery. Materialize-on-demand remains a separate complete-spec alternative.

- [.claude/skills/meerkat-platform/SKILL.md:748-755](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L748-L755)

```text
This snippet stores and plans a
```

- [.claude/skills/meerkat-platform/SKILL.md:776-780](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L776-L780)

```text
session_id: session_result.session_id.clone(),
```

- [meerkat/src/surface/runtime_schedule_host.rs:585-612](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/surface/runtime_schedule_host.rs#L585-L612)

```text
match self.service.read(session_id).await
```

- [meerkat/src/surface/schedule_host.rs:537-605](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/surface/schedule_host.rs#L537-L605)

```text
SessionTargetBinding::MaterializeOnDemandSession
```

## A03-032: TypeScript multimodal image example uses mediaType instead of media_type

**Candidate severity:** medium. **Independent verdict:** confirm.

The prompt block fails TypeScript checking and lacks the wire-required media_type if passed as untyped JavaScript. Camel-case fields on blob/output projections do not apply to ContentBlock inputs.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:1020-1020](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L1020)

```text
{type: "image", mediaType: "...", data: "<base64>"}
```

### Implementation proof

- [sdks/typescript/src/types.ts:111-125](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/types.ts#L111-L125)

```text
readonly media_type: string;
```

  Input-bearing InlineImageBlock and BlobImageBlock both use snake_case media_type.

- [sdks/typescript/src/client.ts:624-643](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L624-L643)

```text
: { ...block, source: "inline" };
```

  Input lowering preserves the block's fields and adds source; it does not rename mediaType into media_type.

### Independent challenge

The native TypeScript content-block input preserves wire-style media_type. It is not one of the SDK's camelCase option fields. The normalization code adds the inline source discriminator but does not translate mediaType, so an untyped caller would not gain the required field either.

- [sdks/typescript/src/types.ts:111-125](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/types.ts#L111-L125)

```text
readonly media_type: string;
```

  Both public image input variants require the snake_case field.

- [sdks/typescript/src/client.ts:624-643](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/sdks/typescript/src/client.ts#L624-L643)

```text
: { ...block, source: "inline" };
```

  The actual block normalization spreads the input rather than converting a mediaType alias.

**Required correction:** Change mediaType to media_type in the TypeScript multimodal image input example. Leave unrelated camelCase SDK options and output projection properties unchanged.

### Correction

Corrected the TypeScript image ContentBlock input property to media_type only. Preserved independent camelCase SDK options and generated-image output projection fields.

- [.claude/skills/meerkat-platform/SKILL.md:1144-1144](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L1144)

```text
{type: "image", media_type: "...", data: "<base64>"}
```

### Final independent review

**PASS:** Only the native TypeScript image ContentBlock input spelling was corrected to media_type. The reference retains legitimately camel-cased SDK result fields and does not conflate them with wire-shaped content inputs.

- [.claude/skills/meerkat-platform/SKILL.md:1144-1144](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L1144)

```text
{type: "image", media_type: "...", data: "<base64>"}
```

- [sdks/typescript/src/types.ts:111-126](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/sdks/typescript/src/types.ts#L111-L126)

```text
readonly media_type: string;
```

- [.claude/skills/meerkat-platform/references/api_reference.md:784-786](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L784-L786)

```text
`mediaType`
```

## A03-033: Structured-output guidance reads JSON from the wrong event and conflates main text with extraction

**Candidate severity:** high. **Independent verdict:** confirm.

A consumer following the recipe parses ordinary prose as JSON and may treat the main-run completion as extraction success even when extraction later fails. This breaks both data shape and terminality.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:1137-1141](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L1137-L1141)

```text
**`output_schema` constrains the agent's final text output**
```

- [.claude/skills/meerkat-platform/SKILL.md:1141-1141](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L1141)

```text
The structured output appears in `RunCompleted` events as a JSON string in the `result` field. Parse it to extract the schema-validated fields.
```

### Implementation proof

- [meerkat-core/src/agent/state.rs:6810-6839](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L6810-L6839)

```text
structured_output: None,
```

  The main run result retains final_text, has no extracted object yet, and emits RunCompleted with extraction_required = true before the extraction turn.

- [meerkat-core/src/agent/state.rs:6707-6743](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L6707-L6743)

```text
self.emit_extraction_succeeded_event(
```

  Successful post-run extraction emits a distinct ExtractionSucceeded event with the typed structured value while RunResult.text remains the primary output.

- [meerkat-core/src/event.rs:2056-2077](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/event.rs#L2056-L2077)

```text
ExtractionSucceeded {
        session_id: SessionId,
        structured_output: Value,
```

  The public event contract has a typed structured_output field, not a guaranteed JSON string in result.

- [meerkat-core/src/agent/state.rs:22110-22115](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L22110-L22115)

```text
assert_eq!(result.text, "main answer");
```

  The extraction contract test expressly preserves main-answer text separately from the extracted {answer: "42"} object.

### Independent challenge

The agent preserves primary text and performs schema extraction as a separate phase. The event type does allow optional structured_output on RunCompleted, so the correction should not claim that field can never exist anywhere. But this actual output_schema path emits main-run completion with structured_output=None and extraction_required, followed by an extraction terminal; parsing result as the extracted JSON is wrong. The implementation and its scripted-client test both preserve this separation.

- [meerkat-core/src/agent/state.rs:6810-6839](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L6810-L6839)

```text
structured_output: None,
```

  Main-run completion is emitted before the separate extraction phase, with the flag indicating that extraction is required.

- [meerkat-core/src/agent/state.rs:6707-6743](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L6707-L6743)

```text
self.emit_extraction_succeeded_event(
```

  Successful extraction stores the typed structured output and emits its own terminal while retaining the main text.

- [meerkat-core/src/event.rs:2056-2077](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/event.rs#L2056-L2077)

```text
ExtractionSucceeded {
        session_id: SessionId,
        structured_output: Value,
```

  The public event contract carries the extracted JSON separately and provides a distinct ExtractionFailed event.

- [meerkat-core/src/agent/state.rs:22104-22116](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L22104-L22116)

```text
assert_eq!(result.text, "main answer");
```

  The inspected test explicitly expects ordinary text plus structured_output={"answer":"42"}; this is not merely a wire-schema inference.

**Required correction:** Describe output_schema as requesting a separate structured extraction rather than constraining the primary assistant text into JSON. Read completed call results from structured_output, with extraction-error handling. In streaming guidance, retain RunCompleted.result as main text and, when extraction_required is true, await ExtractionSucceeded/ExtractionFailed (wire extraction_succeeded/extraction_failed) instead of parsing result or terminating on main-run completion.

### Correction

Documented output_schema as separate post-run extraction, not primary-text JSON enforcement. Directed result consumers to structured_output/extraction_error and streaming consumers to the ExtractionSucceeded/ExtractionFailed terminal when extraction_required is true, retaining RunCompleted.result as main text.

- [.claude/skills/meerkat-platform/SKILL.md:1266-1277](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L1266-L1277)

```text
`RunCompleted.result` is primary text. When `extraction_required` is true,
```

### Final independent review

**PASS:** The replacement consistently treats extraction as post-main-run validation, reads structured_output/extraction_error, and waits for the extraction terminal when required. It does not deny the event type's optional structured_output field or treat main completion as schema success.

- [.claude/skills/meerkat-platform/SKILL.md:1268-1279](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L1268-L1279)

```text
Do not terminate the
```

- [meerkat-core/src/agent/state.rs:6810-6840](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L6810-L6840)

```text
self.emit_run_completed_event(&result, true, ctx.event_tx.as_ref())
```

- [meerkat-core/src/event.rs:2056-2087](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/event.rs#L2056-L2087)

```text
extraction_required: bool,
```

- [meerkat-core/src/agent/state.rs:6707-6743](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L6707-L6743)

```text
self.emit_extraction_succeeded_event(
```

## A03-034: Stream-watchdog guarantee omits the custom-client liveness prerequisite

**Candidate severity:** medium. **Independent verdict:** confirm.

For custom AgentLlmClient implementations, configuring a 300-second inactivity timeout is not sufficient. An implementation using the default trait method can still wait until a hard call/turn deadline, or indefinitely if those are disabled.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/SKILL.md:303-307](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L303-L307)

```text
The stream-inactivity watchdog (`[retry] stream_inactivity_timeout`) is ON
  by default at 300s
```

- [.claude/skills/meerkat-platform/SKILL.md:977-993](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L977-L993)

```text
A provider stream that produces no events inside the watchdog window is
```

### Implementation proof

- [meerkat-core/src/agent.rs:374-390](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent.rs#L374-L390)

```text
fn stream_activity_count(&self) -> Option<u64> {
        None
    }
```

  The foundational client trait defaults to no liveness reporting and explicitly documents that this disables the watchdog for its calls.

- [meerkat-core/src/agent/state.rs:2023-2029](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L2023-L2029)

```text
.stream_activity_count()
                .and(self.retry_policy.stream_inactivity_timeout);
```

  The loop actually gates the configured timeout on the client's optional probe.

- [meerkat-core/src/retry.rs:158-166](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/retry.rs#L158-L166)

```text
Only applies to clients
    /// that report stream liveness
```

  The retry contract states the scope exception that the integration guide omits.

### Independent challenge

The 300-second configured default is real and should remain. The missing qualification is client cooperation: the default AgentLlmClient liveness probe returns None, and the actual watchdog branch is disabled in that case even when the RetryPolicy duration is set. Visible output observation is a different signal and is not a replacement for raw-event liveness.

- [meerkat-core/src/agent.rs:374-390](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent.rs#L374-L390)

```text
fn stream_activity_count(&self) -> Option<u64> {
        None
    }
```

  The trait explicitly requires a monotonic count of raw events, including non-visible events; None means no stream-watchdog protection for that client.

- [meerkat-core/src/agent/state.rs:2023-2029](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/agent/state.rs#L2023-L2029)

```text
.stream_activity_count()
                .and(self.retry_policy.stream_inactivity_timeout);
```

  The actual call loop gates the stall window on both reported liveness and configured timeout.

- [meerkat-core/src/retry.rs:158-166](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/retry.rs#L158-L166)

```text
Only applies to clients
    /// that report stream liveness
```

  RetryPolicy's own contract confirms the prerequisite while retaining the 300-second default.

**Required correction:** Qualify both watchdog descriptions: the default inactivity watchdog applies to clients implementing stream_activity_count() with a monotonic count advanced for every raw provider event, including non-visible events. Custom clients returning the default None have no inactivity watchdog; only any configured hard call/turn deadlines remain. Retain the genuine 300-second default and retryable StreamStalled behavior.

### Correction

Qualified both watchdog descriptions with stream_activity_count's monotonic raw-event counter, including non-visible events. Preserved the 300-second default and retryable StreamStalled behavior; custom clients returning None have no inactivity watchdog, only configured hard deadlines.

- [.claude/skills/meerkat-platform/SKILL.md:312-316](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L312-L316)

```text
`stream_activity_count()`; `"disabled"` opts out.
```

- [.claude/skills/meerkat-platform/SKILL.md:1117-1117](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L1117)

```text
A custom client returning the default `None` has no inactivity
```

### Final independent review

**PASS:** Both watchdog descriptions now require monotonic raw-event liveness reporting, including invisible events, and explicitly exempt default-None custom clients. The real 300-second default and retryable stall outcome were not removed.

- [.claude/skills/meerkat-platform/SKILL.md:312-316](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L312-L316)

```text
A custom client returning the default `None` has no
```

- [.claude/skills/meerkat-platform/SKILL.md:1112-1118](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L1112-L1118)

```text
monotonic count advanced for every raw provider event
```

- [meerkat-core/src/agent.rs:374-390](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent.rs#L374-L390)

```text
fn stream_activity_count(&self) -> Option<u64>
```

- [meerkat-core/src/agent/state.rs:2023-2029](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/agent/state.rs#L2023-L2029)

```text
.and(self.retry_policy.stream_inactivity_timeout)
```

- [meerkat-core/src/retry.rs:129-135](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/retry.rs#L129-L135)

```text
Duration::from_secs(300)
```

## A03-035: Current OAuth route guide omits the shipping Copilot login and its three bindings

**Candidate severity:** medium. **Independent verdict:** confirm.

The apparently complete current route description excludes a default-shipping login backend. Users cannot infer that Copilot uses its own device-login flow and route-specific auth-binding names rather than the direct-provider OAuth names listed here.

### Baseline documentation evidence

- [.claude/skills/meerkat-cli-reference/SKILL.md:481-485](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L481-L485)

```text
Interactive OAuth writes
`global:anthropic_oauth`, `global:openai_oauth`, or `global:google_oauth`;
```

- [.claude/skills/meerkat-platform/SKILL.md:844-844](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/SKILL.md?plain=1#L844)

```text
interactive OAuth writes `global:anthropic_oauth`, `global:openai_oauth`, or `global:google_oauth`
```

### Implementation proof

- [meerkat-cli/Cargo.toml:108-110](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/Cargo.toml#L108-L110)

```text
"gemini", "copilot", "jsonl-store"
```

  Copilot is enabled in the normal shipping CLI feature set.

- [meerkat-cli/src/main.rs:5983-5992](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L5983-L5992)

```text
"anthropic, openai, google, copilot"
```

  The login provider parser explicitly supports copilot in that build.

- [meerkat-cli/src/main.rs:7349-7357](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L7349-L7357)

```text
OpenAI route:    --auth-binding global:copilot_openai\n  Anthropic route: --auth-binding global:copilot_anthropic\n  Gemini route:    --auth-binding global:copilot_gemini
```

  The implemented device-login flow returns these three route bindings, distinct from direct-provider OAuth bindings.

- [meerkat-cli/src/main.rs:6093-6103](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L6093-L6103)

```text
meerkat_copilot::GITHUB_COPILOT_CREDENTIAL_ACCOUNT_ID,
```

  The routes share the Copilot credential account instead of representing three separate provider logins.

### Independent challenge

This is a shipping default-enabled OAuth login route, not an unimplemented catalog promise. I followed the actual alias parser, config provisioning, and device-start/device-poll branch. The three generated bindings share one credential account. Qualify the feature gate and preserve the distinction between LoginProvider::Copilot and the three underlying LLM Provider identities.

- [meerkat-cli/Cargo.toml:108-110](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/Cargo.toml#L108-L110)

```text
"gemini", "copilot", "jsonl-store"
```

  Default CLI builds include Copilot support; reduced builds can deliberately exclude it.

- [meerkat-cli/src/main.rs:5675-5696](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L5675-L5696)

```text
OAuthProviderIdentity::GitHubCopilot => {
```

  The actual login parser accepts this identity when copilot is compiled, rather than merely displaying a help string.

- [meerkat-cli/src/main.rs:7270-7357](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L7270-L7357)

```text
return interactive_copilot_device_login(target, &config, &host_target, auth_service).await;
```

  The implemented interactive flow delegates to GitHub device authorization, polls its result, and prints all three usable auth-binding routes.

- [meerkat-cli/src/main.rs:19054-19078](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-cli/src/main.rs#L19054-L19078)

```text
for binding_id in ["copilot_openai", "copilot_anthropic", "copilot_gemini"] {
```

  The independently inspected provisioning test asserts that all three bindings share the github_copilot credential account and that provisioning is idempotent.

**Required correction:** Add rkat auth login copilot as the feature-qualified GitHub device-flow login. List global:copilot_openai, global:copilot_anthropic, and global:copilot_gemini and explain their shared github_copilot credential account and underlying provider-specific backends. Update repeated current OAuth route summaries in the two skills and API reference consistently; do not add Copilot as a fourth LLM --provider value or rewrite historical OAuth migration text.

### Correction

Updated all repeated current auth summaries with feature-qualified Copilot GitHub device OAuth, github_copilot_oauth, the three global:copilot_* bindings, and shared github_copilot credential account/provider-specific backends. Preserved three LLM provider families and the historical idempotent dev-to-global migration.

- [.claude/skills/meerkat-cli-reference/SKILL.md:496-504](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L496-L504)

```text
GitHub device OAuth (`github_copilot_oauth`) and provisions
```

- [.claude/skills/meerkat-platform/SKILL.md:948-959](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L948-L959)

```text
All three share the `github_copilot` credential account
```

- [.claude/skills/meerkat-platform/references/api_reference.md:408-414](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L408-L414)

```text
`global:copilot_gemini`, all sharing the `github_copilot` credential account.
```

### Final independent review

**PASS:** All repeated current auth summaries include feature-qualified GitHub device login and the three Copilot routes sharing one credential account. Checked parser gating, device flow and route construction; no fourth provider value or three-account login claim was introduced, and legacy migration remains qualified.

- [.claude/skills/meerkat-cli-reference/SKILL.md:496-504](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-cli-reference/SKILL.md?plain=1#L496-L504)

```text
`global:copilot_gemini`
```

- [.claude/skills/meerkat-platform/SKILL.md:950-961](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/SKILL.md?plain=1#L950-L961)

```text
`github_copilot_oauth`
```

- [.claude/skills/meerkat-platform/references/api_reference.md:408-415](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L408-L415)

```text
all sharing the `github_copilot` credential account
```

- [meerkat-cli/src/main.rs:5675-5692](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L5675-L5692)

```text
#[cfg(feature = "copilot")]
```

- [meerkat-cli/src/main.rs:7307-7357](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-cli/src/main.rs#L7307-L7357)

```text
.device_poll(config, host_target, &device.device_code)
```

- [meerkat-copilot/src/protocol.rs:12-43](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-copilot/src/protocol.rs#L12-L43)

```text
GITHUB_COPILOT_CREDENTIAL_ACCOUNT_ID
```

## A03-036: Factory skill introspection passes a display name to a UUID source selector

**Candidate severity:** medium. **Independent verdict:** confirm.

The Result/import corrections from A03-008 and A12-011 leave a separate runtime defect. Once the key resolves, the concrete factory-created composite parses Some("company") as a UUID and errors; a missing key would also prevent the example from loading. Earlier audit/challenge asides endorsed the selector based on its misleading source_name parameter and are explicitly reopened here rather than silently reinterpreted. The independent supplemental challenger also rejected the parent's ancillary assumption that the main platform skill repeats this executable call; the executable example is in the API reference and its aliases.

This supplemental candidate was discovered during final review and independently adjudicated before correction.

### Baseline documentation evidence

- [.claude/skills/meerkat-platform/references/api_reference.md:828-828](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L828)

```text
let doc = runtime.load_from_source(&key, Some("company")).await?;
```

### Implementation proof

- [meerkat/src/factory.rs:3653-3726](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L3653-L3726)

```text
let skill_source: Option<Arc<meerkat_skills::CompositeSkillSource>> =
```

  The shown factory method builds the concrete composite source rather than an arbitrary implementation that might interpret display names.

- [meerkat-skills/src/source/composite.rs:276-299](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-skills/src/source/composite.rs#L276-L299)

```text
let parsed = SourceUuid::parse(target)?;
```

  The concrete supplied-selector path parses its target as a source UUID despite the parameter's source_name spelling.

- [meerkat-core/src/skills/mod.rs:36-41](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/mod.rs#L36-L41)

```text
Uuid::parse_str(value)
```

  A human-readable company display name is not UUID text and cannot satisfy this parser.

### Independent challenge

Independently confirmed by following the concrete call, not the source_name parameter spelling. AgentFactory::build_skill_runtime constructs DefaultSkillEngine<Arc<CompositeSkillSource>>. SkillRuntime, the engine, and the Arc<SkillSource> implementation forward the selector without translating a configured repository name. After canonical-key resolution, CompositeSkillSource calls SourceUuid::parse on every Some selector, which calls uuid::Uuid::parse_str. Therefore Some("company") produces SkillError::Parse with the prefix "invalid source_uuid 'company':" when the shown key resolves; registering a repository with name = "company" does not change that. If runtime construction or canonical-key resolution fails first, that earlier error wins; if the factory returns None, the example body is skipped. None of those cases establishes a working human-readable source selector. The baseline snippet separately failed to compile before its already-adjudicated Result/import repairs; this supplemental finding concerns the runtime failure remaining after those repairs.

- [.claude/skills/meerkat-platform/references/api_reference.md:824-828](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L824-L828)

```text
    let key = SkillKey::new(
        SourceUuid::builtin(),
        SkillName::parse("task-workflow")?,
    );
    let doc = runtime.load_from_source(&key, Some("company")).await?;
```

  The baseline explicitly combines the builtin canonical key with the non-UUID selector company. This is the reopened claim, distinct from Result handling and imports.

- [.claude/skills/meerkat-platform/references/api_reference.md:840-840](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L840)

```text
- **Skill introspection**: `SkillRuntime::list_all_with_provenance()` returns active + shadowed skills; `load_from_source()` bypasses first-wins.
```

  The adjacent explanation should distinguish None's ordinary canonical/precedence load from an explicit, valid UUID source selection rather than imply that all calls bypass precedence.

- [meerkat/embedded_skills/meerkat-platform/references/api_reference.md:1-1 (symlink blob)](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/embedded_skills/meerkat-platform/references/api_reference.md?plain=1#L1)

```text
../../../../.claude/skills/meerkat-platform/references/api_reference.md
```

  This baseline Git blob is a mode-120000 symlink, not another independent Markdown body. A12 observes the same canonical API reference as A03; edit its canonical target only.

- [meerkat/src/factory.rs:3653-3663](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L3653-L3663)

```text
    #[cfg(feature = "skills")]
    pub async fn build_skill_runtime(
        &self,
        config: &Config,
    ) -> Result<Option<Arc<meerkat_core::skills::SkillRuntime>>, BuildAgentError> {
        let skill_source: Option<Arc<meerkat_skills::CompositeSkillSource>> =
            if self.skill_source.is_some() {
                self.skill_source.clone()
            } else if !config.skills.enabled {
                None
            } else {
```

  The method used by the example has a concrete CompositeSkillSource, including the prebuilt-source branch. It is feature-gated and may return no runtime when disabled; it is not a generic user-supplied name-resolving engine.

- [meerkat/src/factory.rs:3714-3723](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/src/factory.rs#L3714-L3723)

```text
                let engine = meerkat_skills::DefaultSkillEngine::new(source, available_caps)
                    .with_inventory_threshold(config.skills.inventory_threshold)
                    .with_max_injection_bytes(config.skills.max_injection_bytes)
                    .with_source_identity_registry(registry);
                let engine = Arc::new(engine);
                Ok::<Arc<meerkat_core::skills::SkillRuntime>, BuildAgentError>(Arc::new(
                    meerkat_core::skills::SkillRuntime::new(engine),
                ))
            })
            .transpose()
```

  The selected composite is installed in DefaultSkillEngine and then SkillRuntime. There is no selector adapter at the factory boundary.

- [meerkat-core/src/skills/mod.rs:1474-1480](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/mod.rs#L1474-L1480)

```text
    pub async fn load_from_source(
        &self,
        key: &SkillKey,
        source_name: Option<&str>,
    ) -> Result<SkillDocument, SkillError> {
        (self.load_from_source_fn)(key.clone(), source_name.map(ToString::to_string)).await
    }
```

  The public runtime method copies the selector into a String; the misleading source_name identifier does not implement name lookup.

- [meerkat-core/src/skills/mod.rs:1403-1406](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/mod.rs#L1403-L1406)

```text
            load_from_source_fn: Arc::new(move |key: SkillKey, source_name: Option<String>| {
                let engine = Arc::clone(&load_from_source_engine);
                Box::pin(async move { engine.load_from_source(&key, source_name.as_deref()).await })
            }),
```

  SkillRuntime's installed closure dispatches unchanged to the concrete engine; it does not resolve display names or skill aliases.

- [meerkat-skills/src/engine.rs:273-285](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-skills/src/engine.rs#L273-L285)

```text
    fn load_from_source(
        &self,
        key: &SkillKey,
        source_name: Option<&str>,
    ) -> impl Future<Output = Result<SkillDocument, SkillError>> + Send {
        let source_name = source_name.map(ToString::to_string);
        async move {
            let canonical_key = self.resolve_key(key)?;
            self.source
                .load_from_source(&canonical_key, source_name.as_deref())
                .await
        }
    }
```

  DefaultSkillEngine resolves the key but forwards the original selector. Canonical-key resolution may fail before selector parsing, and does not turn company into a UUID.

- [meerkat-core/src/skills/mod.rs:1184-1191](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/mod.rs#L1184-L1191)

```text
    fn load_from_source(
        &self,
        key: &SkillKey,
        source_name: Option<&str>,
    ) -> impl Future<Output = Result<SkillDocument, SkillError>> + Send {
        let source_name = source_name.map(ToString::to_string);
        async move { (**self).load_from_source(key, source_name.as_deref()).await }
    }
```

  The SkillSource implementation for Arc<T> forwards to T's override. The Arc around the factory composite does not fall back to the trait's selector-ignoring default.

- [meerkat-skills/src/source/composite.rs:276-298](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-skills/src/source/composite.rs#L276-L298)

```text
    async fn load_from_source(
        &self,
        key: &SkillKey,
        source_name: Option<&str>,
    ) -> Result<SkillDocument, SkillError> {
        let canonical_key = self.resolve_key_for_load(key)?;
        let Some(target) = source_name else {
            return self.load_canonical(&canonical_key).await;
        };
        let target_uuid = {
            let parsed = SourceUuid::parse(target)?;
            let target_key = SkillKey::new(parsed, canonical_key.skill_name.clone());
            self.resolve_key_for_load(&target_key)?.source_uuid
        };
        for named in &self.sources {
            if named.source_uuid() == &target_uuid {
                let mut doc = named.source.load(&canonical_key).await?;
                doc.descriptor.source_name = named.display_name().to_string();
                return Ok(doc);
            }
        }
        Err(SkillError::NotFound { key: canonical_key })
    }
```

  Decisive implementation: Some must parse as a SourceUuid, then resolve through the registry, then match an attached source UUID. None directly takes the normal canonical load. The chosen source is asked for the original canonical_key, not a key rebased onto target_uuid, so replacing company with an unrelated registered UUID is insufficient.

- [meerkat-core/src/skills/mod.rs:37-42](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/mod.rs#L37-L42)

```text
impl SourceUuid {
    pub fn parse(value: &str) -> Result<Self, SkillError> {
        Uuid::parse_str(value)
            .map(Self)
            .map_err(|e| SkillError::Parse(format!("invalid source_uuid '{value}': {e}").into()))
    }
```

  company is not UUID text. Parsing has no display-name or alias fallback and returns the typed Parse error before any physical-source lookup.

- [meerkat-core/src/skills_config.rs:69-78](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills_config.rs#L69-L78)

```text
/// A named skill repository.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SkillRepositoryConfig {
    /// Human-readable name (used in browsing, tracing, shadowing logs).
    pub name: String,
    /// Stable UUID for source governance.
    pub source_uuid: SourceUuid,
    /// Repository type and transport-specific config.
    #[serde(flatten)]
    pub transport: SkillRepoTransport,
```

  A configured repository has separate display-name and UUID fields; configuring a human-readable company name does not make it UUID text.

- [meerkat-core/src/skills_config.rs:252-260](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills_config.rs#L252-L260)

```text
fn repository_to_identity_record(repo: &SkillRepositoryConfig) -> SourceIdentityRecord {
    SourceIdentityRecord {
        source_uuid: repo.source_uuid.clone(),
        display_name: repo.name.clone(),
        transport_kind: repository_transport_kind(&repo.transport),
        fingerprint: repository_fingerprint(repo),
        status: SourceIdentityStatus::Active,
    }
}
```

  Repository construction preserves the UUID as identity and copies name only into display_name; it does not populate a name-to-selector translation.

- [meerkat-core/src/skills/identity.rs:205-229](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/identity.rs#L205-L229)

```text
    /// Resolve a typed `SkillKey` against the registry. Applies any remap
    /// chain, then enforces the source lifecycle: only `Active` sources may
    /// back a resolved skill.
    pub fn resolve(&self, key: &SkillKey) -> Result<ResolvedSkill<'_>, ResolveError> {
        let original = key.clone();
        let canonical = self.apply_remaps(key.clone()).map_err(|_| {
            // A remap cycle is a programmer/config error; surface it as
            // `SkillNotFound` at the resolve boundary so the caller's error
            // taxonomy stays narrow. The richer `SkillError::RemapCycle` is
            // available via `canonical_skill_key`.
            ResolveError::SkillNotFound {
                source_uuid: key.source_uuid.clone(),
                skill_name: key.skill_name.clone(),
            }
        })?;

        let Some(record) = self.sources.get(&canonical.source_uuid) else {
            return Err(ResolveError::SourceUnknown(canonical.source_uuid));
        };

        match record.status {
            SourceIdentityStatus::Active => Ok(ResolvedSkill {
                key: canonical,
                source: record,
            }),
```

  Canonical resolution is typed-key remapping plus UUID registration/lifecycle enforcement. A syntactically valid arbitrary UUID is not sufficient, and resolving a source record is not proof that its backend contains the requested skill.

- [meerkat-core/src/skills/identity.rs:184-195](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/identity.rs#L184-L195)

```text
    pub fn resolve_alias(&self, alias: &str) -> Result<SkillKey, SkillError> {
        let Some(key) = self.aliases.get(alias) else {
            return Err(SkillError::UnknownSkillAlias {
                alias: alias.to_string(),
            });
        };
        self.resolve(key).map(|resolved| resolved.key).map_err(|e| {
            SkillError::Load(
                format!("source identity resolution failed for alias '{alias}': {e}").into(),
            )
        })
    }
```

  A separate skill-alias resolver exists, but no step of the inspected load_from_source path calls it for the selector. Adding an alias company therefore cannot rescue Some("company").

- [meerkat-core/src/skills/mod.rs:1104-1111](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/mod.rs#L1104-L1111)

```text
    /// Load a skill from a specific named source, bypassing first-wins resolution.
    fn load_from_source(
        &self,
        key: &SkillKey,
        _source_name: Option<&str>,
    ) -> impl Future<Output = Result<SkillDocument, SkillError>> + Send {
        async move { self.load(key).await }
    }
```

  Counterexample investigated: a direct leaf source using the SkillSource default can ignore Some("company") and load by key. That is not selection by name, and the factory composite overrides this method, so it does not disprove this finding.

- [meerkat-core/src/skills/mod.rs:1266-1275](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills/mod.rs#L1266-L1275)

```text
    /// Load a skill from a specific named source, bypassing first-wins resolution.
    fn load_from_source(
        &self,
        key: &SkillKey,
        _source_name: Option<&str>,
    ) -> impl Future<Output = Result<SkillDocument, SkillError>> + Send {
        let _ = _source_name;
        let missing = key.clone();
        async move { Err(SkillError::NotFound { key: missing }) }
    }
```

  The other default, on SkillEngine, returns NotFound rather than performing name selection. Custom engines can define different behavior, but this factory installs DefaultSkillEngine's explicit forwarding override.

- [meerkat-core/src/skills_config.rs:235-241](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/skills_config.rs#L235-L241)

```text
        SourceIdentityRecord {
            source_uuid: SourceUuid::builtin(),
            display_name: "embedded".to_string(),
            transport_kind: SourceTransportKind::Embedded,
            fingerprint: "embedded:inventory".to_string(),
            status: SourceIdentityStatus::Active,
        },
```

  The default registry actually registers the builtin source as Active. The supported builtin replacement is not based on an invented UUID.

- [meerkat-skills/src/resolve.rs:79-85](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-skills/src/resolve.rs#L79-L85)

```text
    #[cfg(not(target_arch = "wasm32"))]
    {
        let builtin_identity = active_source_identity(&registry, &records, &SourceUuid::builtin())?;
        let mut sources: Vec<NamedSource> = vec![NamedSource::new(
            builtin_identity,
            SourceNode::Embedded(EmbeddedSkillSource::new()),
        )];
```

  The normal native config-driven source composition physically attaches an EmbeddedSkillSource, first in precedence. Custom composites or the factory's no-injected-source WASM branch are not assumed equivalent.

- [meerkat-skills/src/source/composite.rs:98-110](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-skills/src/source/composite.rs#L98-L110)

```text
    async fn load_canonical(&self, key: &SkillKey) -> Result<SkillDocument, SkillError> {
        for named in &self.sources {
            match named.source.load(key).await {
                Ok(mut doc) => {
                    doc.descriptor.source_name = named.display_name().to_string();
                    return Ok(doc);
                }
                Err(SkillError::NotFound { .. }) => continue,
                Err(e) => return Err(e),
            }
        }
        Err(SkillError::NotFound { key: key.clone() })
    }
```

  None preserves canonical-key and first-success precedence behavior. With the normally attached embedded source containing task-workflow, it returns that document without interpreting any display name.

- [meerkat/Cargo.toml:33-33](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/Cargo.toml#L33)

```text
meerkat-tools = { workspace = true }
```

  The facade links the component that owns the task-workflow inventory registration; the replacement is not relying on a registration from a crate absent from this stock construction.

- [meerkat/Cargo.toml:213-224](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat/Cargo.toml#L213-L224)

```text
skills = [
    "dep:meerkat-skills",
    # Durable builtin SkillKeys must resolve after cross-surface resume even
    # when the facade's comms tool surface is disabled. This links only the
    # feature-owned declarations; `comms` still owns tool/runtime enablement.
    "dep:meerkat-comms",
    "meerkat-skills/capability",
    "meerkat-skills/skills-http",
    "meerkat-tools/skills",
    "meerkat-schedule/skills",
    "meerkat-workgraph/skills",
]
```

  Keep the facade skills feature prerequisite. This feature links the skill engine and component integrations; the example is not a guarantee for arbitrary reduced-feature or custom runtimes.

- [meerkat-tools/src/lib.rs:116-126](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-tools/src/lib.rs#L116-L126)

```text
inventory::submit! {
    meerkat_skills::SkillRegistration {
        id: "task-workflow",
        name: "Task Workflow",
        description: "How to use task_create/task_update/task_list for structured work tracking",
        scope: meerkat_core::skills::SkillScope::Builtin,
        requires_capabilities: &["builtins"],
        body: include_str!("../skills/task-workflow/SKILL.md"),
        extensions: &[],
    }
}
```

  Concrete production registration of the exact slug in the example. It has builtin capability requirements for capability-filtered listing/rendering, but the raw introspection load path above does not itself apply that filter.

- [meerkat-skills/src/source/embedded.rs:20-26](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-skills/src/source/embedded.rs#L20-L26)

```text
fn registration_to_descriptor(reg: &SkillRegistration) -> Result<SkillDescriptor, SkillError> {
    let registration_id = RegistrationId::parse(reg.id)?;
    let skill_name = registration_id.skill_name().clone();
    let key = SkillKey {
        source_uuid: SourceUuid::builtin(),
        skill_name,
    };
```

  The inventory id becomes the exact builtin SkillKey used by the example; there is no display-name or trailing-path conversion.

- [meerkat-skills/src/source/embedded.rs:213-218](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-skills/src/source/embedded.rs#L213-L218)

```text
    async fn load(&self, key: &SkillKey) -> Result<SkillDocument, SkillError> {
        if key.source_uuid != SourceUuid::builtin() {
            return Err(SkillError::NotFound { key: key.clone() });
        }
        scan_registrations().load(key)
    }
```

  The embedded backend accepts the builtin UUID and performs exact-key inventory lookup. SourceNode's load dispatch forwards to this implementation.

- [meerkat-skills/src/source/embedded.rs:79-94](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-skills/src/source/embedded.rs#L79-L94)

```text
    fn load(&self, key: &SkillKey) -> Result<SkillDocument, SkillError> {
        if self.ambiguous_keys.contains(key) {
            return Err(SkillError::Load(
                format!(
                    "ambiguous embedded skill registration for {key}: every colliding owner is excluded"
                )
                .into(),
            ));
        }
        self.registrations
            .get(key)
            .copied()
            .map(registration_to_document)
            .transpose()?
            .ok_or_else(|| SkillError::NotFound { key: key.clone() })
    }
```

  Registration must really exist and be unambiguous; successful UUID registry resolution alone does not guarantee loadability. The repository search found one production task-workflow registration, with other same-id literals confined to embedded-source test fixtures.

**Required correction:** In the canonical API reference, replace the shown load_from_source(&key, Some("company")) with load_from_source(&key, None) for the builtin task-workflow example. Explain that None follows canonical-key/normal precedence loading, whereas the concrete factory composite's Some selector is registered source UUID text, never a configured human-readable source name, and the selected source must actually host the full canonical key. Qualify the adjacent unconditional first-wins-bypass sentence accordingly. Preserve the already-correct Result/import/skills prerequisites and state the native/default-embedded or actual-key/source prerequisites rather than promising arbitrary configurations. If showing explicit selection instead, use a demonstrably registered UUID and matching loadable canonical key. The embedded reference inherits the canonical correction through its symlink. No repeated executable selector call was found in the current main SKILL.md; only repair that file if a repeat is actually present when the fixer inspects it.

### Correction

Replaced the invalid company display-name selector with None for the builtin task-workflow key, stated the stock-native embedded-source prerequisite, and explained registered UUID text plus a loadable full canonical key for explicit selection. Qualified the adjacent introspection summary. No product behavior or main-skill executable example was changed; the embedded API reference remains a canonical symlink.

- [.claude/skills/meerkat-platform/references/api_reference.md:880-880](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L880)

```text
This example assumes the stock native factory's embedded sources
```

- [.claude/skills/meerkat-platform/references/api_reference.md:893-893](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L893)

```text
let doc = runtime.load_from_source(&key, None).await?;
```

- [.claude/skills/meerkat-platform/references/api_reference.md:898-898](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L898)

```text
an explicit `Some(...)` selector is a registered source UUID as text
```

- [.claude/skills/meerkat-platform/references/api_reference.md:900-900](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L900)

```text
the full canonical key; selecting an unrelated source does not rewrite it.
```

### Final independent review

**PASS:** Independently traced the factory's concrete CompositeSkillSource through SkillRuntime, DefaultSkillEngine and Arc forwarding. The replacement None takes canonical-key, first-success loading and never parses a display-name selector. The stock native path attaches the active builtin source, and the linked tools inventory registers the exact task-workflow key; the embedded backend loads that full key. The example now states these native/default-embedded prerequisites rather than promising every custom configuration works. Explicit selection is correctly described as registered source UUID text with full-key loadability, and the adjacent unconditional precedence-bypass statement is qualified. Result/import/skills corrections remain intact. The embedded alias inherits the repair; the main skill has no executable company call requiring a parallel edit. This is source-path verification, not runtime execution.

- [.claude/skills/meerkat-platform/references/api_reference.md:878-885](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L878-L885)

```text
This example assumes the stock native factory's embedded sources, including
```

- [.claude/skills/meerkat-platform/references/api_reference.md:887-895](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L887-L895)

```text
let doc = runtime.load_from_source(&key, None).await?;
```

- [.claude/skills/meerkat-platform/references/api_reference.md:897-900](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L897-L900)

```text
the full canonical key; selecting an unrelated source does not rewrite it.
```

- [.claude/skills/meerkat-platform/references/api_reference.md:910-910](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L910)

```text
`load_from_source(key, None)` loads the canonical key; the factory composite accepts registered source UUID text for explicit source selection.
```

- [meerkat/embedded_skills/meerkat-platform/references/api_reference.md:887-895](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/meerkat-platform/references/api_reference.md?plain=1#L887-L895)

```text
let doc = runtime.load_from_source(&key, None).await?;
```

- [meerkat/src/factory.rs:3653-3723](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/src/factory.rs#L3653-L3723)

```text
let skill_source: Option<Arc<meerkat_skills::CompositeSkillSource>>
```

  The factory's actual source is composite, not an arbitrary name-resolving engine. The native resolver/fallback and disabled/WASM-none branches keep the example conditional.

- [meerkat-core/src/skills/mod.rs:1474-1480](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/skills/mod.rs#L1474-L1480)

```text
(self.load_from_source_fn)(key.clone(), source_name.map(ToString::to_string)).await
```

- [meerkat-skills/src/engine.rs:273-285](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-skills/src/engine.rs#L273-L285)

```text
.load_from_source(&canonical_key, source_name.as_deref())
```

- [meerkat-core/src/skills/mod.rs:1184-1191](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/skills/mod.rs#L1184-L1191)

```text
async move { (**self).load_from_source(key, source_name.as_deref()).await }
```

- [meerkat-skills/src/source/composite.rs:276-298](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-skills/src/source/composite.rs#L276-L298)

```text
return self.load_canonical(&canonical_key).await;
```

  None performs the ordinary load. Some parses SourceUuid, resolves its target and selects a physical source, which still receives the original full canonical_key.

- [meerkat-skills/src/source/composite.rs:98-110](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-skills/src/source/composite.rs#L98-L110)

```text
Err(SkillError::NotFound { .. }) => continue,
```

  Canonical loading retains source-order first-success behavior rather than always bypassing precedence.

- [meerkat-core/src/skills/mod.rs:37-42](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/skills/mod.rs#L37-L42)

```text
Uuid::parse_str(value)
```

- [meerkat-core/src/skills/identity.rs:205-236](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/skills/identity.rs#L205-L236)

```text
let Some(record) = self.sources.get(&canonical.source_uuid) else {
```

  Syntactically valid UUID text alone is insufficient; canonical registration/lifecycle still applies.

- [meerkat-core/src/skills_config.rs:235-241](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/skills_config.rs#L235-L241)

```text
source_uuid: SourceUuid::builtin(),
```

- [meerkat-skills/src/resolve.rs:79-85](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-skills/src/resolve.rs#L79-L85)

```text
SourceNode::Embedded(EmbeddedSkillSource::new()),
```

- [meerkat-tools/src/lib.rs:116-126](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-tools/src/lib.rs#L116-L126)

```text
id: "task-workflow",
```

- [meerkat-skills/src/source/embedded.rs:20-26](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-skills/src/source/embedded.rs#L20-L26)

```text
source_uuid: SourceUuid::builtin(),
```

- [meerkat-skills/src/source/embedded.rs:79-94](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-skills/src/source/embedded.rs#L79-L94)

```text
self.registrations
```

  The physical inventory is indexed by the full key and rejects ambiguous registrations; None is not a promise that arbitrary custom content exists.

- [meerkat-skills/src/source/embedded.rs:213-218](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-skills/src/source/embedded.rs#L213-L218)

```text
scan_registrations().load(key)
```

- [meerkat/Cargo.toml:213-224](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat/Cargo.toml#L213-L224)

```text
"meerkat-tools/skills",
```

## Lane limitations

- **Audit:** This is first-wave static audit evidence, not independent challenge adjudication. Parent must assign separate reviewers to confirm or reject every candidate.
- **Audit:** No Rust, Python, TypeScript, browser, CLI, or provider execution was performed beyond small read-only source/inventory checks; no builds, installs, generated-file updates, or commits were allowed. Snippet failures are established from concrete current signatures, serde shapes, and explicit handler guards.
- **Audit:** Historical 0.4x-to-0.5 guidance was read in its declared scope; no old release checkout or historical binaries were fetched to revalidate that past contract.
- **Audit:** Browser resolver availability is distinguished from configured credential-source reachability. The normal public bootstrap shown in source accepts key-based inputs, so the suggested documentation correction must not invent a new realm-configuration option.
- **Audit:** All evidence and file hashes refer to the manifest baseline 56208b9e6cee078f52c43af8f6b36660bc93eeb4. Repository status was clean at inspection; the only written artifact is this audit file.
- **Audit:** A03-036 was discovered during final review and separately challenged. It overturns earlier positive asides about the company selector, not the original accepted Result/import corrections.
- **Challenge:** Independent source and read-only contract inspection at baseline 56208b9e6cee078f52c43af8f6b36660bc93eeb4. No builds, installations, provider requests, executable login/migration operations, or test suites were run, as instructed; referenced tests are source evidence, not claimed passing executions.
- **Challenge:** All five owned documents matched their manifest SHA-256 values during verification. The 0.4x-to-0.5 migration reference is explicitly historical and contributes no confirmed current-contract correction; later migration/fork/flow changes were considered without rewriting that history.
- **Challenge:** All 35 candidates are confirmed only at the scopes stated above; in particular raw-config Rust embedding, supported Gemini 3.5 examples, flat flow authoring, SessionInfo inheritance, custom-host browser auth, helper auth overrides, and deferred-create mechanics remain valid where qualified.
- **Challenge:** Only this challenge artifact was authored. Canonical skill files, their .codex/.rkat aliases, and embedded skill symlinks were not edited, replaced, or dereferenced for writes. Integration, documentation fixes, and any compile/runtime validation remain with the parent.
- **Challenge:** A separate final-wave challenger confirmed A03-036 after tracing the factory's concrete composite source. The original source-name endorsement under A03-008 is retained as historical review text but is superseded by A03-036; the earlier Result/import finding remains confirmed.
