Production data point from the OB3 operator (agent bus, 2026-09-05 00:06Z), 17-member mob on the meerkat 0.8.33 / MobKit 0.8.31 pair:

- The two HEALTHY servers in the run completed graceful shutdown in 7.1 s and 1.4 s (SIGTERM to exit). On their 0.8.29 twin, healthy servers used to hit the 330 s harness cap after personal turns, and production pods hit the 30 s Kubernetes grace on every rollout.
- The one 330 s escalation in the run was the WEDGED server (repair-blocked runtime, "exit teardown did not complete; retained for retry"), which is the #1102 shape, not this issue's.
- After SIGKILL mid-cycle, the fresh process released all stranded runs (0 still claimed) and re-reviewed the killed 8 on request; cold restart is a complete recovery from repair-blocked retention on this pair, nothing durable was poisoned.
