---
name: release-train-pr-inventory
description: "Live inventory (2026-09-04) of every branch/PR in the 0.8.33 -> 0.8.34 / MobKit 0.8.31 -> 0.8.32 train, with heads and merge order"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5df1b3d1-6021-4c86-b59d-623dd339f75b
  modified: 2026-09-04T14:59:31.677Z
---

State as of 2026-09-04 ~15:05 UTC (update when it changes).

**Meerkat 0.8.33 PUBLISHED** 2026-09-04 ~15:55Z via package-recovery dispatch run 33890685671 (crates.io all 43 incl. meerkat-mob/rkat 0.8.33, PyPI meerkat-sdk 0.8.33, npm @rkat/sdk + @rkat/web 0.8.33). GitHub release v0.8.33 published 18:02Z (22 assets) and Homebrew updated via the rerun of tag run 33878977142; that run reads "failure" only from the tag-to-crates.io SLO step. 0.8.31 Windows asset hashes verified intact. MEERKAT 0.8.33 COMPLETE.

**Meerkat** main = 898db4c20 (squash of #1095 M3 on 2026-09-05 01:33Z, tree identical to branch head e7d4cc44f; before that ae0394e65 #1100 docs mirror of MobKit v0.8.31 on top of ba6644317, the squash of #1087, whose stacked branch carried M1's seven commits too; #1092 closed as merged-by-#1087 because a required-check rule counted its failed SLO `CI gate` from the pull_request run and refused `gh pr merge`; fresh dispatch runs do not satisfy required checks, a new pull_request run does). Remaining stack: M3 #1095 @25871e551 -> #1096 @15eb9e1cc -> #1099 @6f3c18b11 -> #1098 @c4881a9ce (Bazel BUILD regenerated for the stacked mcp-server tests); re-pushes queued sequentially (my pkill mistake killed the first attempts). Then `release patch` -> 0.8.34.
- M1 #1092 fix/provider-request-compat @5e7c5ba4d (rebased on release main, gate passed)
- M2 #1087 fix/mob-author-experience @6222dfdb3 (gate passed incl. TLC)
- M3 #1095 MERGED as 898db4c20 (was fix/build-publish-infra @e7d4cc44f, 4 commits re-picked onto ae0394e65 on 2026-09-05 00:50Z via restack-pick.sh; docs/mobkit/_source.json conflict with #1100 resolved by regenerating the mirror from the local v0.8.31 checkout with M3's new sync script; force-push + gate started 00:52Z, log /tmp/rb/push-m3-restack.log). Chain re-stacked again 2026-09-05 02:40Z (de-flake first so later gates run de-flaked tests): #1096 MERGED as ed6113f60 on 2026-09-05 03:27Z (was @ccdf38790; gate PASSED at 03:06Z on the quiet machine as 96aacef33, same tree; re-picked onto main and re-pushed 03:16Z with cached evidence; hosted CI watcher /tmp/rb/ci-1096.log) -> #1106 MERGED as 669e18305 at 04:24Z (was fix/deflake-cross-host-loopback-retire @0c0cc4638; worktree meerkat-dflake-push; the meerkat-dflake worktree belongs to dflake2-meerkat's #1104 branch) -> #1099 fix/pre-push-target-retention @f1e98d165 (re-picked onto #1107 head 7ffee2c73; gate started 06:14Z, log /tmp/rb/push-1099b.log; earlier stacked gate failed on the #1104 class) -> #1098 fix/stale-discard-observability @4c9e1b699 -> feat/quota-exhausted-wire-kind @4d0a176ac (no PR yet; trees unchanged from 308ca80bf/42e32a52d/b1971dfbd) -> fix/sdk-child-stderr-and-close @6599a4f29 (re-picked onto 4d0a176ac, tree identical to b6a54ac84 which dsdk verified green: fmt, clippy on mcp/rpc/rest, 3 binary_fatal_error_display + 3 binary_tracing_stderr tests; ready to gate) -> fix/mob-actor-member-work-off-loop @e8a6db482 (dloop-meerkat 04:26Z: 5 commits on 6599a4f29 with all review fixups squashed in: arch 1/4/5, rq B1-B4, finding 7; actor_isolation 13/13 + event_pump 45/45; full meerkat-mob suite (--features test-support) 2569/2569 green on e8a6db482 at 06:05Z; new MobError variants MemberReloadRefused/MemberReloadTimedOut and MEMBER_RELOAD_TOTAL_TIMEOUT; actor_isolation 10/10, meerkat-runtime 1914/1914, meerkat-mob full suite green except 3 pre-existing test-support-feature panics; architecture review APPROVE with 5 non-blocking findings (saved /tmp/rb/arch-review-1102.md; findings 1/4/5 being fixed as fixups by dloop-meerkat, findings 2/3 deferred to issue #1105); rust-quality review pending) -> fix/teardown-grace-1104 @3f0afa1a3 (dflake2-meerkat DONE 04:00Z: 7 commits on 6fd47c34b in worktree meerkat-dflake; until-terminal test waits, stop_runtime_executor_until_terminal_if_current API, retirement disposal until-terminal, structural B.10 proof, nextest lane reservation, observation-load conflict budget 8, JSONL blocking flock with 60 s bound; 30x loops green; re-pick onto the chain when its turn comes). Rule: workers HOLD all cargo during a gate window (load-induced failures otherwise). Squash-merging a branch that sits exactly on main reproduces the branch tree, so a stacked branch re-picked onto the new main keeps the same tree and its gate evidence stays valid (that is why the chain is gated stacked, then re-picked and force-pushed after each merge). After dquota: fix/sdk-child-stderr-and-close @3a3491357 (#1103, DONE by worker dsdk 2026-09-05 02:05Z, 3 commits, based on ae0394e65 so it must be re-picked onto the chain head before its push; reviewed: 16 real files, Python 542 pass, fatal_error.rs Display chain for rkat-rpc/rest/mcp) and fix/mob-actor-member-work-off-loop (#1102 redesign, worker dloop-meerkat). CI watcher for #1095: /tmp/rb/ci-1095.log.
- #1099 fix/pre-push-target-retention (dprune) @8433f6e4b: redesigned (passed-gate only, 6h recency, live-process check); gate passed, CI running. The 14:43Z incident root cause was ENOSPC + keep=2 with no recency floor
- #1096 fix/release-doctor-drift (d1091) @035780497: gate passed, CI running
- #1098 fix/stale-discard-observability (d1090): 3 commits (#1090 docs/tests, fix A, loop NoPendingBoundary teardown guard); RBE durable recovery PASSED on 0bcfa2e7f (invocation c01ea79b); stacked head c4881a9ce on top of the retention branch; push re-queued (first attempt failed BUILD freshness), then un-draft.

Meerkat CI gotcha: the `CI gate` job enforces a 20-minute push-to-terminal SLO measured from the run's created_at. Pushing many branches at once makes BuildBuddy lanes contend and the gate fails with every job green; `gh run rerun` keeps the old created_at, so trigger a FRESH run (`gh workflow run ci.yml --ref <branch>`) once the queue is quiet. Stagger pushes.

**MobKit** (main b96552f3 frozen)
- Step 1 DONE: repin #399 merged (424e0f05); release PR #400 squash-merged as 255c65ae `chore: release v0.8.31 (#400)`. MOBKIT 0.8.31 COMPLETE and the 0.8.33/0.8.31 pair CLOSED downstream (HomeCore run-9b 2/2 on the published asset, activation 134 live, OB3 registry-pair PASS, active-occurrence liveness proof at 20:47:59Z; announced closed on the bus 20:52Z): tag v0.8.31 (af8e115b) on 255c65ae; release run 33901667164 all green (5 binaries, GitHub release, crate + SDK packages, docs). Verified crates.io meerkat-mobkit 0.8.31, PyPI meerkat-mobkit, npm, GitHub assets. MobKit main is now 255c65ae; K1-K3 and #396/#397 need one more rebase onto it (changelog stamp conflict) before merging.
- Step 2/3 STACK on release main 255c65ae (per-commit changelog refit via /tmp/rb/restack.sh; non-changelog diffs verified unchanged): K1 #393 @86185ae0 (14 commits; BUILD regenerated so CARGO_PKG_VERSION stamps are 0.8.31, the version-parity CI gate requires it) <- K2 #395 @53698c09 <- K3 #394 @697ac6dc <- #396 roster @bb51a92a <- #397 health @eb4224d5 (final v3 stack). ALL MERGED: K1 #393 -> 3af4fb81, K2 #395 -> 66db3a1e, K3 #394 -> 4c90b513, roster #396 -> db988f17, health #397 -> de602d55 (MobKit main de602d55 as of 20:15Z). Merge in that order with MERGE COMMITS (`Merge pull request #N: title`) after v0.8.31 is tagged; each head needs a green CI run (dispatch for fix/docs branches). Flow-editor browser smoke flakes ~1 in 2 runs (#398).
- MobKit 0.8.32 content beyond the stack: de-flake branch (dflake-mobkit: #398 flow-editor smoke, #401 swarm timing) and fix/schedule-watchdog-paused-rows (dwatchdog: HomeCore item, paused/deleted schedules' overdue rows must not count as stalled). HomeCore item (2) per_turn_injection default is already in 0.8.31 (f82cb29e), answered on the bus.
- Follow-up to investigate (from HomeCore END records for activations 134/135 on 0.8.33/0.8.31): every boot on this tuple shows a single `actor_loop_stalled` at materialization (not fatal, 17/17 healthy). Possibly related to the in-loop materialization path analysed in meerkat #1093. Ask HomeCore for the exact log line and timing when working 0.8.32/0.8.34 follow-ups.
- OB3 field finding 2026-09-04 22:25Z (meerkat #1102): one member's repair-blocked persistent runtime (durable projection failed -> mark_durability_reload_required) wedged deliver.submit_work admissions to UNRELATED members for the full 600 s budget; fleet events stopped. Latent on 0.8.29 too. Worker dwedge is tracing the shared-lock chain and whether MobKit has a per-member cold-reload verb. LUKA'S INSTRUCTION (2026-09-04 ~22:40Z): consider whether the serialized mob-actor loop is a design mistake; if so REDESIGN it, test thoroughly (deterministic isolation harness: one member wedged, N others keep completing; per-member ordering preserved; load variant), and roll it into the release (MobKit 0.8.32, Meerkat 0.8.34 for any runtime seam). Design verdict to be posted on the bus before implementation. This is now the primary objective for 0.8.32.
- Step 4: repin to 0.8.34 -> release v0.8.32.
- Both repos have a required-check rule: `gh pr merge` is refused ("add --admin") while any check-run on the head is failed, even if a parallel run is green. Remedy: `gh run rerun <failed-run> --failed` (updates the same check-runs) and merge after it passes; do not use --admin.
- MobKit CI: pull_request events do not run; dispatch `gh workflow run ci.yml --ref <branch>` and read commit check-runs. ANTHROPIC_API_KEY secret added 2026-09-04 (e2e-live green).

Helper scripts live in /tmp/rb on the VM (resolve-changelog.py for release-stamp conflicts, resolve-changelog-union.py for concurrent-addition conflicts; both drop literal conflict-marker lines).


## #1102 redesign (decided 2026-09-05 00:58Z, Luka: "consider if that serialized approach is a mistake. if so, redesign, test thoroughly and roll into the release")
Verdict (analyst report /tmp/rb/dwedge-report.md, also posted as a comment on meerkat #1102 and on the bus): the serialized mob-actor loop is not the mistake; per-member blocking work executed inline in it is. Adopted option A-lite + C. Two implementation branches, lead pushes them:
- Meerkat: /home/luka/src/wt/meerkat-dloop, branch fix/mob-actor-member-work-off-loop (worker dloop-meerkat): ensure-steps out of handle_submit_work into the spawned admission task, bounded ensure_pump join, ResumeLifecycle readiness as JoinSet re-entering as commands, skip closed-reply commands + bounded submit_work, typed MobError::MemberReloadRequired, MobHandle::reload_member_registration, inline-step watchdog, per-member single-flight; deterministic isolation/boot/load/ordering tests. Ships in 0.8.34 (goes into the Meerkat push chain after dquota).
- MobKit: /home/luka/src/wt/mobkit-dloop, branch fix/actor-stall-breaker-reload-verb @754769ae (part 1 rebased 04:36Z onto MobKit main 1d9577e00 after PR #403 showed CONFLICTING on CHANGELOG; worktree mobkit-dloop1; PR #403 MERGED 06:30Z as b669d362b (merge commit)) and fix/actor-stall-breaker-reload-verb-0834 @1d54237c (rebased onto 754769ae 04:59Z; part 2 + BacklogFull/ReloadRefused/ReloadTimedOut typed mappings, member_health.last_reload; hooks off, lane 240/240 against meerkat e8a6db482; to be rebased onto 754769ae and recommitted with hooks after the 0.8.34 repin; API contract copied from dloop-meerkat's UNCOMMITTED tree, re-verify after it commits). Full report /tmp/rb/dloop-mobkit-report.md. Finding: on 0.8.31 retire_member then send does NOT re-materialize (Retiring never -> Dormant); corrected on the bus and #1102 on 2026-09-05 02:50Z. Part 1 against =0.8.33 (stall_id circuit breaker, probe channel-closed = terminated, mobkit/member_health, mobkit/reload_member via retire+rematerialize, typed reload-required classification); part 2 on branch fix/actor-stall-breaker-reload-verb-0834 (uses the 0.8.34 primitives, committed with hooks off, recommit after the repin). Ships in 0.8.32. Part 1 gate passed 04:17Z, PR #403 opened, CI run 33938690795 dispatched (watcher /tmp/rb/ci-mobkit-403.log); never check out branches in a worker's worktree.
- Acceptance: HomeCore reruns the isolation and boot variants on its production clone lineage before 0.8.32 tags (HomeCore invariant: one member's blocking work cannot delay another member's dispatch or the liveness probe).

De-flake workers restarted 2026-09-05 01:35Z after the first pair (dflake-meerkat, dflake-mobkit, dwatchdog) went 3.5 h with no branch and no reply to two pings: dflake2-meerkat in /home/luka/src/wt/meerkat-dflake (fix/deflake-cross-host-loopback-retire, issues #1094 #1097 #1101) DONE 2026-09-05 02:31Z @1f5e25cf0, 3 test-only commits on 898db4c20, reviewed; plan: gate it right after #1096 and re-stack #1099/#1098/dquota/dsdk on top so later gates run the de-flaked tests. Same worker continues on #1104 (load-sensitive teardown tests; analysis approved: witness+until_terminal test fixes, stop_runtime_executor until-terminal API, retirement archive must not treat in-progress teardown as terminal) on branch fix/teardown-grace-1104, dflake2-mobkit in /home/luka/src/wt/mobkit-dflake (fix/deflake-flow-editor-swarm, issues #398 #401) DONE @2fc0f7cf, 2 commits on de602d55; PR #402 MERGED 03:58Z as 1d9577e00 (merge commit). The watchdog scope was folded into dloop-mobkit part 1 (stall breaker + probe semantics).


LESSON 2026-09-05 03:15Z: a stacked PR head whose parent is the pre-squash branch (not main) shows CONFLICTING/DIRTY on GitHub once the parent PR is squash-merged (CHANGELOG hunk), and GitHub then creates NO pull_request workflow runs at all (only pull_request_target ones), so CI never appears; close/reopen does not help. Always re-pick the next branch onto the new main BEFORE pushing it (tree stays identical, so the gate evidence cache makes the push fast). #1096 was re-picked to ccdf38790 (same tree as 96aacef33) and force-pushed.


REORDER 2026-09-05 04:55Z: the #1099 gate (hook scripts only) failed 7 tests of the #1104 caller-grace class with just one capped worker running, so fix/teardown-grace-1104 moves to the FRONT of the queue: re-picked onto main 669e18305 as 7ffee2c73 = PR #1107 MERGED as 605b288a2 at 06:18Z (tree identical to 7ffee2c73). After it merges, re-pick #1099 (c7ed1d59c content), #1098, quota, #1103, #1102 onto the new main in that order, one gate at a time. MobKit part 1 (754769ae) gate push waits for a CPU window.


TLC lane note 2026-09-05 05:20Z: the #1104 gate (7ffee2c73) failed in `machine codegen + verify` with a Java StackOverflowError inside the bounded adaptive_mob_bundle witness (TLC 192 workers, -Xss256m) while computing the initial state; the branch commits no spec changes (the hook regenerates from the meerkat_machine manifests because it adds the StopRuntimeExecutorUntilTerminal command). `make machine-verify` on the release checkout (48e5610c1) passes in 4 min; a direct run on the 1104 worktree PASSED too (05:22Z, no regenerated-file drift), so the overflow was transient; gate re-pushed 05:23Z (log /tmp/rb/push-1104b.log). Hosted CI never runs this lane (cargo.yml's machine-verify job is not part of the CI runs), so it is local-only. Local tla2tools.jar is a 2026-09-03 master nightly (the v1.8.0 release URL is a rolling pre-release).


CHAIN 2026-09-05 06:20Z after #1107 merged (main 605b288a2): #1099 MERGED as 1b88dfe50 at 07:14Z (was @7f05005b7) -> #1098 MERGED as 524386368 at 08:00Z (was @3bcc3b588) -> feat/quota-exhausted-wire-kind @13ed2245d (gate started 07:46Z stacked on c5420046c, log /tmp/rb/push-dquota.log; no PR yet) -> fix/sdk-child-stderr-and-close @f1c69c3bf (#1103) -> fix/mob-actor-member-work-off-loop @2cfcf3107 (#1102; on the re-picked base actor_isolation+event_pump 58/58 and cold_restart_mob_resume+host_materialize_serving binaries 49/49 green, 06:27Z). Each gets gated stacked on its predecessor, then re-picked onto main after the predecessor's squash merge (tree identical, cached evidence).


MobKit #404 (2026-09-05 07:55Z, OB3 finding): after retire_member the identity is stuck in Retiring, so reconcile_roster_remove refuses to remove it and send cannot re-materialize it. Worker dloop-mobkit fixed it on fix/retired-identity-removable @9251b52c = PR #405 MERGED 08:45Z as e08aaa2a7 (merge commit), off MobKit main b669d362b; Retiring kept as the retired terminal state, revived only by operations addressed to it, indirect hydration refuses typed; lane 312/312) for 0.8.32; ready to gate + PR when CPU allows. OB3 twin-full run 3 on 0.8.33/0.8.31 with their client-timeout fix: PASS 50/0/9, 97/97 reviewed, graceful shutdowns 5-21 s.


WASM BREAK 2026-09-05 07:57Z: the quota branch gate ran `make test-sdk-web` (because it touches sdks/web/src/generated/events.ts) and the wasm32 build of meerkat-anthropic failed: #1087 (M1 commit a2d34fc70, cache-control knobs) imports AnthropicCacheControlPolicy under #[cfg(not(target_arch = "wasm32"))] while default_cache_control_for_backend uses it unconditionally. Main has been broken for @rkat/web since ba6644317; none of the later gates or hosted CI (WASM check submitter) caught it. Fix (import made unconditional, the type is not cfg-gated in meerkat-core) folded into the quota branch as its own commit; verification via `make test-sdk-web` in the dquota worktree (/tmp/rb/test-sdk-web-quota.log). Mandatory for the 0.8.34 release (the release lane builds @rkat/web).


OB3 END RECORD 2026-09-05 08:14Z for the 0.8.33/0.8.31 repin: run 3 PASS (50/0/9, 97/97 reviewed in 7.5 min, 0 errors, graceful shutdowns 5-21 s); runs 1-2 failed only on OB3's own 30 s client timeout that surfaced meerkat #1102. Deploy is Luka's decision; their deploy PR carries MOBKIT_BRIDGE_ACTOR_ADMISSION_SECS=120 and actor-loop debug logging. Retire-during-cycle probe: hand-off to a Retiring target fails typed (IdentityMaterializationFailure) and the cycle completes; same #404 bug underneath.


2026-09-05 08:55Z: quota branch gained commit d37ea46a4 (wasm32 import fix); `BINARYEN_CORES=16 make test-sdk-web` passed end to end in 30 min (wasm-opt on the 100 MB opt-level-0 module is the slow step; at 193 threads it never finishes). Quota re-picked onto main 524386368; gate push must carry BINARYEN_CORES=16 and will take ~65 min. PR body drafted at /tmp/rb/pr-dquota.md.


CHAIN 2026-09-05 08:57Z (main 524386368 after #1098): PR #1109 MERGED as dc0dd074e at 10:15Z (was feat/quota-exhausted-wire-kind @e1abec4c2) -> fix/sdk-child-stderr-and-close @136bc2037 (#1103; gate started 09:57Z stacked on e1abec4c2, log /tmp/rb/push-dsdk.log, no PR yet, body /tmp/rb/pr-dsdk.md) -> fix/mob-actor-member-work-off-loop @30528d843 (#1102). Then `release patch` -> 0.8.34 (push main, wait CI + semver readiness, push tag), verify registries, then MobKit repin to =0.8.34, recommit the 0834 branch with hooks, release 0.8.32.
