SPECIFICATION Spec
CONSTANTS
  BooleanValues = {TRUE, FALSE}
  DetachedJobRestartClassValues = {"Adoptable", "CheckpointResumable", "Replayable", "NonResumable"}
  DetachedJobTerminalKindValues = {"Succeeded", "Failed", "Cancelled", "WorkerLost", "NeedsAttention"}
  NatValues = {1}
  StringValues = {"alpha"}
INVARIANTS
  fence_tracks_claim_count
  no_attempt_has_no_attempt_authority
  checkpoint_requires_attempt
  active_execution_requires_attempt_authority
  terminal_requires_delivery
  nonterminal_has_no_terminal_delivery
CONSTRAINTS
  CiStateConstraint
