# A14: Agent definitions and generic development skills

This ledger preserves the baseline evidence and independent disposition. See [the audit index](README.md) for scope, commits, and publication boundaries.

## A14-001: Active comms reviewers require specification and checklist files absent from the checkout

**Candidate severity:** medium. **Independent verdict:** confirm.

A caller using the advertised phase-review prompt cannot establish the phase contract as instructed. The agents either stop immediately or risk inventing a replacement specification.

### Baseline documentation evidence

- [.claude/agents/integration-sheriff.md:36-37](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/integration-sheriff.md?plain=1#L36-L37)

```text
Read `CHECKLIST-COMMS.md` to understand what Phase X covers.
```

- [.claude/agents/rct-guardian.md:35-36](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rct-guardian.md?plain=1#L35-L36)

```text
Read `CHECKLIST-COMMS.md` to understand what Phase X covers.
```

- [.claude/agents/rct-guardian.md:62-67](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rct-guardian.md?plain=1#L62-L67)

```text
Config defaults don't match DESIGN-COMMS.md spec
```

- [.claude/agents/spec-auditor.md:33-37](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/spec-auditor.md?plain=1#L33-L37)

```text
Read the relevant sections of `DESIGN-COMMS.md` carefully. Note every requirement.
```

- [.claude/agents/methodology-integrity.md:44-49](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/methodology-integrity.md?plain=1#L44-L49)

```text
Read DESIGN-COMMS.md and cross-reference.
```

- [.claude/agents/methodology-integrity.md:60-63](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/methodology-integrity.md?plain=1#L60-L63)

```text
Read CHECKLIST-COMMS.md. For tasks marked `[x]`:
```

### Implementation proof

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
for name in ('CHECKLIST-COMMS.md','DESIGN-COMMS.md'):
 print(f'{name}: exists={Path(name).exists()}')
PY
git ls-files -- 'CHECKLIST-COMMS.md' 'DESIGN-COMMS.md' ':(glob)**/CHECKLIST-COMMS.md' ':(glob)**/DESIGN-COMMS.md'
```

  CHECKLIST-COMMS.md: exists=False
  DESIGN-COMMS.md: exists=False
  The tracked-file query produced no paths.

  These are mandatory inputs in currently registered agent instructions, but neither root file nor a tracked relocation with the same basename exists. This is a reproducible lookup failure, not disagreement with a historical specification.

### Independent challenge

The advertised invocation supplies only a phase, but the four active agent definitions require fixed COMMS files that the checkout does not contain. This is a missing-input/portability defect, not evidence that an old comms specification is substantively wrong. The unrelated image-generation specification under .rct is not a justified replacement.

- [.claude/agents/integration-sheriff.md:33-37](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/integration-sheriff.md?plain=1#L33-L37)

```text
Read `CHECKLIST-COMMS.md` to understand what Phase X covers.
```

  The current review procedure requires this particular input, rather than accepting an invocation-supplied checklist.

- [.claude/agents/spec-auditor.md:33-37](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/spec-auditor.md?plain=1#L33-L37)

```text
Read the relevant sections of `DESIGN-COMMS.md` carefully. Note every requirement.
```

  The specification is likewise a mandatory named input.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path, PurePosixPath
import subprocess
paths=subprocess.check_output(['git','ls-files','-z'],text=True).split('\0')
for name in ('CHECKLIST-COMMS.md','DESIGN-COMMS.md'):
 print(name+': root_exists='+str(Path(name).exists())+'; tracked_basename_matches='+repr([p for p in paths if PurePosixPath(p).name==name]))
PY
```

  CHECKLIST-COMMS.md: root_exists=False; tracked_basename_matches=[]
  DESIGN-COMMS.md: root_exists=False; tracked_basename_matches=[]

  Independently checked both the working-tree lookups and the complete tracked-file basename inventory. No relocation with either basename supplies the required inputs.

**Required correction:** In all four agent definitions, replace assumptions that these root-level files exist with an explicit phase plus caller-supplied, verified repository-relative specification/checklist input contract. Update repeated role, lookup, and comparison references consistently. If retaining the original comms-only historical workflow, label that scope and report unavailable inputs rather than inventing a substitute specification. Do not create replacement COMMS documents or repurpose .rct metadata.

### Correction

All four descriptions and review procedures now require an invocation-supplied phase plus repository-relative specification and checklist paths. They verify readable inputs within the active checkout, refuse external paths/symlinks, and report unavailable review contracts instead of inventing requirements or substituting unrelated .rct metadata. Repeated role, comparison, and checklist references use those supplied inputs. No replacement COMMS files were created.

- [.claude/agents/integration-sheriff.md:34-37](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/integration-sheriff.md?plain=1#L34-L37)

```text
- The specification and checklist paths, supplied by the invoking task and relative to the active repository root.
```

- [.claude/agents/rct-guardian.md:33-36](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rct-guardian.md?plain=1#L33-L36)

```text
If an input is missing or unavailable, report the missing review contract instead of issuing a compliance verdict.
```

- [.claude/agents/spec-auditor.md:40-44](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/spec-auditor.md?plain=1#L40-L44)

```text
Read the supplied checklist to understand what Phase X covers. Note which sections of the supplied specification apply.
```

- [.claude/agents/methodology-integrity.md:28-31](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/methodology-integrity.md?plain=1#L28-L31)

```text
Verify that both inputs are readable files within the active checkout and that the checklist identifies the requested final phase.
```

- Read-only reproduction:

```text
python3 -B -c "from pathlib import Path, PurePosixPath; import subprocess; tracked=subprocess.check_output(['git','ls-files','-z'],text=True).split('\0'); print({name: {'root_exists': Path(name).exists(), 'tracked_matches': [p for p in tracked if PurePosixPath(p).name==name]} for name in ('CHECKLIST-COMMS.md','DESIGN-COMMS.md')})"
```

  Both names have root_exists=False and tracked_matches=[].

  The current checkout cannot supply the former mandatory fixed inputs. No external path was inspected.

### Final independent review

**PASS:** All four comms reviewers now require a phase and caller-supplied repository-relative specification/checklist files, verify the requested phase and readable in-checkout inputs, and report an unavailable contract instead of inventing requirements. Their descriptions and repeated specification/checklist references agree with that contract. The missing legacy files were not fabricated, and the existing unrelated .rct task was not substituted.

- [.claude/agents/integration-sheriff.md:34-44](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/integration-sheriff.md?plain=1#L34-L44)

```text
If an input is missing or unavailable, report the missing review contract instead of issuing a compliance verdict.
```

  The required inputs and scope lookup now use the supplied in-checkout contract; the frontmatter invocation at line 3 also supplies both paths.

- [.claude/agents/rct-guardian.md:33-43](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rct-guardian.md?plain=1#L33-L43)

```text
- The specification and checklist paths, supplied by the invoking task and relative to the active repository root.
```

  The representation reviewer has the same explicit inputs, containment check, and unavailable-contract behavior. Its remaining default-comparison rule at line 74 now refers to the supplied specification.

- [.claude/agents/spec-auditor.md:31-44](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/spec-auditor.md?plain=1#L31-L44)

```text
Do not invent requirements or substitute unrelated `.rct/` metadata or an archived design.
```

  The specification reviewer verifies the caller's files before reading their phase-specific requirements. Its role at line 11 also names the caller-supplied specification.

- [.claude/agents/methodology-integrity.md:28-35](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/methodology-integrity.md?plain=1#L28-L35)

```text
Verify that both inputs are readable files within the active checkout and that the checklist identifies the requested final phase.
```

  The final-phase reviewer now has the same explicit contract. Its deferral and completion checks at lines 57 and 70 use the supplied specification and checklist.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path, PurePosixPath
import subprocess
tracked=subprocess.check_output(['git','ls-files','-z'],text=True).split('\0')
for name in ('CHECKLIST-COMMS.md','DESIGN-COMMS.md'):
    print(name, {'root_exists': Path(name).exists(), 'tracked_basename_matches': [p for p in tracked if PurePosixPath(p).name==name]})
PY
```

  CHECKLIST-COMMS.md {'root_exists': False, 'tracked_basename_matches': []}
  DESIGN-COMMS.md {'root_exists': False, 'tracked_basename_matches': []}

  Independently checked both actual root lookups and the complete tracked basename inventory. Reporting missing caller inputs is appropriate; no replacement COMMS documents exist.

## A14-002: The spec-accuracy reviewer is pinned to another personal checkout

**Candidate severity:** medium. **Independent verdict:** confirm.

The reviewer ignores the caller's actual repository/plan and always reads a fixed personal worktree. Even if that path exists for its owner, it can provide a different revision's contract and is not a portable review input.

### Baseline documentation evidence

- [.claude/agents/spec-accuracy-gate.md:21-22](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/spec-accuracy-gate.md?plain=1#L21-L22)

```text
- **The plan**: `/Users/luka/.codex/worktrees/a4b6/raik/docs/plan-combined-roadmap.md` — Read this file first, every time.
```

### Implementation proof

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path, PurePosixPath
import re
text=Path('.claude/agents/spec-accuracy-gate.md').read_text()
path=re.search(r'\*\*The plan\*\*: `([^`]+)`',text).group(1)
print('Forced roadmap input:',path)
print('Absolute:',PurePosixPath(path).is_absolute())
print('Lexically within checkout:',PurePosixPath(path).is_relative_to(PurePosixPath(str(Path.cwd()))))
PY
```

  Forced roadmap input: /Users/luka/.codex/worktrees/a4b6/raik/docs/plan-combined-roadmap.md
  Absolute: True
  Lexically within checkout: False

  The required input is lexically outside this checkout and cannot be supplied by cloning this repository. The command inspects only the assigned agent file; it does not follow or test the external path.

### Independent challenge

The instruction is unconditional and tied to a specific personal checkout, not an illustrative placeholder or a supplied task input. Its lexical location alone proves the review is not bound to the active checkout. I did not inspect the external file and make no claim about its existence, contents, or historical correctness.

- [.claude/agents/spec-accuracy-gate.md:19-22](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/spec-accuracy-gate.md?plain=1#L19-L22)

```text
- **The plan**: `/Users/luka/.codex/worktrees/a4b6/raik/docs/plan-combined-roadmap.md` — Read this file first, every time.
```

  This names an actual personal absolute path and mandates it for every review.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path, PurePosixPath
import re
text=Path('.claude/agents/spec-accuracy-gate.md').read_text()
roadmap=PurePosixPath(re.search(r'\*\*The plan\*\*: `([^`]+)`',text).group(1))
print('Forced roadmap absolute:',roadmap.is_absolute())
print('Forced roadmap lexically within checkout:',roadmap.is_relative_to(PurePosixPath(str(Path.cwd()))))
PY
```

  Forced roadmap absolute: True
  Forced roadmap lexically within checkout: False

  The check reads only the agent definition and compares path components; it neither stats nor follows the personal target.

**Required correction:** Make the plan path an explicit invocation input alongside the phase. Resolve repository-relative inputs against the active checkout and verify availability before review; report a missing contract instead of reading a fixed personal worktree. Do not assert that the former target is absent or rewrite the historical roadmap.

### Correction

Removed the unconditional personal-worktree roadmap path. The agent description, all three invocation examples, input contract, and extraction step now use the caller-supplied repository-relative plan and phase. Missing/unreadable plans or missing phases produce an unavailable-contract report; paths and symlinks into another checkout are excluded. No claim is made about the former external file's existence or contents.

- [.claude/agents/spec-accuracy-gate.md:3-3](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/spec-accuracy-gate.md?plain=1#L3)

```text
Supply the phase number and a repository-relative plan path.
```

- [.claude/agents/spec-accuracy-gate.md:21-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/spec-accuracy-gate.md?plain=1#L21-L24)

```text
- **The plan path**: A repository-relative path supplied by the invoking task.
```

- [.claude/agents/spec-accuracy-gate.md:24-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/spec-accuracy-gate.md?plain=1#L24)

```text
If the plan or phase is missing or unavailable, report the unavailable review contract with the missing input; do not invent a replacement plan or infer requirements from the implementation.
```

### Final independent review

**PASS:** The fixed personal-worktree roadmap has been removed. The agent description, all three invocation examples, required inputs, and contract-extraction step consistently use the supplied repository-relative plan and phase. Missing inputs are explicitly not grounds to infer a plan from implementation. No assertion about the old external file's existence was added.

- [.claude/agents/spec-accuracy-gate.md:3-3](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/spec-accuracy-gate.md?plain=1#L3)

```text
Supply the phase number and a repository-relative plan path.
```

  The public invocation contract now supplies the necessary plan. Each of the three examples on the same line includes the repository-relative plan path.

- [.claude/agents/spec-accuracy-gate.md:21-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/spec-accuracy-gate.md?plain=1#L21-L24)

```text
Resolve the plan path against the active repository root. Verify that it is a readable file within this checkout and that it contains the requested phase before reviewing.
```

  The active checkout, not a personal worktree, owns path resolution. The rest of line 24 excludes cross-checkout paths/symlinks and requires an unavailable-contract report for missing inputs.

- [.claude/agents/spec-accuracy-gate.md:28-29](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/spec-accuracy-gate.md?plain=1#L28-L29)

```text
Read the verified, caller-supplied plan file thoroughly.
```

  The actual review procedure no longer reintroduces the removed path.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
p=Path('.claude/agents/spec-accuracy-gate.md')
s=p.read_text()
print({'personal_path_present': '/Users/' in s or '/home/' in s, 'examples_with_relative_plan': s.count('and the repository-relative plan path.')})
PY
```

  {'personal_path_present': False, 'examples_with_relative_plan': 3}

  The complete current agent definition, rather than the inaccessible former roadmap, is the authority for its invocation instructions. The baseline diff contains the removal, not relocation to another absolute path.

## A14-003: The methodology review's comms stub scan names a nonexistent directory

**Candidate severity:** low. **Independent verdict:** confirm.

Both mandatory stub scans emit a filesystem error and omit the intended core module. Treating their output as a clean scan would miss part of the review scope.

### Baseline documentation evidence

- [.claude/agents/methodology-integrity.md:30-34](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/methodology-integrity.md?plain=1#L30-L34)

```text
grep -r "todo!" meerkat-comms*/src/ meerkat-core/src/comms
```

- [.claude/agents/methodology-integrity.md:30-34](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/methodology-integrity.md?plain=1#L30-L34)

```text
grep -r "unimplemented!" meerkat-comms*/src/ meerkat-core/src/comms
```

### Implementation proof

- Read-only reproduction:

```text
grep -r 'todo!' meerkat-core/src/comms; printf 'documented grep exit=%s\n' "$?"
```

  grep: meerkat-core/src/comms: No such file or directory
  documented grep exit=2

  The documented operand fails before inspecting core's comms module.

- [meerkat-core/src/comms.rs:79-79](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/comms.rs#L79)

```text
pub struct PeerId(
```

  Core's comms types are implemented in the file comms.rs, not the referenced directory.

### Independent challenge

Both scan commands contain an operand that genuinely fails. This is not a generic-template adaptation preference: the agent names Meerkat's existing source layout, whose core comms module is a file. The error can leave the intended core portion uninspected.

- [.claude/agents/methodology-integrity.md:30-34](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/methodology-integrity.md?plain=1#L30-L34)

```text
grep -r "todo!" meerkat-comms*/src/ meerkat-core/src/comms
grep -r "unimplemented!" meerkat-comms*/src/ meerkat-core/src/comms
```

  Both concrete commands use the missing path without the .rs extension.

- [meerkat-core/src/comms.rs:77-79](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/comms.rs#L77-L79)

```text
pub struct PeerId(#[cfg_attr(feature = "schema", schemars(with = "String"))] pub Uuid);
```

  The intended source exists at the file path comms.rs.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
import subprocess
for needle in ('todo!', 'unimplemented!'):
 r=subprocess.run(['grep','-r',needle,'meerkat-core/src/comms'],text=True,capture_output=True)
 print('Missing comms operand:',needle,'exit='+str(r.returncode),r.stderr.strip())
print('Actual core comms.rs file:',Path('meerkat-core/src/comms.rs').is_file())
PY
```

  Missing comms operand: todo! exit=2 grep: meerkat-core/src/comms: No such file or directory
  Missing comms operand: unimplemented! exit=2 grep: meerkat-core/src/comms: No such file or directory
  Actual core comms.rs file: True

  Independently reproduced the erroneous operand without running a test suite or altering files.

**Required correction:** Change meerkat-core/src/comms to meerkat-core/src/comms.rs in both stub scans, retaining the comms crate source scope. Do not characterize grep errors as a clean scan; a no-match exit and a filesystem error are different outcomes.

### Correction

Both comms stub scans now include meerkat-core/src/comms.rs while retaining meerkat-comms*/src/. Added the grep exit-status distinction so no-match results are not confused with filesystem/scan failures. Executed both corrected read-only scans after checking their source trees for symlinks: each returned 1 with zero matches and empty stderr.

- [.claude/agents/methodology-integrity.md:39-40](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/methodology-integrity.md?plain=1#L39-L40)

```text
grep -r "todo!" meerkat-comms*/src/ meerkat-core/src/comms.rs
grep -r "unimplemented!" meerkat-comms*/src/ meerkat-core/src/comms.rs
```

- [.claude/agents/methodology-integrity.md:42-42](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/methodology-integrity.md?plain=1#L42)

```text
For these scans, grep exit 0 means matches, 1 means no matches, and greater than 1 means a scan error. Report scan errors; they are not evidence of clean code.
```

- [meerkat-core/src/comms.rs:79-79](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/comms.rs#L79)

```text
pub struct PeerId(#[cfg_attr(feature = "schema", schemars(with = "String"))] pub Uuid);
```

### Final independent review

**PASS:** Both stub scans now inspect the real core comms file while retaining the comms crate source directory. I executed both corrected scans after checking the scanned trees for symlinks: each returned exit 1, empty stdout, and empty stderr. The added explanation correctly separates no matches from a filesystem/scan failure.

- [.claude/agents/methodology-integrity.md:39-42](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/methodology-integrity.md?plain=1#L39-L42)

```text
grep -r "todo!" meerkat-comms*/src/ meerkat-core/src/comms.rs
grep -r "unimplemented!" meerkat-comms*/src/ meerkat-core/src/comms.rs
```

  Both formerly failing operands were corrected, not just one.

- [.claude/agents/methodology-integrity.md:42-42](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/methodology-integrity.md?plain=1#L42)

```text
For these scans, grep exit 0 means matches, 1 means no matches, and greater than 1 means a scan error. Report scan errors; they are not evidence of clean code.
```

  The instructions do not turn a failed scan into a clean-code assertion.

- [meerkat-core/src/comms.rs:77-79](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/comms.rs#L77-L79)

```text
pub struct PeerId(#[cfg_attr(feature = "schema", schemars(with = "String"))] pub Uuid);
```

  The corrected operand is an actual core comms implementation file.

- Read-only reproduction:

```text
python3 -B - <<'PY'
import glob, os, subprocess
from pathlib import Path
roots=glob.glob('meerkat-comms*/src/')
assert roots
for root in roots:
    for directory, dirs, files in os.walk(root,followlinks=False):
        assert not any((Path(directory)/n).is_symlink() for n in dirs+files)
for needle in ('todo!','unimplemented!'):
    r=subprocess.run(['grep','-r',needle,*roots,'meerkat-core/src/comms.rs'],capture_output=True,text=True)
    print(needle, 'exit', r.returncode, 'stdout', repr(r.stdout), 'stderr', repr(r.stderr))
PY
```

  todo! exit 1 stdout '' stderr ''
  unimplemented! exit 1 stdout '' stderr ''

  These are real read-only scans of the corrected scope, not simulated test results. This only verifies the scan behavior, not phase completion.

## A14-004: Comms review rules misclassify explicitly selected test lanes as XFAIL abuse

**Candidate severity:** medium. **Independent verdict:** reject.

The unconditional BLOCK rule rejects valid test organization that the repository's own runner deliberately uses. Reviewers need to detect suppressed required coverage, not demand that resource/provider-dependent lane tests run in every broad suite.

### Baseline documentation evidence

- [.claude/agents/integration-sheriff.md:44-49](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/integration-sheriff.md?plain=1#L44-L49)

```text
Ignored tests are only acceptable if they reference an external bug number.
```

- [.claude/agents/integration-sheriff.md:79-85](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/integration-sheriff.md?plain=1#L79-L85)

```text
- `#[ignore]` on feature tests without external bug reference
```

- [.claude/agents/methodology-integrity.md:37-42](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/methodology-integrity.md?plain=1#L37-L42)

```text
`#[ignore]` on feature tests without external bug reference = BLOCK.
```

- [.claude/agents/methodology-integrity.md:67-76](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/methodology-integrity.md?plain=1#L67-L76)

```text
- `#[ignore]` on feature tests without bug reference
```

### Implementation proof

- [meerkat-comms/tests/e2e.rs:337-339](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-comms/tests/e2e.rs#L337-L339)

```text
#[ignore = "lane:e2e-smoke"]
```

  A real comms feature test is intentionally opted out of broad runs by a named-lane annotation, not an external-bug reference.

- [tests/integration/src/e2e_lanes.rs:4520-4537](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/integration/src/e2e_lanes.rs#L4520-L4537)

```text
test_name: "e2e_smoke_mcp_multimodal_blob_response_transport",
```

  Scenario 83 registers this exact comms test as a Smoke-lane CargoTest with integration-real-tests enabled. It is not an abandoned or silently skipped implementation test.

- [tests/integration/src/e2e_lanes.rs:1470-1477](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/integration/src/e2e_lanes.rs#L1470-L1477)

```text
"--ignored",
```

  The canonical CargoTest runner explicitly selects ignored tests, making the annotation a lane-selection mechanism.

- [.cargo/config.toml:71-77](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.cargo/config.toml#L71-L77)

```text
e2e-smoke = "nextest run -p meerkat-integration-tests --test e2e_smoke_lane --run-ignored ignored-only
```

  The repository exposes the corresponding opt-in lane through its committed command catalog.

### Independent challenge

The repository certainly has intentional opt-in tests, but this does not by itself falsify a normative, phase-scoped reviewer acceptance rule. More importantly, the candidate's concrete example does not match the documented detector: the pattern requires the closing bracket immediately after ignore, whereas the cited test uses an annotated lane marker. The Integration Sheriff command also scans only core, not the cited comms test file. No actual false BLOCK for a required phase test was demonstrated. Changing the acceptance policy to bless lane markers would be a policy change, not a proven documentation-accuracy repair.

- [.claude/agents/methodology-integrity.md:37-42](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/methodology-integrity.md?plain=1#L37-L42)

```text
grep -r "#\[ignore\]" meerkat-comms*/src/ meerkat-comms*/tests/ meerkat-core/src/ meerkat-core/tests/
```

  The executable detector specifically matches bare #[ignore], not #[ignore = "..."] attributes.

- [.claude/agents/integration-sheriff.md:44-49](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/integration-sheriff.md?plain=1#L44-L49)

```text
grep -r "#\[ignore\]" meerkat-core/src/ meerkat-core/tests/
```

  This detector does not inspect the comms test used as the candidate's example; the accompanying acceptance statement is review policy.

- [meerkat-comms/tests/e2e.rs:337-339](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-comms/tests/e2e.rs#L337-L339)

```text
#[ignore = "lane:e2e-smoke"]
```

  The actual annotation is materially different from the detector's literal target.

- [tests/integration/src/e2e_lanes.rs:4520-4537](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/tests/integration/src/e2e_lanes.rs#L4520-L4537)

```text
test_name: "e2e_smoke_mcp_multimodal_blob_response_transport",
```

  This confirms the opt-in test is registered. It proves intentional lane organization, not an exception to the separate agent's normative gate policy.

- Read-only reproduction:

```text
python3 -B - <<'PY'
import subprocess
r=subprocess.run(['grep','-n',r'#\[ignore\]','meerkat-comms/tests/e2e.rs'],text=True,capture_output=True)
print('Documented literal ignore grep on cited comms test file: exit='+str(r.returncode)+' output='+repr(r.stdout))
r=subprocess.run(['grep','-n',r'#\[ignore','meerkat-comms/tests/e2e.rs'],text=True,capture_output=True)
print('Actual ignore annotations:\n'+r.stdout.rstrip())
PY
```

  Documented literal ignore grep on cited comms test file: exit=1 output=''
  Actual ignore annotations:
  338:#[ignore = "lane:e2e-smoke"]

  The candidate's proposed automatic misclassification is not reproduced by the documented search. This rejection does not endorse the policy or establish that every ignored test is acceptable.

**Disposition:** Rejected; no correction authorized for this claim.

## A14-005: The Rust quality gate incorrectly describes core as having no I/O dependencies

**Candidate severity:** medium. **Independent verdict:** confirm.

The active reviewer is primed to treat legitimate core-owned configuration/path/locking work as a crate-boundary violation based on a retired no-I/O premise.

### Baseline documentation evidence

- [.claude/agents/rust-quality-gate.md:18-18](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rust-quality-gate.md?plain=1#L18)

```text
- `meerkat-core` — Agent loop, types, budget, retry, state machine (no I/O deps)
```

### Implementation proof

- [meerkat-core/Cargo.toml:36-40](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/Cargo.toml#L36-L40)

```text
fs4 = "0.13"
```

  Core has native filesystem-locking and Tokio dependencies in its real package manifest.

- [meerkat-core/src/config_store.rs:392-411](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/config_store.rs#L392-L411)

```text
tokio::fs::rename(&tmp_path, &self.path).await?;
```

  FileConfigStore::set performs directory creation, file creation/writing, sync, and atomic rename inside core. This is production native I/O, not only a test dependency.

### Independent challenge

Unlike a preference about new dependencies, the parenthetical is a factual description of the current crate. Both the production manifest and FileConfigStore contradict it. The correction should describe bounded native I/O without suggesting that arbitrary provider/back-end functionality belongs in core.

- [.claude/agents/rust-quality-gate.md:16-18](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rust-quality-gate.md?plain=1#L16-L18)

```text
- `meerkat-core` — Agent loop, types, budget, retry, state machine (no I/O deps)
```

  The claim is in the current workspace crate map.

- [meerkat-core/Cargo.toml:36-40](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/Cargo.toml#L36-L40)

```text
fs4 = "0.13"
```

  Filesystem locking is a native production dependency, not solely a dev-dependency.

- [meerkat-core/src/config_store.rs:392-411](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/config_store.rs#L392-L411)

```text
tokio::fs::rename(&tmp_path, &self.path).await?;
```

  The production ConfigStore implementation creates directories/files, writes and syncs configuration, and renames the completed file. The I/O is directly implemented within core.

**Required correction:** Remove the false no-I/O-dependencies description. Describe core's foundational contracts/runtime-neutral logic and bounded native configuration/file/locking responsibilities while preserving provider and feature-backend ownership boundaries. Do not relocate code or change dependency policy as part of this factual crate-map fix.

### Correction

Replaced the false no-I/O-dependencies crate description with foundational contracts/runtime-neutral logic plus bounded native configuration, path, and locking I/O. Explicitly retained provider and feature-backend ownership boundaries; no dependency policy or implementation was changed.

- [.claude/agents/rust-quality-gate.md:18-18](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L18)

```text
- `meerkat-core` — Foundational contracts and runtime-neutral logic (agent loop, types, budget, retry, state machine), plus bounded native configuration, path, and locking I/O
```

- [.claude/agents/rust-quality-gate.md:31-31](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L31)

```text
Keep provider clients and general feature backends in their owning crates, not in core.
```

- [meerkat-core/Cargo.toml:36-40](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/Cargo.toml#L36-L40)

```text
fs4 = "0.13"
```

- [meerkat-core/src/config_store.rs:392-411](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/config_store.rs#L392-L411)

```text
tokio::fs::rename(&tmp_path, &self.path).await?;
```

- [meerkat-core/src/mcp_config.rs:573-590](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/mcp_config.rs#L573-L590)

```text
FileExt::lock_exclusive(&file)
```

### Final independent review

**PASS:** The false no-I/O description was replaced with foundational/runtime-neutral responsibilities plus bounded native configuration, path, and locking I/O. Actual production dependencies and implementations support that description. The added ownership boundary keeps provider clients and general feature backends out of core; it does not change dependency policy or the separately rejected schemars review rule.

- [.claude/agents/rust-quality-gate.md:18-18](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L18)

```text
plus bounded native configuration, path, and locking I/O
```

  The factual crate map no longer claims that core has no I/O dependencies.

- [.claude/agents/rust-quality-gate.md:31-31](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L31)

```text
Keep provider clients and general feature backends in their owning crates, not in core.
```

  The correction is bounded and does not authorize arbitrary backend ownership in core.

- [meerkat-core/Cargo.toml:36-40](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/Cargo.toml#L36-L40)

```text
fs4 = "0.13"
```

  Filesystem locking is a native production dependency under the non-wasm target dependency table.

- [meerkat-core/src/config_store.rs:392-411](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/config_store.rs#L392-L411)

```text
tokio::fs::rename(&tmp_path, &self.path).await?;
```

  ConfigStore::set performs real directory/file creation, writes, synchronization, and rename in core.

- [meerkat-core/src/mcp_config.rs:573-590](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/mcp_config.rs#L573-L590)

```text
FileExt::lock_exclusive(&file)
```

  Core also implements bounded native configuration-file locking; the new map describes existing code rather than moving responsibilities.

## A14-006: The Rust quality gate uses the nonexistent meerkat-mcp-client package name

**Candidate severity:** low. **Independent verdict:** confirm.

The crate map sends reviewers to a package/path that is not part of this repository.

### Baseline documentation evidence

- [.claude/agents/rust-quality-gate.md:22-22](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rust-quality-gate.md?plain=1#L22)

```text
- `meerkat-mcp-client` — MCP protocol client
```

### Implementation proof

- [meerkat-mcp/Cargo.toml:1-3](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mcp/Cargo.toml#L1-L3)

```text
name = "meerkat-mcp"
```

  The actual MCP client package declares this name and describes itself as the client connecting to tool servers.

- [Cargo.toml:3-54](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Cargo.toml#L3-L54)

```text
"meerkat-mcp",
```

  The complete workspace member inventory contains meerkat-mcp, meerkat-mcp-server, and meerkat-mob-mcp, not a meerkat-mcp-client member.

### Independent challenge

The MCP client implementation's actual package name is meerkat-mcp. I also inspected package names from every explicit workspace member manifest, rather than treating a missing directory search as proof that no renamed package exists.

- [meerkat-mcp/Cargo.toml:1-3](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-mcp/Cargo.toml#L1-L3)

```text
name = "meerkat-mcp"
description = "MCP client for Meerkat (connect to tool servers)"
```

  The package declaration supplies both the correct name and its client role.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
import tomllib
workspace=tomllib.loads(Path('Cargo.toml').read_text())['workspace']
packages=[tomllib.loads((Path(member)/'Cargo.toml').read_text())['package']['name'] for member in workspace['members']]
print('Workspace package names containing mcp:',[p for p in packages if 'mcp' in p])
print('meerkat-mcp-client is a workspace package:','meerkat-mcp-client' in packages)
PY
```

  Workspace package names containing mcp: ['meerkat-mcp', 'meerkat-mcp-server', 'meerkat-mob-mcp', 'mcp-test-server']
  meerkat-mcp-client is a workspace package: False

  The complete manifest-defined package inventory confirms the negative claim without invoking Cargo.

**Required correction:** Replace meerkat-mcp-client with meerkat-mcp in the Rust quality gate's crate map. No crate rename or source change is required.

### Correction

The Rust quality gate now identifies the MCP protocol client by its actual package name, meerkat-mcp. Re-read the package manifest and the complete workspace-defined package inventory; no crate was renamed.

- [.claude/agents/rust-quality-gate.md:24-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L24)

```text
- `meerkat-mcp` — MCP protocol client
```

- [meerkat-mcp/Cargo.toml:2-3](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mcp/Cargo.toml#L2-L3)

```text
name = "meerkat-mcp"
description = "MCP client for Meerkat (connect to tool servers)"
```

- [Cargo.toml:3-54](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/Cargo.toml#L3-L54)

```text
"meerkat-mcp",
```

### Final independent review

**PASS:** The MCP client entry now names the actual meerkat-mcp package. I independently parsed every explicit workspace member's manifest; meerkat-mcp-client is not a workspace package. No rename or implementation change was needed.

- [.claude/agents/rust-quality-gate.md:24-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L24)

```text
- `meerkat-mcp` — MCP protocol client
```

  The corrected review map points to the actual package.

- [meerkat-mcp/Cargo.toml:1-3](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-mcp/Cargo.toml#L1-L3)

```text
name = "meerkat-mcp"
description = "MCP client for Meerkat (connect to tool servers)"
```

  The package declaration independently establishes the name and client role.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
import tomllib
members=tomllib.loads(Path('Cargo.toml').read_text())['workspace']['members']
packages=[tomllib.loads((Path(p)/'Cargo.toml').read_text())['package']['name'] for p in members]
print('workspace_mcp_packages',[n for n in packages if 'mcp' in n])
print('invalid_meerkat_mcp_client_present','meerkat-mcp-client' in packages)
PY
```

  workspace_mcp_packages ['meerkat-mcp', 'meerkat-mcp-server', 'meerkat-mob-mcp', 'mcp-test-server']
  invalid_meerkat_mcp_client_present False

  This negative claim is grounded in the manifest-defined package inventory, not a failed text search or a Cargo build.

## A14-007: Provider review guidance still assigns implementation ownership to the client shim

**Candidate severity:** medium. **Independent verdict:** confirm.

These active architecture reviewers search and police the pre-split ownership map, and the quality-gate invocation example names a nonexistent implementation file. Compatibility imports through meerkat-client remain valid, but they are not the source ownership boundary.

### Baseline documentation evidence

- [.claude/agents/meerkat-rust-zealot.md:26-26](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/meerkat-rust-zealot.md?plain=1#L26)

```text
- `meerkat-client` — LLM providers (Anthropic, OpenAI, Gemini) implementing AgentLlmClient
```

- [.claude/agents/rust-quality-gate.md:19-19](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rust-quality-gate.md?plain=1#L19)

```text
- `meerkat-client` — LLM providers (Anthropic, OpenAI, Gemini)
```

- [.claude/agents/rust-quality-gate.md:3-3](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rust-quality-gate.md?plain=1#L3)

```text
meerkat-client/src/gemini_stream.rs
```

### Implementation proof

- [meerkat-client/src/lib.rs:14-21](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-client/src/lib.rs#L14-L21)

```text
pub use meerkat_anthropic::AnthropicClient;
```

  The client crate re-exports the LLM-core surface and the separate Anthropic implementation rather than owning them.

- [meerkat-client/src/lib.rs:43-46](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-client/src/lib.rs#L43-L46)

```text
pub use meerkat_gemini::GeminiClient;
```

  The same shim boundary applies to Gemini and OpenAI.

- [meerkat-gemini/src/client.rs:2078-2078](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-gemini/src/client.rs#L2078)

```text
impl LlmClient for GeminiClient {
```

  The real Gemini wire client is implemented in the provider-owned crate.

- [meerkat-llm-core/src/adapter.rs:551-551](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-llm-core/src/adapter.rs#L551)

```text
impl AgentLlmClient for LlmClientAdapter {
```

  The adapter to core's AgentLlmClient contract lives in LLM-core, distinct from provider wire-client implementation.

### Independent challenge

Confirmed as an implementation-ownership correction, not an import-compatibility break. The zealot uses its list to find and police implementation homes, while meerkat-client now consists of re-exports. The Gemini example should point at the provider owner, but a hypothetical new filename need not already exist; its absence alone would not be a defect.

- [meerkat-client/src/lib.rs:14-21](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-client/src/lib.rs#L14-L21)

```text
pub use meerkat_anthropic::AnthropicClient;
```

  The actual client surface re-exports the separate Anthropic crate and shared LLM-core types.

- [meerkat-client/src/lib.rs:42-46](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-client/src/lib.rs#L42-L46)

```text
pub use meerkat_openai::{OpenAiClient, OpenAiCompatibleClient, OpenAiCompatibleClientOptions};
```

  OpenAI and Gemini are also provider-owned re-exports, not implementation modules owned by this shim.

- [meerkat-gemini/src/client.rs:2076-2081](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-gemini/src/client.rs#L2076-L2081)

```text
impl LlmClient for GeminiClient {
```

  The provider wire-client implementation is in meerkat-gemini.

- [meerkat-llm-core/src/adapter.rs:549-551](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-llm-core/src/adapter.rs#L549-L551)

```text
impl AgentLlmClient for LlmClientAdapter {
```

  The core AgentLlmClient adaptation lives in the shared LLM-core crate; do not collapse the wire-client and agent-client contracts.

**Required correction:** Update both agent maps to distinguish the meerkat-client compatibility shim, shared meerkat-llm-core contracts/adapter, and the meerkat-anthropic/openai/gemini implementation crates. Move the quality-gate Gemini example into the provider crate, for example meerkat-gemini/src/client.rs. Preserve valid downstream imports through the compatibility shim; do not require an import sweep.

### Correction

Both review maps distinguish shared LLM wire-client contracts and AgentLlmClient adaptation in meerkat-llm-core, provider-owned LlmClient implementations in meerkat-anthropic/openai/gemini, and the meerkat-client compatibility shim. Added source-based adapter review guidance and explicitly preserved downstream compatibility imports. Moved the quality-gate Gemini example to meerkat-gemini/src/client.rs.

- [.claude/agents/meerkat-rust-zealot.md:26-28](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/meerkat-rust-zealot.md?plain=1#L26-L28)

```text
- `meerkat-llm-core` — Shared LLM wire-client contracts/plumbing and `LlmClientAdapter`, which implements core's `AgentLlmClient`
- `meerkat-anthropic` / `meerkat-openai` / `meerkat-gemini` — Provider-specific `LlmClient` implementations
- `meerkat-client` — Compatibility shim re-exporting shared LLM-core and provider surfaces
```

- [.claude/agents/rust-quality-gate.md:19-21](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L19-L21)

```text
- `meerkat-client` — Compatibility shim re-exporting shared LLM-core and provider surfaces
```

- [.claude/agents/rust-quality-gate.md:3-3](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L3)

```text
<writes code to meerkat-gemini/src/client.rs and related files>
```

- [.claude/agents/meerkat-rust-zealot.md:44-44](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/meerkat-rust-zealot.md?plain=1#L44)

```text
Existing downstream `meerkat_client::*` imports remain valid; do not demand an import sweep.
```

- [meerkat-client/src/lib.rs:14-21](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-client/src/lib.rs#L14-L21)

```text
pub use meerkat_anthropic::AnthropicClient;
```

- [meerkat-llm-core/src/adapter.rs:551-551](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-llm-core/src/adapter.rs#L551)

```text
impl AgentLlmClient for LlmClientAdapter {
```

- [meerkat-gemini/src/client.rs:2078-2078](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-gemini/src/client.rs#L2078)

```text
impl LlmClient for GeminiClient {
```

### Final independent review

**PASS:** Both architectural review maps now distinguish the compatibility shim, shared LLM wire-client/adapter owner, and three provider implementation owners. The Gemini example points to its actual provider source. The wording correctly distinguishes LlmClient implementations from AgentLlmClient adaptation and explicitly preserves downstream compatibility imports.

- [.claude/agents/meerkat-rust-zealot.md:26-28](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/meerkat-rust-zealot.md?plain=1#L26-L28)

```text
- `meerkat-llm-core` — Shared LLM wire-client contracts/plumbing and `LlmClientAdapter`, which implements core's `AgentLlmClient`
- `meerkat-anthropic` / `meerkat-openai` / `meerkat-gemini` — Provider-specific `LlmClient` implementations
- `meerkat-client` — Compatibility shim re-exporting shared LLM-core and provider surfaces
```

  The duplicate-functionality review now searches the post-split implementation owners.

- [.claude/agents/rust-quality-gate.md:19-21](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L19-L21)

```text
- `meerkat-client` — Compatibility shim re-exporting shared LLM-core and provider surfaces
```

  The quality gate carries the same three-way distinction; its line 31 and the zealot's line 44 explicitly forbid demanding an import sweep.

- [.claude/agents/rust-quality-gate.md:3-3](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rust-quality-gate.md?plain=1#L3)

```text
<writes code to meerkat-gemini/src/client.rs and related files>
```

  The provider implementation example no longer directs changes into the client shim.

- [meerkat-client/src/lib.rs:14-21](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-client/src/lib.rs#L14-L21)

```text
pub use meerkat_anthropic::AnthropicClient;
```

  The shim's concrete exports re-export the shared LLM-core surface and provider-owned Anthropic client.

- [meerkat-client/src/lib.rs:42-46](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-client/src/lib.rs#L42-L46)

```text
pub use meerkat_openai::{OpenAiClient, OpenAiCompatibleClient, OpenAiCompatibleClientOptions};
```

  The real feature-gated OpenAI and Gemini re-exports substantiate compatibility preservation rather than requiring downstream import rewrites.

- [meerkat-llm-core/src/adapter.rs:549-551](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-llm-core/src/adapter.rs#L549-L551)

```text
impl AgentLlmClient for LlmClientAdapter {
```

  Agent-client adaptation is actually owned by LLM-core.

- [meerkat-anthropic/src/client.rs:1325-1329](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-anthropic/src/client.rs#L1325-L1329)

```text
impl LlmClient for AnthropicClient {
```

  Anthropic's provider crate owns its wire-client implementation.

- [meerkat-openai/src/client.rs:2220-2224](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-openai/src/client.rs#L2220-L2224)

```text
impl LlmClient for OpenAiClient {
```

  OpenAI's provider crate owns its wire-client implementation.

- [meerkat-gemini/src/client.rs:2076-2080](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-gemini/src/client.rs#L2076-L2080)

```text
impl LlmClient for GeminiClient {
```

  Gemini's provider crate owns the implementation used in the corrected example.

## A14-008: The quality gate bans a schema dependency required by the runtime tool-schema path

**Candidate severity:** medium. **Independent verdict:** reject.

The unconditional exclusion rule would flag or break legitimate runtime schema generation. Feature-gating optional export derives remains useful, but it cannot be stated as a universal ban on the existing runtime dependency.

### Baseline documentation evidence

- [.claude/agents/rust-quality-gate.md:64-64](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rust-quality-gate.md?plain=1#L64)

```text
12. **Feature-gated derives**: `#[cfg_attr(feature = "schema", derive(JsonSchema))]` — `schemars` must stay out of the runtime dependency graph.
```

### Implementation proof

- [meerkat-core/Cargo.toml:28-30](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/Cargo.toml#L28-L30)

```text
schemars = { workspace = true }
```

  The dependency is unconditional, not an optional dependency activated by the schema feature.

- [meerkat-core/Cargo.toml:49-53](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/Cargo.toml#L49-L53)

```text
schema = []
```

  The feature does not remove schemars from the runtime dependency graph.

- [meerkat-core/src/schema.rs:91-94](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/schema.rs#L91-L94)

```text
pub fn tool_input_schema_for<T: schemars::JsonSchema>() -> Value {
```

  Core's actual tool-schema emission seam needs JsonSchema and calls schema_for!.

- [meerkat-tools/src/schema.rs:13-24](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-tools/src/schema.rs#L13-L24)

```text
pub fn schema_for<T: JsonSchema>() -> Value {
```

  Tool definitions use this runtime helper, including the unconditional EmptyObject JsonSchema derive.

### Independent challenge

The existing runtime really does depend on schemars, but the cited sentence is a normative review preference for changed code, not a claim that the current dependency graph lacks schemars. The full agent also classifies this as an additional quality check, allows minor violations with warnings, and explicitly excludes unchanged pre-existing issues. Existing unconditional schema generation alone therefore does not prove the review preference is a factual documentation defect or authorize weakening it. Unlike A14-005's factual crate-map claim, this candidate needs a separately authorized policy decision.

- [.claude/agents/rust-quality-gate.md:60-64](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rust-quality-gate.md?plain=1#L60-L64)

```text
## Additional Quality Checks (Violations = Strong Warnings or FAIL if egregious)
```

  Rule 12 occurs under a prescriptive quality-policy heading, not a description of the actual graph.

- [.claude/agents/rust-quality-gate.md:133-135](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rust-quality-gate.md?plain=1#L133-L135)

```text
- **PASS with warnings** if only rules 11-16, 18, 20 have minor issues.
```

  The candidate overstates the rule as an unconditional runtime ban; the agent's decision framework permits a warning for rule 12.

- [.claude/agents/rust-quality-gate.md:140-142](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rust-quality-gate.md?plain=1#L140-L142)

```text
- You are reviewing recently changed code, not auditing the entire codebase. Don't flag pre-existing issues in unchanged code unless the new code interacts with them in a way that creates a new problem.
```

  Current existing schema code is not, by itself, proof of a misapplied changeset review.

- [meerkat-core/Cargo.toml:28-30](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/Cargo.toml#L28-L30)

```text
schemars = { workspace = true }
```

  I verified the unconditional dependency rather than disputing the candidate's source observation.

- [meerkat-core/src/schema.rs:91-93](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/schema.rs#L91-L93)

```text
pub fn tool_input_schema_for<T: schemars::JsonSchema>() -> Value {
```

  Runtime schema generation also exists. This establishes a design-policy tension, not a false descriptive claim in the cited review rule.

**Disposition:** Rejected; no correction authorized for this claim.

## A14-009: Repository-specific phase reviewers bypass the Cargo lane wrapper

**Candidate severity:** low. **Independent verdict:** confirm.

These are current Meerkat-specific reviewer commands, unlike the intentionally generic Cargo examples in reusable skills. Bypassing the wrapper loses the repository's tested toolchain/cache/lane setup, particularly when reviewers run together.

### Baseline documentation evidence

- [.claude/agents/integration-sheriff.md:39-42](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/integration-sheriff.md?plain=1#L39-L42)

```text
cargo test -p meerkat-core   # or relevant crate for the phase
```

- [.claude/agents/rct-guardian.md:38-41](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rct-guardian.md?plain=1#L38-L41)

```text
cargo test -p meerkat-core   # or relevant crate for the phase
```

### Implementation proof

- [Makefile:11-11](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Makefile#L11)

```text
CARGO ?= ./scripts/repo-cargo
```

  The real developer command surface selects the repository wrapper.

- [scripts/repo-cargo:76-92](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/repo-cargo#L76-L92)

```text
lane_key="${RUST_LANE_ID}"
```

  The wrapper implements named concurrent-agent lanes and worktree-keyed fallbacks.

- [scripts/repo-cargo:110-117](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/repo-cargo#L110-L117)

```text
export CARGO_TARGET_DIR="${CARGO_TARGET_DIR:-${cache_root}/${repo_key}/targets/${target_cache_schema}/${toolchain_key}/${lane_key}}"
```

  The documented bare command omits the wrapper's actual cache/home/target selection, not merely a cosmetic alias.

### Independent challenge

Narrowly confirmed for these two Meerkat-specific phase agents. Bare cargo test remains a valid Cargo command and is not proven to produce different test assertions; the defect is bypassing this repository's selected toolchain/cache/lane entry point. This rationale does not apply to portable Rust or RCT templates.

- [.claude/agents/integration-sheriff.md:39-42](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/integration-sheriff.md?plain=1#L39-L42)

```text
cargo test -p meerkat-core   # or relevant crate for the phase
```

  This is a concrete command in a repository-specific agent, not a placeholder project example.

- [.claude/agents/rct-guardian.md:38-41](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/agents/rct-guardian.md?plain=1#L38-L41)

```text
cargo test -p meerkat-core   # or relevant crate for the phase
```

  The second repository-specific reviewer repeats the bypass.

- [Makefile:11-11](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Makefile#L11)

```text
CARGO ?= ./scripts/repo-cargo
```

  The actual repository command surface defaults to the wrapper.

- [scripts/repo-cargo:76-92](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/repo-cargo#L76-L92)

```text
lane_key="${RUST_LANE_ID}"
```

  The wrapper selects explicit agent lanes and falls back to worktree-derived isolation.

- [scripts/repo-cargo:114-117](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/repo-cargo#L114-L117)

```text
export CARGO_TARGET_DIR="${CARGO_TARGET_DIR:-${cache_root}/${repo_key}/targets/${target_cache_schema}/${toolchain_key}/${lane_key}}"
```

  This has concrete behavior absent from the bare command. I inspected, but did not execute, the wrapper.

**Required correction:** Use ./scripts/repo-cargo test -p meerkat-core, or the phase's relevant package, in these two commands. A short note may specify distinct RUST_LANE_ID values for simultaneous same-checkout reviewers. Do not rewrite generic templates to depend on Meerkat-only scripts or require broad workspace builds.

### Correction

The two repository-specific review commands now use ./scripts/repo-cargo with the existing package scope. Added distinct RUST_LANE_ID guidance for simultaneous same-checkout reviewers. Left generic cross-project Cargo snippets and templates unchanged; no Cargo command was executed.

- [.claude/agents/integration-sheriff.md:48-50](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/integration-sheriff.md?plain=1#L48-L50)

```text
./scripts/repo-cargo test -p meerkat-core   # or relevant crate for the phase
```

- [.claude/agents/rct-guardian.md:47-49](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rct-guardian.md?plain=1#L47-L49)

```text
For simultaneous reviewers in the same checkout, set a distinct `RUST_LANE_ID` for each reviewer.
```

- [Makefile:11-11](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/Makefile#L11)

```text
CARGO ?= ./scripts/repo-cargo
```

- [scripts/repo-cargo:76-92](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/repo-cargo#L76-L92)

```text
lane_key="${RUST_LANE_ID}"
```

- [scripts/repo-cargo:114-117](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/repo-cargo#L114-L117)

```text
export CARGO_TARGET_DIR="${CARGO_TARGET_DIR:-${cache_root}/${repo_key}/targets/${target_cache_schema}/${toolchain_key}/${lane_key}}"
```

### Final independent review

**PASS:** The two Meerkat-specific phase test commands now use the repository's Cargo wrapper while retaining package scope. Their distinct RUST_LANE_ID guidance matches the wrapper's real lane selection. Generic Cargo commands in reusable RCT and Rust templates were preserved. No test or wrapper invocation was executed during this review.

- [.claude/agents/integration-sheriff.md:48-50](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/integration-sheriff.md?plain=1#L48-L50)

```text
./scripts/repo-cargo test -p meerkat-core   # or relevant crate for the phase
```

  The integration reviewer uses the existing package-scoped wrapper and documents distinct lanes for simultaneous same-checkout reviewers.

- [.claude/agents/rct-guardian.md:47-49](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/agents/rct-guardian.md?plain=1#L47-L49)

```text
./scripts/repo-cargo test -p meerkat-core   # or relevant crate for the phase
```

  The representation reviewer uses the same repository-specific entry point without a broad workspace command.

- [Makefile:11-11](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/Makefile#L11)

```text
CARGO ?= ./scripts/repo-cargo
```

  The actual Make command surface chooses this wrapper.

- [scripts/repo-cargo:76-92](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/repo-cargo#L76-L92)

```text
lane_key="${RUST_LANE_ID}"
```

  The wrapper honors an explicitly chosen reviewer lane and otherwise derives a worktree-keyed fallback.

- [scripts/repo-cargo:114-117](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/repo-cargo#L114-L117)

```text
export CARGO_TARGET_DIR="${CARGO_TARGET_DIR:-${cache_root}/${repo_key}/targets/${target_cache_schema}/${toolchain_key}/${lane_key}}"
```

  This is concrete cache isolation behavior, not a claim that bare Cargo changes test assertions. The wrapper exists and has its executable bit set.

- [.claude/skills/rct-methodology/references/checklist_template.md:14-17](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/checklist_template.md?plain=1#L14-L17)

```text
- "cargo test -p types-crate"
```

  The portable checklist retains generic Cargo examples. Its complete YAML sample and the strict-pipeline sample remain byte-identical to the baseline.

## A14-010: The advertised self-contained Luka Loop cannot be scaffolded because its payload is missing

**Candidate severity:** high. **Independent verdict:** confirm.

The skill presents a runnable, self-contained implementation/review/finalization loop, but following its required setup always fails before installing any loop scripts. Its timeout, locking, retry, and finalization guarantees also have no shipped implementation to inspect here.

### Baseline documentation evidence

- [.claude/skills/rct-methodology/SKILL.md:64-70](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/SKILL.md?plain=1#L64-L70)

```text
Scaffold with:
`scripts/luka_scaffold.py /path/to/repo`
```

- [.claude/skills/rct-methodology/references/luka_loop.md:51-57](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/luka_loop.md?plain=1#L51-L57)

```text
Use the scaffold script to create the structure and templates:
```

- [.claude/skills/rct-methodology/references/luka_setup_flow.md:43-47](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/luka_setup_flow.md?plain=1#L43-L47)

```text
python <CODEX_HOME>/skills/rct-methodology/scripts/luka_scaffold.py <repo>
```

- [.claude/skills/rct-methodology/references/luka_setup_flow.md:61-69](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/luka_setup_flow.md?plain=1#L61-L69)

```text
.rct/scripts/validate_checklist.py
```

- [.claude/skills/rct-methodology/references/checklist_template.md:44-49](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/checklist_template.md?plain=1#L44-L49)

```text
Use `.rct/scripts/render_checklist.py` to render a human view to:
```

- [.claude/skills/rct-methodology/references/strict_pipeline.md:73-77](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/strict_pipeline.md?plain=1#L73-L77)

```text
After scaffolding, use the repo-local `.rct/scripts/render_checklist.py`.
```

### Implementation proof

- [.claude/skills/rct-methodology/scripts/luka_scaffold.py:19-24](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/scripts/luka_scaffold.py#L19-L24)

```text
asset_root = skill_root / 'assets' / 'luka_loop' / '.rct'
```

  The implementation requires a bundled assets/luka_loop/.rct tree and exits before copying anything if it is absent.

- Read-only reproduction:

```text
python3 -B .claude/skills/rct-methodology/scripts/luka_scaffold.py .; status=$?; printf 'scaffold exit=%s\n' "$status"
```

  Missing assets: /Users/luka/src/copilot-worktrees/meerkat/luka-crnkovicfriis-abk-reimagined-adventure/.claude/skills/rct-methodology/assets/luka_loop/.rct
  scaffold exit=1

  The documented scaffold operation was reproduced. The script stopped at its existence guard before creating or modifying files.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
for p in ('.rct/scripts/luka_loop.sh','.rct/scripts/review_harness.sh','.rct/scripts/validate_checklist.py','.rct/scripts/render_checklist.py'):
 print(f'{p}: exists={Path(p).exists()}')
PY
git ls-files -- '.claude/skills/rct-methodology/assets' '.rct/scripts'
```

  .rct/scripts/luka_loop.sh: exists=False
  .rct/scripts/review_harness.sh: exists=False
  .rct/scripts/validate_checklist.py: exists=False
  .rct/scripts/render_checklist.py: exists=False
  The tracked assets/scripts query produced no paths.

  The existing repository has RCT metadata, but neither a bundled automation payload nor already-scaffolded automation provides the advertised commands.

- [.claude/skills/rct-methodology/scripts/render_checklist.py:9-17](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/scripts/render_checklist.py#L9-L17)

```text
import yaml
```

  The actual standalone renderer remains available and has an explicit PyYAML dependency; its following path setup reads .rct/checklist.yaml from the working directory.

### Independent challenge

Confirmed only as an availability/prerequisite mismatch in this bundled skill. The scaffold script unconditionally requires an adjacent assets/luka_loop/.rct payload, but the local distribution has only SKILL.md, references, and scripts. Invoking its actual code with write operations trapped exits at that missing-assets guard. A separate complete installation could supply the payload; neither the general methodology nor the absent orchestrator's claimed safety properties have thereby been disproved. No requirement to vendor third-party assets or implement an orchestrator is accepted.

- [.claude/skills/rct-methodology/scripts/luka_scaffold.py:19-24](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/scripts/luka_scaffold.py#L19-L24)

```text
asset_root = skill_root / 'assets' / 'luka_loop' / '.rct'

if not asset_root.exists():
    print(f"Missing assets: {asset_root}", file=sys.stderr)
    sys.exit(1)
```

  The actual implementation has no alternate payload lookup or download step before this guard.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
from unittest.mock import patch
import runpy, shutil, sys
skill=Path('.claude/skills/rct-methodology')
print('RCT skill entries:',sorted(p.name for p in skill.iterdir()))
with patch.object(sys,'argv',[str(skill/'scripts/luka_scaffold.py'),'.']), patch.object(Path,'mkdir',side_effect=AssertionError('write forbidden')), patch.object(Path,'chmod',side_effect=AssertionError('write forbidden')), patch.object(shutil,'copy2',side_effect=AssertionError('write forbidden')):
 try:
  runpy.run_path(str(skill/'scripts/luka_scaffold.py'),run_name='__main__')
 except SystemExit as e:
  print('Scaffold under write traps: exit='+str(e.code))
PY
```

  RCT skill entries: ['SKILL.md', 'references', 'scripts']
  Scaffold under write traps: exit=1
  stderr: Missing assets: /Users/luka/src/copilot-worktrees/meerkat/luka-crnkovicfriis-abk-reimagined-adventure/.claude/skills/rct-methodology/assets/luka_loop/.rct

  The independent reproduction reached the real availability guard without any directory creation, copy, or permission change.

- [.claude/skills/rct-methodology/scripts/render_checklist.py:9-17](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/scripts/render_checklist.py#L9-L17)

```text
checklist = repo / ".rct" / "checklist.yaml"
out_md = repo / ".rct" / "outputs" / "CHECKLIST.md"
```

  The separately bundled renderer is usable from a project root with PyYAML; it does not require the missing loop payload.

- [.claude/skills/rct-methodology/scripts/render_checklist.py:23-26](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/scripts/render_checklist.py#L23-L26)

```text
phases = data.get("phases", [])
```

  The renderer's supported input is the skill's phases-based checklist. I executed it against the bundled checklist example with all output writes intercepted and verified the phase heading, commands, and tasks. Do not promise compatibility with arbitrary existing .rct metadata.

**Required correction:** Consistently distinguish manual RCT methodology from optional Luka automation requiring a complete payload. State that assets/luka_loop/.rct and the resulting .rct/scripts automation are not supplied in this checkout; make scaffold, validation, loop, automatic finalization, and safety-feature instructions conditional on a separately available implementation rather than unconditionally runnable here. Include the main skill's required post-load guide in that scope correction. The supported fallback may be python3 .claude/skills/rct-methodology/scripts/render_checklist.py from the project root, with existing PyYAML and a phases-based checklist matching the supplied template. Preserve reusable templates and design descriptions; do not restore/install assets, invent an upstream URL, change scripts, or overwrite existing .rct project metadata.

### Correction

All five documents now distinguish the supported manual RCT workflow from optional Luka automation. They state that assets/luka_loop/.rct and the resulting .rct/scripts automation are not supplied here. Scaffolding, automated validation/reviews, loop invocation, finalization, and safety properties are conditional on a separately supplied and verified complete implementation. The required post-load guide and reviewer-harness instructions follow the same availability rule. Documented the shipped renderer invocation from the target project root with Python 3, existing PyYAML, and a phases-based checklist matching the unchanged template; rendering is not validation. No tooling, upstream URL, assets, or .rct metadata were created or replaced.

- [.claude/skills/rct-methodology/SKILL.md:61-72](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/SKILL.md?plain=1#L61-L72)

```text
python3 .claude/skills/rct-methodology/scripts/render_checklist.py
```

- [.claude/skills/rct-methodology/SKILL.md:77-85](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/SKILL.md?plain=1#L77-L85)

```text
Luka automation is optional and **unavailable in this checkout**. The bundled
skill does not supply `assets/luka_loop/.rct`, and the resulting repo-local
`.rct/scripts/` automation is also absent.
```

- [.claude/skills/rct-methodology/SKILL.md:98-105](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/SKILL.md?plain=1#L98-L105)

```text
- that Luka automation is unavailable here; give a loop invocation only for a separately supplied and verified complete installation,
```

- [.claude/skills/rct-methodology/references/luka_loop.md:5-14](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/luka_loop.md?plain=1#L5-L14)

```text
The layout, transitions, prompts, scripts, and safety properties below are
design descriptions, not verified features available here.
```

- [.claude/skills/rct-methodology/references/luka_setup_flow.md:63-66](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/luka_setup_flow.md?plain=1#L63-L66)

```text
The standalone renderer is supplied even without the Luka payload. Require
Python 3, PyYAML in that interpreter, and a phases-based `.rct/checklist.yaml`
matching `checklist_template.md`.
```

- [.claude/skills/rct-methodology/references/luka_setup_flow.md:81-87](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/luka_setup_flow.md?plain=1#L81-L87)

```text
validator is supplied. Only in a complete installation with a verified validator:
```

- [.claude/skills/rct-methodology/references/luka_setup_flow.md:104-110](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/luka_setup_flow.md?plain=1#L104-L110)

```text
No automatic finalization is supplied here. For a complete installation:
```

- [.claude/skills/rct-methodology/references/checklist_template.md:46-52](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/checklist_template.md?plain=1#L46-L52)

```text
The standalone renderer is supplied; the Luka `assets/luka_loop/.rct` payload
and repo-local `.rct/scripts/` automation are not.
```

- [.claude/skills/rct-methodology/references/strict_pipeline.md:42-47](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/strict_pipeline.md?plain=1#L42-L47)

```text
The spec → plan → checklist pipeline is usable manually.
```

- [.claude/skills/rct-methodology/scripts/luka_scaffold.py:19-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/scripts/luka_scaffold.py#L19-L24)

```text
asset_root = skill_root / 'assets' / 'luka_loop' / '.rct'
```

- [.claude/skills/rct-methodology/scripts/render_checklist.py:9-26](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/scripts/render_checklist.py#L9-L26)

```text
phases = data.get("phases", [])
```

### Final independent review

**PASS:** All five assigned documents now distinguish usable manual RCT planning/rendering from optional Luka automation that is not supplied here. The main skill's post-load guide, setup flow, automation commands, reviewer harness, safety expectations, and finalization are appropriately conditional. I exercised the actual scaffolder with mutation attempts forbidden: it exited at Missing assets without attempting a write. I exercised the actual renderer against the bundled phases template with input/output intercepted in memory and verified headings, commands, reviewers, task/spec IDs, done conditions, and gate markers. Existing unrelated task YAML was preserved, and no external payload, validator, or orchestrator was invented.

- [.claude/skills/rct-methodology/SKILL.md:61-85](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/SKILL.md?plain=1#L61-L85)

```text
Luka automation is optional and **unavailable in this checkout**.
```

  The same section supplies the real standalone renderer, Python 3/PyYAML and phases-schema prerequisites, project-root working directory, relocation caveat, and instruction not to overwrite unrelated .rct metadata.

- [.claude/skills/rct-methodology/SKILL.md:98-105](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/SKILL.md?plain=1#L98-L105)

```text
- that Luka automation is unavailable here; give a loop invocation only for a separately supplied and verified complete installation,
```

  The required post-load response cannot honestly offer the absent loop as a shipped workflow. The harness language at lines 179-181 is similarly conditional.

- [.claude/skills/rct-methodology/references/luka_loop.md:5-14](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/luka_loop.md?plain=1#L5-L14)

```text
The layout, transitions, prompts, scripts, and safety properties below are
design descriptions, not verified features available here.
```

  The full design is preserved without claiming local implementation. The safety section at lines 106-115 asks the reader to verify a separately supplied implementation, and finalization at lines 125-129 is not promised here.

- [.claude/skills/rct-methodology/references/luka_setup_flow.md:63-87](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/luka_setup_flow.md?plain=1#L63-L87)

```text
The standalone renderer is supplied even without the Luka payload. Require
Python 3, PyYAML in that interpreter, and a phases-based `.rct/checklist.yaml`
matching `checklist_template.md`.
```

  Rendering is explicitly not validation. Checklist validation defaults to manual review, while scaffold/run/finalization are expressly conditional elsewhere in this setup flow.

- [.claude/skills/rct-methodology/references/checklist_template.md:46-68](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/checklist_template.md?plain=1#L46-L68)

```text
Rendering does not validate
the checklist or execute gates.
```

  The sample documents the actual standalone renderer and only permits a repo-local automation script if a complete external installation supplies and verifies it.

- [.claude/skills/rct-methodology/references/strict_pipeline.md:42-47](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/strict_pipeline.md?plain=1#L42-L47)

```text
Scaffolding, automated validation/review,
loop execution, and automatic finalization require a separately supplied,
verified implementation; do not assume these folders or commands exist.
```

  The strict pipeline remains usable manually and does not claim that optional directories or commands were installed.

- [.claude/skills/rct-methodology/scripts/luka_scaffold.py:19-24](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/scripts/luka_scaffold.py#L19-L24)

```text
asset_root = skill_root / 'assets' / 'luka_loop' / '.rct'

if not asset_root.exists():
    print(f"Missing assets: {asset_root}", file=sys.stderr)
    sys.exit(1)
```

  The real implementation has an early mandatory payload guard. Executing this exact module under an audit hook that rejects file mutation/process execution returned exit 1 at that guard with zero mutation/process attempts.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
for name in ('.claude/skills/rct-methodology/assets/luka_loop/.rct','.rct/scripts'):
    p=Path(name)
    assert not any(q.is_symlink() for q in [p,*p.parents])
    print(name, 'exists', p.exists())
PY
```

  .claude/skills/rct-methodology/assets/luka_loop/.rct exists False
  .rct/scripts exists False

  The missing-asset limitation was independently verified in this checkout without probing another installation or following an external symlink.

- [.claude/skills/rct-methodology/scripts/render_checklist.py:9-26](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/scripts/render_checklist.py#L9-L26)

```text
repo = Path.cwd()
checklist = repo / ".rct" / "checklist.yaml"
out_md = repo / ".rct" / "outputs" / "CHECKLIST.md"
```

  The documented working directory and output path match the real script. It imports PyYAML and selects data.get('phases', []); it is not a general checklist validator.

- [.claude/skills/rct-methodology/scripts/render_checklist.py:74-93](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/scripts/render_checklist.py#L74-L93)

```text
lines.append(f"- [{done}] {text} (Done when: {done_when})")
```

  The real renderer was executed, not reimplemented: Path.open supplied the exact bundled YAML sample, and Path.mkdir/Path.write_text captured the intended output in memory. It emitted the sample phase, both generic verification commands, reviewers, task/spec IDs, and done conditions to the declared output path. No file or gate was created/executed.

- [.rct/checklist.yaml:1-7](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.rct/checklist.yaml#L1-L7)

```text
project: "Assistant Image Generation Substrate"
tasks:
```

  The existing unrelated task uses top-level tasks, not the renderer's phases schema. An intercepted run of that input produced zero phase headings; the corrected prerequisite and no-overwrite warning are material. All three existing task YAML files remain byte-identical to the baseline.

## A14-011: RCT reviewer prompts forbid reading the implementation they must verify

**Candidate severity:** medium. **Independent verdict:** confirm.

The intended rule is to keep specification authority in .rct, but the actual wording bans all external paths, including source and tests. A reviewer obeying it can only re-read the plan rather than independently verify implementation and done conditions.

### Baseline documentation evidence

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:20-23](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L20-L23)

```text
**You MUST ignore ALL files outside `.rct/`:**
```

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:259-264](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L259-L264)

```text
If you find yourself reading a file path that does NOT start with `.rct/`, STOP and use `.rct/spec.yaml` instead.
```

### Implementation proof

- Read-only reproduction:

```text
git ls-files -- .rct
```

  .rct/checklist.yaml
  .rct/evidence.md
  .rct/plan.yaml
  .rct/source-of-truth.md
  .rct/spec.yaml
  .rct/traceability.md

  The allowed tree contains specification/process metadata, not the product implementation or its tests.

- [meerkat-core/src/config_store.rs:392-411](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/config_store.rs#L392-L411)

```text
async fn set(&self, config: Config) -> Result<(), ConfigError> {
```

  Actual behavior to audit lives in workspace source paths outside .rct, as do the other crate implementations declared by Cargo.toml.

- [Cargo.toml:3-54](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Cargo.toml#L3-L54)

```text
"meerkat-core",
```

  The workspace manifest locates product crates outside the process-artifact directory. A source review cannot obey the literal blanket path prohibition.

### Independent challenge

The restriction can reasonably intend to exclude competing specifications, but its literal path-wide prohibition prevents the source inspection the same reviewer is required to perform. This is a self-defeating instruction, not merely a stricter review preference: actual product code and tests live outside .rct, and reading that code does not give it specification authority.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:20-23](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L20-L23)

```text
**You MUST ignore ALL files outside `.rct/`:**
```

  The universal wording excludes more than alternative specification documents.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:264-264](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L264)

```text
If you find yourself reading a file path that does NOT start with `.rct/`, STOP and use `.rct/spec.yaml` instead.
```

  The repeated Spec Auditor instruction explicitly prohibits reading ordinary source paths.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:300-304](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L300-L304)

```text
Compare checklist marks against actual implementation:
```

  The review still requires observing implementation, not just accepting the checklist.

- [meerkat-core/src/config_store.rs:392-399](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-core/src/config_store.rs#L392-L399)

```text
async fn set(&self, config: Config) -> Result<(), ConfigError> {
```

  A concrete production implementation is outside the permitted prefix. The workspace member inventory likewise places product crates outside .rct.

**Required correction:** Restrict the exclusion to competing sources of specification authority. Explicitly permit and require reading relevant implementation, tests, build/configuration files, and task deliverables anywhere in the repository as behavioral evidence, while retaining .rct/spec.yaml as the selected task's normative spec. Narrow both the universal ALL-files rule and the repeated STOP instruction; do not replace the task's specification with unrelated docs.

### Correction

Narrowed the universal path prohibition and the repeated Spec Auditor STOP instruction to competing specification authority. Reviewers must inspect and cite relevant implementation, tests, build/configuration files, and task deliverables anywhere in the repository, including documentation deliverables. Aligned the repeated deferral guidance with that distinction while retaining .rct/spec.yaml as the selected task's normative specification.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:20-30](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L20-L30)

```text
Read the relevant implementation, tests, build/configuration files, and task
deliverables anywhere within the repository, including outside `.rct/`.
```

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:271-275](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L271-L275)

```text
Read and cite relevant implementation, tests, build/configuration files, and
task deliverables anywhere in the repository as behavioral evidence. Do not
stop reading source merely because its path is outside `.rct/`.
```

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:309-312](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L309-L312)

```text
Also inspect relevant implementation and task deliverables elsewhere in the
repository for deferrals, including documentation deliverables.
```

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:520-522](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L520-L522)

```text
Cross-reference against `.rct/spec.yaml` MUST statements. Do not use unrelated
`docs/` content as specification authority; inspect relevant task deliverables
there as evidence when applicable.
```

- [meerkat-core/src/config_store.rs:392-399](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/config_store.rs#L392-L399)

```text
async fn set(&self, config: Config) -> Result<(), ConfigError> {
```

  Actual behavioral evidence lives outside .rct; reading it does not confer specification authority.

### Final independent review

**PASS:** The universal ALL-files exclusion and the repeated Spec Auditor STOP rule now exclude competing requirements authority, not behavioral evidence. Relevant source, tests, build/configuration, and deliverables including docs/README files must be inspected and cited. Repeated deferral instructions are consistent with this distinction, while .rct/spec.yaml remains the selected task's normative specification.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:20-34](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L20-L34)

```text
Read the relevant implementation, tests, build/configuration files, and task
deliverables anywhere within the repository, including outside `.rct/`.
```

  The universal rule now permits and requires implementation evidence rather than banning source paths.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:271-275](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L271-L275)

```text
Do not
stop reading source merely because its path is outside `.rct/`.
```

  The Spec Auditor's formerly contradictory STOP rule is explicitly reversed for evidence gathering without granting unrelated documents specification authority.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:309-312](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L309-L312)

```text
Also inspect relevant implementation and task deliverables elsewhere in the
repository for deferrals, including documentation deliverables.
```

  The repeated deferral check no longer suppresses evidence from task deliverables outside .rct.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:520-522](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L520-L522)

```text
Cross-reference against `.rct/spec.yaml` MUST statements. Do not use unrelated
`docs/` content as specification authority; inspect relevant task deliverables
there as evidence when applicable.
```

  The final methodology reviewer uses the same authority/evidence distinction.

- [meerkat-core/src/config_store.rs:392-399](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-core/src/config_store.rs#L392-L399)

```text
async fn set(&self, config: Config) -> Result<(), ConfigError> {
```

  Concrete production behavior resides outside .rct. Reading this implementation to verify a requirement is now allowed; its location cannot create or replace the task's specification.

## A14-012: Reviewer prompts point to plan.md instead of the RCT plan.yaml input

**Candidate severity:** low. **Independent verdict:** confirm.

The reviewer looks up phase-to-spec mappings in the wrong file.

### Baseline documentation evidence

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:12-16](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L12-L16)

```text
- `.rct/plan.md` — implementation plan
```

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:271-273](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L271-L273)

```text
Refer to `.rct/plan.md` for the mapping.
```

### Implementation proof

- [.claude/skills/rct-methodology/scripts/luka_scaffold.py:42-47](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/scripts/luka_scaffold.py#L42-L47)

```text
print("1) Fill .rct/spec.yaml, .rct/plan.yaml, .rct/checklist.yaml")
```

  The bundled setup implementation names plan.yaml as the plan input, not plan.md.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
for p in ('.rct/plan.md','.rct/plan.yaml'):
 print(f'{p}: exists={Path(p).exists()}')
PY
git ls-files -- .rct/plan.md .rct/plan.yaml
```

  .rct/plan.md: exists=False
  .rct/plan.yaml: exists=True
  .rct/plan.yaml

  The current tracked RCT plan also uses the YAML name, so the reviewer reference fails even independently of the missing loop payload.

### Independent challenge

Confirmed as the default layout spelling used by this skill's own scaffold interface, not a universal requirement that every project plan must be YAML. The checked-in .rct project is corroborating evidence only; it is not the authority for all portable templates.

- [.claude/skills/rct-methodology/scripts/luka_scaffold.py:42-47](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rct-methodology/scripts/luka_scaffold.py#L42-L47)

```text
print("1) Fill .rct/spec.yaml, .rct/plan.yaml, .rct/checklist.yaml")
```

  The executable scaffold's intended interface names plan.yaml.

- [.rct/plan.yaml:1-9](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.rct/plan.yaml#L1-L9)

```text
phases:
```

  The existing tracked plan uses the YAML filename and includes phase-to-spec mappings.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
for name in ('.rct/plan.md','.rct/plan.yaml'):
 print(name+': exists='+str(Path(name).exists()))
PY
```

  .rct/plan.md: exists=False
  .rct/plan.yaml: exists=True

  Independently verified the concrete references without running or scaffolding a workflow.

**Required correction:** Replace the two default .rct/plan.md references at the artifact list and phase-to-spec mapping with .rct/plan.yaml. Do not rename existing project files or impose this naming on unrelated external projects.

### Correction

Both default implementation-plan references now use .rct/plan.yaml: the artifact inventory and the phase-to-spec mapping. No existing plan file was renamed and no naming rule was imposed on unrelated projects.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:15-15](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L15)

```text
- `.rct/plan.yaml` — implementation plan
```

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:284-284](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L284)

```text
Refer to `.rct/plan.yaml` for the mapping.
```

- [.claude/skills/rct-methodology/scripts/luka_scaffold.py:42-47](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/scripts/luka_scaffold.py#L42-L47)

```text
print("1) Fill .rct/spec.yaml, .rct/plan.yaml, .rct/checklist.yaml")
```

### Final independent review

**PASS:** Both default plan references now use .rct/plan.yaml. This matches the bundled scaffold's intended interface and the existing plan filename without claiming that the absent scaffold payload is available or forcing all external projects to use YAML. No project plan was renamed.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:12-16](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L12-L16)

```text
- `.rct/plan.yaml` — implementation plan
```

  The artifact inventory uses the correct default.

- [.claude/skills/rct-methodology/references/reviewer_prompts.md:282-284](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/references/reviewer_prompts.md?plain=1#L282-L284)

```text
Refer to `.rct/plan.yaml` for the mapping.
```

  The phase-to-spec lookup uses the same filename.

- [.claude/skills/rct-methodology/scripts/luka_scaffold.py:42-47](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rct-methodology/scripts/luka_scaffold.py#L42-L47)

```text
print("1) Fill .rct/spec.yaml, .rct/plan.yaml, .rct/checklist.yaml")
```

  The local script defines this intended default interface even though its missing-payload guard prevents scaffolding here.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
s=Path('.claude/skills/rct-methodology/references/reviewer_prompts.md').read_text()
print('default_plan_references', {'plan.yaml':s.count('.rct/plan.yaml'),'plan.md':s.count('.rct/plan.md')})
for p in ('.rct/plan.md','.rct/plan.yaml'):
    print(p, 'exists', Path(p).exists())
PY
```

  default_plan_references {'plan.yaml': 2, 'plan.md': 0}
  .rct/plan.md exists False
  .rct/plan.yaml exists True

  The full prompt file contains exactly the two corrected references. The live filename is corroboration, not a universal naming rule for portable projects.

## A14-013: Skill packaging documentation overstates what the bundled validator checks

**Candidate severity:** medium. **Independent verdict:** confirm.

A successful package is presented as proof of checks the script does not perform. Broken YAML or missing bundled resources can therefore be distributed while the documented review is assumed complete.

### Baseline documentation evidence

- [.claude/skills/skill-creator/SKILL.md:189-195](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/skill-creator/SKILL.md?plain=1#L189-L195)

```text
- Description completeness and quality
```

- [.claude/skills/skill-creator/SKILL.md:189-195](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/skill-creator/SKILL.md?plain=1#L189-L195)

```text
- File organization and resource references
```

- [.claude/skills/skill-creator/SKILL.md:189-195](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/skill-creator/SKILL.md?plain=1#L189-L195)

```text
- YAML frontmatter format and required fields
```

### Implementation proof

- [.claude/skills/skill-creator/scripts/package_skill.py:48-55](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/skill-creator/scripts/package_skill.py#L48-L55)

```text
valid, message = validate_skill(skill_path)
```

  Packaging delegates its content validation to the single bundled quick validator before creating the archive.

- [.claude/skills/skill-creator/scripts/quick_validate.py:25-35](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/skill-creator/scripts/quick_validate.py#L25-L35)

```text
match = re.match(r"^---\n(.*?)\n---", content, re.DOTALL)
```

  The implementation recognizes delimiters and field substrings with regex rather than parsing YAML.

- [.claude/skills/skill-creator/scripts/quick_validate.py:48-56](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/skill-creator/scripts/quick_validate.py#L48-L56)

```text
return True, "Skill is valid!"
```

  After a hyphen-case name check and an angle-bracket check on the description, it succeeds. It never checks description completeness, resource targets, or skill-directory/name agreement.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
from unittest.mock import patch
p=Path('.claude/skills/skill-creator/scripts/quick_validate.py')
ns={'__name__':'audit_read_only'}
exec(compile(p.read_text(),str(p),'exec'),ns)
cases={
 'invalid_yaml':'---\nname: example\ndescription: [unclosed\n---\nBody\n',
 'todo_and_missing_resource':'---\nname: example\ndescription: TODO\n---\n[Missing](references/does-not-exist.md)\n',
}
for label,content in cases.items():
 with patch.object(Path,'exists',autospec=True,side_effect=lambda path: path.name=='SKILL.md'),patch.object(Path,'read_text',return_value=content):
  print(f'validate_skill({label}) = {ns["validate_skill"]("different-directory")}')
PY
```

  validate_skill(invalid_yaml) = (True, 'Skill is valid!')
  validate_skill(todo_and_missing_resource) = (True, 'Skill is valid!')
  A separate in-memory yaml.safe_load of the unclosed YAML sequence raised ParserError.

  Read-only synthetic inputs exercise the actual validator function. Only SKILL.md is modeled as existing; any attempted resource existence check would return false. No files, package archives, or mock skill directories are created.

### Independent challenge

The packaging promise is testable and materially exceeds the real quick validator. Independent in-memory cases for malformed YAML, an unfinished description plus a missing resource, and a directory/name mismatch all passed. Negative controls for an uppercase name, angle brackets, and absent frontmatter failed, confirming that the real function—not a blanket-success stub—was exercised. No archive was created.

- [.claude/skills/skill-creator/scripts/package_skill.py:48-55](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/skill-creator/scripts/package_skill.py#L48-L55)

```text
valid, message = validate_skill(skill_path)
```

  Packaging delegates validation to this function and continues on success.

- [.claude/skills/skill-creator/scripts/quick_validate.py:25-35](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/skill-creator/scripts/quick_validate.py#L25-L35)

```text
match = re.match(r"^---\n(.*?)\n---", content, re.DOTALL)
```

  The implementation extracts frontmatter with regex and checks field substrings; it does not parse YAML.

- [.claude/skills/skill-creator/scripts/quick_validate.py:48-56](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/skill-creator/scripts/quick_validate.py#L48-L56)

```text
return True, "Skill is valid!"
```

  After the limited name and description-character checks, there is no resource-target, directory-name, or description-completeness validation.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
from unittest.mock import patch
p=Path('.claude/skills/skill-creator/scripts/quick_validate.py')
ns={'__name__':'challenge_read_only'}
exec(compile(p.read_text(),str(p),'exec'),ns)
cases={
 'malformed_yaml':'---\nname: example\ndescription: [unclosed\n---\nBody\n',
 'todo_description_missing_resource':'---\nname: example\ndescription: TODO\n---\n[missing](references/does-not-exist.md)\n',
 'directory_name_mismatch':'---\nname: other-name\ndescription: Useful complete description\n---\nBody\n'}
for name,text in cases.items():
 with patch.object(Path,'exists',autospec=True,side_effect=lambda p:p.name=='SKILL.md'),patch.object(Path,'read_text',return_value=text):
  print(name,repr(ns['validate_skill']('different-directory')))
PY
```

  malformed_yaml (True, 'Skill is valid!')
  todo_description_missing_resource (True, 'Skill is valid!')
  directory_name_mismatch (True, 'Skill is valid!')

  Only SKILL.md is modeled as existing. No actual skill directory, resource, or package is written; any resource existence check would fail.

**Required correction:** Limit the automatic-validation description to the implemented folder/SKILL.md existence checks, delimiter/field-presence checks, name spelling checks, and limited description-character check. Explicitly assign valid YAML, description completeness, directory/name agreement, and resource-reference verification to manual pre-packaging review unless separately implemented later. Preserve the accurate .zip behavior, all attribution, and LICENSE.txt unchanged; do not modify the validator to make the current prose true.

### Correction

Packaging guidance now accurately limits automatic checks to folder/SKILL.md existence, regex delimiter/field-marker checks, name spelling, and the limited captured-description character check. Explicit manual pre-packaging review covers actual YAML parsing, description completeness/quality, directory/name agreement, and resource existence. Kept the .zip behavior and all license/frontmatter attribution; neither validator nor packager was modified or run to produce an archive.

- [.claude/skills/skill-creator/SKILL.md:191-195](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/SKILL.md?plain=1#L191-L195)

```text
- Frontmatter delimiters match the validator's regex, with `name:` and `description:` markers present
```

- [.claude/skills/skill-creator/SKILL.md:199-207](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/SKILL.md?plain=1#L199-L207)

```text
These regex checks do not parse YAML, assess description completeness or quality,
check directory/name agreement, or verify resource references.
```

- [.claude/skills/skill-creator/SKILL.md:203-207](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/SKILL.md?plain=1#L203-L207)

```text
- Confirm that the skill directory and declared name agree.
```

- [.claude/skills/skill-creator/scripts/quick_validate.py:25-35](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/scripts/quick_validate.py#L25-L35)

```text
match = re.match(r"^---\n(.*?)\n---", content, re.DOTALL)
```

- [.claude/skills/skill-creator/scripts/quick_validate.py:48-56](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/scripts/quick_validate.py#L48-L56)

```text
return True, "Skill is valid!"
```

- [.claude/skills/skill-creator/scripts/package_skill.py:48-55](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/scripts/package_skill.py#L48-L55)

```text
valid, message = validate_skill(skill_path)
```

### Final independent review

**PASS:** The packaging section now precisely limits automatic checks to directory/SKILL.md existence and the actual regex-based delimiter, marker, name, and captured-description checks. It explicitly assigns real YAML parsing, description quality/completeness, directory/name agreement, and resource existence to manual pre-packaging review. I exercised the real quick validator in memory: malformed YAML, TODO/missing resource, and directory/name mismatch still pass; uppercase or double-hyphen names, description angle brackets, absent frontmatter, and a missing name marker fail. The .zip behavior, license pointer, license bytes, validator, and packager were preserved.

- [.claude/skills/skill-creator/SKILL.md:191-201](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/SKILL.md?plain=1#L191-L201)

```text
These regex checks do not parse YAML, assess description completeness or quality,
check directory/name agreement, or verify resource references.
```

  Successful packaging is no longer presented as proof of checks the script does not perform.

- [.claude/skills/skill-creator/SKILL.md:203-209](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/SKILL.md?plain=1#L203-L209)

```text
- Parse the frontmatter as YAML and verify the required fields and intended values.
- Review the description for completeness, clear triggers, and quality.
- Confirm that the skill directory and declared name agree.
- Review file organization and verify every referenced script, reference, and asset exists at the documented location.
```

  Every accepted missing check is assigned to explicit manual review rather than silently delegated back to the limited validator.

- [.claude/skills/skill-creator/scripts/quick_validate.py:25-35](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/scripts/quick_validate.py#L25-L35)

```text
match = re.match(r"^---\n(.*?)\n---", content, re.DOTALL)
```

  The real implementation uses regex extraction and field-marker presence, not YAML parsing.

- [.claude/skills/skill-creator/scripts/quick_validate.py:38-56](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/scripts/quick_validate.py#L38-L56)

```text
desc_match = re.search(r"description:\s*(.+)", frontmatter)
```

  The subsequent checks are limited to captured name spelling and angle brackets in the regex-captured description; there is no completeness, resource, or directory/name validation.

- [.claude/skills/skill-creator/scripts/package_skill.py:31-55](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/scripts/package_skill.py#L31-L55)

```text
valid, message = validate_skill(skill_path)
```

  The packager checks input directory and SKILL.md existence, then delegates content checks to the exact quick validator exercised in memory.

- [.claude/skills/skill-creator/scripts/package_skill.py:58-75](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/scripts/package_skill.py#L58-L75)

```text
zip_filename = output_path / f"{skill_name}.zip"
```

  skill_name is the directory name, and archive paths are relative to its parent. The corrected prose accurately retains the folder-named .zip behavior; no archive was created for this review.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
from unittest.mock import patch
p=Path('.claude/skills/skill-creator/scripts/quick_validate.py')
ns={'__name__':'v14_read_only'}
exec(compile(p.read_text(),str(p),'exec'),ns)
cases={
 'malformed_yaml':'---\nname: example\ndescription: [unclosed\n---\nBody\n',
 'todo_and_missing_resource':'---\nname: example\ndescription: TODO\n---\n[Missing](references/does-not-exist.md)\n',
 'directory_name_mismatch':'---\nname: other-name\ndescription: A useful description\n---\nBody\n',
 'uppercase_name':'---\nname: Bad-Name\ndescription: Useful\n---\nBody\n',
 'angle_brackets':'---\nname: good-name\ndescription: <invalid>\n---\nBody\n',
 'no_frontmatter':'Body only\n'
}
for name,content in cases.items():
 with patch.object(Path,'exists',autospec=True,side_effect=lambda p:p.name=='SKILL.md'),patch.object(Path,'read_text',return_value=content):
  print(name,ns['validate_skill']('different-directory'))
PY
```

  malformed_yaml (True, 'Skill is valid!')
  todo_and_missing_resource (True, 'Skill is valid!')
  directory_name_mismatch (True, 'Skill is valid!')
  uppercase_name (False, "Name 'Bad-Name' should be hyphen-case (lowercase letters, digits, and hyphens only)")
  angle_brackets (False, 'Description cannot contain angle brackets (< or >)')
  no_frontmatter (False, 'No YAML frontmatter found')

  This exercises the real function with no filesystem writes. Only SKILL.md is modeled as existing, so a resource check would fail if one were implemented. Separately, installed PyYAML rejected the malformed example with ParserError.

- [.claude/skills/skill-creator/SKILL.md:1-5](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/skill-creator/SKILL.md?plain=1#L1-L5)

```text
license: Complete terms in LICENSE.txt
```

  The license pointer and all other non-description metadata are unchanged.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
import hashlib, subprocess
p='.claude/skills/skill-creator/LICENSE.txt'
raw=Path(p).read_bytes()
old=subprocess.check_output(['git','show','56208b9e6cee078f52c43af8f6b36660bc93eeb4:'+p])
print('license_byte_identical',raw==old)
print('license_sha256',hashlib.sha256(raw).hexdigest())
PY
```

  license_byte_identical True
  license_sha256 cfc7749b96f63bd31c3c42b5c471bf756814053e847c10f3eb003417bc523d30

  The Apache License text was preserved byte-for-byte. The validator and packager are also byte-identical to the baseline.

## A14-014: Hook troubleshooting omits Meerkat's repository-owned hook installer

**Candidate severity:** medium. **Independent verdict:** reject.

The troubleshooting instructions occur in a skill that explicitly explains Meerkat's installed hooks and current toolchain. For this repository, the generic commands are not a sufficient repair: they do not establish the required repository-owned wrapper. This does not assert that every pre-commit installation destroys an existing legacy hook.

### Baseline documentation evidence

- [.claude/skills/rust-cicd-pipeline/SKILL.md:251-255](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L251-L255)

```text
pre-commit install
pre-commit install --hook-type pre-push
```

### Implementation proof

- [Makefile:550-562](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Makefile#L550-L562)

```text
install -m 0755 scripts/git-hooks/pre-push "$$hooks/pre-push"
```

  Meerkat's actual installer installs a repository-owned Git hook, not merely pre-commit's generic adapter.

- [scripts/git-hooks/pre-push:4-5](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/git-hooks/pre-push#L4-L5)

```text
exec "${root}/scripts/pre-push-dispatch.sh" "$@"
```

  The required hook enters the exact-ref dispatcher.

- [scripts/pre-push-dispatch.sh:63-82](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/scripts/pre-push-dispatch.sh#L63-L82)

```text
while read -r next_local_ref next_local_sha next_remote_ref next_remote_sha; do
```

  The dispatcher consumes raw Git ref updates and enforces a single exact pushed ref before validation. Installing only a generic pre-commit hook in a fresh checkout does not install this code.

### Independent challenge

The troubleshooting commands are exactly the installer supplied by the deliberately portable Makefile template. The generic setup copies that template and the main quick-setup instructions already say make install-hooks, which selects the right implementation in either a generated project or Meerkat. Meerkat's custom dispatcher is real, but its existence does not make generic pre-commit installation advice false. No sentence in the cited troubleshooting block specifically claims to reinstall Meerkat's dispatcher, and no destructive replacement behavior was demonstrated. Do not turn this into a requirement that portable projects have Meerkat's hook scripts.

- [.claude/skills/rust-cicd-pipeline/references/makefile-template.md:106-111](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/references/makefile-template.md?plain=1#L106-L111)

```text
	pre-commit install
	pre-commit install --hook-type pre-push
```

  These are the executable template's actual generic install-hooks recipe, not a mistaken spelling of the repository-owned installer.

- [.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py:67-72](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py#L67-L72)

```text
content = extract_makefile(template)
```

  The setup implementation emits this portable recipe. Its output was independently captured in memory and matched it.

- [Makefile:550-561](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Makefile#L550-L561)

```text
install -m 0755 scripts/git-hooks/pre-push "$$hooks/pre-push"
```

  The current repository has a different legitimate installer; this is not proof that the portable recipe is inaccurate.

- [.claude/skills/rust-cicd-pipeline/SKILL.md:80-85](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L80-L85)

```text
make install-hooks
```

  The guide's main setup entry point already dispatches through the project's own Makefile. A14-015 separately addresses the genuinely misleading unqualified hook-behavior table.

**Disposition:** Rejected; no correction authorized for this claim.

## A14-015: The hook-behavior table describes Meerkat but links to a different portable template

**Candidate severity:** low. **Independent verdict:** confirm.

The skill mixes a reusable setup promise with repository-specific behavior without marking the switch in this section. Following its template link does not produce the claimed commit/push behavior.

### Baseline documentation evidence

- [.claude/skills/rust-cicd-pipeline/SKILL.md:13-16](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L13-L16)

```text
- **Progressive Validation**: Auto-fix and generated-file sync on commit;
```

- [.claude/skills/rust-cicd-pipeline/SKILL.md:135-142](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L135-L142)

```text
The `.pre-commit-config.yaml` runs:
```

- [.claude/skills/rust-cicd-pipeline/SKILL.md:139-152](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L139-L152)

```text
See `references/pre-commit-config.yaml` for the template.
```

### Implementation proof

- [.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py:75-78](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py#L75-L78)

```text
content = read_template("pre-commit-config.yaml")
```

  The setup script copies the referenced portable template; it does not synthesize the Meerkat-specific hook set described in the table.

- [.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml:7-18](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml#L7-L18)

```text
entry: cargo fmt --all -- --check
```

  The portable commit hook checks formatting rather than auto-fixing it.

- [.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml:20-38](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml#L20-L38)

```text
entry: cargo clippy --all-targets -- -D warnings
```

  The generic template runs Clippy and make test in its default commit stage, unlike the described Meerkat push-only validation arrangement.

- [.pre-commit-config.yaml:12-43](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.pre-commit-config.yaml#L12-L43)

```text
entry: scripts/repo-cargo fmt --all
```

  The claimed auto-fix/Bazel/dogma sequence is real in Meerkat's root configuration, not in the portable reference the reader is told to copy.

### Independent challenge

This is a concrete discrepancy between the promised copied-template behavior and the output of setup_cicd.py, not a reason to impose Meerkat's rules on that template. The copied portable configuration runs check-only formatting, Clippy, and unit tests at its default commit stage; the unqualified prose instead describes the root repository's auto-fix/generated-file policy and links directly to the portable template.

- [.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py:75-78](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py#L75-L78)

```text
content = read_template("pre-commit-config.yaml")
```

  The real setup copies the reference configuration unchanged. I captured create_pre_commit_config output in memory and verified byte equality with the template.

- [.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml:7-18](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml#L7-L18)

```text
entry: cargo fmt --all -- --check
```

  With default_stages set to pre-commit, this checks formatting on commit; it does not auto-fix.

- [.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml:20-38](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml#L20-L38)

```text
entry: cargo clippy --all-targets -- -D warnings
```

  Clippy and make test also use the default commit stage. Parsing the captured output confirmed these stages and the separate pre-push test-all/doc/audit hooks.

- [.pre-commit-config.yaml:12-43](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.pre-commit-config.yaml#L12-L43)

```text
entry: scripts/repo-cargo fmt --all
```

  The auto-fix/generated-file policy belongs to Meerkat's root configuration, which is not the generated portable output.

**Required correction:** Label the existing auto-fix/Bazel/dogma hook description as Meerkat-specific. Separately describe the portable template's actual stages: formatting verification, Clippy, unit tests and general checks at commit; full tests, documentation, and audit at push. Scope the introductory progressive-validation claim and accompanying diagram accordingly. Keep the portable template and its generic commands unchanged; this correction is confined to the skill's explanatory prose.

### Correction

Scoped the introductory progressive-validation claim and hook diagram to the portable template's actual behavior. Added a separate portable-stage description: check-only formatting, Clippy, unit tests, secrets/general file checks on commit; full tests, documentation, and audit on push. Explicitly labeled Meerkat's root auto-fix/Bazel/dogma hooks as a separate repository policy, and distinguished the later repository test-lane timing from the portable commit-time unit hook. Portable templates and generic installation/release semantics are unchanged.

- [.claude/skills/rust-cicd-pipeline/SKILL.md:15-17](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L15-L17)

```text
- **Progressive Validation (portable template)**: Formatting verification, Clippy,
  unit tests, secrets, and general file checks on commit; full tests,
  documentation builds, and dependency audit on push
```

- [.claude/skills/rust-cicd-pipeline/SKILL.md:24-25](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L24-L25)

```text
The hook column below describes the portable template.
```

- [.claude/skills/rust-cicd-pipeline/SKILL.md:141-156](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L141-L156)

```text
- Formatting verification: `cargo fmt --all -- --check` (not an auto-fix)
```

- [.claude/skills/rust-cicd-pipeline/SKILL.md:161-178](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L161-L178)

```text
Meerkat's root `.pre-commit-config.yaml` is separate from the portable template.
```

- [.claude/skills/rust-cicd-pipeline/SKILL.md:312-314](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L312-L314)

```text
The following lane timing describes Meerkat's repository policy, not the portable
template's commit-time unit-test hook described above.
```

- [.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py:75-78](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py#L75-L78)

```text
content = read_template("pre-commit-config.yaml")
```

- [.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml:7-38](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml#L7-L38)

```text
entry: cargo fmt --all -- --check
```

- [.pre-commit-config.yaml:12-43](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.pre-commit-config.yaml#L12-L43)

```text
entry: scripts/repo-cargo fmt --all
```

### Final independent review

**PASS:** The introduction, hook diagram, stage list, and subsequent lane-timing scope now distinguish the portable generated project from Meerkat's root hook policy. I ran the actual setup functions with write_file replaced by an in-memory capture: the emitted hook configuration exactly matches the portable reference, with check-only formatting, Clippy, unit tests and general checks at commit, and full tests/docs/audit at push. The portable template and setup implementation remain unchanged; Meerkat-only scripts were not injected. The separately rejected installer and version-validation policy claims remain rejected.

- [.claude/skills/rust-cicd-pipeline/SKILL.md:15-25](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L15-L25)

```text
- **Progressive Validation (portable template)**: Formatting verification, Clippy,
  unit tests, secrets, and general file checks on commit; full tests,
  documentation builds, and dependency audit on push
```

  The introductory promise and explicitly scoped hook column match the generated portable template, not Meerkat's root hooks.

- [.claude/skills/rust-cicd-pipeline/SKILL.md:141-159](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L141-L159)

```text
- Formatting verification: `cargo fmt --all -- --check` (not an auto-fix)
```

  The portable section lists the actual commit/push stages, acknowledges file/type filters, and states that it does not install Meerkat's scripts or generated-file synchronization.

- [.claude/skills/rust-cicd-pipeline/SKILL.md:161-178](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L161-L178)

```text
Meerkat's root `.pre-commit-config.yaml` is separate from the portable template.
```

  The auto-fix/Bazel/dogma behavior is now correctly labeled as repository-specific rather than promised by the copied template.

- [.claude/skills/rust-cicd-pipeline/SKILL.md:312-320](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L312-L320)

```text
The following lane timing describes Meerkat's repository policy, not the portable
template's commit-time unit-test hook described above.
```

  The later timing table no longer appears to contradict the portable commit-time unit hook.

- [.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py:75-78](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py#L75-L78)

```text
content = read_template("pre-commit-config.yaml")
```

  The actual generator copies the reference hook configuration unchanged. Its output was captured from the real create_pre_commit_config function, not inferred only from another document.

- [.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml:7-38](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml#L7-L38)

```text
entry: cargo fmt --all -- --check
```

  default_stages is pre-commit; the formatting, Clippy, and make test hooks do not override it.

- [.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml:59-88](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml#L59-L88)

```text
entry: make test-all
```

  The full-test, documentation, and audit hooks each explicitly select pre-push.

- [.pre-commit-config.yaml:12-44](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/.pre-commit-config.yaml#L12-L44)

```text
entry: scripts/repo-cargo fmt --all
```

  The root configuration separately supplies formatting auto-fix, path-filtered Bazel regeneration, and dogma synchronization.

- [scripts/pre-push-unit.sh:327-362](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/scripts/pre-push-unit.sh#L327-L362)

```text
"workspace integration lane"
```

  The current repository's deterministic gate really includes unit, integration, and e2e-fast lanes; the newly labeled repository timing is supported by implementation rather than assumed from the portable template.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
import yaml
p=Path('.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py')
ns={'__name__':'v14_read_only','__file__':str(p)}
exec(compile(p.read_text(),str(p),'exec'),ns)
out={}
ns['write_file']=lambda path,content,*a,**kw:out.setdefault(path.name,content)
ns['create_pre_commit_config'](Path('v14-memory-only'))
ns['create_makefile'](Path('v14-memory-only'),'review-crate')
print('template_byte_equal',out['.pre-commit-config.yaml']==(p.parent.parent/'references/pre-commit-config.yaml').read_text())
cfg=yaml.safe_load(out['.pre-commit-config.yaml'])
for repo in cfg['repos']:
 for hook in repo['hooks']:
  if 'entry' in hook:
   print(hook['id'],hook.get('stages',cfg['default_stages']),hook['entry'])
print('Meerkat_wrapper_in_generated_files','scripts/repo-cargo' in ''.join(out.values()))
print(next(s for s in out['Makefile'].splitlines() if s.startswith('ci:')))
PY
```

  template_byte_equal True
  cargo-fmt ['pre-commit'] cargo fmt --all -- --check
  cargo-clippy ['pre-commit'] cargo clippy --all-targets -- -D warnings
  cargo-test ['pre-commit'] make test
  cargo-test-all ['pre-push'] make test-all
  cargo-doc ['pre-push'] make doc
  cargo-audit ['pre-push'] make audit
  Meerkat_wrapper_in_generated_files False
  ci: fmt-check lint test-all audit

  The in-memory output verifies actual generator/template behavior. The full captured hook inventory also places secrets and general file checks at pre-commit. The independent verify-version target and generic pre-commit install recipes are retained, not newly wired into every CI invocation.

## A14-016: Automatic tag-version verification is advertised but not wired into the portable pipeline

**Candidate severity:** low. **Independent verdict:** reject.

The generic setup provides an optional verify-version target, but users copying the templates and running the documented CI command do not receive the advertised automatic release-tag guard. Meerkat's separate repository release workflow does have its own checks and is not the defect.

### Baseline documentation evidence

- [.claude/skills/rust-cicd-pipeline/SKILL.md:13-19](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/SKILL.md?plain=1#L13-L19)

```text
- **Version Consistency**: Automatic verification between tag and Cargo.toml
```

- [.claude/skills/rust-cicd-pipeline/references/makefile-template.md:139-151](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/references/makefile-template.md?plain=1#L139-L151)

```text
verify-version:
```

### Implementation proof

- [.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py:67-72](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py#L67-L72)

```text
content = extract_makefile(template)
```

  The generated generic Makefile is exactly the first fenced template block, with only CRATE_NAME substituted. There is no additional release/tag wiring in this step.

- Read-only reproduction:

```text
python3 -B - <<'PY'
from pathlib import Path
import subprocess
p=Path('.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py')
ns={'__name__':'audit_read_only','__file__':str(p)}
exec(compile(p.read_text(),str(p),'exec'),ns)
makefile=ns['customize_template'](ns['extract_makefile'](ns['read_template']('makefile-template.md')),{'CRATE_NAME':'audit-example'})
r=subprocess.run(['make','--dry-run','--file','-','ci'],input=makefile,text=True,capture_output=True)
print(r.stdout)
print('exit='+str(r.returncode))
PY
```

  exit=0. The generated ci recipe prints cargo fmt --all -- --check; cargo clippy --all-targets --all-features -- -D warnings -D clippy::all -D clippy::pedantic; cargo test --all-features --all-targets; and cargo deny check. It never invokes verify-version, cargo metadata, git describe, or jq.

  This executes the real template extractor and Make dependency selection in dry-run mode, without creating a Makefile or building anything. The advertised one-command generic CI path does not automatically run the tag check.

- [.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml:59-88](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/references/pre-commit-config.yaml#L59-L88)

```text
entry: make test-all
```

  The portable push hooks invoke tests, docs, and audit; they do not supply missing tag/version verification either.

### Independent challenge

The candidate proves that make ci does not depend on verify-version, but it does not establish that the guide promises this dependency. The template does supply an automated version-comparison target expressly labeled for release validation; automation of the comparison is not the same claim as automatic scheduling on every CI invocation. Its recipe extracts both versions and exits nonzero on mismatch. The generic setup is a set of customizable Make/hooks templates, not a supplied tag-triggered CI service. Treating the headline alone as a guarantee that ci installs or runs a tag workflow is an unsupported interpretation. No wiring change or mandatory Meerkat release policy is accepted.

- [.claude/skills/rust-cicd-pipeline/references/makefile-template.md:139-148](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/references/makefile-template.md?plain=1#L139-L148)

```text
elif [ "$$VERSION" != "$$TAG" ]; then \
		echo "$(RED)Version mismatch: Cargo.toml has $$VERSION but tag is $$TAG$(NC)"; \
		exit 1; \
```

  The shipped release-validation target actually automates the advertised comparison; it is not a stub or a manual-inspection instruction.

- [.claude/skills/rust-cicd-pipeline/references/makefile-template.md:77-79](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/references/makefile-template.md?plain=1#L77-L79)

```text
ci: fmt-check lint test-all audit
```

  The separate CI target indeed omits release-tag validation. This observed separation is not itself proof of a contradictory promise.

- [.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py:67-72](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py#L67-L72)

```text
content = customize_template(content, {"CRATE_NAME": crate_name})
```

  Executing create_makefile with write_file replaced by an in-memory capture showed that the generated file contains the independent ci and verify-version targets exactly as supplied.

- [.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py:239-246](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/.claude/skills/rust-cicd-pipeline/scripts/setup_cicd.py#L239-L246)

```text
print("1. Review and customize the generated files")
```

  The installer explicitly hands integration/customization to the project. Absence of a preconfigured tag trigger is not evidence that the provided comparison capability is missing.

**Disposition:** Rejected; no correction authorized for this claim.

## Lane limitations

- **Audit:** No nested agents, builds, test suites, installations, commits, branch changes, provider calls, workflow dispatches, or source/document edits were performed. The scaffold reproduction exited before its write loop; template and validator experiments used stdin/in-memory data. The repository was clean after the read-only probes.
- **Audit:** All 22 assigned document hashes matched the manifest and HEAD matched 56208b9e6cee078f52c43af8f6b36660bc93eeb4 when audited. The ledger is a first-wave candidate list, not independent adjudication or an implementation record.
- **Audit:** Inspected symlinks only with lstat/readlink: .claude/skills/banana-skill -> /Users/luka/.codex/skills/banana-skill; .claude/skills/openai-docs -> /Users/luka/.codex/skills/openai-docs; .claude/skills/rct-lite -> /Users/luka/.codex/skills/rct-lite. None of those personal external targets was followed, read, edited, or judged against an assumed installation contract.
- **Audit:** Inspected the seven repo-local alias texts: .codex/skills/meerkat-platform and .rkat/skills/meerkat-platform both point to ../../.claude/skills/meerkat-platform; the two meerkat/embedded_skills SKILL.md aliases and three embedded platform-reference aliases point to the corresponding .claude canonical files. Content/alias coverage belongs to the canonical-source lane; no competing alias finding or duplicated skill copy is proposed.
- **Audit:** The Force MCP server implementation and catalog are not checked into this cohort's source evidence. Its agent parameter names, session semantics, supported models, and environment availability remain unverified rather than being inferred from Meerkat's unrelated provider catalog.
- **Audit:** Missing Luka Loop assets prevent verification of the documented runtime status set, all/blockers/final retry sequence, timeout, locking, .done markers, PID checks, log limits, and commit/push finalization. A14-010 records the demonstrated packaging/availability failure; no extra findings assume how absent scripts would behave.
- **Audit:** Generic templates were evaluated as reusable content. Placeholder crate/model-independent examples, historical example dates, the generic Rust 1.75 pin, and raw Cargo commands were not automatically rewritten to Meerkat-specific versions, packages, or wrapper commands. The license and explicitly historical keystone proposal were preserved.
- **Challenge:** Independently adjudicated all 16 candidates: 12 confirmed with bounded scopes and 4 rejected (A14-004, A14-008, A14-014, A14-016). Rejections distinguish normative review policy and portable-template integration choices from false factual descriptions; they do not endorse every existing policy.
- **Challenge:** Read all 22 manifest documents. At the start of review, all 22 SHA-256 hashes matched the manifest and HEAD was 56208b9e6cee078f52c43af8f6b36660bc93eeb4. The repository was initially clean. No documentation, product code, template, dependency, or license was modified by this challenger.
- **Challenge:** Only challenge-A14.json is written. No nested agents, builds, Cargo/Make validation runs, hook installation, dependency installation, commits, provider calls, or external workflow execution occurred. Script experiments used python3 -B, in-memory output capture, and explicit traps/intercepts for filesystem mutations.
- **Challenge:** Inspected the 10 listed symlink entries using lstat/readlink only. Did not follow the three external personal skill symlinks or inspect the absolute personal roadmap target. No claim is made about complete external skill installations, The Force server behavior, or the availability of third-party assets elsewhere.
- **Challenge:** A14-010 accepts only the demonstrated missing-payload prerequisite in this checkout, not a requirement to vendor optional automation. Absent loop implementations prevent checking their locking, timeout, retries, and finalization behavior; these were not independently labeled false.
- **Challenge:** The renderer was exercised with all output writes intercepted. It renders the bundled phases-based checklist example, while the existing unrelated repository checklist uses top-level tasks and yielded only its title. The fallback correction is therefore explicitly limited to the skill's supported input schema; changing existing .rct metadata is not an accepted new finding.
- **Challenge:** Portable template behavior was checked through the actual setup functions with write_file replaced by an in-memory capture, then by inspecting their emitted recipes and parsing the emitted hook stages with already-installed PyYAML. No recipe invoking Cargo, pre-commit, jq, or a release was executed; A14-016 does not certify those tools' external availability.
- **Challenge:** The Apache License 2.0 text in .claude/skills/skill-creator/LICENSE.txt was read and its manifest hash matched. License and attribution preservation remain required; the accepted packaging-documentation correction does not authorize changing that license.
- **Challenge:** No new findings were added beyond the original 16 candidates. Historical design notes, placeholder project examples, generic raw-Cargo templates, and preference-only review rules were not turned into current implementation defects.
