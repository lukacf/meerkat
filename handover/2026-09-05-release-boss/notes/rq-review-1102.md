# rust-quality review: #1102 actor-loop redesign (received via teammate message 2026-09-05 01:50Z)
Scope: bea8475e0..0aae030d6 on fix/mob-actor-member-work-off-loop. Read-only. VERDICT: REQUEST_CHANGES (finding 1 blocking).

1. HIGH blocking. event_pump.rs:1263-1275 (ensure_pump) and :1440-1470 (ensure_pump_with_tap): early return after pump_installed_while_barrier_released drops the replaced entry: loses entry.taps (old subscribers see EOF), entry.obligation_keepalive (_with_tap), and waiters.fail_all_for(&entry.expected_member) when expected_member changed. Fix: pass taps + keepalive into the helper (:1110-1135 holds `existing` under the state lock), merge on exact match; on mismatch fail_all_for before yielding.
2. MEDIUM. begin_resume_lifecycle / spawn_resume_readiness_fanout / resume_lifecycle_readiness_resolved: pending_resume_lifecycle only blocks a second ResumeLifecycle; other lifecycle commands can interleave during the fan-out. PreCommit fails closed. PostCommit: finish_resume_lifecycle_post_commit runs reconcile_resume_topology (roster write) and ResumeOrchestrator against a possibly Stopped mob. Recommend LifecycleOperationPending { intent: "explicit_resume" } rejection while pending, or re-probe phase before commit/finish. join_next Err dropped = missing outcome reads as success.
(remainder pending)
2 (end). join_next Err should be a typed Internal fail-closed conversion, not log-and-continue.
3. MEDIUM. actor.rs diff 1978-1992, 2329-2362: AwaitTurnCompletion (TurnCompleted) still goes straight to spawn_turn_completed_reply with readiness there, outside the lane; finish_submit_work_dispatch kickoff bypasses the lane too. TurnCompleted can overtake parked IngressAccepted deliveries to the same member. Route through the lane or qualify the CHANGELOG ordering claim (CHANGELOG.md:75-99); per_member_delivery_order_is_preserved covers one ack mode.
4. MEDIUM. Reload verb (actor.rs spawn_member_registration_reload diff 1024-1107, provisioner.rs:9420-9532, handle.rs:11178-11190): (a) no end-to-end bound (MEMBER_RETIRE_TOTAL_TIMEOUT bounds only the discard; revival/readiness unbounded; handle uses unbounded send_actor_command); (b) discard succeeds regardless of store health; probe pass + full-load failure -> ResolveMemberRevivalFailed marks member Broken (actor.rs:12340-12370): probe durable resume authority before discard or document precondition; (c) provisioner.rs:9448-9456, 9469-9472 reuse RetirementInProgress { stage: "durability_reload_discard" } for a reload timeout (mislabel).
(findings 5-7 pending)
5. LOW. MemberReloadRequired detail JSON retryable:false but ErrorCode::SessionBusy / MobFailureClass::TargetBusy read transient (error.rs:507-511, 1318-1325, 1400-1405, 1492-1495). Distinct code/class (deferred, #1105).
6. LOW. ReviveMemberLiveMaterialization arm (actor.rs diff 1878-1893) still executes the full revival inline on the loop; pre-existing, watchdog will page on it. Follow-on (#1105).
7. LOW Rust quality. Duplicate #[allow(clippy::too_many_arguments)] on spawn_turn_completed_reply; ActorInlineStepWatchdog has no Drop (checker task leaks on unwind); MemberReloadOutcome / MemberAdmissionBacklogSnapshot / SessionDurabilityReloadRequired lack #[non_exhaustive] (precedent WorkDeliveryReceipt); MobError::Internal(format!) in provisioner reload path continues a pre-existing pattern.
No defect: lanes (no lost wakeup, no deadlock, abort_all before command_rx drop, close() on fail-stop makes lane sends fail typed); DSL SubmitWork transitions are update{} guards + RequestRuntimeIngress emit only, nothing to reconcile after a lane-side failure; every SubmitWork constructor holds reply_rx for the round trip so closed receiver == caller left; CHANGELOG breaking entries correct; MobProvisioner::reload_degraded_runtime_registration has a default body.
