# A18: Machine and composition specification documentation

This ledger preserves the baseline evidence and independent disposition. See [the audit index](README.md) for scope, commits, and publication boundaries.

## A18-001: Generated WorkAttention coverage invents a mandatory pause-deadline invariant

**Candidate severity:** medium. **Independent verdict:** confirm.

The current generated coverage anchor contradicts the canonical machine and the same documents' generated invariant/effect tables. Most materially, paused_has_pause_deadline suggests indefinite pauses are invalid even though they are an explicit supported state. The other obsolete names also make the coverage note unreliable for locating actual invariants and effects. This is a documentation-source error, not an authority-boundary or domain-adapter defect.

### Baseline documentation evidence

- [specs/machines/work_attention_lifecycle/contract.md:170-170](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/work_attention_lifecycle/contract.md?plain=1#L170)

```text
effects Paused, Resumed, Stopped, Superseded; invariants active_has_no_pause_deadline, paused_has_pause_deadline, stopped_has_stop_time, superseded_has_target
```

- [specs/machines/work_attention_lifecycle/mapping.md:11-11](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/work_attention_lifecycle/mapping.md?plain=1#L11)

```text
effects Paused, Resumed, Stopped, Superseded; invariants active_has_no_pause_deadline, paused_has_pause_deadline, stopped_has_stop_time, superseded_has_target
```

### Implementation proof

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:236-247](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L236-L247)

```text
invariant paused_has_pause_state {
            self.lifecycle_phase == Phase::Paused || self.paused_until_utc_ms == None
        }
```

  The complete canonical invariant block has live_has_no_terminal_time, paused_has_pause_state and superseded_records_successor. Its pause predicate says a deadline must be absent outside Paused, not present inside Paused. None of the four invariant names in the anchor note belongs to this block.

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:256-275](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L256-L275)

```text
self.paused_until_utc_ms = until_utc_ms;
```

  Both PauseActive and PausePaused copy the optional deadline without a non-None guard and emit AttentionPaused, not a DSL effect named Paused.

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:369-378](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L369-L378)

```text
self.paused_until_utc_ms == None
                    || self.paused_until_utc_ms.get("value") > now_utc_ms
```

  The canonical eligibility classifier explicitly treats an indefinite pause as pending/ineligible. This is an intentionally supported state, not a malformed pause.

- [meerkat-workgraph/tests/attention_contracts.rs:2613-2626](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-workgraph/tests/attention_contracts.rs#L2613-L2626)

```text
WorkAttentionStatus::Paused { until: None }
```

  The existing regression test successfully requests until: None and asserts this status. The test was inspected, not executed; the DSL guards and updates independently establish the behavior.

- [meerkat-machine-schema/src/catalog/coverage.rs:2108-2129](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L2108-L2129)

```text
invariants active_has_no_pause_deadline, paused_has_pause_deadline, stopped_has_stop_time, superseded_has_target
```

  This source-owned free-form anchor note emits the same stale text into both documents, while the adjacent typed claims correctly use AttentionPaused/AttentionResumed/AttentionSuperseded/AttentionStopped and paused_has_pause_state. Mechanical regeneration alone cannot correct the prose.

### Independent challenge

Confirmed as incorrect documentary coverage prose, not as an enforced mandatory-deadline invariant or a product defect. I checked the domain-facing adapter as a possible explanation for the different names: it passes an optional deadline directly to the canonical Pause input and returns a binding, rather than providing a competing Paused-effect alphabet. The actual invariant allows Paused with no deadline; it only prohibits a retained pause deadline outside Paused. Both pause transitions accept None, and the eligibility transition explicitly classifies that state as ineligible. The generated anchor lists nonexistent canonical invariant names and ambiguous/noncanonical lifecycle-effect names, while its separate typed coverage claims already use the correct vocabulary. Correct the note, not the DSL or the claims.

- [specs/machines/work_attention_lifecycle/mapping.md:10-14](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/work_attention_lifecycle/mapping.md?plain=1#L10-L14)

```text
invariants active_has_no_pause_deadline, paused_has_pause_deadline, stopped_has_stop_time, superseded_has_target
```

  This is current generated anchor prose, not a historical note. The same note is at contract.md:170; the adjacent scenario already lists the actual canonical invariant names.

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:236-275](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L236-L275)

```text
self.lifecycle_phase == Phase::Paused || self.paused_until_utc_ms == None
```

  The complete invariant block is live_has_no_terminal_time, paused_has_pause_state, and superseded_records_successor. This predicate is true for Paused whether the deadline is Some or None. PauseActive and PausePaused guard only lifecycle/revision, copy until_utc_ms, and emit AttentionPaused.

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:369-378](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L369-L378)

```text
self.paused_until_utc_ms == None
```

  ClassifyEligibilityPausedPending explicitly includes None and emits AttentionEligibilityClassified { eligible: false }. An indefinite pause is represented intentionally, not merely tolerated accidentally by an incomplete guard.

- [meerkat-workgraph/src/machine.rs:56-70](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-workgraph/src/machine.rs#L56-L70)

```text
until_utc_ms: until.map(datetime_to_millis)
```

  The domain-facing pause adapter preserves optionality and applies the DSL before synchronizing its projection. There is no adapter-side conversion to a required deadline that could rescue the misleading note.

- [meerkat-machine-schema/src/catalog/coverage.rs:2107-2129](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L2107-L2129)

```text
effects Paused, Resumed, Stopped, Superseded; invariants active_has_no_pause_deadline, paused_has_pause_deadline, stopped_has_stop_time, superseded_has_target
```

  This literal is the source of both bad generated paragraphs. The neighboring CoverageClaims instead use AttentionPaused, AttentionResumed, AttentionSuperseded, AttentionStopped, and paused_has_pause_state.

- [meerkat-machine-schema/src/catalog/coverage.rs:3078-3100](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L3078-L3100)

```text
.filter(|anchor| claimed_by(&anchor.claims, name))
```

  Coverage attribution consumes explicit claims, not note text; scenarios likewise filter scenario.claims. The note is additionally declared documentation-only at lines 76-85. A literal-only correction does not change even the typed coverage relationships.

- [xtask/src/machines.rs:298-335](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/xtask/src/machines.rs#L298-L335)

```text
&render_machine_contract_markdown(&machine.schema, &machine.coverage)
```

  Generation sends coverage to contract and mapping renderers, but sends only machine.schema to the semantic-model and kernel renderers. artifacts.rs:818-829 prints anchor.note; render.rs:247-295 prints the same note into mapping coverage. This establishes the documentary output dependency and excludes a runtime/formal behavior change from editing this note alone.

**Required correction:** Edit only the work_attention_lifecycle CoverageAnchor.note literal in meerkat-machine-schema/src/catalog/coverage.rs: name the lifecycle effects AttentionPaused, AttentionResumed, AttentionStopped, and AttentionSuperseded, and the actual invariants live_has_no_terminal_time, paused_has_pause_state, and superseded_records_successor; explicitly state that Paused may have no deadline. Keep the typed CoverageClaims, IDs, DSL predicates, guards, and transitions unchanged. Then regenerate specs/machines/work_attention_lifecycle/contract.md and specs/machines/work_attention_lifecycle/mapping.md through the existing machine-codegen path. Exact unchanged generator dependencies are xtask/src/machines.rs::machine_codegen_at_root, meerkat-machine-codegen/src/artifacts.rs::render_machine_contract_markdown, and meerkat-machine-codegen/src/render.rs::render_machine_mapping_coverage plus merge_mapping_document. The source entry being edited is documentary metadata, not semantic authority: no kernel or TLA behavior change is required or authorized.

### Correction

Corrected only work_attention_lifecycle's CoverageAnchor.note: the four Attention-prefixed lifecycle effects and three actual invariant names now match the DSL, and the note explicitly permits Paused with no deadline. IDs, typed CoverageClaims, predicates, guards, and transitions are unchanged. No further source correction is required. Parent completed canonical make machine-codegen; corrected documentary metadata is now present in both generated contract and mapping projections.

- [meerkat-machine-schema/src/catalog/coverage.rs:2112-2112](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L2112)

```text
effects AttentionPaused, AttentionResumed, AttentionStopped, AttentionSuperseded; invariants live_has_no_terminal_time, paused_has_pause_state, superseded_records_successor; Paused may have no deadline
```

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:236-247](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L236-L247)

```text
self.lifecycle_phase == Phase::Paused || self.paused_until_utc_ms == None
```

  The invariant prohibits a retained deadline outside Paused; it does not require a deadline within Paused.

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:256-275](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L256-L275)

```text
self.paused_until_utc_ms = until_utc_ms;
```

  Both pause transitions copy the optional deadline without requiring Some and emit AttentionPaused.

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:369-378](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L369-L378)

```text
self.paused_until_utc_ms == None
```

  The eligibility classifier intentionally handles indefinite pauses as ineligible.

- [specs/machines/work_attention_lifecycle/contract.md:170-170](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/work_attention_lifecycle/contract.md?plain=1#L170)

```text
Paused may have no deadline
```

- [specs/machines/work_attention_lifecycle/mapping.md:11-11](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/work_attention_lifecycle/mapping.md?plain=1#L11)

```text
Paused may have no deadline
```

### Final independent review

**PASS:** The corrected WorkAttention anchor names the four actual Attention-prefixed lifecycle effects and the three canonical invariants, and explicitly permits Paused without a deadline. This is supported by the optional domain input, both canonical pause transitions, and the explicit indefinite-pause eligibility branch; it is not a newly imposed behavior. Both generated occurrences already contain the corrected source literal. The source-to-projection check below verifies all fourteen changed lines across all six generated files for A18-001 through A18-003, with every other byte unchanged from baseline 56208b9e6cee078f52c43af8f6b36660bc93eeb4. The separate Rust check proves that the eight A18 catalog edits alter only the designated note, summary, description, and statement string contents, preserving all typed claims, identities, routes, invariant kinds, and surrounding code. The final on-disk outputs complete this correction.

- [meerkat-machine-schema/src/catalog/coverage.rs:2112-2112](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L2112)

```text
effects AttentionPaused, AttentionResumed, AttentionStopped, AttentionSuperseded; invariants live_has_no_terminal_time, paused_has_pause_state, superseded_records_successor; Paused may have no deadline
```

  This is the corrected documentary source literal. The adjacent CoverageClaims remain unchanged.

- [specs/machines/work_attention_lifecycle/contract.md:170-170](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/work_attention_lifecycle/contract.md?plain=1#L170)

```text
effects AttentionPaused, AttentionResumed, AttentionStopped, AttentionSuperseded; invariants live_has_no_terminal_time, paused_has_pause_state, superseded_records_successor; Paused may have no deadline
```

  The generated contract now carries the complete corrected note, rather than the obsolete mandatory-deadline vocabulary.

- [specs/machines/work_attention_lifecycle/mapping.md:11-11](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/work_attention_lifecycle/mapping.md?plain=1#L11)

```text
effects AttentionPaused, AttentionResumed, AttentionStopped, AttentionSuperseded; invariants live_has_no_terminal_time, paused_has_pause_state, superseded_records_successor; Paused may have no deadline
```

  The second generated occurrence agrees exactly with the source and contract; the mapping's typed transition/effect/invariant attribution is otherwise byte-identical to baseline.

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:236-247](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L236-L247)

```text
self.lifecycle_phase == Phase::Paused || self.paused_until_utc_ms == None
```

  The complete invariant block contains live_has_no_terminal_time, paused_has_pause_state, and superseded_records_successor. The quoted predicate forbids retained deadlines outside Paused, not absent deadlines within Paused.

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:256-275](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L256-L275)

```text
self.paused_until_utc_ms = until_utc_ms;
```

  PauseActive and PausePaused copy the optional input after checking lifecycle and revision, without requiring Some, and emit AttentionPaused.

- [meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs:369-378](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/work_attention_lifecycle.rs#L369-L378)

```text
self.paused_until_utc_ms == None
                    || self.paused_until_utc_ms.get("value") > now_utc_ms
```

  The canonical eligibility classifier deliberately handles an indefinite pause and emits eligible: false.

- [meerkat-workgraph/src/machine.rs:56-70](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-workgraph/src/machine.rs#L56-L70)

```text
until_utc_ms: until.map(datetime_to_millis)
```

  The domain adapter preserves optionality; there is no adapter-side mandatory deadline that could contradict the corrected note.

- [meerkat-workgraph/tests/attention_contracts.rs:2613-2626](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-workgraph/tests/attention_contracts.rs#L2613-L2626)

```text
WorkAttentionStatus::Paused { until: None }
```

  The existing contract test requests an indefinite pause and asserts that result. Inspected as corroborating source evidence, not executed by this reviewer.

- [meerkat-machine-schema/src/catalog/coverage.rs:76-85](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L76-L85)

```text
Documentation
    /// only — element attribution is the explicit typed [`Self::claims`]
    /// binding, never derived from this prose.
```

  The changed note is explicitly non-authoritative metadata.

- [meerkat-machine-schema/src/catalog/coverage.rs:3081-3099](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L3081-L3099)

```text
.filter(|anchor| claimed_by(&anchor.claims, name))
```

  The actual attribution implementation reads explicit claims; the scenario branch likewise reads scenario.claims, not note or summary text.

- [xtask/src/machines.rs:298-335](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/src/machines.rs#L298-L335)

```text
&render_machine_contract_markdown(&machine.schema, &machine.coverage)
```

  The canonical generator passes coverage to Markdown renderers, while model/config/kernel renderers consume the unchanged machine schema. No DSL or renderer edit was introduced.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import re, subprocess
base='56208b9e6cee078f52c43af8f6b36660bc93eeb4'
allowed={'meerkat-machine-schema/src/catalog/coverage.rs':{229,473,489,516,2112,2773},'meerkat-machine-schema/src/catalog/compositions.rs':{182,211}}
for f,expected in allowed.items():
    old=subprocess.check_output(['git','show',base+':'+f]).decode().splitlines(keepends=True)
    new=Path(f).read_text().splitlines(keepends=True)
    assert len(old)==len(new)
    changed={n for n,(a,b) in enumerate(zip(old,new),1) if a!=b}
    assert changed==expected
    for n in changed:
        a=re.fullmatch(r'(\s*(?:statement:\s*)?)"([^"\n]*)"(.*\n?)',old[n-1])
        b=re.fullmatch(r'(\s*(?:statement:\s*)?)"([^"\n]*)"(.*\n?)',new[n-1])
        assert a and b and a.group(1)==b.group(1) and a.group(3)==b.group(3)
        new[n-1]=b.group(1)+'"'+a.group(2)+'"'+b.group(3)
    assert old==new
    print(f+': only string contents changed at '+','.join(map(str,sorted(changed)))+'; all other bytes identical')
print('PASS: 8 documentary literals; typed claims, routes, kinds, IDs and other catalog data unchanged')
PY
```

  coverage.rs: only string contents changed at 229,473,489,516,2112,2773; compositions.rs: only string contents changed at 182,211. All other bytes identical. PASS: 8 documentary literals; typed claims, routes, kinds, IDs and other catalog data unchanged.

  This does not indiscriminately strip all Rust strings: it checks the exact eight allowed positions, identical nonliteral prefixes/suffixes, and restores only those literals to recover the complete baseline files. Typed identity and CoverageClaims strings therefore cannot hide behind normalization.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import json, subprocess
m=json.loads(Path('/Users/luka/.copilot/session-state/58aee1cf-9b35-434f-914f-4f2db67b816a/files/verify-manifest-V16.json').read_text())
base='56208b9e6cee078f52c43af8f6b36660bc93eeb4'
checks={}
for item in m['items']:
    for projection in item['fix'].get('expected_projections',[]):
        source=projection['source_field'].split()[0]
        source_path,source_line=source.rsplit(':',1)
        literal=Path(source_path).read_text().splitlines()[int(source_line)-1].split('"')[1]
        assert projection['expected_line'].endswith(' — '+literal)
        for target in projection['targets']:
            checks.setdefault(target['path'],{})[target['original_line']]=projection['expected_line']
for f,expected in checks.items():
    old=subprocess.check_output(['git','show',base+':'+f]).decode().splitlines(keepends=True)
    new=Path(f).read_text().splitlines(keepends=True)
    assert len(old)==len(new)
    assert {n for n,(a,b) in enumerate(zip(old,new),1) if a!=b}==set(expected)
    for n,line in expected.items():
        assert new[n-1]==line+'\n'
        old[n-1]=line+'\n'
    assert old==new
    print(f+': exact source projections at '+','.join(map(str,sorted(expected)))+'; remainder byte-identical')
print('PASS: '+str(sum(map(len,checks.values())))+' exact projections across '+str(len(checks))+' generated files')
PY
```

  PASS: 14 exact projections across 6 generated files. WorkAttention contract:170 and mapping:11; MeerkatMachine contract:25435,25446,25447,25450 and mapping:11,22,23,26; schedule_bundle contract:21,31,40 and mapping:16. Every remaining byte equals the baseline.

  All expectations were checked against the actual Rust literal and actual generated file, not accepted from the manifest. The unchanged Markdown portions include semantic tables, typed coverage attribution, generated markers, and handwritten mapping prefixes. This verifies that generation is present and that no stronger semantic claim was silently added to other generated sections.

## A18-002: MeerkatMachine coverage prose still describes the old realtime-intent and topology lifecycle

**Candidate severity:** medium. **Independent verdict:** confirm.

The schema-derived tables are current, but free-form coverage summaries retain the previous realtime intent/projection and topology-reconfiguration narrative. Their current typed claims point to MCP/context/interaction-stream handling, Reset and supervisor trust publication instead. Readers following the scenario prose are directed to a lifecycle no longer represented by that contract. This finding is narrowly about these descriptions and matching obsolete phrases in the long generated owner anchor; it does not claim that current LiveExecutionChannel, recovery/rebinding or transcript functionality is absent.

### Baseline documentation evidence

- [specs/machines/meerkat_machine/contract.md:25446-25446](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/meerkat_machine/contract.md?plain=1#L25446)

```text
project realtime intent, begin replace detach binding, require reattach, publish signal, reconnect progress
```

- [specs/machines/meerkat_machine/contract.md:25447-25447](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/meerkat_machine/contract.md?plain=1#L25447)

```text
realtime projection advance/refreshed/reset
```

- [specs/machines/meerkat_machine/contract.md:25450-25450](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/meerkat_machine/contract.md?plain=1#L25450)

```text
begin live topology reconfigure, mark detached, apply identity or visibility, complete/abort/fail topology
```

- [specs/machines/meerkat_machine/mapping.md:22-23](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/meerkat_machine/mapping.md?plain=1#L22-L23)

```text
project realtime intent, begin replace detach binding, require reattach, publish signal, reconnect progress
```

- [specs/machines/meerkat_machine/mapping.md:26-26](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/meerkat_machine/mapping.md?plain=1#L26)

```text
begin live topology reconfigure, mark detached, apply identity or visibility, complete/abort/fail topology
```

### Implementation proof

- [meerkat-machine-schema/src/catalog/coverage.rs:471-490](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L471-L490)

```text
CoverageClaims::none().transitions(&["Reset"])
```

  The current product_turn_streaming scenario claims only Reset. The preceding realtime_connection_projection scenario's complete typed transition set consists of McpServerConnected/Failed/Disconnected/Reload, AdvanceSessionContext and InteractionStream lifecycle transitions. Its old realtime-intent/binding/projection narrative is not the current coverage it declares.

- [meerkat-machine-schema/src/catalog/coverage.rs:514-518](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L514-L518)

```text
.effects(&["PublishSupervisorTrustEdge", "RevokeSupervisorTrustEdge"])
```

  The current live_topology_and_supervision scenario claims these two supervisor trust effects, not a begin/detach/apply/complete/abort topology protocol. The stale narrative is authored in this source, so it survives fresh generation.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import re
s=Path('meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs').read_text()
start=s.index('input MeerkatMachineInput {');end=s.index('\n        surface_only',start)
names=re.findall(r'^            ([A-Z]\w*)(?:\s*\{|\s*,)',s[start:end],re.M)
print('input_count=',len(names))
print('old_intent_or_topology_inputs=',[n for n in names if 'Realtime' in n or 'Topology' in n])
print('current_open_channel_inputs=',[n for n in names if n in ['ResolveLiveOpenAdmission','BindLiveExecutionChannel','AbandonLiveOpenAdmission']])
PY
```

  input_count= 376
  old_intent_or_topology_inputs= []
  current_open_channel_inputs= ['ResolveLiveOpenAdmission', 'BindLiveExecutionChannel', 'AbandonLiveOpenAdmission']

  This scopes the negative evidence to the complete canonical input declaration, not a repository-wide failed search. Independently, the full source-to-contract pass checked all 376 inputs, 432 state fields, 234 effects and 2,419 expanded transitions. The old-named lifecycle is not part of the current machine alphabet; the positive current channel authority is retained and was inspected directly.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:5429-5441](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L5429-L5441)

```text
ResolveLiveOpenAdmission {
                session_id: String,
                channel_id: String,
                llm_identity: SessionLlmIdentity,
            },
```

  The current vocabulary explicitly admits a live channel, followed by BindLiveExecutionChannel carrying runtime_id, fence_token, generation and canonical_seed_cursor. This is positive evidence of the present contract, not a claim that all live functionality was removed.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:24001-24023](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L24001-L24023)

```text
transition BindLiveExecutionChannel {
```

  The implemented current transition binds an admitted channel to an exact runtime incarnation and fences callbacks. The audit read its guards and updates rather than treating transport-host implementation mechanics as competing authority.

### Independent challenge

Confirmed with a narrow scope: these summaries advertise the obsolete realtime-intent/projection and topology-reconfiguration sequences, not the coverage currently declared by those scenarios. I did not treat a small typed claim set as exhaustive proof that every other behavior is absent. Instead I checked the full canonical input declaration, concrete current MCP/context/stream/reset/supervisor transitions, and the live-open runtime entry into generated authority. Current live admission, incarnation-fenced execution binding, recovery, transcript handling, and model-routing topology facts remain real; a blanket removal of realtime/live/topology terminology would be wrong. Stable scenario IDs can remain. Only their misleading prose and the matching obsolete lifecycle descriptions in the owner anchor need correction.

- [meerkat-machine-schema/src/catalog/coverage.rs:471-490](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L471-L490)

```text
realtime projection advance/refreshed/reset
```

  The realtime_connection_projection summary advertises intent/binding work, but its explicit claims are MCP-server, session-context, and interaction-stream transitions. The product_turn_streaming summary contains this quoted projection lifecycle, while its only transition claim is Reset. These are the source literals reproduced at contract.md:25446-25447 and mapping.md:22-23.

- [meerkat-machine-schema/src/catalog/coverage.rs:514-518](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L514-L518)

```text
.effects(&["PublishSupervisorTrustEdge", "RevokeSupervisorTrustEdge"])
```

  The third affected scenario declares these supervisor-trust effects, while its prose claims a begin/detach/apply/complete/abort/fail live-topology workflow. The source owner anchor at line 229 also retains realtime-binding/live-topology wording.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import re
s = Path('meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs').read_text()
a = s.index('input MeerkatMachineInput {')
b = s.index('\n        surface_only', a)
names = re.findall(r'^            ([A-Z]\w*)(?:\s*\{|\s*,)', s[a:b], re.M)
print('inputs:', len(names))
print('Realtime/Topology input names:', [n for n in names if 'Realtime' in n or 'Topology' in n])
print('current live admission/binding inputs:', [n for n in names if n in {'ResolveLiveOpenAdmission', 'BindLiveExecutionChannel', 'AbandonLiveOpenAdmission'}])
PY
```

  inputs: 376
  Realtime/Topology input names: []
  current live admission/binding inputs: ['ResolveLiveOpenAdmission', 'BindLiveExecutionChannel', 'AbandonLiveOpenAdmission']

  Independently executed, read-only enumeration of the complete canonical input declaration at lines 4582-6396, not a failed repository-wide search. This proves absence of that old-named input vocabulary, not absence of all realtime-related effects, fields, or behavior.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:13086-13103](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L13086-L13103)

```text
emit RuntimeNotice { kind: RuntimeNoticeKind::Reset, detail: "runtime reset" }
```

  Reset clears the current run/fence/pre-run and stop/silent-intent fields, returns to Idle, and emits a runtime-reset notice. It is not a realtime projection advance/refreshed/reset lifecycle.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:29986-30032](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L29986-L30032)

```text
transition AdvanceSessionContext
```

  The concrete session-context and interaction-stream transitions update their own machine fields and emit SessionContextAdvanced/InteractionStreamStateChanged. McpServerConnected at lines 29787-29796 updates mcp_server_states. These positively corroborate the current scope of the first scenario.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:30866-30905](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L30866-L30905)

```text
emit PublishSupervisorTrustEdge
```

  BindSupervisor records the supervisor binding and pending publication facts before emitting the claimed trust effect. This is a concrete current supervisor lifecycle, not evidence of the removed topology-reconfiguration sequence.

- [meerkat-runtime/src/meerkat_machine/runtime_control.rs:11187-11233](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/runtime_control.rs#L11187-L11233)

```text
crate::meerkat_machine::dsl::MeerkatMachineInput::ResolveLiveOpenAdmission
```

  The feature-gated runtime method really submits the current typed live-open input and constructs its result from LiveOpenAdmissionResolved. Current live behavior must not be removed or replaced to make the old summaries true.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:24001-24045](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L24001-L24045)

```text
guard "generation_binding_matches" { self.active_runtime_generation == Some(generation) }
```

  BindLiveExecutionChannel verifies channel, runtime, fence, and generation and records an Active execution channel. This is positive source proof that current live binding remains present despite removal of the old intent/topology alphabet.

- [meerkat-machine-schema/src/catalog/coverage.rs:3081-3099](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L3081-L3099)

```text
.filter(|scenario| claimed_by(&scenario.claims, name))
```

  Scenario summaries and anchor notes are not parsed to derive coverage semantics. Editing summary/note literals while preserving claims and IDs leaves attribution unchanged.

- [xtask/src/machines.rs:298-335](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/xtask/src/machines.rs#L298-L335)

```text
&render_machine_mapping_coverage(&machine.schema, &machine.coverage)
```

  Coverage is a Markdown input, separate from the schema-only kernel and semantic-model inputs. artifacts.rs:1084-1097 and render.rs:285-293 print scenario.summary verbatim; this makes regeneration necessary but no product/formal change necessary.

**Required correction:** In meerkat-machine-schema/src/catalog/coverage.rs, correct the meerkat_machine anchor note and the summary literals for realtime_connection_projection, product_turn_streaming, and live_topology_and_supervision. Describe the existing MCP/context/interaction-stream coverage, runtime Reset, and supervisor-trust effects without the obsolete realtime-intent/projection or begin/detach/apply/complete/abort/fail topology sequence. Preserve scenario/anchor IDs and CoverageClaims; do not add new live coverage claims merely to rescue the old wording. Preserve valid current live-channel, incarnation/recovery, transcript, and model-routing semantics. Regenerate specs/machines/meerkat_machine/contract.md and specs/machines/meerkat_machine/mapping.md. The unchanged dependency chain is xtask/src/machines.rs::machine_codegen_at_root -> meerkat-machine-codegen/src/artifacts.rs::render_machine_contract_markdown and meerkat-machine-codegen/src/render.rs::render_machine_mapping_coverage/merge_mapping_document. Only documentary metadata and its Markdown projections need change; generated kernels, TLA behavior, and runtime source do not.

### Correction

Removed the old realtime-binding/live-topology lifecycle descriptions from the meerkat_machine owner note. Narrowed the three scenario summaries to their existing MCP/context/interaction-stream transitions, runtime Reset, and supervisor-trust effects. Stable anchor/scenario IDs and every typed CoverageClaims field remain identical. No new live claims were added; live admission, incarnation/recovery, transcript, and model-routing semantics were not edited. Parent completed canonical make machine-codegen; corrected documentary metadata is now present in both generated contract and mapping projections.

- [meerkat-machine-schema/src/catalog/coverage.rs:229-229](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L229)

```text
MCP server lifecycle, peer ready operation, peer request, peer response, peer ingress, peer endpoint projection, session context advancement, interaction stream, product turn, ingress, supervisor trust-edge publication/revocation
```

- [meerkat-machine-schema/src/catalog/coverage.rs:471-490](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L471-L490)

```text
runtime Reset from Initializing, Idle, Attached, or Retired returns to Idle and emits the Reset runtime notice
```

  The adjoining summaries now describe the unchanged Attached-phase MCP/context/stream claims and Reset claim.

- [meerkat-machine-schema/src/catalog/coverage.rs:514-518](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L514-L518)

```text
supervisor trust-edge publication and revocation through PublishSupervisorTrustEdge and RevokeSupervisorTrustEdge
```

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:13086-13103](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L13086-L13103)

```text
emit RuntimeNotice { kind: RuntimeNoticeKind::Reset, detail: "runtime reset" }
```

  Reset admits Initializing, Idle, Attached, and Retired and targets Idle; this is the current positive contract, not a replacement realtime projection lifecycle.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:29787-29833](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L29787-L29833)

```text
self.mcp_server_states.insert(server_id, McpServerState::Connected);
```

  Current per-phase MCP connection, failure, disconnection, and reload transitions include Attached.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:29986-30069](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L29986-L30069)

```text
transition AdvanceSessionContext
```

  The actual context and reservation/attachment/completion/expiry/early-closure transitions establish the narrowed scenario scope.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:30866-30905](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L30866-L30905)

```text
emit PublishSupervisorTrustEdge
```

  BindSupervisor establishes pending trust-publication facts and emits the claimed current trust effect.

- [specs/machines/meerkat_machine/contract.md:25447-25447](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/meerkat_machine/contract.md?plain=1#L25447)

```text
runtime Reset from Initializing
```

- [specs/machines/meerkat_machine/mapping.md:23-23](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/meerkat_machine/mapping.md?plain=1#L23)

```text
runtime Reset from Initializing
```

### Final independent review

**PASS:** The MeerkatMachine anchor and all three affected summaries now describe exactly the existing narrowed coverage: Attached-phase MCP outcomes/context/interaction-stream handling, runtime Reset, and supervisor trust-edge publication/revocation. The obsolete realtime-intent/projection and live-topology reconfiguration sequences are gone from source and both generated projections. Stable scenario IDs and every typed CoverageClaims entry remain unchanged. This does not claim that all live or topology-related behavior disappeared: the complete canonical input declaration still contains the current live-open/bind/abandon authority, and the incarnation-fenced binding/runtime ingress remain intact. The generated contract and mapping already agree with all four corrected source literals.

- [meerkat-machine-schema/src/catalog/coverage.rs:229-229](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L229)

```text
runtime Reset, stop/stopped executor, destroy/destroyed, ensure executor, runtime notice, silent intents, recycle, MCP server lifecycle
```

  The owner note no longer advertises the retired realtime-binding lifecycle. Its live-topology phrase is replaced by accurate supervisor trust-edge wording without renaming the anchor or changing claims.

- [meerkat-machine-schema/src/catalog/coverage.rs:471-490](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L471-L490)

```text
MCP server connection outcomes and reload, session context advancement, and interaction stream reservation, attachment, completion, expiry, and early closure in the Attached runtime phase
```

  The ten preserved claims immediately below are exactly the Attached MCP/context/interaction-stream transitions. The next scenario retains only Reset and now describes runtime Reset rather than a realtime projection lifecycle.

- [meerkat-machine-schema/src/catalog/coverage.rs:514-518](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L514-L518)

```text
supervisor trust-edge publication and revocation through PublishSupervisorTrustEdge and RevokeSupervisorTrustEdge
```

  The preserved live_topology_and_supervision ID now has a summary matching its existing two effect claims.

- [specs/machines/meerkat_machine/contract.md:25435-25435](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/meerkat_machine/contract.md?plain=1#L25435)

```text
peer endpoint projection, session context advancement, interaction stream, product turn, ingress, supervisor trust-edge publication/revocation
```

  The corrected owner note is already present in the generated contract.

- [specs/machines/meerkat_machine/contract.md:25446-25450](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/meerkat_machine/contract.md?plain=1#L25446-L25450)

```text
- `product_turn_streaming` — runtime Reset from Initializing, Idle, Attached, or Retired returns to Idle and emits the Reset runtime notice
```

  All three corrected scenario rows are present at 25446, 25447, and 25450; their exact equality with source is covered by the fourteen-projection check in A18-001.

- [specs/machines/meerkat_machine/mapping.md:22-26](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/meerkat_machine/mapping.md?plain=1#L22-L26)

```text
- `live_topology_and_supervision` — supervisor trust-edge publication and revocation through PublishSupervisorTrustEdge and RevokeSupervisorTrustEdge
```

  The mapping has the same three corrected scenario rows at 22, 23, and 26, plus the corrected owner note at 11. Typed attribution below those rows is byte-identical to baseline.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:13087-13103](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L13087-L13103)

```text
emit RuntimeNotice { kind: RuntimeNoticeKind::Reset, detail: "runtime reset" }
```

  The actual Reset guard admits Initializing, Idle, Attached, or Retired; the transition clears runtime fields, enters Idle, and emits this notice.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:29787-29832](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L29787-L29832)

```text
per_phase [Idle, Attached, Running, Retired, Stopped]
```

  McpServerConnected, Failed, Disconnected, and Reload include the Attached variants claimed by this scenario. The corrected description describes that claimed subset, not the full alphabet.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:29986-30070](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L29986-L30070)

```text
emit SessionContextAdvanced { updated_at_ms: updated_at_ms }
```

  AdvanceSessionContext and the adjacent Reserved/Attached/Completed/Expired/ClosedEarly interaction-stream transitions corroborate the remaining scenario summary.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:30866-30905](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L30866-L30905)

```text
emit PublishSupervisorTrustEdge {
```

  BindSupervisor records the machine-owned binding and publication obligation before emitting the first claimed trust effect.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:31823-31872](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L31823-L31872)

```text
emit RevokeSupervisorTrustEdge {
```

  The current peer/epoch-guarded RevokeSupervisor transition emits the second claimed effect; the repair does not invent a topology protocol.

- [meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs:24001-24045](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs#L24001-L24045)

```text
guard "generation_binding_matches" { self.active_runtime_generation == Some(generation) }
```

  Current BindLiveExecutionChannel still checks session/channel, runtime, fence, and generation and records an active channel. Only obsolete coverage prose was removed.

- [meerkat-runtime/src/meerkat_machine/runtime_control.rs:11187-11233](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/meerkat_machine/runtime_control.rs#L11187-L11233)

```text
crate::meerkat_machine::dsl::MeerkatMachineInput::ResolveLiveOpenAdmission
```

  The production live-open method still enters canonical authority and builds its result from the generated LiveOpenAdmissionResolved effect. No runtime source was altered.

- Read-only reproduction:

```text
python3 - <<'PY'
import re
from pathlib import Path
s=Path('meerkat-machine-schema/src/catalog/dsl/meerkat_machine.rs').read_text()
a=s.index('input MeerkatMachineInput {'); b=s.index('\n        surface_only',a)
names=re.findall(r'^            ([A-Z]\w*)(?:\s*\{|\s*,)',s[a:b],re.M)
c=Path('specs/machines/meerkat_machine/contract.md').read_text()
a=c.index('## Inputs\n'); b=c.index('\n## Signals',a)
projected=re.findall(r'^- `([^`]+)`',c[a:b],re.M)
assert len(names)==len(set(names)) and set(names)==set(projected)
print('canonical inputs:',len(names),'generated classification rows:',len(projected),'same unique alphabet')
print('Realtime/Topology input names:',[n for n in names if 'Realtime' in n or 'Topology' in n])
current=['ResolveLiveOpenAdmission','BindLiveExecutionChannel','AbandonLiveOpenAdmission']
assert all(n in names for n in current)
print('preserved current live inputs:',current)
retired=['project realtime intent','begin replace detach binding','realtime projection advance/refreshed/reset','begin live topology reconfigure','complete/abort/fail topology']
for f in ['meerkat-machine-schema/src/catalog/coverage.rs','specs/machines/meerkat_machine/contract.md','specs/machines/meerkat_machine/mapping.md']:
    assert not any(t in Path(f).read_text() for t in retired)
print('retired prose absent from source and both projections')
PY
```

  376 unique canonical inputs match 385 generated input/classification rows, whose extra rows repeat classifications rather than add inputs. Realtime/Topology input names: []. ResolveLiveOpenAdmission, BindLiveExecutionChannel, and AbandonLiveOpenAdmission remain present. All five obsolete prose fragments are absent from the source and both projections.

  The negative alphabet evidence enumerates the complete canonical input declaration rather than relying on a failed repository search. Current live semantics are positively identified and preserved.

## A18-003: Schedule composition summaries omit overdue and in-flight supersession

**Candidate severity:** medium. **Independent verdict:** confirm.

The transaction and coverage summaries still describe the earlier pending-future subset without telling readers that revision changes and deletion also revoke already-due and in-flight nonterminal occurrences. That omission is material to safe schedule updates and interpreting completion races. The current machine explicitly broadened the operation while deliberately retaining its historical effect name. This is not a request to rename that effect or reinterpret the shell as lifecycle authority.

### Baseline documentation evidence

- [specs/compositions/schedule_bundle/contract.md:21-21](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/compositions/schedule_bundle/contract.md?plain=1#L21)

```text
revision-affecting schedule updates supersede pending future occurrences before replanning
```

- [specs/compositions/schedule_bundle/contract.md:31-31](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/compositions/schedule_bundle/contract.md?plain=1#L31)

```text
pending future occurrences are superseded only by the schedule revision route rather than by ad hoc shell mutation
```

- [specs/compositions/schedule_bundle/contract.md:40-40](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/compositions/schedule_bundle/contract.md?plain=1#L40)

```text
revision-affecting schedule updates supersede pending future occurrences through the explicit route
```

- [specs/compositions/schedule_bundle/mapping.md:16-16](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/compositions/schedule_bundle/mapping.md?plain=1#L16)

```text
revision-affecting schedule updates supersede pending future occurrences through the explicit route
```

### Implementation proof

- [meerkat-machine-schema/src/catalog/dsl/schedule_lifecycle.rs:93-108](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/schedule_lifecycle.rs#L93-L108)

```text
// ALL outstanding (non-terminal) occurrences of the schedule —
```

  The canonical effect documentation explicitly includes Pending, Claimed, Dispatching and AwaitingCompletion, revokes in-flight claims at revision-affecting commit time, and explains that the retained SupersedePendingOccurrences name is historical rather than a scope limit.

- [meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs:1835-1854](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs#L1835-L1854)

```text
self.lifecycle_phase == Phase::Pending
                || self.lifecycle_phase == Phase::Claimed
                || self.lifecycle_phase == Phase::Dispatching
                || self.lifecycle_phase == Phase::AwaitingCompletion
```

  The actual SupersedePendingOrLive guard accepts every listed nonterminal phase without a future-due-time condition. Its update records supersession and emits the reciprocal acknowledgement, establishing the broader behavior independently of comments.

- [meerkat-schedule/src/service.rs:1683-1699](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/src/service.rs#L1683-L1699)

```text
"delete must supersede the driver-claimed in-flight occurrence at commit time"
```

  The existing service regression test claims a due occurrence, deletes its schedule, asserts Superseded and verifies superseded_ack_ids. It also asserts no live occurrence remains. The test was inspected, not executed.

- [meerkat-machine-schema/src/catalog/compositions.rs:182-183](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/compositions.rs#L182-L183)

```text
"revision-affecting schedule updates supersede pending future occurrences before replanning",
```

  This composition transaction-plan description is the source of the narrowed generated contract wording.

- [meerkat-machine-schema/src/catalog/compositions.rs:211-211](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/compositions.rs#L211)

```text
statement: "pending future occurrences are superseded only by the schedule revision route rather than by ad hoc shell mutation".into(),
```

  The same incomplete scope is authored in the behavioral invariant statement.

- [meerkat-machine-schema/src/catalog/coverage.rs:2771-2774](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L2771-L2774)

```text
"revision-affecting schedule updates supersede pending future occurrences through the explicit route",
```

  This source scenario emits the repeated incomplete description in both the contract and mapping.

### Independent challenge

Confirmed as a material scope omission, with two important restrictions. The sweep targets nonterminal occurrences of older schedule revisions, not every row or newly planned current-revision work; it includes overdue Pending and Claimed/Dispatching/AwaitingCompletion. Both memory and SQLite commit paths already implement this through occurrence authority. Also, the sentence printed under Behavioral Invariants is explanatory text, not the invariant predicate: the actual kind is ObservedRouteInputOriginatesFromEffect, whose formal condition is scoped to inputs observed on the named route. Do not turn this documentation repair into a stronger all-input provenance or sweep-completeness invariant. Update the transaction/scenario descriptions to the existing sweep, and replace the invariant sentence with accurate route-scoped provenance wording. Supersession is not a promise to cancel already-dispatched external work.

- [meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs:1835-1854](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs#L1835-L1854)

```text
transition SupersedePendingOrLive
```

  Its guard admits Pending, Claimed, Dispatching, and AwaitingCompletion with no due-time predicate. It records supersession, enters Superseded, and emits both Superseded and OccurrencesSuperseded. Thus the advertised pending-future subset is not the current operation's full scope.

- [meerkat-schedule/src/store.rs:964-991](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-schedule/src/store.rs#L964-L991)

```text
&& occurrence.schedule_revision < supersession.superseded_by_revision()
```

  The memory store's commit-time sweep selects the schedule's nonterminal older-revision rows, with no future-due or Pending-only filter, then applies typed Supersede. This supplies the exact revision qualifier missing from an unqualified 'all occurrences' repair.

- [meerkat-store/src/schedule_sqlite_store.rs:2873-2918](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-store/src/schedule_sqlite_store.rs#L2873-L2918)

```text
if occurrence.is_terminal()
            || occurrence.schedule_revision >= supersession.superseded_by_revision()
```

  The persistent backend independently has the same scope. Its query filters by schedule_id, not due time; its only row exclusions here are terminality and non-older revision. It invokes OccurrenceLifecycleInput::Supersede and persists the authority-derived receipt and row.

- [meerkat-store/src/schedule_sqlite_store.rs:1079-1116](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-store/src/schedule_sqlite_store.rs#L1079-L1116)

```text
committed_schedule = apply_supersession_feedback(committed_schedule, acks)
```

  The schedule mutation, outstanding-occurrence sweep, reciprocal acknowledgements, and updated schedule are all handled within the same transaction before tx.commit(). This is already current commit behavior, not something to implement for the docs.

- [meerkat-machine-schema/src/catalog/compositions.rs:178-214](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/compositions.rs#L178-L214)

```text
statement: "pending future occurrences are superseded only by the schedule revision route rather than by ad hoc shell mutation".into()
```

  The misleading strings are authored here: transaction description at line 182 and this invariant statement at line 211. The invariant's separate kind is ObservedRouteInputOriginatesFromEffect with a specific route_name; changing that kind would be a semantic change and is not required.

- [meerkat-machine-schema/src/catalog/coverage.rs:2771-2775](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/catalog/coverage.rs#L2771-L2775)

```text
revision-affecting schedule updates supersede pending future occurrences through the explicit route
```

  This independent source summary supplies the repeated scenario text in both generated documents. A composition statement/description edit alone would leave these two occurrences unfixed.

- [meerkat-machine-codegen/src/artifacts.rs:6193-6201](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/src/artifacts.rs#L6193-L6201)

```text
render_composition_invariant_formula(self.schema, &invariant.kind)
```

  The executable semantic-model generator derives the invariant formula from kind, not statement. In contrast, the Markdown renderer at lines 1214-1264 prints plan.description and invariant.statement. This is the decisive separation between a generated documentary string and a semantic invariant.

- [meerkat-machine-codegen/src/artifacts.rs:12036-12056](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/src/artifacts.rs#L12036-L12056)

```text
input_packet.source_route = {}
```

  The ObservedRouteInputOriginatesFromEffect formula conditions its provenance check on this named source route as well as the machine/input variant. It does not state that every Supersede input is on that route or quantify over all nonterminal occurrence rows. The repaired prose must not claim those stronger formal guarantees.

- [meerkat-machine-schema/src/composition.rs:719-733](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-schema/src/composition.rs#L719-L733)

```text
pub description: String
```

  The transaction description is distinct from the typed name, trigger, store primitive, routes, and protocols. The proposed edit is solely this human-readable field, not the plan's operative membership.

- [xtask/src/machines.rs:354-404](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/xtask/src/machines.rs#L354-L404)

```text
&render_composition_contract_markdown(&composition.schema, &composition.coverage)
```

  The main generation path writes the semantic model from the schema, renders the contract from schema plus coverage, and merges the coverage mapping. schedule_bundle declares driver: None (catalog/compositions.rs:171), so no production driver is emitted for it. Unchanged kinds/routes/DSL plus changed documentary literals require changes only in the two Markdown outputs.

**Required correction:** Change the revision_supersede_and_replan description literal and superseded_occurrence_originates_from_schedule_revision.statement literal in meerkat-machine-schema/src/catalog/compositions.rs, plus the revision-supersede-route summary literal in meerkat-machine-schema/src/catalog/coverage.rs. The transaction/scenario prose should explain commit-time supersession of all outstanding nonterminal occurrences from older revisions, including overdue Pending and in-flight Claimed/Dispatching/AwaitingCompletion, through typed occurrence Supersede and reciprocal acknowledgement. For the invariant statement, use the narrower accurate meaning: observed Supersede inputs delivered on revision_supersede_enters_occurrence_authority originate from schedule.SupersedePendingOccurrences. Do not imply that this formal invariant proves sweep completeness or forbids every other typed Supersede ingress; do not promise cancellation of external work. Keep the historical effect name, CompositionInvariantKind and its fields, routes, references, transaction identities, DSL, witnesses, and claims unchanged. Regenerate specs/compositions/schedule_bundle/contract.md and specs/compositions/schedule_bundle/mapping.md via xtask/src/machines.rs::machine_codegen_at_root using the unchanged meerkat-machine-codegen/src/artifacts.rs::render_composition_contract_markdown and meerkat-machine-codegen/src/render.rs::render_composition_mapping_coverage/merge_mapping_document. These catalog entries are Rust-hosted documentary strings: description/statement/summary edits do not change runtime or executable TLA semantics. The diagnostic render_composition_module also prints statement text, but it is not the checked-in semantic-model emission path and needs no renderer edit.

### Correction

Corrected the transaction description and scenario summary to the existing commit-time sweep of the schedule's outstanding nonterminal older-revision occurrences, including overdue Pending and in-flight Claimed/Dispatching/AwaitingCompletion, through typed Supersede and reciprocal acknowledgement. Explicitly avoided promising cancellation of already-dispatched external work. Separately narrowed the invariant statement to provenance of observed Supersede inputs on revision_supersede_enters_occurrence_authority from schedule.SupersedePendingOccurrences; it explicitly does not prove sweep completeness or exclude other typed Supersede ingress. The historical effect name, invariant kind and fields, route/transaction identities, references, witnesses, claims, DSL, and runtime are unchanged. Parent completed canonical make machine-codegen; corrected documentary metadata is now present in both generated contract and mapping projections.

- [meerkat-machine-schema/src/catalog/compositions.rs:182-182](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/compositions.rs#L182)

```text
all outstanding nonterminal occurrences of the schedule from older revisions at commit time, including overdue Pending and in-flight Claimed/Dispatching/AwaitingCompletion
```

- [meerkat-machine-schema/src/catalog/compositions.rs:211-211](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/compositions.rs#L211)

```text
observed occurrence.Supersede inputs delivered on revision_supersede_enters_occurrence_authority originate from schedule.SupersedePendingOccurrences; this route-scoped provenance invariant neither proves sweep completeness nor excludes other typed Supersede ingress
```

- [meerkat-machine-schema/src/catalog/coverage.rs:2773-2773](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L2773)

```text
through typed occurrence Supersede and reciprocal OccurrencesSuperseded -> ConfirmOccurrencesSuperseded acknowledgement; supersession does not promise cancellation of already-dispatched external work
```

- [meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs:1835-1854](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs#L1835-L1854)

```text
emit OccurrencesSuperseded { occurrence_id: self.occurrence_id, superseding_revision: superseded_by_revision }
```

  SupersedePendingOrLive accepts Pending, Claimed, Dispatching, and AwaitingCompletion without a due-time condition and emits reciprocal acknowledgement.

- [meerkat-schedule/src/store.rs:964-991](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-schedule/src/store.rs#L964-L991)

```text
&& occurrence.schedule_revision < supersession.superseded_by_revision()
```

  The in-memory commit selects this schedule's nonterminal older-revision rows, including overdue rows and in-flight phases, then applies typed Supersede.

- [meerkat-store/src/schedule_sqlite_store.rs:2873-2918](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-store/src/schedule_sqlite_store.rs#L2873-L2918)

```text
|| occurrence.schedule_revision >= supersession.superseded_by_revision()
```

  SQLite excludes terminal or non-older rows and applies typed Supersede to the remaining schedule rows.

- [meerkat-store/src/schedule_sqlite_store.rs:1079-1116](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-store/src/schedule_sqlite_store.rs#L1079-L1116)

```text
committed_schedule = apply_supersession_feedback(committed_schedule, acks)
```

  The occurrence sweep and reciprocal feedback are applied inside the same transaction before commit.

- [meerkat-machine-codegen/src/artifacts.rs:12036-12056](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-codegen/src/artifacts.rs#L12036-L12056)

```text
input_packet.source_route = {}
```

  ObservedRouteInputOriginatesFromEffect checks inputs observed on the named route; it is not an all-ingress provenance or sweep-completeness formula.

- [meerkat-machine-codegen/src/artifacts.rs:6193-6201](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-codegen/src/artifacts.rs#L6193-L6201)

```text
render_composition_invariant_formula(self.schema, &invariant.kind)
```

  The executable model derives the formula from the unchanged kind, not the edited documentary statement.

- [specs/compositions/schedule_bundle/contract.md:21-21](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/schedule_bundle/contract.md?plain=1#L21)

```text
all outstanding nonterminal occurrences
```

- [specs/compositions/schedule_bundle/mapping.md:16-16](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/schedule_bundle/mapping.md?plain=1#L16)

```text
all outstanding nonterminal occurrences
```

### Final independent review

**PASS:** The transaction description and scenario summary now describe the actual commit-time sweep of the schedule's nonterminal older-revision occurrences, including overdue Pending and Claimed/Dispatching/AwaitingCompletion, deletion, typed Supersede, and reciprocal acknowledgement. They do not sweep terminal/current-revision rows or promise cancellation of already-dispatched external work. Separately, the behavioral-invariant statement accurately states only the named route's observed-input provenance; it explicitly disclaims sweep completeness and exclusion of other typed Supersede ingress. The stable historical effect name, invariant kind and fields, routes, transaction identities, witnesses, and claims are unchanged. The schedule contract's three corrected rows and mapping's corrected scenario are already generated. Actual TLA formulas still derive from the invariant kind, not this explanatory statement; formal artifacts and supporting runtime code are unchanged.

- [meerkat-machine-schema/src/catalog/compositions.rs:179-185](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/compositions.rs#L179-L185)

```text
revision-affecting schedule updates (including deletion) supersede all outstanding nonterminal occurrences of the schedule from older revisions at commit time, including overdue Pending and in-flight Claimed/Dispatching/AwaitingCompletion, through typed occurrence Supersede and reciprocal OccurrencesSuperseded -> ConfirmOccurrencesSuperseded acknowledgement; supersession does not promise cancellation of already-dispatched external work
```

  Only the transaction's description string changed. Its identity, trigger, store primitive, and route membership did not.

- [meerkat-machine-schema/src/catalog/compositions.rs:201-213](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/compositions.rs#L201-L213)

```text
observed occurrence.Supersede inputs delivered on revision_supersede_enters_occurrence_authority originate from schedule.SupersedePendingOccurrences; this route-scoped provenance invariant neither proves sweep completeness nor excludes other typed Supersede ingress
```

  This is the corrected statement beside the unchanged ObservedRouteInputOriginatesFromEffect kind and its exact route/machine/input/effect identifiers.

- [meerkat-machine-schema/src/catalog/coverage.rs:2771-2775](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/coverage.rs#L2771-L2775)

```text
including overdue Pending and in-flight Claimed/Dispatching/AwaitingCompletion, through typed occurrence Supersede and reciprocal OccurrencesSuperseded -> ConfirmOccurrencesSuperseded acknowledgement
```

  The separate scenario source was also corrected; its revision-supersede-route identity and CoverageClaims::none() remain unchanged.

- [specs/compositions/schedule_bundle/contract.md:21-21](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/schedule_bundle/contract.md?plain=1#L21)

```text
supersede all outstanding nonterminal occurrences of the schedule from older revisions at commit time
```

  The generated transaction row contains the complete qualified sweep description and the external-cancellation disclaimer.

- [specs/compositions/schedule_bundle/contract.md:31-31](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/schedule_bundle/contract.md?plain=1#L31)

```text
this route-scoped provenance invariant neither proves sweep completeness nor excludes other typed Supersede ingress
```

  The generated behavioral-invariant row preserves the narrower formal meaning instead of laundering the stronger operational sweep into a formal guarantee.

- [specs/compositions/schedule_bundle/contract.md:40-40](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/schedule_bundle/contract.md?plain=1#L40)

```text
through typed occurrence Supersede and reciprocal OccurrencesSuperseded -> ConfirmOccurrencesSuperseded acknowledgement; supersession does not promise cancellation of already-dispatched external work
```

  The generated scenario reflects the independent corrected coverage summary.

- [specs/compositions/schedule_bundle/mapping.md:16-16](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/schedule_bundle/mapping.md?plain=1#L16)

```text
including overdue Pending and in-flight Claimed/Dispatching/AwaitingCompletion
```

  The fourth affected generated occurrence is fixed. Its complete line is identical to the source summary, as verified by the cross-projection assertions in A18-001.

- [meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs:1835-1854](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs#L1835-L1854)

```text
self.lifecycle_phase == Phase::Pending
                || self.lifecycle_phase == Phase::Claimed
                || self.lifecycle_phase == Phase::Dispatching
                || self.lifecycle_phase == Phase::AwaitingCompletion
```

  SupersedePendingOrLive accepts precisely the listed live phases, with no future-due-time predicate, enters Superseded, and emits Superseded plus OccurrencesSuperseded.

- [meerkat-schedule/src/store.rs:964-991](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-schedule/src/store.rs#L964-L991)

```text
occurrence.schedule_id == committed_schedule.schedule_id
                        && !occurrence.is_terminal()
                        && occurrence.schedule_revision < supersession.superseded_by_revision()
```

  The memory store selects all nonterminal older-revision rows for this schedule, regardless of due time, and drives the typed occurrence Supersede input.

- [meerkat-store/src/schedule_sqlite_store.rs:2873-2918](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-store/src/schedule_sqlite_store.rs#L2873-L2918)

```text
if occurrence.is_terminal()
            || occurrence.schedule_revision >= supersession.superseded_by_revision()
```

  The persistent backend's schedule-scoped query has the same exclusions and no future-time or Pending-only restriction. Each selected row passes through occurrence authority before its receipt and row are written.

- [meerkat-store/src/schedule_sqlite_store.rs:1079-1116](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-store/src/schedule_sqlite_store.rs#L1079-L1116)

```text
committed_schedule = apply_supersession_feedback(committed_schedule, acks)
```

  The outstanding-row sweep, reciprocal feedback, and final schedule write occur in the same transaction before tx.commit(), supporting the commit-time and acknowledgement wording.

- [meerkat-machine-schema/src/catalog/compositions.rs:138-168](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/compositions.rs#L138-L168)

```text
"occurrence_supersede_ack_returns_to_schedule"
```

  The unchanged route pair carries SupersedePendingOccurrences to occurrence.Supersede and OccurrencesSuperseded back to schedule.ConfirmOccurrencesSuperseded.

- [meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs:1869-1882](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-schema/src/catalog/dsl/occurrence_lifecycle.rs#L1869-L1882)

```text
The completion
        // waiter spawned for the dispatched delivery cannot be quiesced by
        // that commit
```

  The canonical late-arrival model explains why supersession must not be documented as cancelling already-dispatched external work.

- [specs/compositions/schedule_bundle/model.tla:5605-5605](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/schedule_bundle/model.tla#L5605)

```text
input_packet.machine = "occurrence" /\ input_packet.variant = "Supersede" /\ input_packet.source_route = "revision_supersede_enters_occurrence_authority"
```

  The actual generated antecedent is restricted to observed packets on the named route. Its consequent proves matching source effect/route payload provenance, not existence or coverage of every eligible occurrence and not exclusion of other input routes.

- [meerkat-machine-codegen/src/artifacts.rs:6193-6201](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-codegen/src/artifacts.rs#L6193-L6201)

```text
render_composition_invariant_formula(self.schema, &invariant.kind)
```

  The executable model emitter uses the typed kind. Markdown uses plan.description and invariant.statement at lines 1214-1264. The separate diagnostic render_composition_module prints statement at render.rs:241, but xtask's checked-in model path calls render_composition_semantic_model, not that diagnostic renderer.

- [xtask/src/machines.rs:351-404](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/src/machines.rs#L351-L404)

```text
let composition_model = render_composition_semantic_model(&composition.schema)
```

  This is the actual model-generation path; contract/mapping are generated separately from schema plus coverage. schedule_bundle has driver: None, so these string edits do not alter a generated production driver.

- Read-only reproduction:

```text
python3 - <<'PY'
import subprocess
base='56208b9e6cee078f52c43af8f6b36660bc93eeb4'
tracked=subprocess.check_output(['git','ls-files','-z']).decode().split('\0')
def semantic_path(p):
    return bool(p) and (p.startswith('meerkat-machine-schema/src/catalog/dsl/') or (p.startswith('specs/') and (p.endswith('.tla') or p.endswith('.cfg'))) or '/src/generated/' in p or p.startswith('meerkat-machine-codegen/') or p=='xtask/src/machines.rs' or p.startswith('meerkat-runtime/src/') or p in ['meerkat-schedule/src/store.rs','meerkat-schedule/src/service.rs','meerkat-store/src/schedule_sqlite_store.rs','meerkat-workgraph/src/machine.rs','Makefile','xtask/tests/machine_verify_all_tlc_test.sh','specs/machines/validate.sh','.cargo/config.toml'])
paths=[p for p in tracked if semantic_path(p)]
changed=subprocess.check_output(['git','diff','--name-only',base,'--',*paths]).decode().strip()
assert not changed, changed
print('PASS:',len(paths),'tracked semantic/supporting files unchanged')
print('TLA/config files:',sum(p.endswith(('.tla','.cfg')) for p in paths))
print('Catalog DSL files:',sum(p.startswith('meerkat-machine-schema/src/catalog/dsl/') for p in paths))
print('Generated Rust files:',sum('/src/generated/' in p and p.endswith('.rs') for p in paths))
PY
```

  PASS: 314 tracked semantic/supporting files unchanged, including 99 TLA/config files, 18 catalog DSL files, and 75 generated Rust files. The selected renderers, runtime sources, supporting stores/adapters, and validation commands are unchanged.

  Together with the exact eight-literal source check, this establishes that the documentary correction did not change canonical predicates, guards, transitions, generated kernels, formal models, witnesses/configuration, or runtime realization.

## A18-004: Validation READMEs do not distinguish the bounded lane from the expensive full sweep

**Candidate severity:** medium. **Independent verdict:** confirm.

Both READMEs list these commands together without explaining that they no longer select the same validation lane. A reader can accidentally launch the hours-long full composition sweep, or incorrectly describe a bounded Make run as the full sweep. The machine README mentions only build-directory isolation as a reason to prefer Make, omitting the more important coverage/cost distinction. The raw commands are valid; the defect is the missing distinction, not the mere use of Cargo.

### Baseline documentation evidence

- [specs/machines/README.md:41-43](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/README.md?plain=1#L41-L43)

```text
- `make machine-verify`
- `cargo xtask machine-verify --all`
- `./specs/machines/validate.sh`
```

- [specs/compositions/README.md:52-55](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/compositions/README.md?plain=1#L52-L55)

```text
- `make machine-verify`
- `cargo xtask machine-codegen --all`
- `cargo xtask machine-check-drift --all`
- `cargo xtask machine-verify --all`
```

### Implementation proof

- [Makefile:431-439](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Makefile#L431-L439)

```text
./xtask/tests/machine_verify_all_tlc_test.sh $(XTASK_BIN)
```

  make machine-verify invokes the canonical budgeted wrapper. The adjacent authoritative target comments explicitly distinguish its composition skips and bounded adaptive witness from a bare --all invocation.

- [Makefile:441-445](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Makefile#L441-L445)

```text
# The full, budget-unbounded sweep — includes the mob-seam composition
# ci.cfg state space (hours). On-demand only; use the TLC cluster for it.
```

  The repository now exposes machine-verify-full for the unbounded lane and explicitly warns that its state-space sweep takes hours.

- [xtask/tests/machine_verify_all_tlc_test.sh:75-81](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/xtask/tests/machine_verify_all_tlc_test.sh#L75-L81)

```text
tlc -workers "${tlc_workers}" -config "${adaptive_witness}" "${adaptive_model}"
```

  The bounded wrapper still proves the adaptive layer-terminal witness; it is not equivalent to omitting all adaptive verification.

- [xtask/tests/machine_verify_all_tlc_test.sh:98-100](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/xtask/tests/machine_verify_all_tlc_test.sh#L98-L100)

```text
exec "${xtask_bin}" machine-verify --all --skip-cargo-tests \
  --skip-tlc-composition meerkat_mob_seam \
  --skip-tlc-composition adaptive_mob_bundle
```

  These are actual coverage and execution-cost differences between make machine-verify and an unqualified machine-verify --all.

- [specs/machines/validate.sh:8-10](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/validate.sh#L8-L10)

```text
exec "$CARGO" xtask machine-verify --all "$@"
```

  The README's validate.sh alternative invokes the full sweep as well; it does not inherit the Make lane's skips.

### Independent challenge

Confirmed as an actionable omission about differing validation coverage and cost, not invalid commands or an explicit false assertion that they are equivalent. The READMEs present unqualified validation lists; the machine README recommends Make only for target-directory isolation. The current Make lane runs an adaptive witness and then skips two full composition sweeps, whereas the unqualified xtask/script/full-Make alternatives do not. 'Full' means the default Ci-profile catalog sweep without those exclusions, not the Deep profile or a mathematically unbounded state space. I verified command routing and defaults statically; the hours-long estimate is the Makefile's documented warning, not a measured result from this review.

- [specs/machines/README.md:37-48](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/README.md?plain=1#L37-L48)

```text
- `cargo xtask machine-verify --all`
```

  The list places Make, bare xtask, and validate.sh together without coverage/cost labels. The following paragraph identifies build isolation, not the different TLC selection, as the reason to prefer Make.

- [specs/compositions/README.md:48-55](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/compositions/README.md?plain=1#L48-L55)

```text
- `make machine-verify`
```

  This validation list also includes the unqualified xtask --all form without distinguishing the two lanes or mentioning the full-Make target.

- [Makefile:431-445](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/Makefile#L431-L445)

```text
./xtask/tests/machine_verify_all_tlc_test.sh $(XTASK_BIN)
```

  machine-verify uses the bounded wrapper, while machine-verify-full directly calls $(XTASK_BIN) machine-verify --all. The adjacent comments explicitly distinguish the budgeted skips from an hours-scale full ci.cfg sweep.

- [xtask/tests/machine_verify_all_tlc_test.sh:75-100](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/xtask/tests/machine_verify_all_tlc_test.sh#L75-L100)

```text
exec "${xtask_bin}" machine-verify --all --skip-cargo-tests \
  --skip-tlc-composition meerkat_mob_seam \
  --skip-tlc-composition adaptive_mob_bundle
```

  The wrapper first runs witness-layer_terminal_feedback.cfg, then passes the two precise composition exclusions and skips Cargo-backed post-checks. It does not omit every check of the adaptive composition: drift/structural checks remain, and the witness is explicitly executed.

- [specs/machines/validate.sh:8-10](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/machines/validate.sh#L8-L10)

```text
exec "$CARGO" xtask machine-verify --all "$@"
```

  Without additional caller arguments, this script invokes the full sweep through scripts/repo-cargo, not the bounded wrapper. .cargo/config.toml:19 maps xtask to run -p xtask --features machine-authority --.

- [xtask/src/machines.rs:46-65](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/xtask/src/machines.rs#L46-L65)

```text
#[arg(long, value_enum, default_value_t = VerifyProfile::Ci)]
```

  Bare --all defaults to Ci, with empty composition exclusions and no skip flags. This prevents the fix from accidentally describing the full lane as Deep or unbounded formal verification.

- [xtask/tests/machine_verify_all_tlc_test.sh:23-41](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/xtask/tests/machine_verify_all_tlc_test.sh#L23-L41)

```text
MACHINE_VERIFY_TLC_DRIFT_ONLY=1
```

  The wrapper fails if tlc is absent unless the caller explicitly opts into a labeled drift-only fallback. xtask/src/machines.rs:4102-4103 also rejects absent TLC. Documentation should state the TLC prerequisite and must not call the drift-only exception TLC verification.

**Required correction:** Edit the Validation sections of specs/machines/README.md and specs/compositions/README.md. Clearly identify make machine-verify as the normal budgeted TLC lane: full meerkat_mob_seam and adaptive_mob_bundle composition sweeps are excluded, drift/structural checks remain, and the bounded adaptive layer-terminal witness runs. Document make machine-verify-full as the expensive on-demand default-Ci-profile full catalog sweep; label unqualified xtask machine-verify --all and, in the machine README, validate.sh as full-sweep alternatives unless callers supply overriding flags. Do not call full verification Deep or mathematically unbounded. Separate generation and drift commands from TLC verification; state that tlc must be on PATH, and if mentioning MACHINE_VERIFY_TLC_DRIFT_ONLY=1, explicitly label it weaker drift-only checking. No Makefile, script, generator, model, or product behavior changes are needed.

### Correction

Changed only the Validation sections. Generation and drift are separated from TLC. Both READMEs identify the normal budgeted Make lane, its two full-composition exclusions, retained drift/structural checks, bounded adaptive layer-terminal witness, and skipped Cargo-backed post-checks. Both label machine-verify-full and unqualified wrapped xtask --all as the expensive default-Ci full sweep, not Deep or unbounded formal verification; the machine README also labels validate.sh as a full-sweep alternative unless flags override it. Both require tlc on PATH. No drift-only fallback is presented as TLC verification.

- [specs/machines/README.md:44-56](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/README.md?plain=1#L44-L56)

```text
`make machine-verify` is the normal budgeted TLC lane.
```

- [specs/compositions/README.md:57-69](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/README.md?plain=1#L57-L69)

```text
hours; "full" does not select the `Deep` profile or an unbounded state space.
```

- [Makefile:431-445](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/Makefile#L431-L445)

```text
./xtask/tests/machine_verify_all_tlc_test.sh $(XTASK_BIN)
```

  The normal Make target routes through the budgeted wrapper; machine-verify-full directly invokes machine-verify --all.

- [xtask/tests/machine_verify_all_tlc_test.sh:75-100](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/tests/machine_verify_all_tlc_test.sh#L75-L100)

```text
exec "${xtask_bin}" machine-verify --all --skip-cargo-tests
```

  The wrapper first runs witness-layer_terminal_feedback.cfg, then excludes meerkat_mob_seam and adaptive_mob_bundle full TLC sweeps.

- [xtask/src/machines.rs:63-65](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/src/machines.rs#L63-L65)

```text
#[arg(long, value_enum, default_value_t = VerifyProfile::Ci)]
```

  Unqualified full verification defaults to the Ci profile, not Deep.

- [xtask/src/machines.rs:410-442](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/src/machines.rs#L410-L442)

```text
ensure_composition_ci_structural_invariants(
```

  Drift is checked at entry and skipped TLC compositions still undergo the structural-invariant contract check.

- [specs/machines/validate.sh:8-10](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/validate.sh#L8-L10)

```text
exec "$CARGO" xtask machine-verify --all "$@"
```

  The script uses the full invocation and forwards caller overrides rather than selecting the budgeted wrapper.

- [xtask/tests/machine_verify_all_tlc_test.sh:23-41](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/tests/machine_verify_all_tlc_test.sh#L23-L41)

```text
if ! command -v tlc >/dev/null 2>&1; then
```

  The TLC lane fails without tlc unless the caller explicitly chooses a separately labeled weaker drift-only fallback.

### Final independent review

**PASS:** Both Validation sections now separate generation/drift from TLC and accurately distinguish the normal budgeted Make wrapper from the expensive full catalog sweep. They name both skipped full composition sweeps, retain drift/structural checks, identify the adaptive layer-terminal witness and skipped Cargo post-checks, require tlc on PATH, and distinguish default Ci from Deep or unbounded state-space claims. Bare wrapped xtask --all and the machine README's validate.sh alternative are correctly identified as full-sweep defaults unless caller flags override them. Source routing and defaults corroborate every new distinction; the hours estimate is documented Makefile guidance, not a timing measurement by this reviewer. No command, model, script, or historical introductory prose changed.

- [specs/machines/README.md:39-58](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/README.md?plain=1#L39-L58)

```text
- `make machine-verify` is the normal budgeted TLC lane. It retains drift and
  structural checks, runs the bounded adaptive layer-terminal witness
```

  The full updated section separates codegen/drift, states the TLC prerequisite, names the two exclusions and witness path, labels full/xtask/validate.sh defaults, and retains the per-machine command.

- [specs/compositions/README.md:50-69](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/README.md?plain=1#L50-L69)

```text
- `make machine-verify-full` is the expensive, on-demand full catalog sweep with
  the default `Ci` (`ci.cfg`) profile and no composition exclusions.
```

  The composition README makes the same accurate distinction and explicitly says full is not Deep or an unbounded state space.

- [Makefile:431-445](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/Makefile#L431-L445)

```text
./xtask/tests/machine_verify_all_tlc_test.sh $(XTASK_BIN)
```

  machine-verify invokes the budgeted wrapper; the separate machine-verify-full target invokes $(XTASK_BIN) machine-verify --all. Adjacent comments describe the expensive ci.cfg sweep and its hours-scale budget.

- [xtask/tests/machine_verify_all_tlc_test.sh:75-100](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/tests/machine_verify_all_tlc_test.sh#L75-L100)

```text
exec "${xtask_bin}" machine-verify --all --skip-cargo-tests \
  --skip-tlc-composition meerkat_mob_seam \
  --skip-tlc-composition adaptive_mob_bundle
```

  The wrapper runs witness-layer_terminal_feedback.cfg before these exact exclusions, rather than dropping all adaptive verification.

- [xtask/src/machines.rs:415-449](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/src/machines.rs#L415-L449)

```text
ensure_no_drift(root, selection)?;
```

  Drift runs before TLC selection; excluded compositions still pass ensure_composition_ci_structural_invariants before the loop continues.

- [xtask/src/machines.rs:46-65](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/src/machines.rs#L46-L65)

```text
#[arg(long, value_enum, default_value_t = VerifyProfile::Ci)]
```

  The unqualified command defaults to Ci with no requested composition exclusions or skip flags. The profile mapping at 4077-4080 selects ci.cfg versus deep.cfg.

- [specs/machines/validate.sh:8-10](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/machines/validate.sh#L8-L10)

```text
exec "$CARGO" xtask machine-verify --all "$@"
```

  The script defaults CARGO to ./scripts/repo-cargo and forwards caller arguments; it does not route through the budgeted Make wrapper.

- [xtask/tests/machine_verify_all_tlc_test.sh:23-41](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/xtask/tests/machine_verify_all_tlc_test.sh#L23-L41)

```text
error: tlc not on PATH but this lane advertises TLC-backed verification.
```

  Missing TLC is a failure unless an explicit, labeled drift-only escape is selected. The README does not misrepresent that weaker escape as TLC verification; bare xtask independently refuses absent TLC at machines.rs:4102-4103.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import subprocess, os
base='56208b9e6cee078f52c43af8f6b36660bc93eeb4'
for f in ['specs/machines/README.md','specs/compositions/README.md']:
    before=subprocess.check_output(['git','show',base+':'+f]).decode()
    after=Path(f).read_text()
    assert before.split('Validation:',1)[0]==after.split('Validation:',1)[0]
    if 'When the workspace is busy' in before:
        assert before.split('When the workspace is busy',1)[1]==after.split('When the workspace is busy',1)[1]
    print(f+': only Validation section changed')
for f in ['scripts/repo-cargo','specs/machines/validate.sh','xtask/tests/machine_verify_all_tlc_test.sh','specs/compositions/adaptive_mob_bundle/witness-layer_terminal_feedback.cfg']:
    assert Path(f).is_file()
    if f.endswith('.sh') or f=='scripts/repo-cargo': assert os.access(f,os.X_OK)
print('cited script/witness paths exist; scripts executable')
PY
```

  Both READMEs change only their Validation sections. All cited wrapper/script/witness paths exist, and the three executable scripts are executable.

  The historical introductory material and trailing build-isolation guidance are preserved; no broken new repository path or script invocation was introduced.

## A18-005: The handwritten Meerkat/Mob seam anchor points to the removed flat runtime module

**Candidate severity:** low. **Independent verdict:** confirm.

Following the primary code anchor fails because MeerkatMachine was split into a directory module. The generated coverage block already points to the correct location, but generation intentionally preserves this handwritten prefix, so ordinary regeneration cannot fix it. The historical cutover explanation itself need not be rewritten.

### Baseline documentation evidence

- [specs/compositions/meerkat_mob_seam/mapping.md:8-9](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/compositions/meerkat_mob_seam/mapping.md?plain=1#L8-L9)

```text
- `meerkat-runtime/src/meerkat_machine.rs` — Meerkat-side runtime/session
  authority
```

### Implementation proof

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
for p in ['meerkat-runtime/src/meerkat_machine.rs','meerkat-runtime/src/meerkat_machine/mod.rs']:
 print(f'{p}: is_file={Path(p).is_file()}')
PY
```

  meerkat-runtime/src/meerkat_machine.rs: is_file=False
  meerkat-runtime/src/meerkat_machine/mod.rs: is_file=True

  The advertised source path does not exist, while the directory-module replacement does. The other three handwritten Rust anchors were also checked and exist.

- [meerkat-runtime/src/lib.rs:71-71](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/lib.rs#L71)

```text
pub mod meerkat_machine;
```

  This is the runtime crate's actual module declaration; with the flat file absent, its implementation is the existing meerkat_machine/mod.rs.

- [meerkat-runtime/src/meerkat_machine/mod.rs:1-6](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/mod.rs#L1-L6)

```text
//! MeerkatMachine — session-scoped execution kernel.
```

  The existing module is the same runtime/session authority the handwritten anchor intends to identify.

### Independent challenge

Confirmed. The cutover discussion is historically framed and should be preserved, but Primary code anchors is an unqualified source-navigation section, not a quotation of a historical file tree. Its runtime path does not exist. The current Rust module declaration resolves to the existing directory module, whose API is the intended Meerkat-side runtime authority. The generated coverage block already names that file, but the merge algorithm intentionally preserves this handwritten prefix, so regeneration alone would not repair it.

- [specs/compositions/meerkat_mob_seam/mapping.md:6-14](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/specs/compositions/meerkat_mob_seam/mapping.md?plain=1#L6-L14)

```text
`meerkat-runtime/src/meerkat_machine.rs`
```

  The broken path is in the handwritten Primary code anchors section, above GENERATED_COVERAGE_START, not in generated coverage.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
for name in ['meerkat-runtime/src/meerkat_machine.rs', 'meerkat-runtime/src/meerkat_machine/mod.rs']:
    print(f'{name}: is_file={Path(name).is_file()}')
PY
```

  meerkat-runtime/src/meerkat_machine.rs: is_file=False
  meerkat-runtime/src/meerkat_machine/mod.rs: is_file=True

  Independently executed read-only path check. The other three handwritten module anchors were also checked and exist.

- [meerkat-runtime/src/lib.rs:70-72](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/lib.rs#L70-L72)

```text
pub mod meerkat_machine;
```

  The crate exports this module; with no flat .rs file, Rust resolves it to the existing meerkat_machine/mod.rs directory entry point.

- [meerkat-runtime/src/meerkat_machine/mod.rs:1-17](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-runtime/src/meerkat_machine/mod.rs#L1-L17)

```text
MeerkatMachine — session-scoped execution kernel.
```

  The replacement is the intended session/runtime module, not an unrelated similarly named implementation.

- [meerkat-machine-codegen/src/render.rs:1455-1471](https://github.com/lukacf/meerkat/blob/56208b9e6cee078f52c43af8f6b36660bc93eeb4/meerkat-machine-codegen/src/render.rs#L1455-L1471)

```text
let before = existing[..start].trim_end();
```

  merge_mapping_document preserves the pre-marker text and replaces only the generated block. The fix must edit the handwritten anchor itself; no catalog or generator change is necessary.

**Required correction:** Replace only meerkat-runtime/src/meerkat_machine.rs with meerkat-runtime/src/meerkat_machine/mod.rs in the handwritten Primary code anchors entry of specs/compositions/meerkat_mob_seam/mapping.md. Preserve the historical cutover prose and the generated coverage block. This is a documentary path correction; no source-generator edit, regeneration, runtime change, or formal change is required.

### Correction

Replaced only the handwritten Primary code anchors path with meerkat-runtime/src/meerkat_machine/mod.rs. Historical cutover prose and the entire generated coverage block are byte-identical to HEAD. All four handwritten Rust anchor targets exist. Regeneration is not needed for this ID.

- [specs/compositions/meerkat_mob_seam/mapping.md:8-9](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/meerkat_mob_seam/mapping.md?plain=1#L8-L9)

```text
`meerkat-runtime/src/meerkat_machine/mod.rs`
```

- [meerkat-runtime/src/lib.rs:70-72](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/lib.rs#L70-L72)

```text
pub mod meerkat_machine;
```

- [meerkat-runtime/src/meerkat_machine/mod.rs:1-6](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/meerkat_machine/mod.rs#L1-L6)

```text
//! MeerkatMachine — session-scoped execution kernel.
```

- [meerkat-machine-codegen/src/render.rs:1455-1471](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-codegen/src/render.rs#L1455-L1471)

```text
let before = existing[..start].trim_end();
```

  Canonical mapping merge preserves the handwritten prefix; direct repair of this anchor was required.

### Final independent review

**PASS:** The handwritten primary runtime anchor now names the real directory module. The complete file differs from baseline only by this single path substitution; the historical cutover explanation and entire generated coverage block are byte-identical. All four handwritten Rust anchors resolve to existing files, the old flat runtime path is absent, and Rust's exported module resolves to the replacement directory entry point. This closes the exact path-navigation defect without changing its historical context, catalog, or generator.

- [specs/compositions/meerkat_mob_seam/mapping.md:6-14](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/specs/compositions/meerkat_mob_seam/mapping.md?plain=1#L6-L14)

```text
- `meerkat-runtime/src/meerkat_machine/mod.rs` — Meerkat-side runtime/session
  authority
```

  The corrected path is in the handwritten Primary code anchors section, above the generated coverage marker.

- [meerkat-runtime/src/lib.rs:71-71](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/lib.rs#L71)

```text
pub mod meerkat_machine;
```

  This is the crate's actual exported module declaration.

- [meerkat-runtime/src/meerkat_machine/mod.rs:1-7](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-runtime/src/meerkat_machine/mod.rs#L1-L7)

```text
//! MeerkatMachine — session-scoped execution kernel.
```

  The replacement is the intended runtime/session authority, not an unrelated similarly named file.

- [meerkat-machine-codegen/src/render.rs:1455-1469](https://github.com/lukacf/meerkat/blob/a01a53cda5e430d688e57cdd0ca7b685d0b520f6/meerkat-machine-codegen/src/render.rs#L1455-L1469)

```text
let before = existing[..start].trim_end();
```

  The generator preserves the pre-marker handwritten prefix, so the direct one-path correction is appropriate and remains intact after generation.

- Read-only reproduction:

```text
python3 - <<'PY'
from pathlib import Path
import re, subprocess
f='specs/compositions/meerkat_mob_seam/mapping.md'
old=subprocess.check_output(['git','show','56208b9e6cee078f52c43af8f6b36660bc93eeb4:'+f]).decode()
new=Path(f).read_text()
assert old.replace('`meerkat-runtime/src/meerkat_machine.rs`','`meerkat-runtime/src/meerkat_machine/mod.rs`')==new
marker='<!-- GENERATED_COVERAGE_START -->'
assert old.split(marker,1)[1]==new.split(marker,1)[1]
anchors=re.findall(r'^- `([^`]+\.rs)`',new.split(marker,1)[0],re.M)
assert len(anchors)==4
for p in anchors:
    assert Path(p).is_file()
    print(p+': exists')
assert not Path('meerkat-runtime/src/meerkat_machine.rs').exists()
print('PASS: only intended path substitution; historical prose and generated coverage unchanged')
PY
```

  All four anchors exist: meerkat-runtime/src/meerkat_machine/mod.rs, meerkat-mob/src/runtime/handle.rs, meerkat-mob/src/runtime/actor.rs, and meerkat-mob/src/runtime/session_service.rs. The old flat path does not exist. PASS: only intended path substitution; historical prose and generated coverage unchanged.

  The exact whole-file comparison proves the required preservation, including the generated block after the parent's completed generation.

## Lane limitations

- **Audit:** This is the first-wave audit artifact, not independent adjudication or implementation. Candidate findings require the separate challenge cohort. No documentation or product source was changed.
- **Audit:** No Cargo, Rust build, TLC model checker, installation, or nested agent was run. No usable checkout-local or exact-worktree cached xtask binary was found, so the official drift/verification commands were not executed. The repository's authoritative renderer and orchestration source were inspected instead.
- **Audit:** Mechanical checks were read-only Python programs operating in memory. They tokenized balanced Rust declarations, resolved the three macro invocation owners, reproduced per_phase expansion and generator phase-guard extraction, and compared the Markdown's rendered semantic sections. They are source-level projection checks, not Rust compilation, proof that every arbitrary guard/update expression is correct, or a substitute for behavioral/model-checking suites.
- **Audit:** Full contract comparison was semantic-section based rather than byte-for-byte renderer execution: from-phase lists were compared as sets; input signatures were compared across the combined public/internal alphabet with independent visibility checks. All eleven complete command-plan blocks and all twenty-four generated mapping blocks were reconstructed and compared, normalizing trailing whitespace. Composition headings, route/driver/plan/invariant content and coverage were checked against their schema owners.
- **Audit:** All handwritten text outside generated mapping markers, both READMEs, all coverage prose, and the nine compact composition contracts were read. Large generated machine transition prose was exhaustively compared mechanically instead of impractical line-by-line rereading. No independent handwritten state/transition table or diagram was present in the assigned files.
- **Audit:** Cross-owner reference checks validated producer effects and obligation fields, realizing actors, declared feedback targets, invariant links and adaptive instance rebinding, using alphabets already verified against the source DSL. Runtime-internal feedback was explicitly included; its absence from the public-only input subset was not treated as a defect. These checks do not assert that every production handoff has been exercised end to end.
- **Audit:** Historical 0.6/cutover statements, explicit precursor anchors, intentional unclaimed coverage, schema-local Rust owner self bindings, per-phase shorthand, scoped noncanonical helper authorities, and generated/shell projection mechanics were not reported as doctrine or freshness errors merely because newer implementation detail exists.
- **Audit:** Regression test bodies cited in findings were inspected but not executed. Existing generated companion TLA files were read only to verify README representation claims; their state spaces and liveness properties were not checked.
- **Challenge:** Independent challenge of exactly the five A18 candidates, not a second claim to have audited every line of all generated specifications. HEAD was 56208b9e6cee078f52c43af8f6b36660bc93eeb4 and the repository was clean at inspection; all 50 manifest file hashes matched their recorded values.
- **Challenge:** No builds, Cargo/TLC tests, generators, installations, commits, repository mutations, or nested agents were executed. Source and existing regression-test bodies were inspected; test success and model-checking success are not claimed.
- **Challenge:** Generator dependency and documentary-only conclusions are based on the actual source call graph and consumers. A later fixer must edit the named source literals first, use the existing regeneration mechanism, and verify that only the intended Markdown projections change and no kernel or executable TLA semantics change.
- **Challenge:** CoverageClaims and CompositionInvariantKind are semantically meaningful even though adjacent note, summary, description, and statement strings are documentary. In particular, A18-003 must retain its route-scoped invariant kind rather than strengthening behavior to match an overbroad prose replacement. A18-004's runtime-cost estimate was not benchmarked.
